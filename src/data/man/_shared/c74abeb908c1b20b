AT(1)                     BSD General Commands Manual                    AT(1)

NNAAMMEE
     aatt, bbaattcchh — queue, examine or delete jobs for later execution

SSYYNNOOPPSSIISS
     aatt [--bbmm] [--ff _f_i_l_e] [--ll [_j_o_b _._._.]] [--qq _q_u_e_u_e] --tt _t_i_m_e___a_r_g | _t_i_m_e_s_p_e_c
     aatt --cc | --rr _j_o_b _._._.
     bbaattcchh [--mm] [--ff _f_i_l_e] [--qq _q_u_e_u_e] [_t_i_m_e_s_p_e_c]

DDEESSCCRRIIPPTTIIOONN
     aatt and bbaattcchh read commands from standard input or a specified file which
     are to be executed at a later time, via the user's shell as specified by
     the SHELL environment variable.  If SHELL is not set, the shell in the
     user's password database entry is used instead.  If all else fails, sh(1)
     will be used.

     The related programs are as follows:

     aatt      Executes commands at a specified time.

     bbaattcchh   Executes commands when system load levels permit.  In other
             words, when the load average drops below 1.5, or the value speci‐
             fied in the invocation of cron(8).

     The options are as follows:

     --bb      An alias for bbaattcchh.

     --cc _j_o_b _._._.
             Prints the jobs listed on the command line to standard output.

     --ff _f_i_l_e
             Reads the job from _f_i_l_e rather than standard input.

     --ll [_j_o_b _._._.]
             Displays the queue of jobs which are currently awaiting execu‐
             tion.  If a _j_o_b argument is specified, only the specified jobs
             will be displayed.  Unless the user is the superuser, only the
             user's own jobs will be displayed.

     --mm      Send mail to the user when the job has completed, even if there
             was no output.

     --qq _q_u_e_u_e
             Uses the specified queue.  A queue designation consists of a sin‐
             gle letter.  Valid queue designations range from aa to zz and AA to
             ZZ.  The cc queue is the default for aatt and the EE queue for bbaattcchh.
             Queues with higher letters run with increased niceness.  If a job
             is submitted to a queue designated with an uppercase letter, it
             is treated as if it had been submitted to batch at that time.  If
             the user specified the --ll option and aatt is given a specific
             queue, only jobs pending in that queue will be shown.

     --rr _j_o_b _._._.
             Remove the specified job(s) from the aatt queue.

     --tt _t_i_m_e___a_r_g
             Specify the job time.  The argument should be of the form
             [[_c_c]_y_y]_m_m_d_d_H_H_M_M[._S_S] (matching touch(1)'s --tt format), where the
             parts of the argument represent the following:

                   _c_c_y_y    Year.  If yy is specified, but cc is not, a value
                           for yy between 69 and 99 results in a cc value of
                           19.  Otherwise, a cc value of 20 is used.
                   _m_m      Month: a number from 1 to 12.
                   _d_d      Day: a number from 1 to 31.
                   _H_H      Hour: a number from 0 to 23.
                   _M_M      Minute: a number from 0 to 59.
                   _S_S      Second: a number from 0 to 60 (permitting a leap
                           second), preceded by a period.  The default is 0.

     _t_i_m_e_s_p_e_c
             As well as the --tt option, aatt alternatively allows some moderately
             complex _t_i_m_e_s_p_e_c specifications.  It accepts times of the form
             _H_H_M_M or _H_H_:_M_M to run a job at a specific time of day (if that
             time is already past, the next day is assumed).  It is also pos‐
             sible to specify mmiiddnniigghhtt, nnoooonn, nnooww, or tteeaattiimmee (4pm), or have a
             time-of-day suffixed with AAMM or PPMM for running in the morning or
             the evening.  To say what day the job will be run, give a date in
             the form _m_o_n_t_h_-_n_a_m_e _d_a_y with an optional _y_e_a_r, or giving a date
             of the form _d_d_._m_m_._c_c_y_y, _d_d_._m_m_._y_y, _m_m_/_d_d_/_c_c_y_y, _m_m_/_d_d_/_y_y, _m_m_d_d_c_c_y_y,
             or _m_m_d_d_y_y.

             The year may be given as two or four digits.  If the year is
             given as two digits, it is taken to occur as soon as possible in
             the future, which may be in the next century — unless it's last
             year, in which case it's considered to be a typo.

             The specification of a date must follow the specification of the
             time of day.  A time like [nnooww] ++ _c_o_u_n_t _t_i_m_e_-_u_n_i_t_s may be given,
             where the time-units can be mmiinnuutteess, hhoouurrss, ddaayyss, wweeeekkss, mmoonntthhss,
             or yyeeaarrss (the singular forms are also accepted).  To tell aatt to
             run the job today or tomorrow, suffix the time with ttooddaayy or
             ttoommoorrrrooww.  The nneexxtt keyword may be used as an alias for ++ 11.

             For example, to run a job at 4pm three days from now, use aatt 44ppmm
             ++ 33 ddaayyss.  To run a job at 10:00am on July 31, use aatt 1100aamm JJuull
             3311.  To run a job at 1am tomorrow, use aatt 11aamm ttoommoorrrrooww.  To run a
             job at midnight in one week's time, use aatt mmiiddnniigghhtt nneexxtt wweeeekk.

     For both aatt and bbaattcchh, commands are read from standard input (or the file
     specified with the --ff option) and executed.  The working directory, the
     environment (except for the variables BASH_VERSINFO, DISPLAY, EUID,
     GROUPS, PPID, SHELLOPTS, SSH_AGENT_PID, SSH_AUTH_SOCK, TERM, TERMCAP,
     UID, and _), and the _u_m_a_s_k are retained from the time of invocation.  An
     aatt or bbaattcchh command invoked from a su(1) shell will retain the current
     user ID.  The user will be mailed standard error and standard output from
     his commands, if any.  If aatt is executed from a su(1) shell, the owner of
     the login shell will receive the mail.

     For non-root users, permission to run aatt is determined by the files
     _/_v_a_r_/_c_r_o_n_/_a_t_._a_l_l_o_w and _/_v_a_r_/_c_r_o_n_/_a_t_._d_e_n_y.  _N_o_t_e: these files must be
     readable by group crontab (if they exist).

     If the file _/_v_a_r_/_c_r_o_n_/_a_t_._a_l_l_o_w exists, only usernames mentioned in it are
     allowed to use aatt.  If _/_v_a_r_/_c_r_o_n_/_a_t_._a_l_l_o_w does not exist,
     _/_v_a_r_/_c_r_o_n_/_a_t_._d_e_n_y is checked.  Every username not mentioned in it is then
     allowed to use aatt.  If neither exists, only the superuser is allowed to
     run aatt.

     An empty _/_v_a_r_/_c_r_o_n_/_a_t_._d_e_n_y means that every user is allowed to use these
     commands.  This is the default configuration.

FFIILLEESS
     /var/cron/atjobs    directory containing job files
     /var/cron/at.allow  allow permission control
     /var/cron/at.deny   deny permission control

EEXXIITT SSTTAATTUUSS
     The aatt utility exits with one of the following values:

           0       Jobs were successfully submitted, removed, or listed.
           >0      An error occurred.

SSEEEE AALLSSOO
     atq(1), atrm(1), nice(1), sh(1), touch(1), umask(2), cron(8)

SSTTAANNDDAARRDDSS
     The aatt and bbaattcchh utilities are compliant with the  specification.

     The aatt flags [--bbcc] and the tteeaattiimmee _t_i_m_e_s_p_e_c are extensions to that speci‐
     fication.

      states that bbaattcchh is equivalent to running "aatt --qq bb --mm nnooww".  This im‐
     plementation permits a _t_i_m_e_s_p_e_c argument, as well as the ability to read
     from a file (--ff) and specify a job queue (--qq), and does not send mail to
     the user (--mm) by default.

     By default,  schedules aatt jobs in queue aa and bbaattcchh jobs in queue bb.

     The at.allow/deny mechanism is marked by  as being an X/Open System In‐
     terfaces option.

AAUUTTHHOORRSS
     aatt was mostly written by Thomas Koenig <_i_g_2_5_@_r_z_._u_n_i_-_k_a_r_l_s_r_u_h_e_._d_e>.  The
     time parsing routines are by David Parsons <_o_r_c_@_p_e_l_l_._c_h_i_._i_l_._u_s>.

BBUUGGSS
     aatt and bbaattcchh as presently implemented are not suitable when users are
     competing for resources.

BSD                            November 5, 2024                            BSD
