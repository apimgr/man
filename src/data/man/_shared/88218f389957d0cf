VR(4)                    BSD Kernel Interfaces Manual                    VR(4)

NNAAMMEE
     vvrr — VIA Rhine I/II/III 10/100 Ethernet device

SSYYNNOOPPSSIISS
     vvrr** aatt ppccii??
     aammpphhyy** aatt mmiiii??
     iiccsspphhyy** aatt mmiiii??
     ssqqpphhyy** aatt mmiiii??

DDEESSCCRRIIPPTTIIOONN
     The vvrr driver provides support for PCI Ethernet adapters and embedded
     controllers based on the VIA Technologies VT3043 Rhine I, VT86C100A Rhine
     II, VT6102 Rhine II, and VT6105/VT6105M Rhine III Fast Ethernet con‐
     troller chips, including the following:

     ••   AOpen/Acer ALN-320
     ••   D-Link DFE-520TX, DFE-530TX
     ••   Hawking Technologies PN102TX
     ••   Soekris Engineering lan1741

     The VIA Rhine chips use bus master DMA and have a software interface de‐
     signed to resemble that of the DEC 21x4x "tulip" chips.  The major dif‐
     ferences are that the receive filter in the Rhine chips is much simpler
     and is programmed through registers rather than by downloading a special
     setup frame through the transmit DMA engine, and that on older chips
     transmit and receive DMA buffers must be longword aligned.  The Rhine
     chips are meant to be interfaced with external physical layer devices via
     an MII bus.  They support both 10 and 100Mbps speeds in either full or
     half duplex.

     The vvrr driver for the VT6105M controller supports IPv4 IP/TCP/UDP trans‐
     mit/receive checksum offload and VLAN tag insertion and stripping.  The
     vvrr driver additionally supports Wake on LAN (WoL).  See arp(8) and
     ifconfig(8) for more details.

     The vvrr driver supports the following media types:

     autoselect   Enable autoselection of the media type and options.  The
                  user can manually override the autoselected mode by adding
                  media options to the appropriate hostname.if(5) file.

     10baseT      Set 10Mbps operation.  The _m_e_d_i_a_o_p_t option can also be used
                  to select either _f_u_l_l_-_d_u_p_l_e_x or _h_a_l_f_-_d_u_p_l_e_x modes.

     100baseTX    Set 100Mbps (Fast Ethernet) operation.  The _m_e_d_i_a_o_p_t option
                  can also be used to select either _f_u_l_l_-_d_u_p_l_e_x or _h_a_l_f_-_d_u_p_l_e_x
                  modes.

     The vvrr driver supports the following media options:

     full-duplex  Force full duplex operation.

     half-duplex  Force half duplex operation.

     Note that the 100baseTX media type is only available if supported by the
     adapter.

     For more information on configuring this device, see ifconfig(8).

DDIIAAGGNNOOSSTTIICCSS
     vvrr00:: ccoouullddnn''tt mmaapp mmeemmoorryy  A fatal initialization error has occurred.

     vvrr00:: ccoouullddnn''tt mmaapp iinntteerrrruupptt  A fatal initialization error has occurred.

     vvrr00:: wwaattcchhddoogg ttiimmeeoouutt  The device has stopped responding to the network,
     or there is a problem with the network connection (cable).

     vvrr00:: nnoo mmeemmoorryy ffoorr rrxx lliisstt  The driver failed to allocate an mbuf for the
     receiver ring.

     vvrr00:: nnoo mmeemmoorryy ffoorr ttxx lliisstt  The driver failed to allocate an mbuf for the
     transmitter ring when allocating a pad buffer or collapsing an mbuf chain
     into a cluster.

SSEEEE AALLSSOO
     amphy(4), arp(4), icsphy(4), ifmedia(4), intro(4), netintro(4), pci(4),
     sqphy(4), hostname.if(5), ifconfig(8)

     _T_h_e _V_I_A _T_e_c_h_n_o_l_o_g_i_e_s _V_T_8_6_C_1_0_0_A _d_a_t_a _s_h_e_e_t, https://www.viatech.com.

HHIISSTTOORRYY
     The vvrr device driver first appeared in FreeBSD 3.0.  OpenBSD support
     first appeared in OpenBSD 2.5.

AAUUTTHHOORRSS
     The vvrr driver was written by Bill Paul <_w_p_a_u_l_@_c_t_r_._c_o_l_u_m_b_i_a_._e_d_u>.

CCAAVVEEAATTSS
     The vvrr driver copies transmit mbuf chains into longword-aligned buffers
     prior to transmission in order to pacify the VT3043 and VT86C100A chips.
     If buffers are not aligned correctly, the chip will round the supplied
     buffer address and begin DMAing from the wrong location.  This buffer
     copying impairs transmit performance on slower systems but can't be
     avoided.  On faster machines (e.g., a Pentium II), the performance impact
     is much less noticeable.

BSD                            February 18, 2022                           BSD
