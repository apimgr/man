The  parameters  "mode_t  mode"  and "unsigned int value" are op‐
tional.  The named semaphore named is initialized and  opened  as
specified  by the argument and a semaphore descriptor is returned
to the calling process.  The value of is formed by the  following
values: O_CREAT         create the semaphore if it does not exist
O_EXCL          error if create and semaphore exists If is speci‐
fied,  requires  an additional two arguments.  specifies the per‐
missions for the semaphore as described in and  modified  by  the
process’  umask  value (see The semaphore is created with an ini‐
tial which must be less than or equal to If is specified and  the
semaphore exists, fails.  The check for the existence of the sem‐
aphore  and the creation of the semaphore are atomic with respect
to all processes calling with and set.  When a new  semaphore  is
created, it is given the user ID and group ID which correspond to
the effective user and group IDs of the calling process. There is
no  visible  entry  in  the file system for the created object in
this implementation.  There are additional restrictions for  non‐
macOS  platforms  only:  if  the calling process is signed with a
Team ID entitlement, then is meaning calls to or will not observe
any semaphores created by processes signed  with  different  Team
IDs.  If  a process lacks a Team ID entitlement, then and operate
on a global namespace, which is always searched  as  a  fallback.
The  returned  semaphore  descriptor  is available to the calling
process until it is closed with or until the caller exits or  ex‐
ecs.   If  a  process makes repeated calls to with the same argu‐
ment, the same descriptor is returned for each  successful  call,
unless has been called on the semaphore in the interim.  If fails
for any reason, it will return a value of and sets On success, it
returns  a  semaphore  descriptor.  The named semaphore is opened
unless: The required permissions (for reading and/or writing) are
denied for the given flags; or is specified, the object does  not
exist,  and  permission  to  create the semaphore is denied.  and
were specified and the semaphore exists.  The operation  was  in‐
terrupted  by  a  signal.   The operation is not supported; or is
specified and exceeds The process has already reached  its  limit
for  semaphores or file descriptors in use.  exceeded characters,
or Team ID of calling binary exceeded  PSEMTEAMIDLEN  characters.
Too  many  semaphores or file descriptors are open on the system.
is not set and the named semaphore does not exist.  is specified,
the file does not exist, and there is insufficient  space  avail‐
able to create the semaphore.  is specified in the POSIX Realtime
Extension (1003.1b‐1993/1003.1i‐1995).
