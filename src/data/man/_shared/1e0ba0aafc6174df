_f_g_e_t_w_c(3)                  Library Functions Manual                  _f_g_e_t_w_c(3)

NNAAMMEE
       fgetwc, getwc - read a wide character from a FILE stream

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssttddiioo..hh>>
       ##iinncclluuddee <<wwcchhaarr..hh>>

       wwiinntt__tt ffggeettwwcc((FFIILLEE **_s_t_r_e_a_m));;
       wwiinntt__tt ggeettwwcc((FFIILLEE **_s_t_r_e_a_m));;

DDEESSCCRRIIPPTTIIOONN
       The  ffggeettwwcc() function is the wide‐character equivalent of the ffggeettcc(3)
       function.  It reads a wide character from _s_t_r_e_a_m and  returns  it.   If
       the end of stream is reached, or if _f_e_r_r_o_r_(_s_t_r_e_a_m_) becomes true, it re‐
       turns WWEEOOFF.  If a wide‐character conversion error occurs, it sets _e_r_r_n_o
       to EEIILLSSEEQQ and returns WWEEOOFF.

       The  ggeettwwcc()  function  or macro functions identically to ffggeettwwcc().  It
       may be implemented as a macro, and may evaluate its argument more  than
       once.  There is no reason ever to use it.

       For nonlocking counterparts, see uunnlloocckkeedd__ssttddiioo(3).

RREETTUURRNN VVAALLUUEE
       On  success,  ffggeettwwcc() returns the next wide‐character from the stream.
       Otherwise, WWEEOOFF is returned, and _e_r_r_n_o is set to indicate the error.

EERRRROORRSS
       Apart from the usual ones, there is

       EEIILLSSEEQQ The data obtained from the input stream does not  form  a  valid
              character.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       ffggeettwwcc(), ggeettwwcc() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       C11, POSIX.1‐2008.

HHIISSTTOORRYY
       POSIX.1‐2001, C99.

NNOOTTEESS
       The behavior of ffggeettwwcc() depends on the LLCC__CCTTYYPPEE category of the
       current locale.

       In the absence of additional information passed to the ffooppeenn(3) call,
       it is reasonable to expect that ffggeettwwcc() will actually read a multibyte
       sequence from the stream and then convert it to a wide character.

SSEEEE AALLSSOO
       ffggeettwwss(3), ffppuuttwwcc(3), uunnggeettwwcc(3), uunnlloocckkeedd__ssttddiioo(3)

Linux man‐pages (unreleased)      2025‐05‐17                         _f_g_e_t_w_c(3)
