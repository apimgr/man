SMSC(4)                  BSD Kernel Interfaces Manual                  SMSC(4)

NNAAMMEE
     ssmmsscc — USB Microchip LAN9xxx Fast Ethernet driver

SSYYNNOOPPSSIISS
     To load the driver as a module at boot time, place the following line in
     loader.conf(5):

           if_smsc_load="YES"

     Alternatively, to compile this driver into the kernel, place the follow‐
     ing lines in your kernel configuration file:

           ddeevviiccee uuhhccii
           ddeevviiccee oohhccii
           ddeevviiccee uussbb
           ddeevviiccee mmiiiibbuuss
           ddeevviiccee uueetthheerr
           ddeevviiccee ssmmsscc

DDEESSCCRRIIPPTTIIOONN
     The ssmmsscc device driver provides support for USB Fast Ethernet adapters
     based on the Microchip (formerly SMSC) LAN9xxx chipsets.

     For more information on configuring this device, see ifconfig(8).

HHAARRDDWWAARREE
     The following devices are supported by the ssmmsscc driver:

     ••   LAN9500, LAN9500A, LAN9505 and LAN9505A based Ethernet adapters
     ••   LAN89530, LAN9530 and LAN9730 based Ethernet adapters
     ••   LAN951x Ethernet adapters with integrated USB hub

SSEEEE AALLSSOO
     arp(4), intro(4), miibus(4), netintro(4), usb(4), ifconfig(8)

HHIISSTTOORRYY
     The ssmmsscc device driver first appeared in FreeBSD 10.0.

AAUUTTHHOORRSS
     The ssmmsscc driver was written by Ben Gray <_b_g_r_a_y_@_F_r_e_e_B_S_D_._o_r_g>.

BSD                               May 7, 2024                              BSD
