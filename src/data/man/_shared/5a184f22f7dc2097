The  file  is  the  local source of group information.  It can be
used in conjunction with the Hesiod domain and the  maps  and  as
controlled  by  The  file  consists of newline separated records,
usually one per group, containing four  colon  separated  fields.
Each  line has the form: These fields are as follows: Name of the
group.  Group’s password.  The group’s decimal  ID.   Group  mem‐
bers.   The field is the group name used for granting file access
to users who are members of the group.  The field is  the  number
associated  with  the  group  name.   They  should both be unique
across the system (and often across a  group  of  systems)  since
they  control  file  access.   The field is an optional password.
This field is rarely used and an asterisk is normally  placed  in
it rather than leaving it blank.  The field contains the names of
users granted the privileges of The member names are separated by
commas  without spaces or newlines.  A user is automatically in a
group if that group was specified in their  entry  and  does  not
need  to  be  added to that group in the file.  Very large groups
can be accommodated over multiple lines by  specifying  the  same
group  name  in  all  of  them; other than this, each line has an
identical format to that described above.  This can be  necessary
to  avoid  the  record’s  length limit, which is currently set to
1024 characters.  Note that the limit can be queried  through  by
using       the      parameter.       For      example:      big‐
grp:*:1000:user001,user002,user003,...,user099,user100       big‐
grp:*:1000:user101,user102,user103,...   The  group with the name
has a special meaning to the command: if it exists  and  has  any
members,  only users listed in that group are allowed to to If is
specified for the database in then lookups occur from the  Hesiod
domain.   If  is specified for the database in then lookups occur
from the and map.  If is specified for the database,  and  either
or  is  specified for the database in then the file may also con‐
tain lines of the format +name:*::  which  causes  the  specified
group  to  be included from the Hesiod domain or the map (respec‐
tively).  If no group name is specified, or the plus sign appears
alone on line, all groups are included from the Hesiod domain  or
the  map.  Hesiod or compat references may appear anywhere in the
file, but the single plus sign form should be on the  last  line,
for  historical  reasons.   Only  the first group with a specific
name encountered, whether in the file  itself,  or  included  via
Hesiod  or will be used.  A file format appeared in The file for‐
mat first appeared in SunOS.  The Hesiod support  first  appeared
in The command does not change the passwords.
