The  pseudo  device allows display fonts for the wscons subsystem
to be loaded dynamically into the kernel.  The fonts  are  loaded
dynamically using the utility.  Fonts cannot be unloaded from the
kernel.
