_s_t_a_t_i_c___a_s_s_e_r_t(3)           Library Functions Manual           _s_t_a_t_i_c___a_s_s_e_r_t(3)

NNAAMMEE
       static_assert, _Static_assert - fail compilation if assertion is false

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<aasssseerrtt..hh>>

       vvooiidd ssttaattiicc__aasssseerrtt((bbooooll _c_o_n_s_t_a_n_t_‐_e_x_p_r_e_s_s_i_o_n,, ccoonnsstt cchhaarr **_m_s_g));;

       /* Since C23: */
       vvooiidd ssttaattiicc__aasssseerrtt((bbooooll _c_o_n_s_t_a_n_t_‐_e_x_p_r_e_s_s_i_o_n));;

DDEESSCCRRIIPPTTIIOONN
       This  macro is similar to aasssseerrtt(3), but it works at compile time, gen‐
       erating a compilation error (with an optional message) when  the  input
       is false (i.e., compares equal to zero).

       If the input is nonzero, no code is emitted.

       _m_s_g must be a string literal.  Since C23, this argument is optional.

       There’s  a keyword, __SSttaattiicc__aasssseerrtt(), that behaves identically, and can
       be used without including _<_a_s_s_e_r_t_._h_>.

RREETTUURRNN VVAALLUUEE
       No value is returned.

VVEERRSSIIOONNSS
       In C11, the second argument (_m_s_g) was mandatory; since C23, it  can  be
       omitted.

SSTTAANNDDAARRDDSS
       C11 and later.

EEXXAAMMPPLLEESS
       ssttaattiicc__aasssseerrtt()  can’t  be  used  in  some  places, like for example at
       global scope.  For that, a macro mmuusstt__bbee() can be written in  terms  of
       ssttaattiicc__aasssseerrtt().   The following program uses the macro to get the size
       of an array safely.

           #include <assert.h>
           #include <stdint.h>
           #include <stdio.h>
           #include <stdlib.h>
           #include <string.h>

           /*
            * This macro behaves like static_assert(), failing to
            * compile if its argument is not true.  However, it always
            * returns 0, which allows using it everywhere an expression
            * can be used.
            */
           #define must_be(e)                                        \
           (                                                         \
               0 * (int) sizeof(                                     \
                   struct {                                          \
                       static_assert(e);                             \
                       int  ISO_C_forbids_a_struct_with_no_members;  \
                   }                                                 \
               )                                                     \
           )

           #define is_same_type(a, b)  \
               __builtin_types_compatible_p(typeof(a), typeof(b))

           #define is_array(arr)       (!is_same_type((arr), &*(arr)))
           #define must_be_array(arr)  must_be(is_array(arr))

           #define sizeof_array(arr)   (sizeof(arr) + must_be_array(arr))
           #define NITEMS(arr)         (sizeof((arr)) / sizeof((arr)[0]) \
                                        + must_be_array(arr))

           int     foo[10];
           int8_t  bar[sizeof_array(foo)];

           int
           main(void)
           {
               for (size_t i = 0; i < NITEMS(foo); i++) {
                   foo[i] = i;
               }

               memcpy(bar, foo, sizeof_array(bar));

               for (size_t i = 0; i < NITEMS(bar); i++) {
                   printf("%d,", bar[i]);
               }

               exit(EXIT_SUCCESS);
           }

SSEEEE AALLSSOO
       aasssseerrtt(3)

Linux man‐pages (unreleased)      2025‐09‐20                  _s_t_a_t_i_c___a_s_s_e_r_t(3)
