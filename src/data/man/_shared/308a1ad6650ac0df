The  system call performs the operation indicated by on the sema‐
phore set indicated by A fourth argument, a is required for  cer‐
tain values of For the commands that use the argument, is defined
as follows: union semun {
        int     val;            /* value for SETVAL */
        struct   semid_ds *buf;  /* buffer for IPC_STAT & IPC_SET
*/
        u_short *array;         /* array for GETALL &  SETALL  */
};  Commands  are performed as follows: Fetch the semaphore set’s
storing it in the memory pointed to by Changes the and members of
the semaphore set’s to match those of the struct  pointed  to  by
The  calling  process’s  effective uid must match either or or it
must have superuser privileges.  Immediately  removes  the  sema‐
phore  set  from the system.  The calling process’s effective uid
must equal the semaphore set’s or or the process must have  supe‐
ruser  privileges.   Return the value of semaphore number Set the
value of semaphore number to Outstanding adjust  on  exit  values
for this semaphore in any process are cleared.  Return the pid of
the  last process to perform an operation on semaphore number Re‐
turn the number of processes waiting for semaphore  number  value
to  become  greater than its current value.  Return the number of
processes waiting for semaphore number value to become 0.   Fetch
the  value  of  all  of  the semaphores in the set into the array
pointed to by Set the values of all of the semaphores in the  set
to  the  values  in the array pointed to by Outstanding adjust on
exit values for all semaphores in this set, in  any  process  are
cleared.  The is defined as follows: struct semid_ds {
        struct   ipc_perm  sem_perm;      /* operation permission
struct */
        struct  sem *sem_base;  /* pointer to first semaphore  in
set */
        u_short sem_nsems;      /* number of sems in set */
        time_t  sem_otime;      /* last operation time */
        long     sem_pad1;        /*  SVABI/386  says I need this
here */
        time_t  sem_ctime;      /* last change time */
                                /* Times measured in  secs  since
*/
                                /* 00:00:00 GMT, Jan. 1, 1970 */
        long     sem_pad2;        /*  SVABI/386  says I need this
here */
        long    sem_pad3[4];    /* SVABI/386  says  I  need  this
here  */ }; On success, when is one of or returns the correspond‐
ing value; otherwise, 0 is returned.  On failure, ‐1 is returned,
and is set to indicate the error.  The system call will fail  if:
Permission  denied  due to mismatch between operation and mode of
semaphore set.  No semaphore set corresponds to The  argument  is
not  in  the  range  of valid semaphores for given semaphore set.
The calling process’s effective uid does not match the uid of the
semaphore set’s owner or creator.  or attempted to  set  a  sema‐
phore  outside the allowable range The include files and are nec‐
essary.  may update some semaphore elements before  returning  an
error.
