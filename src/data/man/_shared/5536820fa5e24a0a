creates  style  secure  and  insecure databases for the specified
file.  These databases are then installed into and  respectively.
The file is installed into The file must be in the correct format
(see  It is important to note that the format used in this system
is different from the historic Version 7 style format.   The  op‐
tions  are  as follows: Store data in big‐endian format (see also
Specify the size of the memory cache in  megabytes  used  by  the
hashing  library.   On  systems  with  a large user base, a small
cache size can lead to prohibitively long database  file  rebuild
times.   As  a rough guide, the memory usage of in megabytes will
be a little bit more than twice the figure  specified  here.   If
unspecified,  this  value will be calculated based on the size of
the input file up to a maximum of 8 megabytes.  Change  the  root
directory of the generated files from to Store data in little‐en‐
dian  format  (see also Use to report errors.  Create a Version 7
style password file and install it into Update the  secure  data‐
base  only.   This  is  useful when only encrypted passwords have
changed.  This option negates the effect of  any  option.   Don’t
re‐build  the  database  files, but instead modify or add entries
for the specified user only.  This option may only be  used  when
the  line  number  and  user  name  in the password file have not
changed, or when adding a new user from  the  last  line  in  the
password  file.   Upgrade  or downgrade databases to the numbered
version.  Version is the old format (up to and including with the
4 byte time fields and version is the new format with the 8  byte
time  fields  (greater  than  cannot read version databases.  All
versions above can read and write both version and version  data‐
bases.   By  default  the databases stay in the version they were
before the command was run.  Mention when a  version  change  oc‐
curs.   Print  a  warning  if the system is using old style data‐
bases.  The two databases differ in that the secure version  con‐
tains  the user’s encrypted password and the insecure version has
an asterisk The databases are used by the C library password rou‐
tines (see The current password file.  A Version 7  format  pass‐
word  file.   The  insecure  password database file.  A temporary
file.  The secure password database file.  A temporary file.  ex‐
its zero on success, non‐zero on failure.  Previous  versions  of
the  system  had a program similar to which built style databases
for the password file but depended on the calling programs to in‐
stall them.  The program was renamed in order that previous users
of the program not be surprised by the changes in  functionality.
Because of the necessity for atomic update of the password files,
uses  to  install  them.   This,  however, requires that the file
specified on the command line live on the same file system as the
directory.  There are the obvious races with multiple people run‐
ning on different password files at the same  time.   The  front‐
ends  to  and handle the locking necessary to avoid this problem.
The database files are copied when  the  option  is  used.   Real
locking  would  make this unnecessary.  Although the DB format is
endian‐transparent, the data stored in the DB is not.  Also,  the
format  doesn’t  lend  itself  to insertion or removal of records
from arbitrary locations in the password file.  This is difficult
to fix without breaking compatibility.  Using  the  option  on  a
system  where  multiple  users  share the same UID can have unex‐
pected results.
