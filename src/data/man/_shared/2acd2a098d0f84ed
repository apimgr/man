RC(8)                     BSD System Manager's Manual                    RC(8)

NNAAMMEE
     rrcc, rrcc..ffiirrssttttiimmee, rrcc..llooccaall, rrcc..sseeccuurreelleevveell — command scripts for system
     startup

DDEESSCCRRIIPPTTIIOONN
     rrcc is the command script that is invoked by init(8) when the system
     starts up.  It performs system housekeeping chores and starts up system
     daemons.  Additionally, rrcc is intricately tied to the netstart(8) script,
     which runs commands and daemons pertaining to the network.  rrcc is also
     used to execute any rc.d(8) scripts defined in rc.conf.local(8).  The
     rrcc..sseeccuurreelleevveell, rrcc..ffiirrssttttiimmee, and rrcc..llooccaall scripts hold commands which
     are pertinent only to a specific site.

     All of these startup scripts are controlled to some extent by variables
     defined in rc.conf(8), which specify which daemons and services to run.

     Before init(8) starts rrcc, it sets the process priority, umask, and re‐
     source limits according to the “daemon” login class as described in
     login.conf(5).  It then starts rrcc and attempts to execute the sequence of
     commands therein.

     The first part of rrcc runs an fsck(8) with option --pp to “preen” all disks
     of minor inconsistencies resulting from the last system shutdown and to
     check for serious inconsistencies caused by hardware or software failure.
     If this auto-check and repair succeeds, then the second part of rrcc is
     run.  However, if the file _/_f_a_s_t_b_o_o_t exists, fsck will not be invoked.
     The file is then removed so that fsck will be run on subsequent boots.

     The second part of rrcc then asks rc.conf(8) for configuration variables,
     mounts filesystems, saves dmesg(8) output to the file
     _/_v_a_r_/_r_u_n_/_d_m_e_s_g_._b_o_o_t, starts system daemons, preserves editor files,
     clears the scratch directory _/_t_m_p, uses savecore(8) to save any possible
     core image that might have been generated as a result of a system crash,
     and relinks kernel objects in a random order.

     If at any point the boot script fails, init(8) enters single-user mode,
     allowing the superuser a shell on the console.  On exiting this mode,
     init again invokes rrcc, but this time without performing the file system
     preen.

     Before rrcc starts most system daemons, netstart(8) is executed.

     rrcc..sseeccuurreelleevveell is executed by rrcc to start daemons that must be run before
     the security level changes.  Following this, rrcc then sets the security
     level to '1' if it wasn't set already by rrcc..sseeccuurreelleevveell.  See
     securelevel(7) for the effects of setting the security level.

     If rrcc..ffiirrssttttiimmee exists, it is executed once and then deleted.  Any output
     is mailed to root.

     rrcc..llooccaall is executed towards the end of rrcc (it is not the very last as
     there are a few services that must be started at the very end).  Nor‐
     mally, rrcc..llooccaall contains commands and daemons that are not part of the
     stock installation.

FFIILLEESS
     /etc/netstart          Command script for network startup.
     /etc/rc                Command scripts for system startup.
     /etc/rc.conf           System daemon configuration database.
     /etc/rc.conf.local     Site specific daemon configuration database.
     /etc/rc.d              Directory to hold rc.d(8) scripts.
     /etc/rc.d/rc.subr      Functions used by the rc.d(8) scripts.
     /etc/rc.firsttime      Commands run on the first boot after creation.
     /etc/rc.local          Site specific command scripts for system startup.
     /etc/rc.securelevel    Commands run before the security level changes.
     /etc/rc.shutdown       Commands run at system shutdown.
     /etc/examples/rc.*     Examples of site specific scripts.
     /fastboot              Tells rrcc not to run fsck(8) during the next boot.
     /var/run/dmesg.boot    Copy of dmesg(8) saved by rrcc at boot time.
     /etc/random.seed       Random seed file read by the bootloader; updated
                            at system startup and shutdown.
     /var/db/host.random    Random seed file read by rrcc and the installer on
                            upgrades; updated at system startup and shutdown.

SSEEEE AALLSSOO
     sysctl.conf(5), securelevel(7), init(8), netstart(8), rc.conf(8),
     rc.d(8), rc.shutdown(8), rcctl(8)

HHIISSTTOORRYY
     The rrcc command appeared in Version 4 AT&T UNIX.

BSD                              June 1, 2025                              BSD
