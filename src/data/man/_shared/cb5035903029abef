The  driver is the backend for the device.  It supports three The
first is which allocates a free pseudo‐terminal device, sets  its
user  ID to the calling user, it, and returns the opened file de‐
scriptors for both the master and the slave  pseudo‐terminal  de‐
vice  to  the  caller in a This struct has the following content:
struct ptmget {
        int     cfd;
        int     sfd;
        char    cn[PATH_MAX];
        char    sn[PATH_MAX]; }; where  and  contain  the  master
resp.  slave  device’s  file descriptor and and the corresponding
paths in the file system.  The device supports two more which  is
used  by which is used by The device is included with the pseudo‐
device It can be disabled by adding to the kernel  configuration.
access  device  cloning device, used to implement Unix98 ptys The
device appeared in and was ported to
