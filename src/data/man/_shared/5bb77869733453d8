Pamsummcol User Manual(1)   General Commands Manual  Pamsummcol User Manual(1)



NNAAMMEE
       pamsummcol - summarize (sum, average, etc) a Netpbm image by column


SSYYNNOOPPSSIISS
       ppaammssuummmmccooll { --ssuumm | --mmeeaann | --mmiinn | --mmaaxx } [_i_m_a_g_e_f_i_l_e]

       All  options  can  be abbreviated to their shortest unique prefix.  You
       may use two hyphens instead of one.  You may separate  an  option  name
       and its value with white space instead of an equals sign.


DDEESSCCRRIIPPTTIIOONN
       This program is part of NNeettppbbmm(1).

       ppaammssuummmmccooll  reads  a  Netpbm  image (PNM or PAM) and performs a summary
       function over all the rows in each column (sum, mean, etc.).   It  pro‐
       duces  an  image  of the same kind that the same width and depth as the
       input, and one row high.  Its sample values are the result of the  sum‐
       mary.

       ppaammssuummmmccooll performs the summary operation on each plane independently.

       ppaammssuummmmccooll  performs  the operation on the actual sample values, not on
       the light intensities represented by them in the case that the image is
       a PGM or PPM image.

       If  you  want  to  summarize by row instead of by column, run the input
       through ppaammfflliipp first (and if you want the output to be a single column
       instead of a single row, use ppaammfflliipp again).

       If  you  want  to  summarize over the entire image (getting a one-tuple
       output image), use ppaammssuummmm to get a summary row, ppaammfflliipp to  turn  that
       into a column, the ppaammssuummmm again to summarize the column.

       If you want to summarize the individual samples in an entire image, in‐
       stead of by tuple, use ppaammssuummmm.

       ppaammssuummmmccooll performs the operation on the actual sample values,  not  on
       the light intensities represented by them in the case that the image is
       a PGM or PPM image or PAM equivalent.  You can use ppnnmmggaammmmaa to  convert
       such  an image to one with samples proportional to light intensity, and
       then use ppaammssuummmmccooll on the result.

       You can achieve the same thing as ppaammssuummmmccooll --mmeeaann with ppaammssccaallee.  Just
       scale  vertically to a single row, without scaling horizontally at all.
       Use the pixel mixing method.


OOPPTTIIOONNSS
       In addition to the options common to all programs  based  on  libnetpbm
       (most notably --qquuiieett, see
        Common Options ⟨index.html#commonoptions⟩ ), ppaammssuummmmccooll recognizes the
       following command line options:

       You must specify exactly one of --ssuumm, --mmeeaann, --mmiinn, or --mmaaxx.



       --ssuumm

              This option makes the summary function addition.
                   In each column and plane of  the  output  row,  the  sample
              value is the
                   sum  of all the samples values in the same column and plane
              of the input.
                   If a result is greater than the image maxval, it is clipped
              to
                   the maxval.


       --mmeeaann

              This option makes the summary function arithmetic mean.
                   In  each  column  and  plane  of the output row, the sample
              value is the
                   mean of all the samples values in the same column and plane
              of the input.


       --mmiinn

              This option makes the summary function arithmetic minimum.
                   In  each  column  and  plane  of the output row, the sample
              value is the
                   minimum of all the samples values in the  same  column  and
              plane of
                   the input.


       --mmaaxx

              This option makes the summary function arithmetic maximum.
                   In  each  column  and  plane  of the output row, the sample
              value is
                   the maximum of all the samples values in  the  same  column
              and
                   plane of the input.




SSEEEE AALLSSOO
       ppaammssuummmm(1), ppaammfflliipp(1), ppaammffuunncc(1), ppaammaarriitthh(1), ppaammssccaallee(1), ppaamm(1),


HHIISSTTOORRYY
       ppaammssuummmmccooll was added to Netpbm in Release 10.21 (March 2004).

DDOOCCUUMMEENNTT SSOOUURRCCEE
       This  manual  page was generated by the Netpbm tool 'makeman' from HTML
       source.  The master documentation is at

              hhttttpp::////nneettppbbmm..ssoouurrcceeffoorrggee..nneett//ddoocc//ppaammssuummmmccooll..hhttmmll

netpbm documentation            25 January 2009      Pamsummcol User Manual(1)
