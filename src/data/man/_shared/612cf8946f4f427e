is a pseudo‐device driver which provides support for feeding back
PCM  data  from  consumers of an device to userland.  The raw PCM
data readable from is encoded in stereo little‐endian 16‐bit lin‐
ear PCM at 44100 Hz.  The pseudo‐device driver receives data from
and feeds the raw PCM output to is created once is  opened.   The
following  example streams an MP3 to an Apple AirTunes compatible
device: $  rtunes  ‐  <  /dev/pad0  &  $  mpg123  ‐a  /dev/audio1
mozart.mp3 Record the output of an application (in this case, au‐
dioplay):  $  ffmpeg  ‐f  s16le ‐ar 44100 ‐ac 2 ‐i /dev/pad0 out‐
put.wav $ audioplay ‐d /dev/audio1 input.wav The driver  appeared
in
