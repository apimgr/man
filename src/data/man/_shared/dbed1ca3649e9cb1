The function grafts a filesystem object onto the system file tree
at  the  point The argument describes the filesystem object to be
mounted.  The argument tells the kernel how to interpret (See be‐
low).  The contents of the filesystem  become  available  through
the  new  mount  point  Any  files in at the time of a successful
mount are swept under the carpet so to speak, and are unavailable
until the filesystem is unmounted.  The following may  be  speci‐
fied  to  suppress  default semantics which affect filesystem ac‐
cess.  The filesystem should be treated as  read‐only;  Even  the
super‐user  may  not  write on it.  Do not allow files to be exe‐
cuted from the filesystem.  Do not honor setuid or setgid bits on
files when executing them.  Do not interpret special files on the
filesystem.  Union with underlying filesystem instead of  obscur‐
ing  it.  All I/O to the filesystem should be done synchronously.
Enable data protection on the filesystem  if  the  filesystem  is
configured  for it.  The flag indicates that the mount command is
being applied to an already mounted filesystem.  This allows  the
mount  flags  to be changed without requiring that the filesystem
be unmounted and remounted.  Some filesystems may not  allow  all
flags  to be changed.  For example, most filesystems will not al‐
low a change from read‐write to read‐only.  The flag  causes  the
vfs  subsystem  to  update  its data structures pertaining to the
specified already mounted filesystem.  The argument  defines  the
type  of  the  filesystem.  is a pointer to a structure that con‐
tains the type specific arguments to mount.  The format for these
argument structures is described in  the  manual  page  for  each
filesystem.   The  function  call  is  equivalent to the function
call, except in the use of the second argument.  It takes an open
file descriptor representing  the  mount  point  instead  of  the
string  literal  containing  full  path to the mount point in the
filesystem  hierarchy.   The  function  call  disassociates   the
filesystem  from the specified mount point The argument may spec‐
ify to specify that the filesystem should be  forcibly  unmounted
even  if files are still active.  Active special devices continue
to work, but any further accesses to any other active  files  re‐
sult  in  errors  even if the filesystem is later remounted.  The
function call is equivalent to the function call, except  in  the
use of the first argument.  It takes an open file descriptor rep‐
resenting  the mount point instead of the string literal contain‐
ing full path to the mount point  in  the  filesystem  hierarchy.
The and return the value 0 if the mount was successful, otherwise
‐1  is  returned  and  the variable is set to indicate the error.
and returns the value 0 if the unmount succeeded; otherwise ‐1 is
returned and the variable is set to indicate the error.  and will
fail when one of the following occurs: The caller is not the  su‐
per‐user, and the device‐node and the mountpoint do not have ade‐
quate  ownership  and permissions.  A component of a pathname ex‐
ceeded characters, or an entire path  name  exceeded  characters.
Too  many  symbolic links were encountered in translating a path‐
name.  A component of does not exist.  A component of  is  not  a
directory,  or  a  path prefix of is not a directory.  A pathname
contains a  character  with  the  high‐order  bit  set.   Another
process  currently  holds  a  reference  to  points  outside  the
process’s allocated address space.  and may fail with one of  the
following  errors:  The caller is not the super‐user, and the was
not done by the user.  A system policy denied the  operation.   A
component  of the path is not a directory.  The pathname contains
a character with the high‐order bit set.  A component of a  path‐
name exceeded characters, or an entire path name exceeded charac‐
ters.   Too  many  symbolic links were encountered in translating
the pathname.  A component of does not exist.  The requested  di‐
rectory is not in the mount table.  A process is holding a refer‐
ence  to a file located on the filesystem.  An I/O error occurred
while writing cached filesystem information.  points outside  the
process’s  allocated address space.  Some of the error codes need
translation to more obvious messages.   and  function  calls  ap‐
peared  in  function  call first appeared in macOS version 10.13.
function call first appeared in macOS version 16.
