_i_o_p_l(2)                       System Calls Manual                      _i_o_p_l(2)

NNAAMMEE
       iopl - change I/O privilege level

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssyyss//iioo..hh>>

       [[[[ddeepprreeccaatteedd]]]] iinntt iiooppll((iinntt _l_e_v_e_l));;

DDEESSCCRRIIPPTTIIOONN
       iiooppll() changes the I/O privilege level of the calling thread, as speci‐
       fied by the two least significant bits in _l_e_v_e_l.

       The  I/O privilege level for a normal thread is 0.  Permissions are in‐
       herited from parents to children.

       This call is deprecated, is significantly slower than iiooppeerrmm(2), and is
       only provided for older X servers which require access to all 65536 I/O
       ports.  It is mostly for the i386 architecture.  On many  other  archi‐
       tectures it does not exist or will always return an error.

RREETTUURRNN VVAALLUUEE
       On  success,  zero is returned.  On error, -1 is returned, and _e_r_r_n_o is
       set to indicate the error.

EERRRROORRSS
       EEIINNVVAALL _l_e_v_e_l is greater than 3.

       EENNOOSSYYSS This call is unimplemented.

       EEPPEERRMM  The calling thread has insufficient privilege  to  call  iiooppll();
              the CCAAPP__SSYYSS__RRAAWWIIOO capability is required to raise the I/O privi‐
              lege level above its current value.

VVEERRSSIIOONNSS
       glibc2  has  a prototype both in _<_s_y_s_/_i_o_._h_> and in _<_s_y_s_/_p_e_r_m_._h_>.  Avoid
       the latter, it is available on i386 only.

SSTTAANNDDAARRDDSS
       Linux.

HHIISSTTOORRYY
       Prior to Linux 5.5 iiooppll() allowed  the  thread  to  disable  interrupts
       while  running  at  a  higher  I/O privilege level.  This will probably
       crash the system, and is not recommended.

       Prior to Linux 3.7, on some architectures (such as  i386),  permissions
       _w_e_r_e  inherited  by  the  child  produced by ffoorrkk(2) and were preserved
       across eexxeeccvvee(2).  This behavior was  inadvertently  changed  in  Linux
       3.7, and won’t be reinstated.

SSEEEE AALLSSOO
       iiooppeerrmm(2), oouuttbb(2), ccaappaabbiilliittiieess(7)

Linux man‐pages (unreleased)      2025‐05‐17                           _i_o_p_l(2)
