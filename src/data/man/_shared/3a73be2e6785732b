_g_r_e_p_c(1)                    General Commands Manual                   _g_r_e_p_c(1)

NNAAMMEE
       grepc - find declarations, definitions, and uses in source code

SSYYNNOOPPSSIISS
       ggrreeppcc [_o_p_t_i_o_n ...] _p_a_t_t_e_r_n [_f_i_l_e ...]

DDEESSCCRRIIPPTTIIOONN
       ggrreeppcc(1) searches for declarations, definitions, and/or uses of _p_a_t_t_e_r_n
       in each source _f_i_l_e.

       _p_a_t_t_e_r_n  is  a PCRE pattern.  It normally represents an identifier, and
       word boundaries are implicitly added to it in most cases.

       If no _f_i_l_e is given, this program reads standard input.  Otherwise,  it
       reads  all  files  specified, and searches under any specified directo‐
       ries, recursively, without following symbolic links.

OOPPTTIIOONNSS
       --AA _n   Print _n lines of trailing context after a match.

       --BB _n   Print _n lines of leading context before a match.

       --CC _n   Print _n lines of context surrounding a match.

       --cc     Do not print matches, and instead print a count of them.

       --hh     Suppress the prefixing of file names on output.

       --ii     Ignore case distinctions in _p_a_t_t_e_r_n.

       --ll     Suppress normal output; instead print the  name  of  each  input
              file  from  which output would normally have been printed.  Each
              file is only printed once.

       --mm _n   Stop processing after finding _n matches.

       --nn     Prefix each line of output with the 1‐based line  number  within
              its input file.

       --tt _t_y_p_e
              Restrict the search to a specific _t_y_p_e of code.  This option can
              be  passed  multiple  times to search for various types of code.
              The acceptable values depend on the driver  used  for  the  lan‐
              guage.  See ggrreeppcc__cc(1), which is the default driver.

CCAAVVEEAATTSS
   CCrraasshheess
       In  some cases, internal calls to ppccrree22ggrreepp(1) may fail after consuming
       too much resources.  To solve that, use --tt to  restrict  the  types  of
       code of your search.

   SSeeeekkaabbllee ffiilleess
       Files  specified  in  the  command line must be seekable and able to be
       opened twice.  If you need to read a non‐seekable file, redirect it  to
       the standard input.

EEXXAAMMPPLLEESS
       alx@devuan:/usr/lib/gcc$ ggrreeppcc ‐‐nn aalliiggnnooff ..;
       ./x86_64-linux-gnu/15/include/stdalign.h:33:#define alignof _Alignof

SSEEEE AALLSSOO
       ggrreeppcc__cc(1), ppccrree22ggrreepp(1), ppccrree22ssyynnttaaxx(3), ppccrree22ppaatttteerrnn(3)

Linux man‐pages (unreleased)        (date)                            _g_r_e_p_c(1)
