is used to create site‐specific device special files.  Each argu‐
ment  may  be  the word or a site‐specific argument.  By default,
there are no valid site‐specific arguments, and the argument  has
no effect; This may be changed by editing the script.  The script
is  in  Devices  are created in the current working directory; in
normal use, should be invoked with as the current working  direc‐
tory.   Supported  options for are the same as for special device
files directory script that invokes with  the  argument.   script
described  in  this  man page The command appeared in Handling of
the same command line options as and the use of as a function li‐
brary, was added in The relationship between and is  complex:  If
is  invoked  with the or argument, then it will invoke as a child
process, with options  similar  to  those  that  were  originally
passed to and with the argument.  uses shell functions defined in
This  is  done by loading using the shell command, with the vari‐
able set (to inform that it should behave as a function  library,
not as an independent program).
