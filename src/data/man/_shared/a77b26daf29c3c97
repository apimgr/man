is  the userland interface to the Topfield TF5000PVR range of DVB
recorders (nicknamed ‘Toppy’) which uses the device driver.   The
options  are  as follows: Communicate with instead of the default
Using the services of the driver, can perform the following oper‐
ations, specified by the operand:  Display  disk  size  and  free
space List the files in the specified Default is the Toppy’s root
directory.   Make the specified You must supply the full pathname
to Delete the file or directory specified by Rename the  file  or
directory specified by the operand to the destination file or di‐
rectory  specified by the operand.  Copy from the Toppy to on the
local file system.  If is omitted, the file will be  copied  into
the  current directory and will be named using the last component
of the operand.  If is then will be copied to the  standard  out‐
put.   The  following options are available for the command: Dis‐
play a progress bar.  This option is useful if you wish to resume
a previously interrupted command.  Instead of restarting from the
beginning of the file, the transfer will  resume  where  it  left
off.   Enable  mode.  This instructs the Toppy to drop everything
and concentrate on transferring the file as quickly as  possible.
You  will  be able to watch live TV, but all other functions, in‐
cluding changing channel via the remote control, will be inopera‐
tive.  Copy on the local filesystem to on the Toppy.   If  speci‐
fies  a directory on the Toppy, the last component of the operand
will be appended to the operand.  The options described  for  the
command  (above) also apply to the command.  The default Topfield
TF5000PVR instance.  The command first appeared in  and  was  in‐
spired by written by Steve Bennett and written by Peter Urbanec
