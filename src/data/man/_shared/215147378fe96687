The  interface forms the backend part of the paravirtualized dri‐
vers used by domains to offer a block device  interface,  similar
to  a  hard disk.  interfaces are backed either by a physical de‐
vice directly, or an image file mounted  through  All  interfaces
follow  the  naming convention, where represents the guest domain
identifier, and an arbitrary identifier.  This identifier is usu‐
ally associated to the device node as seen by the guest using and
numbers.  For example, identifier means major and  minor  identi‐
fied as under Linux convention.  For the guest device name speci‐
fied  in the guest configuration file does not matter, and can be
chosen arbitrarily.  A interface will appear as  a  block  device
inside  a  guest  domain.  In the XenStore, and are identified by
(virtual block device) entries.  Gives the device used as  inter‐
face  for  the given guest domain, and its size, in bytes.  Gives
the backend identifier, guest domain ID, event  channel  ID,  and
protocol  used  for block level communication.  When this message
appears in the system message buffer with error 16 the device  is
likely to be already mounted.  It must be unmounted first, as the
system  will  refuse  to open it a second time.  The driver first
appeared in The driver was written by
