The  command  symbolically  displays the contents of various net‐
work‐related data structures.  There are a number of output  for‐
mats,  depending  on  the  options for the information presented.
The first form of the command displays a list of  active  sockets
for  each protocol.  The second form presents the contents of one
of the other network data structures according to the option  se‐
lected.   Using  the  third form, with a interval specified, will
continuously display the information regarding packet traffic  on
the configured network interfaces.  The fourth form displays sta‐
tistics about the named protocol.  The fifth and sixth forms dis‐
play  per  interface statistics for the specified protocol or ad‐
dress family.  The options have the following meaning:  With  the
default  display, show the address of any protocol control blocks
associated with sockets; used for debugging.   With  the  default
display,  show the state of all sockets; normally sockets used by
server processes are not shown.  With the default  display,  show
the  current  peers.   To show only the peers listening to a spe‐
cific interface, use the option.  If the option is present,  show
the  current statistics.  With the interface display (option show
bytes in and out, instead of packets in and out.  With either in‐
terface display (option or an interval, as described below), show
the number of dropped packets.  Limit statistics or address  con‐
trol  block  reports  to those of the specified The following ad‐
dress families are recognized: for for for for for for and or for
Show information related to multicast  (group  address)  routing.
By  default,  show the IP Multicast virtual‐interface and routing
tables.  If the option is also present,  show  multicast  routing
statistics.   When used with in combination with either or output
"human‐readable" byte counts.  Show information about the  speci‐
fied  interface; used with a interval as described below.  If the
option (with the option) or the option is present,  show  per‐in‐
terface  statistics  on  the  for  the specified or respectively.
Show the state of interfaces which have been auto‐configured (in‐
terfaces statically configured into a system, but not located  at
boot time are not shown).  If the options is also present, multi‐
cast  addresses  currently in use are shown for each Ethernet in‐
terface and for each IP interface address.   Multicast  addresses
are  shown on separate lines following the interface address with
which they are associated.  If the option (with  the  option)  or
the  option  is present, show per‐interface statistics on all in‐
terfaces for the specified or  respectively.   Don’t  show  link‐
level  routes (e.g., IPv4 ARP or IPv6 neighbour cache).  With the
option, display wider fields for the IPv6 multicast routing table
and columns.  Use instead of to retrieve information and  extract
values associated with the name list from the specified core.  If
the  option  is not given but the option is given, the default is
used.  Show statistics recorded by  the  mbuf  memory  management
routines  (the network manages a private pool of memory buffers).
If the kernel option is set, extra info can be retrieved with See
also Use instead of to retrieve information and extract the  name
list  from  the  specified system.  For the default behavior when
only option is given, see the description about when is  in  Show
network  addresses  and ports as numbers (normally interprets ad‐
dresses and ports and attempts  to  display  them  symbolically).
This  option  may  be used with any of the display formats.  Dump
the contents of the protocol control block (PCB) located at  ker‐
nel  virtual address This address may be obtained using the flag.
The default protocol is TCP, but  may  be  overridden  using  the
flag.   Show  statistics  about which is either a well‐known name
for a protocol or an alias  for  it.   Some  protocol  names  and
aliases  are  listed  in the file A null response typically means
that there are no interesting numbers  to  report.   The  program
will  complain if is unknown or if there is no statistics routine
for it.  Show software interrupt queue setting/statistics for all
protocols.  Show the routing tables.  When is also present,  show
routing  statistics  instead.   Show network addresses as numbers
(as with but show ports symbolically).  Show per‐protocol statis‐
tics.  If this option is repeated, counters with a value of  zero
are  suppressed.  Show MPLS Tags for the routing tables.  If mul‐
tiple tags exists, they will be comma separated, first tag  being
the  BoS  one.  With the option, display the current value of the
watchdog timer function.  Show extra  (verbose)  detail  for  the
routing  tables or avoid truncation of long addresses.  Show net‐
work interface statistics at intervals of seconds.  Force use  of
when retrieving information.  Some features of may not be (fully)
supported  when  using This flag forces the use of the latter re‐
gardless, and emits a message if a not yet fully  supported  fea‐
ture  is used in conjunction with it.  This flag might be removed
at any time; do not rely on its presence.  The  default  display,
for  active  sockets,  shows the local and remote addresses, send
and receive queue sizes (in bytes), protocol,  and  the  internal
state   of  the  protocol.   Address  formats  are  of  the  form
‘‘host.port’’ or ‘‘network.port’’ if a socket’s address specifies
a network but no specific host address.  When known the host  and
network  addresses  are  displayed  symbolically according to the
data bases and respectively.  If a symbolic name for  an  address
is unknown, or if the option is specified, the address is printed
numerically,  according to the address family.  For more informa‐
tion regarding the Internet ‘‘dot format,’’ refer to Unspecified,
or ‘‘wildcard’’, addresses and ports appear as  ‘‘*’’.   You  can
use  the command to find out which process or processes hold ref‐
erences to a socket.  The interface display provides a  table  of
cumulative  statistics regarding packets transferred, errors, and
collisions.  The network addresses of the interface and the maxi‐
mum transmission unit (‘‘mtu’’) are also displayed.  The  routing
table  display  indicates  the available routes and their status.
Each route consists of a destination host or network and a  gate‐
way  to  use in forwarding packets.  The flags field shows a col‐
lection of information about the route stored as binary  choices.
The individual flags are discussed in more detail in the and man‐
ual pages.  Direct routes are created for each interface attached
to  the  local host; the gateway field for such entries shows the
address of the outgoing interface.  The refcnt  field  gives  the
current  number of active uses of the route.  Connection oriented
protocols normally hold on to a single route for the duration  of
a  connection while connectionless protocols obtain a route while
sending to the same destination.  The use field provides a  count
of  the  number  of packets sent using that route.  The mtu entry
shows the mtu associated with that route.  This mtu value is used
as the basis for the TCP maximum segment size.  The ’L’ flag  ap‐
pended  to  the mtu value indicates that the value is locked, and
that path mtu discovery is turned off for that  route.   A  indi‐
cates that the mtu for this route has not been set, and a default
TCP maximum segment size will be used.  The interface entry indi‐
cates  the network interface used for the route.  When is invoked
with the option and a interval argument, it  displays  a  running
count  of  statistics related to network interfaces.  An obsoles‐
cent version of this option used a numeric parameter with no  op‐
tion,  and  is  currently  supported  for backward compatibility.
This display consists of a column for the primary interface  (the
first interface found during autoconfiguration) and a column sum‐
marizing  information  for all interfaces.  The primary interface
may be replaced with another  interface  with  the  option.   The
first line of each screen of information contains a summary since
the  system  was  last rebooted.  Subsequent lines of output show
values accumulated over the preceding interval.  The first  char‐
acter  of  the flags column in the option shows the status of the
descriptor which has three different values: Idle (’I’),  Waiting
(’W’)  and  Timed  Out  (’T’).   The  second  character indicates
whether the promisc flag is set.  The third  character  indicates
the status of the immediate mode.  The fourth character indicates
whether  the  peer will have the ability to see the packets sent.
And the fifth character shows the header  complete  flag  status.
The  command  appeared  in  IPv6  support  was added by WIDE/KAME
project.  The notion of errors is ill‐defined.
