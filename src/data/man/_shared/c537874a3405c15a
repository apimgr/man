FDESCFS(5)                  BSD File Formats Manual                 FDESCFS(5)

NNAAMMEE
     ffddeessccffss — file-descriptor file system

SSYYNNOOPPSSIISS
     fdescfs /dev/fd fdescfs rw 0 0

DDEESSCCRRIIPPTTIIOONN
     The file-descriptor file system, or ffddeessccffss, provides access to the per-
     process file descriptor namespace in the global file system namespace.
     The conventional mount point is _/_d_e_v_/_f_d.

     The file system's contents appear as a list of numbered files which cor‐
     respond to the open files of the process reading the directory.  The
     files _/_d_e_v_/_f_d_/_0 through _/_d_e_v_/_f_d_/_# refer to file descriptors which can be
     accessed through the file system.

     The following mount options can be used when mounting ffddeessccffss filesystem:

     nnoodduupp     For file descriptors referencing vnodes, instead of the dup(2)
               semantic described above, implement re-opening of the refer‐
               enced vnode.  See below for more details.

     lliinnrrddllnnkk  Report the type of the ffddeessccffss vnode as VLNK instead of FreeBSD
               traditional VCHR.  For linux(4) ABI compatibility mount ffddeessccffss
               volume with the lliinnrrddllnnkk option.

     rrddllnnkk     Treat ffddeessccffss vnodes as symbolic links consistently, in partic‐
               ular, follow the resolved name for the name lookups.  This op‐
               tion is strictly stronger than the lliinnrrddllnnkk option, it changes
               not only the type returned by stat(2), but also causes the
               ffddeessccffss files to behave as symlinks.

     For ffddeessccffss mounted without the nnoodduupp mount option, if the file descrip‐
     tor is open and the mode the file is being opened with is a subset of the
     mode of the existing descriptor, the call:

           fd = open("/dev/fd/0", mode);

     and the call:

           fd = fcntl(0, F_DUPFD, 0);

     are equivalent.  Flags to the open(2) call other than O_RDONLY, O_WRONLY
     and O_RDWR are ignored.

     For ffddeessccffss mounted with the nnoodduupp option, and file descriptor referenc‐
     ing a vnode, the call:

           fd = open("/dev/fd/0", mode);

     reopens the referenced vnode with the specified _m_o_d_e.  In other words,
     the ooppeenn() call above is equivalent to

           fd = openat(0, "", O_EMPTY_PATH, mode);

     In particular, if the file descriptor was opened with the O_PATH flag,
     then either O_EMPTY_PATH or ooppeenn() over ffddeessccffss mount with nnoodduupp option
     allows one to convert it to a regularly opened file, assuming that the
     current permissions allow the requested _m_o_d_e.

     _N_o_t_e_: _/_d_e_v_/_f_d_/_0, _/_d_e_v_/_f_d_/_1 and _/_d_e_v_/_f_d_/_2 files are created by default
     when devfs alone is mounted.  ffddeessccffss creates entries for all file de‐
     scriptors opened by the process.

FFIILLEESS
     /dev/fd/#

EEXXAAMMPPLLEESS
     To mount a ffddeessccffss volume located on _/_d_e_v_/_f_d:

           mount -t fdescfs none /dev/fd

     For linux(4) ABI compatibility:

           mount -t fdescfs -o linrdlnk none /compat/linux/dev/fd

     For substitute of O_EMPTY_PATH flag use:

           mount -t fdescfs -o nodup none /dev/fdpath

SSEEEE AALLSSOO
     devfs(5), mount(8)

HHIISSTTOORRYY
     The ffddeessccffss file system first appeared in 4.4BSD.  The ffddeessccffss manual
     page first appeared in FreeBSD 2.2.

AAUUTTHHOORRSS
     The ffddeessccffss manual page was written by Mike Pritchard <_m_p_p_@_F_r_e_e_B_S_D_._o_r_g>,
     and was based on the manual page written by Jan-Simon Pendry.

BSD                              Jul 11, 2023                              BSD
