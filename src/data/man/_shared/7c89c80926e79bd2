The  driver provides support for controller and drive on the bus.
block mode Mitsumi devices raw mode Mitsumi devices The  hardware
is  difficult to probe accurately.  Historically, the probe would
accept any return values as indicating that an drive was present.
Other  devices, particularly or would then be incorrectly claimed
by the driver.  The driver now only accepts result codes known to
indicate Mitsumi‐compatible CD controllers, but may  reject  some
hardware  which  returns other result codes.  enables the  origi‐
nal promiscuous probe behaviour. Use with extreme caution.
