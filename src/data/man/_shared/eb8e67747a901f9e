DBUS-MONITOR(1)                  User Commands                 DBUS-MONITOR(1)



NNAAMMEE
       dbus-monitor - debug probe to print message bus messages

SSYYNNOOPPSSIISS
       ddbbuuss--mmoonniittoorr [--system | --session | --address _A_D_D_R_E_S_S] [--profile |
                    --monitor | --pcap | --binary] [_w_a_t_c_h _e_x_p_r_e_s_s_i_o_n_s]


DDEESSCCRRIIPPTTIIOONN
       The ddbbuuss--mmoonniittoorr command is used to monitor messages going through a
       D-Bus message bus. See hhttttpp::////wwwwww..ffrreeeeddeesskkttoopp..oorrgg//ssooffttwwaarree//ddbbuuss// for
       more information about the big picture.

       There are two well-known message buses: the systemwide message bus
       (installed on many systems as the "messagebus" service) and the
       per-user-login-session message bus (started each time a user logs in).
       The --system and --session options direct ddbbuuss--mmoonniittoorr to monitor the
       system or session buses respectively. If neither is specified,
       ddbbuuss--mmoonniittoorr monitors the session bus.

       ddbbuuss--mmoonniittoorr has two different text output modes: the 'classic'-style
       monitoring mode, and profiling mode. The profiling format is a compact
       format with a single line per message and microsecond-resolution timing
       information. The --profile and --monitor options select the profiling
       and monitoring output format respectively.

       ddbbuuss--mmoonniittoorr also has two binary output modes. The binary mode,
       selected by --binary, outputs the entire binary message stream (without
       the initial authentication handshake). The PCAP mode, selected by
       --pcap, adds a PCAP file header to the beginning of the output, and
       prepends a PCAP message header to each message; this produces a binary
       file that can be read by, for instance, Wireshark.

       If no mode is specified, ddbbuuss--mmoonniittoorr uses the monitoring output
       format.

       In order to get ddbbuuss--mmoonniittoorr to see the messages you are interested in,
       you should specify a set of watch expressions as you would expect to be
       passed to the _d_b_u_s___b_u_s___a_d_d___m_a_t_c_h function.

       The message bus configuration may keep ddbbuuss--mmoonniittoorr from seeing all
       messages, especially if you run the monitor as a non-root user.

OOPPTTIIOONNSS
       ----ssyysstteemm
           Monitor the system message bus.

       ----sseessssiioonn
           Monitor the session message bus. (This is the default.)

       ----aaddddrreessss AADDDDRREESSSS
           Monitor an arbitrary message bus given at ADDRESS.

       ----pprrooffiillee
           Use the profiling output format.

       ----mmoonniittoorr
           Use the monitoring output format. (This is the default.)

EEXXAAMMPPLLEE
       Here is an example of using dbus-monitor to watch for the gnome typing
       monitor to say things


             dbus-monitor "type='signal',sender='org.gnome.TypingMonitor',interface='org.gnome.TypingMonitor'"


AAUUTTHHOORR
       dbus-monitor was written by Philip Blundell. The profiling output mode
       was added by Olli Salli.

BBUUGGSS
       Please send bug reports to the D-Bus mailing list or bug tracker, see
       hhttttpp::////wwwwww..ffrreeeeddeesskkttoopp..oorrgg//ssooffttwwaarree//ddbbuuss//



D-Bus 1.14.10                                                  DBUS-MONITOR(1)
