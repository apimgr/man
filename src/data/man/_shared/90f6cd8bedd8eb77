The  file contains configuration entries for in a fashion similar
to Only one entry per line is permitted.  Every entry  must  have
all  fields populated.  Each field can be separated by a tab or a
space.  Comments are denoted by a at the  beginning  of  a  line.
There  are two kinds of configuration lines, and By default, con‐
figuration lines are i.e. the address specified refers to the ad‐
dresses on the local machine.  To switch to between and  configu‐
ration  lines you can specify the stanzas: and On and lines means
use the default, or wildcard match.  In addition, for lines means
use the values from the matched configuration  line.   The  first
four  fields, and are used to match the or addresses, whereas the
last 3 fields and are used to modify the filtering  action.   The
first  field denotes the as an address, mask, and port.  The syn‐
tax for the is:         [<address>|<interface>][/<mask>][:<port>]
The can be an IPv4 address in numeric format, an IPv6 address  in
numeric  format  and enclosed by square brackets, or an interface
name.  Mask modifiers are not allowed on interfaces  because  in‐
terfaces can have multiple addresses in different protocols where
the  mask  has  a different size.  The is always numeric, but the
can be either numeric or  symbolic.   The  second  field  is  the
socket  or  numeric.   The  third  field  is the or numeric.  The
fourth field is the effective user of the daemon process  report‐
ing the event, either as a username or a userid.  The rest of the
fields  control  the  behavior  of the filter.  The field, is the
name of the packet filter rule to be used.  If the starts with  a
then the default rulename is prepended to the given name.  If the
contains  a  the  remaining portion of the name is interpreted as
the mask to be applied to the  address  specified  in  the  rule,
causing  a  single  rule violation to block the entire subnet for
the configured prefix.  The field contains the number  of  failed
attempts  before  access is blocked, defaulting to meaning never,
and the last field specifies the amount of time  since  the  last
access  that  the  blocking  rule should be active, defaulting to
meaning forever.  The default unit for is seconds,  but  one  can
specify  suffixes  for  different  units, such as for minutes for
hours and for days.  Matching is done first by checking the rules
individually, in the order of the most specific to the least spe‐
cific.  If a match is found, then the rules are applied.  The and
fields can be altered by the rule that matched.  The rules can be
used for allowing specific addresses, changing the mask size, the
rule that the packet filter uses, the number of failed  attempts,
or the block duration.  Configuration file.  # Block ssh, after 3
attempts  for  6  hours  on  the  bnx0  interface [local] # loca‐
tion      type    proto   owner   name    nfail   duration
bnx0:ssh        *       *       *       *       3       6h   [re‐
mote]           #           Never          block          1.2.3.4
1.2.3.4:ssh     *       *       *       *       *       * # Never
block      the       example       IPv6       subnet       either
[2001:db8::]/32:ssh     *       *       *       *       *       *
# For addresses coming from 8.8.0.0/16 block class C networks in‐
stead # individual hosts, but keep the rest of the blocking para‐
meters                          the                         same.
8.8.0.0/16:ssh  *       *       *       /24     =       =   first
appeared in support for was implemented in
