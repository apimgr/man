FORWARD(5)                  BSD File Formats Manual                 FORWARD(5)

NNAAMMEE
     ffoorrwwaarrdd — email forwarding information file

DDEESSCCRRIIPPTTIIOONN
     Users may put a ..ffoorrwwaarrdd file in their home directory.  If this file ex‐
     ists, smtpd(8) forwards email to the destinations specified therein.

     A ..ffoorrwwaarrdd file contains a list of expansion values, as described in
     aliases(5).  Each expansion value should be on a line by itself.  Expan‐
     sion is performed under the user ID of the ..ffoorrwwaarrdd file owner.

     Permissions on the ..ffoorrwwaarrdd file are very strict and expansion is re‐
     jected if the file is group or world-writable; if the home directory is
     group writeable; or if the file is not owned by the user.

     Users should avoid editing the ..ffoorrwwaarrdd file directly, to prevent deliv‐
     ery failures from occurring if a message arrives while the file is not
     fully written.  The best option is to use a temporary file and use the
     mv(1) command to atomically overwrite the former ..ffoorrwwaarrdd.  Alterna‐
     tively, setting the sticky(8) bit on the home directory will cause the
     ..ffoorrwwaarrdd lookup to return a temporary failure, causing mails to be de‐
     ferred.

FFIILLEESS
     ~/.forward     Email forwarding information.

EEXXAAMMPPLLEESS
     The following file forwards mail to “user@example.com”, and pipes the
     same mail to “examplemda”.

           # empty lines are ignored

           user@example.com        # anything after # is ignored
           "|/path/to/examplemda"

SSEEEE AALLSSOO
     aliases(5), smtpd(8)

CCAAVVEEAATTSS
     The pipe ‘|’ and :include: mechanisms are not allowed for the root user.

BSD                            September 5, 2024                           BSD
