shows  information about which devices are known, version of mopd
suite or information about a MOP‐image.  If is given, information
about the MOP‐image is read from the header of  the  file.   Show
all  the  Ethernets  attached to the system.  Show version of the
mopd suite.  In some implementations the same interface can occur
more than once.
