The  function   returns  the  path in a caller provided buffer of
length indicated by associated with a filesystem  object  identi‐
fied  by  and  is  a  pointer  to  a structure which identifies a
filesystem to which the object belongs.  It is  obtained  by  the
value  returned  for  ATTR_CMN_FSID  in a previous call to or the
field of the structure returned by can be any one of of a  object
identifier  i.e.  ATTR_CMN_FILEID  returned  by  or  field of the
structure returned by or a link id returned in ATTR_CMNEXT_LINKID
by a previous call to Using a linkid will result in a more  accu‐
rate  path in case the filesystem object is a hard link. If a in‐
ode number is passed and the object is a hard link,  any  one  of
the  multiple  paths  to  that filesystem object may be returned.
Upon successful completion, returns the length of  the  path  in‐
cluding the null terminator. Otherwise, a value of ‐1 is returned
and  errno is set to indicate the error.  Not all volumes support
A volume can be tested for support by using to get the volume ca‐
pabilities attribute ATTR_VOL_CAPABILITIES, and then testing  the
VOL_CAP_FMT_PATH_FROM_ID  flag.   The function will fail if: Read
permissions are denied on any component of the pathname.  The un‐
derlying filesystem does not support this call.  is  larger  than
8192  An  I/O  error occurred while reading from the file system.
The calling process does not have  appropriate  privileges.   The
Filesystem  object does not exist.  restrict_buf points to memory
not valid in the callers  address  space.   restrict_buf  is  not
large enough to hold the path.  The function call appeared in ma‐
cOS version 10.13
