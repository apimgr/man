_p_e_r_s_i_s_t_e_n_t_‐_k_e_y_r_i_n_g(7)  Miscellaneous Information Manual  _p_e_r_s_i_s_t_e_n_t_‐_k_e_y_r_i_n_g(7)

NNAAMMEE
       persistent‐keyring - per‐user persistent keyring

DDEESSCCRRIIPPTTIIOONN
       The  persistent keyring is a keyring used to anchor keys on behalf of a
       user.  Each UID the kernel deals with has its  own  persistent  keyring
       that  is  shared between all threads owned by that UID.  The persistent
       keyring has a name (description) of the  form  ___p_e_r_s_i_s_t_e_n_t_._<_U_I_D_>  where
       _<_U_I_D_> is the user ID of the corresponding user.

       The  persistent keyring may not be accessed directly, even by processes
       with the appropriate UID.  Instead, it must first be linked to one of a
       process’s keyrings, before  that  keyring  can  access  the  persistent
       keyring  by virtue of its possessor permits.  This linking is done with
       the kkeeyyccttll__ggeett__ppeerrssiisstteenntt(3) function.

       If a persistent keyring does not exist  when  it  is  accessed  by  the
       kkeeyyccttll__ggeett__ppeerrssiisstteenntt(3) operation, it will be automatically created.

       Each time the kkeeyyccttll__ggeett__ppeerrssiisstteenntt(3) operation is performed, the per‐
       sistent keyring’s expiration timer is reset to the value in:

           /proc/sys/kernel/keys/persistent_keyring_expiry

       Should  the  timeout be reached, the persistent keyring will be removed
       and everything it pins can then be garbage collected.  The keyring will
       then be re‐created on a subsequent call to kkeeyyccttll__ggeett__ppeerrssiisstteenntt(3).

       The persistent keyring is not directly searched by  rreeqquueesstt__kkeeyy(2);  it
       is  searched  only  if  it  is  linked into one of the keyrings that is
       searched by rreeqquueesstt__kkeeyy(2).

       The persistent keyring is independent of cclloonnee(2),  ffoorrkk(2),  vvffoorrkk(2),
       eexxeeccvvee(2),  and __eexxiitt(2).  It persists until its expiration timer trig‐
       gers, at which point it is garbage collected.  This allows the  persis‐
       tent  keyring  to  carry keys beyond the life of the kernel’s record of
       the corresponding UID (the destruction of which results in the destruc‐
       tion of the uusseerr--kkeeyyrriinngg(7) and the uusseerr--sseessssiioonn--kkeeyyrriinngg(7)).  The per‐
       sistent keyring can thus be used  to  hold  authentication  tokens  for
       processes  that  run without user interaction, such as programs started
       by ccrroonn(8).

       The persistent keyring is used to store UID‐specific objects that them‐
       selves have limited lifetimes (e.g., kerberos tokens).  If those tokens
       cease to be used (i.e., the persistent keyring is not  accessed),  then
       the  timeout  of  the persistent keyring ensures that the corresponding
       objects are automatically discarded.

   SSppeecciiaall ooppeerraattiioonnss
       The _k_e_y_u_t_i_l_s library provides the kkeeyyccttll__ggeett__ppeerrssiisstteenntt(3) function for
       manipulating persistent keyrings.  (This function is  an  interface  to
       the  kkeeyyccttll(2) KKEEYYCCTTLL__GGEETT__PPEERRSSIISSTTEENNTT operation.)  This operation allows
       the calling thread to get the persistent keyring corresponding  to  its
       own UID or, if the thread has the CCAAPP__SSEETTUUIIDD capability, the persistent
       keyring corresponding to some other UID in the same user namespace.

NNOOTTEESS
       Each  user  namespace  owns  a keyring called _._p_e_r_s_i_s_t_e_n_t___r_e_g_i_s_t_e_r that
       contains links to all of the persistent keys in that  namespace.   (The
       _._p_e_r_s_i_s_t_e_n_t___r_e_g_i_s_t_e_r  keyring  can be seen when reading the contents of
       the  _/_p_r_o_c_/_k_e_y_s  file  for  the  UID  0   in   the   namespace.)    The
       kkeeyyccttll__ggeett__ppeerrssiisstteenntt(3)  operation  looks for a key with a name of the
       form ___p_e_r_s_i_s_t_e_n_t_.UID in that keyring, creates the key if  it  does  not
       exist, and links it into the keyring.

SSEEEE AALLSSOO
       kkeeyyccttll(1), kkeeyyccttll(3), kkeeyyccttll__ggeett__ppeerrssiisstteenntt(3), kkeeyyrriinnggss(7),
       pprroocceessss--kkeeyyrriinngg(7), sseessssiioonn--kkeeyyrriinngg(7), tthhrreeaadd--kkeeyyrriinngg(7),
       uusseerr--kkeeyyrriinngg(7), uusseerr--sseessssiioonn--kkeeyyrriinngg(7)

Linux man‐pages (unreleased)      2025‐09‐21             _p_e_r_s_i_s_t_e_n_t_‐_k_e_y_r_i_n_g(7)
