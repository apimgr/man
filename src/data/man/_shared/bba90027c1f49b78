Extended  attributes  extend the basic attributes associated with
files and directories in the file system.   They  are  stored  as
name:data  pairs  associated with file system objects (files, di‐
rectories, symlinks, etc).  retrieves a list of names of extended
attributes associated with the given in the file  system.   is  a
data buffer of bytes for the names of the extended attributes as‐
sociated with The extended attribute names are simple NULL‐termi‐
nated  UTF‐8 strings and are returned in arbitrary order.  No ex‐
tra padding is provided between names in the  buffer.   The  list
will only include names of extended attributes to which the call‐
ing  process  has  access.   The function returns the size of the
list of names.  controls how the attribute list is generated:  do
not follow symbolic links.  normally lists attributes of the tar‐
get of if it is a symbolic link.  With this option, will list at‐
tributes  of  the  link itself.  do not follow any symbolic links
encountered during pathname resolution. An error is returned if a
symlink is encountered before the last  component  of  path.   If
does  not reside in the hierarchy beneath the starting directory,
an error is returned.  and will list  HFS  Plus  Compression  ex‐
tended  attribute(s)  (if present) for the file referred to by or
If is set to NULL, the function returns the size of the  list  of
extended attribute names.  This facility can be used to determine
the  size of a buffer sufficiently large to hold the names of the
attributes currently associated with is identical to except  that
it  returns  the list of extended attribute names associated with
the open file referenced by file descriptor On success, the  size
of  the extended attribute name list is returned.  If no accessi‐
ble extended attributes are associated  with  the  given  or  the
function returns zero.  On failure, ‐1 is returned and the global
variable is set as follows.  The file system does not support ex‐
tended  attributes or has the feature disabled.  (as indicated by
is too small to hold the list of names.  or refer to a file  sys‐
tem  object that does not support extended attributes.  For exam‐
ple, resource forks don’t support extended attributes.  A  compo‐
nent  of  prefix  is  not a directory.  exceeds UTF‐8 bytes, or a
component of exceeds characters, or the  entire  exceeds  charac‐
ters.   Search permission is denied for a component of or permis‐
sion is denied to read the list of  attributes  from  this  file.
Too  many  symbolic links were encountered resolving If XATTR_NO‐
FOLLOW_ANY option was passed and a symbolic link was  encountered
in  translating  the pathname.  points to an invalid address.  An
I/O error occurred.  has an unsupported bit set.  XATTR_NOFOLLOW,
XATTR_NOFOLLOW_ANY or XATTR_RESOLVE_BENEATH option was passed  in
If  XATTR_RESOLVE_BENEATH  was  passed and does not reside in the
directory hierarchy beneath the starting  directory.   and  first
appeared in Mac OS X 10.4.
