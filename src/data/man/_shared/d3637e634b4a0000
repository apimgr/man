ZOPEN(3)                 BSD Library Functions Manual                 ZOPEN(3)

NNAAMMEE
     zzooppeenn — open a gzip compressed stream

LLIIBBRRAARRYY
     Compression Library (libz, -lz)

SSYYNNOOPPSSIISS
     _F_I_L_E _*
     zzooppeenn(_c_o_n_s_t _c_h_a_r _*_p_a_t_h, _c_o_n_s_t _c_h_a_r _*_m_o_d_e);

     _F_I_L_E _*
     zzddooppeenn(_i_n_t _f_d, _c_o_n_s_t _c_h_a_r _*_m_o_d_e);

DDEESSCCRRIIPPTTIIOONN
     The zzooppeenn() function opens a gzip file whose name is the string pointed
     to by _p_a_t_h and returns a stream which can be used to access the uncom‐
     pressed contents of the file.  The zzddooppeenn() variant takes a gzip file
     referenced by the file descriptor _f_d, analogous to fdopen(3).  They are
     wrappers around zlib(3) and the standard stream I/O APIs.

     The argument _m_o_d_e has the same meaning as it does in fopen(3).

     The zzooppeenn() and zzddooppeenn() functions will associate the read, write, seek
     and close functions of zlib(3) with the returned stream.

RREETTUURRNN VVAALLUUEESS
     Upon successful completion zzooppeenn() and zzddooppeenn() return a FILE pointer.
     Otherwise, NULL is returned and the global variable _e_r_r_n_o is set to indi‐
     cate the error.

EERRRROORRSS
     In addition to the errors documented for fopen(3) and fdopen(3), the
     functions may also fail for:

     [ENOMEM]           Insufficient memory is available.

CCOOMMPPAATTIIBBIILLIITTYY
     The implementation of zzooppeenn() function first appeared in NetBSD 1.6 and
     FreeBSD 4.5.  zzddooppeenn() first appeared in FreeBSD 13.0.  These functions
     may not be portable to systems other than FreeBSD.

SSEEEE AALLSSOO
     fdopen(3), fopen(3), funopen(3), zlib(3)

BSD                            December 6, 2018                            BSD
