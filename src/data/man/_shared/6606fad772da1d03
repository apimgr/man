MD5(1)                    BSD General Commands Manual                   MD5(1)

NNAAMMEE
     mmdd55, sshhaa11, sshhaa225566, sshhaa551122 — calculate a message digest (checksum) for a
     file

SSYYNNOOPPSSIISS
     mmdd55 [--bbccppqqrrttxx] [--CC _c_h_e_c_k_l_i_s_t] [--hh _h_a_s_h_f_i_l_e] [--ss _s_t_r_i_n_g] [_f_i_l_e _._._.]
     sshhaa11 [--bbccppqqrrttxx] [--CC _c_h_e_c_k_l_i_s_t] [--hh _h_a_s_h_f_i_l_e] [--ss _s_t_r_i_n_g] [_f_i_l_e _._._.]
     sshhaa225566 [--bbccppqqrrttxx] [--CC _c_h_e_c_k_l_i_s_t] [--hh _h_a_s_h_f_i_l_e] [--ss _s_t_r_i_n_g] [_f_i_l_e _._._.]
     sshhaa551122 [--bbccppqqrrttxx] [--CC _c_h_e_c_k_l_i_s_t] [--hh _h_a_s_h_f_i_l_e] [--ss _s_t_r_i_n_g] [_f_i_l_e _._._.]

DDEESSCCRRIIPPTTIIOONN
     These utilities take as input a message of arbitrary length and produce
     as output a message digest (checksum) of the input.

     Two messages having the same message digest (a collision) have been pro‐
     duced for MD5 and for SHA-1, so their use is deprecated.

     The options for use with each command are as follows:

     --bb      Output checksums in base64 notation, not hexadecimal.

     --CC _c_h_e_c_k_l_i_s_t
             Compare the checksum of each _f_i_l_e against the checksums in the
             _c_h_e_c_k_l_i_s_t.  Any specified _f_i_l_e that is not listed in the
             _c_h_e_c_k_l_i_s_t will generate an error.

     --cc      If this option is specified, the _f_i_l_e options become checklists.
             Each checklist should contain hash results, which will be veri‐
             fied against the specified paths.  Both normal (BSD) and reverse
             (GNU) forms are supported in either hexadecimal or base64 format.
             Output consists of the digest used, the file name, and an OK,
             FAILED, or MISSING for the result of the comparison.  Entries in
             normal format can be validated using any of the supported check‐
             sums (see cksum(1)).  Entries in reverse format are validated us‐
             ing the default algorithm.  If no file is given, stdin is used.

     --hh _h_a_s_h_f_i_l_e
             Place the checksum into _h_a_s_h_f_i_l_e instead of stdout.

     --pp      Echo stdin to stdout and append the checksum to stdout.

     --qq      Only print the checksum (quiet mode) or if used in conjunction
             with the --cc flag, only print the failed cases.

     --rr      Reverse the format of the hash algorithm output, similar to the
             output format used by cksum(1) (but without the number of
             octets).  This is also the format used by the GNU cksum(1) util‐
             ity.

     --ss _s_t_r_i_n_g
             Print a checksum of the given _s_t_r_i_n_g.

     --tt      Run a built-in time trial.  Specifying --tt multiple times results
             in the number of rounds being multiplied by 10 for each addi‐
             tional flag.

     --xx      Run a built-in test script.

EEXXIITT SSTTAATTUUSS
     These utilities exit 0 on success, and >0 if an error occurs.

SSEEEE AALLSSOO
     cksum(1)

SSTTAANNDDAARRDDSS
     R. Rivest, _T_h_e _M_D_5 _M_e_s_s_a_g_e_-_D_i_g_e_s_t _A_l_g_o_r_i_t_h_m, RFC 1321, April 1992.  J.
     Burrows, _T_h_e _S_e_c_u_r_e _H_a_s_h _S_t_a_n_d_a_r_d, FIPS PUB 180-1.

     D. Eastlake and P. Jones, _U_S _S_e_c_u_r_e _H_a_s_h _A_l_g_o_r_i_t_h_m _1 _(_S_H_A_1_), RFC 3174,
     September 2001.  _S_e_c_u_r_e _H_a_s_h _S_t_a_n_d_a_r_d, FIPS PUB 180-2.

BSD                            October 31, 2023                            BSD
