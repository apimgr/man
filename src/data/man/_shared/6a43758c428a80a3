RDMA_GET_PEER_ADDR(3)    Librdmacm Programmer's Manual   RDMA_GET_PEER_ADDR(3)



NNAAMMEE
       rdma_get_peer_addr   -  Returns  the  remote  IP  address  of  a  bound
       rdma_cm_id.

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<rrddmmaa//rrddmmaa__ccmmaa..hh>>

       ssttrruucctt ssoocckkaaddddrr ** rrddmmaa__ggeett__ppeeeerr__aaddddrr ((ssttrruucctt rrddmmaa__ccmm__iidd **_i_d));;

AARRGGUUMMEENNTTSS
       id          RDMA identifier.

DDEESSCCRRIIPPTTIIOONN
       Returns the remote IP address associated with an rdma_cm_id.

RREETTUURRNN VVAALLUUEE
       Returns a pointer to the sockaddr address of the  connected  peer.   If
       the rdma_cm_id is not connected, the contents of the sockaddr structure
       will be set to all zeroes.

SSEEEE AALLSSOO
       rdma_resolve_addr(3),    rdma_get_src_port(3),    rdma_get_dst_port(3),
       rdma_get_local_addr(3)



librdmacm                         2007-05-15             RDMA_GET_PEER_ADDR(3)
