WDCTL(8)                     System Administration                    WDCTL(8)



NNAAMMEE
       wdctl - show hardware watchdog status

SSYYNNOOPPSSIISS
       wwddccttll [options] [_d_e_v_i_c_e...]

DDEESSCCRRIIPPTTIIOONN
       Show hardware watchdog status. The default device is _/_d_e_v_/_w_a_t_c_h_d_o_g. If
       more than one device is specified then the output is separated by one
       blank line.

       If the device is already used or user has no permissions to read from
       the device, then wwddccttll reads data from sysfs. In this case information
       about supported features (flags) might be missing.

       Note that the number of supported watchdog features is hardware
       specific.

OOPPTTIIOONNSS
       --ff, ----ffllaaggss _l_i_s_t
           Print only the specified flags.

       --FF, ----nnooffllaaggss
           Do not print information about flags.

       --II, ----nnooiiddeenntt
           Do not print watchdog identity information.

       --nn, ----nnoohheeaaddiinnggss
           Do not print a header line for flags table.

       --oo, ----oouuttppuutt _l_i_s_t
           Define the output columns to use in table of watchdog flags. If no
           output arrangement is specified, then a default set is used. Use
           ----hheellpp to get list of all supported columns.

       --OO, ----oonneelliinnee
           Print all wanted information on one line in key="value" output
           format.

       --pp, ----sseettpprreettiimmeeoouutt _s_e_c_o_n_d_s
           Set the watchdog pre-timeout in seconds. A watchdog pre-timeout is
           a notification generated by the watchdog before the watchdog reset
           might occur in the event the watchdog has not been serviced. This
           notification is handled by the kernel and can be configured to take
           an action using sysfs or by ----sseettpprreeggoovveerrnnoorr.

       --gg, ----sseettpprreeggoovveerrnnoorr _g_o_v_e_r_n_o_r
           Set pre-timeout governor name. For available governors see default
           wwddccttll output.

       --rr, ----rraaww
           Use the raw output format.

       --ss, ----sseettttiimmeeoouutt _s_e_c_o_n_d_s
           Set the watchdog timeout in seconds.

       --TT, ----nnoottiimmeeoouuttss
           Do not print watchdog timeouts.

       --xx, ----ffllaaggss--oonnllyy
           Same as --II --TT.

       --hh, ----hheellpp
           Display help text and exit.

       --VV, ----vveerrssiioonn
           Print version and exit.

AAUUTTHHOORRSS
       Karel Zak <kzak@redhat.com>, Lennart Poettering
       <lennart@poettering.net>

RREEPPOORRTTIINNGG BBUUGGSS
       For bug reports, use the issue tracker at
       https://github.com/util-linux/util-linux/issues.

AAVVAAIILLAABBIILLIITTYY
       The wwddccttll command is part of the util-linux package which can be
       downloaded from Linux Kernel Archive
       <https://www.kernel.org/pub/linux/utils/util-linux/>.



util-linux 2.38.1                 2022-05-11                          WDCTL(8)
