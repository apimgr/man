TIMER_SETTIME(2)            BSD System Calls Manual           TIMER_SETTIME(2)

NNAAMMEE
     ttiimmeerr__ggeettoovveerrrruunn, ttiimmeerr__ggeettttiimmee, ttiimmeerr__sseettttiimmee — per-process timers (RE‐
     ALTIME)

LLIIBBRRAARRYY
     POSIX Real-time Library (librt, -lrt)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ttiimmee..hh>>

     _i_n_t
     ttiimmeerr__ggeettoovveerrrruunn(_t_i_m_e_r___t _t_i_m_e_r_i_d);

     _i_n_t
     ttiimmeerr__ggeettttiimmee(_t_i_m_e_r___t _t_i_m_e_r_i_d, _s_t_r_u_c_t _i_t_i_m_e_r_s_p_e_c _*_v_a_l_u_e);

     _i_n_t
     ttiimmeerr__sseettttiimmee(_t_i_m_e_r___t _t_i_m_e_r_i_d, _i_n_t _f_l_a_g_s,
         _c_o_n_s_t _s_t_r_u_c_t _i_t_i_m_e_r_s_p_e_c _*_r_e_s_t_r_i_c_t _v_a_l_u_e,
         _s_t_r_u_c_t _i_t_i_m_e_r_s_p_e_c _*_r_e_s_t_r_i_c_t _o_v_a_l_u_e);

DDEESSCCRRIIPPTTIIOONN
     The ttiimmeerr__ggeettttiimmee() system call stores the amount of time until the spec‐
     ified timer, _t_i_m_e_r_i_d, expires and the reload value of the timer into the
     space pointed to by the _v_a_l_u_e argument.  The _i_t___v_a_l_u_e member of this
     structure contains the amount of time before the timer expires, or zero
     if the timer is disarmed.  This value is returned as the interval until
     timer expiration, even if the timer was armed with absolute time.  The
     _i_t___i_n_t_e_r_v_a_l member of _v_a_l_u_e contains the reload value last set by
     ttiimmeerr__sseettttiimmee().

     The ttiimmeerr__sseettttiimmee() system call sets the time until the next expiration
     of the timer specified by _t_i_m_e_r_i_d from the _i_t___v_a_l_u_e member of the _v_a_l_u_e
     argument and arms the timer if the _i_t___v_a_l_u_e member of _v_a_l_u_e is non-zero.
     If the specified timer was already armed when ttiimmeerr__sseettttiimmee() is called,
     this call resets the time until next expiration to the value specified.
     If the _i_t___v_a_l_u_e member of _v_a_l_u_e is zero, the timer is disarmed.  If the
     timer is disarmed, then pending signal is removed.

     If the flag TIMER_ABSTIME is not set in the argument _f_l_a_g_s,
     ttiimmeerr__sseettttiimmee() behaves as if the time until next expiration is set to be
     equal to the interval specified by the _i_t___v_a_l_u_e member of _v_a_l_u_e.  That
     is, the timer expires in _i_t___v_a_l_u_e nanoseconds from when the call is made.
     If the flag TIMER_ABSTIME is set in the argument _f_l_a_g_s, ttiimmeerr__sseettttiimmee()
     behaves as if the time until next expiration is set to be equal to the
     difference between the absolute time specified by the it_value member of
     value and the current value of the clock associated with _t_i_m_e_r_i_d.  That
     is, the timer expires when the clock reaches the value specified by the
     _i_t___v_a_l_u_e member of _v_a_l_u_e.  If the specified time has already passed, the
     system call succeeds and the expiration notification is made.

     The reload value of the timer is set to the value specified by the
     _i_t___i_n_t_e_r_v_a_l member of _v_a_l_u_e.  When a timer is armed with a non-zero
     _i_t___i_n_t_e_r_v_a_l, a periodic (or repetitive) timer is specified.

     Time values that are between two consecutive non-negative integer multi‐
     ples of the resolution of the specified timer are rounded up to the
     larger multiple of the resolution.  Quantization error will not cause the
     timer to expire earlier than the rounded time value.

     If the argument _o_v_a_l_u_e is not NULL, the ttiimmeerr__sseettttiimmee() system call
     stores, in the location referenced by _o_v_a_l_u_e, a value representing the
     previous amount of time before the timer would have expired, or zero if
     the timer was disarmed, together with the previous timer reload value.
     Timers do not expire before their scheduled time.

     Only a single signal is queued to the process for a given timer at any
     point in time.  When a timer for which a signal is still pending expires,
     no signal is queued, and a timer overrun will occur.  When a timer expi‐
     ration signal is accepted by a process, the ttiimmeerr__ggeettoovveerrrruunn() system
     call returns the timer expiration overrun count for the specified timer.
     The overrun count returned contains the number of extra timer expirations
     that occurred between the time the signal was generated (queued) and when
     it was accepted, up to but not including an maximum of {DELAYTIMER_MAX}.
     If the number of such extra expirations is greater than or equal to
     {DELAYTIMER_MAX}, then the overrun count is set to {DELAYTIMER_MAX}.  The
     value returned by ttiimmeerr__ggeettoovveerrrruunn() applies to the most recent expira‐
     tion signal acceptance for the timer.  If no expiration signal has been
     delivered for the timer, the return value of ttiimmeerr__ggeettoovveerrrruunn() is un‐
     specified.

RREETTUURRNN VVAALLUUEESS
     If the ttiimmeerr__ggeettoovveerrrruunn() system call succeeds, it returns the timer ex‐
     piration overrun count as explained above.  Otherwise the value -1 is re‐
     turned, and the global variable _e_r_r_n_o is set to indicate the error.

     The ttiimmeerr__ggeettttiimmee() and ttiimmeerr__sseettttiimmee() functions return the value 0 if
     successful; otherwise the value -1 is returned and the global variable
     _e_r_r_n_o is set to indicate the error.

EERRRROORRSS
     The ttiimmeerr__sseettttiimmee() system call will fail if:

     [EINVAL]           A _v_a_l_u_e structure specified a nanosecond value less
                        than zero or greater than or equal to 1000 million,
                        and the _i_t___v_a_l_u_e member of that structure did not
                        specify zero seconds and nanoseconds.

     These system calls may fail if:

     [EINVAL]           The _t_i_m_e_r_i_d argument does not correspond to an ID re‐
                        turned by ttiimmeerr__ccrreeaattee() but not yet deleted by
                        ttiimmeerr__ddeelleettee().

     The ttiimmeerr__sseettttiimmee() system call may fail if:

     [EINVAL]           The _i_t___i_n_t_e_r_v_a_l member of _v_a_l_u_e is not zero and the
                        timer was created with notification by creation of a
                        new thread (_s_i_g_e_v___s_i_g_e_v___n_o_t_i_f_y was SIGEV_THREAD) and a
                        fixed stack address has been set in the thread attri‐
                        bute pointed to by _s_i_g_e_v___n_o_t_i_f_y___a_t_t_r_i_b_u_t_e_s.

     The ttiimmeerr__ggeettttiimmee() and ttiimmeerr__sseettttiimmee() system calls may fail if:

     [EFAULT]           Any arguments point outside the allocated address
                        space or there is a memory protection fault.

SSEEEE AALLSSOO
     clock_getres(2), timer_create(2), siginfo(3)

SSTTAANNDDAARRDDSS
     The ttiimmeerr__ggeettoovveerrrruunn(), ttiimmeerr__ggeettttiimmee(), and ttiimmeerr__sseettttiimmee() system calls
     conform to IEEE Std 1003.1-2004 (“POSIX.1”).

HHIISSTTOORRYY
     Support for POSIX per-process timer first appeared in FreeBSD 7.0.

BSD                           September 11, 2000                           BSD
