receives  a  database  sent by and writes it as a local database.
By default, expects to be started from if stdin is a  socket  and
expects  to receive the dumped database over stdin otherwise.  If
the database is sent over the network, it  is  authenticated  and
encrypted.  Only connections authenticated with the principal are
accepted.  Options supported: database read from stdin print dump
to stdout not started from inetd keytab to use for authentication
create v4 type DB
