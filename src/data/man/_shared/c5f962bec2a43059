In  conjunction  with the driver enables a system running as a VM
guest to mount an exported file system by the host via virtio‐9p.
It exports a 9p end‐point of virtio‐9p  via  a  character  device
file for Each exported file system is assigned a character device
and accessible via and so on, respectively, in exporting order by
the  host.   The following command mounts the first exported file
system by the host at # mount_9p ‐cu /dev/vio9p0 /mnt/9p The dri‐
ver first appeared in The driver was written by
