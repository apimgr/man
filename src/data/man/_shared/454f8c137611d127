The  utility  writes  the  current value of a configurable system
limit or option variable to the standard  output.   The  argument
specifies  the  system  variable to be queried.  The names of the
system variables are from with the leading removed.  The argument
specifies the pathname variable to be queried for  the  specified
argument.   The names of the pathname variables are from with the
leading removed.  When invoked with the option writes a  list  of
all applicable variables and their values to the standard output,
in  the  format = For compatibility with other operating systems,
will ignore leading underscores in the names specified in the and
arguments.  To retrieve the number of configured processors, use:
$ getconf NPROCESSORS_CONF To  retrieve  the  maximum  number  of
bytes  (excluding  the trailing for a filename in the current di‐
rectory, use: $ getconf NAME_MAX .  The utility conforms to
