provides an interface for displaying and changing the contents of
the  EEPROM or openprom.  The tool is only installed on supported
platforms.  Without any arguments, will list  all  of  the  known
fields  and their corresponding values.  When given the name of a
specific field, will display that value or set it  if  the  field
name  is followed by and a value.  Only the super‐user may modify
the contents of the EEPROM or openprom.  The options are as  fol‐
lows:  Commands  are  taken  from  stdin and displayed on stdout.
will fix incorrect checksum values and exit.  This flag  is  qui‐
etly  ignored  on  systems  with an openprom.  On systems with an
EEPROM, use instead of the default On systems with  an  openprom,
use instead of the default If checksum values are incorrect, will
ignore  them  and continue after displaying a warning.  This flag
is quietly ignored on systems with an  openprom.   The  following
options  are  valid  only  on the SPARC and will produce an error
when used on a Sun 3: On systems with  an  openprom,  be  verbose
when setting a value.  Systems with an EEPROM are always verbose.
The  option is also present on sparc64, macppc, and prep systems.
The following fields and values are for systems with an EEPROM: A
valid date, such as The strings and  are  also  acceptable.   How
much  memory, in megabytes, is installed in the system.  How much
memory, in megabytes, is to be tested upon power‐up.  The size of
the screen.  Acceptable values are and If true, the  system  will
reboot upon reset.  Otherwise, the system will fall into the mon‐
itor.   If  true,  the  system will use the boot device stored in
Specifies the default boot device in the form cc(x,x,x), where is
a combination of two letters such as or and each is a hexadecimal
number between 0 and ff, less the prepending This  value  is  for
all Sun keyboards.  Specifies the console type.  Valid values are
and  If  true,  the keys click annoyingly.  This is a string very
similar to that used by It specifies the default boot device when
the diagnostic switch is turned on.  A 40‐character,  NULL‐termi‐
nated  string specifying the kernel or standalone program to load
when the diagnostic switch is turned on.  An 8‐bit integer speci‐
fying the number of columns on the  console.   An  8‐bit  integer
specifying  the number of rows on the console.  Use the baud rate
stored in instead of the default 9600.  A 16‐bit integer specify‐
ing the baud rate to use on ttya.   If  true,  disables  RTS/DTR.
Similar  to  but  for ttyb.  Similar to but for ttyb.  Similar to
but for ttyb.  An 80‐character, NULL‐terminated string to use  at
power‐up  instead  of  the default Sun banner.  Note that the and
fields are not currently supported.  Since the  openprom  is  de‐
signed  such  that the field names are arbitrary, explaining them
here is dubious.  Below are field names and values  that  one  is
likely  to see on a system with an openprom.  NOTE: this list may
be incomplete or incorrect due to differences  between  revisions
of the openprom.  If true, the old EEPROM‐style interface will be
used  while in the monitor, rather than the openprom‐style inter‐
face.  A 32‐bit integer specifying the  number  of  megabytes  of
memory  to  test  upon  power‐up.   A  64bitx64bit  bitmap in Sun
Iconedit format.  To set the bitmap, give  the  pathname  of  the
file  containing  the image.  NOTE: this property is not yet sup‐
ported.  If true, enables the use of the bitmap stored in  rather
than  the  default Sun logo.  A string to use at power up, rather
than the default Sun banner.  If true, enables  the  use  of  the
banner stored in rather than the default Sun banner.  A string of
five  comma separated fields in the format The first field is the
baud rate.  The second field is the number  of  data  bits.   The
third  field  is  the  parity;  acceptable  values for parity are
(none), (even), (odd), (mark), and (space).  The fourth field  is
the  number  of stop bits.  The fifth field is the field; accept‐
able values are (none), (RTS/CTS), and (Xon/Xoff).  If true,  the
system will ignore RTS/DTR.  If true, the system will ignore car‐
rier  detect.  Similar to but for ttyb.  Similar to but for ttyb.
Similar to but for ttyb.  Four digits in  the  format  specifying
which  order  to probe the sbus at power‐up.  It is unlikely that
this value should ever be changed.  An 8‐bit  integer  specifying
the  number of columns on the console.  An 8‐bit integer specify‐
ing the number of rows on the console.  If true, the system  will
boot  automatically at power‐up.  If true, the system will reboot
upon reset.  Otherwise, system will fall into the  monitor.   One
of  the  strings  or specifying the default console input device.
One of the strings or specifying the default console  output  de‐
vice.   If true, the keys click annoyingly.  A string in the for‐
mat describing the translation of  physical  to  logical  target.
Similar to but for tapes.  The default translation is The SCSI ID
of the on‐board SCSI controller.  A 7‐character string describing
a  date,  such as Similar to describing when the CPU was last up‐
dated.  If true, the system will boot and run in diagnostic mode.
The EEPROM device on systems with an EEPROM.  The openprom device
on systems with an openprom.  The nvram device on  PReP  systems.
The  fields  and their values are not necessarily well defined on
systems with an openprom.  Your mileage may vary.   There  are  a
few  fields known to exist in some revisions of the EEPROM and/or
openprom that are not yet supported.  Most notable are those  re‐
lating  to  password protection of the EEPROM or openprom.  Avoid
gratuitously changing the contents of the EEPROM.  It has a  lim‐
ited number of write cycles.  The date parser isn’t very intelli‐
gent.
