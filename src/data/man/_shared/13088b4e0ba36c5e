OSD(9)                   BSD Kernel Developer's Manual                  OSD(9)

NNAAMMEE
     oossdd, oossdd__rreeggiisstteerr, oossdd__ddeerreeggiisstteerr, oossdd__sseett, oossdd__rreesseerrvvee,
     oossdd__sseett__rreesseerrvveedd, oossdd__ffrreeee__rreesseerrvveedd, oossdd__ggeett, oossdd__ddeell, oossdd__ccaallll, oossdd__eexxiitt
     — Object Specific Data

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//oossdd..hh>>

     _t_y_p_e_d_e_f _v_o_i_d
     (**oossdd__ddeessttrruuccttoorr__tt)(_v_o_i_d _*_v_a_l_u_e);

     _t_y_p_e_d_e_f _i_n_t
     (**oossdd__mmeetthhoodd__tt)(_v_o_i_d _*_o_b_j, _v_o_i_d _*_d_a_t_a);

     _i_n_t
     oossdd__rreeggiisstteerr(_u___i_n_t _t_y_p_e, _o_s_d___d_e_s_t_r_u_c_t_o_r___t _d_e_s_t_r_u_c_t_o_r,
         _c_o_n_s_t _o_s_d___m_e_t_h_o_d___t _*_m_e_t_h_o_d_s);

     _v_o_i_d
     oossdd__ddeerreeggiisstteerr(_u___i_n_t _t_y_p_e, _u___i_n_t _s_l_o_t);

     _i_n_t
     oossdd__sseett(_u___i_n_t _t_y_p_e, _s_t_r_u_c_t _o_s_d _*_o_s_d, _u___i_n_t _s_l_o_t, _v_o_i_d _*_v_a_l_u_e);

     _v_o_i_d _*_*
     oossdd__rreesseerrvvee(_u___i_n_t _s_l_o_t);

     _i_n_t
     oossdd__sseett__rreesseerrvveedd(_u___i_n_t _t_y_p_e, _s_t_r_u_c_t _o_s_d _*_o_s_d, _u___i_n_t _s_l_o_t, _v_o_i_d _*_*_r_s_v,
         _v_o_i_d _*_v_a_l_u_e);

     _v_o_i_d
     oossdd__ffrreeee__rreesseerrvveedd(_v_o_i_d _*_*_r_s_v);

     _v_o_i_d _*
     oossdd__ggeett(_u___i_n_t _t_y_p_e, _s_t_r_u_c_t _o_s_d _*_o_s_d, _u___i_n_t _s_l_o_t);

     _v_o_i_d
     oossdd__ddeell(_u___i_n_t _t_y_p_e, _s_t_r_u_c_t _o_s_d _*_o_s_d, _u___i_n_t _s_l_o_t);

     _i_n_t
     oossdd__ccaallll(_u___i_n_t _t_y_p_e, _u___i_n_t _m_e_t_h_o_d, _v_o_i_d _*_o_b_j, _v_o_i_d _*_d_a_t_a);

     _v_o_i_d
     oossdd__eexxiitt(_u___i_n_t _t_y_p_e, _s_t_r_u_c_t _o_s_d _*_o_s_d);

DDEESSCCRRIIPPTTIIOONN
     The oossdd framework provides a mechanism to dynamically associate arbitrary
     data at run-time with any kernel data structure which has been suitably
     modified for use with oossdd.  The one-off modification required involves
     embedding a _s_t_r_u_c_t _o_s_d inside the kernel data structure.

     An additional benefit is that after the initial change to a structure is
     made, all subsequent use of oossdd with the structure involves no changes to
     the structure's layout.  By extension, if the data structure is part of
     the ABI, oossdd provides a way of extending the structure in an ABI preserv‐
     ing manner.

     The details of the embedded _s_t_r_u_c_t _o_s_d are not relevant to consumers of
     the oossdd framework and should not be manipulated directly.

     Data associated with a structure is referenced by the oossdd framework using
     a type/slot identifier pair.  Types are statically defined in <_s_y_s_/_o_s_d_._h>
     and provide a high-level grouping for slots to be registered under.  Slot
     identifiers are dynamically assigned by the framework when a data type is
     registered using oossdd__rreeggiisstteerr() and remains valid until a corresponding
     call to oossdd__ddeerreeggiisstteerr().

   FFuunnccttiioonnss
     The oossdd__rreeggiisstteerr() function registers a type/slot identifier pair with
     the oossdd framework for use with a new data type.  The function may sleep
     and therefore cannot be called from a non-sleepable context.  The _t_y_p_e
     argument specifies which high-level type grouping from <_s_y_s_/_o_s_d_._h> the
     slot identifier should be allocated under.  The _d_e_s_t_r_u_c_t_o_r argument spec‐
     ifies an optional osd_destructor_t function pointer that will be called
     for objects of the type being registered which are later destroyed by the
     oossdd__ddeell() function.  NULL may be passed if no destructor is required.
     The _m_e_t_h_o_d_s argument specifies an optional array of osd_method_t function
     pointers which can be later invoked by the oossdd__ccaallll() function.  NULL may
     be passed if no methods are required.  The _m_e_t_h_o_d_s argument is currently
     only useful with the OSD_JAIL type identifier.

     The oossdd__ddeerreeggiisstteerr() function deregisters a previously registered
     type/slot identifier pair.  The function may sleep and therefore cannot
     be called from a non-sleepable context.  The _t_y_p_e argument specifies
     which high-level type grouping from <_s_y_s_/_o_s_d_._h> the slot identifier is
     allocated under.  The _s_l_o_t argument specifies the slot identifier which
     is being deregistered and should be the value that was returned by
     oossdd__rreeggiisstteerr() when the data type was registered.

     The oossdd__sseett() function associates a data object pointer with a kernel
     data structure's _s_t_r_u_c_t _o_s_d member.  The _t_y_p_e argument specifies which
     high-level type grouping from <_s_y_s_/_o_s_d_._h> the slot identifier is allo‐
     cated under.  The _o_s_d argument is a pointer to the kernel data struc‐
     ture's _s_t_r_u_c_t _o_s_d which will have the _v_a_l_u_e pointer associated with it.
     The _s_l_o_t argument specifies the slot identifier to assign the _v_a_l_u_e
     pointer to.  The _v_a_l_u_e argument points to a data object to associate with
     _o_s_d.

     The oossdd__sseett__rreesseerrvveedd() function does the same as oossdd__sseett(), but with an
     extra argument _r_s_v that is internal-use memory previously allocated via
     oossdd__rreesseerrvvee().

     The oossdd__ggeett() function returns the data pointer associated with a kernel
     data structure's _s_t_r_u_c_t _o_s_d member from the specified type/slot identi‐
     fier pair.  The _t_y_p_e argument specifies which high-level type grouping
     from <_s_y_s_/_o_s_d_._h> the slot identifier is allocated under.  The _o_s_d argu‐
     ment is a pointer to the kernel data structure's _s_t_r_u_c_t _o_s_d to retrieve
     the data pointer from.  The _s_l_o_t argument specifies the slot identifier
     to retrieve the data pointer from.

     The oossdd__ddeell() function removes the data pointer associated with a kernel
     data structure's _s_t_r_u_c_t _o_s_d member from the specified type/slot identi‐
     fier pair.  The _t_y_p_e argument specifies which high-level type grouping
     from <_s_y_s_/_o_s_d_._h> the slot identifier is allocated under.  The _o_s_d argu‐
     ment is a pointer to the kernel data structure's _s_t_r_u_c_t _o_s_d to remove the
     data pointer from.  The _s_l_o_t argument specifies the slot identifier to
     remove the data pointer from.  If an osd_destructor_t function pointer
     was specified at registration time, the destructor function will be
     called and passed the data pointer for the type/slot identifier pair
     which is being deleted.

     The oossdd__ccaallll() function calls the specified osd_method_t function pointer
     for all currently registered slots of a given type on the specified _o_b_j
     and _d_a_t_a pointers.  The function may sleep and therefore cannot be called
     from a non-sleepable context.  The _t_y_p_e argument specifies which high-
     level type grouping from <_s_y_s_/_o_s_d_._h> to call the method for.  The _m_e_t_h_o_d
     argument specifies the index into the osd_method_t array that was passed
     to oossdd__rreeggiisstteerr().  The _o_b_j and _d_a_t_a arguments are passed to the method
     function pointer of each slot.

     The oossdd__eexxiitt() function removes all data object pointers from all cur‐
     rently registered slots for a given type for the specified kernel data
     structure's _s_t_r_u_c_t _o_s_d member.  The _t_y_p_e argument specifies which high-
     level type grouping from <_s_y_s_/_o_s_d_._h> to remove data pointers from.  The
     _o_s_d argument is a pointer to the kernel data structure's _s_t_r_u_c_t _o_s_d to
     remove all data object pointers for all currently registered slots from.

IIMMPPLLEEMMEENNTTAATTIIOONN NNOOTTEESS
     oossdd uses a two dimensional matrix (array of arrays) as the data structure
     to manage the external data associated with a kernel data structure's
     _s_t_r_u_c_t _o_s_d member.  The type identifier is used as the index into the
     outer array, and the slot identifier is used as the index into the inner
     array.  To set or retrieve a data pointer for a given type/slot identi‐
     fier pair, oossdd__sseett() and oossdd__ggeett() perform the equivalent of ar‐
     ray[type][slot], which is both constant time and fast.

     If oossdd__sseett() is called on a _s_t_r_u_c_t _o_s_d for the first time, the array for
     storing data pointers is dynamically allocated using malloc(9) with
     M_NOWAIT to a size appropriate for the slot identifier being set.  If a
     subsequent call to oossdd__sseett() attempts to set a slot identifier which is
     numerically larger than the slot used in the previous oossdd__sseett() call,
     realloc(9) is used to grow the array to the appropriate size such that
     the slot identifier can be used.  To maximise the efficiency of any code
     which calls oossdd__sseett() sequentially on a number of different slot identi‐
     fiers (e.g., during an initialisation phase) one should loop through the
     slot identifiers in descending order from highest to lowest.  This will
     result in only a single malloc(9) call to create an array of the largest
     slot size and all subsequent calls to oossdd__sseett() will proceed without any
     realloc(9) calls.

     It is possible for oossdd__sseett() to fail to allocate this array.  To ensure
     that such allocation succeeds, oossdd__rreesseerrvvee() may be called (in a non-
     blocking context), and it will pre-allocate the memory via malloc(9) with
     M_WAITOK.  Then this pre-allocated memory is passed to
     oossdd__sseett__rreesseerrvveedd(), which will use it if necessary or otherwise discard
     it.  The memory may also be explicitly discarded by calling
     oossdd__ffrreeee__rreesseerrvveedd().  As this method always allocates memory whether or
     not it is ultimately needed, it should be used only rarely, such as in
     the unlikely event that oossdd__sseett() fails.

     The oossdd API is geared towards slot identifiers storing pointers to the
     same underlying data structure type for a given oossdd type identifier.
     This is not a requirement, and khelp(9) for example stores completely
     different data types in slots under the OSD_KHELP type identifier.

   LLoocckkiinngg
     oossdd internally uses a mix of mutex(9), rmlock(9) and sx(9) locks to pro‐
     tect its internal data structures and state.

     Responsibility for synchronising access to a kernel data structure's
     _s_t_r_u_c_t _o_s_d member is left to the subsystem that uses the data structure
     and calls the oossdd API.

     oossdd__ggeett() only acquires an rmlock(9) in read mode, therefore making it
     safe to use in the majority of contexts within the kernel including most
     fast paths.

RREETTUURRNN VVAALLUUEESS
     oossdd__rreeggiisstteerr() returns the slot identifier for the newly registered data
     type.

     oossdd__sseett() and oossdd__sseett__rreesseerrvveedd() return zero on success or ENOMEM if the
     specified type/slot identifier pair triggered an internal realloc(9)
     which failed ( oossdd__sseett__rreesseerrvveedd() will always succeed when _r_s_v is non-
     NULL).

     oossdd__ggeett() returns the data pointer for the specified type/slot identifier
     pair, or NULL if the slot has not been initialised yet.

     oossdd__rreesseerrvvee() returns a pointer suitable for passing to
     oossdd__sseett__rreesseerrvveedd() or oossdd__ffrreeee__rreesseerrvveedd().

     oossdd__ccaallll() returns zero if no method is run or the method for each slot
     runs successfully.  If a method for a slot returns non-zero, oossdd__ccaallll()
     terminates prematurely and returns the method's error to the caller.

SSEEEE AALLSSOO
     khelp(9)

HHIISSTTOORRYY
     The Object Specific Data (OSD) facility first appeared in FreeBSD 8.0.

AAUUTTHHOORRSS
     The oossdd facility was written by Pawel Jakub Dawidek <_p_j_d_@_F_r_e_e_B_S_D_._o_r_g>.

     This manual page was written by Lawrence Stewart <_l_s_t_e_w_a_r_t_@_F_r_e_e_B_S_D_._o_r_g>.

BSD                            October 07, 2024                            BSD
