The utility is a Serial Port Profile daemon, providing serial ac‐
cess  over  an RFCOMM connection to a remote device.  can work in
client or server mode.  In client mode, initiates an RFCOMM  con‐
nection  to  the at the remote In server mode, registers the with
the local SDP server and listens on the specified RFCOMM  for  an
incoming  connection,  or  the  first  unused channel if none was
given.  The options are as follows: Client mode.  Specify the ad‐
dress of the remote RFCOMM device.  The address can be  specified
as  BD_ADDR  or  name.  If given as a name, then the utility will
attempt to resolve the name via In server mode, specify  the  RF‐
COMM channel number to listen on.  will register the service with
the local daemon.  Note that registering services with is a priv‐
ileged  operation.   Use  the local device with the specified ad‐
dress.  The device can be specified by BD_ADDR  or  device  name.
See  for  a  list  of available devices.  If no is specified, the
connection will be set up on a  system  determined  device.   Set
connection  link  mode.   Supported modes are: require devices be
paired.  auth, plus enable encryption.  encryption,  plus  change
of  link key.  Specify the value to be used for the RFCOMM proto‐
col.  In client mode where Service Discovery is being used,  this
value  will  be  ignored.  Name of service class to connect to or
register.  If no is given, the  default  service  class  will  be
used.   Known  service  classes are: Dialup Networking LAN access
using PPP Serial Port In client mode, the service  class  may  be
given  as a channel number, for instances where the remote device
does not provide Service Discovery.  Slave pseudo tty  name.   If
this  option  is  given, will detach from the controlling process
after the Bluetooth connection is  made,  and  operate  over  the
named pair.  Otherwise, stdin/stdout will be used.  master pseudo
terminals  slave  pseudo terminals Will open an RFCOMM connection
to the server at on channel Once the connection has  been  estab‐
lished,  will  detach and can be used to communicate with the re‐
mote serial port on the server, e.g. with the use of In order  to
use  to  automatically create a secured link for use in your con‐
figuration file.  The program first appeared in and was ported to
by under the sponsorship of Please report if found.
