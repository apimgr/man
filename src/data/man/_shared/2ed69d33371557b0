SF_BUF(9)                BSD Kernel Developer's Manual               SF_BUF(9)

NNAAMMEE
     ssff__bbuuff — manage temporary kernel address space mapping for memory pages

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ssff__bbuuff..hh>>

     _s_t_r_u_c_t _s_f___b_u_f _*
     ssff__bbuuff__aalllloocc(_s_t_r_u_c_t _v_m___p_a_g_e _*_m, _i_n_t _f_l_a_g_s);

     _v_o_i_d
     ssff__bbuuff__ffrreeee(_s_t_r_u_c_t _s_f___b_u_f _*_s_f);

     _v_m___o_f_f_s_e_t___t
     ssff__bbuuff__kkvvaa(_s_t_r_u_c_t _s_f___b_u_f _*_s_f);

     _s_t_r_u_c_t _v_m___p_a_g_e _*
     ssff__bbuuff__ppaaggee(_s_t_r_u_c_t _s_f___b_u_f _*_s_f);

DDEESSCCRRIIPPTTIIOONN
     The ssff__bbuuff interface, historically the sendfile(2) buffer interface, al‐
     lows kernel subsystems to manage temporary kernel address space mappings
     for physical memory pages.  On systems with a direct memory map region
     (allowing all physical pages to be visible in the kernel address space at
     all times), the _s_t_r_u_c_t _s_f___b_u_f will point to an address in the direct map
     region; on systems without a direct memory map region, the _s_t_r_u_c_t _s_f___b_u_f
     will manage a temporary kernel address space mapping valid for the life‐
     time of the _s_t_r_u_c_t _s_f___b_u_f.

     Call ssff__bbuuff__aalllloocc() to allocate a _s_t_r_u_c_t _s_f___b_u_f for a physical memory
     page.  ssff__bbuuff__aalllloocc() is not responsible for arranging for the page to be
     present in physical memory; the caller should already have arranged for
     the page to be wired, i.e., by calling vm_page_wire(9).  Several flags
     may be passed to ssff__bbuuff__aalllloocc():

     SFB_CATCH       Cause ssff__bbuuff__aalllloocc() to abort and return NULL if a signal
                     is received waiting for a _s_t_r_u_c_t _s_f___b_u_f to become avail‐
                     able.

     SFB_NOWAIT      Cause ssff__bbuuff__aalllloocc() to return NULL rather than sleeping
                     if a _s_t_r_u_c_t _s_f___b_u_f is not immediately available.

     SFB_CPUPRIVATE  Cause ssff__bbuuff__aalllloocc() to only arrange that the temporary
                     mapping be valid on the current CPU, avoiding unnecessary
                     TLB shootdowns for mappings that will only be accessed on
                     a single CPU at a time.  The caller must ensure that ac‐
                     cesses to the virtual address occur only on the CPU from
                     which ssff__bbuuff__aalllloocc() was invoked, perhaps by using
                     sscchheedd__ppiinn().

     Call ssff__bbuuff__kkvvaa() to return a kernel mapped address for the page.

     Call ssff__bbuuff__ppaaggee() to return a pointer to the page originally passed into
     ssff__bbuuff__aalllloocc().

     Call ssff__bbuuff__ffrreeee() to release the _s_t_r_u_c_t _s_f___b_u_f reference.  The caller is
     responsible for releasing any wiring they have previously acquired on the
     physical page; ssff__bbuuff__ffrreeee() releases only the temporary kernel address
     space mapping, not the page itself.

     Uses of this interface include managing mappings of borrowed pages from
     user memory, such as in zero-copy socket I/O, or pages of memory from the
     buffer cache referenced by mbuf external storage for sendfile(2).

SSEEEE AALLSSOO
     sendfile(2), vm_page_wire(9)

AAUUTTHHOORRSS
     The _s_t_r_u_c_t _s_f___b_u_f API was designed and implemented by Alan L. Cox.  This
     manual page was written by Robert N. M. Watson.

BSD                            January 28, 2007                            BSD
