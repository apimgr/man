The  driver provides a software watchdog timer that works with If
the timer expires, the system reboots if the boolean variable  is
otherwise,  the system will panic.  is accessible as the variable
hw.swwdog.reboot and defaults to The default period of is 60 sec‐
onds.  As with other watchdog timers, the driver prevents a  sys‐
tem  from  suspending when the watchdog is armed.  The driver was
written by Only one watchdog timer can be  active  at  any  given
time.  (Arguably, this is a bug in the watchdog timer framework.)
Therefore,  only  a single instance of the device can be created.
Kernel tickle mode is useless with and  arguably  should  be  re‐
jected,  since  both  it and this driver rely on the same callout
mechanism; if one is blocked, almost certainly the  other  is  as
well.  The alarm option to isn’t implemented.
