_w_c_t_o_b(3)                   Library Functions Manual                   _w_c_t_o_b(3)

NNAAMMEE
       wctob - try to represent a wide character as a single byte

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<wwcchhaarr..hh>>

       iinntt wwccttoobb((wwiinntt__tt _c));;

DDEESSCCRRIIPPTTIIOONN
       The  wwccttoobb() function tests whether the multibyte representation of the
       wide character _c, starting in the initial state, consists of  a  single
       byte.  If so, it is returned as an _u_n_s_i_g_n_e_d _c_h_a_r.

       Never  use this function.  It cannot help you in writing international‐
       ized programs.  Internationalized programs must never distinguish  sin‐
       gle‐byte and multibyte characters.

RREETTUURRNN VVAALLUUEE
       The wwccttoobb() function returns the single‐byte representation of _c, if it
       exists, or EEOOFF otherwise.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       wwccttoobb() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       C11, POSIX.1‐2008.

HHIISSTTOORRYY
       POSIX.1‐2001, C99.

NNOOTTEESS
       The behavior of wwccttoobb() depends on the LLCC__CCTTYYPPEE category of the current
       locale.

       This function should never be used.  Internationalized programs must
       never distinguish single‐byte and multibyte characters.  Use either
       wwccttoommbb(3) or the thread‐safe wwccrrttoommbb(3) instead.

SSEEEE AALLSSOO
       bbttoowwcc(3), wwccrrttoommbb(3), wwccttoommbb(3)

Linux man‐pages (unreleased)      2025‐05‐17                          _w_c_t_o_b(3)
