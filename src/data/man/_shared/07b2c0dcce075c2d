_p_o_p_e_n(3)                   Library Functions Manual                   _p_o_p_e_n(3)

NNAAMMEE
       popen, pclose - pipe stream to or from a process

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssttddiioo..hh>>

       FFIILLEE **ppooppeenn((ccoonnsstt cchhaarr **_c_o_m_m_a_n_d,, ccoonnsstt cchhaarr **_t_y_p_e));;
       iinntt ppcclloossee((FFIILLEE **_s_t_r_e_a_m));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       ppooppeenn(), ppcclloossee():
           _POSIX_C_SOURCE >= 2
               || /* glibc <= 2.19: */ _BSD_SOURCE || _SVID_SOURCE

DDEESSCCRRIIPPTTIIOONN
       The  ppooppeenn()  function opens a process by creating a pipe, forking, and
       invoking the shell.  Since a pipe is by definition unidirectional,  the
       _t_y_p_e  argument  may  specify only reading or writing, not both; the re‐
       sulting stream is correspondingly read‐only or write‐only.

       The _c_o_m_m_a_n_d argument is a pointer to a null‐terminated string  contain‐
       ing  a shell command line.  This command is passed to _/_b_i_n_/_s_h using the
       --cc flag; interpretation, if any, is performed by the shell.

       The _t_y_p_e argument is a pointer to a null‐terminated string  which  must
       contain  either  the letter 'r' for reading or the letter 'w' for writ‐
       ing.  Since glibc 2.9, this argument can additionally include the  let‐
       ter  'e', which causes the close‐on‐exec flag (FFDD__CCLLOOEEXXEECC) to be set on
       the underlying file descriptor; see the description  of  the  OO__CCLLOOEEXXEECC
       flag in ooppeenn(2) for reasons why this may be useful.

       The  return  value  from ppooppeenn() is a normal standard I/O stream in all
       respects save  that  it  must  be  closed  with  ppcclloossee()  rather  than
       ffcclloossee(3).   Writing  to  such a stream writes to the standard input of
       the command; the command’s standard output is the same as that  of  the
       process  that called ppooppeenn(), unless this is altered by the command it‐
       self.  Conversely, reading from the stream reads the command’s standard
       output, and the command’s standard input is the same  as  that  of  the
       process that called ppooppeenn().

       Note that output ppooppeenn() streams are block buffered by default.

       The ppcclloossee() function waits for the associated process to terminate and
       returns the exit status of the command as returned by wwaaiitt44(2).

RREETTUURRNN VVAALLUUEE
       ppooppeenn():  on  success,  returns a pointer to an open stream that can be
       used to read or write to the pipe; if  the  ffoorrkk(2)  or  ppiippee(2)  calls
       fail, or if the function cannot allocate memory, NULL is returned.

       ppcclloossee():  on  success,  returns  the  exit  status  of the command; if
       wwaaiitt44(2) returns an error, or some other error is detected, -1  is  re‐
       turned.

       On failure, both functions set _e_r_r_n_o to indicate the error.

EERRRROORRSS
       The ppooppeenn() function does not set _e_r_r_n_o if memory allocation fails.  If
       the  underlying  ffoorrkk(2) or ppiippee(2) fails, _e_r_r_n_o is set to indicate the
       error.  If the _t_y_p_e argument is invalid,  and  this  condition  is  de‐
       tected, _e_r_r_n_o is set to EEIINNVVAALL.

       If ppcclloossee() cannot obtain the child status, _e_r_r_n_o is set to EECCHHIILLDD.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       ppooppeenn(), ppcclloossee() T}   Thread safety  MT‐Safe

VVEERRSSIIOONNSS
       The 'e' value for _t_y_p_e is a Linux extension.

SSTTAANNDDAARRDDSS
       POSIX.1‐2008.

HHIISSTTOORRYY
       POSIX.1‐2001.

CCAAVVEEAATTSS
       Carefully read Caveats in ssyysstteemm(3).

BBUUGGSS
       Since the standard input of a command opened for reading shares its
       seek offset with the process that called ppooppeenn(), if the original
       process has done a buffered read, the command’s input position may not
       be as expected.  Similarly, the output from a command opened for
       writing may become intermingled with that of the original process.  The
       latter can be avoided by calling fffflluusshh(3) before ppooppeenn().

       Failure to execute the shell is indistinguishable from the shell’s
       failure to execute the command, or an immediate exit of the command.
       The only hint is an exit status of 127.

SSEEEE AALLSSOO
       sshh(1), ffoorrkk(2), ppiippee(2), wwaaiitt44(2), ffcclloossee(3), fffflluusshh(3), ffooppeenn(3),
       ssttddiioo(3), ssyysstteemm(3)

Linux man‐pages (unreleased)      2025‐09‐21                          _p_o_p_e_n(3)
