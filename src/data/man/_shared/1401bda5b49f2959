MUNMAP(2)                   BSD System Calls Manual                  MUNMAP(2)

NNAAMMEE
     mmuunnmmaapp — remove a mapping

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//mmmmaann..hh>>

     _i_n_t
     mmuunnmmaapp(_v_o_i_d _*_a_d_d_r, _s_i_z_e___t _l_e_n);

DDEESSCCRRIIPPTTIIOONN
     The mmuunnmmaapp() system call deletes the mappings and guards for the speci‐
     fied address range, and causes further references to addresses within the
     range to generate invalid memory references.

RREETTUURRNN VVAALLUUEESS
     The mmuunnmmaapp() function returns the value 0 if successful; otherwise the
     value -1 is returned and the global variable _e_r_r_n_o is set to indicate the
     error.

EERRRROORRSS
     The mmuunnmmaapp() system call will fail if:

     [EINVAL]           The _l_e_n argument was zero, or some part of the region
                        being unmapped is outside the valid address range for
                        a process.

SSEEEE AALLSSOO
     madvise(2), mincore(2), mmap(2), mprotect(2), msync(2), getpagesize(3)

HHIISSTTOORRYY
     The mmuunnmmaapp() system call first appeared in 4.4BSD.

BSD                            January 15, 2025                            BSD
