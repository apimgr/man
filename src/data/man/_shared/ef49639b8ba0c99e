The  command  configures  datasets  within a storage pool, as de‐
scribed in A dataset is identified by a unique  path  within  the
namespace.  For  example:  where  the maximum length of a dataset
name is (256 bytes).  A dataset can be one of  the  following:  A
dataset  of  type can be mounted within the standard system name‐
space and behaves like other file systems. While file systems are
designed to be compliant, known issues exist that prevent compli‐
ance in some cases.  Applications that depend on  standards  con‐
formance  might  fail  due  to nonstandard behavior when checking
file system free space.  A logical volume exported as  a  raw  or
block device. This type of dataset should only be used under spe‐
cial circumstances. File systems are typically used in most envi‐
ronments.   A  read‐only  version of a file system or volume at a
given point in time. It is specified as or A storage  pool  is  a
logical  collection of devices that provide space for datasets. A
storage pool is also the root of the file system hierarchy.   The
root of the pool can be accessed as a file system, such as mount‐
ing and unmounting, taking snapshots, and setting properties. The
physical  storage  characteristics,  however,  are managed by the
command.  See for more information on creating and  administering
pools.   A  snapshot is a read‐only copy of a file system or vol‐
ume. Snapshots can be created extremely  quickly,  and  initially
consume  no  additional space within the pool. As data within the
active dataset changes, the  snapshot  consumes  more  data  than
would otherwise be shared with the active dataset.  Snapshots can
have  arbitrary  names.  Snapshots  of  volumes  can be cloned or
rolled back, but cannot be accessed independently.   File  system
snapshots  can be accessed under the directory in the root of the
file system. Snapshots are automatically mounted  on  demand  and
may  be unmounted at regular intervals. The visibility of the di‐
rectory can be controlled by the property.  A clone is a writable
volume or file system whose initial contents are the same as  an‐
other  dataset. As with snapshots, creating a clone is nearly in‐
stantaneous, and initially consumes no additional space.   Clones
can  only  be created from a snapshot. When a snapshot is cloned,
it creates an implicit dependency between the parent  and  child.
Even  though  the  clone is created somewhere else in the dataset
hierarchy, the original snapshot cannot be destroyed as long as a
clone exists. The property exposes this dependency, and the  com‐
mand  lists any such dependencies, if they exist.  The clone par‐
ent‐child dependency relationship can be reversed  by  using  the
subcommand.  This  causes  the  "origin"  file system to become a
clone of the specified file system, which makes  it  possible  to
destroy  the file system that the clone was created from.  Creat‐
ing a file system is a simple operation, so the  number  of  file
systems  per  system is likely to be numerous. To cope with this,
automatically manages mounting and unmounting file systems  with‐
out  the  need  to  edit the file. All automatically managed file
systems are mounted by at boot time.  By  default,  file  systems
are  mounted  under  where  is the name of the file system in the
namespace. Directories are created and destroyed  as  needed.   A
file system can also have a mount point set in the property. This
directory is created as needed, and automatically mounts the file
system  when the command is invoked (without editing The property
can be inherited, so if has a mount point of  then  automatically
inherits  a mount point of A file system property of prevents the
file system from being mounted.  If needed, file systems can also
be managed with traditional tools If a file system’s mount  point
is set to makes no attempt to manage the file system, and the ad‐
ministrator  is  responsible for mounting and unmounting the file
system.  Deduplication is the process for removing redundant data
at the block‐level, reducing the total amount of data stored.  If
a file system has the property enabled, duplicate data blocks are
removed  synchronously.  The  result  is that only unique data is
stored and common components are shared among files.   Properties
are  divided  into  two types, native properties and user‐defined
(or "user") properties. Native properties either export  internal
statistics  or  control  behavior. In addition, native properties
are either editable or read‐only. User properties have no  effect
on  behavior,  but you can use them to annotate datasets in a way
that is meaningful in  your  environment.  For  more  information
about user properties, see the section, below.  Every dataset has
a  set  of properties that export statistics about the dataset as
well as control various behaviors. Properties are inherited  from
the  parent unless overridden by the child. Some properties apply
only to certain types of  datasets  (file  systems,  volumes,  or
snapshots).   The  values  of numeric properties can be specified
using human‐readable suffixes (for example, and so forth,  up  to
for  zettabyte). The following are all valid (and equal) specifi‐
cations: 1536M, 1.5g, 1.50GB The values of non‐numeric properties
are case sensitive and must be lowercase, except for The  follow‐
ing  native  properties consist of read‐only statistics about the
dataset. These properties can be neither set, nor inherited.  Na‐
tive  properties  apply  to  all  dataset  types unless otherwise
noted.  The amount of space available to the dataset and all  its
children,  assuming  that there is no other activity in the pool.
Because space is shared within a pool, availability can  be  lim‐
ited by any number of factors, including physical pool size, quo‐
tas, reservations, or other datasets within the pool.  This prop‐
erty  can  also  be referred to by its shortened column name, For
non‐snapshots, the compression ratio achieved for  the  space  of
this  dataset,  expressed as a multiplier.  The property includes
descendant datasets, and, for clones, does not include the  space
shared  with the origin snapshot.  For snapshots, the is the same
as the property. Compression can be turned on by running: The de‐
fault value is The time this dataset was created.  For snapshots,
this property is a comma‐separated list of filesystems or volumes
which are clones of this snapshot.  The clones’ property is  this
snapshot.   If  the property is not empty, then this snapshot can
not be destroyed (even with the or options).  This property is if
the snapshot has been marked for deferred destroy  by  using  the
command.  Otherwise, the property is The total number of filesys‐
tems and volumes that exist under this location  in  the  dataset
tree.  This value is only available when a has been set somewhere
in the tree under which the dataset resides.  The amount of space
that is accessible by this dataset.  See the property.  The logi‐
cal  space  ignores  the  effect  of the and properties, giving a
quantity closer to the amount  of  data  that  applications  see.
However,  it does include space consumed by metadata.  This prop‐
erty can also be referred to by its shortened  column  name,  The
amount  of space that is consumed by this dataset and all its de‐
scendents.  See the property.  The logical space ignores the  ef‐
fect  of  the  and  properties,  giving  a quantity closer to the
amount of data that applications see.  This property can also  be
referred to by its shortened column name, For file systems, indi‐
cates whether the file system is currently mounted. This property
can be either or For cloned file systems or volumes, the snapshot
from  which  the  clone  was created. See also the property.  For
filesystems or volumes which have saved partially‐completed state
from this opaque token can be provided to to resume and  complete
the  The amount of data that is accessible by this dataset, which
may or may not be shared with other datasets in the pool. When  a
snapshot  or  clone  is created, it initially references the same
amount of space as the file system or  snapshot  it  was  created
from,  since  its contents are identical.  This property can also
be referred to by its shortened column name, The compression  ra‐
tio achieved for the space of this dataset, expressed as a multi‐
plier.   See  also  the  property.  The total number of snapshots
that exist under this location in the dataset tree.   This  value
is only available when a has been set somewhere in the tree under
which  the  dataset  resides.  The type of dataset: The amount of
space consumed by this dataset and all its descendents.  This  is
the value that is checked against this dataset’s quota and reser‐
vation.  The  space used does not include this dataset’s reserva‐
tion, but does take into account the reservations of any  descen‐
dent  datasets.  The amount of space that a dataset consumes from
its parent, as well as the amount of space that are freed if this
dataset is recursively destroyed, is the  greater  of  its  space
used  and  its reservation.  When snapshots (see the section) are
created, their space is initially shared between the snapshot and
the file system, and possibly with  previous  snapshots.  As  the
file  system  changes,  space  that was previously shared becomes
unique to the snapshot, and counted in the snapshot’s space used.
Additionally, deleting snapshots can increase the amount of space
unique to (and used by) other snapshots.   The  amount  of  space
used, available, or referenced does not take into account pending
changes. Pending changes are generally accounted for within a few
seconds.  Committing  a change to a disk using or does not neces‐
sarily guarantee that the space usage information is updated  im‐
mediately.  The properties decompose the properties into the var‐
ious  reasons  that space is used. Specifically, These properties
are only available for datasets  created  with  pool  version  13
pools  and  higher.  The amount of space consumed by snapshots of
this dataset. In particular, it is the amount of space that would
be freed if all of this dataset’s snapshots were destroyed.  Note
that  this is not simply the sum of the snapshots’ properties be‐
cause space can be shared by multiple snapshots.  The  amount  of
space  used  by  this dataset itself, which would be freed if the
dataset were destroyed (after first removing any  and  destroying
any  necessary  snapshots  or  descendents).  The amount of space
used by children of this dataset, which would be freed if all the
dataset’s children were destroyed.  The amount of space used by a
set on this dataset, which would be freed  if  the  was  removed.
The  amount  of  space  consumed  by  the  specified user in this
dataset. Space is charged to the owner of each file, as displayed
by The amount of space charged is displayed by and See  the  sub‐
command for more information.  Unprivileged users can access only
their  own  space  usage.  The  root user, or a user who has been
granted the privilege with  can  access  everyone’s  usage.   The
properties  are not displayed by The user’s name must be appended
after the symbol, using one of the following  forms:  POSIX  name
(for example, POSIX numeric ID (for example, This property is set
to  the number of user holds on this snapshot. User holds are set
by using the command.  The amount of space consumed by the speci‐
fied group in this dataset. Space is charged to the group of each
file, as displayed by See the property for more information.  Un‐
privileged users can only access their own groups’  space  usage.
The  root user, or a user who has been granted the privilege with
can access all groups’ usage.  For volumes, specifies  the  block
size  of  the  volume.  The cannot be changed once the volume has
been written, so it should be set at volume  creation  time.  The
default for volumes is 8 Kbytes. Any power of 2 from 512 bytes to
128  Kbytes  is  valid.  This property can also be referred to by
its shortened column name, The amount of space  written  to  this
dataset since the previous snapshot.  The amount of space written
to  this dataset since the specified snapshot.  This is the space
that is referenced by this dataset but was not referenced by  the
specified  snapshot.   The  may  be specified as a short snapshot
name (just the part after the in which case  it  will  be  inter‐
preted  as a snapshot in the same filesystem as this dataset. The
may be a full snapshot name which for clones may be a snapshot in
the origin’s filesystem (or the origin of the  origin’s  filesys‐
tem, etc).  The following native properties can be used to change
the  behavior  of  a dataset.  Controls how entries are inherited
when files and directories are created. A  file  system  with  an
property  of  does not inherit any entries. A file system with an
property value of only inherits inheritable entries that  specify
"deny"  permissions. The property value (the default) removes the
and permissions when the entry is inherited. A file  system  with
an property value of inherits all inheritable entries without any
modifications made to the entries when they are inherited. A file
system  with  an property value of has the same meaning as except
that the inherit the execute permission only if the file creation
mode also requests the execute bit.  When the property  value  is
set  to files are created with a mode determined by the inherita‐
ble If no inheritable exist that affect the mode, then  the  mode
is  set in accordance to the requested mode from the application.
Controls how an is modified during A file system with an property
of (the default) deletes all entries that do  not  represent  the
mode  of  the file. An property of reduces permissions granted in
all entries found in the such that they are no greater  than  the
group  permissions specified by A file system with an property of
indicates that no changes are made to the other than creating  or
updating  the  necessary entries to represent the new mode of the
file or directory.  An property of will cause  the  operation  to
return  an  error  when used on any file or directory which has a
non‐trivial whose entries can not be represented by a  mode.   is
required  to change the set user ID, set group ID, or sticky bits
on a file or directory, as they do not have  equivalent  entries.
In order to use on a file or directory with a non‐trivial when is
set  to  you must first remove all entries which do not represent
the current mode.  Controls whether the access time for files  is
updated  when  they  are  read.  Turning this property off avoids
producing write traffic when reading files and can result in sig‐
nificant performance gains, though it might confuse  mailers  and
other similar utilities. The default value is If this property is
set  to the file system cannot be mounted, and is ignored by Set‐
ting this property to is similar to setting the property  to  ex‐
cept  that  the dataset still has a normal property, which can be
inherited. Setting this property to allows datasets  to  be  used
solely  as a mechanism to inherit properties. One example of set‐
ting is to have two datasets with the same so that  the  children
of  both  datasets  appear  in the same directory, but might have
different inherited characteristics.  When the value  is  set,  a
dataset can only be mounted and unmounted explicitly. The dataset
is  not  mounted automatically when the dataset is created or im‐
ported, nor is it mounted by the command or unmounted by the com‐
mand.  This property is not  inherited.   Controls  the  checksum
used  to  verify data integrity. The default value is which auto‐
matically selects an appropriate algorithm (currently,  but  this
may  change  in  future  releases).  The value disables integrity
checking on user data.  The value not only disables integrity but
also disables maintaining parity for user data.  This setting  is
used  internally  by  a dump device residing on a RAID‐Z pool and
should not be used by any other dataset.  Disabling checksums  is
a  recommended practice.  The and checksum algorithms require en‐
abling the appropriate features on the pool.  Please see for more
information on these algorithms.  Changing this property  affects
only newly‐written data.  Controls the compression algorithm used
for this dataset.  Setting compression to indicates that the cur‐
rent  default  compression algorithm should be used.  The default
balances compression and decompression  speed,  with  compression
ratio  and  is  expected  to work well on a wide variety of work‐
loads.  Unlike all other settings for this property, on does  not
select  a  fixed compression type.  As new compression algorithms
are added to ZFS and enabled on a pool, the  default  compression
algorithm  may  change.  The current default compression algorthm
is either or, if the feature is enabled,  The  compression  algo‐
rithm  is  optimized  for performance while providing decent data
compression. Setting compression to uses  the  compression  algo‐
rithm. The compression algorithm uses the same compression as the
command. You can specify the level by using the value where is an
integer  from  1  (fastest)  to  9 (best compression ratio). Cur‐
rently, is equivalent to (which is also the default for The  com‐
pression algorithm compresses runs of zeros.  The compression al‐
gorithm  is  a high‐performance replacement for the algorithm. It
features significantly faster compression and  decompression,  as
well  as  a moderately higher compression ratio than but can only
be used on pools with the feature set to See for details  on  ZFS
feature  flags  and  the  feature.  This property can also be re‐
ferred to by its shortened column name Changing this property af‐
fects only newly‐written data.  Controls the number of copies  of
data stored for this dataset. These copies are in addition to any
redundancy  provided by the pool, for example, mirroring or RAID‐
Z. The copies are stored on different  disks,  if  possible.  The
space  used  by multiple copies is charged to the associated file
and dataset, changing the property and  counting  against  quotas
and  reservations.   Changing  this  property only affects newly‐
written data. Therefore, set this property at  file  system  cre‐
ation  time  by using the option.  Configures deduplication for a
dataset. The default value is The default deduplication  checksum
is  (this may change in the future).  When is enabled, the check‐
sum defined here overrides the property. Setting the value to has
the same effect as the setting If set to will do  a  byte‐to‐byte
comparsion  in  case  of  two blocks having the same signature to
make sure the block contents are identical.  The property is cur‐
rently not supported on Controls whether processes  can  be  exe‐
cuted  from  within  this  file  system. The default value is The
property is currently not  supported  on  Limits  the  number  of
filesystems  and  volumes  that can exist under this point in the
dataset tree.  The limit is not enforced if the user  is  allowed
to  change  the limit.  Setting a on a descendent of a filesystem
that already has a does not override the  ancestor’s  but  rather
imposes  an additional limit.  This feature must be enabled to be
used Controls the mount point used for this file system. See  the
section  for more information on how this property is used.  When
the property is changed for a file system, the  file  system  and
any  children  that inherit the mount point are unmounted. If the
new value is then they remain unmounted. Otherwise, they are  au‐
tomatically  remounted  in  the  new location if the property was
previously or or if they were mounted  before  the  property  was
changed.  In  addition,  any shared file systems are unshared and
shared in the new location.  The property is currently  not  sup‐
ported  on Controls what is cached in the primary cache (ARC). If
this property is set to then  both  user  data  and  metadata  is
cached.  If  this  property  is set to then neither user data nor
metadata is cached. If this property is set to then only metadata
is cached. The default value is Limits  the  amount  of  space  a
dataset and its descendents can consume. This property enforces a
hard  limit  on the amount of space used. This includes all space
consumed by descendents, including file  systems  and  snapshots.
Setting  a  quota on a descendent of a dataset that already has a
quota does not override the ancestor’s quota, but rather  imposes
an  additional  limit.   Quotas  cannot be set on volumes, as the
property acts as an implicit quota.  Limits the number  of  snap‐
shots that can be created on a dataset and its descendents.  Set‐
ting  a  on a descendent of a dataset that already has a does not
override the ancestor’s but rather imposes an  additional  limit.
The  limit  is  not enforced if the user is allowed to change the
limit.  For example, this means that  recursive  snapshots  taken
from  the  global zone are counted against each delegated dataset
within a jail.  This feature must be enabled to  be  used  Limits
the  amount  of space consumed by the specified user.  Similar to
the property, the space calculation does not include  space  that
is  used  by  descendent  datasets, such as snapshots and clones.
User space consumption is identified by the  property.   Enforce‐
ment of user quotas may be delayed by several seconds. This delay
means  that a user might exceed their quota before the system no‐
tices that they are over quota and begins  to  refuse  additional
writes with the error message. See the subcommand for more infor‐
mation.   Unprivileged  users  can  only access their own groups’
space usage. The root user, or a user who has  been  granted  the
privilege  with  can get and set everyone’s quota.  This property
is not available on volumes, on file systems before version 4, or
on pools before version 15. The properties are not  displayed  by
The  user’s  name must be appended after the symbol, using one of
the following forms: POSIX name (for example,  POSIX  numeric  ID
(for  example,  Limits the amount of space consumed by the speci‐
fied group. Group space consumption is identified  by  the  prop‐
erty.  Unprivileged users can access only their own groups’ space
usage.  The  root user, or a user who has been granted the privi‐
lege with can get and set all groups’ quotas.   Controls  whether
this  dataset  can  be modified. The default value is Specifies a
suggested block size for files in the file system. This  property
is  designed  solely  for use with database workloads that access
files in fixed‐size records.  automatically tunes block sizes ac‐
cording to internal algorithms optimized for typical access  pat‐
terns.   For  databases  that  create very large files but access
them in small random chunks, these algorithms may be  suboptimal.
Specifying  a  greater  than  or  equal to the record size of the
database can result in significant performance gains. Use of this
property for general purpose file systems  is  strongly  discour‐
aged,  and  may adversely affect performance.  The size specified
must be a power of two greater than or equal to 512 and less than
or equal to 128 Kbytes.  If the feature is enabled on  the  pool,
the  size  may  be up to 1 Mbyte.  See for details on ZFS feature
flags.  Changing the file system’s affects only files created af‐
terward; existing files are unaffected.  This property  can  also
be  referred to by its shortened column name, Controls what types
of metadata are stored redundantly.  ZFS stores an extra copy  of
metadata,  so  that if a single block is corrupted, the amount of
user data lost is limited.  This extra copy is in addition to any
redundancy provided at the pool level and is in  addition  to  an
extra  copy  specified by the property For example if the pool is
mirrored, and then ZFS stores 6 copies of most  metadata,  and  4
copies  of data and some metadata.  When set to ZFS stores an ex‐
tra copy of all metadata.  If a single on‐disk block is  corrupt,
at  worst  a  single  block  of user data bytes long can be lost.
When set to ZFS stores an extra copy of most types  of  metadata.
This can improve performance of random writes, because less meta‐
data  must  be  written.   In practice, at worst about 100 blocks
bytes each of user data can be lost if a single on‐disk block  is
corrupt.   The exact behavior of which metadata blocks are stored
redundantly may change in future releases.  The default value  is
Limits  the  amount of space a dataset can consume. This property
enforces a hard limit on the amount  of  space  used.  This  hard
limit  does not include space used by descendents, including file
systems and snapshots.  The minimum amount of space guaranteed to
a dataset, not including its  descendents.  When  the  amount  of
space  used  is below this value, the dataset is treated as if it
were taking up the amount of space specified by  The  reservation
is  accounted  for in the parent datasets’ space used, and counts
against the parent datasets’ quotas and reservations.  If is set,
a snapshot is only allowed if there is  enough  free  pool  space
outside  of this reservation to accommodate the current number of
"referenced" bytes in the dataset.  This property can also be re‐
ferred to by its shortened column name,  The  minimum  amount  of
space  guaranteed  to  a  dataset  and  its descendents. When the
amount of space used is below this value, the dataset is  treated
as  if  it  were  taking  up the amount of space specified by its
reservation.  Reservations  are  accounted  for  in  the   parent
datasets’ space used, and count against the parent datasets’ quo‐
tas  and  reservations.  This property can also be referred to by
its shortened column name, Controls what is cached  in  the  sec‐
ondary  cache  (L2ARC). If this property is set to then both user
data and metadata is cached. If this property is set to then nei‐
ther user data nor metadata is cached. If this property is set to
then only metadata is  cached.  The  default  value  is  Controls
whether  the  bit  is  respected for the file system. The default
value is The property currently has no effect on Controls whether
the file system is shared via and what options are used.  A  file
system with a property of is managed the traditional way via Oth‐
erwise, the file system is automatically shared and unshared with
the and commands. If the property is set to no export options are
used. Otherwise, export options are equivalent to the contents of
this property. The export options may be comma‐separated. See for
a  list  of  valid  options.   When the property is changed for a
dataset, the daemon is reloaded.  Provide a hint  to  about  han‐
dling of synchronous requests in this dataset.  If is set to (the
default), will use pool log devices (if configured) to handle the
requests  at  low  latency.  If is set to will not use configured
pool log devices.  will instead optimize  synchronous  operations
for  global pool throughput and efficient use of resources.  Con‐
trols whether the directory is hidden or visible in the  root  of
the file system as discussed in the section. The default value is
Controls  the  behavior  of synchronous requests (e.g.  O_DSYNC).
This property accepts the following values:  This  is  the  POSIX
specified behavior of ensuring all synchronous requests are writ‐
ten  to stable storage and all devices are flushed to ensure data
is not cached by device controllers (this is the  default).   All
file  system  transactions  are  written and flushed before their
system calls return. This has a large performance penalty.   Dis‐
ables  synchronous  requests.  File  system transactions are only
committed to stable storage periodically. This option  will  give
the  highest performance.  However, it is very dangerous as would
be ignoring the synchronous transaction demands  of  applications
such  as  databases or Administrators should only use this option
when the risks are understood.  For volumes, specifies the  logi‐
cal size of the volume. By default, creating a volume establishes
a  reservation  of  equal  size. For storage pools with a version
number of 9 or higher, a is set instead. Any changes to  are  re‐
flected  in  an  equivalent change to the reservation (or The can
only be set to a multiple of and cannot be zero.  The reservation
is kept equal to the volume’s logical size to prevent  unexpected
behavior for consumers. Without the reservation, the volume could
run out of space, resulting in undefined behavior or data corrup‐
tion, depending on how the volume is used. These effects can also
occur when the volume size is changed while it is in use (partic‐
ularly when shrinking the size). Extreme care should be used when
adjusting  the  volume  size.   Though not recommended, a "sparse
volume" (also known as "thin provisioning")  can  be  created  by
specifying the option to the command, or by changing the reserva‐
tion  after  the  volume has been created. A "sparse volume" is a
volume where the reservation is less then the volume size.   Con‐
sequently,  writes to a sparse volume can fail with when the pool
is low on space. For a sparse volume,  changes  to  are  not  re‐
flected  in the reservation.  This property specifies how volumes
should be exposed to the OS.  Setting it to  exposes  volumes  as
providers,  providing  maximal  functionality.  Setting it to ex‐
poses volumes only as cdev device in devfs.  Such volumes can  be
accessed only as raw disk device files, i.e. they can not be par‐
titioned,  mounted,  participate  in  RAIDs,  etc,  but  they are
faster, and in some use scenarios with untrusted  consumer,  such
as  NAS  or  VM storage, can be more safe.  Volumes with property
set to are not  exposed  outside  ZFS,  but  can  be  snapshoted,
cloned,  replicated,  etc,  that  can be suitable for backup pur‐
poses.  Value means that volumes exposition is controlled by sys‐
tem‐wide sysctl/tunable where and are encoded as 1, 2 and  3  re‐
spectively.   The  default values is This property can be changed
any time, but so far it is processed only during volume  creation
and  pool import.  The property is currently not supported on The
property is currently not supported on The following three  prop‐
erties  cannot  be  changed after the file system is created, and
therefore, should be set when the file system is created. If  the
properties are not set with the or commands, these properties are
inherited  from  the  parent dataset. If the parent dataset lacks
these properties due to having been created prior to  these  fea‐
tures  being supported, the new file system will have the default
values for these properties.  Indicates  whether  the  file  name
matching  algorithm used by the file system should be case‐sensi‐
tive, case‐insensitive, or allow a combination of both styles  of
matching.  The  default  value for the property is Traditionally,
UNIX and POSIX file systems have case‐sensitive file names.   The
value for the property indicates that the file system can support
requests  for  both  case‐sensitive and case‐insensitive matching
behavior.  Indicates whether the file  system  should  perform  a
normalization of file names whenever two file names are compared,
and  which normalization algorithm should be used. File names are
always stored unmodified, names are normalized  as  part  of  any
comparison  process.  If  this  property  is set to a legal value
other than and the property was left unspecified, the property is
automatically set to The default value of the  property  is  This
property cannot be changed after the file system is created.  In‐
dicates whether the file system should reject file names that in‐
clude  characters that are not present in the character code set.
If this property is explicitly set to the normalization  property
must  either not be explicitly set or be set to The default value
for the property is This property cannot  be  changed  after  the
file  system is created.  The properties are also new permissions
that can be assigned to non‐privileged users by using  the  dele‐
gated administration feature.  When a file system is mounted, ei‐
ther  through  for  legacy  mounts or the command for normal file
systems, its mount options are set according to  its  properties.
The  correlation  between properties and mount options is as fol‐
lows: In addition, these options can be set on a per‐mount  basis
using  the  option, without affecting the property that is stored
on disk. The values specified on the command  line  override  the
values  stored  in  the dataset. These properties are reported as
"temporary" by the command. If the properties are  changed  while
the  dataset  is mounted, the new setting overrides any temporary
settings.  In addition to the standard  native  properties,  sup‐
ports  arbitrary  user properties. User properties have no effect
on behavior, but applications or administrators can use  them  to
annotate  datasets  (file systems, volumes, and snapshots).  User
property names must contain a colon character to distinguish them
from native properties. They may contain lowercase letters,  num‐
bers, and the following punctuation characters: colon dash period
and  underscore The expected convention is that the property name
is divided into two portions such as but this  namespace  is  not
enforced  by  User  property names can be at most 256 characters,
and cannot begin with a dash When making programmatic use of user
properties, it is strongly suggested to  use  a  reversed  domain
name  for  the  component  of property names to reduce the chance
that two independently‐developed packages use the  same  property
name  for  different  purposes. Property names beginning with are
reserved for use by Sun Microsystems.  The values of user proper‐
ties are arbitrary strings, are always inherited, and  are  never
validated.  All of the commands that operate on properties and so
forth can be used to manipulate both native properties  and  user
properties.  Use  the  command  to  clear a user property. If the
property is not defined in any parent dataset, it is removed  en‐
tirely. Property values are limited to 1024 characters.  All sub‐
commands that modify state are logged persistently to the pool in
their  original  form.   Displays  a help message.  Creates a new
file system. The file system is automatically  mounted  according
to  the property inherited from the parent.  Creates all the non‐
existing parent datasets. Datasets created in this manner are au‐
tomatically mounted according  to  the  property  inherited  from
their  parent.  Any  property specified on the command line using
the option is ignored. If the target filesystem  already  exists,
the  operation completes successfully.  Newly created file system
is not mounted.  Sets the specified property as  if  the  command
was  invoked  at  the  same time the dataset was created. Any ed‐
itable property can also be set at creation  time.  Multiple  op‐
tions  can be specified. An error results if the same property is
specified in multiple options.  Creates a  volume  of  the  given
size.  The  volume  is exported as a block device in where is the
name of the volume in the namespace. The size represents the log‐
ical size as exported by the device. By default, a reservation of
equal size is created.  is automatically rounded up to the  near‐
est  128  Kbytes to ensure that the volume has an integral number
of blocks regardless  of  Creates  all  the  non‐existing  parent
datasets.  Datasets  created  in  this  manner  are automatically
mounted according to the property inherited  from  their  parent.
Any  property  specified  on the command line using the option is
ignored. If the target filesystem already exists,  the  operation
completes successfully.  Creates a sparse volume with no reserva‐
tion.  See  in the section for more information about sparse vol‐
umes.  Equivalent to If this option is specified  in  conjunction
with  the  resulting  behavior  is undefined.  Sets the specified
property as if the command was  invoked  at  the  same  time  the
dataset  was  created.  Any  editable property can also be set at
creation time. Multiple options can be specified.  An  error  re‐
sults if the same property is specified in multiple options.  De‐
stroys  the  given  dataset. By default, the command unshares any
file systems that are currently shared, unmounts any file systems
that are currently mounted, and refuses to destroy a dataset that
has active dependents (children or clones).  Recursively  destroy
all  children.   Recursively  destroy  all  dependents, including
cloned file systems outside the target hierarchy.  Force  an  un‐
mount  of  any file systems using the command. This option has no
effect on non‐file systems or unmounted file systems.  Do a  dry‐
run  ("No‐op")  deletion. No data will be deleted. This is useful
in conjunction with the or flags to determine what data would  be
deleted.   Print  machine‐parsable  verbose information about the
deleted data.  Print verbose information about the deleted  data.
Extreme  care should be taken when applying either the or the op‐
tions, as they can destroy large portions of a pool and cause un‐
expected behavior for mounted file systems  in  use.   The  given
snapshots  are  destroyed  immediately if and only if the command
without the option would have destroyed it.  Such  immediate  de‐
struction would occur, for example, if the snapshot had no clones
and  the user‐initiated reference count were zero.  If a snapshot
does not qualify for immediate destruction, it is marked for  de‐
ferred  deletion.  In  this state, it exists as a usable, visible
snapshot until both of the preconditions listed above are met, at
which point it is destroyed.  An inclusive range of snapshots may
be specified by separating the first and last  snapshots  with  a
percent  sign  The first and/or last snapshots may be left blank,
in which case the filesystem’s oldest or newest snapshot will  be
implied.  Multiple snapshots (or ranges of snapshots) of the same
filesystem  or  volume may be specified in a comma‐separated list
of snapshots.  Only the snapshot’s short name (the part after the
should be specified when using a range or comma‐separated list to
identify multiple snapshots.  Destroy (or mark for deferred dele‐
tion) all snapshots with this name in  descendent  file  systems.
Recursively  destroy all clones of these snapshots, including the
clones, snapshots, and children.  If this flag is specified,  the
flag  will  have  no effect.  Do a dry‐run ("No‐op") deletion. No
data will be deleted. This is useful in conjunction with  the  or
flags  to  determine  what data would be deleted.  Print machine‐
parsable verbose information about the deleted data.  Print  ver‐
bose  information  about  the deleted data.  Defer snapshot dele‐
tion.  Extreme care should be taken when applying either  the  or
the  options,  as  they  can destroy large portions of a pool and
cause unexpected behavior for mounted file systems in  use.   The
given  bookmark  is  destroyed.  Creates snapshots with the given
names. All previous modifications by successful system  calls  to
the  file  system are part of the snapshots.  Snapshots are taken
atomically, so that all snapshots correspond to the  same  moment
in  time.  See the section for details.  Recursively create snap‐
shots of all descendent datasets Sets the specified property; see
for details.  Roll back the given dataset to a previous snapshot.
When a dataset is rolled back, all data that  has  changed  since
the  snapshot  is discarded, and the dataset reverts to the state
at the time of the snapshot. By default, the command  refuses  to
roll  back to a snapshot other than the most recent one. In order
to do so, all intermediate snapshots and bookmarks  must  be  de‐
stroyed by specifying the option.  The options do not recursively
destroy the child snapshots of a recursive snapshot.  Only direct
snapshots  of the specified filesystem are destroyed by either of
these options.  To completely roll back a recursive snapshot, you
must rollback the individual child snapshots.  Destroy any  snap‐
shots  and bookmarks more recent than the one specified.  Destroy
any more recent snapshots and bookmarks, as well as any clones of
those snapshots.  Used with the option to force an unmount of any
clone file systems that are to be destroyed.  Creates a clone  of
the  given  snapshot.  See  the  section  for details. The target
dataset can be located anywhere in the hierarchy, and is  created
as  the  same type as the original.  Creates all the non‐existing
parent datasets. Datasets created in this  manner  are  automati‐
cally mounted according to the property inherited from their par‐
ent. If the target filesystem or volume already exists, the oper‐
ation  completes  successfully.  Sets the specified property; see
for details.  Promotes a clone file system to no longer be depen‐
dent on its "origin" snapshot. This makes it possible to  destroy
the  file  system that the clone was created from. The clone par‐
ent‐child dependency relationship is reversed, so that the origin
file system becomes a clone of the specified  file  system.   The
snapshot  that  was  cloned,  and  any snapshots previous to this
snapshot, are now owned by the promoted clone. The space they use
moves from the origin file  system  to  the  promoted  clone,  so
enough space must be available to accommodate these snapshots. No
new space is consumed by this operation, but the space accounting
is  adjusted.  The  promoted  clone must not have any conflicting
snapshot names of its own. The subcommand can be used  to  rename
any  conflicting  snapshots.   Renames the given dataset. The new
target can be located anywhere in the hierarchy, with the  excep‐
tion  of snapshots. Snapshots can only be renamed within the par‐
ent file system or volume. When renaming a snapshot,  the  parent
file system of the snapshot does not need to be specified as part
of  the  second  argument.  Renamed  file systems can inherit new
mount points, in which case they are unmounted and  remounted  at
the   new  mount  point.   Creates  all  the  nonexistent  parent
datasets. Datasets  created  in  this  manner  are  automatically
mounted  according  to  the property inherited from their parent.
Do not remount file systems during rename.  If  a  file  system’s
property  is  set to or file system is not unmounted even if this
option is not given.  Force unmount any filesystems that need  to
be unmounted in the process.  This flag has no effect if used to‐
gether  with  the  flag.  Recursively rename the snapshots of all
descendent datasets. Snapshots are the only dataset that  can  be
renamed  recursively.   Lists  the  property  information for the
given datasets in tabular form. If specified, you can list  prop‐
erty  information  by the absolute pathname or the relative path‐
name. By default, all file systems  and  volumes  are  displayed.
Snapshots  are  displayed  if the property is (the default is The
following fields are displayed, Recursively display any  children
of  the  dataset  on  the  command line.  Recursively display any
children of the dataset, limiting the recursion  to  A  depth  of
will  display only the dataset and its direct children.  Used for
scripting mode. Do not print headers and  separate  fields  by  a
single  tab instead of arbitrary white space.  Display numbers in
parsable (exact) values.  A comma‐separated list of properties to
display. The property must be: One of the properties described in
the section A user property The value to display the dataset name
The value to display space usage properties on file  systems  and
volumes. This is a shortcut for specifying syntax.  A comma‐sepa‐
rated  list  of  types  to  display, where is one of For example,
specifying displays only snapshots.  A property for  sorting  the
output  by  column  in  ascending order based on the value of the
property. The property must be one of the properties described in
the section, or the special value to sort by  the  dataset  name.
Multiple  properties  can be specified at one time using multiple
property options. Multiple options are  evaluated  from  left  to
right in decreasing order of importance.  The following is a list
of sorting criteria: Numeric types sort in numeric order.  String
types  sort in alphabetical order.  Types inappropriate for a row
sort that row to the literal bottom, regardless of the  specified
ordering.   If  no sorting options are specified the existing be‐
havior of is preserved.  Same as the option, but sorts  by  prop‐
erty  in  descending order.  Sets the property or list of proper‐
ties to the given value(s) for each dataset.  Only  some  proper‐
ties  can be edited. See the "Properties" section for more infor‐
mation on what properties can be set and acceptable  values.  Nu‐
meric  values  can  be  specified as exact values, or in a human‐
readable form with a suffix of (for bytes, kilobytes,  megabytes,
gigabytes, terabytes, petabytes, exabytes, or zettabytes, respec‐
tively). User properties can be set on snapshots. For more infor‐
mation,  see  the  section.   Displays  properties  for the given
datasets. If no datasets are specified, then the command displays
properties for all datasets on the system. For each property, the
following columns are displayed: Dataset name Property name Prop‐
erty value Property source. Can either be local, default,  tempo‐
rary,  inherited,  received, or none (‐).  All columns except the
column are displayed by default. The columns to  display  can  be
specified  by  using the option. This command takes a comma‐sepa‐
rated list of properties as described in the and  sections.   The
special value can be used to display all properties that apply to
the  given dataset’s type (filesystem, volume, snapshot, or book‐
mark).  Recursively display properties for any children.   Recur‐
sively  display  any children of the dataset, limiting the recur‐
sion to A depth of will display only the dataset and  its  direct
children.   Display  output  in  a  form  more  easily  parsed by
scripts. Any headers are omitted, and fields are explicitly sepa‐
rated by a single tab instead of an arbitrary  amount  of  space.
Display  numbers  in  parsable (exact) values.  A comma‐separated
list of columns to display. Supported values are  Default  values
are The keyword specifies all columns.  A comma‐separated list of
types  to  display,  where is one of For example, specifying dis‐
plays only snapshots.  A comma‐separated list of sources to  dis‐
play.  Those  properties coming from a source other than those in
this list are ignored. Each source must be one of the  following:
The default value is all sources.  Clears the specified property,
causing  it to be inherited from an ancestor, restored to default
if no ancestor has the property set, or with the option  reverted
to the received value if one exists.  See the section for a list‐
ing of default values, and details on which properties can be in‐
herited.   Recursively  inherit  the given property for all chil‐
dren.  Revert the property to the received value if  one  exists;
otherwise operate as if the option was not specified.  Displays a
list  of file systems that are not the most recent version.  Dis‐
plays filesystem versions supported by the current software.  The
current  filesystem  version  and all previous supported versions
are displayed, along with an explanation of the features provided
with each version.  Upgrades file systems to a new  on‐disk  ver‐
sion.  Once  this is done, the file systems will no longer be ac‐
cessible on systems  running  older  versions  of  the  software.
streams generated from new snapshots of these file systems cannot
be  accessed  on  systems running older versions of the software.
In general, the file system version is independent  of  the  pool
version.  See for information on the command.  In some cases, the
file system version and the pool version are interrelated and the
pool version must be upgraded before the file system version  can
be  upgraded.   Upgrade the specified file system and all descen‐
dent file systems.  Upgrade to the specified If the flag  is  not
specified, this command upgrades to the most recent version. This
option  can only be used to increase the version number, and only
up to the most recent version supported by  this  software.   Up‐
grade all file systems on all imported pools.  Upgrade the speci‐
fied  file  system.   Displays  space consumed by, and quotas on,
each user in the specified filesystem or  snapshot.  This  corre‐
sponds  to  the  and  properties.   Print  numeric  ID instead of
user/group name.  Do not print headers, use tab‐delimited output.
Use exact (parsable) numeric output.  Display only the  specified
fields  from  the  following  set:  The default is to display all
fields.  Sort output by this field. The and flags may  be  speci‐
fied  multiple times to sort first by one field, then by another.
The default is Sort by this field in  reverse  order.  See  Print
only  the  specified types from the following set: The default is
The default can be changed to include group types.  Translate SID
to POSIX ID. This flag currently has no effect on Displays  space
consumed  by, and quotas on, each group in the specified filesys‐
tem or snapshot. This subcommand is identical to except that  the
default  types to display are Displays all file systems currently
mounted.  Mounts file systems.  Report mount  progress.   Perform
an  overlay  mount.  Overlay  mounts  are not supported on An op‐
tional, comma‐separated list of mount options to use  temporarily
for  the  duration  of  the  mount.  See the section for details.
Mount all available file systems.  This command may  be  executed
on  system startup by For more information, see variable in Mount
the specified filesystem.  Unmounts currently mounted  file  sys‐
tems.   Forcefully  unmount  the  file system, even if it is cur‐
rently in use.  Unmount all available file systems.  Unmount  the
specified  filesystem.  The command can also be given a path to a
file system mount point on the system.  Shares file systems  that
have  the  property  set.   Share  all file systems that have the
property set.  This command may be executed on system startup  by
For  more  information,  see  variable  in  Share  the  specified
filesystem according to the property.  File  systems  are  shared
when  the  property  is set.  Unshares file systems that have the
property set.  Unshares file systems that have the property  set.
This  command  may be executed on system shutdown by For more in‐
formation, see variable in Unshare the specified filesystem.  The
command  can  also be given a path to a file system shared on the
system.  Creates a bookmark of  the  given  snapshot.   Bookmarks
mark  the point in time when the snapshot was created, and can be
used as the incremental source for a command.  This feature  must
be  enabled to be used.  See for details on ZFS feature flags and
the feature.  Creates a stream representation of the  last  argu‐
ment  (not  part  of  or which is written to standard output. The
output can be redirected to a file or to a different system  (for
example,  using By default, a full stream is generated.  Generate
an incremental stream from the first to the second The  incremen‐
tal source can be specified as the last component of the snapshot
name and it is assumed to be from the same file system as the in‐
cremental  target.  If the destination is a clone, the source may
be the origin snapshot, which must be fully specified (for  exam‐
ple, not just Generate a stream package that sends all intermedi‐
ary  snapshots from the first to the second For example, is simi‐
lar to The incremental source may be specified as  with  the  op‐
tion.   Generate  a replication stream package, which will repli‐
cate the specified filesystem, and all descendent  file  systems,
up  to  the  named snapshot. When received, all properties, snap‐
shots, descendent file systems, and clones are preserved.  If the
or flags are used in conjunction with the  flag,  an  incremental
replication  stream  is  generated. The current values of proper‐
ties, and current snapshot and file system names are set when the
stream is received. If the flag is specified when this stream  is
received,  snapshots  and  file  systems that do not exist on the
sending side are  destroyed.   Generate  a  deduplicated  stream.
Blocks  which  would  have  been  sent multiple times in the send
stream will only be sent once.  The receiving  system  must  also
support this feature to receive a deduplicated stream.  This flag
can be used regardless of the dataset’s property, but performance
will be much better if the filesystem uses a dedup‐capable check‐
sum  (eg.  Generate a stream which may contain blocks larger than
128KB.  This flag has no effect if the pool feature is  disabled,
or  if  the  property of this filesystem has never been set above
128KB.  The receiving system must have the pool  feature  enabled
as  well.   See for details on ZFS feature flags and the feature.
Generate a more compact stream by  using  WRITE_EMBEDDED  records
for  blocks  which  are stored more compactly on disk by the pool
feature.  This flag has no effect if  the  feature  is  disabled.
The  receiving system must have the feature enabled.  If the fea‐
ture is active on the sending system, then the  receiving  system
must  have  that feature enabled as well.  See for details on ZFS
feature flags and the feature.  Include the dataset’s  properties
in  the  stream. This flag is implicit when is specified. The re‐
ceiving system must also support  this  feature.   Do  a  dry‐run
("No‐op")  send.   Do not generate any actual send data.  This is
useful in conjunction with the or flags to  determine  what  data
will  be  sent.  In this case, the verbose output will be written
to standard output (contrast with a non‐dry‐run, where the stream
is written to standard output and  the  verbose  output  goes  to
standard  error).   Print  machine‐parsable  verbose  information
about the stream package generated.   Print  verbose  information
about  the stream package generated.  This information includes a
per‐second report of how much data has been sent.  The format  of
the stream is committed. You will be able to receive your streams
on  future  versions of Generate a send stream, which may be of a
filesystem, and may be incremental from a bookmark.  If the  des‐
tination  is  a filesystem or volume, the pool must be read‐only,
or the filesystem must not be mounted.  When the stream generated
from a filesystem or volume is  received,  the  default  snapshot
name  will be Generate an incremental send stream.  The incremen‐
tal source must be an earlier snapshot in the destination’s  his‐
tory.   It  will  commonly be an earlier snapshot in the destina‐
tion’s filesystem, in which case it can be specified as the  last
component  of  the name If the incremental target is a clone, the
incremental source can be the  origin  snapshot,  or  an  earlier
snapshot in the origin’s filesystem, or the origin’s origin, etc.
Generate  a  stream  which  may contain blocks larger than 128KB.
This flag has no effect if the pool feature is  disabled,  or  if
the  property  of this filesystem has never been set above 128KB.
The receiving system must have the pool feature enabled as  well.
See for details on ZFS feature flags and the feature.  Generate a
more  compact  stream  by using WRITE_EMBEDDED records for blocks
which are stored more compactly on  disk  by  the  pool  feature.
This  flag has no effect if the feature is disabled.  The receiv‐
ing system must have the feature enabled.  If the feature is  ac‐
tive  on  the sending system, then the receiving system must have
that feature enabled as well.  See for  details  on  ZFS  feature
flags  and  the  feature.  Creates a send stream which resumes an
interrupted receive.  The is the value of this  property  on  the
filesystem or volume that was being received into.  See the docu‐
mentation  for  for  more details.  Creates a snapshot whose con‐
tents are as specified in the stream provided on standard  input.
If  a  full stream is received, then a new file system is created
as well. Streams are created using the subcommand, which  by  de‐
fault  creates  a full stream.  can be used as an alias for If an
incremental stream is received, then the destination file  system
must  already  exist, and its most recent snapshot must match the
incremental stream’s source. For the destination device  link  is
destroyed  and recreated, which means the cannot be accessed dur‐
ing the operation.  When a snapshot  replication  package  stream
that is generated by using the command is received, any snapshots
that  do not exist on the sending location are destroyed by using
the command.  The name of the snapshot (and  file  system,  if  a
full  stream is received) that this subcommand creates depends on
the argument type and the or option.  If the argument is a  snap‐
shot  name,  the  specified is created. If the argument is a file
system or volume name, a snapshot with the same name as the  sent
snapshot  is  created within the specified or If the or option is
specified, the snapshot name is determined by appending the  sent
snapshot’s  name to the specified If the option is specified, all
but the pool name of the sent snapshot path is appended (for  ex‐
ample,  appended  from  sent snapshot and if the option is speci‐
fied, only the tail of the sent snapshot path  is  appended  (for
example, appended from sent snapshot In the case of any file sys‐
tems  needed  to replicate the path of the sent snapshot are cre‐
ated within the specified file system.  Use the full  sent  snap‐
shot path without the first element (without pool name) to deter‐
mine  the  name of the new snapshot as described in the paragraph
above.  Use only the last element of the sent  snapshot  path  to
determine  the name of the new snapshot as described in the para‐
graph above.  File system that is associated  with  the  received
stream  is  not  mounted.   Print  verbose  information about the
stream and the time required to perform  the  receive  operation.
Do  not  actually  receive the stream. This can be useful in con‐
junction with the option to verify the name the receive operation
would use.  Forces the stream to be received as a  clone  of  the
given  snapshot.   If the stream is a full send stream, this will
create the filesystem described by the stream as a clone  of  the
specified  snapshot. Which snapshot was specified will not affect
the success or failure of the receive, as long  as  the  snapshot
does exist.  If the stream is an incremental send stream, all the
normal  verification  will be performed.  Force a rollback of the
file system to the most recent snapshot before performing the re‐
ceive operation. If receiving an incremental  replication  stream
(for example, one generated by destroy snapshots and file systems
that  do not exist on the sending side.  If the receive is inter‐
rupted, save the partially received state, rather  than  deleting
it.   Interruption  may  be  due  to premature termination of the
stream if the stream is being read over a  network  connection  a
checksum  error in the stream, termination of the process, or un‐
clean shutdown of the system.  The receive can be resumed with  a
stream generated by where the is the value of the property of the
filesystem  or  volume which is received into.  To use this flag,
the storage pool must have the feature enabled.  See for  details
on  ZFS  feature  flags.  Abort an interrupted deleting its saved
partially received state.  Displays permissions  that  have  been
delegated  on  the  specified filesystem or volume. See the other
forms of for more information.  Delegates administration  permis‐
sion  for the file systems to non‐privileged users.  Specifies to
whom the permissions are  delegated.  Multiple  entities  can  be
specified  as  a  comma‐separated list. If neither of the options
are specified, then the argument is interpreted preferentially as
the keyword then as a user name, and lastly as a group  name.  To
specify  a  user  or group named use the or options. To specify a
group with the same name as a user, use  the  option.   Specifies
that the permissions be delegated to The permissions to delegate.
Multiple  permissions may be specified as a comma‐separated list.
Permission names are the same as subcommand and  property  names.
See the property list below. Property set names, which begin with
an  at  sign  may  be  specified. See the form below for details.
Specifies where the permissions are delegated. If neither of  the
options  are specified, or both are, then the permissions are al‐
lowed for the file system or volume, and all of its  descendents.
If  only  the  option is used, then is allowed "locally" only for
the specified file system.  If only the option is used,  then  is
allowed  only  for  the descendent file systems.  Permissions are
generally the ability to use a subcommand or change  a  property.
The following permissions are available: also have the permission
that  is being allowed also have the ’create’ ability and ’mount’
ability in the origin file system object number, and the  ability
to create snapshots necessary to ’zfs diff’ also have the ’mount’
and  ’promote’ ability in the origin file system releasing a user
hold which might destroy the snapshot also have the  ’mount’  and
’create’  ability in the new parent sharing file systems over the
protocol Sets "create time" permissions.  These  permissions  are
granted  (locally) to the creator of any newly‐created descendent
file system.  Defines or adds permissions to  a  permission  set.
The set can be used by other commands for the specified file sys‐
tem  and  its  descendents.  Sets  are  evaluated dynamically, so
changes to a set are immediately reflected. Permission sets  fol‐
low  the  same  naming  restrictions as ZFS file systems, but the
name must begin with an "at sign" and can  be  no  more  than  64
characters  long.  Removes permissions that were granted with the
command. No permissions are explicitly denied, so  other  permis‐
sions granted are still in effect. For example, if the permission
is  granted by an ancestor. If no permissions are specified, then
all permissions for the specified are removed. Specifying  option
not all permissions for every user and group. See the command for
a description of the options.  Recursively remove the permissions
from  this  file system and all descendents.  Removes permissions
from a permission set. If no permissions are specified, then  all
permissions  are removed, thus removing the set entirely.  Adds a
single reference, named with the argument, to the specified snap‐
shot or snapshots. Each snapshot has its own tag  namespace,  and
tags  must  be  unique  within that space.  If a hold exists on a
snapshot, attempts to destroy that snapshot by using the  command
returns  Specifies  that a hold with the given tag is applied re‐
cursively to the snapshots of all descendent file systems.  Lists
all existing user references for the given dataset  or  datasets.
Used for scripting mode. Do not print headers and separate fields
by  a  single tab instead of arbitrary white space.  Display num‐
bers in parsable (exact) values.  Lists the holds that are set on
the descendent snapshots of the named datasets or  snapshots,  in
addition  to  listing  the  holds on the named snapshots, if any.
Recursively display any holds on the named snapshots, or  descen‐
dent  snapshots  of the named datasets or snapshots, limiting the
recursion to Removes a single reference, named with the argument,
from the specified snapshot or snapshots. The  tag  must  already
exist  for  each  snapshot.  Recursively releases a hold with the
given tag on the snapshots of all descendent file systems.   Dis‐
play  the difference between a snapshot of a given filesystem and
another snapshot of that filesystem from a later time or the cur‐
rent contents of the filesystem.  The first column is a character
indicating the type of change, the other columns  indicate  path‐
name, new pathname change in link count, and optionally file type
and/or  change time.  The types of change are: Display an indica‐
tion of the type of file, in a manner similar to  the  option  of
Give more parsable tab‐separated output, without header lines and
without  arrows.   Display  the  path’s  inode change time as the
first column of output.  The following exit values are  returned:
Successful  completion.  An error occurred.  Invalid command line
options were specified.  The following  commands  create  a  file
system  named  and a file system named The mount point is set for
the parent file system, and is  automatically  inherited  by  the
child  file  system.   The  following  command creates a snapshot
named This snapshot is mounted on demand in the directory at  the
root of the file system.  The following command creates snapshots
named of and all of its descendent file systems. Each snapshot is
mounted  on  demand in the directory at the root of its file sys‐
tem. The second command destroys  the  newly  created  snapshots.
The  following command disables the property for all file systems
under The next command explicitly enables for The following  com‐
mand  lists  all  active  file systems and volumes in the system.
Snapshots are displayed if the property is The default is See for
more information on pool properties.
   NAME                      USED  AVAIL  REFER  MOUNTPOINT
   pool                      450K   457G    18K  /pool
   pool/home                 315K   457G    21K  /home
   pool/home/anne             18K   457G    18K  /home/anne
   pool/home/bob             276K   457G    276K   /home/bob  The
following  command  sets  a  quota of 50 Gbytes for The following
command  lists  all  properties   for   NAME             PROPERTY
VALUE                        SOURCE      pool/home/bob       type
filesystem             ‐ pool/home/bob  creation              Tue
Jul 21 15:53 2009   ‐  pool/home/bob   used                   21K
‐  pool/home/bob   available             20.0G                  ‐
pool/home/bob   referenced             21K                      ‐
pool/home/bob    compressratio          1.00x                   ‐
pool/home/bob   mounted                yes                      ‐
pool/home/bob  quota                 20G                    local
pool/home/bob   reservation            none                   de‐
fault       pool/home/bob        recordsize                  128K
default     pool/home/bob      mountpoint               /home/bob
default      pool/home/bob        sharenfs                    off
default       pool/home/bob        checksum                    on
default       pool/home/bob        compression                 on
local        pool/home/bob         atime                       on
default       pool/home/bob        devices                     on
default       pool/home/bob        exec                        on
default      pool/home/bob       filesystem_limit            none
default       pool/home/bob        setuid                      on
default      pool/home/bob        readonly                    off
default      pool/home/bob       snapdir                   hidden
default      pool/home/bob       snapshot_limit              none
default      pool/home/bob      aclmode                   discard
default    pool/home/bob     aclinherit                restricted
default       pool/home/bob        canmount                    on
default       pool/home/bob        xattr                       on
default        pool/home/bob        copies                      1
default       pool/home/bob        version                      5
‐  pool/home/bob   utf8only              off                    ‐
pool/home/bob   normalization          none                     ‐
pool/home/bob    casesensitivity        sensitive               ‐
pool/home/bob  vscan                  off                     de‐
fault        pool/home/bob        nbmand                      off
default      pool/home/bob        sharesmb                    off
default       pool/home/bob       refquota                   none
default      pool/home/bob       refreservation              none
default       pool/home/bob        primarycache               all
default      pool/home/bob        secondarycache              all
default        pool/home/bob        usedbysnapshots             0
‐ pool/home/bob  usedbydataset          21K                     ‐
pool/home/bob    usedbychildren         0                       ‐
pool/home/bob   usedbyrefreservation   0                        ‐
pool/home/bob   logbias                latency                de‐
fault       pool/home/bob        dedup                        off
default                  pool/home/bob                   mlslabel
‐ pool/home/bob  sync                  standard               de‐
fault      pool/home/bob        refcompressratio            1.00x
‐  The  following  command  gets a single property value.  on The
following command lists all properties with  local  settings  for
NAME            PROPERTY               VALUE pool/home/bob  quota
20G pool/home/bob  compression           on The following command
reverts the contents of to the snapshot named deleting all inter‐
mediate snapshots.  The following command creates a writable file
system whose initial contents are the same as The following  com‐
mands  illustrate  how  to test out changes to a file system, and
then replace the original file system with the changed one, using
clones, clone promotion, and renaming:  Populate  with  data  and
continue  with  the  following  commands: Now make changes to and
continue with the following commands: Once the legacy version  is
no  longer  needed,  it  can be destroyed.  The following command
causes and to inherit the property from their parent.   The  fol‐
lowing commands send a full stream and then an incremental stream
to  a remote machine, restoring them into and respectively.  must
contain the file system and must not initially contain Using  the
Option  The  following command sends a full stream of to a remote
machine, receiving it into The portion of the received snapshot’s
name is determined from the name of the sent snapshot.  must con‐
tain the file system If does not exist, it is created as an empty
file system.  The following example sets the  user‐defined  prop‐
erty  for a dataset.  The following example shows how to maintain
a history of snapshots with a consistent naming scheme. To keep a
week’s worth of snapshots, the user destroys the oldest snapshot,
renames the remaining snapshots, and then creates a new snapshot,
as follows: Setting Property Options on a  ZFS  File  System  The
following  command  shows  how  to set property options to enable
root access for a specific network on the file system.  The  con‐
tents  of  the  property are valid options.  Another way to write
this command with the same result is:  Delegating  Administration
Permissions  on  a Dataset The following example shows how to set
permissions so that user can create,  destroy,  mount,  and  take
snapshots on The permissions on are also displayed.  ‐‐‐‐ Permis‐
sions  on  tank/cindys ‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐ Lo‐
cal+Descendent permissions:
        user cindys create,destroy,mount,snapshot  The  following
example  shows  how  to  grant anyone in the group to create file
systems in This syntax also allows staff members to destroy their
own file systems, but not destroy anyone else’s file system.  The
permissions   on   are   also  displayed.   ‐‐‐‐  Permissions  on
tank/users   ‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐   Permission
sets:
        destroy Local+Descendent permissions:
        group  staff create,mount Defining and Granting a Permis‐
sion Set on a Dataset The following example shows how  to  define
and grant a permission set on the file system. The permissions on
are    also    displayed.    ‐‐‐‐   Permissions   on   tank/users
‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐ Permission sets:
        @pset create,destroy,mount,snapshot Local+Descendent per‐
missions:
        group staff @pset The following example  shows  to  grant
the  ability  to  set quotas and reservations on the file system.
The permissions on  are  also  displayed.   ‐‐‐‐  Permissions  on
users/home  ‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐ Local+Descen‐
dent permissions:
        user cindys quota,reservation NAME               PROPERTY
VALUE   SOURCE  users/home/marks  quota     10G    local The fol‐
lowing example shows how to remove the snapshot  permission  from
the  group  on  the file system. The permissions on are also dis‐
played.        ‐‐‐‐       Permissions        on        tank/users
‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐ Permission sets:
        @pset create,destroy,mount,snapshot Local+Descendent per‐
missions:
        group  staff @pset The following example shows how to see
what has changed between a prior snapshot of a  ZFS  Dataset  and
its  current state.  The option is used to indicate type informa‐
tion for the  files  affected.   M        /        /tank/test/  M
F            /tank/test/linked           (+1)      R            F
/tank/test/oldname    ‐>     /tank/test/newname     ‐           F
/tank/test/deleted  +        F       /tank/test/created M       F
/tank/test/modified This manual page is a reimplementation of the
manual page modified and customized for and  licensed  under  the
Common Development and Distribution License The implementation of
this manual page was initially written by
