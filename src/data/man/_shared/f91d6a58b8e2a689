_g_e_t_p_w(3)                   Library Functions Manual                   _g_e_t_p_w(3)

NNAAMMEE
       getpw - reconstruct password line entry

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##ddeeffiinnee __GGNNUU__SSOOUURRCCEE             /* See feature_test_macros(7) */
       ##iinncclluuddee <<ssyyss//ttyyppeess..hh>>
       ##iinncclluuddee <<ppwwdd..hh>>

       [[[[ddeepprreeccaatteedd]]]] iinntt ggeettppww((uuiidd__tt _u_i_d,, cchhaarr **_b_u_f));;

DDEESSCCRRIIPPTTIIOONN
       The ggeettppww() function reconstructs the password line entry for the given
       user  ID _u_i_d in the buffer _b_u_f.  The returned buffer contains a line of
       format

           nnaammee::ppaasssswwdd::uuiidd::ggiidd::ggeeccooss::ddiirr::sshheellll

       The _p_a_s_s_w_d structure is defined in _<_p_w_d_._h_> as follows:

           struct passwd {
               char   *pw_name;       /* username */
               char   *pw_passwd;     /* user password */
               uid_t   pw_uid;        /* user ID */
               gid_t   pw_gid;        /* group ID */
               char   *pw_gecos;      /* user information */
               char   *pw_dir;        /* home directory */
               char   *pw_shell;      /* shell program */
           };

       For more information about the fields of this structure, see ppaasssswwdd(5).

RREETTUURRNN VVAALLUUEE
       The ggeettppww() function returns 0 on success; on error, it returns -1, and
       _e_r_r_n_o is set to indicate the error.

       If _u_i_d is not found in the password database, ggeettppww() returns -1,  sets
       _e_r_r_n_o to 0, and leaves _b_u_f unchanged.

EERRRROORRSS
       00 or EENNOOEENNTT
              No user corresponding to _u_i_d.

       EEIINNVVAALL _b_u_f is NULL.

       EENNOOMMEEMM Insufficient memory to allocate _p_a_s_s_w_d structure.

FFIILLEESS
       _/_e_t_c_/_p_a_s_s_w_d
              password database file

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       ggeettppww() T}   Thread safety  MT‐Safe locale

SSTTAANNDDAARRDDSS
       None.

HHIISSTTOORRYY
       SVr2.

BBUUGGSS
       The ggeettppww() function is dangerous as it may overflow the provided
       buffer _b_u_f.  It is obsoleted by ggeettppwwuuiidd(3).

SSEEEE AALLSSOO
       eennddppwweenntt(3), ffggeettppwweenntt(3), ggeettppwweenntt(3), ggeettppwwnnaamm(3), ggeettppwwuuiidd(3),
       ppuuttppwweenntt(3), sseettppwweenntt(3), ppaasssswwdd(5)

Linux man‐pages (unreleased)      2025‐09‐21                          _g_e_t_p_w(3)
