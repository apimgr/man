The system call atomically performs the array of operations indi‐
cated by on the semaphore set indicated by The length of is indi‐
cated  by Each operation is encoded in a which is defined as fol‐
lows: struct sembuf {
        u_short sem_num;        /* semaphore # */
        short   sem_op;         /* semaphore operation */
        short   sem_flg;        /* operation flags */ }; For each
element in and determine an operation to be  performed  on  sema‐
phore  number  in the set.  The values and may be into the member
in order to modify the behavior of the given operation.  The  op‐
eration performed depends as follows on the value of When is pos‐
itive and the process has alter permission, the semaphore’s value
is incremented by value.  If is specified, the semaphore’s adjust
on exit value is decremented by value.  A positive value for gen‐
erally  corresponds  to a process releasing a resource associated
with the semaphore.   The  behavior  when  is  negative  and  the
process has alter permission, depends on the current value of the
semaphore:  If the current value of the semaphore is greater than
or equal to the absolute value of then the value  is  decremented
by  the absolute value of If is specified, the semaphore’s adjust
on exit value is incremented by the absolute value of If the cur‐
rent value of the semaphore is less than the  absolute  value  of
one  of the following happens: If was specified, then returns im‐
mediately with a return value of Otherwise, the  calling  process
is  put  to sleep until one of the following conditions is satis‐
fied: Some other process removes the semaphore with the option of
In this case, returns immediately with  a  return  value  of  The
process  receives  a  signal that is to be caught.  In this case,
the process will resume execution as defined by  The  semaphore’s
value is greater than or equal to the absolute value of When this
condition  becomes  true, the semaphore’s value is decremented by
the absolute value of the semaphore’s adjust on exit value is in‐
cremented by the absolute value of A negative value for generally
means that a process is waiting for a resource to  become  avail‐
able.   When  is zero and the process has read permission, one of
the following will occur: If the current value of  the  semaphore
is  equal to zero then can return immediately.  If was specified,
then returns immediately with a return value  of  Otherwise,  the
calling process is put to sleep until one of the following condi‐
tions is satisfied: Some other process removes the semaphore with
the  option  of  In  this case, returns immediately with a return
value of The process receives a signal that is to be caught.   In
this  case,  the  process will resume execution as defined by The
semaphore’s value becomes zero.  For each semaphore a process has
in use, the kernel maintains an value,  as  alluded  to  earlier.
When  a  process  exits, either voluntarily or involuntarily, the
adjust on exit value for each semaphore is  added  to  the  sema‐
phore’s value.  This can be used to insure that a resource is re‐
leased  if  a  process  terminates unexpectedly.  The system call
will fail if: Too many operations are specified.   Permission  is
denied,  due  to a mismatch between the operation and the mode of
the semaphore set.  The semaphore’s value  would  result  in  the
process being put to sleep and is specified.  is not in the range
of  valid  semaphores  for the set.  The semaphore set is removed
from the system.  The system call is interrupted by a signal.  No
semaphore set corresponds to or the process would exceed the sys‐
tem‐defined limit for the number of per‐process structures.   The
system  pool is full.  The requested operation would cause either
the semaphore’s current value or its adjust‐on‐exit value to  ex‐
ceed the system‐imposed limits.  The include files and are neces‐
sary.   The  system call may block waiting for memory even if was
specified.
