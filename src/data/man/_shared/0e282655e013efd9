BUS_CONFIG_INTR(9)       BSD Kernel Developer's Manual      BUS_CONFIG_INTR(9)

NNAAMMEE
     BBUUSS__CCOONNFFIIGG__IINNTTRR, bbuuss__ccoonnffiigg__iinnttrr — configure interrupt polarity and trig‐
     ger mode

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ppaarraamm..hh>>
     ##iinncclluuddee <<ssyyss//bbuuss..hh>>

     _i_n_t
     BBUUSS__CCOONNFFIIGG__IINNTTRR(_d_e_v_i_c_e___t _b_u_s, _d_e_v_i_c_e___t _d_e_v, _i_n_t _i_r_q,
         _e_n_u_m _i_n_t_r___t_r_i_g_g_e_r _t_r_i_g, _e_n_u_m _i_n_t_r___p_o_l_a_r_i_t_y _p_o_l);

     _i_n_t
     bbuuss__ccoonnffiigg__iinnttrr(_d_e_v_i_c_e___t _d_e_v, _i_n_t _i_r_q, _e_n_u_m _i_n_t_r___t_r_i_g_g_e_r _t_r_i_g,
         _e_n_u_m _i_n_t_r___p_o_l_a_r_i_t_y _p_o_l);

DDEESSCCRRIIPPTTIIOONN
     The BBUUSS__CCOONNFFIIGG__IINNTTRR() method allows bus or device drivers to provide in‐
     terrupt polarity and trigger mode to parent buses.  This typically bub‐
     bles all the way up to the root bus (e.g. nexus) where the necessary ac‐
     tions are taken to actually program the hardware.  Since the
     BBUUSS__CCOONNFFIIGG__IINNTTRR() method takes an interrupt number, it is assumed but not
     necessarily required that it is called prior to BUS_SETUP_INTR(9).

     The bbuuss__ccoonnffiigg__iinnttrr() function is a simple wrapper around
     BBUUSS__CCOONNFFIIGG__IINNTTRR().

     The _t_r_i_g argument can be one of:

     INTR_TRIGGER_CONFORM  The interrupt trigger mode is standard for the bus
                           to which the device is attached.

     INTR_TRIGGER_EDGE     The interrupt is edge triggered.  This means that
                           the interrupt is raised by the rising edge of the
                           signal on the interrupt line.  The signal typically
                           reverts to the original state so as to cause a
                           spike.

     INTR_TRIGGER_LEVEL    The interrupt is level triggered.  This means that
                           the interrupt is raised when the signal on the in‐
                           terrupt line transitions and remains unchanged af‐
                           ter that until the interrupt has been serviced, af‐
                           ter which the signal transitions back.

     The _p_o_l argument can be any one of:

     INTR_POLARITY_CONFORM  The interrupt polarity is standard for the bus to
                            which the device is attached.

     INTR_POLARITY_HIGH     The interrupt is activated by a high voltage on
                            the interrupt line.

     INTR_POLARITY_LOW      The interrupt is activated by a low voltage on the
                            interrupt line.

RREETTUURRNN VVAALLUUEESS
     Zero is returned on success, otherwise an appropriate error is returned.

SSEEEE AALLSSOO
     BUS_SETUP_INTR(9), BUS_TEARDOWN_INTR(9), device(9), driver(9)

HHIISSTTOORRYY
     The BBUUSS__CCOONNFFIIGG__IINNTTRR() method first appeared in FreeBSD 5.2.

AAUUTTHHOORRSS
     This manual page was written by Marcel Moolenaar <_m_a_r_c_e_l_@_x_c_l_l_n_t_._n_e_t>.

BSD                            January 16, 2025                            BSD
