MALLOC(3)                BSD Library Functions Manual                MALLOC(3)

NNAAMMEE
     rreeaallllooccff — memory reallocation function

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssttddlliibb..hh>>

     _v_o_i_d _*
     rreeaallllooccff(_v_o_i_d _*_p_t_r, _s_i_z_e___t _s_i_z_e);

DDEESSCCRRIIPPTTIIOONN
     The rreeaallllooccff() function is identical to the rreeaalllloocc() function, except
     that it will free the passed pointer when the requested memory cannot be
     allocated.  This is a FreeBSD specific API designed to ease the problems
     with traditional coding styles for rreeaalllloocc() causing memory leaks in li‐
     braries.

RREETTUURRNN VVAALLUUEESS
     The rreeaallllooccff() function returns a pointer, possibly identical to _p_t_r, to
     the allocated memory if successful; otherwise a NULL pointer is returned,
     and _e_r_r_n_o is set to ENOMEM if the error was the result of an allocation
     failure.  The rreeaallllooccff() function deletes the original buffer when an er‐
     ror occurs.

SSEEEE AALLSSOO
     realloc(3)

HHIISSTTOORRYY
     The rreeaallllooccff() function first appeared in FreeBSD 3.0.

BSD                            January 31, 2010                            BSD
