is  used  to  initialize and clear ext2 file systems before first
use.  Before running the disk must be labeled using builds a file
system on the specified special device basing its defaults on the
information in the disk label.  Typically the defaults  are  rea‐
sonable, however has numerous options to allow the defaults to be
selectively  overridden.  Options with numeric arguments may con‐
tain an optional (case‐insensitive) suffix: Bytes; causes no mod‐
ification.  (Default) Kilo; multiply the argument by 1024.  Mega;
multiply the argument by 1048576.  Giga; multiply the argument by
1073741824.  The following  options  define  the  general  layout
policies.   The block size of the file system, in bytes.  It must
be a power of two.  The smallest allowable size  is  1024  bytes.
The default size depends upon the size of the file system: 1 KB 4
KB  Set  the inode size.  Defaults to 128, and can also be set to
256 for compatibility with ext4.  Create a file system  image  in
The  file  system  size needs to be specified with No attempts to
use or update the disk label will be made.  The fragment size  of
the file system in bytes.  It must be the same with blocksize be‐
cause  the current ext2fs implementation doesn’t support fragmen‐
tation.  Do not require that the file system type listed  in  the
disk  label  is  This specifies the density of inodes in the file
system.  If fewer inodes are desired, a larger number  should  be
used;  to  create  more  inodes a smaller number should be given.
The percentage of space reserved from normal users;  the  minimum
free  space threshold.  The default value used is 5%.  Causes the
file system parameters to be printed out without really  creating
the  file  system.   This  specifies the number of inodes for the
file system.  If both and are specified  then  takes  precedence.
The  default  number  of  inodes  is  calculated from a number of
blocks in the file system.  Select the  filesystem‐format.   this
option  is  primarily used to build root file systems that can be
understood by old or dumb firmwares for bootstrap.  the  default.
Various  extended  (and  sometimes incompatible) features are en‐
abled (though not all features are supported  on  Currently  only
the  following  features  are  supported:  Prepare  some reserved
structures which enable future file system resizing.  Store  file
types  in  directory entries to improve performance.  Prepare su‐
perblock backups for the utility on  not  all  but  sparse  block
groups.  Enable files larger than 2G bytes.  The size of the file
system  in  sectors.  An suffix will be interpreted as the number
of sectors (the default).  All other suffixes are interpreted  as
per  other numeric arguments, except that the number is converted
into sectors by dividing by the sector size (as specified by  af‐
ter  suffix interpretation.  If no is specified then the filesys‐
tem size defaults to that of the partition, or, if is  specified,
the  existing  file.   If  is negative the specified size is sub‐
tracted from the default size (reserving space at the end of  the
partition).   This  controls the amount of information written to
stdout: No output.  Overall size and cylinder group  details.   A
progress  bar  (dots ending at right hand margin).  The first few
super‐block  backup  sector  numbers  are  displayed  before  the
progress bar.  All the super‐block backup sector numbers are dis‐
played  (no  progress  bar).   The default is 3.  If is specified
stops before outputting the progress bar.  This specifies a  vol‐
ume  name  for  the file system.  Pre‐zeros the file system image
created with The following option overrides  the  standard  sizes
for  the disk geometry.  The default value is taken from the disk
label.  Changing this default is useful only when using to  build
a  file  system whose raw image will eventually be used on a dif‐
ferent type of disk than the one on which it is initially created
(for example on a write‐once  disk).   Note  that  changing  this
value  from  its  default will make it impossible for to find the
alternative superblocks if the standard superblock is lost.   The
size  of  a sector in bytes (almost never anything but 512).  De‐
faults to 512.  There is no option to specify the  metadata  byte
order  on  the  file system to be created because the native ext2
file system is always little endian even  on  big  endian  hosts.
The  file  system is created with inode generation numbers to im‐
prove NFS security.  The owner and group IDs of the root node and
reserved blocks of the new file system are set to  the  effective
UID  and  GID  of the user initializing the file system.  For the
command to succeed, the disk label should first be  updated  such
that  the  fstype  field for the partition is set to unless or is
used.  The partition size is found using not  by  inspecting  the
disk  label.   The  block  size and fragment size will be written
back to the disk label only if the last character  of  references
the same partition as the minor device number.  The command first
appeared  in  The command was written by The command is still ex‐
perimental and there are few sanity checks.  The command  doesn’t
have  options  to  specify each REV1 file system feature indepen‐
dently.  The command doesn’t support the bad block list accounted
by the bad blocks inode.  Many newer ext2  file  system  features
(especially  journaling) are not supported yet.  Some features in
file systems created by the command might not be recognized prop‐
erly by the utility.  There is no native tool in the distribution
for resizing ext2 file systems yet.
