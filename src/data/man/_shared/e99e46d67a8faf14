The  utility  is used to register a temporary PIN with the daemon
for the purposes of pairing Bluetooth devices.  The PIN  will  be
valid  for  5  minutes or until used, whichever comes first.  The
options are as follows: Specify the remote device  address.   The
address  can  be  specified  as BD_ADDR or a name.  If a name was
specified, the utility attempts to resolve the name  via  Specify
the local device address.  The device can be specified by BD_ADDR
or  device name.  See for a list of devices available.  If no de‐
vice is specified, the PIN will be valid for  any  local  device.
Specify length of PIN to generate, where 1
 16.  Pair with remote device.  will attempt to open an authenti‐
cated  L2CAP  connection  to the Service Discovery Service on the
remote device, to force immediate pairing.  The PIN to  register.
The  PIN may be up to 16 bytes in length.  Generate a random PIN,
the default length is 4  bytes.   Specify  path  to  the  control
socket.  The default path is for Itronix, Inc
