is an interpreter that runs the test program given in after load‐
ing  the library.  is not a real interpreter though: it is just a
wrapper around the system‐wide shell defined by the configuration
value in executes the interpreter, loads  the  library  and  then
runs   the   script.    Scripts   using  should  start  with:  #!
/usr/bin/env atf‐sh The following options are available: Shows  a
short summary of all available options and their purpose.
