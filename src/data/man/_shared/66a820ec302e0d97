makes  terminal‐dependent information available to users or shell
applications.  The options are as follows: The terminal  name  as
specified  in the database, for example, or If not specified, re‐
trieves the variable from the environment.  outputs a  string  if
the is of type string; a number if it is of type integer.  Other‐
wise, exits 0 if the terminal has the capability and 1 if it does
not, without further action.  If the is of type string, and takes
arguments (e.g. cursor movement, the terminfo sequence) the argu‐
ments  are  taken from the command line immediately following the
attribute.  The following special attributes are available: Clear
the screen (the sequence).   Initialize  the  terminal  (the  se‐
quence).  Print the descriptive name of the user’s terminal type.
Reset  the  terminal  (the  and sequence).  The exit status of is
based on the last attribute specified.  If the  attribute  is  of
type  string  or of type integer, exits 0 if the attribute is de‐
fined for this terminal type and 1 if it  is  not.   If  the  at‐
tribute  is of type boolean, exits 0 if the terminal has this at‐
tribute, and 1 if it does not.  exits 2 if  any  error  occurred.
clear  the screen and goto line 5 column 10 goto line 6 column 11
and delete 6 characters The command appears in can’t really  dis‐
tinguish between different types of attributes.  Not all terminfo
entries  contain  the  reset sequence, so using the init sequence
may be more useful.
