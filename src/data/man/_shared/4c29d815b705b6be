GCONCAT(8)                BSD System Manager's Manual               GCONCAT(8)

NNAAMMEE
     ggccoonnccaatt — disk concatenation control utility

SSYYNNOOPPSSIISS
     ggccoonnccaatt ccrreeaattee [--vv] _n_a_m_e _p_r_o_v _._._.
     ggccoonnccaatt ddeessttrrooyy [--ffvv] _n_a_m_e _._._.
     ggccoonnccaatt llaabbeell [--hhvv] _n_a_m_e _p_r_o_v _._._.
     ggccoonnccaatt aappppeenndd [--hhvv] _n_a_m_e _p_r_o_v
     ggccoonnccaatt ssttoopp [--ffvv] _n_a_m_e _._._.
     ggccoonnccaatt cclleeaarr [--vv] _p_r_o_v _._._.
     ggccoonnccaatt dduummpp _p_r_o_v _._._.
     ggccoonnccaatt lliisstt
     ggccoonnccaatt ssttaattuuss
     ggccoonnccaatt llooaadd
     ggccoonnccaatt uunnllooaadd

DDEESSCCRRIIPPTTIIOONN
     The ggccoonnccaatt utility is used for device concatenation configuration.  The
     concatenation can be configured using two different methods: “manual” or
     “automatic”.  When using the “manual” method, no metadata are stored on
     the devices, so the concatenated device has to be configured by hand ev‐
     ery time it is needed.  The “automatic” method uses on-disk metadata to
     detect devices.  Once devices are labeled, they will be automatically de‐
     tected and configured.

     The first argument to ggccoonnccaatt indicates an action to be performed:

     ccrreeaattee   Concatenate the given devices with specified _n_a_m_e.  This is the
              “manual” method.  The kernel module _g_e_o_m___c_o_n_c_a_t_._k_o will be
              loaded if it is not loaded already.

     llaabbeell    Concatenate the given devices with the specified _n_a_m_e.  This is
              the “automatic” method, where metadata are stored in every de‐
              vice's last sector.  The kernel module _g_e_o_m___c_o_n_c_a_t_._k_o will be
              loaded if it is not loaded already.

              Additional options include:

              --hh  Hardcode providers' names in metadata.

     aappppeenndd   Append a new device to the end of an existing concatenate device
              with the specified _n_a_m_e.

              If the existing device is using the “manual” method, the new de‐
              vice is simply appended as-is.

              If the existing device is using the “automatic” method, the de‐
              vice is appended persistently.  New ggccoonnccaatt metadata is written
              to all existing components, as well as to the newly added one.

              Additional options include:

              --hh  Hardcode providers' names in metadata.

     ssttoopp     Turn off existing concatenate device by its _n_a_m_e.  This command
              does not touch on-disk metadata!

              Additional options include:

              --ff  Stop the given device even if it is opened.

     ddeessttrrooyy  Same as ssttoopp.

     cclleeaarr    Clear metadata on the given devices.

     dduummpp     Dump metadata stored on the given devices.

     lliisstt     See geom(8).

     ssttaattuuss   See geom(8).

     llooaadd     See geom(8).

     uunnllooaadd   See geom(8).

     Additional options:

     --vv      Be more verbose.

SSYYSSCCTTLL VVAARRIIAABBLLEESS
     The following sysctl(8) variables can be used to control the behavior of
     the CCOONNCCAATT GEOM class.  The default value is shown next to each variable.

     _k_e_r_n_._g_e_o_m_._c_o_n_c_a_t_._d_e_b_u_g: 0
             Debug level of the CCOONNCCAATT GEOM class.  This can be set to a num‐
             ber between 0 and 3 inclusive.  If set to 0 minimal debug infor‐
             mation is printed, and if set to 3 the maximum amount of debug
             information is printed.

EEXXIITT SSTTAATTUUSS
     Exit status is 0 on success, and 1 if the command fails.

EEXXAAMMPPLLEESS
     The following example shows how to configure four disks for automatic
     concatenation, create a file system on it, and mount it:

           gconcat label -v data /dev/da0 /dev/da1 /dev/da2 /dev/da3
           newfs /dev/concat/data
           mount /dev/concat/data /mnt
           [...]
           umount /mnt
           gconcat stop data
           gconcat unload

     Configure concatenated provider on one disk only.  Create file system.
     Add two more disks and extend existing file system.

           gconcat label data /dev/da0
           newfs /dev/concat/data
           gconcat label data /dev/da0 /dev/da1 /dev/da2
           growfs /dev/concat/data

SSEEEE AALLSSOO
     geom(4), loader.conf(5), geom(8), growfs(8), gvinum(8), mount(8),
     newfs(8), sysctl(8), umount(8)

HHIISSTTOORRYY
     The ggccoonnccaatt utility appeared in FreeBSD 5.3.

AAUUTTHHOORRSS
     Pawel Jakub Dawidek <_p_j_d_@_F_r_e_e_B_S_D_._o_r_g>

BSD                              June 14, 2021                             BSD
