ISLOWER(3)               BSD Library Functions Manual               ISLOWER(3)

NNAAMMEE
     iisslloowweerr, iisslloowweerr__ll — lower-case character test

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ccttyyppee..hh>>

     _i_n_t
     iisslloowweerr(_i_n_t _c);

     _i_n_t
     iisslloowweerr__ll(_i_n_t _c, _l_o_c_a_l_e___t _l_o_c);

DDEESSCCRRIIPPTTIIOONN
     The iisslloowweerr() and iisslloowweerr__ll() functions test for any lower-case letters.
     The value of the argument must be representable as an _u_n_s_i_g_n_e_d _c_h_a_r or
     the value of EOF.

     In the ASCII character set, this includes the following characters (with
     their numeric values shown in octal):

     141 ``a''     142 ``b''     143 ``c''     144 ``d''     145 ``e''
     146 ``f''     147 ``g''     150 ``h''     151 ``i''     152 ``j''
     153 ``k''     154 ``l''     155 ``m''     156 ``n''     157 ``o''
     160 ``p''     161 ``q''     162 ``r''     163 ``s''     164 ``t''
     165 ``u''     166 ``v''     167 ``w''     170 ``x''     171 ``y''
     172 ``z''
     The iisslloowweerr__ll() function takes an explicit locale argument, whereas the
     iisslloowweerr() function uses the current global or per-thread locale.

RREETTUURRNN VVAALLUUEESS
     The iisslloowweerr() and iisslloowweerr__ll() functions return zero if the character
     tests false and return non-zero if the character tests true.

CCOOMMPPAATTIIBBIILLIITTYY
     The 4.4BSD extension of accepting arguments outside of the range of the
     _u_n_s_i_g_n_e_d _c_h_a_r type in locales with large character sets is considered ob‐
     solete and may not be supported in future releases.  The iisswwlloowweerr() or
     iisswwlloowweerr__ll() function should be used instead.

SSEEEE AALLSSOO
     ctype(3), ctype_l(3), iswlower(3), iswlower_l(3), tolower(3),
     tolower_l(3), xlocale(3), ascii(7)

SSTTAANNDDAARRDDSS
     The iisslloowweerr() function conforms to ISO/IEC 9899:1990 (“ISO C90”).  The
     iisslloowweerr__ll() function conforms to IEEE Std 1003.1-2008 (“POSIX.1”).

HHIISSTTOORRYY
     The iisslloowweerr() function first appeared in Version 7 AT&T UNIX.

BSD                            December 19, 2022                           BSD
