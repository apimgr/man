The  utility  reads  lines  from the named applies a configurable
line numbering filter operation, and writes  the  result  to  the
standard  output.   If is a single dash or absent, reads from the
standard input.  The utility treats the text it reads in terms of
logical pages.  Unless specified otherwise, line numbering is re‐
set at the start of each logical page.  A logical  page  consists
of  a  header,  a  body  and a footer section; empty sections are
valid.  Different line numbering options are independently avail‐
able for header, body and footer sections.  The starts of logical
page sections are signaled by input lines containing nothing  but
one  of  the  following sequences of delimiter characters: If the
input does not contain any logical page section signaling  direc‐
tives, the text being read is assumed to consist of a single log‐
ical page body.  The following options are available: Specify the
logical  page  body  lines  to be numbered.  Recognized arguments
are: Number all lines.  Number only  non‐empty  lines.   No  line
numbering.   Number only those lines that contain the basic regu‐
lar expression specified by The default  for  logical  page  body
lines  is  t.   Specify the delimiter characters used to indicate
the start of a logical page section in the input file.   At  most
two  characters may be specified; if only one character is speci‐
fied, the first character is replaced and  the  second  character
remains  unchanged.   The default characters are ‘‘\:’’.  Specify
the same as except for logical page footer  lines.   The  default
for  logical  page footer lines is n.  Specify the same as except
for logical page header lines.   The  default  for  logical  page
header  lines  is  n.  Specify the increment value used to number
logical page lines.  The default value is 1.  If numbering of all
lines is specified for the current logical section using the cor‐
responding a, a or a option, specify the number of adjacent blank
lines to be considered as one.  For example, 2  results  in  only
the second adjacent blank line being numbered.  The default value
is  1.  Specify the line numbering output format.  Recognized ar‐
guments are: Left justified.  Right justified, leading zeros sup‐
pressed.  Right justified, leading zeros kept.   The  default  is
rn.  Specify that line numbering should not be restarted at logi‐
cal  page  delimiters.  Specify the characters used in separating
the line number and the corresponding  text  line.   The  default
setting  is  a  single  tab character.  Specify the initial value
used to number logical page lines; see also  the  description  of
the option.  The default value is 1.  Specify the number of char‐
acters  to be occupied by the line number; if the is insufficient
to hold the line number, it will be truncated to its  least  sig‐
nificant  digits.   The  default  is 6.  The utility is compliant
with the specification.  The utility first appeared in
