provides  machine‐independent bus support for Advanced Configura‐
tion and Power Interface (ACPI) devices and includes several ACPI
device drivers.  The implementation of  ACPI  integrates  Intel’s
ACPI  Component  Architecture  (ACPI‐CA)  for  the OS‐independent
part.  The ACPI‐CA provides OS‐neutral ACPI functionalities  such
as  ACPI  BIOS table support, an ACPI event framework and an ACPI
Machine Language (AML) interpreter.  Options: Enable various  de‐
bug facilities.  Enable verbose debug messages.  Determine if the
ACPI driver should attempt to activate inactive devices.  The de‐
fault  is  off.   Force a given Differentiated System Description
Table (DSDT) instead of the version supplied by the BIOS.  Use to
specify a DSDT.  If is not specified, default to in the build di‐
rectory.  Do not use ACPI with any BIOS made  on  or  before  the
specified  year.   Do  not call the ACPI "_DIS" method to disable
interrupt links.  This may be required on specific  chipset  sys‐
tems,  which hard hang when this method is called instead of hav‐
ing it fail gracefully.  The following variables are provided  by
the  driver:  The address of the ACPI root pointer in system mem‐
ory.  The system sleep state.  A list of system sleep states that
the machine supports.  The possible  values  are:  fully  running
power on suspend (CPU and hard disks are off) similar to S3, usu‐
ally  not  implemented  suspend‐to‐RAM  suspend‐to‐disk (not sup‐
ported on power off A boolean variable that controls whether  the
PC  speaker  beeps upon resume.  Only available on i386 and amd64
architectures.  Defines the handling of the graphics card on i386
and amd64 architectures.  The supported values are: No attempt to
reset the VGA controller will be made.  Call the  VGA  BIOS  when
still  in real mode.  This can result in direct reboots.  In that
case, use or from the package.  Call the VGA BIOS using  the  in‐
kernel x86 emulator.  If the system has problems in resuming from
the  S3  state, experimenting with different values may provide a
solution.   The  number  of  dispatched  General  Purpose  Events
(GPEs).  The number of System Control Interrupts (SCIs).  See for
a  brief  description  of  both GPEs and SCIs.  The number of The
number of AML methods executed by the  interpreter.   This  read‐
only  node describes the ACPI power state of devices.  The values
range from D0 to D3 This node represents devices  that  can  wake
the  system  from  the S3 or S4 sleep state.  By default, and are
allowed to wake the system, provided that the devices are present
and the firmware supports wake‐up capabilities for  the  devices.
ACPI  supports  several machine‐dependent and machine‐independent
devices, some specific to ACPI and some configured via it.   ACPI
AC  adapters.   ACPI  batteries.   ACPI  power and sleep buttons.
ACPI processors.   ACPI  direction  application  launch  buttons.
ACPI  embedded  controllers.   ACPI  Embedded Controller Boot Re‐
source Table (ECDT).  ACPI fans.  ACPI lid switches.  ACPI  power
meters.   ACPI  SMBus  via  control method interface (CMI).  ACPI
thermal zones.  ACPI display adapter and  output  devices.   ACPI
support  for Windows Management Instrumentation.  ACPI watchdogs.
ASUSTeK  voltage,  temperature  and  fan  sensors.   ASUS  laptop
hotkeys.   AT Timer.  NS8250‐, NS16450‐, and NS16550‐based serial
ports.  Floppy disk controllers.  Fujitsu brightness and pointer.
Fujitsu hotkeys.  HP 3D DriveGuard accelerometer.  High Precision
Event Timer (HPET).  HP Quick Launch Buttons.  Joystick/Game port
interface.  Standard ISA parallel port interface.  Roland MPU‐401
(compatible) MIDI UART.  AT‐style speaker sound.   SD  Host  Con‐
troller.   IBM/Lenovo  ThinkPad laptop device driver.  Abit uGuru
Hardware monitor.  Toshiba Libretto device.  Toshiba Dynabook de‐
vice.  Winbond W83L518D Integrated Media Reader.   Windows  Sound
System‐compatible  sound cards Yamaha OPL3‐SA2 and OPL3‐SA3 audio
device driver.  PC keyboard controllers.  Sony Miscellaneous Con‐
troller Sony programmable I/O controller.  Although the situation
has become better over the years, ACPI is typically prone to var‐
ious errors, ranging from blatant flaws in the firmware  to  bugs
in  the implementation.  Before anything else, it is a good prac‐
tice to upgrade the BIOS to the latest version available from the
vendor.  To ease the task  of  diagnosing  and  fixing  different
problems, the ACPICA reference implementation provides a rich fa‐
cility  of  different  debugging methods.  In these are generally
only available if the kernel has been compiled with  the  option.
The  compile  time  option  enables  some  verbose debug messages
printed during the system startup.  In a (see system, the  infor‐
mation can be printed also at runtime, regardless of the presence
of To print the messages, the module using the option ACPI inter‐
prets  bytecode known as ACPI Machine Language (AML), provided by
the BIOS as a memory image during the system bootstrap.  Most  of
the  AML  relevant to is implemented in the so‐called Differenti‐
ated System Descriptor Table (DSDT).  provides support for  over‐
riding  the  default  DSDT  supplied  by the BIOS.  The following
steps can be used to override the DSDT: Dump the  raw  DSDT  with
Disassemble  the  table with Modify the disassembled table.  Com‐
pile the table with using the option  Either  copy  the  file  to
src/sys/dev/acpi/acpica/Osd/custom_dsdt.hex  or  use  the  option
ACPI_DSDT_FILE="/some/directory/custom_dsdt.hex"  in  the  kernel
configuration  file.  Define in the kernel configuration file and
rebuild.  The ACPICA interpreter provides its  own  debugger  for
low‐level  debugging.   It  can  be used to display internal data
structures and namespace objects, and to debug the  execution  of
control  methods.   Single  step and breakpoint functionality are
available.  In this is integrated to the in‐kernel  In  order  to
enter  the ACPICA debugger from use the command with the argument
provides three variables that control the debug  output  at  run‐
time.   The  variable  limits the output to a specific ACPI layer
and the variable controls the debug level.   Both  variables  are
string  literals.   The  third variable is This is a boolean that
controls whether debug messages internal to the AML are  enabled.
For  the  first  two variables, the possible values are: In addi‐
tion, there is that is used by ACPICA as the default debug level.
It includes and The debug layer can be divided into  two  groups:
the  first one is specific to the ACPICA interpreter and the sec‐
ond one contains the internal ACPI components of The constant in‐
cludes all specific parts.  The ACPICA interpreter  uses  several
debug  levels  internally,  but  the specific parts are typically
limited to and The debug output can be stopped by setting  to  As
an example, a driver may have defined the component it belongs to
and the name of the module: #define _COMPONENT      ACPI_BUS_COM‐
PONENT  ACPI_MODULE_NAME        ("acpi_example")  The  driver may
also utilize the debug facility:  ACPI_DEBUG_PRINT((ACPI_DB_INFO,
"Failed  to evaluate _STA\n")); With these options the debug mes‐
sage from the macro is only visible when is either or a  compound
constant  including it, and is or some constant that includes it.
Finally, it can be noted that the ACPI  implementation  uses  the
prefix  whereas the debug level variable is always specified with
the prefix Another example can be mentioned for the  use  of  The
following could appear in an ASL code: Method(_Q19, 0, NotSerial‐
ized) {         Store("_Q19 invoked", Debug)         Notify(ACAD,
0x80)  } When is set to 1, the message stored to the debug object
is printed every time the method is called  by  the  interpreter.
The driver appeared in Authors of the subsystem include and among
others.   Most  of  the ACPI power management functionalities are
not implemented.  The option should not be necessary.
