ATF-SH(1)                 BSD General Commands Manual                ATF-SH(1)

NNAAMMEE
     aattff--sshh — interpreter for shell-based test programs

SSYYNNOOPPSSIISS
     aattff--sshh [--ss _s_h_e_l_l] _s_c_r_i_p_t

DDEESSCCRRIIPPTTIIOONN
     aattff--sshh is an interpreter that runs the test program given in _s_c_r_i_p_t after
     loading the atf-sh(3) library.

     aattff--sshh is not a real interpreter though: it is just a wrapper around the
     system-wide shell defined by _A_T_F___S_H_E_L_L.  aattff--sshh executes the interpreter,
     loads the atf-sh(3) library and then runs the script.  You must consider
     aattff--sshh to be a POSIX shell by default and thus should not use any non-
     standard extensions.

     The following options are available:

     --ss _s_h_e_l_l     Specifies the shell to use instead of the value provided by
                  _A_T_F___S_H_E_L_L.

EENNVVIIRROONNMMEENNTT
     _A_T_F___L_I_B_E_X_E_C_D_I_R    Overrides the builtin directory where aattff--sshh is lo‐
                       cated.  Should not be overridden other than for testing
                       purposes.
     _A_T_F___P_K_G_D_A_T_A_D_I_R    Overrides the builtin directory where _l_i_b_a_t_f_-_s_h_._s_u_b_r is
                       located.  Should not be overridden other than for test‐
                       ing purposes.
     _A_T_F___S_H_E_L_L         Path to the system shell to be used in the generated
                       scripts.  Scripts must not rely on this variable being
                       set to select a specific interpreter.
     _A_T_F___S_H_E_L_L         Path to the system shell to be used in the generated
                       scripts.

EEXXAAMMPPLLEESS
     Scripts using atf-sh(3) should start with:

           #! /usr/bin/env atf-sh

     Alternatively, if you want to explicitly choose a shell interpreter, you
     cannot rely on env(1) to find aattff--sshh.  Instead, you have to hardcode the
     path to aattff--sshh in the script and then use the --ss option afterwards as a
     _s_i_n_g_l_e _p_a_r_a_m_e_t_e_r:

           #! /path/to/bin/atf-sh -s/bin/bash

SSEEEE AALLSSOO
     atf-sh(3)

BSD                              March 6, 2017                             BSD
