The  daemon  is  used to transmit/receive audio to/from Bluetooth
devices such as speakers or headphones, using the Advanced  Audio
Distribution  Profile (A2DP).  Remote device address.  The may be
given as BDADDR or a name.  If a name was specified, attempts  to
resolve the name via Allows setting the maximum bitpool value for
your  device.  This may have to be lowered if audio is distorted.
Use with only the option and the maximum bitpool for your  device
will  be printed to stdout.  Run in the background.  Do not close
connection on end of file (EOF).  Useful  for  Local  device  ad‐
dress.   May  be given as BDADDR or device name.  Initiate a Dis‐
cover command.  Useful when used as an audio sink as some devices
need this to pair.  Allows registering as an audio sink receiving
an incoming connection.  Allows setting the Maximum  Transmission
Unit  (MTU) for the streaming connections.  For most devices this
would be 672 but if audio is broken or  has  gaps  lowering  this
value may help.  Set L2CAP connection link mode.  Supported modes
are: Require devices to be paired.  Auth, plus enable encryption.
Encryption,  plus  change of link key.  Allows changing the l2cap
psm for an audio sink the default is 25.  Most all  devices  will
use 25.  Test mode can be used with to decode and receive encoded
music  from  stdout/stdin to file.  Allows shifting the volume of
the music by 1 or 2 bits prior to encoding for increased  volume.
as this may mean the output is quite loud.  Be verbose.  Files to
stream.  These can be WAV files or CD‐Audio Red Book data or out‐
put  from  a  device.   If not specified, the default is stdin or
stdout if applied and is used as an audio sink.  When writing  to
a file or stdout it is raw 16‐bit little endian (pulse coded mod‐
ulation) PCM data.  So a utility must be used to convert the file
to  add  a  header or use the command as stated in This specifies
that all possible input formats are accepted and then one is cho‐
sen starting with joint stereo then stereo then  2  channel  then
mono.   This specifies that the input is monophonic.  This speci‐
fies that the input is dual‐channel.  This specifies that the in‐
put is joint‐stereo.  This specifies that the  input  is  stereo.
This  is  the  default.   Specifying  joint  stereo as opposed to
stereo would allow for a slightly higher quality of  playback  as
will only encode stereo and monophonic streams.  The frequency of
the  input.   Where rate is one of 0, 16000, 32000, 44100, 48000.
This defaults to 44.1 kHz.  A value of 0 will arbitrate from  the
highest to lowest frequency.  Bit Allocation is negotiated start‐
ing with Loudness then SNR.  Audio is encoded with SNR (Signal to
Noise  Ratio) bit allocation.  Audio is encoded with Loudness bit
allocation.  This is the default.  Number of blocks  to  use  for
encoding.  Where blocks is one of 0, 4, 8, 12, 16.  This defaults
to  16  blocks.  In the case of 0 the number of blocks is negoti‐
ated starting from 16 down to 4.  Number of SBC bands is  negoti‐
ated  8 then 4.  Audio is encoded with 4 SBC bands.  Audio is en‐
coded with 8 SBC bands.  This is the default.  It may  be  neces‐
sary to use to set the class of your Bluetooth adapter to that of
headphones  when  using  as an audio sink.  I.e., Then start with
before pairing.  This is necessary as some devices  only  perform
an  just  after  pairing and cache the result.  It is possible to
specify multiples of This will mean that the  specified  combina‐
tions  are  reported  as being accepted by the source/sink.  When
used as a sink all modes, bands, blocks and allocation modes will
be accepted unless specified as options.  I.e., will only  accept
a  connection  with  a rate of 44.1kHz.  Encode and send audio to
address Encode and send audio from to address Decode stream  from
any  connected address and write it to Decode stream from address
and send to speakers.  For some devices playback  from  file  re‐
sults  in  rapid playback or playback with stutter.  For best re‐
sults use with
