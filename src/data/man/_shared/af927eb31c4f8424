The  program  rebuilds  the  database used by the program.  It is
usually run once per week, see The file systems and  files  (not)
scanned  can  be  configured in The program supports this option:
Use (in format) as the configuration file instead of the  default
Default database The command appeared in
