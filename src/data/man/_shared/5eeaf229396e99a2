ACL_SET_TAG_TYPE(3)      BSD Library Functions Manual      ACL_SET_TAG_TYPE(3)

NNAAMMEE
     aaccll__sseett__ttaagg__ttyyppee — set the tag type of an ACL entry

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ttyyppeess..hh>>
     ##iinncclluuddee <<ssyyss//aaccll..hh>>

     _i_n_t
     aaccll__sseett__ttaagg__ttyyppee(_a_c_l___e_n_t_r_y___t _e_n_t_r_y___d, _a_c_l___t_a_g___t _t_a_g___t_y_p_e);

DDEESSCCRRIIPPTTIIOONN
     The aaccll__sseett__ttaagg__ttyyppee() function is a POSIX.1e call that sets the ACL tag
     type of ACL entry _e_n_t_r_y___d to the value of _t_a_g___t_y_p_e.

     Valid values are:

        ACL_USER_OBJ     Permissions apply to file owner
        ACL_USER         Permissions apply to additional user specified by
                         qualifier
        ACL_GROUP_OBJ    Permissions apply to file group
        ACL_GROUP        Permissions apply to additional group specified by
                         qualifier
        ACL_MASK         Permissions specify mask
        ACL_OTHER        Permissions apply to other
        ACL_OTHER_OBJ    Same as ACL_OTHER
        ACL_EVERYONE     Permissions apply to everyone@

     Calling aaccll__sseett__ttaagg__ttyyppee() with _t_a_g___t_y_p_e equal to ACL_MASK, ACL_OTHER or
     ACL_OTHER_OBJ brands the ACL as POSIX.1e.  Calling it with ACL_EVERYONE
     brands the ACL as NFSv4.

RREETTUURRNN VVAALLUUEESS
     The aaccll__sseett__ttaagg__ttyyppee() function returns the value 0 if successful; other‐
     wise the value -1 is returned and the global variable _e_r_r_n_o is set to in‐
     dicate the error.

EERRRROORRSS
     The aaccll__sseett__ttaagg__ttyyppee() function fails if:

     [EINVAL]           Argument _e_n_t_r_y___d is not a valid descriptor for an ACL
                        entry.  Argument _t_a_g___t_y_p_e is not a valid ACL tag type.
                        ACL is already branded differently.

SSEEEE AALLSSOO
     acl(3), acl_get_brand_np(3), acl_get_tag_type(3), posix1e(3)

SSTTAANNDDAARRDDSS
     POSIX.1e is described in IEEE POSIX.1e draft 17.

HHIISSTTOORRYY
     POSIX.1e support was introduced in FreeBSD 4.0.  The aaccll__sseett__ttaagg__ttyyppee()
     function was added in FreeBSD 5.0.

AAUUTTHHOORRSS
     The aaccll__sseett__ttaagg__ttyyppee() function was written by Chris D. Faulhaber
     <_j_e_d_g_a_r_@_f_x_p_._o_r_g>.

BSD                              June 25, 2009                             BSD
