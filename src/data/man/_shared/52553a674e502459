DWARF_FINISH(3)          BSD Library Functions Manual          DWARF_FINISH(3)

NNAAMMEE
     ddwwaarrff__ffiinniisshh, ddwwaarrff__oobbjjeecctt__ffiinniisshh — free resources associated with a de‐
     bug descriptor

LLIIBBRRAARRYY
     DWARF Access Library (libdwarf, -ldwarf)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<lliibbddwwaarrff..hh>>

     _i_n_t
     ddwwaarrff__ffiinniisshh(_D_w_a_r_f___D_e_b_u_g _d_b_g, _D_w_a_r_f___E_r_r_o_r _*_e_r_r);

     _i_n_t
     ddwwaarrff__oobbjjeecctt__ffiinniisshh(_D_w_a_r_f___D_e_b_u_g _d_b_g, _D_w_a_r_f___E_r_r_o_r _*_e_r_r);

DDEESSCCRRIIPPTTIIOONN
     The ddwwaarrff__ffiinniisshh() and ddwwaarrff__oobbjjeecctt__ffiinniisshh() functions are used to re‐
     lease the resources associated with a debug descriptor allocated by a
     prior call to dwarf_init(3) and dwarf_object_init(3) respectively.

     Argument _d_b_g denotes a valid _D_w_a_r_f___D_e_b_u_g instance.  Argument _e_r_r will be
     used to record error information in case of an error.

     After a call to ddwwaarrff__ffiinniisshh() or ddwwaarrff__oobbjjeecctt__ffiinniisshh(), the argument _d_b_g
     will be invalid and should not be used further.

     For _D_w_a_r_f___D_e_b_u_g descriptors opened using dwarf_init(3), the application
     would need to explicitly release the _E_l_f instance associated with the de‐
     scriptor by first retrieving the instance using dwarf_get_elf(3) and
     closing it using elf_end(3).

RREETTUURRNN VVAALLUUEESS
     These functions return DW_DLV_OK if successful.  In case of an error, the
     functions return DW_DLV_ERROR and record additional information in argu‐
     ment _e_r_r.

EEXXAAMMPPLLEESS
     To deallocate a _D_w_a_r_f___D_e_b_u_g instance allocated using dwarf_elf_init(3)
     use:

           Dwarf_Debug dbg;
           Dwarf_Error de;

           if (dwarf_finish(dbg, &de) != DW_DLV_OK)
                   errx(EXIT_FAILURE, "dwarf_finish: %s", dwarf_errmsg(de));

     To deallocate a _D_w_a_r_f___D_e_b_u_g instance allocated using dwarf_object_init(3)
     use:

           Dwarf_Debug dbg;
           Dwarf_Error de;

           if (dwarf_object_finish(dbg, &de) != DW_DLV_OK)
                   errx(EXIT_FAILURE, "dwarf_object_finish: %s",
                       dwarf_errmsg(de));

     To deallocate a _D_w_a_r_f___D_e_b_u_g instance allocated using dwarf_init(3) use:

           Dwarf_Debug dbg;
           Dward_Error de;
           Elf *e;

           if (dwarf_get_elf(dbg, &e, &de) != DW_DLV_OK)
                   errx(EXIT_FAILURE, "dwarf_get_elf: %s", dwarf_errmsg(&de));

           if (dwarf_finish(dbg, &de) != DW_DLV_OK)
                   errx(EXIT_FAILURE, "dwarf_finish: %s", dwarf_errmsg(de));

           (void) elf_end(e);

SSEEEE AALLSSOO
     dwarf_elf_init(3), dwarf_get_elf(3), dwarf_init(3), dwarf_object_init(3),
     elf_end(3)

BSD                            November 9, 2011                            BSD
