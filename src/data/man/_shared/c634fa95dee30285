When called without options, the command parses the configuration
file  and  any  direct maps that it references, and mounts or un‐
mounts file systems to match.  These options are available: Flush
caches, discarding possibly stale information obtained from  maps
and  directory  services.   Define a variable.  It is only useful
with Force unmount, to be used with Do not mount or unmount  any‐
thing.   Instead  parse  and  any direct maps, then print them to
standard output.  When specified more than once,  all  the  maps,
including  indirect ones, will be parsed and shown.  This is use‐
ful when debugging configuration problems.  Specify mount options
to be used along with the ones specified in the maps.  It is only
useful with Try to unmount file systems mounted by mounts are not
unmounted.  To unmount all mounts, use Increase  verbosity.   Un‐
mount  all  file  systems  mounted by The command appeared in The
command appeared in The command appeared in The was developed  by
under sponsorship from the Foundation.  The was ported to and by
