Internet  File  Transfer  Protocol  server  is  the Internet File
Transfer Protocol server process.  The server uses  the  protocol
and  listens  at the port specified in the service specification;
see Available options: When is specified, bind to IPv4  addresses
only.  When is specified, bind to IPv6 addresses only.  Define as
the  directory to into for anonymous logins.  Default is the home
directory for the ftp user.  This can also be specified with  the
directive.  Check whether as if connecting from if provided would
be  granted access under the restrictions given in and exit with‐
out attempting a connection.  exits with an exit code of 0 if ac‐
cess would be granted, or 1 otherwise.  This can  be  useful  for
testing configurations.  Change the root directory of the config‐
uration  files from to This changes the directory for the follow‐
ing files: and the file specified by the directive.  Run as  dae‐
mon.   will  listen  on the default FTP port for incoming connec‐
tions and fork a child for each connection.  This is lower  over‐
head  than starting from and thus might be useful on busy servers
to reduce load.  Debugging information is written to  the  syslog
using  a  facility  of Use for the escape sequence (see Stops the
flag from detaching from the tty and going into  the  background.
Equivalent to ‐h ‘hostname‘ Explicitly set the hostname to adver‐
tise as to The default is the hostname associated with the IP ad‐
dress  that  is  listening  on.  This ability (with or without in
conjunction with is useful when configuring servers, each listen‐
ing on separate addresses as separate names.  Refer to  for  more
information  on  starting  services  to listen on specific IP ad‐
dresses.  Log style entries to Each successful and failed session
is logged using syslog with a facility of If this option is spec‐
ified more than once, the retrieve (get),  store  (put),  append,
delete,  make  directory,  remove directory and rename operations
and their file name arguments are  also  logged.   Don’t  attempt
translation  of IP addresses to hostnames.  Use as the data port,
overriding the default of using the port one less that  the  port
is  listening on.  Disable the use of pid files for keeping track
of the number of logged‐in users per class.  This may reduce  the
load  on heavily loaded servers.  Enable the use of pid files for
keeping track of the number of logged‐in users per  class.   This
is  the  default.  Permanently drop root privileges once the user
is logged in.  The use of this option may result  in  the  server
using  a  port other than the (listening‐port ‐ 1) for style com‐
mands, which is contrary to the specification,  but  in  practice
very  few  clients  rely upon this behaviour.  See below for more
details.  Require a secure authentication mechanism like Kerberos
or S/Key to be used.  Don’t log each concurrent session  to  This
is the default.  Log each concurrent session to making them visi‐
ble  to  commands  such as Use as the version to advertise in the
login banner and in the output of and instead of the default ver‐
sion information.  If is empty or then don’t display any  version
information.   Don’t log each session to Log each session to mak‐
ing them visible to commands such as This is  the  default.   Log
style  entries  to  the syslog, prefixed with using a facility of
These syslog entries can be converted to a  style  file  suitable
for  input  into  a  third‐party log analysis tool with a command
similar to: The file can be used to disable access.  If the  file
exists, displays it and exits.  If the file exists, prints it be‐
fore  issuing  the message.  If the file exists (under the chroot
directory if applicable), prints it  after  a  successful  login.
This  may be changed with the directive The server currently sup‐
ports the following requests.  The case of the  requests  is  ig‐
nored.   The following non‐standard or specific commands are sup‐
ported by the SITE request.  The following requests (as specified
in and are recognized, but are not implemented:  and  The  server
will  abort an active file transfer only when the command is pre‐
ceded by a Telnet "Interrupt Process" (IP) signal  and  a  Telnet
"Synch"  signal in the command Telnet stream, as described in In‐
ternet If a command is received during a data transfer,  preceded
by  a Telnet IP and Synch, transfer status will be returned.  in‐
terprets file names according to the conventions used by This al‐
lows users to use the metacharacters authenticates users  accord‐
ing  to  five rules.  The login name must be in the password data
base, and not have a null password.  In this case a password must
be provided by the client before any file operations may be  per‐
formed.   If  the user has an S/Key key, the response from a suc‐
cessful command will include an S/Key challenge.  The client  may
choose  to  respond with a command giving either a standard pass‐
word or an S/Key one‐time password.  The  server  will  automati‐
cally  determine which type of password it has been given and at‐
tempt to authenticate accordingly.  See for more  information  on
S/Key authentication.  S/Key is a Trademark of Bellcore.  The lo‐
gin  name  must  be  allowed based on the information in The user
must have a standard shell returned by If the user’s shell  field
in  the password database is empty, the shell is assumed to be As
per the user’s shell must be listed with full path in If directed
by the file the session’s root directory will be  changed  by  to
the directory specified in the directive (if set), or to the home
directory  of  the  user.  This facility may also be triggered by
enabling the boolean in However, the user  must  still  supply  a
password.   This  feature  is  intended as a compromise between a
fully anonymous account and a fully privileged account.  The  ac‐
count  should also be set up as for an anonymous account.  If the
user name is or an anonymous account must be present in the pass‐
word file (user In this case the user is allowed  to  log  in  by
specifying  any  password (by convention an email address for the
user should be used as the password).  The server performs  a  to
the  directory specified in the directive (if set), the directory
(if set), or to the home directory of the user.  The server  then
performs  a to the directory specified in the directive (if set),
otherwise to If other restrictions are  required  (such  as  dis‐
abling  of certain commands and the setting of a specific umask),
then appropriate entries in are required.  If the first character
of the password supplied by an anonymous user is then the verbose
messages displayed at login and upon a  command  are  suppressed.
When displays various files back to the client (such as and vari‐
ous escape strings are replaced with information pertinent to the
current  connection.   The  supported  escape  strings are: Class
name.  Current working directory.  Email address given with Local
hostname.  Maximum number of users for this class.   Displays  if
there’s  no  limit.  Current number of users for this class.  Re‐
mote hostname.  If the result of the most recent or was not print
an If the result of the most recent or was not print  an  Current
time.  User name.  A character.  In order that system security is
not breached, it is recommended that the subtrees for the and ac‐
counts  be  constructed with care, following these rules (replace
in the following directory names  with  the  appropriate  account
name  for users): Make the home directory owned by and unwritable
by anyone.  Make this directory owned by and unwritable by anyone
(mode 555).  Generally any  conversion  commands  should  be  in‐
stalled  here  (mode  111).  Make this directory owned by and un‐
writable by anyone (mode 555).  The files (see and (see  must  be
present  for  the  command  to be able to display owner and group
names instead of numbers.  The password field in is not used, and
should not contain real passwords.  The file if present, will  be
printed  after  a  successful  login.  These files should be mode
444.  This directory and the subdirectories beneath it should  be
owned  by  the  users and groups responsible for placing files in
them, and be writable only by  them  (mode  755  or  775).   They
should  be owned or writable by ftp or its group.  This directory
is where anonymous users place files  they  upload.   The  owners
should  be  the  user  and an appropriate group.  Members of this
group will be the only users with access  to  these  files  after
they  have  been uploaded; these should be people who know how to
deal with them appropriately.  If you wish anonymous users to  be
able  to see the names of the files in this directory the permis‐
sions should be 770, otherwise they should be 370.  The following
directives should be used: This will result  in  anonymous  users
being  able  to upload files to this directory, but they will not
be able to download them, delete them, or overwrite them, due  to
the  umask  and  disabling of the commands mentioned above.  This
directory is used to create temporary files which contain the er‐
ror messages generated by a conversion  or  command.   The  owner
should  be  the user The permissions should be 300.  If you don’t
enable conversion commands, or don’t want anonymous users upload‐
ing files here (see above), then  don’t  create  this  directory.
However,  error messages from conversion or commands won’t be re‐
turned to the user.  (This is the traditional  behaviour.)   Note
that  the  directive can be used to prevent users uploading here.
To set up "ftp‐only" accounts that  provide  only  but  no  valid
shell  login, you can copy/link to and enter to to allow logging‐
in via into the accounts, which must have as login  shell.   List
of  normal  users whose root directory should be changed via Con‐
figure file conversions  and  other  settings.   List  of  unwel‐
come/restricted users.  Welcome notice before login.  Welcome no‐
tice after login.  If it exists, displayed and access is refused.
State file of logged‐in processes for the class List of logged‐in
users  on  the  system.   Login history database.  recognizes all
commands in follows the guidelines in recognizes all commands  in
(although  they  are  not supported yet), and supports the exten‐
sions from and The command appeared in Various features  such  as
the  functionality,  and support was implemented in and later re‐
leases by Luke Mewburn.  The server must run as the super‐user to
create sockets with privileged port numbers (i.e, those less than
which is 1024).  If is listening on a privileged  port  it  main‐
tains  an  effective  user id of the logged in user, reverting to
the super‐user only when binding addresses to privileged sockets.
The option can be used to override this behaviour and force priv‐
ileges to be permanently revoked; see  below  for  more  details.
may have trouble handling connections from scoped IPv6 addresses,
or IPv4 mapped addresses IPv4 connection on socket For the latter
case,  running  two  daemons, one for IPv4 and one for IPv6, will
avoid the problem.  provides no restrictions on the command,  and
this  can  lead  to security problems, as can be fooled into con‐
necting to any service on any host.  With the feature of the com‐
mands with different host addresses, or TCP ports lower than will
be rejected.  This also prevents from working.  Use of  this  op‐
tion  is  recommended, and enabled by default.  By default uses a
port that is one less than the port it is listening on to  commu‐
nicate back to the client for the and commands, unless overridden
with  As the default port for (21) is a privileged port below re‐
tains the ability to switch back to root privileges to bind these
ports.  In order to increase security by reducing  the  potential
for a bug in providing a remote root compromise, will permanently
drop  root privileges if one of the following is true: is running
on a port greater than and the user has logged in as a  or  user.
was  invoked  with Don’t create if you don’t want anonymous users
to upload files there.  That directory is only necessary  if  you
want  to display the error messages of conversion commands to the
user.  Note that if uploads are disabled with the directive  then
this  directory  cannot  be abused by the user in this way, so it
should be safe to create.  To  avoid  possible  denial‐of‐service
attacks,  requests  against files larger than 10240 bytes will be
denied if the current transfer is (ASCII).
