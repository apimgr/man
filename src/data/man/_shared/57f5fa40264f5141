The  access and modification times of the file named by or refer‐
enced by are changed as specified  by  the  argument  The  inode‐
change‐time of the file is set to the current time.  If specifies
a  relative path, it is relative to the current working directory
if is and otherwise relative to the directory associated with the
file descriptor The field of a structure can be set to  the  spe‐
cial  value  to set the current time, or to to leave the time un‐
changed.  In either case, the field is ignored.  If is it is  as‐
sumed  to  point to an array of two timespec structures.  The ac‐
cess time is set to the value of the first element, and the modi‐
fication time is set to the value of the second element.   If  is
this  is equivalent to passing a pointer to an array of two time‐
spec structures with both fields set to If both  fields  are  the
timestamps remain unchanged and no permissions are needed for the
file  itself, although search permissions may be required for the
path prefix.  The call may or may not succeed if the  named  file
does  not exist.  If both fields are the caller must be the owner
of the file, have permission to write the file, or be the  super‐
user.  For all other values of the timestamps, the caller must be
the  owner  of the file or be the super‐user.  The values for the
argument of the system call are constructed by  a  bitwise‐inclu‐
sive  OR  of flags from the following list, defined in If names a
symbolic link, the symbolic link’s times  are  changed.   By  de‐
fault,  changes  the times of the file referenced by the symbolic
link.  If names a symbolic link, the symbolic  link’s  times  are
changed.   In addition, if any symbolic link is enountered during
pathname resolution, an error is returned.  By  default,  changes
the  times  of the file referenced by the symbolic link.  If does
not reside in the hierarchy beneath the  starting  directory,  an
error is returned.  These system calls will fail if: The argument
is  or  both  values are and the effective user ID of the process
does not match the owner of the file, and is not the  super‐user,
and write access is denied.  The component of at least one of the
values  specified  by  the  argument  has  a value less than 0 or
greater than 999999999 and is not equal to or An  I/O  error  oc‐
curred while reading or writing the affected inode.  The argument
is  not  nor  are both values nor are both values and the calling
process’s effective user ID does not match the owner of the  file
and  is  not the super‐user.  The named file has its immutable or
append‐only flag set, see the manual page for  more  information.
The  file  system  containing the file is mounted read‐only.  The
system call will fail if: The argument does not refer to a  valid
descriptor.   The  system call will fail if: Search permission is
denied for a component of the path prefix.  The argument does not
specify an absolute path and the argument is neither nor a  valid
file descriptor.  The argument points outside the process’s allo‐
cated address space.  Too many symbolic links were encountered in
translating  the  pathname.   A  component of a pathname exceeded
characters, or an entire  path  name  exceeded  characters.   The
named file does not exist.  A component of the path prefix is not
a directory.  The argument is not an absolute path and is neither
nor  a  file  descriptor  associated with a directory.  if AT_RE‐
SOLVE_BENEATH is passed and does not reside in the directory  hi‐
erarchy beneath the starting directory.  The and system calls are
expected to conform to
