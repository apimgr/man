The  command can be used to control the NPF packet filter.  For a
description of NPF’s configuration file, see The first  argument,
specifies  the action to take.  Valid commands are: Enable packet
inspection using the  currently  loaded  configuration,  if  any.
Note that this command does not load or reload the configuration,
or affect existing connections.  Disable packet inspection.  This
command  does  not  change the currently loaded configuration, or
affect existing connections.  Load or reload  configuration  from
file.   The  configuration  file at will be used unless a file is
specified by All connections will be preserved during the reload,
except those which will lose NAT policy due to removal.  NAT pol‐
icy is determined by the translation type and address.  Note that
change of filter criteria will not expire associated connections.
The reload operation (i.e., replacing the ruleset,  NAT  policies
and tables) is atomic.  Flush configuration.  That is, remove all
rules,  tables and expire all connections.  This command does not
disable packet inspection.  Show the current state and configura‐
tion.  Syntax of printed configuration is for the  user  and  may
not  match  the  syntax.  Validate the configuration file and the
processed form.  The configuration file at will be used unless  a
file  is specified by The may also be set to "‐" for reading from
Add a rule to a dynamic ruleset specified by On success,  returns
a  unique  identifier  which  can be used to remove the rule with
command.  The identifier is alphanumeric string.  Remove  a  rule
from  a  dynamic  ruleset specified by This method uses SHA1 hash
computed on a rule to identify it.  Although very unlikely, it is
subject to hash collisions.  For a fully reliable and more  effi‐
cient  method,  it  is recommended to use command.  Remove a rule
specified by unique from a dynamic ruleset specified by List  all
rules  in  the dynamic ruleset specified by Remove all rules from
the dynamic ruleset specified by In table add the IP address  and
optionally  netmask,  specified  by Only the tables of type "lpm"
support masks.  In table remove the  IP  address  and  optionally
netmask,  specified  by  Only  the  tables  of type "lpm" support
masks.  Query the table for a specific IP address,  specified  by
If  no mask is specified, a single host is assumed.  List all en‐
tries in the currently loaded table specified by  This  operation
is expensive and should be used with caution.  Replace the exist‐
ing table specified by with a new table built from the file spec‐
ified by Optionally, the new table will: be named effectively re‐
naming  the table.  If not specified, the name of the table being
replaced will be used.  be of type currently supported types  are
or If not specified, the type of the table being replaced will be
used.   Save the active configuration with a snapshot of the cur‐
rent connections.  The data will be stored in the file  unless  a
file  is  specified  by Administrator may want to stop the packet
inspection before saving.  Load the saved configuration file  and
the  connections  from  the file.  Note that any existing connec‐
tions will be destroyed.  Administrator may want to start  packet
inspection  after  the  load.  Print various statistics.  Process
the active configuration (if is set), the given binary configura‐
tion (if is set) or the given  the  plain  configuration  (if  is
set).   Print the byte‐code of each rule and the encoded configu‐
ration data.  Also, if is set,  write  the  binary  configuration
data  into  the given file.  This is primarily for developer use.
Display a list of tracked connections: Display only IPv4  connec‐
tions.   Display  only IPv6 connections.  Don’t display a header.
Try to resolve addresses.  Only show NAT  connections.   Restrict
the  display  width.   Display only connections through the named
interface.  Reloading the configuration is a relatively expensive
operation.  Therefore, frequent reloads should be  avoided.   Use
of tables should be considered as an alternative design.  See for
details.   control device default configuration file Starting the
NPF packet filter: # npfctl reload # npfctl start #  npfctl  show
Addition and removal of entries in the table whose ID is "vip": #
npfctl  table  "vip"  add  10.0.0.1  #  npfctl  table  "vip"  rem
182.168.0.0/24 Replacing the existing table which  has  ID  "svr"
with  a  new  const table populated from file "/tmp/npf_vps_new",
and renamed to "vps": # npfctl table "svr" replace  ‐n  "vps"  ‐t
const  "/tmp/npf_vps_new"  NPF first appeared in NPF was designed
and implemented by
