_____p_p_c___g_e_t___t_i_m_e_b_a_s_e(3)      Library Functions Manual      _____p_p_c___g_e_t___t_i_m_e_b_a_s_e(3)

NNAAMMEE
       __ppc_get_timebase,  __ppc_get_timebase_freq - get the current value of
       the Time Base Register on Power architecture and its frequency.

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssyyss//ppllaattffoorrmm//ppppcc..hh>>

       uuiinntt6644__tt ____ppppcc__ggeett__ttiimmeebbaassee((vvooiidd));;
       uuiinntt6644__tt ____ppppcc__ggeett__ttiimmeebbaassee__ffrreeqq((vvooiidd));;

DDEESSCCRRIIPPTTIIOONN
       ____ppppcc__ggeett__ttiimmeebbaassee() reads the current value of the Time Base  Register
       and returns its value, while ____ppppcc__ggeett__ttiimmeebbaassee__ffrreeqq() returns the fre‐
       quency in which the Time Base Register is updated.

       The Time Base Register is a 64‐bit register provided by Power Architec‐
       ture  processors.   It stores a monotonically incremented value that is
       updated at a system‐dependent frequency that may be different from  the
       processor frequency.

RREETTUURRNN VVAALLUUEE
       ____ppppcc__ggeett__ttiimmeebbaassee()  returns a 64‐bit unsigned integer that represents
       the current value of the Time Base Register.

       ____ppppcc__ggeett__ttiimmeebbaassee__ffrreeqq() returns a 64‐bit unsigned integer that repre‐
       sents the frequency at which the Time Base Register is updated.

SSTTAANNDDAARRDDSS
       GNU.

HHIISSTTOORRYY
       ____ppppcc__ggeett__ttiimmeebbaassee()
              glibc 2.16.

       ____ppppcc__ggeett__ttiimmeebbaassee__ffrreeqq()
              glibc 2.17.

EEXXAAMMPPLLEESS
       The following program will calculate the time, in  microseconds,  spent
       between two calls to ____ppppcc__ggeett__ttiimmeebbaassee().

   PPrrooggrraamm ssoouurrccee

       #include <inttypes.h>
       #include <stdint.h>
       #include <stdio.h>
       #include <stdlib.h>
       #include <sys/platform/ppc.h>

       /* Maximum value of the Time Base Register: 2^60 - 1.
          Source: POWER ISA.  */
       #define MAX_TB 0xFFFFFFFFFFFFFFF

       int
       main(void)
       {
           uint64_t tb1, tb2, diff;
           uint64_t freq;

           freq = __ppc_get_timebase_freq();
           printf("Time Base frequency = %"PRIu64" Hz\n", freq);

           tb1 = __ppc_get_timebase();

           // Do some stuff...

           tb2 = __ppc_get_timebase();

           if (tb2 > tb1) {
               diff = tb2 - tb1;
           } else {
               /* Treat Time Base Register overflow.  */
               diff = (MAX_TB - tb2) + tb1;
           }

           printf("Elapsed time  = %1.2f usecs\n",
                  (double) diff * 1000000 / freq);

           exit(EXIT_SUCCESS);
       }

SSEEEE AALLSSOO
       ttiimmee(2), uusslleeeepp(3)

Linux man‐pages (unreleased)      2025‐05‐17             _____p_p_c___g_e_t___t_i_m_e_b_a_s_e(3)
