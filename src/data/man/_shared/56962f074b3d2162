_s_e_t___t_i_d___a_d_d_r_e_s_s(2)            System Calls Manual           _s_e_t___t_i_d___a_d_d_r_e_s_s(2)

NNAAMMEE
       set_tid_address - set pointer to thread ID

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssyyss//ssyyssccaallll..hh>>      /* Definition of SSYYSS__** constants */
       ##iinncclluuddee <<uunniissttdd..hh>>

       ppiidd__tt ssyyssccaallll((SSYYSS__sseett__ttiidd__aaddddrreessss,, iinntt **_t_i_d_p_t_r));;

       _N_o_t_e:  glibc  provides  no wrapper for sseett__ttiidd__aaddddrreessss(), necessitating
       the use of ssyyssccaallll(2).

DDEESSCCRRIIPPTTIIOONN
       For each thread, the kernel maintains two attributes (addresses) called
       _s_e_t___c_h_i_l_d___t_i_d and _c_l_e_a_r___c_h_i_l_d___t_i_d.  These two  attributes  contain  the
       value NULL by default.

       _s_e_t___c_h_i_l_d___t_i_d
              If  a thread is started using cclloonnee(2) with the CCLLOONNEE__CCHHIILLDD__SSEETT‐‐
              TTIIDD flag, _s_e_t___c_h_i_l_d___t_i_d is set to the value passed in  the  _c_t_i_d
              argument of that system call.

              When  _s_e_t___c_h_i_l_d___t_i_d  is set, the very first thing the new thread
              does is to write its thread ID at this address.

       _c_l_e_a_r___c_h_i_l_d___t_i_d
              If   a   thread   is   started   using   cclloonnee(2)    with    the
              CCLLOONNEE__CCHHIILLDD__CCLLEEAARRTTIIDD  flag,  _c_l_e_a_r___c_h_i_l_d___t_i_d is set to the value
              passed in the _c_t_i_d argument of that system call.

       The system call sseett__ttiidd__aaddddrreessss() sets the  _c_l_e_a_r___c_h_i_l_d___t_i_d  value  for
       the calling thread to _t_i_d_p_t_r.

       When  a  thread  whose _c_l_e_a_r___c_h_i_l_d___t_i_d is not NULL terminates, then, if
       the thread is sharing memory with other threads, then 0 is  written  at
       the  address  specified  in _c_l_e_a_r___c_h_i_l_d___t_i_d and the kernel performs the
       following operation:

           futex(clear_child_tid, FUTEX_WAKE, 1, NULL, NULL, 0);

       The effect of this operation is to wake a single thread  that  is  per‐
       forming  a  futex  wait  on the memory location.  Errors from the futex
       wake operation are ignored.

RREETTUURRNN VVAALLUUEE
       sseett__ttiidd__aaddddrreessss() always returns the caller’s thread ID.

EERRRROORRSS
       sseett__ttiidd__aaddddrreessss() always succeeds.

SSTTAANNDDAARRDDSS
       Linux.

HHIISSTTOORRYY
       Linux 2.5.48.

       Details as given here are valid since Linux 2.5.49.

SSEEEE AALLSSOO
       cclloonnee(2), ffuutteexx(2), ggeettttiidd(2)

Linux man‐pages (unreleased)      2025‐05‐17                _s_e_t___t_i_d___a_d_d_r_e_s_s(2)
