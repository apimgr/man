subsystem  The  command  is used to manipulate the file integrity
subsystem.  Load the fingerprint entries contained in  if  speci‐
fied,  or  the default signatures file otherwise.  This operation
is only allowed in learning mode (strict level zero).   The  fol‐
lowing  flags are allowed with this command: Evaluate fingerprint
on load, as opposed to when the file is accessed.  Keep the file‐
names in the entry for more  accurate  logging.   Enable  verbose
output.   Delete either a single entry or all entries on from be‐
ing monitored by Dump the database from the kernel.  Only entries
that have the filename will be presented.  This can  be  used  to
recover  a  lost  database:  # veriexecctl dump > /etc/signatures
Delete all entries in the database.  Query for information  asso‐
ciated  with Filename, mount, fingerprint, fingerprint algorithm,
evaluation status, and entry type.  pseudo‐device default  signa‐
tures  file  first appeared in The kernel is expected to have the
pseudo‐device.
