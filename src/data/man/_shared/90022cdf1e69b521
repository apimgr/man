creates  a database for the specified file.  If no file is speci‐
fied, then is used.  The database is installed into for CDB  for‐
mat  and  into for DB format (see The file must be in the correct
format (see The options are as follows: Put the output  databases
in the named file.  Don’t warn about duplicate services.  This is
the  default  behavior.  Print the services file to stdout, omit‐
ting duplicate entries and comments.  Create a  specific  version
of  the  database  style.   can be to request the CDB format (de‐
fault) or to request the DB format.  The DB format is useful  for
compatibility  with  old  statically linked binaries.  Warn about
duplicate services.  The databases are used by the C library ser‐
vices routines (see exits zero on success, non‐zero  on  failure.
The  current  services  database.  A temporary file.  The current
services file.  Because guarantees not to install a partial  des‐
tination  file  it  must  build a temporary file in the same file
system and if successful use  to  install  over  the  destination
file.   If fails it will leave the previous version of the desti‐
nation file intact.
