If the file exists, a record of individual login and logout times
are  written  to  it  by  and respectively.  The program examines
these records and writes the accumulated connect time for all lo‐
gins to the standard output.  Options available: Display the con‐
nect times in 24 hour chunks.  Display  individual  user  totals.
Only do accounting logins on certain ttys.  The specification can
start with to indicate not this and end with to indicate all sim‐
ilarly  named  ttys.   Multiple flags may be specified.  Read raw
connect time data from instead of the default file Display totals
for the given individuals only.  If no arguments are given,  dis‐
plays  the  total amount of login time for all active accounts on
the system.  The default file is an  infinitely  increasing  file
unless  frequently truncated.  This is normally done by the daily
daemon scripts scheduled by which rename and rotate the files be‐
fore truncating them (and keep about a week’s worth on hand).  No
login times are collected, however, if the file does  not  exist.
For  example, ac ‐p ‐t "ttyd*" > modems ac ‐p ‐t "!ttyd*" > other
allows times recorded in to be charged out at  a  different  rate
than  connect  time  accounting file rotated files An command ap‐
peared in This version of was written for from the  specification
provided by various systems’ manual pages.
