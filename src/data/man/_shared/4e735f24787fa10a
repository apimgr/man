KVM_NLIST(3)             BSD Library Functions Manual             KVM_NLIST(3)

NNAAMMEE
     kkvvmm__nnlliisstt, kkvvmm__nnlliisstt22 — retrieve symbol table names from a kernel image

LLIIBBRRAARRYY
     Kernel Data Access Library (libkvm, -lkvm)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<kkvvmm..hh>>
     ##iinncclluuddee <<nnlliisstt..hh>>

     _i_n_t
     kkvvmm__nnlliisstt(_k_v_m___t _*_k_d, _s_t_r_u_c_t _n_l_i_s_t _*_n_l);

     _i_n_t
     kkvvmm__nnlliisstt22(_k_v_m___t _*_k_d, _s_t_r_u_c_t _k_v_m___n_l_i_s_t _*_n_l);

DDEESSCCRRIIPPTTIIOONN
     The kkvvmm__nnlliisstt() function retrieves the symbol table entries indicated by
     the name list argument _n_l.  This argument points to an array of nlist
     structures, terminated by an entry whose _n___n_a_m_e field is NULL (see
     nlist(3)).  Each symbol is looked up using the _n___n_a_m_e field, and if
     found, the corresponding _n___t_y_p_e and _n___v_a_l_u_e fields are filled in.  These
     fields are set to 0 if the symbol is not found.

     The kldsym(2) system call is used to locate symbols in live kernels.
     This is a less than perfect emulation of the nlist values but has the ad‐
     vantage of being aware of kernel modules and is reasonably fast.

     The kkvvmm__nnlliisstt22() function retrieves the symbol table entries indicated by
     the name list argument _n_l.  This argument points to an array of _s_t_r_u_c_t
     _k_v_m___n_l_i_s_t structures, terminated by an entry whose _n___n_a_m_e field is NULL
     These structures are similar to the nlist structures used by kkvvmm__nnlliisstt()
     except that the _n___v_a_l_u_e field uses a different type (_k_v_a_d_d_r___t) to avoid
     truncation when examining non-native kernel images.

RREETTUURRNN VVAALLUUEESS
     The kkvvmm__nnlliisstt() and kkvvmm__nnlliisstt22() functions return the number of invalid
     entries found.  If the kernel symbol table was unreadable, -1 is re‐
     turned.

SSEEEE AALLSSOO
     kldsym(2), kvm(3), kvm_close(3), kvm_getargv(3), kvm_getenvv(3),
     kvm_geterr(3), kvm_getprocs(3), kvm_native(3), kvm_open(3),
     kvm_openfiles(3), kvm_read(3), kvm_write(3)

HHIISSTTOORRYY
     The kkvvmm__nnlliisstt22() function first appeared in FreeBSD 11.0.

BSD                            November 27, 2015                           BSD
