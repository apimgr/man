SFTP-SERVER(8)            BSD System Manager's Manual           SFTP-SERVER(8)

NNAAMMEE
     ssffttpp--sseerrvveerr — OpenSSH SFTP server subsystem

SSYYNNOOPPSSIISS
     ssffttpp--sseerrvveerr [--eehhRR] [--dd _s_t_a_r_t___d_i_r_e_c_t_o_r_y] [--ff _l_o_g___f_a_c_i_l_i_t_y] [--ll _l_o_g___l_e_v_e_l]
                 [--PP _d_e_n_i_e_d___r_e_q_u_e_s_t_s] [--pp _a_l_l_o_w_e_d___r_e_q_u_e_s_t_s] [--uu _u_m_a_s_k]
     ssffttpp--sseerrvveerr --QQ _p_r_o_t_o_c_o_l___f_e_a_t_u_r_e

DDEESSCCRRIIPPTTIIOONN
     ssffttpp--sseerrvveerr is a program that speaks the server side of SFTP protocol to
     stdout and expects client requests from stdin.  ssffttpp--sseerrvveerr is not in‐
     tended to be called directly, but from sshd(8) using the SSuubbssyysstteemm op‐
     tion.

     Command-line flags to ssffttpp--sseerrvveerr should be specified in the SSuubbssyysstteemm
     declaration.  See sshd_config(5) for more information.

     Valid options are:

     --dd _s_t_a_r_t___d_i_r_e_c_t_o_r_y
             Specifies an alternate starting directory for users.  The path‐
             name may contain the following tokens that are expanded at run‐
             time: %% is replaced by a literal '%', %d is replaced by the home
             directory of the user being authenticated, and %u is replaced by
             the username of that user.  The default is to use the user's home
             directory.  This option is useful in conjunction with the
             sshd_config(5) CChhrroooottDDiirreeccttoorryy option.

     --ee      Causes ssffttpp--sseerrvveerr to print logging information to stderr instead
             of syslog for debugging.

     --ff _l_o_g___f_a_c_i_l_i_t_y
             Specifies the facility code that is used when logging messages
             from ssffttpp--sseerrvveerr.  The possible values are: DAEMON, USER, AUTH,
             LOCAL0, LOCAL1, LOCAL2, LOCAL3, LOCAL4, LOCAL5, LOCAL6, LOCAL7.
             The default is AUTH.

     --hh      Displays ssffttpp--sseerrvveerr usage information.

     --ll _l_o_g___l_e_v_e_l
             Specifies which messages will be logged by ssffttpp--sseerrvveerr.  The pos‐
             sible values are: QUIET, FATAL, ERROR, INFO, VERBOSE, DEBUG, DE‐
             BUG1, DEBUG2, and DEBUG3.  INFO and VERBOSE log transactions that
             ssffttpp--sseerrvveerr performs on behalf of the client.  DEBUG and DEBUG1
             are equivalent.  DEBUG2 and DEBUG3 each specify higher levels of
             debugging output.  The default is ERROR.

     --PP _d_e_n_i_e_d___r_e_q_u_e_s_t_s
             Specifies a comma-separated list of SFTP protocol requests that
             are banned by the server.  ssffttpp--sseerrvveerr will reply to any denied
             request with a failure.  The --QQ flag can be used to determine the
             supported request types.  If both denied and allowed lists are
             specified, then the denied list is applied before the allowed
             list.

     --pp _a_l_l_o_w_e_d___r_e_q_u_e_s_t_s
             Specifies a comma-separated list of SFTP protocol requests that
             are permitted by the server.  All request types that are not on
             the allowed list will be logged and replied to with a failure
             message.

             Care must be taken when using this feature to ensure that re‐
             quests made implicitly by SFTP clients are permitted.

     --QQ _p_r_o_t_o_c_o_l___f_e_a_t_u_r_e
             Queries protocol features supported by ssffttpp--sseerrvveerr.  At present
             the only feature that may be queried is “requests”, which may be
             used to deny or allow specific requests (flags --PP and --pp respec‐
             tively).

     --RR      Places this instance of ssffttpp--sseerrvveerr into a read-only mode.  At‐
             tempts to open files for writing, as well as other operations
             that change the state of the filesystem, will be denied.

     --uu _u_m_a_s_k
             Sets an explicit umask(2) to be applied to newly-created files
             and directories, instead of the user's default mask.

     On some systems, ssffttpp--sseerrvveerr must be able to access _/_d_e_v_/_l_o_g for logging
     to work, and use of ssffttpp--sseerrvveerr in a chroot configuration therefore re‐
     quires that syslogd(8) establish a logging socket inside the chroot di‐
     rectory.

SSEEEE AALLSSOO
     sftp(1), ssh(1), sshd_config(5), sshd(8)

     T. Ylonen and S. Lehtinen, _S_S_H _F_i_l_e _T_r_a_n_s_f_e_r _P_r_o_t_o_c_o_l, draft-ietf-secsh-
     filexfer-02.txt, October 2001, work in progress material.

HHIISSTTOORRYY
     ssffttpp--sseerrvveerr first appeared in OpenBSD 2.8.

AAUUTTHHOORRSS
     Markus Friedl <_m_a_r_k_u_s_@_o_p_e_n_b_s_d_._o_r_g>

BSD                              July 27, 2021                             BSD
