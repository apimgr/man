PMC_INIT(3)              BSD Library Functions Manual              PMC_INIT(3)

NNAAMMEE
     ppmmcc__iinniitt — initialize library

LLIIBBRRAARRYY
     Performance Counters Library (libpmc, -lpmc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ppmmcc..hh>>

     _i_n_t
     ppmmcc__iinniitt(_v_o_i_d);

DDEESSCCRRIIPPTTIIOONN
     Function ppmmcc__iinniitt() initializes the PMC library.  This function must be
     called before any of the other functions in the library.

RREETTUURRNN VVAALLUUEESS
     The ppmmcc__iinniitt() function returns the value 0 if successful; otherwise the
     value -1 is returned and the global variable _e_r_r_n_o is set to indicate the
     error.

EERRRROORRSS
     A call to ppmmcc__iinniitt() may fail with the following errors:

     [ENOENT]           The hwpmc(4) module was not found in the kernel.

     [EPROGMISMATCH]    The library's version number did not match that ex‐
                        pected by hwpmc(4).

     [ENXIO]            PMC hardware on this system is unsupported.

SSEEEE AALLSSOO
     pmc(3), hwpmc(4)

BSD                            November 24, 2007                           BSD
