EDDSA is the name given in the CBOR Object Signing and Encryption
(COSE) RFC to EDDSA over Curve25519 with SHA‐512.  The COSE EDDSA
API  of  is an auxiliary API with routines to convert between the
different EDDSA public key types used in and In EDDSA public keys
are abstracted by the type.  The function returns a pointer to  a
newly allocated, empty type.  If memory cannot be allocated, NULL
is returned.  The function releases the memory backing where must
have been previously allocated by On return, is set to NULL.  Ei‐
ther  or may be NULL, in which case is a NOP.  The function fills
with the contents of No references to  are  kept.   The  function
fills  with the contents of where points to bytes.  No references
to are kept.  The function converts to  a  newly  allocated  type
with  a  reference count of 1.  No internal references to the re‐
turned pointer are kept.  If an error occurs, returns NULL.   The
and  functions  return  on  success.  On error, a different error
code defined in is returned.
