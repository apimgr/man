prints  up  to  10 chars of each in large letters on the standard
output.  The options are: Use as the background color (character)
rather than the default Use as the foreground  color  (character)
rather  than  the  default  If  is  then  the actual letter being
printed is used as the foreground color in the same way that  LPD
does  it.   The results are quite variable.  Use the more attrac‐
tive LPD font instead of the traditional font.  The utility first
appeared in
