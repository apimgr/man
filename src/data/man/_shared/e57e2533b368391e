system  bootstrapping  procedures This document provides informa‐
tion on using common features in the boot loader.  Additional in‐
formation may be found in architecture‐specific manual pages.  In
the native boot protocol, options are passed from the boot loader
to the kernel via flag bits in the variable (see Some boot  load‐
ers may also support other boot protocols.  Some boot loaders may
present  a menu, which may be configured via In interactive mode,
the boot loader will present a prompt, allowing  input  of  these
commands:  The  default  will  be  set  to the disk that the boot
loader was loaded from.  To boot from an alternate disk, the full
name of the device should be given at the prompt.  is of the form
where is the device from which to boot, is the unit  number,  and
is the partition letter.  The following list of supported devices
may  vary  from installation to installation: Hard disks.  Floppy
drives.  The default is if the boot loader fails to  successfully
open  that  image,  it  then tries (expected to be a kernel image
compressed by gzip), followed by and finally Alternate system im‐
ages can be loaded by just specifying the name of the image.  Op‐
tions are: Sets the machine‐dependent flag in Sets  the  machine‐
dependent flag in Sets the machine‐dependent flag in Sets the ma‐
chine‐dependent  flag  in Sets the flag in This causes the kernel
to prompt for the root file system device, the system crash  dump
device,  and  the path to Sets the flag in This causes subsequent
reboot attempts to halt instead of rebooting.  Sets the  flag  in
This  causes the kernel to enter the device configuration manager
as soon as possible during the boot.  allows devices  to  be  en‐
abled  or  disabled, and allows device locators (such as hardware
addresses or bus numbers) to be modified before  the  kernel  at‐
tempts to attach the devices.  Sets the flag in Requests the ker‐
nel  to enter debug mode, in which it waits for a connection from
a kernel debugger; see Sets the flag in Informs the kernel that a
mini‐root file system is present in memory.   Sets  the  flag  in
Boot  the system in quiet mode.  Sets the flag in Boot the system
in single‐user mode.  Sets the flag in Boot the system in verbose
mode.  Sets the flag in Boot the system with debug  messages  en‐
abled.  Sets the flag in Boot the system in silent mode.  Immedi‐
ately  switch the console to the specified device and reprint the
banner.  must be one of or See in Set the default drive and  par‐
tition  for  subsequent  filesystem operations.  Without an argu‐
ment, print the current setting.  is of  the  form  specified  in
Print  an  overview about commands and arguments.  Print a direc‐
tory listing of containing inode number, filename, and file type.
can contain a device specification.  Reboot the  system.   In  an
emergency,  the  bootstrap  methods described in the installation
notes for the specific architecture can be  used.   boot  program
code  loaded by the primary bootstrap system code gzip‐compressed
system code master copy of the boot program (copy to /boot)  pri‐
mary bootstrap for filesystem type fstype, copied to the start of
the  partition by Architecture‐specific manual pages (such as The
kernel file name must be specified before, not  after,  the  boot
options.  Any specified after the boot options, e.g.: is ignored,
and the default kernel is booted.
