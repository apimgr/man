The  command  produces  output similar to but for all machines on
the local network.  If no report has been received from a machine
for 11 minutes then assumes the machine is down, and does not re‐
port the users last known to be logged into that machine.   If  a
user  hasn’t  typed  to the system for a minute or more, then re‐
ports this idle time.  Include all users.  By default, if a  user
hasn’t  typed  to  the  system for an hour or more, then the user
will be omitted from the output.  Write column headings above the
regular output.  List only the names and the number of users cur‐
rently logged on.  When this option is used,  all  other  options
are  ignored.   information  about other machines The command ap‐
peared in This is unwieldy when the number of machines on the lo‐
cal net is large.
