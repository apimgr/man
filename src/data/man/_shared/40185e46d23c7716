is  used  to  initialize and clear file systems before first use.
Before running the disk must be labeled using builds a file  sys‐
tem  on  the  specified special device basing its defaults on the
information in the disk label.  Typically the defaults  are  rea‐
sonable, however has numerous options to allow the defaults to be
selectively  overridden.  Options with numeric arguments may con‐
tain an optional (case‐insensitive) suffix: Bytes; causes no mod‐
ification.  (Default) Kilo; multiply the argument by 1024.  Mega;
multiply the argument by 1048576.  Giga; multiply the argument by
1073741824.  Tera; multiply the argument by  1099511627776.   The
following  options define the general layout policies.  This sets
the obsolete maxcontig parameter.  Specify the metadata byte  or‐
der  of the file system to be created.  Valid byte orders are and
If no byte order is specified, the file system is created in host
byte order.  The block size of the file  system,  in  bytes.   It
must  be  a  power  of  two.  The smallest allowable size is 4096
bytes.  The default size depends upon the size of the  file  sys‐
tem:  4  KB  8 KB 16 KB 32 KB Set the maximum extent size to This
indicates the maximum number of blocks any single file can  allo‐
cate  out  of a cylinder group before it is forced to begin allo‐
cating blocks from another cylinder group.  The default is  about
one  quarter  of  the  total blocks in a cylinder group.  See for
more details on how to set this option.  Create a file system im‐
age in The file system size needs to be  specified  with  No  at‐
tempts  to  use or update the disk label will be made.  The frag‐
ment size of the file system in bytes.  It must be a power of two
ranging in value between and The optimal ratio is 8:1.  Other ra‐
tios are possible, but are not recommended, and may  produce  un‐
predictable  results.   The default size depends upon the size of
the file system: 0.5 KB 1 KB 2 KB 4 KB Treat  garbage  parameters
as  non‐fatal.   Using  this  option  may result in a file system
which causes a kernel panic and should only be used for  testing.
The expected average file size for the file system.  The expected
average number of files per directory on the file system.  Do not
require  that the file system type listed in the disk label is or
This specifies the density of inodes  in  the  file  system.   If
fewer inodes are desired, a larger number should be used; to cre‐
ate more inodes a smaller number should be given.  The default is
to  create an inode for every bytes of data space: 2 KB 4 KB 8 KB
16 KB The percentage of space reserved  from  normal  users;  the
minimum free space threshold.  The default value used is 5%.  See
for more details on how to set this option.  Causes the file sys‐
tem parameters to be printed out without really creating the file
system.   This specifies the number of inodes for the filesystem.
If both and are specified  then  takes  precedence.   Select  the
filesystem‐format.   4.3BSD;  This  option  is  primarily used to
build root file systems that can  be  understood  by  older  boot
ROMs.   This  generates an FFSv1 file system with level 1 format.
FFSv1; normal Fast File System, level 4 format.  Also known as or
This is the default.  FFSv2; enhanced Fast  File  System,  suited
for  more  than 1 Terabyte capacity.  This is also known as FFSv2
plus support for extended attributes and  access  control  lists.
See  for  more information about format levels.  To create an LFS
filesystem see To create a Linux ext2 filesystem see Optimization
preference; either or The file system can either be instructed to
try to minimize the time spent allocating blocks, or  to  try  to
minimize  the  space  fragmentation on the disk.  If the value of
minfree (see above) is less than 5%, the default is  to  optimize
for  space;  if  the value of minfree is greater than or equal to
5%, the default is to optimize for time.  See for more details on
how to set this option.  enable a quota.  can be one of or to en‐
able the specified quota type.  Multiple can be  used  to  enable
all  types  at once.  The size of the file system in sectors.  An
suffix will be interpreted as the  number  of  sectors  (the  de‐
fault).   All other suffixes are interpreted as per other numeric
arguments, except that the number is converted  into  sectors  by
dividing  by the sector size (as specified by after suffix inter‐
pretation.  If no is specified then the filesystem size  defaults
to that of the partition, or, if is specified, the existing file.
If  is negative the specified size is subtracted from the default
size (reserving space at the end of the partition).  Uses  infor‐
mation  for  the specified disk from instead of trying to get the
information from the disk label.  This controls the amount of in‐
formation written to stdout: No output.  Overall size and  cylin‐
der  group  details.   A  progress bar (dots ending at right hand
margin).  The first few super‐block  backup  sector  numbers  are
displayed  before  the  progress bar.  All the super‐block backup
sector numbers are displayed (no progress bar).  The  default  is
3.   If  is  specified  stops before outputting the progress bar.
This specifies that an Apple UFS  filesystem  should  be  created
with the given volume name.  Pre‐zeros the file system image cre‐
ated  with  The following option overrides the standard sizes for
the disk geometry.  The default value is taken from the disk  la‐
bel.   Changing this default is useful only when using to build a
file system whose raw image will eventually be used on a  differ‐
ent  type  of  disk than the one on which it is initially created
(for example on a write‐once  disk).   Note  that  changing  this
value  from  its  default will make it impossible for to find the
alternative superblocks if the standard superblock is lost.   The
size  of  a sector in bytes (almost never anything but 512).  De‐
faults to 512.  The file system is created with inode  generation
numbers  to improve NFS security.  The owner and group IDs of the
root node of the new file system are set to the effective UID and
GID of the user initializing the file system.  For the command to
succeed, the disk label should first be  updated  such  that  the
fstype  field  for  the partition is set to or unless or is used.
To create and populate a filesystem image within a file  use  the
utility.  The partition size is found using not by inspecting the
disk  label.   The  block  size and fragment size will be written
back to the disk label only if the last character  of  references
the  same partition as the minor device number.  Unless is speci‐
fied, must be a raw device.  This means that for example or  must
be specified instead of The command appeared in
