_i_s_a_t_t_y(3)                  Library Functions Manual                  _i_s_a_t_t_y(3)

NNAAMMEE
       isatty - test whether a file descriptor refers to a terminal

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<uunniissttdd..hh>>

       iinntt iissaattttyy((iinntt _f_d));;

DDEESSCCRRIIPPTTIIOONN
       The  iissaattttyy()  function tests whether _f_d is an open file descriptor re‐
       ferring to a terminal.

RREETTUURRNN VVAALLUUEE
       iissaattttyy() returns 1 if _f_d is an open file descriptor referring to a ter‐
       minal; otherwise 0 is returned, and _e_r_r_n_o is set to indicate the error.

EERRRROORRSS
       EEBBAADDFF  _f_d is not a valid file descriptor.

       EENNOOTTTTYY _f_d refers to a file other than a terminal.  On some  older  ker‐
              nels,  some  types of files resulted in the error EEIINNVVAALL in this
              case (which is a violation of POSIX, which specifies  the  error
              EENNOOTTTTYY).

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       iissaattttyy() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       POSIX.1‐2008.

HHIISSTTOORRYY
       POSIX.1‐2001, SVr4, 4.3BSD.

SSEEEE AALLSSOO
       ffssttaatt(2), ttttyynnaammee(3)

Linux man‐pages (unreleased)      2025‐05‐17                         _i_s_a_t_t_y(3)
