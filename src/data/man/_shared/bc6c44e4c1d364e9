The  utility  provides  the necessary functionality to manipulate
GUID partition tables but see below for how and where functional‐
ity is missing.  The basic usage model of the tool  follows  that
of  the tool.  The general options are described in the following
paragraph.  The remaining paragraphs describe the individual com‐
mands with their options.  Here we conclude by mentioning that  a
is either a special file corresponding to a disk‐like device or a
regular  file.  The command is applied to each listed on the com‐
mand line.  The general options allow the user to change  default
settings  or otherwise change the behaviour that is applicable to
all commands.  Not all commands use all default settings, so some
general options may not have an effect on all  commands.   Ignore
existing  MBR  (Hybrid MBR/GPT mode).  Override the default media
size for the device (obtained from the kernel if possible) or de‐
faulting to the file size for plain files.   Do  not  update  the
wedge information that changed.  You need to use the command man‐
ually update the device’s wedge configuration if you do that.  Do
not  print  error  messages.   This is not implemented completely
yet.  Open the device for reading only.  Currently this option is
primarily useful for the command, but the intent is to use it  to
implement  dry‐run  behaviour.   Override the default sector size
for the device (obtained from the  kernel  if  possible)  or  for
plain  files.  Specify a timestamp to be used for uuid generation
so that uuids are not random and can  be  consistent  for  repro‐
ducible builds.  The timestamp can be a pathname, where the time‐
stamps  are  derived  from that file, a parseable date for parse‐
date(3) (this option is not yet available in the tools build), or
an integer value interpreted as the number of  seconds  from  the
Epoch.   Controls  the verbosity level.  The level increases with
every occurrence of this option.  There is no formalized  defini‐
tion of the different levels yet.  The command allows the user to
add  a  new  partition to an existing table.  By default, it will
create a UFS partition covering the first available block  of  an
unused  disk  space.  The command‐specific options can be used to
control this behaviour.  The option allows the user to specify an
alignment for the start and size.   The  alignment  is  given  in
bytes  and may have a suffix to indicate its magnitude.  will at‐
tempt to align the partition.  The  option  allows  the  user  to
specify  the starting (beginning) sector number of the partition.
The minimum sector number is 1, but has to fall inside an  unused
region  of disk space that is covered by the GPT.  The option al‐
lows the user to specify which (free) entry in the GPT  table  is
to be used for the new partition.  By default, the first free en‐
try  is  selected.  The option allows the user to specify a label
for the partition.  The option allows the  user  to  specify  the
size  of  the partition.  If there is no suffix, or the suffix is
or then size is in sectors, otherwise size is in bytes which must
be a multiple of the device’s sector size.  Accepted suffix units
(case insensitive) are to denote bytes, to denote  kilobytes,  to
denote megabytes and to denote gigabytes, to denote terabytes, to
denote  petabytes, and to denote exabytes.  The minimum size is 1
sector.  The option allows the  user  to  specify  the  partition
type.   The type is given as an UUID, but accepts Apple HFS Apple
UFS BIOS Boot EFI System legacy swap  UFS/UFS2  vinum  ZFS  Linux
data Linux RAID Linux swap Linux LVM Microsoft basic data ‐ NTFS,
FAT32  ("msdos"), FAT16, also used for UDF Microsoft reserved ccd
component Cryptographic Disk FFSv1/FFSv2 LFS RAIDFrame  component
swap  as aliases for the most commonly used partition types.  The
command dumps the MBR or (PMBR) and GPT partition tables to stan‐
dard output or to a file specified by the argument in a format to
be used by the command.  The format is a plist.  It should not be
modified.  The command allows the user to configure the partition
that contains the primary bootstrap program, used during The  op‐
tions  sets  the PMBR partition active.  This should not normally
be necessary, but some firmware might require it.  If is omitted,
the active flag will be cleared from the PMBR label.  The  option
allows  the  user  to specify the filename from which should read
the bootcode.  The default is to read  from  The  partition  that
should  contain the primary bootstrap code, is selected using the
and options.  One of these three options is required.  The option
selects the partition given by the The option selects the  parti‐
tion by If there are multiple partitions with the same label, the
first  one  found will be used.  The option selects the partition
starting at block The command allows the user  to  create  a  new
(empty) GPT.  By default, one cannot create a GPT when the device
contains  a  MBR, however this can be overridden with the option.
If the option is specified, an existing MBR is destroyed and  any
partitions  described  by the MBR are lost.  The options sets the
PMBR partition active.  The option tells to create only the  pri‐
mary  table and not the backup table.  This option is only useful
for debugging and should  not  be  used  otherwise.   The  option
changes the default number of partitions the GPT can accommodate.
This  is  used  whenever  a  new GPT is created.  By default, the
utility will create space for 128 partitions (or  32  sectors  of
512  bytes).  The command allows the user to destroy an existing,
possibly not empty GPT.  The option instructs to destroy the  ta‐
ble in a way that it can be recovered.  The command displays size
information  about  the media and information from the GPT header
if it exists.  The command allows the user to  label  any  parti‐
tions  that  match  the selection.  At least one of the following
selection options must be specified.  The option  specifies  that
all  partitions should be labeled.  It is mutually exclusive with
all other selection options.  The option  selects  the  partition
that  starts  at  the given block number.  The option selects the
partition with the given partition number.   The  option  selects
all  partitions that have the given label.  This can cause multi‐
ple partitions to be relabeled.  The option  selects  all  parti‐
tions  that  have the given size.  This can cause multiple parti‐
tions to be labeled.  The option selects all partitions that have
the given type.  The type is given as an UUID or by  the  aliases
that  the command accepts.  This can cause multiple partitions to
be labeled.  The or options specify the new label to be  assigned
to the selected partitions.  The option is used to read the label
from  the  specified  file.  Only the first line is read from the
file and the trailing newline character is stripped.  If the file
name is the dash or minus sign the label is read from  the  stan‐
dard  input.  The option is used to specify the label in the com‐
mand line.  The label is assumed to be  encoded  in  UTF‐8.   The
command allows the user to migrate an MBR‐based disk partitioning
into  a  GPT‐based  partitioning.  By default, the MBR is not mi‐
grated when it contains partitions of an unknown type.  This  can
be  overridden with the option.  Specifying the option will cause
unknown partitions to be ignored and any data in it to  be  lost.
The  options sets the PMBR partition active.  The option prevents
migrating disk labels into GPT partitions  by  creating  the  GPT
equivalent of a slice.  Note that the option is not applicable to
partitions.   The option changes the default number of partitions
the GPT can accommodate.  This is used whenever a new GPT is cre‐
ated.  By default, the utility will create space for  128  parti‐
tions  (or  32 sectors of 512 bytes).  The command requires space
at the beginning and the end of the device outside any partitions
to store the GPTs.  Space is required for the GPT header and  the
GPT partition table.  See the option for the size of the GPT par‐
tition  table.  By default, just about all devices have a minimum
of 62 sectors free at the beginning of the  device,  but  do  not
have  any  free  space at the end.  For the default GPT partition
table size on a 512 byte sector size device, 33  sectors  at  the
end  of  the device would need to be freed.  The command tries to
restore the GPT partition label from the backup near the  end  of
the  disk.   It  is  very  useful  in  case the primary label was
deleted.  The command allows the user to remove any and all  par‐
titions that match the selection.  It uses the same selection op‐
tions  as  the command.  See above for a description of these op‐
tions.  Partitions are removed by clearing  the  partition  type.
No  other information is changed.  The command allows the user to
resize a partition.  The partition may be shrunk and if there  is
sufficient  free  space  immediately  after it then it may be ex‐
panded.  The option allows the new size to be  specified,  other‐
wise  the  partition  will  be increased to the maximum available
size.  If there is no suffix, or the suffix is or then size is in
sectors, otherwise size is in bytes which must be a  multiple  of
the  device’s  sector  size.  Accepted suffix units are to denote
bytes, to denote kilobytes, to denote megabytes and to denote gi‐
gabytes.  The minimum size is 1 sector.  If the option is  speci‐
fied then the size will be adjusted to be a multiple of alignment
if  possible.   If  the option is specified then the utility will
not print output when a resize is not required.  The command  al‐
lows  the  user  to  resize  a disk.  With GPTs, a backup copy is
stored at the end of the disk.  If the underlying medium  changes
size then the backup copy needs to be moved to the new end of the
disk,  and the last sector available for data storage needs to be
adjusted.  This command does that.  If the backup copy no  longer
exists  due  to the medium shrinking, then a new backup copy will
be created using the primary copy.  The  option  allows  the  new
size  to  be  specified, otherwise the backup copy will automati‐
cally be placed at the current end of the disk.  If there  is  no
suffix,  or  the  suffix is or then size is in sectors, otherwise
size is in bytes which must be a multiple of the device’s  sector
size.  Accepted suffix units are to denote bytes, to denote kilo‐
bytes,  to  denote  megabytes and to denote gigabytes.  Using the
option allows you to move the backup copy prior to  resizing  the
medium.   This is primarily useful when shrinking the medium.  If
the option is specified then the utility will  not  print  output
when  a resize is not required.  The command restores a partition
table that was previously saved using the command.  The partition
table is read from standard input or a file specified in the  ar‐
gument  and  is  expected to be in the format of a plist.  It as‐
sumes an empty disk.  The option can be used to blank  the  disk.
The new disk does not have to be the same size as the old disk as
long  as  all  the  partitions fit, as will automatically adjust.
However, the new disk must use the same sector size  as  the  old
disk.   The  command sets various partition attributes.  The flag
lists all available attributes.  The option specifies  which  at‐
tributes  to  set and may be specified more than once, or the at‐
tributes can be comma‐separated.  If the option and no option are
specified, all attributes are removed.  The or the option specify
which entry to update.  The possible attributes are The  biosboot
flag  is  used  to  indicate  which partition should be booted by
legacy BIOS boot code.  See the  command  for  more  information.
The  bootme  flag  is  used to indicate which partition should be
booted by UEFI boot code.  The other attributes are for  compati‐
bility  with and are not currently used by They may be used by in
the future.  The command displays the current partitioning on the
listed devices and gives an overall view of  the  disk  contents.
With  the option the GPT partition GUID will be displayed instead
of the GPT partition type.  With the option the GPT partition la‐
bel will be displayed instead of the GPT  partition  type.   With
the option the GPT partition type is displayed as an UUID instead
of  in a user friendly form.  With the or the option, all the de‐
tails of a particular GPT partition will be displayed.  The  for‐
mat  of  this display is subject to change.  With the option, all
information for all  GPT  partitions  (just  like  with  will  be
printed.   None  of the options have any effect on non‐GPT parti‐
tions.  The order of precedence for the options are: The  command
allows the user to change the type of any and all partitions that
match  the  selection.  It uses the same selection options as the
command.  See above for a description of these options.  The flag
lists available types.  The command unsets various partition  at‐
tributes.   The  flag lists all available attributes.  The option
specifies which attributes to unset and  may  be  specified  more
than  once.   Alternatively  a comma separated list of attributes
can be used.  The or the option specifies which entry to  update.
The possible attributes are The biosboot flag is used to indicate
which  partition  should be booted by legacy BIOS boot code.  See
the command for more information.  The other attributes  are  for
compatibility  with and are not currently used by any code.  They
may be used by code in the future.  The command allows  the  user
to  change  the UUID of any and all partitions that match the se‐
lection.  It uses the same selection options as the command.  See
above for a description of these options.  If is used,  then  the
header UUID is changed as well.  The primary purpose of this com‐
mand  is  for use after cloning a disk to prevent collisions when
both disks are used in the same system.  The command exits with a
failure status (1) when the header command is  used  and  no  GPT
header  is found.  This can be used to check for the existence of
a GPT in shell scripts.  nas# gpt show wd3
       start        size  index  contents
           0           1         PMBR
           1  3907029167 nas# gpt create wd3 nas# gpt show wd3
       start        size  index  contents
           0           1         PMBR
           1           1         Pri GPT header
           2          32         Pri GPT table
          34  3907029101
  3907029135          32         Sec GPT table
  3907029167           1         Sec GPT header nas# gpt  add  ‐s
10486224  ‐t swap ‐i 1 wd3 nas# gpt label ‐i 1 ‐l swap_1 wd3 par‐
tition 1 on rwd3d labeled swap_1 nas# gpt show wd3
       start        size  index  contents
           0           1         PMBR
           1           1         Pri GPT header
           2          32         Pri GPT table
          34    10486224      1  GPT part ‐ NetBSD swap
    10486258  3896542877
  3907029135          32         Sec GPT table
  3907029167           1         Sec GPT header nas# gpt show  ‐l
wd3
       start        size  index  contents
           0           1         PMBR
           1           1         Pri GPT header
           2          32         Pri GPT table
          34    10486224      1  GPT part ‐ "swap_1"
    10486258  3896542877
  3907029135          32         Sec GPT table
  3907029167           1         Sec GPT header nas# Booting from
GPT on a BIOS system: this creates a bootable partition.  xotica#
gpt  create  wd1 xotica# gpt add ‐b 1024 ‐l bootroot ‐t ffs ‐s 1g
wd1   /dev/rwd1:   Partition   1    added:    49f48d5a‐b10e‐11dc‐
b99b‐0019d1879648 1024 2097152 xotica ˜# dmesg | tail ‐2 wd1: GPT
GUID:  660e0630‐0a3f‐47c0‐bc52‐c88bcec79392  dk0  at  wd1: "boot‐
root", 2097152 blocks at 1024, type: ffs xotica# gpt biosboot  ‐L
bootroot  wd1  xotica#  newfs  dk0  xotica# installboot /dev/rdk0
/usr/mdec/bootxx_ffsv1 xotica# mount  /dev/dk0  /mnt  xotica#  cp
/usr/mdec/boot  /mnt  Note  that  is not needed for UEFI systems.
The utility appeared in for ia64.  utility first appeared in  The
development  of the utility is still work in progress.  Many nec‐
essary features are missing or partially implemented.   In  prac‐
tice  this means that the manual page, supposed to describe these
features, is farther removed from being complete or  useful.   As
such,  missing  functionality  is not even documented as missing.
However, it is believed that the currently present  functionality
is  reliable and stable enough that this tool can be used without
bullet‐proof footware if one thinks one does not  make  mistakes.
It is expected that the basic usage model does not change, but it
is  possible  that  future versions will not be compatible in the
strictest sense of the word.  Also,  options  primarily  intended
for  diagnostic  or  debug purposes may be removed in future ver‐
sions.  Another possibility is that the current  usage  model  is
accompanied  by  other  interfaces  to  make the tool usable as a
back‐end.  This all depends on demand and thus feedback.
