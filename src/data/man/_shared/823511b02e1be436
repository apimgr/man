reads  or  modifies a disk label on which is used by the on hard‐
ware to find partitions to  boot  from.   only  reads/writes  the
first 512 bytes of The supported options are: Ignore an incorrect
magic  number  in  the disk label.  Synthesize a new label rather
than reading what is there.  Quiet mode ‐ don’t print unnecessary
babble (currently this suppresses the prompt).   Ignore  checksum
errors  when  reading  the  label.  Note that is dangerous, espe‐
cially when combined with since  it  will  then  happily  believe
whatever  garbage  it  may  find  in the label.  When using these
flags, all values should be checked carefully, both those printed
by and the partition table printed by prints a prompt and expects
commands.  The following commands are understood:  Show  a  short
help message.  Change partition (see below).  Print label, except
for  the  partition table.  Print the partition table.  Quit pro‐
gram (error if no write since last change).  Quit program (uncon‐
ditionally) [EOF also quits].  Set label in the kernel  (orthogo‐
nal to Change a non‐partition label value.  Write (possibly modi‐
fied) label out.  The through commands will accept, for the para‐
meter,  the syntax used by (For those not familiar with this syn‐
tax, means cylinders + tracks + sectors.   For  example,  if  the
disk  has 16 tracks of 32 sectors, means (3*16*32)+(4*32)+5=1669.
This calculation always uses the and values  as  printed  by  the
command;  in  particular,  if they are zero (which they will ini‐
tially be if is used), this syntax is not very useful.  Some  ad‐
ditional  strings  are  accepted.  For the parameter, (where is a
partition letter) indicates that the partition should start  with
the first free cylinder after partition indicates that the parti‐
tion  should start at the same place as partition For the parame‐
ter, indicates that the partition should end at the same place as
partition (even if partition ends partway  through  a  cylinder);
indicates  that  the  partition should end with the last cylinder
before partition and means that the partition’s size  should  ex‐
actly  match  partition  size.  Note that supports 16 partitions.
supports only 8.  Labels written by when partitions  through  are
all  set  are  identical to labels.  If any of the partitions are
nontrivial, information about them is tucked into some  otherwise
unused  space  in  the  label format.  The command changes fields
printed by the command.   For  example,  if  the  command  prints
ascii: ST15230N cyl 5657 alt 2 hd 19 sec 78 rpm: 0          pcyl:
0          apc:  0           obs1:  0  obs2:  0         intrlv: 1
ncyl: 5657      acyl: 0 nhead: 19       nsect: 78        obs3:  0
obs4: 0 then would set the value to 6204, or would set the ascii‐
label  string  to  that  string.   performs  very few consistency
checks on the values you supply, and the  ones  it  does  perform
never  generate  errors, only warnings.  It may be that the space
in the label where the information for the extended partitions is
saved is used by Not very many consistency checks are done on the
arguments, and those only produce warnings.  doesn’t  support  16
partitions in a disk label yet.
