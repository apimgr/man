is  used  to  convert  a  map  to a map. This is a inverse map of
user@host (or user!host) back to alias.  uses as the  input  map,
and  if  is given, use that as the output map.  If the output map
isn’t given don’t create database.  This can be useful when or is
given.  The options are as follows: Assume domain names  are  OK.
Only useful together with or Check host to verify that it exists.
Same  as  but  also  check  for  any MX‐record.  Capitalize name.
E.g., mats.o.jansson becomes Mats.O.Jansson.  Assume  UUCP  names
are  OK.  Only useful together with or Ignored (only provided for
compatibility with SunOS 4.1.x).  Verbose mode.
