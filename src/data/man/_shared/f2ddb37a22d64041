LOCKSPOOL(1)              BSD General Commands Manual             LOCKSPOOL(1)

NNAAMMEE
     lloocckkssppooooll — lock user's system mailbox

SSYYNNOOPPSSIISS
     lloocckkssppooooll [_u_s_e_r_n_a_m_e]

DDEESSCCRRIIPPTTIIOONN
     lloocckkssppooooll is useful for a client mail program to attain proper locking.
     lloocckkssppooooll obtains a _u_s_e_r_n_a_m_e_._l_o_c_k for the calling user and retains it un‐
     til stdin is closed or a signal like SIGINT, SIGTERM, or SIGHUP is re‐
     ceived.  Additionally, the superuser may specify the name of a user in
     order to lock a different mailbox.

     If lloocckkssppooooll is able to create the lock file, “1” is written to stdout,
     otherwise “0” is written and an error message is written to stderr.
     lloocckkssppooooll will try up to 10 times to get the lock (sleeping for a short
     period in between tries).

     Typical usage is for a user mail agent (such as mail(1)) to open a pipe
     to lloocckkssppooooll when it needs to lock the user's mail spool.  Closing the
     pipe will cause lloocckkssppooooll to release the lock.

FFIILLEESS
     /var/mail/username.lock  user's mail lock file

EEXXIITT SSTTAATTUUSS
     The lloocckkssppooooll utility exits 0 on success, and 1 if an error occurs.

SSEEEE AALLSSOO
     mail(1), mail.local(8), smtpd(8)

HHIISSTTOORRYY
     The lloocckkssppooooll program appeared in OpenBSD 2.4.

BSD                            January 25, 2019                            BSD
