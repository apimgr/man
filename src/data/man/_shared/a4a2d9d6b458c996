The  utility  reads space, tab, newline and end‐of‐file delimited
strings from the standard input and executes with the strings  as
arguments.  Any arguments specified on the command line are given
to upon each invocation, followed by some number of the arguments
read  from  the standard input of This is repeated until standard
input is exhausted.  Spaces, tabs and newlines may be embedded in
arguments using single (‘‘ ’ ’’)  or  double  (‘‘"’’)  quotes  or
backslashes  (‘‘\’’).   Single quotes escape all non‐single quote
characters, excluding newlines, up to the matching single  quote.
Double  quotes  escape all non‐double quote characters, excluding
newlines, up to the matching double quote.  Any single character,
including newlines, may be escaped by a backslash.   The  options
are as follows: Change to expect NUL (‘‘\0’’) characters as sepa‐
rators,  instead  of spaces and newlines.  This is expected to be
used in concert with the function in Use as a logical EOF marker.
Execute for each input line, replacing one or more occurrences of
in up to (or 5 if no flag is specified) arguments to with the en‐
tire line of input.  The resulting arguments,  after  replacement
is done, will not be allowed to grow beyond (or 255 if no flag is
specified) bytes; this is implemented by concatenating as much of
the argument containing as possible, to the constructed arguments
to  up  to  bytes.  The size limit does not apply to arguments to
which do not contain and furthermore, no replacement will be done
on itself.  Implies If this option is  specified,  will  use  the
data  read from standard input to replace the first occurrence of
instead of appending that data after all other  arguments.   This
option will not affect how many arguments will be read from input
or the size of the command(s) will generate The option just moves
where  those  arguments will be placed in the command(s) that are
executed.  The must show up as a distinct to It will not be  rec‐
ognized if, for instance, it is in the middle of a quoted string.
Furthermore,  only  the first occurrence of the will be replaced.
For example, the following command will copy the  list  of  files
and  directories which start with an uppercase letter in the cur‐
rent directory to Call for every lines read.  If EOF  is  reached
and  fewer lines have been read than then will be called with the
available lines.  Set the maximum number of arguments taken  from
standard  input  for each invocation of An invocation of will use
less than standard input arguments if the number of bytes accumu‐
lated (see the option) exceeds the specified or there  are  fewer
than  arguments  remaining for the last invocation of The current
default value for is 5000.  Reopen stdin as in the child  process
before  executing the command.  This is useful if you want to run
an interactive application.  Parallel mode: run at  most  invoca‐
tions  of  at once.  Echo each command to be executed and ask the
user whether it should be executed.  An affirmative response,  in
the  POSIX  locale,  causes the command to be executed, any other
response causes it to be skipped.  No commands  are  executed  if
the  process  is  not attached to a terminal.  Compatibility with
GNU The GNU version of runs the argument at least once,  even  if
input  is  empty, and it supports a option to inhibit this behav‐
ior.  The version of does not run the argument  on  empty  input,
but  it  supports  the option for command‐line compatibility with
GNU but the option does nothing in the  version  of  Specify  the
maximum  number  of arguments that will do replacement in.  If is
negative, the number of arguments in  which  to  replace  is  un‐
bounded.  Specify the amount of space (in bytes) that can use for
replacements.  The default for is 255.  Set the maximum number of
bytes  for  the  command  line  length provided to The sum of the
length of the utility name, the arguments  passed  to  (including
terminators)  and  the  current  environment will be less than or
equal to this number.  The current default value for is  ‐  4096.
Echo the command to be executed to standard error immediately be‐
fore it is executed.  Force to terminate immediately if a command
line  containing  arguments will not fit in the specified (or de‐
fault) command line length.  If is omitted, is  used.   Undefined
behavior may occur if reads from the standard input.  The utility
exits  immediately  (without  processing  any further input) if a
command line cannot be assembled, cannot be invoked,  an  invoca‐
tion of is terminated by a signal, or an invocation of exits with
a value of 255.  used to read responses in prompt mode exits with
one  of  the following values: All invocations of returned a zero
exit status.  One or more invocations of returned a nonzero  exit
status.   The  exited  with a 255 exit status.  The was killed or
stopped by a signal.  The was found but  could  not  be  invoked.
The  could not be found.  Some other error occurred.  The utility
is expected to be compliant.  The and  options  are  non‐standard
extensions which may not be available on other operating systems.
The  utility appeared in PWB UNIX 1.0.  It made its first BSD ap‐
pearance in the 4.3 Reno release.  The meaning of 123,  124,  and
125 exit values and the option were taken from GNU xargs.  If at‐
tempts  to  invoke  another command such that the number of argu‐
ments or the size of the environment is increased, it risks fail‐
ing with The utility does not take multibyte characters into  ac‐
count  when  performing  string  comparisons for the and options,
which may lead to incorrect results in some locales.
