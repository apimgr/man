_r_e_p_e_r_t_o_i_r_e_m_a_p(5)              File Formats Manual             _r_e_p_e_r_t_o_i_r_e_m_a_p(5)

NNAAMMEE
       repertoiremap - map symbolic character names to Unicode code points

DDEESSCCRRIIPPTTIIOONN
       A  repertoire  map  defines  mappings  between symbolic character names
       (mnemonics) and Unicode code points when compiling a  locale  with  lloo‐‐
       ccaalleeddeeff(1).  Using a repertoire map is optional, it is needed only when
       symbolic names are used instead of now preferred Unicode code points.

   SSyynnttaaxx
       The  repertoiremap  file  starts  with a header that may consist of the
       following keywords:

       _c_o_m_m_e_n_t___c_h_a_r
              is followed by a character that will  be  used  as  the  comment
              character  for  the rest of the file.  It defaults to the number
              sign (#).

       _e_s_c_a_p_e___c_h_a_r
              is followed by a character that should be  used  as  the  escape
              character  for  the  rest  of  the  file to mark characters that
              should be interpreted in a special  way.   It  defaults  to  the
              backslash (\).

       The  mapping  section starts with the keyword _C_H_A_R_I_D_S in the first col‐
       umn.

       The mapping lines have the following form:

       _<_s_y_m_b_o_l_i_c_‐_n_a_m_e_> _<_c_o_d_e_‐_p_o_i_n_t_> _c_o_m_m_e_n_t
              This defines exactly one mapping, _c_o_m_m_e_n_t being optional.

       The mapping section ends with the string _E_N_D _C_H_A_R_I_D_S.

FFIILLEESS
       _/_u_s_r_/_s_h_a_r_e_/_i_1_8_n_/_r_e_p_e_r_t_o_i_r_e_m_a_p_s
              Usual default repertoire map path.

SSTTAANNDDAARRDDSS
       POSIX.2.

NNOOTTEESS
       Repertoire maps are deprecated in favor of Unicode code points.

EEXXAAMMPPLLEESS
       A mnemonic for the Euro sign can be defined as follows:

       <Eu> <U20AC> EURO SIGN

SSEEEE AALLSSOO
       llooccaallee(1), llooccaalleeddeeff(1), cchhaarrmmaapp(5), llooccaallee(5)

Linux man‐pages (unreleased)      2025‐05‐17                  _r_e_p_e_r_t_o_i_r_e_m_a_p(5)
