GELF_NEWPHDR(3)          BSD Library Functions Manual          GELF_NEWPHDR(3)

NNAAMMEE
     eellff3322__nneewwpphhddrr, eellff6644__nneewwpphhddrr, ggeellff__nneewwpphhddrr — allocate an ELF program
     header table

LLIIBBRRAARRYY
     ELF Access Library (libelf, -lelf)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<lliibbeellff..hh>>

     _E_l_f_3_2___P_h_d_r _*
     eellff3322__nneewwpphhddrr(_E_l_f _*_e_l_f, _s_i_z_e___t _c_o_u_n_t);

     _E_l_f_6_4___P_h_d_r _*
     eellff6644__nneewwpphhddrr(_E_l_f _*_e_l_f, _s_i_z_e___t _c_o_u_n_t);

     ##iinncclluuddee <<ggeellff..hh>>

     _v_o_i_d _*
     ggeellff__nneewwpphhddrr(_E_l_f _*_e_l_f, _s_i_z_e___t _c_o_u_n_t);

DDEESSCCRRIIPPTTIIOONN
     These functions allocate an ELF Program Header table for an ELF descrip‐
     tor.  _E_l_f_3_2___P_h_d_r and _E_l_f_6_4___P_h_d_r descriptors are described further in
     elf(5).

     Functions eellff3322__nneewwpphhddrr() and eellff6644__nneewwpphhddrr() allocate a table of _c_o_u_n_t
     _E_l_f_3_2___P_h_d_r and _E_l_f_6_4___P_h_d_r descriptors respectively, discarding any exist‐
     ing program header table already present in the ELF descriptor _e_l_f.  A
     value of zero for argument _c_o_u_n_t may be used to delete an existing pro‐
     gram header table from an ELF descriptor.

     Function ggeellff__nneewwpphhddrr() will return a table of _E_l_f_3_2___P_h_d_r or _E_l_f_6_4___P_h_d_r
     with _c_o_u_n_t elements depending on the ELF class of ELF descriptor _e_l_f.

     The functions set the ELF_F_DIRTY flag on the program header table.  All
     members of the returned array of Phdr structures will be initialized to
     zero.

     After a successful call to these functions, the pointer returned by a
     prior call to eellff3322__ggeettpphhddrr() or eellff6644__ggeettpphhddrr() on the same descriptor
     _e_l_f will no longer be valid.

RREETTUURRNN VVAALLUUEESS
     The functions a valid pointer if successful, or NULL in case an error was
     encountered.

CCOOMMPPAATTIIBBIILLIITTYY
     The ggeellff__nneewwpphhddrr() function uses a type of _v_o_i_d _* for its returned value.
     This differs from some other implementations of the elf(3) API, which use
     an _u_n_s_i_g_n_e_d _l_o_n_g return type.

EERRRROORRSS
     These functions may fail with the following errors:

     [ELF_E_ARGUMENT]  Argument _e_l_f was NULL.

     [ELF_E_ARGUMENT]  Argument _e_l_f was not a descriptor for an ELF object.

     [ELF_E_CLASS]     ELF descriptor _e_l_f was of an unrecognized class.

     [ELF_E_RESOURCE]  An out of memory condition was detected.

     [ELF_E_SEQUENCE]  An executable header was not allocated for ELF descrip‐
                       tor _e_l_f before using these APIs.

SSEEEE AALLSSOO
     elf(3), elf32_getphdr(3), elf32_newehdr(3), elf64_getphdr(3),
     elf64_newehdr(3), elf_flagphdr(3), elf_getphnum(3), gelf(3),
     gelf_getphdr(3), gelf_newehdr(3), elf(5)

BSD                              June 12, 2019                             BSD
