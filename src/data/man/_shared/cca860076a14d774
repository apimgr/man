The  driver  supports at least the following adapters: The modems
appear at first as a device containing the and drivers and,  upon
receipt of a SCSI command, will detach from the USB bus and reat‐
tach  as  a  Wireless WAN modem.  Unless disabled by clearing the
variable the driver will handle that automatically.   The  modems
provide  a  number  of IO channels spread over several USB inter‐
faces which are mapped by function to a standard port  number  in
each  driver  instance.  The defined channels are: Apart from the
Network port, which is attached as a network interface, the ports
are attached as devices using the port number as the minor device
number.  In order to connect using the Modem tty should  be  used
The  Network  port  provides  a direct IPv4 interface, but before
this can be used the modem needs to be placed in  connected  mode
and network settings subsequently retrieved using the proprietary
and  AT  commands  on  the Control port.  Note that the Modem and
Network ports should not be enabled at the same time for USB per‐
formance reasons.  This driver originated as the module for writ‐
ten by It was rewritten for and to provide more  complete  device
support  with  information extracted from the driver for provided
by The rewrite and this manual page by
