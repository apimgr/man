The program copies the audio device to the named audiofile or, if
the file name is ‐, to the standard output.  The output file will
contain  either a Sun/NeXT audio header, a RIFF/WAVE audio header
or no header at all.  Sun output files using a linear PCM  encod‐
ing  are  written  with big‐endian signed samples, possibly after
converting  these  from  little‐endian   or   unsigned   samples.
RIFF/WAVE  files  are  written  in little‐endian, signed samples,
also converting if necessary.  The  default  output  is  Sun/NeXT
format, but if the output file ends with a file extension it will
be  written  as  RIFF/WAVE.  The following options are available:
Append to the specified file, rather than overwriting.   Set  the
audio  device  read  buffer  size  to  The  default  value is the
record.buffer_size of the audio device.  Set the balance to  This
value  must  be  between 0 and 63.  Set number of channels to Set
the audio device to be The default is Set encoding to  either  or
or  any  other  value  reported by The default encoding is If the
output format is the file will contain slinear_be samples, if  it
is  then  slinear_le,  independent of the argument to Setting the
argument to still may be important since it is used in an call to
the kernel to choose the kind of data provided.  Set  the  output
header format to Currently supported formats are and for Sun/NeXT
audio,  WAV,  and no header, respectively.  Force.  Normally when
appending to audiofiles using the option, the sample  rates  must
match.  The option will allow a discrepancy to be ignored.  Print
a  help  message.   If supported by the format, add the string to
the output header.  Set the monitor volume.  Set  the  precision.
This  value is the number of bits per sample, and is normally ei‐
ther or though the values and are also valid.  Set the input port
to The valid values of are and Be quiet.  Set the sampling  rate.
This  value  is  per‐second.  Typical values are 8000, 44100, and
48000, which are the telephone, CD Audio, and DAT  Audio  default
sampling rates.  Sets the maximum amount of time to record.  For‐
mat  is  [hh:]mm:ss[.dddddd].  Be verbose.  Set the volume (gain)
to This value must be between 0 and 255.  the audio  control  de‐
vice to be used.  the audio device to be used.  Record CD quality
audio  to a WAVE file: The program was first seen in SunOS 5.  It
was first made available in RIFF/WAVE support,  and  support  for
converting  signed/unsigned  and  big/little‐endian  samples  was
first made available in The program was written by WAV big‐endian
samples are converted to little‐endian, rather than a RIFX header
being written.
