attribute  on  a  These functions represent an Apple extension to
and and as such should not be used by  programs  intending  their
code  to  be  portable to other platforms.  The function sets the
universal binary preferences for the spawn attribute value refer‐
enced by from the memory containing the referenced by with a size
of elements; the actual number of elements that are  set  in  the
attribute  is  returned  in  When or is subsequently invoked on a
Universal binary with the the elements which  were  set  will  be
used,  in the order they were set, to select the first element in
the list which matches any of those available  in  the  Universal
binary,  using the best slice matching that If there is no match,
then the attempt to create the child process will fail  with  the
error  EBADARCH.   If  the  is the last element in the list, then
rather than returning EBADARCH on no match, the system  will  in‐
stead  fall back to the standard Universal binary grading prefer‐
ence order.  If called multiple times on the  same  the  previous
preferences will be overwritten.  The function gets the universal
binary  preferences  for  the spawn attribute value referenced by
(set by a prior call to or into the memory containing the  refer‐
enced  by  with a prereserved size of elements; the actual number
of elements that are copied from the attribute is returned in  On
success,  these functions return 0; on failure they return an er‐
ror number from and modify the value of Additionally, if success‐
ful, will modify the contents of the array with the  current  at‐
tribute values.  These functions may fail if: The value specified
by  is  invalid.   The  value of is invalid.  Nonstandard The and
function calls appeared in Mac OS X.
