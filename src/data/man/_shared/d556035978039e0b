NETSTART(8)               BSD System Manager's Manual              NETSTART(8)

NNAAMMEE
     nneettssttaarrtt — network startup script

SSYYNNOOPPSSIISS
     sshh _/_e_t_c_/_n_e_t_s_t_a_r_t [--nn] [_i_n_t_e_r_f_a_c_e _._._.]

DDEESSCCRRIIPPTTIIOONN
     nneettssttaarrtt is a sh(1) script invoked by rc(8) during an automatic reboot
     and after single-user mode is exited; it performs network initialization.

     nneettssttaarrtt can also be used to start newly created interfaces.  Its behav‐
     iour is controlled to some extent by variables defined in rc.conf(8).

     During the system boot, nneettssttaarrtt is executed.  nneettssttaarrtt performs the fol‐
     lowing operations, in the sequence given:

           ••   Configure the loopback interface.
           ••   Configure all physical interfaces.
           ••   Configure non-physical interfaces that can provide the default
               gateway, such as aggr(4), trunk(4), svlan(4), vlan(4), carp(4),
               and pppoe(4).
           ••   Initialize the routing table and set up the default routes.
           ••   Configure tunnel interfaces that require a working network to
               reach the other end of the tunnel, such as tun(4), tap(4),
               gif(4), etherip(4), gre(4), egre(4), pflow(4), and wg(4).

     After the system is completely initialized, it is possible to start newly
     created interfaces or apply configuration from hostname.if(5) files to an
     existing interfaces.

     The options are as follows:

     --nn      Reports the steps that would be taken, without actually configur‐
             ing anything.

SSEEEE AALLSSOO
     multicast(4), defaultdomain(5), hostname.if(5), mygate(5), ifconfig(8),
     rc(8), rc.conf(8)

HHIISSTTOORRYY
     The nneettssttaarrtt command first appeared in 4.0BSD.

BSD                            October 25, 2022                            BSD
