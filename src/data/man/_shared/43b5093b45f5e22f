RPC.UMNTALL(8)            BSD System Manager's Manual           RPC.UMNTALL(8)

NNAAMMEE
     rrppcc..uummnnttaallll — notify NFS servers about unmounted NFS file systems

SSYYNNOOPPSSIISS
     rrppcc..uummnnttaallll [--ee _e_x_p_i_r_e] [--hh _h_o_s_t] [--kk] [--pp _r_e_m_o_t_e_p_a_t_h] [--vv]

DDEESSCCRRIIPPTTIIOONN
     The rrppcc..uummnnttaallll utility is proposed in the NFS RPC specification; see _N_F_S
     _V_e_r_s_i_o_n _3 _P_r_o_t_o_c_o_l _S_p_e_c_i_f_i_c_a_t_i_o_n, RFC 1813, Appendix I.  It uses remote
     procedure calls to remove mount entries from _/_v_a_r_/_d_b_/_m_o_u_n_t_d_t_a_b on the re‐
     mote NFS server.  It is called automatically without any parameters dur‐
     ing startup and shutdown of the system.  This ensures that showmount(8)
     does not display old and expired entries.  The rrppcc..uummnnttaallll utility is
     only needed on client side, where mount_nfs(8) adds a mount entry with
     the current date to _/_v_a_r_/_d_b_/_m_o_u_n_t_t_a_b, and umount(8) removes the entry
     again.  The rrppcc..uummnnttaallll utility cares about all remaining entries in this
     table which result from crashes or unproper shutdowns.

     The options are as follows:

     --ee _e_x_p_i_r_e  All entries which are not actually mounted or older than
                _e_x_p_i_r_e (seconds) are removed from _/_v_a_r_/_d_b_/_m_o_u_n_t_t_a_b.  This may
                be the case for DNS changes or long out of service periods.
                Default expire time is 86400 seconds (one day).

     --hh _h_o_s_t    Only remove the specific hostname.  Send a UMNTALL RPC to the
                NFS server.

     --kk         Keep entries for existing NFS file systems.  Compare the NFS
                file systems from the mounttab against the kernel mountlist
                and do not send the RPC to existing mount entries.  Useful
                during startup of the system.  It may be possible that there
                are already mounted NFS file systems, so calling RPC UMOUNT is
                not a good idea.  This is the case if the user has rebooted to
                'single user mode' and starts up the system again.

     --pp _p_a_t_h    Only remove the specific mount-path.  Send a UMOUNT RPC to the
                NFS server.  This option implies the --hhoosstt option.

     --vv         Verbose, additional information is printed for each processed
                mounttab entry.

FFIILLEESS
     /var/db/mounttab  mounted nfs-file systems

SSEEEE AALLSSOO
     mount_nfs(8), mountd(8), umount(8)

HHIISSTTOORRYY
     The rrppcc..uummnnttaallll utility first appeared in FreeBSD 4.0.

AAUUTTHHOORRSS
     Martin Blapp <_m_b_@_i_m_p_._c_h>

BSD                            November 17, 1999                           BSD
