The utility is an implementation of the WPA Supplicant component,
i.e.,  the  part that runs in the client stations.  It implements
WPA key negotiation with a WPA Authenticator and EAP  authentica‐
tion  with  an  Authentication Server.  In addition, controls the
roaming and IEEE 802.11  authentication/association  support  and
can be used to configure static WEP keys based on identified net‐
works.   The utility is designed to be a program that runs in the
background and acts as  the  backend  component  controlling  the
wireless connection.  It supports separate frontend programs such
as the text‐based program.  The following arguments must be spec‐
ified  on the command line: Use the specified wireless interface.
Use the settings in the specified configuration file when  manag‐
ing the wireless interface.  See for a description of the config‐
uration  file  syntax and contents.  Changes to the configuration
file can be reloaded by sending a signal to the process  or  with
the  utility,  using  The following options are available: Detach
from the controlling terminal and run as a daemon process in  the
background.   Optional  bridge interface name.  Control interface
parameter.  Driver name (can be either bsd or wired)  Enable  de‐
bugging messages.  If this option is supplied twice, more verbose
messages  are displayed.  Messages are sent to stdout by default,
even when daemonised.  This can be changed with the flag.  Speci‐
fies a file to store entropy.  Specifies a  file  to  send  debug
messages to when enabled with the flag.  Global control interface
group.   Global  control  interface.  Show help text.  Additional
configuration file.  Include key information in debugging output.
Display the license for this program on the  terminal  and  exit.
Matches  the  interface  name  using  or all interfaces if is not
specified.  If multiple interfaces are matched then  will  manage
them  all with a single process.  Specify an additional interface
and configuration file.  If  multiple  interfaces  are  specified
then  will  manage  them  all  with  a  single process.  Override
ctrl_interface parameter for new interface.  Override driver  pa‐
rameter  for  new  interface.   File in which to save the process
PID.  Specify driver parameters.   Decrease  debugging  verbosity
(i.e.,  counteract  the  use  of  the  flag).   Send log messages
through instead of to the terminal.  Include timestamp  in  debug
messages.   Display version information on the terminal and exit.
Wait for a control interface monitor before starting.  The  util‐
ity first appeared in The utility was written by This manual page
is derived from the file included in the distribution.
