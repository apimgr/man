SSH-KEYSCAN(1)            BSD General Commands Manual           SSH-KEYSCAN(1)

NNAAMMEE
     sssshh--kkeeyyssccaann — gather SSH public keys from servers

SSYYNNOOPPSSIISS
     sssshh--kkeeyyssccaann [--4466ccDDHHqqvv] [--ff _f_i_l_e] [--OO _o_p_t_i_o_n] [--pp _p_o_r_t] [--TT _t_i_m_e_o_u_t]
                 [--tt _t_y_p_e] [_h_o_s_t | _a_d_d_r_l_i_s_t _n_a_m_e_l_i_s_t]

DDEESSCCRRIIPPTTIIOONN
     sssshh--kkeeyyssccaann is a utility for gathering the public SSH host keys of a num‐
     ber of hosts.  It was designed to aid in building and verifying
     _s_s_h___k_n_o_w_n___h_o_s_t_s files, the format of which is documented in sshd(8).
     sssshh--kkeeyyssccaann provides a minimal interface suitable for use by shell and
     perl scripts.

     sssshh--kkeeyyssccaann uses non-blocking socket I/O to contact as many hosts as pos‐
     sible in parallel, so it is very efficient.  The keys from a domain of
     1,000 hosts can be collected in tens of seconds, even when some of those
     hosts are down or do not run sshd(8).  For scanning, one does not need
     login access to the machines that are being scanned, nor does the scan‐
     ning process involve any encryption.

     Hosts to be scanned may be specified by hostname, address or by CIDR net‐
     work range (e.g. 192.168.16/28).  If a network range is specified, then
     all addresses in that range will be scanned.

     The options are as follows:

     --44      Force sssshh--kkeeyyssccaann to use IPv4 addresses only.

     --66      Force sssshh--kkeeyyssccaann to use IPv6 addresses only.

     --cc      Request certificates from target hosts instead of plain keys.

     --DD      Print keys found as SSHFP DNS records.  The default is to print
             keys in a format usable as an ssh(1) _k_n_o_w_n___h_o_s_t_s file.

     --ff _f_i_l_e
             Read hosts or “addrlist namelist” pairs from _f_i_l_e, one per line.
             If ‘-’ is supplied instead of a filename, sssshh--kkeeyyssccaann will read
             from the standard input.  Names read from a file must start with
             an address, hostname or CIDR network range to be scanned.  Ad‐
             dresses and hostnames may optionally be followed by comma-sepa‐
             rated name or address aliases that will be copied to the output.
             For example:

             192.168.11.0/24
             10.20.1.1
             happy.example.org
             10.0.0.1,sad.example.org

     --HH      Hash all hostnames and addresses in the output.  Hashed names may
             be used normally by ssh(1) and sshd(8), but they do not reveal
             identifying information should the file's contents be disclosed.

     --OO _o_p_t_i_o_n
             Specify a key/value option.  At present, only a single option is
             supported:

             hhaasshhaallgg=_a_l_g_o_r_i_t_h_m
                     Selects a hash algorithm to use when printing SSHFP
                     records using the --DD flag.  Valid algorithms are “sha1”
                     and “sha256”.  The default is to print both.

     --pp _p_o_r_t
             Connect to _p_o_r_t on the remote host.

     --qq      Quiet mode: do not print server host name and banners in com‐
             ments.

     --TT _t_i_m_e_o_u_t
             Set the timeout for connection attempts.  If _t_i_m_e_o_u_t seconds have
             elapsed since a connection was initiated to a host or since the
             last time anything was read from that host, the connection is
             closed and the host in question considered unavailable.  The de‐
             fault is 5 seconds.

     --tt _t_y_p_e
             Specify the type of the key to fetch from the scanned hosts.  The
             possible values are “ecdsa”, “ed25519”, “ecdsa-sk”, “ed25519-sk”,
             or “rsa”.  Multiple values may be specified by separating them
             with commas.  The default is to fetch all the above key types.

     --vv      Verbose mode: print debugging messages about progress.

     If an ssh_known_hosts file is constructed using sssshh--kkeeyyssccaann without veri‐
     fying the keys, users will be vulnerable to _m_a_n _i_n _t_h_e _m_i_d_d_l_e attacks.
     On the other hand, if the security model allows such a risk, sssshh--kkeeyyssccaann
     can help in the detection of tampered keyfiles or man in the middle at‐
     tacks which have begun after the ssh_known_hosts file was created.

FFIILLEESS
     _/_e_t_c_/_s_s_h_/_s_s_h___k_n_o_w_n___h_o_s_t_s

EEXXAAMMPPLLEESS
     Print the RSA host key for machine _h_o_s_t_n_a_m_e:

           $ ssh-keyscan -t rsa hostname

     Search a network range, printing all supported key types:

           $ ssh-keyscan 192.168.0.64/25

     Find all hosts from the file _s_s_h___h_o_s_t_s which have new or different keys
     from those in the sorted file _s_s_h___k_n_o_w_n___h_o_s_t_s:

           $ ssh-keyscan -t rsa,ecdsa,ed25519 -f ssh_hosts | \
                   sort -u - ssh_known_hosts | diff ssh_known_hosts -

SSEEEE AALLSSOO
     ssh(1), sshd(8)

     _U_s_i_n_g _D_N_S _t_o _S_e_c_u_r_e_l_y _P_u_b_l_i_s_h _S_e_c_u_r_e _S_h_e_l_l _(_S_S_H_) _K_e_y _F_i_n_g_e_r_p_r_i_n_t_s, RFC
     4255, 2006.

AAUUTTHHOORRSS
     David Mazieres <_d_m_@_l_c_s_._m_i_t_._e_d_u> wrote the initial version, and Wayne
     Davison <_w_a_y_n_e_d_@_u_s_e_r_s_._s_o_u_r_c_e_f_o_r_g_e_._n_e_t> added support for protocol version
     2.

BSD                             October 4, 2025                            BSD
