Based on the values of and returns the identifier of a newly cre‐
ated or previously existing set of semaphores.  The key is analo‐
gous  to  a  filename: it provides a handle that names an IPC ob‐
ject.  There are three ways to specify a key: IPC_PRIVATE may  be
specified,  in  which  case a new IPC object will be created.  An
integer constant may be specified.  If no IPC object  correspond‐
ing  to  is  specified  and the IPC_CREAT bit is set in a new one
will be created.  The function may be used to generate a key from
a pathname.  The mode of a newly created IPC object is determined
by the following constants into the  argument:  Read  access  for
user.   Alter access for user.  Read access for group.  Alter ac‐
cess for group.  Read access for other.  Alter access for  other.
If  a new set of semaphores is being created, is used to indicate
the number of semaphores the set should contain.  Otherwise,  may
be specified as 0.  The system call returns the id of a semaphore
set  if successful; otherwise, ‐1 is returned and is set to indi‐
cate the error.  The system call will fail if: Access  permission
failure.   IPC_CREAT and IPC_EXCL were specified, and a semaphore
set corresponding to already exists.  The  number  of  semaphores
requested  is  either  less than 1 or greater than the system im‐
posed maximum per set.  A semaphore identifier exists for the ar‐
gument key, but the number of semaphores in  the  set  associated
with it is less than and is non‐zero.  No semaphore set was found
corresponding to and IPC_CREAT was not specified.  Insufficiently
many  semaphores  are available.  The kernel could not allocate a
The include files and are necessary.
