The  file configures all the features provided by the daemon.  It
is composed by a series of each of which defines a group  of  The
file format is free‐form: new line markers are ignored as well as
indentation.   Comments  start with the sign and extend until the
end of line.  A is like a variable assignment.  It  has  a  name,
which goes to the left of the equal sign, and a value, which goes
to  the  right.   The assignment ends with a semicolon.  It looks
like: There is no difference between  string  or  integer  values
when  defining  them.   The  value  must  be surrounded by double
quotes if it contains whitespace.  Booleans are specified as  in‐
tegers,  where means false and stands for true.  Even though, the
program cares about this and will emit a warning if you have done
an incorrect assignment.  Note that it will not  accept  unrecog‐
nized  names.   A is a type of block that defines how the program
behaves when run in a specific mode.  A mode inherits  properties
defined  in  the global scope.  It has the following syntax: mode
mode_name {
        property1 = value1;
        ...
        propertyN = valueN; } There are two recognized modes, and
describes what they do in detail.  The following  properties  can
be  defined in the global scope, thus affecting all modes, or in‐
side the mode definition, to override global values.  The  device
name to use.  Defaults to Specify an optional fifo where to redi‐
rect all mouse events, no matter if they have been processed.  By
default,  no fifo is used.  Whitespace separated list of modes to
be activated when running.  Defaults to Set to 1 to not  fork  in
the  background.  The basename of the pidfile used to control the
process.  Pidfiles are always created under and have  the  exten‐
sion  automatically added.  By default it is set to daemon’s pro‐
gram name.  notification device.  Defaults to You will  not  want
to  change this.  Virtual console number which holds the X server
(if any).  The argument specifies the console  number  (the  same
found  in Unset by default.  Number of seconds to wait before re‐
activating the mouse when returning from the X console (specified
by the property).  Defaults to 5.  The following  properties  are
only useful when running in the mode: Assigns a command to a but‐
ton,  which  will be executed using the call.  The part selects a
button to which the command is assigned; the first button is num‐
bered and the maximum depends on the mouse type.  The part can be
either or representing the events  emitted  when  the  button  is
pressed and released, respectively.  The following properties are
only useful when running in the mode: Set to 1 to swap mouse but‐
tons,  specially  useful for left handed users.  X axis slowdown.
This positive integer specifies how many events in  the  vertical
direction  should  be ignored before changing the current column.
It defaults to 0.  Y axis slowdown.  This positive integer speci‐
fies how many events in the horizontal direction  should  be  ig‐
nored  before  changing  the current row.  It defaults to 3.  De‐
fault configuration file.  Location of sample files.  The config‐
uration file first appeared in
