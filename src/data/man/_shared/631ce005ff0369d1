starts  a terminal session on a remote host first attempts to use
the standard Berkeley authorization mechanism.  The  options  are
as  follows:  Use  IPv4 addresses only.  Use IPv6 addresses only.
The option allows an eight‐bit input data path at all times; oth‐
erwise parity bits are stripped except  when  the  remote  side’s
stop  and  start  characters  are  other than The option turns on
socket debugging (see on the TCP sockets used  for  communication
with  the remote host.  The option stops any character from being
recognized as an escape character.  When used  with  the  option,
this  provides  a  completely transparent connection.  The option
allows user specification of the escape character,  which  is  by
default.  This specification may be as a literal character, or as
an  octal value in the form \nnn.  the option specifies an alter‐
nate for the remote login.  If this option is not specified, your
local username will be used.  Set the socket  option,  which  can
improve  interactive  responsiveness  at the possible downside of
increased network load.  Uses the given instead of  the  one  as‐
signed  to the service May be given either as symbolic name or as
number.  A line of the form disconnects  from  the  remote  host.
Similarly,  the  line  will suspend the session, and suspends the
send portion of the rlogin, but allows  output  from  the  remote
system.  By default, the tilde character is the escape character,
and  normally  control‐Y  is  the delayed‐suspend character.  All
echoing takes place at the remote site, so that (except  for  de‐
lays) the is transparent.  Flow control via ˆS/ˆQ and flushing of
input and output on interrupts are handled properly.  The follow‐
ing  environment variable is used by Determines the user’s termi‐
nal type.  The command appeared in More of the environment should
be propagated.
