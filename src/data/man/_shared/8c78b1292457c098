The  driver  handles all ADB hotkey devices within the framework.
So far it only translates button events back to their correspond‐
ing function key codes.  Actually send hotkey events at least for
the classes we can handle like display brightness.
