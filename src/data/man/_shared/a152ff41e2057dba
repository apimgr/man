DWARF_GET_STR(3)         BSD Library Functions Manual         DWARF_GET_STR(3)

NNAAMMEE
     ddwwaarrff__ggeett__ssttrr — retrieve a string from the DWARF string section

LLIIBBRRAARRYY
     DWARF Access Library (libdwarf, -ldwarf)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<lliibbddwwaarrff..hh>>

     _i_n_t
     ddwwaarrff__ggeett__ssttrr(_D_w_a_r_f___D_e_b_u_g _d_b_g, _D_w_a_r_f___O_f_f _o_f_f_s_e_t, _c_h_a_r _*_*_s_t_r_i_n_g,
         _D_w_a_r_f___S_i_g_n_e_d _*_l_e_n, _D_w_a_r_f___E_r_r_o_r _*_e_r_r);

DDEESSCCRRIIPPTTIIOONN
     Function ddwwaarrff__ggeett__ssttrr() retrieves a NUL-terminated string from the DWARF
     string section “.debug_str”.

     Argument _d_b_g should reference a DWARF debug context allocated using
     dwarf_init(3).

     Argument _o_f_f_s_e_t should be an offset, relative to the “.debug_str” sec‐
     tion, specifying the start of the desired string.

     Argument _s_t_r_i_n_g should point to a location which will hold a returned
     pointer to a NUL-terminated string.

     Argument _l_e_n should point to a location which will hold the length of the
     returned string.  The returned length does not include the space needed
     for the NUL-terminator.

     If argument _e_r_r is not NULL, it will be used to store error information
     in case of an error.

RREETTUURRNN VVAALLUUEESS
     Function ddwwaarrff__ggeett__ssttrr() returns DW_DLV_OK when it succeeds.  It returns
     DW_DLV_NO_ENTRY if there is no “.debug_str” section associated with the
     specified debugging context, or if the provided offset _o_f_f_s_e_t is at the
     very end of “.debug_str” section.  In case of an error, it returns
     DW_DLV_ERROR and sets the argument _e_r_r.

EEXXAAMMPPLLEESS
     To retrieve all the strings in the DWARF string section, use:

           Dwarf_Debug dbg;
           Dwarf_Off offset;
           Dwarf_Signed len;
           Dwarf_Error de;
           char *str;
           int ret

           offset = 0;
           while ((ret = dwarf_get_str(dbg, offset, &str, &len, &de)) ==
                   DW_DLV_OK) {
                   /* .. Use the retrieved string. .. */
                   offset += len + 1; /* Account for the terminating NUL. */
           }

           if (ret == DW_DLV_ERROR)
                   warnx("dwarf_get_str: %s", dwarf_errmsg(de));

EERRRROORRSS
     Function ddwwaarrff__ggeett__ssttrr() can fail with:

     [DW_DLE_ARGUMENT]  One of the arguments _d_b_g, _s_t_r_i_n_g or _l_e_n was NULL.

     [DW_DLE_ARGUMENT]  Argument _o_f_f_s_e_t was out of range.

     [DW_DLE_NO_ENTRY]  The debugging context _d_b_g did not contain a
                        “.debug_str” string section.

     [DW_DLE_NO_ENTRY]  Argument _o_f_f_s_e_t was at the very end of the
                        “.debug_str” section.

SSEEEE AALLSSOO
     dwarf(3), dwarf_init(3)

BSD                              April 3, 2011                             BSD
