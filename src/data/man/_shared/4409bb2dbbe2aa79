is  a  library  interface  to the web server.  The library can be
used to embed a webserver in your applications.  Normal operation
sees the process be initialised using the  function,  which  will
set  up  the default port and other internal settings, allocating
any necessary space as needed.  The function returns 1 on sucess,
0 on failure.  The function is used to specify the  virtual  host
name  for  the  web server.  A NULL host name will mean that will
use the local value for the host name, as returned by  This  vir‐
tual hostname should be a fully qualified domain name.  The final
argument to is the name of the directory to serve as the root di‐
rectory of the web server tree.  Once the server has been set up,
it serves requests by using the function, which returns a pointer
to a request structure, and which deals with the request, and an‐
swers  the  client.   The request space is de‐allocated using the
function.  Preferences are set using the  function  function  and
queried  using  the two function.  This is the main interface for
selecting options, and for setting preferences.  The memory allo‐
cated by for both the httpd structure and the preferences will be
freed.  The library first appeared in wrote this  high‐level  in‐
terface.  This manual page was written by
