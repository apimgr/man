makes  or  verifies a FIDO2 credential.  A credential may be (de‐
noting ECDSA over NIST P‐256 with  SHA‐256),  (denoting  2048‐bit
RSA  with  PKCS#1.5 padding and SHA‐256), or (denoting EDDSA over
Curve25519 with SHA‐512).  If is not specified, is assumed.  When
making a credential, the authenticator may require  the  user  to
authenticate  with  a  PIN.  If the option is not specified, will
prompt the user for the PIN.  If a is available, will use  it  to
obtain  the PIN.  Otherwise, is used.  The input of is defined by
the parameters of the credential to be  made/verified.   See  the
section  for  details.  The output of is defined by the result of
the selected operation.  See the section for details.  If a  cre‐
dential is successfully created or verified, exits 0.  Otherwise,
exits 1.  The options are as follows: Tells to make a new creden‐
tial on Tells to verify a credential.  Request the credential’s a
32‐byte  symmetric  key associated with the generated credential.
If making a credential, set the credential’s protection level  to
where  is  the credential’s protection level in decimal notation.
Please refer to for the set of possible values.  If  verifying  a
credential,  check  whether the credential’s protection level was
signed by the authenticator as Causes to emit debugging output on
If making a credential, enable the FIDO2  hmac‐secret  extension.
If  verifying  a credential, check whether the extension data bit
was signed by the authenticator.  Tells to read the parameters of
the credential from instead of Tells to write output  on  instead
of  Tells  to  be  quiet.  If a PIN is required and is specified,
will fail.  Create a resident credential.   Resident  credentials
are  called  in  CTAP 2.1.  Create a U2F credential.  By default,
will use FIDO2 if supported by the authenticator, and fallback to
U2F otherwise.  If making a credential,  request  user  verifica‐
tion.   If verifying a credential, check whether the user verifi‐
cation bit was signed by the authenticator.  The  input  of  con‐
sists  of  base64  blobs  and  UTF‐8 strings separated by newline
characters (’\n’).  When making a credential, expects  its  input
to  consist  of: client data hash (base64 blob); relying party id
(UTF‐8 string); user name (UTF‐8 string); user id (base64  blob).
When  verifying  a  credential,  expects its input to consist of:
client data hash (base64 blob); relying party id (UTF‐8  string);
credential  format  (UTF‐8  string);  authenticator  data (base64
blob); credential id (base64 blob); attestation signature (base64
blob); attestation certificate (optional,  base64  blob).   UTF‐8
strings  passed to must not contain embedded newline or NUL char‐
acters.  The output of consists of base64 blobs,  UTF‐8  strings,
and  PEM‐encoded  public  keys  separated  by  newline characters
(’\n’).  Upon the successful generation of a credential, outputs:
client data hash (base64 blob); relying party id (UTF‐8  string);
credential  format  (UTF‐8  string);  authenticator  data (base64
blob); credential id (base64 blob); attestation signature (base64
blob); attestation certificate, if present  (base64  blob).   the
credential’s  associated 32‐byte symmetric key if present (base64
blob).  Upon the successful verification of  a  credential,  out‐
puts:  credential  id  (base64 blob); PEM‐encoded credential key.
Create a new credential on verify it, and save  the  id  and  the
public  key  of  the credential in Please note that handles Basic
Attestation and Self Attestation transparently.  In the  case  of
Basic  Attestation,  the validity of the authenticator’s attesta‐
tion certificate is verified.
