PROPERTIES(3)            BSD Library Functions Manual            PROPERTIES(3)

NNAAMMEE
     pprrooppeerrttiieess__rreeaadd, pprrooppeerrttyy__ffiinndd, pprrooppeerrttiieess__ffrreeee — functions to allow cre‐
     ating simple property lists from ASCII file data

LLIIBBRRAARRYY
     System Utilities Library (libutil, -lutil)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<lliibbuuttiill..hh>>

     _p_r_o_p_e_r_t_i_e_s
     pprrooppeerrttiieess__rreeaadd(_i_n_t _f_d);

     _c_h_a_r _*
     pprrooppeerrttyy__ffiinndd(_p_r_o_p_e_r_t_i_e_s _l_i_s_t, _c_o_n_s_t _c_h_a_r _*_n_a_m_e);

     _v_o_i_d
     pprrooppeerrttiieess__ffrreeee(_p_r_o_p_e_r_t_i_e_s _l_i_s_t);

DDEESSCCRRIIPPTTIIOONN
     typedef struct _properties {
             struct _properties *next;
             char *name;
             char *value;
     } *properties;

     The function pprrooppeerrttiieess__rreeaadd() reads _n_a_m_e _= _v_a_l_u_e pairs from the file de‐
     scriptor passed in _f_d and returns the head of a new property list, assum‐
     ing that the file's contents have been parsed properly, or NULL in case
     of error.

     The pprrooppeerrttyy__ffiinndd() function returns the associated value string for the
     property named _n_a_m_e if found, otherwise NULL.  The value returned may be
     up to PROPERTY_MAX_VALUE bytes in length.

     The pprrooppeerrttiieess__ffrreeee() function is used to free the structure returned by
     pprrooppeerrttiieess__rreeaadd() when it is no longer needed.

FFIILLEE FFOORRMMAATT
     Each property in the file is assumed to have the format of _n_a_m_e _= _v_a_l_u_e
     where _n_a_m_e is an alphanumeric string (and any punctuation not including
     the `=' character) and _v_a_l_u_e is an arbitrary string of text terminated by
     a newline character.  If newlines are desired, the entire value should be
     enclosed in { } (curly-bracket) characters.  Any line beginning with a #
     or ; character is assumed to be a comment and will be ignored.

AAUUTTHHOORRSS
     Jordan Hubbard

BBUUGGSS
     Simplistic.

BSD                             October 7, 1998                            BSD
