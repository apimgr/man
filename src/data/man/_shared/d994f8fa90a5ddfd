The  command  provides  the  QEMU fw_cfg configuration files in a
file system tree at point The directory specified by is converted
to an absolute path before use.  The options are as follows:  Use
instead  of  for  the QEMU device.  Use as group for files in the
file system instead of the active group id.  Use  as  permissions
for  directories  instead  of  the default Use as permissions for
files instead of the default Use as user for files  in  the  file
system  instead  of the active user id.  A command first appeared
in
