RCMDSH(3)                BSD Library Functions Manual                RCMDSH(3)

NNAAMMEE
     rrccmmddsshh — return a stream to a remote command without superuser

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<uunniissttdd..hh>>

     _i_n_t
     rrccmmddsshh(_c_h_a_r _*_*_a_h_o_s_t, _i_n_t _i_n_p_o_r_t, _c_o_n_s_t _c_h_a_r _*_l_o_c_u_s_e_r,
         _c_o_n_s_t _c_h_a_r _*_r_e_m_u_s_e_r, _c_o_n_s_t _c_h_a_r _*_c_m_d, _c_o_n_s_t _c_h_a_r _*_r_s_h_p_r_o_g);

DDEESSCCRRIIPPTTIIOONN
     The rrccmmddsshh() function is used by normal users to execute a command on a
     remote machine using an authentication scheme based on reserved port num‐
     bers using rshd(8) or the value of _r_s_h_p_r_o_g (if non-NULL).

     The rrccmmddsshh() function looks up the host _*_a_h_o_s_t using gethostbyname(3),
     returning -1 if the host does not exist.  Otherwise _*_a_h_o_s_t is set to the
     standard name of the host and a connection is established to a server re‐
     siding at the well-known Internet port “shell/tcp” (or whatever port is
     used by _r_s_h_p_r_o_g).  The _i_n_p_o_r_t argument is ignored; it is only included to
     provide an interface similar to rcmd(3).

     If the connection succeeds, a socket in the UNIX domain of type
     SOCK_STREAM is returned to the caller, and given to the remote command as
     stdin, stdout, and stderr.

RREETTUURRNN VVAALLUUEESS
     The rrccmmddsshh() function returns a valid socket descriptor on success.  Oth‐
     erwise, -1 is returned and a diagnostic message is printed on the stan‐
     dard error.

SSEEEE AALLSSOO
     rsh(1), socketpair(2), rcmd(3), rshd(8)

HHIISSTTOORRYY
     The rrccmmddsshh() function first appeared in OpenBSD 2.0, and made its way
     into FreeBSD 4.6.

BBUUGGSS
     If rsh(1) encounters an error, a file descriptor is still returned in‐
     stead of -1.

BSD                            September 1, 1996                           BSD
