The  function call atomically creates the specified directory en‐
try (hard link) with the  attributes  of  the  underlying  object
pointed  at  by  If the link is successful, the link count of the
underlying object is incremented;  and  share  equal  access  and
rights  to the underlying object.  If is removed, the file is not
deleted and the link count of the  underlying  object  is  decre‐
mented.   In order for the system call to succeed, must exist and
both and must be  in  the  same  file  system.   As  mandated  by
POSIX.1,  may  not  be a directory.  will resolve and follow sym‐
bolic links contained within both and If the last component of is
a symbolic link, will point the hard link, to the underlying  ob‐
ject  pointed  to by not to the symbolic link itself.  The system
call is equivalent to except in the case where either or or  both
are  relative paths.  In this case a relative path is interpreted
relative to the directory associated with the file descriptor in‐
stead of the current working directory and similarly for and  the
file descriptor Values for are constructed by a bitwise‐inclusive
OR  of  flags from the following list, defined in If names a sym‐
bolic link, a new link for the target of  the  symbolic  link  is
created.   If  names a symbolic link, a new link for the symbolic
link is created.  If a symbolic link is encountered during  path‐
name resolution, an error is returned.  If does not reside in the
hierarchy  beneath  the starting directory, an error is returned.
If is passed the special value in the or parameter,  the  current
working  directory  is used for the respective argument.  If both
and have value the behavior is identical to a call to Unless con‐
tains the or the flags. On OS X, not assigning AT_SYMLINK_FOLLOW,
AT_SYMLINK_NOFOLLOW_ANY or AT_RESOLVE_BENEATH to  may  result  in
some  file  systems  returning an error.  Upon successful comple‐
tion, a value of 0 is returned.  Otherwise, a value of ‐1 is  re‐
turned  and  is set to indicate the error.  will fail and no link
will be created if: A component  of  either  path  prefix  denies
search  permission.  The requested link requires writing in a di‐
rectory with a mode that denies write  permission.   The  current
process  cannot access the existing file.  The directory in which
the entry for the new link is being placed cannot be extended be‐
cause the user’s quota of disk blocks on the file system contain‐
ing the directory has been exhausted.  The link named by  already
exists.   One of the pathnames specified is outside the process’s
allocated address space.  An I/O error occurs while reading  from
or  writing  to the file system to make the directory entry.  Too
many symbolic links are encountered in  translating  one  of  the
pathnames.   This is taken to be indicative of a looping symbolic
link.  The file already has {LINK_MAX} links.  A component  of  a
pathname  exceeds  characters,  or  an  entire path name exceeded
characters.  A component of either path prefix does not exist, or
is a dangling symbolic link.  The file named by does  not  exist,
or is a dangling symbolic link.  The directory in which the entry
for the new link is being placed cannot be extended because there
is  no space left on the file system containing the directory.  A
component of either path prefix is not  a  directory.   The  file
named  by is a directory.  The requested link requires writing in
a directory on a read‐only file system.  The link  named  by  and
the  file named by are on different file systems.  The file named
by is a file that must be materialized before  being  linked  and
the  I/O  policy  of the current thread or process disallows file
materialization In addition to the errors  returned  by  the  the
system  call may fail if: The or argument does not specify an ab‐
solute path and the or argument, respectively, is neither  nor  a
valid file descriptor open for searching.  The value of the argu‐
ment  is  not  valid.   was not set to The underlying file system
does not support this call.  The or argument is not  an  absolute
path  and or respectively, is neither nor a file descriptor asso‐
ciated with a directory.  If AT_SYMLINK_NOFOLLOW_ANY  was  passed
and  a symbolic link was encountered in translating either the or
arguments.  AT_RESOLVE_BENEATH was passed and does not reside  in
the  directory  hierarchy  beneath the starting directory encoun‐
tered in translating either the or arguments.   The  function  is
expected  to conform to The system call is expected to conform to
POSIX.1‐2008 .
