PTHREAD_MUTEX_CONSIST... BSD Library Functions Manual PTHREAD_MUTEX_CONSIST...

NNAAMMEE
     pptthhrreeaadd__mmuutteexx__ccoonnssiisstteenntt — mark state protected by robust mutex as con‐
     sistent

LLIIBBRRAARRYY
     POSIX Threads Library (libpthread, -lpthread)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<pptthhrreeaadd..hh>>

     _i_n_t
     pptthhrreeaadd__mmuutteexx__ccoonnssiisstteenntt(_p_t_h_r_e_a_d___m_u_t_e_x___t _*_m_u_t_e_x);

DDEESSCCRRIIPPTTIIOONN
     If the process containing the thread owning a robust mutex terminates
     while holding the mutex, the mutex becomes inconsistent and the next
     thread that acquires the mutex lock is notified of the state by the re‐
     turn value EOWNERDEAD.  In this case, the mutex does not become normally
     usable again until the state is marked consistent.

     The pptthhrreeaadd__mmuutteexx__ccoonnssiisstteenntt(), when called with the _m_u_t_e_x argument,
     which points to the initialized robust mutex in an inconsistent state,
     marks the by mutex as consistent again.  The consequent unlock of the mu‐
     tex, by either pptthhrreeaadd__mmuutteexx__uunnlloocckk() or other methods, allows other con‐
     tenders to lock the mutex.

     If the mutex in the inconsistent state is not marked consistent by the
     call to pptthhrreeaadd__mmuutteexx__ccoonnssiisstteenntt() before unlock, further attempts to
     lock the _m_u_t_e_x result in the ENOTRECOVERABLE condition reported by the
     locking functions.

RREETTUURRNN VVAALLUUEESS
     If successful, pptthhrreeaadd__mmuutteexx__ccoonnssiisstteenntt() will return zero, otherwise an
     error number will be returned to indicate the error.

EERRRROORRSS
     The pptthhrreeaadd__mmuutteexx__lloocckk() function will fail if:

     [EINVAL]           The mutex pointed to by the _m_u_t_e_x argument is not ro‐
                        bust, or is not in the inconsistent state.

SSEEEE AALLSSOO
     pthread_mutex_init(3), pthread_mutex_lock(3), pthread_mutex_unlock(3),
     pthread_mutexattr_setrobust(3)

SSTTAANNDDAARRDDSS
     The pptthhrreeaadd__mmuutteexx__lloocckk() function conforms to .

BSD                             March 27, 2017                             BSD
