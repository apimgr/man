_p_t_h_r_e_a_d___e_x_i_t(3)            Library Functions Manual            _p_t_h_r_e_a_d___e_x_i_t(3)

NNAAMMEE
       pthread_exit - terminate calling thread

LLIIBBRRAARRYY
       POSIX threads library (_l_i_b_p_t_h_r_e_a_d, _-_l_p_t_h_r_e_a_d)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<pptthhrreeaadd..hh>>

       [[[[nnoorreettuurrnn]]]] vvooiidd pptthhrreeaadd__eexxiitt((vvooiidd **_r_e_t_v_a_l));;

DDEESSCCRRIIPPTTIIOONN
       The pptthhrreeaadd__eexxiitt() function terminates the calling thread and returns a
       value  via  _r_e_t_v_a_l that (if the thread is joinable) is available to an‐
       other thread in the same process that calls pptthhrreeaadd__jjooiinn(3).

       Any clean‐up handlers established by pptthhrreeaadd__cclleeaannuupp__ppuusshh(3) that  have
       not  yet  been popped, are popped (in the reverse of the order in which
       they were pushed) and executed.  If the thread has any  thread‐specific
       data,  then, after the clean‐up handlers have been executed, the corre‐
       sponding destructor functions are called, in an unspecified order.

       When a thread terminates, process‐shared resources (e.g., mutexes, con‐
       dition variables, semaphores, and file descriptors) are  not  released,
       and functions registered using aatteexxiitt(3) are not called.

       After  the  last thread in a process terminates, the process terminates
       as by calling eexxiitt(3) with an exit status of zero; thus, process‐shared
       resources are released and functions  registered  using  aatteexxiitt(3)  are
       called.

RREETTUURRNN VVAALLUUEE
       This function does not return to the caller.

EERRRROORRSS
       This function always succeeds.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       pptthhrreeaadd__eexxiitt() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       POSIX.1‐2008.

HHIISSTTOORRYY
       POSIX.1‐2001.

NNOOTTEESS
       Performing a return from the start function of any thread other than
       the main thread results in an implicit call to pptthhrreeaadd__eexxiitt(), using
       the function’s return value as the thread’s exit status.

       To allow other threads to continue execution, the main thread should
       terminate by calling pptthhrreeaadd__eexxiitt() rather than eexxiitt(3).

       The value pointed to by _r_e_t_v_a_l should not be located on the calling
       thread’s stack, since the contents of that stack are undefined after
       the thread terminates.

BBUUGGSS
       Currently, there are limitations in the kernel implementation logic for
       wwaaiitt(2)ing on a stopped thread group with a dead thread group leader.
       This can manifest in problems such as a locked terminal if a stop
       signal is sent to a foreground process whose thread group leader has
       already called pptthhrreeaadd__eexxiitt().

SSEEEE AALLSSOO
       pptthhrreeaadd__ccrreeaattee(3), pptthhrreeaadd__jjooiinn(3), pptthhrreeaaddss(7)

Linux man‐pages (unreleased)      2025‐05‐17                   _p_t_h_r_e_a_d___e_x_i_t(3)
