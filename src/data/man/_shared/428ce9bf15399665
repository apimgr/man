The  character  device provides a read‐only interface to the cur‐
rent kernel symbol table.  It can be accessed either as a sequen‐
tial file, where it looks like an executable file but with  zero‐
sized  text and data segments, or via represents the symbol table
at the time when the device is opened, and may not  change  until
it  is  closed.   The  in‐kernel symbol manager is designed to be
able to handle any type of symbol table.   However,  only  symbol
tables are currently dealt with.  The command codes below are de‐
fined  in  The (third) argument to the should be a pointer to the
type indicated.  Returns the total size of the current symbol ta‐
ble.  This should be used when allocating a buffer to read in the
whole symbol table to memory.  Returns the value  for  the  given
symbol  name in a symtab‐independent fashion.  struct ksyms_gsym‐
bol  {          const  char   *kg_name;           unsigned   long
*kg_value; }; The struct element should be set to the name of the
requested  value, and the address that points to will receive the
symbol value.  Returns the complete symbol for the  given  symbol
name.  struct ksyms_gsymbol {         const char *kg_name;
        void  *kg_sym; }; The struct element should be set to the
name of the requested symbol, and the found symbol will be  writ‐
ten  to  the address.  It is the callers responsibility to ensure
that enough space for the symbol is allocated.  A  device  exists
in many different operating systems.  This implementation is mod‐
elled in function after Solaris This driver was written by Anders
Magnusson for The driver first appeared in
