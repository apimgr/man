The automounter configuration consists of the configuration file,
which  assigns  file  system  paths to map names, and maps, which
contain actual mount information.  The configuration file is used
by the command.  Map files are read by the daemon.  The file con‐
sists of lines with two or three entries separated by  whitespace
and  terminated  by a newline character: is either a fully speci‐
fied path, or When is a full path,  must  reference  an  indirect
map.  Otherwise, must reference a direct map.  See below.  speci‐
fies map to use.  If begins with it specifies a special map.  See
below.   If  is  not  a fully specified path will search for that
name in Otherwise it will use the path as given.  If the file in‐
dicated by is executable, will assume it is  an  executable  map.
See  below.   Otherwise,  the  file  is  opened  and the contents
parsed.  is an optional field that starts with  and  can  contain
generic  file system mount options.  The following example speci‐
fies that the indirect map will be mounted on /example auto_exam‐
ple Map files consist of lines with a number of entries separated
by whitespace and terminated by newline character: In most cases,
it can be simplified to: is the path component used  by  to  find
the  right  map  entry to use.  It is also used to form the final
mountpoint.  A wildcard can be used  for  the  key.   It  matches
every  directory  that does not match other keys.  Those directo‐
ries will not be visible to the user until accessed.  The  field,
if  present,  must  begin with When mounting the file system, op‐
tions supplied to and options specified in the map entry are con‐
catenated together.  The special option is used to  specify  file
system type.  It is not passed to the mount program as an option.
Instead,  it  is passed as an argument to The default is The spe‐
cial option is used to disable creation of top‐level  directories
for  special  and executable maps.  The optional field is used to
specify multiple mount points for a single key.  The field speci‐
fies the file system to be mounted.  Ampersands in the field  are
replaced with the value of This is typically used with wildcards,
like:  *       192.168.1.1:/share/&  The field may contain refer‐
ences to variables, like: sys     192.168.1.1:/sys/${OSNAME}  De‐
fined variables are: Expands to the output of Same as A literal $
sign.   Expands to the output of Expands to the output of Expands
to the output of Expands to the output  of  Additional  variables
can be defined with the option of and To pass a location that be‐
gins  with  prefix  it  with a colon.  For example, This example,
when put into and with referring to the map as  described  above,
specifies  that the NFS share will be mounted on when any process
attempts to access that mountpoint, with and mount  options,  de‐
scribed  in  x ‐intr,nfsv4 192.168.1.1:/share/example/x Automati‐
cally mount an SMB share on access,  as  a  guest  user,  without
prompting for a password: share ‐fstype=smbfs,‐N ://@server/share
Automatically  mount  the  CD  drive on access: cd ‐fstype=cd9660
:/dev/cd0 Special maps have names beginning with  Supported  spe‐
cial  maps  are:  Query  the  remote  NFS server and map exported
shares.  This map is traditionally mounted on Access to files  on
a remote NFS server is provided through the directory without any
additional configuration.  Directories for individual NFS servers
are  not  present until the first access, when they are automati‐
cally created.  Query devices that are not yet mounted, but  con‐
tain  valid  file systems.  Generally used to access files on re‐
movable media.  Mount file systems  configured  in  as  "noauto".
This  needs  to be set up as a direct map.  Prevent from mounting
anything on the mountpoint.  It is possible to add custom special
maps by adding them, as executable maps named to  the  directory.
If  the  map file specified in has the execute bit set, will exe‐
cute it and parse the standard output instead of parsing the file
contents.  When called without command line arguments,  the  exe‐
cutable  is expected to output a list of available map keys sepa‐
rated by newline characters.  Otherwise, the executable  will  be
called  with  a key name as a command line argument.  Output from
the executable is expected to be the entry for that key, not  in‐
cluding  the key itself.  Indirect maps are referred to in by en‐
tries with a fully qualified path as a mount point, and must con‐
tain only relative paths as keys.  Direct maps are referred to in
by entries with as the mountpoint, and must  contain  only  fully
qualified  paths  as  keys.   For  indirect maps, the final mount
point is determined by concatenating the mountpoint with the  map
entry  key  and  optional map entry mountpoint.  For direct maps,
the final mount point is determined by concatenating the map  en‐
try  key  with  the  optional  map entry mountpoint.  The example
above could be rewritten using direct map, by placing this in  /‐
auto_example  and  this  in  the map file: /example/x ‐intr,nfsv4
192.168.1.1:/share/example/x   /example/share    ‐fstype=smbfs,‐N
://@server/share  /example/cd  ‐fstype=cd9660  :/dev/cd0 Both and
maps may contain entries consisting of a plus sign and map  name:
+auto_master Those entries cause daemon to retrieve the named map
from  directory services (like LDAP) and include it where the en‐
try was.  If the file containing the map  referenced  in  is  not
found, the map will be retrieved from directory services instead.
To retrieve entries from directory services, daemon runs which is
usually  a  shell  script, with map name as the only command line
parameter.  The script should output entries formatted  according
to  or  automounter  map  syntax  to standard output.  An example
script to use LDAP is included in It can be symlinked to The  de‐
fault  location  of the file.  Directory containing shell scripts
to implement special maps and directory services.  The configura‐
tion file functionality was developed by under  sponsorship  from
the  Foundation.  The configuration file functionality was ported
to and by The special map is currently unsupported on
