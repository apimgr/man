ZPOOL-ADD(8)              BSD System Manager's Manual             ZPOOL-ADD(8)

NNAAMMEE
     zzppooooll--aadddd — add vdevs to ZFS storage pool

SSYYNNOOPPSSIISS
     zzppooooll aadddd [--ffggLLnnPP]
           [----aallllooww--iinn--uussee ----aallllooww--rreepplliiccaattiioonn--mmiissmmaattcchh ----aallllooww--aasshhiifftt--mmiissmmaattcchh]
           [--oo _p_r_o_p_e_r_t_y=_v_a_l_u_e] _p_o_o_l _v_d_e_vâ¦

DDEESSCCRRIIPPTTIIOONN
     Adds the specified virtual devices to the given pool.  The _v_d_e_v specifi‐
     cation is described in the _V_i_r_t_u_a_l _D_e_v_i_c_e_s section of zpoolconcepts(7).
     The behavior of the --ff option, and the device checks performed are de‐
     scribed in the zzppooooll ccrreeaattee subcommand.

     --ff      Forces use of _v_d_e_vs, even if they appear in use, have conflicting
             ashift values, or specify a conflicting replication level.  Not
             all devices can be overridden in this manner.

     --gg      Display _v_d_e_v, GUIDs instead of the normal device names.  These
             GUIDs can be used in place of device names for the zpool de‐
             tach/offline/remove/replace commands.

     --LL      Display real paths for _v_d_e_vs resolving all symbolic links.  This
             can be used to look up the current block device name regardless
             of the _/_d_e_v_/_d_i_s_k path used to open it.

     --nn      Displays the configuration that would be used without actually
             adding the _v_d_e_vs.  The actual pool creation can still fail due to
             insufficient privileges or device sharing.

     --PP      Display real paths for _v_d_e_vs instead of only the last component
             of the path.  This can be used in conjunction with the --LL flag.

     --oo _p_r_o_p_e_r_t_y=_v_a_l_u_e
             Sets the given pool properties.  See the zpoolprops(7) manual
             page for a list of valid properties that can be set.  The only
             property supported at the moment is aasshhiifftt.

     ----aallllooww--aasshhiifftt--mmiissmmaattcchh
             Disable the ashift validation which allows mismatched ashift val‐
             ues in the pool.  Adding top-level _v_d_e_vs with different sector
             sizes will prohibit future device removal operations, see
             zpool-remove(8).

     ----aallllooww--iinn--uussee
             Allow vdevs to be added even if they might be in use in another
             pool.

     ----aallllooww--rreepplliiccaattiioonn--mmiissmmaattcchh
             Allow vdevs with conflicting replication levels to be added to
             the pool.

EEXXAAMMPPLLEESS
   EExxaammppllee 11: Adding a Mirror to a ZFS Storage Pool
     The following command adds two mirrored disks to the pool _t_a_n_k, assuming
     the pool is already made up of two-way mirrors.  The additional space is
     immediately available to any datasets within the pool.
           # zzppooooll aadddd _t_a_n_k mmiirrrroorr _s_d_a _s_d_b

   EExxaammppllee 22: Adding Cache Devices to a ZFS Pool
     The following command adds two disks for use as cache devices to a ZFS
     storage pool:
           # zzppooooll aadddd _p_o_o_l ccaacchhee _s_d_c _s_d_d

     Once added, the cache devices gradually fill with content from main mem‐
     ory.  Depending on the size of your cache devices, it could take over an
     hour for them to fill.  Capacity and reads can be monitored using the
     iioossttaatt subcommand as follows:
           # zzppooooll iioossttaatt --vv _p_o_o_l _5

SSEEEE AALLSSOO
     zpool-attach(8), zpool-import(8), zpool-initialize(8), zpool-online(8),
     zpool-remove(8)

BSD                              March 8, 2024                             BSD
