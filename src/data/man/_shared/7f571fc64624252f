The  program  allows  manipulation  of  GPIO (General Purpose In‐
put/Output) device pins.  Such devices can be either part of  the
chipset  or  embedded  CPU, or a separate chip.  The usual way of
using GPIO is to connect some simple devices  such  as  LEDs  and
1‐wire  thermal sensors to its pins.  Each GPIO device has an as‐
sociated device file in the directory.  can be specified with  or
without  the  prefix.  For example, or GPIO pins can be either or
with the values of logical 0 or 1.  If only a number is specified
on the command line, the pin state will be  read  from  the  GPIO
controller  and  displayed.   To  write to a pin, a value must be
specified after the number.  Values can be  either  0  or  1.   A
value  of 2 the pin, i.e. changes its state to the opposite.  In‐
stead of the numerical values, the word or  can  be  used.   Only
pins that have been configured at securelevel 0, typically during
system  startup,  are  accessible  once  the securelevel has been
raised.  Pins can be given symbolic names for  easier  use.   Be‐
sides  using  individual  pins, device drivers that use GPIO pins
can be attached to a device using the command.  Such drivers  can
be detached at runtime using the command.  The following configu‐
ration  are supported by the GPIO framework: input direction out‐
put direction bi‐directional open‐drain output  push‐pull  output
tri‐state  (output  disabled)  internal  pull‐up enabled internal
pull‐down enabled invert input invert output pulsate output at  a
hardware‐defined  frequency  and  duty cycle select alternate pin
function 0 to 7 Note that not all the flags may be  supported  by
the  particular GPIO controller.  When executed with only the de‐
vice name as argument, reads information about  the  GPIO  device
and  displays  it.   At  securelevel  0  the number of physically
available pins is displayed, at higher securelevels the number of
configured (set) pins is displayed.  The options are as  follows:
Operate quietly i.e. nothing is printed to stdout.  Only output a
single number on stdout, representing either the state of the pin
or  the  number  of available pins if no pin number was passed as
argument.  This option is useful  e.g.  when  is  used  in  shell
scripts  to  query  the  state  of a pin.  GPIO device unit file.
Configure pin 20 to have push‐pull output: Write logical 1 to pin
20: Attach a bus on a device on pin 4: Detach the gpioow0 device:
Configure pin 5 as output and name it error_led: Toggle  the  er‐
ror_led: Enumerate all pins and display their symbolic names: The
command  first  appeared in and The program was written by Device
attachment was added by
