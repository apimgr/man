The  utility  removes a user from the system, optionally removing
that user’s home directory and any subdirectories.  Default  val‐
ues  are  taken from the information provided in the file, which,
if running as root, is created using the built‐in defaults if  it
does not exist.  The first form of the command shown above (using
the  option) sets and displays the defaults for the utility.  See
for more information about Without any further options, will show
the current defaults which will be used by the utility.  Together
with one of the options shown for the first version of  the  com‐
mand,  will  set the default to be the new value.  This option is
included if built with Set the preservation value.  If this value
is one of or a non‐zero number, then the user  login  information
will  be preserved.  This option is included if built with In the
second form of the command, after setting any defaults, and  then
reading  values  from  the  following  command  line  options are
processed: Preserve the user information in  the  password  file,
but  do not allow the user to login, by switching the password to
an one, and by setting the user’s shell to the program.  This op‐
tion can be helpful in preserving a user’s files for later use by
members of that person’s group after the user has moved on.  This
value can also be set in the file, using the field.  If the field
has any of the values or a non‐zero number, then user information
preservation will take place.  This option is included  if  built
with  Remove  the  user’s home directory, any subdirectories, and
any files and other entries in them.  Allow  a  samba  user  name
(with  a trailing dollar sign) to be deleted.  This option is in‐
cluded if built with Perform any actions  in  a  verbose  manner.
This  option  is  included if built with Once the information has
been verified, uses to update the user database.  This is run  in
the  background, and, at very large sites could take several min‐
utes.  Until this update is completed, the password file  is  un‐
available for other updates and the new information is not avail‐
able  to  programs.  The utility first appeared in It is based on
the package by the same author.  The utility was written by
