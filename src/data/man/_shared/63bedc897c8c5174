This  driver  supports  PC/AT keyboards within the console frame‐
work.  It doesn’t provide direct device driver entry  points  but
makes  its  functions  available via the internal interface.  The
driver supports a number of different key mappings which  can  be
chosen  from  with  the kernel option at compile time or with the
utility (variable: at runtime.  Other mappings can be used if the
whole keymap is replaced by means of Because PC keyboard hardware
doesn’t contain a beeper, requests  for  cannot  be  handled  di‐
rectly.  On alpha and i386 a helper device attached to the driver
allows  the use of the standard ISA speaker for this purpose.  On
acorn32, performs this function.  To set a German keyboard layout
without and sending an ESC character before the key symbol if the
ALT key is pressed simultaneously, use To set it at kernel  build
time, add to the kernel configuration file.
