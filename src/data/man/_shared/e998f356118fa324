reports  certain  kernel  statistics  kept about process, virtual
memory, disk, trap, and CPU activity.  If is invoked without  any
options, it displays the summary of statistics since boot and ex‐
its.   This  is also referred to as the first line of The options
are as follows: Report on kernel memory caches.  Combine with the
option to see  information  about  memory  pools  that  back  the
caches.   Repeat the display times.  The first display is for the
time since a reboot and each subsequent report is  for  the  time
period  since the last display.  If no interval is specified, the
default is 1 second.  Report the values of system event counters.
Report fork statistics.  Report all hash table  statistics.   Re‐
port hash table statistics for Report the values of system inter‐
rupt  counters.   List  all the hashes supported for and List the
UVM histories being maintained by the kernel.  Extract values as‐
sociated with the name list from the specified  core  instead  of
the  default  Report  on  the list of the kernel memory pools and
their usage.  Extract the name list from the specified system in‐
stead of the default Display up to disks in the standard  output,
instead of the default 2.  Display the contents of the structure.
This  contains  various  paging event and memory status counters.
Display the contents of the structure.  This includes information
about processes and virtual memory.  The process part  shows  the
number  of processes in the following states: on the run queue in
disk I/O wait waiting for paging sleeping The virtual memory sec‐
tion shows: Total virtual memory Active virtual memory in use Ac‐
tive real memory in use Shared virtual memory Active shared  vir‐
tual  memory  Shared  real  memory Active shared real memory Free
memory All memory values are shown in number of pages.  Dump  all
UVM  histories.  Dump the specified UVM history.  Print more ver‐
bose information.  When used with the or options prints  out  all
counters,  not  just those with non‐zero values.  Print more ver‐
bose information about kernel memory pools.   Pause  seconds  be‐
tween  each  display.   If no repeat is specified, the default is
infinity.  The command will accept and honor a negative number of
seconds.  By default, displays the following information:  Infor‐
mation  about the numbers of processes in various states.  in run
queue blocked for  resources  (i/o,  paging,  etc.)   Information
about  the  usage of virtual and real memory.  Virtual pages (re‐
ported in units of 1024 bytes) are considered active if they  be‐
long  to  processes  which are running or have run in the last 20
seconds.  active virtual pages size of the free list  Information
about  page faults and paging activity.  These are averaged every
five seconds, and given in units per second.  total  page  faults
page  reclaims  (simulating  reference bits) pages paged in pages
paged out pages freed per second pages  scanned  by  clock  algo‐
rithm,  per‐second  Disk  transfers per second.  Typically paging
will be split across the available drives.   The  header  of  the
field  is  the first character of the disk name and the unit num‐
ber.  If more than four disk drives are configured in the system,
displays only the first four drives.  To force  to  display  spe‐
cific  drives,  their  names may be supplied on the command line.
Trap/interrupt rate averages per second over last 5 seconds.  de‐
vice interrupts per interval (including clock interrupts)  system
calls  per  interval  CPU context switch rate (switches/interval)
Breakdown of percentage usage of CPU time.  user time for  normal
and  low  priority  processes system time CPU idle default kernel
namelist default memory file The command will print what the sys‐
tem is doing every five seconds; this is a good choice of  print‐
ing  interval  since this is how often some of the statistics are
sampled in the system.  Others vary every second and running  the
output  for  a  while  will make it apparent which are recomputed
every second.  The and options are only available  with  the  de‐
fault  output.  The and options are useful only if the system was
compiled with support for UVM history.
