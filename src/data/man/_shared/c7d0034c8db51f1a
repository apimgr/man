Configuration  of  PHYs is necessary.  See The device driver sup‐
ports network adapters based on the Texas Instruments  ThunderLAN
chip.    Supported  cards  include:  in  baseboard  and  variants
(10BASE‐T‐only variant untested).  in baseboard variant only (the
variant doesn’t use the same chip!).  (This has  been  tested  on
the  Deskpro  4000M only).  The different models of the supported
boards come with some subset of RJ‐45, BNC  and  AUI  connectors.
Media  selection is done using using the standard mechanism.  Re‐
fer to those manual  pages  for  more  information.   The  driver
doesn’t  have full automatic media selection.  By default it will
do an Nway negotiation on the 10BASE‐T port for the speed and du‐
plex mode with the link partner.  If the AUI or BNC port is used,
an explicit media type must be specified to The board  marked  as
untested  will always claim having an AUI connector, where it may
be a BNC one.
