attempts  to read bytes of data from the object referenced by the
descriptor into the buffer pointed to by performs  the  same  ac‐
tion,  but  scatters the input data into the buffers specified by
the members of the array:  iov[0],  iov[1],  ...,  iov[iovcnt-1].
and perform the same functions, but read from the specified posi‐
tion in the file without modifying the file pointer.  For and the
structure is defined as: struct iovec {         char   *iov_base;
/*  Base  address. */         size_t iov_len;    /* Length. */ };
Each entry specifies the base address and length of  an  area  in
memory where data should be placed.  and will always fill an area
completely  before proceeding to the next.  On objects capable of
seeking, the starts at a position given by the pointer associated
with (see Upon return from the pointer is incremented by the num‐
ber of bytes actually read.  Objects  that  are  not  capable  of
seeking  always read from the current position.  The value of the
pointer associated with such an object is undefined.   Upon  suc‐
cessful  completion, and return the number of bytes actually read
and placed in the buffer.  The system guarantees to read the num‐
ber of bytes requested if the descriptor references a normal file
that has that many bytes left before the end‐of‐file, but  in  no
other  case.   and will fail if the parameter exceeds and they do
not attempt a partial read.  If successful, the number  of  bytes
actually read is returned.  Upon reading end‐of‐file, zero is re‐
turned.   Otherwise,  a ‐1 is returned and the global variable is
set to indicate the error.  The and calls  will  succeed  unless:
The  file was marked for non‐blocking I/O, and no data were ready
to be read.  is not a valid file or socket  descriptor  open  for
reading.   points  outside  the  allocated address space.  A read
from a slow device was interrupted before any data arrived by the
delivery of a signal.  The pointer associated with was  negative.
An  I/O  error  occurred while reading from the file system.  The
process group is orphaned.   The  file  is  a  regular  file,  is
greater  than 0, the starting position is before the end‐of‐file,
and the starting position is greater than or equal to the  offset
maximum  established for the open file descriptor associated with
An attempt is made to read a directory.  An attempt to allocate a
memory buffer fails.  Insufficient memory is available.   An  ac‐
tion  is  requested of a device that does not exist.  A requested
action cannot be performed by the device.  An attempt to  read  a
remote  file  through  NFS  that  has already been deleted in the
server.  The connection timed out while  reading  a  remote  file
from  a  soft mounted NFS volume (see The file is a file that re‐
quires materialization and materialization timed out  or  encoun‐
tered  some other temporary failure.  The file is a file that re‐
quires materialization and the I/O policy of the  current  thread
or  process disallows dataless file materialization and calls may
also return the following errors: The specified  file  offset  is
invalid.   The file descriptor is associated with a pipe, socket,
or FIFO.  The call may also return the following errors: The con‐
nection is closed by the peer during a read attempt on a  socket.
A  read  is  attempted  on an unconnected socket.  A transmission
timeout occurs during a read attempt on a socket.  The  and  call
may  also  return the following error: The value provided for ex‐
ceeds The and calls may also return one of the following  errors:
Part of the points outside the process’s allocated address space.
was less than or equal to 0, or greater than 16.  One of the val‐
ues  in the array was negative.  The sum of the values in the ar‐
ray overflowed a 32‐bit integer.  The include files and are  nec‐
essary  for all functions.  The function call is expected to con‐
form to The and functions are expected to conform to is  nonstan‐
dard.   The  function call appeared in The function call appeared
in A function call appeared in
