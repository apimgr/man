The  command  attaches  a log‐structured file system device on to
the file system tree at the point Both and are converted  to  ab‐
solute  paths before use.  In addition, the utility is invoked to
clean the file system periodically.  This command is normally ex‐
ecuted by at boot time.  The options are as follows: Instruct the
cleaner to count bytes written, rather than segments read, to de‐
termine how many segments to clean at once.  Run in  debug  mode.
Instruct the cleaner to use filesystem idle time as the criterion
for  aggressive  cleaning,  instead  of system load.  Options are
specified with a flag followed by a comma separated string of op‐
tions.  See the man page for possible options and their meanings.
Clean segments (or bytes’ worth of segments if is also specified)
at a time.  Don’t start on the file system.  Cause to  read  data
in  small  chunks  when  cleaning  the file system.  The function
first appeared in
