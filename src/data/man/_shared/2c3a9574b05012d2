_s_i_n_c_o_s(3)                  Library Functions Manual                  _s_i_n_c_o_s(3)

NNAAMMEE
       sincos, sincosf, sincosl - calculate sin and cos simultaneously

LLIIBBRRAARRYY
       Math library (_l_i_b_m, _-_l_m)

SSYYNNOOPPSSIISS
       ##ddeeffiinnee __GGNNUU__SSOOUURRCCEE         /* See feature_test_macros(7) */
       ##iinncclluuddee <<mmaatthh..hh>>

       vvooiidd ssiinnccooss((ddoouubbllee _x,, ddoouubbllee **_s_i_n,, ddoouubbllee **_c_o_s));;
       vvooiidd ssiinnccoossff((ffllooaatt _x,, ffllooaatt **_s_i_n,, ffllooaatt **_c_o_s));;
       vvooiidd ssiinnccoossll((lloonngg ddoouubbllee _x,, lloonngg ddoouubbllee **_s_i_n,, lloonngg ddoouubbllee **_c_o_s));;

DDEESSCCRRIIPPTTIIOONN
       Several  applications  need sine and cosine of the same angle _x.  These
       functions compute both at the same time, and store the results in  _*_s_i_n
       and  _*_c_o_s.  Using this function can be more efficient than two separate
       calls to ssiinn(3) and ccooss(3).

       If _x is a NaN, a NaN is returned in _*_s_i_n and _*_c_o_s.

       If _x is positive infinity or negative infinity, a domain error  occurs,
       and a NaN is returned in _*_s_i_n and _*_c_o_s.

RREETTUURRNN VVAALLUUEE
       These functions return _v_o_i_d.

EERRRROORRSS
       See  mmaatthh__eerrrroorr(7) for information on how to determine whether an error
       has occurred when calling these functions.

       The following errors can occur:

       Domain error: _x is an infinity
              _e_r_r_n_o is set to EEDDOOMM (but see BUGS).  An invalid  floating‐point
              exception (FFEE__IINNVVAALLIIDD) is raised.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       ssiinnccooss(), ssiinnccoossff(), ssiinnccoossll() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       GNU.

HHIISSTTOORRYY
       glibc 2.1.

NNOOTTEESS
       To see the performance advantage of ssiinnccooss(), it may be necessary to
       disable ggcccc(1) built‐in optimizations, using flags such as:

           cc -O -lm -fno-builtin prog.c

BBUUGGSS
       Before glibc 2.22, the glibc implementation did not set _e_r_r_n_o to EEDDOOMM
       when a domain error occurred.

SSEEEE AALLSSOO
       ccooss(3), ssiinn(3), ttaann(3)

Linux man‐pages (unreleased)      2025‐05‐17                         _s_i_n_c_o_s(3)
