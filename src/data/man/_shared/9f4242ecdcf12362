MALO(4)                  BSD Kernel Interfaces Manual                  MALO(4)

NNAAMMEE
     mmaalloo — Marvell Libertas IEEE 802.11b/g wireless network device

SSYYNNOOPPSSIISS
     mmaalloo** aatt ccaarrddbbuuss??
     mmaalloo** aatt ppccii??
     mmaalloo** aatt ppccmmcciiaa??

DDEESSCCRRIIPPTTIIOONN
     The mmaalloo driver provides support for Marvell Libertas
     88W8335/88W8310/88W8385 based PCI/CardBus/PCMCIA network adapters.  The
     second generation 88W8335/88W8310 chipsets support 802.11b/g.

     These are the modes the mmaalloo driver can operate in:

     BSS mode       Also known as _i_n_f_r_a_s_t_r_u_c_t_u_r_e mode, this is used when asso‐
                    ciating with an access point, through which all traffic
                    passes.  This mode is the default.

     monitor mode   In this mode the driver is able to receive packets without
                    associating with an access point.  This disables the in‐
                    ternal receive filter and enables the card to capture
                    packets from networks which it wouldn't normally have ac‐
                    cess to, or to scan for access points.

     The mmaalloo driver can be configured to use Wired Equivalent Privacy (WEP)
     or Wi-Fi Protected Access (WPA1 and WPA2).  WPA2 is currently the most
     secure encryption standard for wireless networks supported by OpenBSD.
     It is strongly recommended that neither WEP nor WPA1 are used as the sole
     mechanism to secure wireless communication, due to serious weaknesses.
     WPA1 is disabled by default and may be enabled using the option
     "wwppaapprroottooss _w_p_a_1_,_w_p_a_2".  For standard WPA networks which use pre-shared
     keys (PSK), keys are configured using the "wwppaakkeeyy" option.  WPA-Enter‐
     prise networks require use of the wpa_supplicant package.  The mmaalloo
     driver relies on the software 802.11 stack for both encryption and de‐
     cryption of data frames.

     The mmaalloo driver can be configured at runtime with ifconfig(8) or on boot
     with hostname.if(5).

FFIILLEESS
     The driver needs a set of firmware files which are loaded when an inter‐
     face is brought up:

           /etc/firmware/malo8335-h
           /etc/firmware/malo8335-m
           /etc/firmware/malo8338
           /etc/firmware/malo8385-h
           /etc/firmware/malo8385-m

     These firmware files are not free because Marvell refuses to grant dis‐
     tribution rights.  As a result, even though OpenBSD includes the driver,
     the firmware files cannot be included and users have to download these
     files on their own.

     A prepackaged version of the firmware can be installed using
     fw_update(8).

HHAARRDDWWAARREE
     The following cards are among those supported by the mmaalloo driver:

           _C_a_r_d                  _C_h_i_p       _B_u_s        _S_t_a_n_d_a_r_d
           Ambicom WL54CF        88W8385    PCMCIA     b/g
           I-O DATA WN-G54/CF    88W8385    PCMCIA     b/g
           Netgear WG511v2       88W8310    CardBus    b/g
           Netgear WG311v3       88W8335    PCI        b/g
           SparkLAN WCFM-100     88W8385    PCMCIA     b/g
           Tenda TWL541C         88W8310    CardBus    b/g
           Tenda TWL542P         88W8335    PCI        b/g

EEXXAAMMPPLLEESS
     The following example scans for available networks:

           # ifconfig malo0 scan

     The following hostname.if(5) example configures malo0 to join network
     “mynwid”, using WPA key “mywpakey”, obtaining an IP address using DHCP:

           join mynwid wpakey mywpakey
           inet autoconf

SSEEEE AALLSSOO
     arp(4), cardbus(4), ifmedia(4), intro(4), netintro(4), pci(4),
     hostname.if(5), ifconfig(8)

HHIISSTTOORRYY
     The mmaalloo driver first appeared in OpenBSD 4.1.

AAUUTTHHOORRSS
     The mmaalloo driver was written by Claudio Jeker and Marcus Glocker.

CCAAVVEEAATTSS
     WPA encryption is not supported for PCMCIA devices.

BSD                             March 27, 2025                             BSD
