KSTACK_CONTAINS(9)       BSD Kernel Developer's Manual      KSTACK_CONTAINS(9)

NNAAMMEE
     kkssttaacckk__ccoonnttaaiinnss — determine if an address range lies within the kernel
     stack for a thread

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<mmaacchhiinnee//ssttaacckk..hh>>

     _b_o_o_l
     kkssttaacckk__ccoonnttaaiinnss(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d, _v_m___o_f_f_s_e_t___t _v_a, _s_i_z_e___t _l_e_n);

DDEESSCCRRIIPPTTIIOONN
     This function can be used to determine whether a given address range
     falls within the kernel stack for the thread pointed to by _t_d.

RREETTUURRNN VVAALLUUEESS
     The function kkssttaacckk__ccoonnttaaiinnss() returns true if the range of addresses
     [_v_a..( _v_a+_l_e_n-1)] (both addresses inclusive) lies within the kernel stack
     for the thread pointed to by argument _t_d, or returns false otherwise.

EERRRROORRSS
     This function does not return an error.

SSEEEE AALLSSOO
     kproc(9), kthread(9)

BSD                               May 2, 2023                              BSD
