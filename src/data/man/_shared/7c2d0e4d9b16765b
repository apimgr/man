TRUNCATE(1)               BSD General Commands Manual              TRUNCATE(1)

NNAAMMEE
     ttrruunnccaattee — truncate, extend the length of files, or perform space manage‐
     ment in files

SSYYNNOOPPSSIISS
     ttrruunnccaattee [--cc] --ss [++|--|%%|//]_s_i_z_e[SSUUFFFFIIXX] _f_i_l_e _._._.
     ttrruunnccaattee [--cc] --rr _r_f_i_l_e _f_i_l_e _._._.
     ttrruunnccaattee [--cc] --dd [--oo _o_f_f_s_e_t[SSUUFFFFIIXX]] --ll _l_e_n_g_t_h[SSUUFFFFIIXX] _f_i_l_e _._._.

DDEESSCCRRIIPPTTIIOONN
     The ttrruunnccaattee utility adjusts the length of each regular file given on the
     command-line, or performs space management with the given offset and the
     length over a regular file given on the command-line.

     The following options are available:

     --cc      Do not create files if they do not exist.  The ttrruunnccaattee utility
             does not treat this as an error.  No error messages are displayed
             and the exit value is not affected.

     --rr _r_f_i_l_e
             Truncate or extend files to the length of the file _r_f_i_l_e.

     --ss [++|--|%%|//]_s_i_z_e[SSUUFFFFIIXX]
             If the _s_i_z_e argument is preceded by a plus sign (++), files will
             be extended by this number of bytes.  If the _s_i_z_e argument is
             preceded by a dash (--), file lengths will be reduced by no more
             than this number of bytes, to a minimum length of zero bytes.  If
             the _s_i_z_e argument is preceded by a percent sign (%%), files will
             be round up to a multiple of this number of bytes.  If the _s_i_z_e
             argument is preceded by a slash sign (//), files will be round
             down to a multiple of this number of bytes, to a minimum length
             of zero bytes.  Otherwise, the _s_i_z_e argument specifies an abso‐
             lute length to which all files should be extended or reduced as
             appropriate.

     --dd      Zero a region in the specified file.  If the underlying file sys‐
             tem of the given file supports hole-punching, file system space
             deallocation may be performed in the operation region.

     --oo _o_f_f_s_e_t
             The space management operation is performed at the given _o_f_f_s_e_t
             bytes in the file.  If this option is not specified, the opera‐
             tion is performed at the beginning of the file.

     --ll _l_e_n_g_t_h
             The length of the operation range in bytes.  This option must al‐
             ways be specified if option --dd is specified, and must be greater
             than 0.

     The _s_i_z_e, _o_f_f_s_e_t and _l_e_n_g_t_h arguments may be suffixed with one of KK, MM, GG
     or TT (either upper or lower case) to indicate a multiple of Kilobytes,
     Megabytes, Gigabytes or Terabytes respectively.

     Exactly one of the --rr, --ss and --dd options must be specified.

     If a file is made smaller, its extra data is lost.  If a file is made
     larger, it will be extended as if by writing bytes with the value zero.
     If the file does not exist, it is created unless the --cc option is speci‐
     fied.

     Note that, while truncating a file causes space on disk to be freed, ex‐
     tending a file does not cause space to be allocated.  To extend a file
     and actually allocate the space, it is necessary to explicitly write data
     to it, using (for example) the shell's ‘>>’ redirection syntax, or dd(1).

EEXXIITT SSTTAATTUUSS
     The ttrruunnccaattee utility exits 0 on success, and >0 if an error occurs.  If
     the operation fails for an argument, ttrruunnccaattee will issue a diagnostic and
     continue processing the remaining arguments.

EEXXAAMMPPLLEESS
     Adjust the size of the file _t_e_s_t___f_i_l_e to 10 megabytes but do not create
     it if it does not exist:

           truncate -c -s 10M test_file

     Same as above but create the file if it does not exist:

           truncate -s 10M test_file
           ls -l test_file
           -rw-r--r--  1 root  wheel  10485760 Jul 22 18:48 test_file

     Adjust the size of _t_e_s_t___f_i_l_e to the size of the kernel and create another
     file _t_e_s_t___f_i_l_e_2 with the same size:

           truncate -r /boot/kernel/kernel test_file test_file2
           ls -l /boot/kernel/kernel test_file*
           -r-xr-xr-x  1 root  wheel    31352552 May 15 14:18 /boot/kernel/kernel
           -rw-r--r--  1 root  wheel    31352552 Jul 22 19:15 test_file
           -rw-r--r--  1 root  wheel    31352552 Jul 22 19:15 test_file2

     Increase the size of the file _t_e_s_t___f_i_l_e by 5 megabytes but do not create
     it if it does not exist:

           truncate -s +5M test_file
           ls -l test_file*
           -rw-r--r--  1 root  wheel    36595432 Sep 20 19:17 test_file
           -rw-r--r--  1 root  wheel    31352552 Sep 20 19:15 test_file2

     Reduce the size of the file _t_e_s_t___f_i_l_e by 5 megabytes:

           truncate -s -5M test_file
           ls -l test_file*
           -rw-r--r--  1 root  wheel    31352552 Jul 22 19:19 test_file
           -rw-r--r--  1 root  wheel    31352552 Jul 22 19:15 test_file2

SSEEEE AALLSSOO
     dd(1), touch(1), fspacectl(2), truncate(2)

SSTTAANNDDAARRDDSS
     The ttrruunnccaattee utility conforms to no known standards.

HHIISSTTOORRYY
     The ttrruunnccaattee utility first appeared in FreeBSD 4.2.

AAUUTTHHOORRSS
     The ttrruunnccaattee utility was written by Sheldon Hearn
     <_s_h_e_l_d_o_n_h_@_s_t_a_r_j_u_i_c_e_._n_e_t>.  Hole-punching support of this utility was de‐
     veloped by
     Ka Ho Ng <_k_h_n_g_@_F_r_e_e_B_S_D_._o_r_g>.

BSD                              March 6, 2025                             BSD
