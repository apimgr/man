The  semaphore  referenced by is unlocked, the value of the sema‐
phore is incremented, and all threads which are  waiting  on  the
semaphore are awakened.  is reentrant with respect to signals and
may  be called from within a signal hanlder.  If successful, will
return 0.  Otherwise, ‐1 is returned and is  set.   succeeds  un‐
less:  is  not a valid semaphore descriptor.  is specified in the
POSIX Realtime Extension (1003.1b‐1993/1003.1i‐1995).
