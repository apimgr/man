_f_p_a_t_h_c_o_n_f(3)               Library Functions Manual               _f_p_a_t_h_c_o_n_f(3)

NNAAMMEE
       fpathconf, pathconf - get configuration values for files

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<uunniissttdd..hh>>

       lloonngg ffppaatthhccoonnff((iinntt _f_d,, iinntt _n_a_m_e));;
       lloonngg ppaatthhccoonnff((ccoonnsstt cchhaarr **_p_a_t_h,, iinntt _n_a_m_e));;

DDEESSCCRRIIPPTTIIOONN
       ffppaatthhccoonnff() gets a value for the configuration option _n_a_m_e for the open
       file descriptor _f_d.

       ppaatthhccoonnff()  gets a value for configuration option _n_a_m_e for the filename
       _p_a_t_h.

       The corresponding macros defined in _<_u_n_i_s_t_d_._h_> are minimum  values;  if
       an  application  wants  to take advantage of values which may change, a
       call to ffppaatthhccoonnff() or ppaatthhccoonnff() can be made,  which  may  yield  more
       liberal results.

       Setting  _n_a_m_e  equal to one of the following constants returns the fol‐
       lowing configuration options:

       __PPCC__LLIINNKK__MMAAXX
              The maximum number of links to the file.  If _f_d or _p_a_t_h refer to
              a directory, then the value applies to the whole directory.  The
              corresponding macro is __PPOOSSIIXX__LLIINNKK__MMAAXX.

       __PPCC__MMAAXX__CCAANNOONN
              The maximum length of a formatted input line, where _f_d  or  _p_a_t_h
              must   refer   to   a  terminal.   The  corresponding  macro  is
              __PPOOSSIIXX__MMAAXX__CCAANNOONN.

       __PPCC__MMAAXX__IINNPPUUTT
              The maximum length of an input line, where _f_d or _p_a_t_h must refer
              to a terminal.  The corresponding macro is __PPOOSSIIXX__MMAAXX__IINNPPUUTT.

       __PPCC__NNAAMMEE__MMAAXX
              The maximum length of a filename in the  directory  _p_a_t_h  or  _f_d
              that  the process is allowed to create.  The corresponding macro
              is __PPOOSSIIXX__NNAAMMEE__MMAAXX.

       __PPCC__PPAATTHH__MMAAXX
              The maximum length of a relative pathname when _p_a_t_h or _f_d is the
              current  working  directory.    The   corresponding   macro   is
              __PPOOSSIIXX__PPAATTHH__MMAAXX.

       __PPCC__PPIIPPEE__BBUUFF
              The  maximum number of bytes that can be written atomically to a
              pipe of FIFO.  For ffppaatthhccoonnff(), _f_d should refer  to  a  pipe  or
              FIFO.   For ffppaatthhccoonnff(), _p_a_t_h should refer to a FIFO or a direc‐
              tory; in the latter case, the returned value corresponds to  FI‐
              FOs  created  in  that  directory.   The  corresponding macro is
              __PPOOSSIIXX__PPIIPPEE__BBUUFF.

       __PPCC__CCHHOOWWNN__RREESSTTRRIICCTTEEDD
              This returns a  positive  value  if  the  use  of  cchhoowwnn(2)  and
              ffcchhoowwnn(2)  for  changing  a  file’s  user  ID is restricted to a
              process with appropriate privileges, and changing a file’s group
              ID to a value other than the process’s effective group ID or one
              of its supplementary group IDs is restricted to a  process  with
              appropriate  privileges.   According  to  POSIX.1, this variable
              shall always be defined with a value other than -1.  The  corre‐
              sponding macro is __PPOOSSIIXX__CCHHOOWWNN__RREESSTTRRIICCTTEEDD.

              If  _f_d  or _p_a_t_h refers to a directory, then the return value ap‐
              plies to all files in that directory.

       __PPCC__NNOO__TTRRUUNNCC
              This  returns  nonzero  if  accessing  filenames   longer   than
              __PPOOSSIIXX__NNAAMMEE__MMAAXX  generates an error.  The corresponding macro is
              __PPOOSSIIXX__NNOO__TTRRUUNNCC.

       __PPCC__VVDDIISSAABBLLEE
              This returns nonzero if special character processing can be dis‐
              abled, where _f_d or _p_a_t_h must refer to a terminal.

RREETTUURRNN VVAALLUUEE
       The return value of these functions is one of the following:

       •  On error, -1 is returned and _e_r_r_n_o is set to indicate the error (for
          example, EEIINNVVAALL, indicating that _n_a_m_e is invalid).

       •  If _n_a_m_e corresponds to a maximum or minimum limit, and that limit is
          indeterminate, -1 is returned and _e_r_r_n_o is not changed.  (To distin‐
          guish an indeterminate limit from an error, set _e_r_r_n_o to zero before
          the call, and then check whether _e_r_r_n_o is nonzero  when  -1  is  re‐
          turned.)

       •  If  _n_a_m_e  corresponds  to an option, a positive value is returned if
          the option is supported, and -1 is returned if  the  option  is  not
          supported.

       •  Otherwise,  the  current  value  of the option or limit is returned.
          This value will not be more restrictive than the corresponding value
          that was described to the application in  _<_u_n_i_s_t_d_._h_>  or  _<_l_i_m_i_t_s_._h_>
          when the application was compiled.

EERRRROORRSS
       EEAACCCCEESS (ppaatthhccoonnff()) Search permission is denied for one of the directo‐
              ries in the path prefix of _p_a_t_h.

       EEBBAADDFF  (ffppaatthhccoonnff()) _f_d is not a valid file descriptor.

       EEIINNVVAALL _n_a_m_e is invalid.

       EEIINNVVAALL The  implementation does not support an association of _n_a_m_e with
              the specified file.

       EELLOOOOPP  (ppaatthhccoonnff()) Too many symbolic links were encountered while  re‐
              solving _p_a_t_h.

       EENNAAMMEETTOOOOLLOONNGG
              (ppaatthhccoonnff()) _p_a_t_h is too long.

       EENNOOEENNTT (ppaatthhccoonnff())  A  component of _p_a_t_h does not exist, or _p_a_t_h is an
              empty string.

       EENNOOTTDDIIRR
              (ppaatthhccoonnff()) A component used as a directory in _p_a_t_h is  not  in
              fact a directory.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       ffppaatthhccoonnff(), ppaatthhccoonnff() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       POSIX.1‐2008.

HHIISSTTOORRYY
       POSIX.1‐2001.

NNOOTTEESS
       Files with name lengths longer than the value returned for _n_a_m_e equal
       to __PPCC__NNAAMMEE__MMAAXX may exist in the given directory.

       Some returned values may be huge; they are not suitable for allocating
       memory.

SSEEEE AALLSSOO
       ggeettccoonnff(1), ooppeenn(2), ssttaattffss(2), ccoonnffssttrr(3), ssyyssccoonnff(3)

Linux man‐pages (unreleased)      2025‐09‐21                      _f_p_a_t_h_c_o_n_f(3)
