The interface forms the frontend part of the paravirtualized dri‐
vers  used  by  guest  domains  to have a block device interface.
From a guest point of view, is similar to a hard disk, and can be
treated in the very same way regarding partitioning, file systems
creation and usage, and mounting.  By  default,  a  guest  domain
will  assume  that serves as the root file system.  When the host
is the interface is backed by a interface.  In the XenStore,  and
are  identified by (virtual block device) entries.  Specifies the
event channel used by this interface.  Gives the  total  size  of
the  block  device,  its sector size and total number of sectors.
The backend driver associated with this device does  not  support
cache  flushing operation.  This can be problematic for file sys‐
tem operations that require cache sync to avoid data loss or cor‐
ruption.  The driver first appeared in The driver was written by
