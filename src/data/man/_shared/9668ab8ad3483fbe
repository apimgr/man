CTLD(8)                   BSD System Manager's Manual                  CTLD(8)

NNAAMMEE
     ccttlldd — CAM Target Layer / iSCSI target daemon

SSYYNNOOPPSSIISS
     ccttlldd [--dd] [--ff _c_o_n_f_i_g_-_f_i_l_e] [--uu]
     ccttlldd --tt [--ff _c_o_n_f_i_g_-_f_i_l_e] [--uu]

DDEESSCCRRIIPPTTIIOONN
     The ccttlldd daemon is responsible for managing the CAM Target Layer configu‐
     ration, accepting incoming iSCSI connections, performing authentication
     and passing connections to the kernel part of the native iSCSI target.

     Upon startup, the ccttlldd daemon parses the configuration file.  If it en‐
     counters any errors, ccttlldd exits.  It then compares the configuration with
     the kernel list of LUNs managed by previously running ccttlldd instances, re‐
     moves LUNs no longer existing in the configuration file, and creates new
     LUNs as necessary.  After that it listens for the incoming iSCSI connec‐
     tions, performs authentication, and, if successful, passes the connec‐
     tions to the kernel part of CTL iSCSI target, cfiscsi(4), which handles
     it from that point.

     When it receives a SIGHUP signal, the ccttlldd reloads its configuration and
     applies the changes to the kernel.  Changes are applied in a way that
     avoids unnecessary disruptions; for example removing one LUN does not af‐
     fect other LUNs.

     When exiting gracefully, the ccttlldd daemon removes LUNs it managed and
     forcibly disconnects all the clients.  Otherwise - for example, when
     killed with SIGKILL - LUNs stay configured and clients remain connected.

     To perform administrative actions that apply to already connected ses‐
     sions, such as forcing termination, use ctladm(8).

     The following options are available:

     --ff _c_o_n_f_i_g_-_f_i_l_e
                 Specifies the name of the configuration file.  The default is
                 _/_e_t_c_/_c_t_l_._c_o_n_f.

     --dd          Debug mode.  The daemon sends verbose debug output to stan‐
                 dard error, and does not put itself in the background.  The
                 daemon will also not fork and will exit after processing one
                 connection.  This option is only intended for debugging the
                 target.

     --tt          Test the configuration file for validity and exit.

     --uu          Use UCL configuration file format instead of the traditional
                 non-UCL format.

FFIILLEESS
     /etc/ctl.conf      The configuration file for ccttlldd.  The file format and
                        configuration options are described in ctl.conf(5).
     /var/run/ctld.pid  The default location of the ccttlldd PID file.

EEXXIITT SSTTAATTUUSS
     The ccttlldd utility exits 0 on success, and >0 if an error occurs.

SSEEEE AALLSSOO
     cfiscsi(4), ctl(4), ctl.conf(5), ctladm(8), ctlstat(8)

HHIISSTTOORRYY
     The ccttlldd command appeared in FreeBSD 10.0.

AAUUTTHHOORRSS
     The ccttlldd was developed by Edward Tomasz Napierala <_t_r_a_s_z_@_F_r_e_e_B_S_D_._o_r_g> un‐
     der sponsorship from the FreeBSD Foundation.

BSD                             March 31, 2020                             BSD
