The interface allows asynchronous serial lines to be used as net‐
work  interfaces  using  the protocol.  To use the interface, the
administrator must first create the interface and  assign  a  tty
line  to  it.  The interface is created using the subcommand, and
is used to assign a tty line to the interface.  Once  the  inter‐
face  is  attached,  network source and destination addresses and
other parameters are configured via The interface can use Van Ja‐
cobson header compression and filtering.  The following flags  to
control  these  properties  of  a SLIP link: Turn on Van Jacobson
header compression.  Turn off header compression. (default) Don’t
pass through ICMP packets.  Do pass through  ICMP  packets.  (de‐
fault) If a packet with a compressed header is received, automat‐
ically  enable  compression  of outgoing packets. (default) Don’t
auto‐enable compression.  The interface was handed a message with
addresses formatted in an unsuitable address family;  the  packet
was  dropped.   The  device appeared in can only transmit packets
between preconfigured hosts on an asynchronous serial  link.   It
has  no provision for address negotiation, carriage of additional
protocols (e.g.  and  is  not  designed  for  synchronous  serial
links.   This  is  why  has been superseded by the Point‐to‐Point
Protocol which does all of those things, and much more.
