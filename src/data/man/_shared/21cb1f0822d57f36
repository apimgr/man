is  a  program  for managing keytabs.  Supported options: Verbose
output.  can be one of the following: Adds a key to  the  keytab.
Options  that  are  not  specified will be prompted for. This re‐
quires that you know the password or the hex key of the principal
to add; if what you really want is to add a new principal to  the
keytab,  you should consider the command, which talks to the kad‐
min server.  Update one or several keys to new versions.  By  de‐
fault,  use  the  admin  server  for the realm of a keytab entry.
Otherwise it will use the values specified by the options.  If no
principals are given, all the ones in  the  keytab  are  updated.
Copies all the entries from to For each generate a new key for it
(creating  it  if  it doesn’t already exist), and put that key in
the keytab.  If no is specified, the realm to operate on is taken
from the first principal.  List the keys stored  in  the  keytab.
Removes  the specified key or keys. Not specifying a removes keys
with any version number. Not specifying an removes  keys  of  any
type.   Renames  all  entries in the keytab that match the to Re‐
moves all old versions of a key for which there is a  newer  ver‐
sion that is at least (default one week) old.
