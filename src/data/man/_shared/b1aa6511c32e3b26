creates  a database for the specified file.  If no file is speci‐
fied, then is used.  This database is  installed  into  The  file
must  be  in  the correct format (see The options are as follows:
Put the output databases in the named file.   The  databases  are
used  by  the C library netgroup routines (see exits zero on suc‐
cess, non‐zero on failure.  The current netgroup database A  tem‐
porary  file  The current netgroup file Because guarantees not to
install a partial destination file it must build a temporary file
in the same file system and if successful use to install over the
destination file.  If fails it will leave the previous version of
the destination file intact.
