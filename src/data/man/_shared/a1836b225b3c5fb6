The  utility  is  used to create an image for Das U‐Boot, the The
arguments are as follows: Defines the architecture.  This is  re‐
quired  for format images.  Sets the image load address.  This is
an integer between 0 and This is required for all image types ex‐
cept for script, ramdisk, and kernel_noload.   Defines  the  com‐
pression.  The default is Define the entry point.  This is an in‐
teger between 0 and This option is the same as but it inverts the
bytes to convert the 32 bit integer from little to big endian (or
vice  versa).  If or are not set, the entry point defaults to the
image load address Define the entry point.  This  is  an  integer
between  0 and If or are not set, the entry point defaults to the
image load address Defines the output image format type.  The de‐
fault is Display the usage and exit.  Set the magic used for for‐
mat images.  This is an integer between 0 and The default is  De‐
fines  the  image name.  This is required.  Defines the operating
system type.  The default OS name  is  Defines  the  image  type.
This  is required for format images.  Update the header in an ex‐
isting file instead of creating a new one.  The required argument
is the filename of the kernel that is read.  The  required  argu‐
ment  is the filename of the image file that is created.  returns
1 on failure to read the kernel, generate a header, or create the
image.  Before the image is written, the header is  displayed  to
standard out; for example:
 magic:       0x27051956
 time:        Tue Jun  7 15:58:41 2011
 size:        2482203
 load addr:   0x00020000
 entry point: 0x00020000
 data crc:    0x700fdf53
 os:          2 (netbsd)
 arch:        7 (powerpc)
 type:        2 (kernel)
 comp:        1 (gz)
 name:        NetBSD/evbppc 5.99.52 (INSTALL_PMPPC)
 header  crc:  0x94ea96cf The command first appeared in The util‐
ity was originally written by This manual page was written by
