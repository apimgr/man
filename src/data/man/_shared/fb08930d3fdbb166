_f_m_a(3)                     Library Functions Manual                     _f_m_a(3)

NNAAMMEE
       fma, fmaf, fmal - floating‐point multiply and add

LLIIBBRRAARRYY
       Math library (_l_i_b_m, _-_l_m)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<mmaatthh..hh>>

       ddoouubbllee ffmmaa((ddoouubbllee _x,, ddoouubbllee _y,, ddoouubbllee _z));;
       ffllooaatt ffmmaaff((ffllooaatt _x,, ffllooaatt _y,, ffllooaatt _z));;
       lloonngg ddoouubbllee ffmmaall((lloonngg ddoouubbllee _x,, lloonngg ddoouubbllee _y,, lloonngg ddoouubbllee _z));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       ffmmaa(), ffmmaaff(), ffmmaall():
           _ISOC99_SOURCE || _POSIX_C_SOURCE >= 200112L

DDEESSCCRRIIPPTTIIOONN
       These  functions  compute  _x  *  _y  +  _z.  The result is rounded as one
       ternary operation according to the current rounding mode (see ffeennvv(3)).

RREETTUURRNN VVAALLUUEE
       These functions return the value of _x * _y + _z, rounded as  one  ternary
       operation.

       If _x or _y is a NaN, a NaN is returned.

       If  _x times _y is an exact infinity, and _z is an infinity with the oppo‐
       site sign, a domain error occurs, and a NaN is returned.

       If one of _x or _y is an infinity, the other is 0, and _z is not a NaN,  a
       domain error occurs, and a NaN is returned.

       If  one of _x or _y is an infinity, and the other is 0, and _z is a NaN, a
       domain error occurs, and a NaN is returned.

       If _x times _y is not an infinity times zero (or vice versa), and _z is  a
       NaN, a NaN is returned.

       If the result overflows, a range error occurs, and an infinity with the
       correct sign is returned.

       If  the  result underflows, a range error occurs, and a signed 0 is re‐
       turned.

EERRRROORRSS
       See mmaatthh__eerrrroorr(7) for information on how to determine whether an  error
       has occurred when calling these functions.

       The following errors can occur:

       Domain error: _x _* _y _+ _z or _x _* _y is invalid.
              An invalid floating‐point exception (FFEE__IINNVVAALLIIDD) is raised.

       Range error: result overflow
              An overflow floating‐point exception (FFEE__OOVVEERRFFLLOOWW) is raised.

       Range error: result underflow
              An underflow floating‐point exception (FFEE__UUNNDDEERRFFLLOOWW) is raised.

       These functions do not set _e_r_r_n_o.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{ ffmmaa(),
       ffmmaaff(), ffmmaall() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       C11, POSIX.1‐2008.

HHIISSTTOORRYY
       glibc 2.1.  C99, POSIX.1‐2001.

SSEEEE AALLSSOO
       rreemmaaiinnddeerr(3), rreemmqquuoo(3)

Linux man‐pages (unreleased)      2025‐09‐06                            _f_m_a(3)
