RESOLVD(8)                BSD System Manager's Manual               RESOLVD(8)

NNAAMMEE
     rreessoollvvdd — a daemon to handle nameserver configuration

SSYYNNOOPPSSIISS
     rreessoollvvdd [--ddvv]

DDEESSCCRRIIPPTTIIOONN
     rreessoollvvdd handles the contents of _/_e_t_c_/_r_e_s_o_l_v_._c_o_n_f, which contains details
     of the system's DNS nameservers, and is read by the resolver routines in
     the C library.  Nameservers are learned from various sources, incorpo‐
     rated in a priority order, then user-edited information found in the pre-
     existing file is re-appended.

     rreessoollvvdd checks whether unwind(8) is running and, if so, places a name‐
     server line at the top to cause local resolution:

           nameserver 127.0.0.1

     rreessoollvvdd also monitors the routing socket for proposals learned by
     dhcpleased(8), iked(8), slaacd(8), or network devices which natively
     learn DNS information such as sppp(4) or umb(4).  Proposals can be sent
     manually using the route(8) nnaammeesseerrvveerr command.  The proposals are added
     in priority order, but commented out if unwind(8) is running.

     After that, rreessoollvvdd appends all user-edited lines found in the file.

     rreessoollvvdd also notices if the _/_e_t_c_/_r_e_s_o_l_v_._c_o_n_f file is edited, and will re‐
     write the file, re-blending the various pieces of information.  It will
     also create _/_e_t_c_/_r_e_s_o_l_v_._c_o_n_f if it does not exist or if it is empty.

     The options are as follows:

     --dd      Do not daemonize.  If this option is specified, rreessoollvvdd will run
             in the foreground and log to _s_t_d_e_r_r.

     --vv      Produce more verbose output.

SSEEEE AALLSSOO
     resolv.conf(5), dhcpleased(8), route(8), slaacd(8), unwind(8)

HHIISSTTOORRYY
     The rreessoollvvdd program first appeared in OpenBSD 6.9.

BSD                            February 21, 2023                           BSD
