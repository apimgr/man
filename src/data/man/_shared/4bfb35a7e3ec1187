...   ...   ...   The  utility  sends a signal to the process(es)
specified by the pid operand(s).  Only the  super‐user  may  send
signals to other users’ processes.  The options are as follows: A
symbolic  signal name specifying the signal to be sent instead of
the default Display the name of the signal corresponding  to  may
be  the exit status of a command killed by a signal (see the spe‐
cial parameter or a signal number.  If no operand is given,  dis‐
play  the names of all the signals.  In if the variable is set in
the environment, this uses the POSIX specified format,  otherwise
a  slightly more pleasing layout is used.  A symbolic signal name
specifying the signal to be sent instead of the  default  A  non‐
negative  decimal  integer,  specifying the signal to be sent in‐
stead of the default The following have special meanings: If  su‐
peruser,  broadcast the signal to all processes; otherwise broad‐
cast to all processes belonging to the user.  Broadcast the  sig‐
nal  to  all  processes in the current process group belonging to
the user.  Any other negative is interpreted as the negative of a
process group identifier, and the signal is sent to  all  members
of  that group.  Note that while not required by this implementa‐
tion, if the first operand is negative, it should be preceded  by
the  end  of options indicator, to avoid the being treated as yet
more options.  That is always required if no specific  signal  is
specified and the first is negative, or that pid would be treated
as the Some of the more commonly used signals: 0 (does not affect
the  process; can be used to test whether the process exists) HUP
(hang up) INT (interrupt) QUIT (quit)  ABRT  (abort)  KILL  (non‐
catchable,  non‐ignorable kill) ALRM (alarm clock) TERM (software
termination signal) STOP  (non‐catchable,  non‐ignorable,  causes
process  to temporarily stop, after which it can later be resumed
using CONT (stopped processes resume execution) is a built‐in  to
most  shells,  including and it allows job specifiers of the form
‘‘%...’’ as arguments so process id’s are not as  often  used  as
arguments.  See or the man page for the shell in use for details.
The  utility is expected to be compatible.  A command appeared in
in section 8 of the manual.  The original BSD description was:
