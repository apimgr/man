_p_u_t_w_c_h_a_r(3)                Library Functions Manual                _p_u_t_w_c_h_a_r(3)

NNAAMMEE
       putwchar - write a wide character to standard output

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<wwcchhaarr..hh>>

       wwiinntt__tt ppuuttwwcchhaarr((wwcchhaarr__tt _w_c));;

DDEESSCCRRIIPPTTIIOONN
       The  ppuuttwwcchhaarr()  function  is  the  wide‐character  equivalent  of  the
       ppuuttcchhaarr(3) function.  It writes the wide character _w_c  to  _s_t_d_o_u_t.   If
       _f_e_r_r_o_r_(_s_t_d_o_u_t_) becomes true, it returns WWEEOOFF.  If a wide character con‐
       version error occurs, it sets _e_r_r_n_o to EEIILLSSEEQQ and returns WWEEOOFF.  Other‐
       wise, it returns _w_c.

       For a nonlocking counterpart, see uunnlloocckkeedd__ssttddiioo(3).

RREETTUURRNN VVAALLUUEE
       The ppuuttwwcchhaarr() function returns _w_c if no error occurred, or WWEEOOFF to in‐
       dicate an error.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       ppuuttwwcchhaarr() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       C11, POSIX.1‐2008.

HHIISSTTOORRYY
       POSIX.1‐2001, C99.

NNOOTTEESS
       The behavior of ppuuttwwcchhaarr() depends on the LLCC__CCTTYYPPEE category of the
       current locale.

       It is reasonable to expect that ppuuttwwcchhaarr() will actually write the
       multibyte sequence corresponding to the wide character _w_c.

SSEEEE AALLSSOO
       ffppuuttwwcc(3), uunnlloocckkeedd__ssttddiioo(3)

Linux man‐pages (unreleased)      2025‐05‐17                       _p_u_t_w_c_h_a_r(3)
