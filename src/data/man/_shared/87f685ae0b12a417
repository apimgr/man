The  file  specifies  the behavior of which creates the database.
The file contains a list of newline separated  records,  each  of
which is composed of a keyword and arguments, which are separated
by  white  space.   Arguments  with embedded shell metacharacters
must be quoted in style.  Lines beginning  with  are  treated  as
comments  and  ignored.   However, a in the middle of a line does
not start a comment.  The configuration options are  as  follows:
Specify the location of the database to be created.  Default: Ig‐
nore  files  or  directories.  When building the database, do not
descend into files or directories which match one of  the  speci‐
fied  patterns.   The matched files or directories are not stored
to the database.  Default: Not specified.  Ignore contents of di‐
rectories.  When building the database, do not descend into files
or directories which match one of the  specified  patterns.   The
matched  files  or directories themselves are stored to the data‐
base.  Default: Not  specified.   Ignore  file  system  by  type,
adding  to  the default list.  When building the database, do not
descend into file systems which are of the  specified  The  mount
points  are not stored to the database.  If a is prepended to the
meaning is negated, that is, ignore file  systems  which  do  not
have  the  type.  As a special case, if is specified for the list
is cleared and all file systems are traversed.  is used as an ar‐
gument to The command can be used to find out the types  of  file
systems    that    are    available   on   the   system:   sysctl
vfs.generic.fstypes Default: !local cd9660  fdesc  kernfs  procfs
Specify  base  directories  to  be put in the database.  Default:
Specify the working directory of locate.updatedb, in which a tem‐
porary file is placed.  The temporary  file  is  a  list  of  all
files,  and  you should specify a directory that has enough space
to hold it.  Default: Refer to for the details  of  (see  expres‐
sion)  and (see expression).  The file resides in The file format
first appeared in
