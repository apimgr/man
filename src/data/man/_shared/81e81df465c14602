command enables kernel trace logging for the specified processes.
Kernel  trace  data  is  logged to the file The kernel operations
that are traced include system calls, namei translations,  signal
processing,  and Once tracing is enabled on a process, trace data
will be logged until either the process exits or the trace  point
is  cleared.   A  traced process can generate enormous amounts of
log data quickly.  It is strongly suggested that  users  memorize
how to disable tracing before attempting to trace a process.  The
following  command  is  sufficient to disable tracing on all user
owned processes, and, if executed by  root,  all  processes:  The
trace file is not human readable; use to decode it.  is function‐
ally  the same as except that trace output is printed on standard
output or to the file specified with the option.   is  useful  to
see  the  kernel  operations interleaved with the program output.
The options are as follows: Append to the trace file  instead  of
truncating it.  Disable tracing on all user owned processes, and,
if  executed  by  root,  all  processes in the system.  Clear the
trace points associated with the  specified  file  or  processes.
Descendants;  perform  the  operation for all current children of
the designated processes.  If an emulation of a  process  is  un‐
known,  interpret  system  call maps assuming the named emulation
instead of default "netbsd".  Log trace  records  to  instead  of
Read  the  trace  records from and print them in a human readable
format  to  standard  out.   Enable  (disable)  tracing  on   all
processes in the process group (only one flag is permitted).  In‐
herit;  pass the trace flags to all future children of the desig‐
nated processes.  Poll the trace file for new data and  print  it
to  standard  out.  Only for use together with the option.  Print
at most bytes of data.  This is used for pointer type  arguments,
e.g.,  strings.   The  data  will be escaped in C‐style unless is
specified when it will be output in hex and ascii.  Stop  tracing
if  attempts to write to the trace file would block.  This option
always affects and only affects when writing to If this  flag  is
not  set,  then  the traced program will block until it can write
more data to the trace file descriptor.   Log  trace  records  to
Without  this  option  will  print its output in a human readable
format to standard out.  Enable (disable) tracing  on  the  indi‐
cated  process id (only one flag is permitted).  Display relative
time stamps to output.  Write to the trace file with synchronized
I/O.  Same as the option, but use  absolute  timestamps  instead.
The  string  argument represents the kernel trace points, one per
letter.  The following table equates the letters with the  trace‐
points:  trace  all tracepoints trace exec arguments trace system
calls trace emulation changes trace open file  descriptors  after
exec  trace  trace  namei  translations trace MIB access (sysctl)
trace signal processing trace user data  trace  exec  environment
trace  context switches trace the default set of trace points (c,
e, i, n, s, u) do not trace following trace points Determines the
of the file generated.  Version 0 is the compatible  ktrace  for‐
mat,  and version 1 is the new format with lwp IDs and nanosecond
(instead of microsecond) timestamps.  Execute with the  specified
trace  flags.   The  and options are mutually exclusive.  The and
options are also mutually exclusive.  # trace all  kernel  opera‐
tions of process id 34 # trace all kernel operations of processes
in process group 15 and # pass the trace flags to all current and
future  children  #  disable  all tracing of process 65 # disable
tracing signals on process 70 and all current children  #  enable
tracing of on process 67 # run the command "w", tracing only sys‐
tem calls # disable all tracing to the file "tracedata" # disable
tracing of all processes owned by the user # run the command "w",
displaying  to  standard  output  #  trace process 42 and log the
records to "ktruss.out" # poll ktruss.out for  available  records
and print them The command appeared in The command appeared in
