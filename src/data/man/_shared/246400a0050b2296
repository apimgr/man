UNAME(3)                 BSD Library Functions Manual                 UNAME(3)

NNAAMMEE
     uunnaammee — get system identification

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//uuttssnnaammee..hh>>

     _i_n_t
     uunnaammee(_s_t_r_u_c_t _u_t_s_n_a_m_e _*_n_a_m_e);

DDEESSCCRRIIPPTTIIOONN
     The uunnaammee() function stores NUL-terminated strings of information identi‐
     fying the current system into the structure referenced by _n_a_m_e.

     The _u_t_s_n_a_m_e structure is defined in the <_s_y_s_/_u_t_s_n_a_m_e_._h> header file, and
     contains the following members:

           sysname       Name of the operating system implementation.

           nodename      Network name of this machine.

           release       Release level of the operating system.

           version       Version level of the operating system.

           machine       Machine hardware platform.

RREETTUURRNN VVAALLUUEESS
     The uunnaammee() function returns the value 0 if successful; otherwise the
     value -1 is returned and the global variable _e_r_r_n_o is set to indicate the
     error.

EENNVVIIRROONNMMEENNTT
     UNAME_s  If the environment variable UNAME_s is set, it will override the
              _s_y_s_n_a_m_e member.

     UNAME_r  If the environment variable UNAME_r is set, it will override the
              _r_e_l_e_a_s_e member.

     UNAME_v  If the environment variable UNAME_v is set, it will override the
              _v_e_r_s_i_o_n member.

     UNAME_m  If the environment variable UNAME_m is set, it will override the
              _m_a_c_h_i_n_e member.

EERRRROORRSS
     The uunnaammee() function may fail and set _e_r_r_n_o for any of the errors speci‐
     fied for the library functions sysctl(3).

SSEEEE AALLSSOO
     uname(1), sysctl(3)

SSTTAANNDDAARRDDSS
     The uunnaammee() function conforms to IEEE Std 1003.1-1988 (“POSIX.1”).

HHIISSTTOORRYY
     The uunnaammee() function first appeared in 4.4BSD.

BSD                            December 2, 2005                            BSD
