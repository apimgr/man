The  function,  for  supported Filesystems, causes a snapshot  of
the Filesystem to be created. A snapshot is a read only  copy  of
the  filesystem  frozen  at  a  point in time.  The Filesystem is
identified by the parameter which should be a file descriptor as‐
sociated with the root directory of the filesystem for which  the
snapshot is to be created.  can be any valid name for a component
name  (except  . and ..).  The function causes the named snapshot
to be deleted and the function causes the named  snapshot  to  be
renamed  to the name Available snapshots along with their attrib‐
utes can be listed by calling which is to be used in exactly  the
same way as Snapshots may be useful for backing up the Filesystem
and to restore the Filesystem to a previous state.  Snapshots are
expected  to consume no additional storage on creation but  might
consume additional storage as the active Filesystem is  modified.
Similarly  deletion of files on the active filesystem may not re‐
sult in the storage being available if the snapshot contains  the
file.  Additionally, the underlying Filesystem may impose a limit
on the number of snapshots that  can  be  taken.  For  supporting
Filesystems, a snapshot may be used as a source for a mount. This
can  be  done  by the function. The snapshot will be mounted read
only. When a snapshot is mounted, it cannot be deleted but it can
be renamed.  To revert the filesystem to a previous snapshot, the
can be used. It should be noted that reverting a filesystem to  a
snapshot  is  a destructive operation and causes all changes made
to the filesystem (including snapshots created after the snapshot
being reverted to) to be lost.  All snapshot  functions   require
superuser  privileges and also require an additional entitlement.
The parameter specifies the options that can be passed. only  op‐
tions  are currently defined: Can’t exec from filesystem.  Setuid
bits are not honored on this filesystem.  Don’t interpret special
files.  File system is not appropriate path to user data.  Treats
all files on the disk as though they are  owned  by  the  current
user,  no  matter  who  actually owns them.  Don’t follow symlink
when resolving mount point.  Upon successful  completion,  ,  and
returns  0. Otherwise, a value of ‐1 is returned and errno is set
to indicate the error.  returns the number  of  entries  success‐
fully  read.  A return value of 0 indicates there are no more en‐
tries.  Otherwise, a value of ‐1 is returned and errno is set  to
indicate the error. Return values are the same as Not all volumes
support snapshots. A volume can be tested for snapshot support by
using to get the volume capabilities attribute ATTR_VOL_CAPABILI‐
TIES,  and  then  testing the VOL_CAP_INT_SNAPSHOT flag.  The , ,
and function will fail if: Read permissions are  denied  for  the
caller  on the filesystem The underlying filesystem does not sup‐
port this call.  The value of the parameter is invalid.  There is
no free space remaining on the file system containing  the  file.
The  limit  for  the maximum number of snapshots for a filesystem
has been reached.  An I/O error occurred while  reading  from  or
writing  to  the  file system.  The calling process does not have
appropriate privileges.  The requested operation requires modifi‐
cations in a read‐only file system.  The length of a component of
a pathname is longer than {NAME_MAX}.  dirfd is not a valid  file
descriptor.  dirfd is a file descriptor associated with a non‐di‐
rectory  file.   In  addition, the or functions may fail with the
following errors The The named snapshot to be created already ex‐
ists or the new name already exists for the  snapshot  being  re‐
named.  or functions may fail with the following errors The named
snapshot  does not exist.  function may fail with The named snap‐
shot is currently mounted.  The , , , , and  function  calls  ap‐
peared in macOS version 10.13
