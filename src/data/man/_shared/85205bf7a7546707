The  driver provides userland applications access to devices oth‐
erwise not found as nodes.  The device node operates by  delegat‐
ing ioctl calls to a requested device driver.  Only drivers which
have  registered  with the device can be accessed via this inter‐
face.  The following device drivers register with for volume man‐
agement: Areca Technology Corporation SATA RAID controller  Soft‐
ware  BIOS  RAID  Compaq RAID array controller Compaq Smart ARRAY
5/6 SAS/SATA/SCSI RAID controller LSI Logic & Dell  MegaRAID  SAS
RAID controller LSI Logic MegaRAID SAS Fusion RAID controller LSI
Logic Fusion‐MPT Message Passing Interface II LSI Fusion‐MPT RAID
controller  The following ioctl calls apply to the device: Locate
a named device and give back a cookie to the application for sub‐
sequent ioctl calls.  The cookie is used to tunnel further ioctls
to the right device.  Retrieve number  of  volumes  and  physical
disks  for  a specific device.  Retrieve detailed information for
the specified physical disk.  Information  returned  can  include
status,  size,  channel, target, lun, vendor name, serial number,
and processor device (ses).  Is just the same as but doesn’t  re‐
quire  the  disks  to  be  in volume sets, so this applies to any
physical disk connected to  the  controller.   Note:  this  ioctl
might  not be supported on all hardware.  It is a extension of It
is supported by and It is also supported by but  handled  exactly
the  same as Retrieve detailed information for the specified vol‐
ume.  Information returned can include status, size, RAID  level,
number  of  disks, device name association (sd?) and vendor name.
Control the alarm beeper on the device.   Supported  states  are:
disable  alarm,  enable  alarm,  silence  alarm,  status and test
alarm.  Note: These options might not be supported on  all  hard‐
ware.  It is supported by and Blink an LED of the specified phys‐
ical  disk.   Supported  blink states are: blink LED, unblink LED
and blink alarm LED.  Note: This option is only supported if  the
disk  is governed by and the hardware supports hardware blinking.
It is supported by and Alter  the  state  of  specified  physical
disk.   Supported states are: create/remove hot‐spare, create/re‐
move pass through disk, start/stop consistency check in a volume,
online disk and offline disk, and a manual rebuild kick‐off  des‐
ignation.   Note:  These  options  might  not be supported on all
hardware.  Some of these options are supported by and Online, of‐
fline and hotspare designations are supported by and plus  a  re‐
build designation is supported by all four of these state options
are  the  original states from the other options, including hots‐
pare unmarking, being extensions of Hotspare,  pass  through  and
consistency check options are supported by For operations in vol‐
ume  sets.  It’s able to create and remove a volume set in a sup‐
ported RAID controller.  Note: this ioctl might not be  supported
on  all  hardware.   It is a extension of and is supported by The
utility can be used to perform the above controls from the  user‐
land.   Additionally,  since volume status is normally duplicated
into sensors of type, it is also available  through  and  can  be
monitored  with  and  ioctl tunnel device powerd script for drive
sensors The driver first appeared in and The driver  was  written
by The API was written by and was extended even more for by
