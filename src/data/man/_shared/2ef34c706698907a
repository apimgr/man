SND_DUMMY(4)             BSD Kernel Interfaces Manual             SND_DUMMY(4)

NNAAMMEE
     ssnndd__dduummmmyy — Dummy audio driver

SSYYNNOOPPSSIISS
     To load the driver at boot time, place the following line in
     loader.conf(5):

           snd_dummy_load="YES"

DDEESSCCRRIIPPTTIIOONN
     The ssnndd__dduummmmyy driver implements a virtual testing device, meaning it does
     not correspond to a physical sound card.  It is intended for testing, so
     that test programs do not need to rely on hardware being present in the
     machine in order to run.

     The driver attaches as a regular PCM device, with two channels (one play‐
     back and one recording), as well as a mixer.

     Playback works by discarding all input, and recording by returning si‐
     lence (zeros).

SSEEEE AALLSSOO
     sound(4), loader.conf(5), loader(8)

AAUUTTHHOORRSS
     The ssnndd__dduummmmyy driver was implemented by Christos Margiolis
     <_c_h_r_i_s_t_o_s_@_F_r_e_e_B_S_D_._o_r_g> under sponsorship from the FreeBSD Foundation.

CCAAVVEEAATTSS
     Because the driver automatically attaches once the module is loaded, it
     can only be attached once.

BSD                              July 15, 2024                             BSD
