The  utility  installs and removes disk bootstrap software into a
file system.  can install into or disable an  existing  bootstrap
in  Generally,  disk  bootstrap software consists of two parts: a
bootstrap program usually written into the disklabel area of  the
file system by and a bootstrap program that usually resides as an
ordinary  file  in  the  file  system.  When booting, the primary
bootstrap program is loaded and invoked by the machine’s PROM  or
BIOS.   After  receiving  control of the system it loads and runs
the secondary bootstrap program, which in turn loads and runs the
kernel.  The secondary bootstrap may allow control  over  various
boot  parameters  passed  to  the  kernel.  Perform the following
steps to make a file system bootable: Copy  the  secondary  boot‐
strap  (usually  or to the root directory of the target file sys‐
tem.  Use to install the primary bootstrap program (usually  into
The  following  platforms do not require this step if the primary
bootstrap already exists and the secondary bootstrap file is just
being updated: and The following platform does  not  require  the
first  step  since  a  single bootstrap file is used.  The single
bootstrap is installed like the primary bootstrap on other  plat‐
forms: On some architectures the options of an existing installed
bootstrap,  or  those  of  a  bootstrap file can be changed.  In‐
stalling a new primary bootstrap will reset those options to  de‐
fault  values.   Some  platforms, typically embedded system plat‐
forms, are umbrella platforms that support many  different  indi‐
vidual  board  types,  each with their own boot loader binary and
installation procedure requirements.  On these platforms, it  may
be necessary to provide specific board type information to Infor‐
mation about known boards and their requirements is loaded from a
database at run‐time.  Sometimes these platforms also require the
use  of  3rd‐party boot loader software, such as To support these
platforms, scans known locations for these 3rd‐party boot  loader
packages for database overlays that contain additional board‐spe‐
cific  boot  loader installation information in a file called The
following platforms have this requirement and utilize this  data‐
base  overlay feature: On platforms that boot with UEFI, there is
a single boot loader in the EFI system partition at where is  the
UEFI name for the architecture, such as or is not needed for UEFI
boot.   However,  it may be used on a platform that supports UEFI
boot to make a hybrid image that can be booted with UEFI boot  or
with,  e.g.,  BIOS  or  U‐Boot.  Hybrid images may also require a
special GPT for BIOS boot; see The options and  arguments  recog‐
nized  by  are  as  follows:  When hard‐coding the blocks of into
start from block instead of trying to determine the block numbers
occupied by by examining If this option is supplied, should refer
to an actual secondary bootstrap (rather than the  file  name  of
the  one  present in so that its size can be determined.  Install
at block number instead of the default location for  the  machine
and  file system type.  Clear (remove) any existing bootstrap in‐
stead of installing one.  Edit the options of an  existing  boot‐
strap.   This  can  be used to change the options in bootxx_xxxfs
files, raw disk partitions, and the file.  With and without  show
the  current  options.  Forces to ignore some errors.  Use as the
target machine type.  The default machine is determined from  and
then  The  following  machines  are currently supported by Do not
write to Machine specific options,  comma  separated.   Supported
options  are  (with the machines for they are valid in brackets):
Recalculate and restore the Alpha checksum.  This is the  default
for  Append  to  the  end of which must be a regular file in this
case.  Specify the board type used to determine the correct  boot
loader  image  and  installation procedure.  If omitted, will at‐
tempt to guess the board type based on system information if  run
natively.   (Don’t) read a file.  Modify the default boot command
line.  Set the console device, <console name> must be one of: pc,
com0, com1, com2, com3, com0kbd,  com1kbd,  com2kbd,  com3kbd  or
auto.   Attempt  to  determine the board type from information in
the device tree blob file at If both and options  are  specified,
takes precendence.  Set the IO address to be used for the console
serial  port.  Defaults to the IO address used by the system BIOS
for the specified port.  Set a  boot  time  keyboard  translation
map.  Each character in <keymap> will be replaced by the one fol‐
lowing  it.  For example, an argument of would swap the lowercase
letters and Some boards require a different  boot  loader  binary
and/or  installation  procedure  depending  on what type of media
will be used to boot the system.  For such boards, this option is
required, and omitting it will display a usage message that lists
the valid media types for the board.  For boards that do not  re‐
quire special media handling, this option is not allowed.  Common
values:  sdmmc, emmc, usb.  (Don’t) load kernel modules.  Set the
password which must be entered before the boot menu  can  be  ac‐
cessed.   Reset  the video before booting.  Set the baud rate for
the serial console.  If a value of zero is  specified,  then  the
current  baud  rate  (set by the BIOS) will be used.  Recalculate
and restore  the  Sun  and  compatible  checksum.   The  existing
disklabel  should use no more than 4 partitions.  Set the timeout
before the automatic boot begins to the given number of  seconds.
Use  as  the type of The default operation is to attempt to auto‐
detect this setting.  The following file system  types  are  cur‐
rently  supported  by  Fast File System.  Mirrored RAIDframe File
System.  image.  Note: if a platform needs to hard‐code the block
offset of the secondary bootstrap, it cannot be searched  for  on
this file system type, and must be provided with is a colon‐sepa‐
rated list of search paths to scan for packages with installation
overlays.   If  multiple  overlays are found, overlays from paths
closer to the front of the list take precedence.  If  not  speci‐
fied, environment variable is used if defined; otherwise, the de‐
fault  path is This option is only used on platforms that support
using Verbose operation.  The path name of  the  device  or  file
system  image  that is to operate on.  It is not necessary for to
be a currently mounted file system.  The path name  of  the  boot
block  to  install.  The path name must refer to a file in a file
system that is currently mounted.  The  path  name  of  the  boot
block,  relative  to the root of the file system in the device or
image specified by the argument.  Note that this may refer  to  a
file  in a file system that is not mounted.  Most systems require
to be in the directory of the file system, so the leading is  not
necessary  on Only certain combinations of platform and file sys‐
tem type require that the name of the secondary bootstrap is sup‐
plied as so that information such as the disk block numbers occu‐
pied by the secondary bootstrap can  be  stored  in  the  primary
bootstrap.  These are: exits 0 on success, and >0 if an error oc‐
curs.   uses  the  following environment variables: A colon‐sepa‐
rated list of search paths to scan for packages with installation
overlays.  If multiple overlays are found,  overlays  from  paths
closer  to  the front of the list take precedence.  If not speci‐
fied, the default path is This environment variable is only  used
on  platforms that support using Default value for overriding the
result from Most ports will contain variations of  the  following
files:  Primary bootstrap for file system type Installed into the
bootstrap area of the file system by Primary bootstrap for MS‐DOS
file systems.  This differs from in that it doesn’t  require  the
filesystem to have been initialised with any It also uses the in‐
formation in the to get the media and filesystem properties.  The
field of the BPB must be the offset of the partition in the disk.
This  can  be  set  using the option.  Primary bootstrap for file
systems (the file system prior to Use to confirm the file  system
format  is  Primary  bootstrap for file systems (the default file
system for some platforms as of Use to confirm  the  file  system
format  is Primary bootstrap for file systems.  Primary bootstrap
for file systems (the default LFS  version).   Primary  bootstrap
for  MS‐DOS  file  systems.   Primary  bootstrap for boot images.
This is used by various install media.  Secondary  bootstrap  for
machine type This should be installed into the file system before
is  run.   Synonym  for Installed copy of secondary bootstrap for
machine type Installed copy of secondary bootstrap.  Searched for
by the primary bootstrap if is not found.  The platform covers  a
wide  variety  of  board types, many of which use Running with no
options will display a list of known boards.  Using  the  verbose
option will also display information about which package needs to
be  installed  to support that board, and if the required package
is installed, the path at which it is located.  The default loca‐
tion scanned for packages with installation overlays.  Base board
database, used to provide information about which package is  re‐
quired for a given board.  The bootstrap files currently only ap‐
ply  to  the  SBMIPS  kernels for the SiByte/Broadcom BCM1250 and
BCM1480 CPUs.  secondary bootstrap for and SBMIPS  primary  boot‐
strap  for  ISO  9660  file system.  SBMIPS primary bootstrap for
FFSv1 and FFSv2 file system.  SBMIPS primary bootstrap for  LFSv1
and  LFSv2  file  system.   SBMIPS  primary bootstrap for network
root.  Note that does not  currently  support  evbmips  directly.
primary  bootstrap  for  and  primary bootstrap for ISO 9660 file
system.  Synonym for  primary  bootstrap.   secondary  bootstrap.
Installed copy of secondary bootstrap.  bootstrap.  primary boot‐
strap.   secondary  bootstrap.  Installed copy of secondary boot‐
strap.  Verbosely install the Berkeley Fast File  System  primary
bootstrap  on  to disk Note: the partition (c on some ports, d on
others) is used here, since the a partition probably  is  already
opened (mounted as so would not be able to access it.  Remove the
primary bootstrap from disk Modify the command line to change the
default  from  "netbsd  ‐ASn2"  to  "netbsd ‐S": Install the boot
loader for a Pinebook into an image that will be  written  to  an
SDMMC card: Install / update the boot loader for the current run‐
ning  system  on  the eMMC device and display verbose information
about the procedure: Install a specific package for a  BeagleBone
Black  into  an  image that will be written to an SDMMC card: In‐
stall the System V Boot File System primary bootstrap on to  disk
with  the secondary bootstrap already present in the SysVBFS par‐
tition on the disk: On the p partition is the (i.e., raw)  parti‐
tion.   Install  new boot blocks on an existing mounted root file
system on setting the timeout to five seconds,  after  installing
an  MBR  bootcode  and copying a new secondary bootstrap: See and
for more details.  Pre systems used file systems on  these  plat‐
forms;  double check with to be sure to use the correct secondary
bootstrap.  Create a bootable CD‐ROM with an ISO 9660 file system
for an i386 system with  a  serial  console:  Create  a  bootable
floppy  disk  with an FFSv1 file system for a small custom kernel
(note: bigger kernels needing multiple disks are handled with the
ustarfs file system): Ignore the warnings that displays;  it  can
not  write a disklabel, which is not a problem for a floppy disk.
Create a bootable FAT file system on which should have  the  same
offset  and  size  as  a FAT primary partition in the Master Boot
Record (MBR): The is to reserve space for the primary  bootstrap.
will  display  an such as or the MBR partition type of the appro‐
priate primary partition should be changed to this  value.   Make
the  existing  FAT16 filesystem on bootable.  This can be used to
make USB memory bootable provided it has  512  byte  sectors  and
that  the manufacturer correctly initialised the file system.  It
may also be necessary to use to make the device itself  bootable.
Switch  the  existing installed bootstrap to use a serial console
without reinstalling or altering other options such  as  timeout.
Note  the utility is only required for macppc machines with Open‐
Firmware version 2 to boot.  OpenFirmware  3  cannot  load  boot‐
blocks  specified in the Apple partition map.  Install the Berke‐
ley Fast File System primary bootstrap on to disk  The  secondary
bootstrap  is  located  in The primary bootstrap requires the raw
for the secondary bootstrap, not which  is  used  for  the  Open‐
Firmware  to  load kernels.  Install the bootstrap on to disk In‐
stall the Berkeley Fast File System primary bootstrap on to  disk
requires  that  this  file  system starts at block 0 of the disk.
Install the ISO 9660 primary bootstrap in the file  Make  an  ISO
9660  filesystem  in  the  file  and install the ISO 9660 primary
bootstrap in the filesystem, where the source directory  for  the
ISO  9660 filesystem contains a kernel, the primary bootstrap and
the secondary bootstrap Install the  Berkeley  Fast  File  System
primary bootstrap on to disk with the secondary bootstrap already
present:  Install  the primary bootstrap on to disk The secondary
bootstrap is located in Install the  Berkeley  Fast  File  System
primary bootstrap on to disk with the secondary bootstrap already
present:  This implementation of appeared in The machine indepen‐
dent portion of this implementation of was written by The follow‐
ing people contributed to the  various  machine  dependent  back‐
ends: (pmax), (alpha), (sun2, sun3), (sparc64), (alpha), (amiga),
(sparc), (i386), (next68k), (macppc), (vax), (news68k, newsmips),
and (ews4800mips).  There are not currently primary bootstraps to
support  all  file  systems  types which are capable of being the
root file system.  If a disk has been converted from  to  without
the  contents  of the disk erased, then the original installation
may be auto‐detected instead of the installation.  In this  case,
the  option  must be provided.  The primary bootstrap program can
only load the  secondary  bootstrap  program  from  file  systems
starting  at  the  beginning  (block 0) of disks.  Similarly, the
secondary bootstrap program can only load kernels from file  sys‐
tems  starting  at  the  beginning of disks.  The size of primary
bootstrap programs is restricted to 7.5KB, even though some  file
systems  (e.g.,  ISO  9660)  are able to accommodate larger ones.
The disk must have a boot partition  large  enough  to  hold  the
bootstrap  code.   Currently  the primary bootstrap must be a LIF
format file.  The bootstrap must be installed  in  the  partition
that  starts at the beginning of the mbr partition.  If that is a
valid filesystem and contains the program then it will be used as
the root filesystem, otherwise the partition will be booted.  The
size of primary bootstrap programs is  restricted  to  8KB,  even
though some file systems (e.g., ISO 9660) are able to accommodate
larger  ones.  Due to restrictions in and the secondary bootstrap
implementation, file systems where kernels exist  must  start  at
the beginning of disks.  Currently, doesn’t recognize an existing
Apple  partition map on the disk and always writes a faked map to
make disks bootable.  The bootstrap program  can’t  load  kernels
from partitions.  The size of bootstrap programs is restricted to
the  free  space  before  the file system at the beginning of the
disk minus 8KB.  The secondary bootstrap program  can  only  load
kernels  from  file  systems  starting at the beginning of disks.
The size of primary bootstrap programs is  restricted  to  7.5KB,
even though some file systems (e.g., ISO 9660) are able to accom‐
modate larger ones.  The and secondary bootstrap program can only
load  kernels  from  file  systems  starting  at the beginning of
disks.  The secondary bootstrap program  can  only  load  kernels
from  file  systems starting at the beginning of disks.  The size
of primary bootstrap programs is restricted to 7.5KB, even though
some file systems (e.g., ISO 9660) are able to accommodate larger
ones.
