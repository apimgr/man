VALE(4)                  BSD Kernel Interfaces Manual                  VALE(4)

NNAAMMEE
     vvaallee — a very fast Virtual Local Ethernet using the netmap API

SSYYNNOOPPSSIISS
     ddeevviiccee nneettmmaapp

DDEESSCCRRIIPPTTIIOONN
     vvaallee is a feature of the netmap(4) module that implements multiple Vir‐
     tual switches that can be used to interconnect netmap clients, including
     traffic sources and sinks, packet forwarders, userspace firewalls, and so
     on.

     vvaallee is implemented completely in software, and is extremely fast.  On a
     modern machine it can move almost 20 Million packets per second (Mpps)
     per core with small frames, and about 70 Gbit/s with 1500 byte frames.

OOPPEERRAATTIIOONN
     vvaallee dynamically creates switches and ports as clients connect to it us‐
     ing the netmap(4) API.

     vvaallee ports are named _v_a_l_e_S_S_S_:_P_P_P where _v_a_l_e is the prefix indicating a
     VALE switch rather than a standard interface, _S_S_S indicates a specific
     switch (the colon is a separator), and _P_P_P indicates a port within the
     switch.  Both SSS and PPP have the form [0-9a-zA-Z_]+ , the string cannot
     exceed IFNAMSIZ characters, and PPP cannot be the name of any existing OS
     network interface.

     See netmap(4) for details on the API.

   LLIIMMIITTSS
     vvaallee currently supports up to 254 ports per switch. The maximum number of
     switches is provided by the max_bridges sysctl variable.

SSYYSSCCTTLL VVAARRIIAABBLLEESS
     See netmap(4) for a list of sysctl variables that affect vvaallee bridges.

EEXXAAMMPPLLEESS
     Create one switch, with a traffic generator connected to one port, and a
     netmap-enabled tcpdump instance on another port:

           tcpdump -ni valea:1 &
           pkt-gen  -i valea:0 -f tx &

     Create two switches, each connected to two qemu machines on different
     ports.

           qemu -net nic -net netmap,ifname=vale1:a ... &
           qemu -net nic -net netmap,ifname=vale1:b ... &
           qemu -net nic -net netmap,ifname=vale2:c ... &
           qemu -net nic -net netmap,ifname=vale2:d ... &

SSEEEE AALLSSOO
     netmap(4), valectl(8)

     Luigi Rizzo, Giuseppe Lettieri: VALE, a switched ethernet for virtual ma‐
     chines, June 2012, http://info.iet.unipi.it/~luigi/vale/

AAUUTTHHOORRSS
     The vvaallee switch was designed and implemented in 2012 by Luigi Rizzo and
     Giuseppe Lettieri at the Universita` di Pisa.

     vvaallee was funded by the European Commission within FP7 Projects CHANGE
     (257422) and OPENLAB (287581).

BSD                             August 30, 2024                            BSD
