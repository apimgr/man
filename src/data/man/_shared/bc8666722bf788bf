The  driver provides the SPI frontend attachment for the Sparkfun
SCMD device.  The following variables are inherited from : If the
driver is compiled with this node will appear and can be used  to
set the debugging level.  character device allowing access to the
register space of a main SCMD device The driver first appeared in
The  driver was written by When used in SPI mode, the SCMD device
uses an odd method of doing reads that can become stuck.  See the
command spi_read_one in the utility to try and unstick the device
when this happens.
