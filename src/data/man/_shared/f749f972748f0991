The directory is created with the access permissions specified by
and  restricted by the of the calling process. See for the possi‐
ble permission bit masks for The directory’s owner ID is  set  to
the process’s effective user ID.  The directory’s group ID is set
to  that  of  the parent directory in which it is created.  Note:
the behavior of is undefined when mode bits other than the low  9
bits  are  used.  Use after to explicitly set the other bits (See
example below).  The system call is equivalent to except  in  the
case  where  specifies  a  relative path.  In this case the newly
created directory is created relative to the directory associated
with the file descriptor instead of the  current  working  direc‐
tory.   If is passed the special value in the parameter, the cur‐
rent working directory is used and the behavior is identical to a
call to A 0 return value indicates success.  A  ‐1  return  value
indicates  an error, and an error code is stored in will fail and
no directory will be created if: Search permission is denied  for
a  component  of the path prefix.  Write permission is denied for
the parent directory.  The new directory cannot  be  created  be‐
cause  the  user’s  quota  of disk blocks on the file system that
will contain the directory has been exhausted.  The user’s  quota
of inodes on the file system on which the directory is being cre‐
ated  has been exhausted.  The named file exists.  points outside
the process’s allocated address space.   An  I/O  error  occurred
while making the directory entry or allocating the inode.  An I/O
error  occurred while reading from or writing to the file system.
The named file is the root directory.  Too  many  symbolic  links
were  encountered  in translating the pathname.  This is taken to
be indicative of a looping symbolic link.  The  parent  directory
already has {LINK_MAX} links.  A component of a pathname exceeded
characters, or an entire path name exceeded characters.  A compo‐
nent  of  the  path  prefix  does  not  exist or path is an empty
string.  The new directory cannot be created because there is  no
space  left  on the file system that would contain it.  There are
no free inodes on the file system on which the directory is being
created.  A component of the path prefix is not a directory.  The
parent directory resides on a read‐only file system.  In addition
to the errors returned by the the function may fail if: The argu‐
ment does not specify an absolute path and the argument  is  nei‐
ther  nor  a valid file descriptor open for searching.  The argu‐
ment is not an absolute path and is neither nor a file descriptor
associated with a directory.  The filename does not match the en‐
coding rules.

int main (int argc, const char * argv[]) {         /* The  behav‐
ior  of  mkdir  is undefined for anything other than the "permis‐
sion"   bits    */            if    (mkdir("/tmp/blah",    0777))
                perror("/tmp/blah");

        /*  So  we need to set the sticky/executable bits explic‐
itly   with   chmod   after   calling   mkdir    */            if
(chmod("/tmp/blah",          07777))                         per‐
ror("/tmp/blah");     }

The include file is necessary.  The function conforms to The sys‐
tem call is expected to conform to  POSIX.1‐2008  .   The  system
call appeared in OS X 10.10
