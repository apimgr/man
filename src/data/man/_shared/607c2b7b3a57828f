_m_s_g_g_e_t(2)                     System Calls Manual                    _m_s_g_g_e_t(2)

NNAAMMEE
       msgget - get a System V message queue identifier

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssyyss//mmssgg..hh>>

       iinntt mmssggggeett((kkeeyy__tt _k_e_y,, iinntt _m_s_g_f_l_g));;

DDEESSCCRRIIPPTTIIOONN
       The  mmssggggeett() system call returns the System V message queue identifier
       associated with the value of the _k_e_y argument.  It may be  used  either
       to  obtain  the  identifier of a previously created message queue (when
       _m_s_g_f_l_g is zero and _k_e_y does not have the value IIPPCC__PPRRIIVVAATTEE), or to cre‐
       ate a new set.

       A new message queue is created if _k_e_y has the value IIPPCC__PPRRIIVVAATTEE or  _k_e_y
       isn’t  IIPPCC__PPRRIIVVAATTEE, no message queue with the given key _k_e_y exists, and
       IIPPCC__CCRREEAATT is specified in _m_s_g_f_l_g.

       If _m_s_g_f_l_g specifies both IIPPCC__CCRREEAATT and IIPPCC__EEXXCCLL and a message queue al‐
       ready exists for _k_e_y, then mmssggggeett() fails with  _e_r_r_n_o  set  to  EEEEXXIISSTT.
       (This  is  analogous  to the effect of the combination OO__CCRREEAATT || OO__EEXXCCLL
       for ooppeenn(2).)

       Upon creation, the least significant bits of the argument _m_s_g_f_l_g define
       the permissions of the message queue.  These permission bits  have  the
       same format and semantics as the permissions specified for the _m_o_d_e ar‐
       gument of ooppeenn(2).  (The execute permissions are not used.)

       If  a  new message queue is created, then its associated data structure
       _m_s_q_i_d___d_s (see mmssggccttll(2)) is initialized as follows:

       •  _m_s_g___p_e_r_m_._c_u_i_d and _m_s_g___p_e_r_m_._u_i_d are set to the effective user  ID  of
          the calling process.

       •  _m_s_g___p_e_r_m_._c_g_i_d  and _m_s_g___p_e_r_m_._g_i_d are set to the effective group ID of
          the calling process.

       •  The least significant 9 bits of _m_s_g___p_e_r_m_._m_o_d_e are set to  the  least
          significant 9 bits of _m_s_g_f_l_g.

       •  _m_s_g___q_n_u_m,  _m_s_g___l_s_p_i_d, _m_s_g___l_r_p_i_d, _m_s_g___s_t_i_m_e, and _m_s_g___r_t_i_m_e are set to
          0.

       •  _m_s_g___c_t_i_m_e is set to the current time.

       •  _m_s_g___q_b_y_t_e_s is set to the system limit MMSSGGMMNNBB.

       If the message queue already exists the permissions are verified, and a
       check is made to see if it is marked for destruction.

RREETTUURRNN VVAALLUUEE
       On success, mmssggggeett() returns the message queue identifier  (a  nonnega‐
       tive  integer).   On failure, -1 is returned, and _e_r_r_n_o is set to indi‐
       cate the error.

EERRRROORRSS
       EEAACCCCEESS A message queue exists for _k_e_y, but the calling process does not
              have permission to access the  queue,  and  does  not  have  the
              CCAAPP__IIPPCC__OOWWNNEERR  capability in the user namespace that governs its
              IPC namespace.

       EEEEXXIISSTT IIPPCC__CCRREEAATT and IIPPCC__EEXXCCLL were specified in _m_s_g_f_l_g, but  a  message
              queue already exists for _k_e_y.

       EENNOOEENNTT No  message  queue  exists  for  _k_e_y  and _m_s_g_f_l_g did not specify
              IIPPCC__CCRREEAATT.

       EENNOOMMEEMM A message queue has to be created but the system does  not  have
              enough memory for the new data structure.

       EENNOOSSPPCC A  message  queue has to be created but the system limit for the
              maximum number of message queues (MMSSGGMMNNII) would be exceeded.

SSTTAANNDDAARRDDSS
       POSIX.1‐2024.

HHIISSTTOORRYY
       POSIX.1‐2001, SVr4.

   LLiinnuuxx
       Until Linux 2.3.20, Linux would return EEIIDDRRMM for a mmssggggeett() on  a  mes‐
       sage queue scheduled for deletion.

NNOOTTEESS
       IIPPCC__PPRRIIVVAATTEE isn’t a flag field but a _k_e_y___t type.  If this special value
       is  used for _k_e_y, the system call ignores everything but the least sig‐
       nificant 9 bits of _m_s_g_f_l_g and creates a new message queue (on success).

       The following is a system limit on message queue resources affecting  a
       mmssggggeett() call:

       MMSSGGMMNNII System‐wide limit on the number of message queues.  Before Linux
              3.19,  the  default  value for this limit was calculated using a
              formula based on available system memory.  Since Linux 3.19, the
              default value is 32,000.  On Linux, this limit can be  read  and
              modified via _/_p_r_o_c_/_s_y_s_/_k_e_r_n_e_l_/_m_s_g_m_n_i.

BBUUGGSS
       The name choice IIPPCC__PPRRIIVVAATTEE was perhaps unfortunate, IIPPCC__NNEEWW would more
       clearly show its function.

SSEEEE AALLSSOO
       mmssggccttll(2),     mmssggrrccvv(2),    mmssggssnndd(2),    ffttookk(3),    ccaappaabbiilliittiieess(7),
       mmqq__oovveerrvviieeww(7), ssyyssvviippcc(7)

Linux man‐pages (unreleased)      2025‐10‐29                         _m_s_g_g_e_t(2)
