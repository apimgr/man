_i_o___s_e_t_u_p(2)                   System Calls Manual                  _i_o___s_e_t_u_p(2)

NNAAMMEE
       io_setup - create an asynchronous I/O context

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

       Alternatively, Asynchronous I/O library (_l_i_b_a_i_o, _-_l_a_i_o); see VERSIONS.

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<lliinnuuxx//aaiioo__aabbii..hh>>          /* Defines needed types */

       lloonngg iioo__sseettuupp((uunnssiiggnneedd iinntt _n_r___e_v_e_n_t_s,, aaiioo__ccoonntteexxtt__tt **_c_t_x___i_d_p));;

       _N_o_t_e: There is no glibc wrapper for this system call; see VERSIONS.

DDEESSCCRRIIPPTTIIOONN
       _N_o_t_e:  this  page  describes  the raw Linux system call interface.  The
       wrapper function provided by _l_i_b_a_i_o  uses  a  different  type  for  the
       _c_t_x___i_d_p argument.  See VERSIONS.

       The iioo__sseettuupp() system call creates an asynchronous I/O context suitable
       for concurrently processing _n_r___e_v_e_n_t_s operations.  The _c_t_x___i_d_p argument
       must  not point to an AIO context that already exists, and must be ini‐
       tialized to 0 prior to the call.  On successful  creation  of  the  AIO
       context, _*_c_t_x___i_d_p is filled in with the resulting handle.

RREETTUURRNN VVAALLUUEE
       On  success,  iioo__sseettuupp()  returns  0.  For the failure return, see VER‐
       SIONS.

EERRRROORRSS
       EEAAGGAAIINN The specified _n_r___e_v_e_n_t_s exceeds the limit of  available  events,
              as defined in _/_p_r_o_c_/_s_y_s_/_f_s_/_a_i_o_-_m_a_x_-_n_r (see pprroocc(5)).

       EEFFAAUULLTT An invalid pointer is passed for _c_t_x___i_d_p.

       EEIINNVVAALL _c_t_x___i_d_p  is  not initialized, or the specified _n_r___e_v_e_n_t_s exceeds
              internal limits.  _n_r___e_v_e_n_t_s should be greater than 0.

       EENNOOMMEEMM Insufficient kernel resources are available.

       EENNOOSSYYSS iioo__sseettuupp() is not implemented on this architecture.

VVEERRSSIIOONNSS
       glibc does not provide a wrapper for this system call.  You  could  in‐
       voke  it  using  ssyyssccaallll(2).  But instead, you probably want to use the
       iioo__sseettuupp() wrapper function provided by _l_i_b_a_i_o.

       Note that the _l_i_b_a_i_o wrapper function uses a  different  type  (_i_o___c_o_n_‐
       _t_e_x_t___t _*)  for the _c_t_x___i_d_p argument.  Note also that the _l_i_b_a_i_o wrapper
       does not follow the usual C library conventions for indicating  errors:
       on  error it returns a negated error number (the negative of one of the
       values  listed  in  ERRORS).   If  the  system  call  is  invoked   via
       ssyyssccaallll(2), then the return value follows the usual conventions for in‐
       dicating  an error: -1, with _e_r_r_n_o set to a (positive) value that indi‐
       cates the error.

SSTTAANNDDAARRDDSS
       Linux.

HHIISSTTOORRYY
       Linux 2.5.

SSEEEE AALLSSOO
       iioo__ccaanncceell(2), iioo__ddeessttrrooyy(2), iioo__ggeetteevveennttss(2), iioo__ssuubbmmiitt(2), aaiioo(7)

Linux man‐pages (unreleased)      2025‐09‐21                       _i_o___s_e_t_u_p(2)
