KOBJ(9)                  BSD Kernel Developer's Manual                 KOBJ(9)

NNAAMMEE
     kkoobbjj — a kernel object system for FreeBSD

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ppaarraamm..hh>>
     ##iinncclluuddee <<ssyyss//kkoobbjj..hh>>

     _v_o_i_d
     kkoobbjj__ccllaassss__ccoommppiillee(_k_o_b_j___c_l_a_s_s___t _c_l_s);

     _v_o_i_d
     kkoobbjj__ccllaassss__ccoommppiillee__ssttaattiicc(_k_o_b_j___c_l_a_s_s___t _c_l_s, _k_o_b_j___o_p_s___t _o_p_s);

     _v_o_i_d
     kkoobbjj__ccllaassss__ffrreeee(_k_o_b_j___c_l_a_s_s___t _c_l_s);

     _k_o_b_j___t
     kkoobbjj__ccrreeaattee(_k_o_b_j___c_l_a_s_s___t _c_l_s, _s_t_r_u_c_t _m_a_l_l_o_c___t_y_p_e _*_m_t_y_p_e, _i_n_t _m_f_l_a_g_s);

     _v_o_i_d
     kkoobbjj__iinniitt(_k_o_b_j___t _o_b_j, _k_o_b_j___c_l_a_s_s___t _c_l_s);

     _v_o_i_d
     kkoobbjj__iinniitt__ssttaattiicc(_k_o_b_j___t _o_b_j, _k_o_b_j___c_l_a_s_s___t _c_l_s);

     _v_o_i_d
     kkoobbjj__ddeelleettee(_k_o_b_j___t _o_b_j, _s_t_r_u_c_t _m_a_l_l_o_c___t_y_p_e _*_m_t_y_p_e);

     DDEEFFIINNEE__CCLLAASSSS(_n_a_m_e, _k_o_b_j___m_e_t_h_o_d___t _*_m_e_t_h_o_d_s, _s_i_z_e___t _s_i_z_e);

DDEESSCCRRIIPPTTIIOONN
     The kernel object system implements an object-oriented programming system
     in the FreeBSD kernel.  The system is based around the concepts of inter‐
     faces, which are descriptions of sets of methods; classes, which are
     lists of functions implementing certain methods from those interfaces;
     and objects, which combine a class with a structure in memory.

     Methods are called using a dynamic method dispatching algorithm which is
     designed to allow new interfaces and classes to be introduced into the
     system at runtime.  The method dispatch algorithm is designed to be both
     fast and robust and is only slightly more expensive than a direct func‐
     tion call, making kernel objects suitable for performance-critical algo‐
     rithms.

     Suitable uses for kernel objects are any algorithms which need some kind
     of polymorphism (i.e., many different objects which can be treated in a
     uniform way).  The common behaviour of the objects is described by a
     suitable interface and each different type of object is implemented by a
     suitable class.

     The simplest way to create a kernel object is to call kkoobbjj__ccrreeaattee() with
     a suitable class, malloc type and flags (see malloc(9) for a description
     of the malloc type and flags).  This will allocate memory for the object
     based on the object size specified by the class and initialise it by ze‐
     roing the memory and installing a pointer to the class' method dispatch
     table.  Objects created in this way should be freed by calling
     kkoobbjj__ddeelleettee().

     Clients which would like to manage the allocation of memory themselves
     should call kkoobbjj__iinniitt() or kkoobbjj__iinniitt__ssttaattiicc() with a pointer to the mem‐
     ory for the object and the class which implements it.  It is also possi‐
     ble to use kkoobbjj__iinniitt() and kkoobbjj__iinniitt__ssttaattiicc() to change the class for an
     object.  This should be done with care as the classes must agree on the
     layout of the object.  The device framework uses this feature to asso‐
     ciate drivers with devices.

     The functions kkoobbjj__ccllaassss__ccoommppiillee(), kkoobbjj__ccllaassss__ccoommppiillee__ssttaattiicc() and
     kkoobbjj__ccllaassss__ffrreeee() are used to process a class description to make method
     dispatching efficient.  A client should not normally need to call these
     since a class will automatically be compiled the first time it is used.
     If a class is to be used before malloc(9) and mutex(9) are initialised,
     then kkoobbjj__ccllaassss__ccoommppiillee__ssttaattiicc() should be called with the class and a
     pointer to a statically allocated _k_o_b_j___o_p_s structure before the class is
     used to initialise any objects.  In that case, also kkoobbjj__iinniitt__ssttaattiicc()
     should be used instead of kkoobbjj__iinniitt().

     To define a class, first define a simple array of _k_o_b_j___m_e_t_h_o_d___t.  Each
     method which the class implements should be entered into the table using
     the macro KKOOBBJJMMEETTHHOODD() which takes the name of the method (including its
     interface) and a pointer to a function which implements it.  The table
     should be terminated with two zeros.  The macro DDEEFFIINNEE__CCLLAASSSS() can then
     be used to initialise a _k_o_b_j___c_l_a_s_s___t structure.  The size argument to
     DDEEFFIINNEE__CCLLAASSSS() specifies how much memory should be allocated for each ob‐
     ject.

HHIISSTTOORRYY
     Some of the concepts for this interface appeared in the device framework
     used for the alpha port of FreeBSD 3.0 and more widely in FreeBSD 4.0.

AAUUTTHHOORRSS
     This manual page was written by Doug Rabson.

BSD                            November 14, 2011                           BSD
