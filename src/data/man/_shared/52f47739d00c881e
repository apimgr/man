The driver reports the current voltage on the chip’s ADC channels
through the API.  The driver calculates these values according to
the  currently  selected  reference  voltage  It  can  be changed
through the node The following table shows the  supported  chips.
The  type  of  the  chip can be selected with the argument in the
config file.  The following variables are provided:  Defines  the
reference  voltage  on the chip’s pin in millivolts (mV).  It de‐
faults to the ADC’s maximum output value + 1 in millivolts (e.g.,
4096 for a 12‐bit ADC).  The driver first appeared in The  driver
was written by
