PAM_ISSUE(8)                   Linux-PAM Manual                   PAM_ISSUE(8)



NNAAMMEE
       pam_issue - PAM module to add issue file to user prompt

SSYYNNOOPPSSIISS
       ppaamm__iissssuuee..ssoo [noesc] [issue=_i_s_s_u_e_-_f_i_l_e_-_n_a_m_e]

DDEESSCCRRIIPPTTIIOONN
       pam_issue is a PAM module to prepend an issue file to the username
       prompt. It also by default parses escape codes in the issue file
       similar to some common getty's (using \x format).

       Recognized escapes:

       \\dd
           current day

       \\ll
           name of this tty

       \\mm
           machine architecture (uname -m)

       \\nn
           machine's network node hostname (uname -n)

       \\oo
           domain name of this system

       \\rr
           release number of operating system (uname -r)

       \\tt
           current time

       \\ss
           operating system name (uname -s)

       \\uu
           number of users currently logged in

       \\UU
           same as \u except it is suffixed with "user" or "users" (eg. "1
           user" or "10 users")

       \\vv
           operating system version and build date (uname -v)

OOPPTTIIOONNSS
       nnooeesscc
           Turns off escape code parsing.

       iissssuuee==_i_s_s_u_e_-_f_i_l_e_-_n_a_m_e
           The file to output if not using the default.

MMOODDUULLEE TTYYPPEESS PPRROOVVIIDDEEDD
       Only the aauutthh module type is provided.

RREETTUURRNN VVAALLUUEESS
       PAM_BUF_ERR
           Memory buffer error.

       PAM_IGNORE
           The prompt was already changed.

       PAM_SERVICE_ERR
           A service module error occurred.

       PAM_SUCCESS
           The new prompt was set successfully.

EEXXAAMMPPLLEESS
       Add the following line to /etc/pam.d/login to set the user specific
       issue at login:

                   auth optional pam_issue.so issue=/etc/issue



SSEEEE AALLSSOO
       ppaamm..ccoonnff(5), ppaamm..dd(5), ppaamm(7)

AAUUTTHHOORR
       pam_issue was written by Ben Collins <bcollins@debian.org>.



Linux-PAM Manual                  09/03/2021                      PAM_ISSUE(8)
