is  a TCP/IP server which implements the user identification pro‐
tocol as specified in RFC 1413.  operates by looking up  specific
TCP/IP connections and returning information which may or may not
be  associated  with the process owning the connection.  The fol‐
lowing options are available: Bind to IPv4 addresses only  (valid
with  flag  Bind  to IPv6 addresses only (valid with flag Bind to
the specified This may be an IPv4 or IPv6 address or even a host‐
name.  If a hostname is specified then will resolve it to an  ad‐
dress  (or addresses) and will bind this address (valid with flag
Run in the background (as daemon).  Specify an optional character
set designator to be included in  replies.   should  be  a  valid
charset set as described in the in upper case characters.  Return
instead  of  the  usual  or error replies.  Specify the format to
display info.  The allowed format specifiers  are:  %u      print
user name %U      print user number %g      print (primary) group
name  %G      print  (primary) group number %l      print list of
all groups by name %L      print list of all groups by number The
lists of groups (%l, %L) are comma‐separated, and start with  the
primary  group which is not repeated.  Any other characters (pre‐
ceded by %, and those not preceded by it) are printed  literally.
Specify  a  fall back If the lookup fails then this username will
be returned.  This can be useful for when running this service on
a NAT host and not using the forward/proxy functionality.   Spec‐
ify the group id number or name which the server should switch to
after binding itself to the TCP/IP port.  Same as but without the
restriction that the username in must not match an existing user.
If  the file exists in the home directory of the identified user,
return the username found in that file instead of the real  user‐
name.  If the username found in is that of an existing user, then
the  real  username will be returned.  Specify a will return this
name for all valid ident requests.   Use  for  logging  purposes.
Enables  forwarding  of  ident  queries.   The argument specifies
which packet filter should be used  to  lookup  the  connections,
currently  and  are  supported packet filters.  Note that changes
the ident queries to use the local port on the NAT  host  instead
of the local port on the forwarding host.  This is needed because
otherwise  we  can’t do a lookup on the proxy host.  On the proxy
host, should be enabled with the flag or with the  flag.   Enable
files.   If this file exists in the home directory of the identi‐
fied user then return instead of the normal USERID response.  Re‐
turn numeric user IDs instead of usernames.   Return  instead  of
the  default Specify a proxy server which will be used to receive
proxied ident queries from.  See also the flag how this operates.
Specify an alternative port number under which the server  should
run.   The  default  is port 113 (valid with flag Return a random
name of alphanumeric characters.  If the  flag  is  also  enabled
then a random number will be returned.  Specify a timeout for the
service.  The default timeout is 30 seconds.  Specify the user id
number  or  name  to which the server should switch after binding
itself to the TCP/IP port.  operates from or as  standalone  dae‐
mon.   Put the following lines into to enable as an IPv4 and IPv6
service   via   inetd:   ident   stream   tcp    nowait    nobody
/usr/libexec/identd  identd  ‐l  ident  stream tcp6 nowait nobody
/usr/libexec/identd identd ‐l To run as  standalone  daemon,  use
the flag.  This implementation of is written by Since should typ‐
ically  not  be  run as a privileged user or group, files for use
when running with the or flags will need to be world  accessible.
The same applies for files when running with the flag.  When for‐
warding  is enabled with the flag then will need access to either
(ipfilter), (pf), or Since it’s not a  good  idea  to  run  under
root,  you’ll  need  to adjust group owner/permissions to the de‐
vice(s) and run under that group.
