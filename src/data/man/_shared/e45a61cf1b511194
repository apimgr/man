_l_r_i_n_t(3)                   Library Functions Manual                   _l_r_i_n_t(3)

NNAAMMEE
       lrint,  lrintf, lrintl, llrint, llrintf, llrintl - round to nearest in‐
       teger

LLIIBBRRAARRYY
       Math library (_l_i_b_m, _-_l_m)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<mmaatthh..hh>>

       lloonngg llrriinntt((ddoouubbllee _x));;
       lloonngg llrriinnttff((ffllooaatt _x));;
       lloonngg llrriinnttll((lloonngg ddoouubbllee _x));;

       lloonngg lloonngg llllrriinntt((ddoouubbllee _x));;
       lloonngg lloonngg llllrriinnttff((ffllooaatt _x));;
       lloonngg lloonngg llllrriinnttll((lloonngg ddoouubbllee _x));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       All functions shown above:
           _ISOC99_SOURCE || _POSIX_C_SOURCE >= 200112L

DDEESSCCRRIIPPTTIIOONN
       These functions round their argument to the nearest integer value,  us‐
       ing the current rounding direction (see ffeesseettrroouunndd(3)).

       Note  that  unlike  the rriinntt(3) family of functions, the return type of
       these functions differs from that of their arguments.

RREETTUURRNN VVAALLUUEE
       These functions return the rounded integer value.

       If _x is a NaN or an infinity, or the rounded value is too large  to  be
       stored  in  a _l_o_n_g (_l_o_n_g _l_o_n_g in the case of the llll** functions), then a
       domain error occurs, and the return value is unspecified.

EERRRROORRSS
       See mmaatthh__eerrrroorr(7) for information on how to determine whether an  error
       has occurred when calling these functions.

       The following errors can occur:

       Domain error: _x is a NaN or infinite, or the rounded value is too large
              An invalid floating‐point exception (FFEE__IINNVVAALLIIDD) is raised.

       These functions do not set _e_r_r_n_o.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       llrriinntt(), llrriinnttff(), llrriinnttll(), llllrriinntt(), llllrriinnttff(), llllrriinnttll() T}   Thread
       safety  MT‐Safe

SSTTAANNDDAARRDDSS
       C11, POSIX.1‐2008.

HHIISSTTOORRYY
       glibc 2.1.  C99, POSIX.1‐2001.

SSEEEE AALLSSOO
       cceeiill(3), fflloooorr(3), llrroouunndd(3), nneeaarrbbyyiinntt(3), rriinntt(3), rroouunndd(3)

Linux man‐pages (unreleased)      2025‐05‐17                          _l_r_i_n_t(3)
