_u_s_e_r_‐_s_e_s_._._._‐_k_e_y_r_i_n_g(7) Miscellaneous Information Manual _u_s_e_r_‐_s_e_s_._._._‐_k_e_y_r_i_n_g(7)

NNAAMMEE
       user‐session‐keyring - per‐user default session keyring

DDEESSCCRRIIPPTTIIOONN
       The  user session keyring is a keyring used to anchor keys on behalf of
       a user.  Each UID the kernel  deals  with  has  its  own  user  session
       keyring  that  is shared by all processes with that UID.  The user ses‐
       sion keyring has a name (description) of the form ___u_i_d___s_e_s_._<_U_I_D_>  where
       _<_U_I_D_> is the user ID of the corresponding user.

       The  user session keyring is associated with the record that the kernel
       maintains for the UID.  It comes into existence upon the first  attempt
       to  access either the user session keyring, the uusseerr--kkeeyyrriinngg(7), or the
       sseessssiioonn--kkeeyyrriinngg(7).  The keyring remains pinned in existence so long as
       there are processes running with that real UID or files opened by those
       processes remain open.  (The keyring can also be pinned indefinitely by
       linking it into another keyring.)

       The user session keyring is created on demand when a thread requests it
       or when a thread asks  for  its  sseessssiioonn--kkeeyyrriinngg(7)  and  that  keyring
       doesn’t exist.  In the latter case, a user session keyring will be cre‐
       ated and, if the session keyring wasn’t to be created, the user session
       keyring will be set as the process’s actual session keyring.

       The  user  session  keyring is searched by rreeqquueesstt__kkeeyy(2) if the actual
       session keyring does not exist and is ignored otherwise.

       A special serial number value,  KKEEYY__SSPPEECC__UUSSEERR__SSEESSSSIIOONN__KKEEYYRRIINNGG,  is  de‐
       fined that can be used in lieu of the actual serial number of the call‐
       ing process’s user session keyring.

       From  the kkeeyyccttll(1) utility, ’@@uuss’ can be used instead of a numeric key
       ID in much the same way.

       User session keyrings are independent of cclloonnee(2),  ffoorrkk(2),  vvffoorrkk(2),
       eexxeeccvvee(2),  and  __eexxiitt(2)  excepting that the keyring is destroyed when
       the UID record is destroyed when the last process pinning it exits.

       If a user session keyring does not exist when it is accessed,  it  will
       be created.

       Rather  than relying on the user session keyring, it is strongly recom‐
       mended—especially if  the  process  is  running  as  root—that  a  sseess‐‐
       ssiioonn--kkeeyyrriinngg(7) be set explicitly, for example by ppaamm__kkeeyyiinniitt(8).

NNOOTTEESS
       The  user  session  keyring  was  added  to  support situations where a
       process doesn’t have a session keyring, perhaps because it was  created
       via  a  pathway  that didn’t involve PAM (e.g., perhaps it was a daemon
       started by iinneettdd(8)).  In such a scenario,  the  user  session  keyring
       acts as a substitute for the sseessssiioonn--kkeeyyrriinngg(7).

SSEEEE AALLSSOO
       kkeeyyccttll(1), kkeeyyccttll(3), kkeeyyrriinnggss(7), ppeerrssiisstteenntt--kkeeyyrriinngg(7),
       pprroocceessss--kkeeyyrriinngg(7), sseessssiioonn--kkeeyyrriinngg(7), tthhrreeaadd--kkeeyyrriinngg(7),
       uusseerr--kkeeyyrriinngg(7)

Linux man‐pages (unreleased)      2025‐05‐17            _u_s_e_r_‐_s_e_s_._._._‐_k_e_y_r_i_n_g(7)
