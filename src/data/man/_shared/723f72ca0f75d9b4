disables the framebuffer if the keyboard and mouse are idle for a
period  of  time, and re‐enables the framebuffer when keyboard or
mouse activity resumes.  When killed with a  SIGINT,  SIGHUP,  or
SIGTERM, will re‐enable the framebuffer.  The pid can be found in
the  file  The  options  are as follows: Overriding the other op‐
tions, simply try (once) to blank  the  framebuffer,  then  exit.
Wait  the  number of seconds specified by expressed in the format
before disabling the framebuffer due to inactivity.  The  default
is  600  seconds (10 minutes).  Wait the number of seconds speci‐
fied by expressed in the format  before  re‐enabling  the  frame‐
buffer  once  activity resumes.  The default is .25 seconds.  Use
the framebuffer device instead of the default Add to the list  of
devices  to  monitor for activity.  Do not check the keyboard for
activity.  Do not check the mouse for activity.   Overriding  the
other options, simply try (once) to unblank the framebuffer, then
exit.   Note that the and flags are mutually exclusive.  The key‐
board device.  The mouse device.  The console  device.   The  de‐
fault  framebuffer.  The keyboard for wscons machines.  The mouse
device for wscons machines.  The console device  for  wscons  ma‐
chines.  File containing the pid of checks the access and modifi‐
cation  times of the devices it is monitoring to determine activ‐
ity.  If the devices are on a file system mounted with the and/or
options, it will not function as expected.  A possible workaround
is to use a script as such: #!/bin/sh /usr/sbin/screenblank ‐d  0
‐e 0 && read /usr/bin/pkill screenblank
