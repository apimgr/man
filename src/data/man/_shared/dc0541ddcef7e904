LGE(4)                   BSD Kernel Interfaces Manual                   LGE(4)

NNAAMMEE
     llggee — Level 1 LXT1001 NetCellerator PCI 1Gb Ethernet device

SSYYNNOOPPSSIISS
     llggee** aatt ppccii??
     xxmmpphhyy** aatt mmiiii??

DDEESSCCRRIIPPTTIIOONN
     The llggee driver provides support for various NICs based on the Level 1
     LXT1001 NetCellerator Gigabit Ethernet controller chip, including the
     following:

     ••   Allied Telesis CentreCOM LA1000-PCI-SX
     ••   D-Link DGE-500SX
     ••   SMC TigerCard 1000 (SMC9462SX)

     These NICs are capable of 1000Mbps speeds over fiber optic cable.  The
     LXT1001 also supports a GMII port for use with 10/100/1000 copper PHYs,
     however there aren't currently any NICs on the market that use this fea‐
     ture.

     The LXT1001 supports IPv4 receive IP/TCP/UDP checksum offload as well as
     a 64-bit multicast hash filter.  It also supports jumbo frames.

     The llggee driver supports the following media types:

     aauuttoosseelleecctt   Enable autoselection of the media type and options.  The
                  user can manually override the autoselected mode by adding
                  media options to the appropriate hostname.if(5) file.

     11000000bbaasseeSSXX   Set 1000baseSX operation over fiber optic cable.  Both
                  ffuullll--dduupplleexx and hhaallff--dduupplleexx modes are supported.

     The llggee driver supports the following media options:

     ffuullll--dduupplleexx  Force full duplex operation.

     hhaallff--dduupplleexx  Force half duplex operation.

     For more information on configuring this device, see ifconfig(8).

DDIIAAGGNNOOSSTTIICCSS
     llggee00:: ccaann''tt mmaapp ii//oo ssppaaccee  A fatal initialization error has occurred.

     llggee00:: ccoouullddnn''tt mmaapp iinntteerrrruupptt  A fatal initialization error has occurred.

     llggee00:: nnoo mmeemmoorryy ffoorr jjuummbboo bbuuffffeerr qquueeuuee!!  The driver failed to allocate
     memory for jumbo frames during initialization.

     llggee00:: wwaattcchhddoogg ttiimmeeoouutt  The device has stopped responding to the network,
     or there is a problem with the network connection (cable).

SSEEEE AALLSSOO
     arp(4), ifmedia(4), intro(4), netintro(4), pci(4), xmphy(4),
     hostname.if(5), ifconfig(8)

     _L_e_v_e_l _1 _L_X_T_1_0_0_1 _P_r_o_g_r_a_m_m_i_n_g _M_a_n_u_a_l,
     https://people.freebsd.org/~wpaul/Level1/LXT1001SRM.pdf.

HHIISSTTOORRYY
     The llggee device driver first appeared in OpenBSD 3.0.

AAUUTTHHOORRSS
     The llggee driver was written by Bill Paul <_w_i_l_l_i_a_m_._p_a_u_l_@_w_i_n_d_r_i_v_e_r_._c_o_m>.

BSD                            February 18, 2022                           BSD
