The  file  defines  the  kernel state tunables that can be set at
boot time using (using the switch) via the startup  script.   The
state  to  be  set  is described using a style name.  The MIB and
value must be separated by with no whitespace, for example: Blank
lines, lines with just and comments (beginning with are  ignored.
Line  continuations using backslash are permitted.  Only integral
and string values can be set.  The file resides in The  following
is an example file: # Change max open files kern.maxfiles=1792

# Run Veriexec in IDS mode kern.veriexec.strict=1

#  Enable  IP  packet forwarding net.inet.ip.forwarding=1 Support
for first appeared in
