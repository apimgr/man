attribute  on  a  These functions represent an Apple extension to
and and as such should not be used by  programs  intending  their
code  to  be  portable to other platforms.  The function sets the
precise universal binary  preferences  for  the  spawn  attribute
value  referenced by from the memory containing the referenced by
and the referenced by with a size of elements; the actual  number
of  elements that are set in the attribute is returned in When or
is subsequently invoked on a Universal binary with the  the  ele‐
ments which were set will be used, in the order they were set, to
select  the  first  element  in the list which matches any and of
those available in the Universal binary.  If there is  no  match,
then  the  attempt to create the child process will fail with the
error EBADARCH.  If the and are the last pair in the  list,  then
rather  than  returning EBADARCH on no match, the system will in‐
stead fall back to the standard Universal binary grading  prefer‐
ence  order.  Using as a for any value will select the best slice
for that specific similar to using If called  multiple  times  on
the same the previous preferences will be overwritten.  The func‐
tion  gets the precise universal binary preferences for the spawn
attribute value referenced by (set by a prior call to or into the
memory containing the referenced by and the referenced by with  a
prereserved  size of elements; the actual number of elements that
are copied from the attribute is returned in  On  success,  these
functions  return  0; on failure they return an error number from
and modify the value of Additionally, if successful, will  modify
the  contents  of  the  array  with the current attribute values.
These functions may fail if: The value specified by  is  invalid.
The  value of is invalid.  Nonstandard The and function calls ap‐
peared in macOS 10.16
