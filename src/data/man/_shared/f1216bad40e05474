The  command configures file system snapshot pseudo disk devices.
It will associate the file system snapshot disk with  a  snapshot
of  the  file system mounted on allowing to be accessed as though
it were a disk.  If resides on the snapshotted file system a per‐
sistent snapshot will be created.  This snapshot is active  until
is  unlinked.  Persistent snapshots save the file system as if it
had been unmounted.  Unlinked but open files get removed from the
snapshot.  The time needed to create a  persistent  snapshot  in‐
creases  with  the size of the file system and decreases with the
file system block size.  This snapshot mode is only supported for
FFS file systems.  Otherwise data written  through  the  will  be
saved in If is a regular file, it will be created with length De‐
fault  size  is  the  size of Data is saved to in units of bytes.
The snapshot is an image of the underlying block device at a  mo‐
ment  in  time.  Unlinked but open files are still present on the
snapshot.  The time needed to create a snapshot is independent of
the size of the file system or the file system block  size.   Op‐
tions  indicate an action to be performed: Configures the device.
If successful, references to will access the contents of  at  the
time the snapshot was taken.  If is a directory, a temporary file
will be created in this directory.  This file will be unlinked on
exit.   List  the snapshot devices and indicate which ones are in
use.  If a specific is given, then only that will  be  described.
Unconfigures  the  Be  more verbose listing the snapshot devices.
Unlink after the is configured.  If no action option is given, is
assumed.  Configures the snapshot device as  a  snapshot  of  the
file system, and makes the contents of the snapshot available via
Data written through will be backed up in Configures the snapshot
device  as  a  snapshot of the file system.  Data written through
will be backed up in The backup will take place in units of  8192
bytes.  Unconfigures the device.  The command appeared in
