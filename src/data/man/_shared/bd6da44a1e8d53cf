MQ_GETATTR(2)               BSD System Calls Manual              MQ_GETATTR(2)

NNAAMMEE
     mmqq__ggeettaattttrr — get message queue attributes (REALTIME)

LLIIBBRRAARRYY
     POSIX Real-time Library (librt, -lrt)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<mmqquueeuuee..hh>>

     _i_n_t
     mmqq__ggeettaattttrr(_m_q_d___t _m_q_d_e_s, _s_t_r_u_c_t _m_q___a_t_t_r _*_m_q_s_t_a_t);

DDEESSCCRRIIPPTTIIOONN
     The mmqq__ggeettaattttrr() system call obtains status information and attributes of
     the message queue and the open message queue description associated with
     the message queue descriptor.

     The _m_q_d_e_s argument specifies a message queue descriptor.

     The results are returned in the _m_q___a_t_t_r structure referenced by the
     _m_q_s_t_a_t argument.

     Upon return, the following members will have the values associated with
     the open message queue description as set when the message queue was
     opened and as modified by subsequent mmqq__sseettaattttrr() calls: _m_q___f_l_a_g_s.

     The following attributes of the message queue will be returned as set at
     message queue creation: _m_q___m_a_x_m_s_g, _m_q___m_s_g_s_i_z_e.

     Upon return, the following members within the _m_q___a_t_t_r structure refer‐
     enced by the _m_q_s_t_a_t argument will be set to the current state of the mes‐
     sage queue:

     _m_q___c_u_r_m_s_g_s  The number of messages currently on the queue.

RREETTUURRNN VVAALLUUEESS
     Upon successful completion, the value 0 is returned; otherwise the
     value -1 is returned and the global variable _e_r_r_n_o is set to indicate the
     error.

EERRRROORRSS
     The mmqq__ggeettaattttrr() system call will fail if:

     [EBADF]            The _m_q_d_e_s argument is not a valid message queue de‐
                        scriptor.

SSEEEE AALLSSOO
     mq_open(2), mq_send(2), mq_setattr(2), mq_timedsend(2)

SSTTAANNDDAARRDDSS
     The mmqq__ggeettaattttrr() system call conforms to IEEE Std 1003.1-2004
     (“POSIX.1”).

HHIISSTTOORRYY
     Support for POSIX message queues first appeared in FreeBSD 7.0.

CCOOPPYYRRIIGGHHTT
     Portions of this text are reprinted and reproduced in electronic form
     from IEEE Std 1003.1, 2004 Edition, Standard for Information Technology
     -- Portable Operating System Interface (POSIX), The Open Group Base Spec‐
     ifications Issue 6, Copyright (C) 2001-2004 by the Institute of Electri‐
     cal and Electronics Engineers, Inc and The Open Group.  In the event of
     any discrepancy between this version and the original IEEE and The Open
     Group Standard, the original IEEE and The Open Group Standard is the ref‐
     eree document.  The original Standard can be obtained online at
     http://www.opengroup.org/unix/online.html.

BSD                            February 21, 2014                           BSD
