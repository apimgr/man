The  utility  controls  the  operation of Intel PRO/Wireless 2100
networking devices via driver.  You should not use  this  program
to  configure  IEEE 802.11 parameters.  Use instead.  The options
are as follows: Display adapter’s internal  statistics.   Display
the  radio  switch state (on or off).  The firmware, loaded auto‐
matically, is not shipped with install the package.  The original
archive is available from Intel at: BSS mode  (connection  to  an
access  point)  firmware  IBSS  mode  (point‐to‐point connection)
firmware Monitor mode firmware The utility and this man page were
written by
