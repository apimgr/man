DEVICE_ATTACH(9)         BSD Kernel Developer's Manual        DEVICE_ATTACH(9)

NNAAMMEE
     DDEEVVIICCEE__AATTTTAACCHH — attach a device

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ppaarraamm..hh>>
     ##iinncclluuddee <<ssyyss//bbuuss..hh>>

     _i_n_t
     DDEEVVIICCEE__AATTTTAACCHH(_d_e_v_i_c_e___t _d_e_v);

DDEESSCCRRIIPPTTIIOONN
     Attach a device to the system after the DDEEVVIICCEE__PPRROOBBEE() method has been
     called and has indicated that the device exists.  The DDEEVVIICCEE__AATTTTAACCHH()
     method should initialize the hardware and allocate other system resources
     (such as devfs(5) entries).

     Devices which implement buses should use this method to probe for the ex‐
     istence of devices attached to the bus and add them as children.  If this
     is combined with the use of bus_generic_attach(9) the child devices will
     be automatically probed and attached.

RREETTUURRNN VVAALLUUEESS
     Zero is returned on success, otherwise an appropriate error is returned.

SSEEEE AALLSSOO
     devfs(5), device(9), DEVICE_DETACH(9), DEVICE_IDENTIFY(9),
     DEVICE_PROBE(9), DEVICE_SHUTDOWN(9)

AAUUTTHHOORRSS
     This manual page was written by Doug Rabson <_d_f_r_@_F_r_e_e_B_S_D_._o_r_g>.

BSD                            January 15, 2017                            BSD
