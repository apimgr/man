The  system call returns the final status of the asynchronous I/O
request associated with the structure pointed to  by  The  system
call should only be called once, to obtain the final status of an
asynchronous  I/O  operation  (once  returns something other than
However, if is not called at all, will leak  resources.   If  the
asynchronous I/O request has completed, the status is returned as
described  in or On failure, returns and sets to indicate the er‐
ror condition.  The system call will fail if: The  argument  does
not reference an outstanding asynchronous I/O request.  The asyn‐
chronous I/O request is still in progress.
The type of the return value has changed.  The system call is ex‐
pected  to  conform  to  the standard.  The system call first ap‐
peared in This manual page was written by
