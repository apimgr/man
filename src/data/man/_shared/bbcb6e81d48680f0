NSLOOKUP(1)               BSD General Commands Manual              NSLOOKUP(1)

NNAAMMEE
     nnssllooookkuupp — query Internet name servers interactively

SSYYNNOOPPSSIISS
     nnssllooookkuupp [--ooppttiioonn] [_n_a_m_e | _-] [_s_e_r_v_e_r]

DDEESSCCRRIIPPTTIIOONN
     The nnssllooookkuupp command queries Internet domain name servers.  It has two
     modes: interactive and non-interactive.  Interactive mode allows the user
     to query name servers for information about various hosts and domains or
     to print a list of hosts in a domain.  Non-interactive mode is used to
     print just the name and requested information for a host or domain.

     Interactive mode is entered in the following cases:

     1.   when no arguments are given (the default name server will be used)

     2.   when the first argument is a hyphen (-) and the second argument is
          the host name or Internet address of a name server.

     Non-interactive mode is used when the name or Internet address of the
     host to be looked up is given as the first argument.  The optional second
     argument specifies the host name or address of a name server.

     Options can also be specified on the command line if they precede the ar‐
     guments and are prefixed with a hyphen.  For example, to change the de‐
     fault query type to host information, and the initial timeout to 10 sec‐
     onds, type:

           nslookup -query=hinfo -timeout=10

     The --vveerrssiioonn option causes nnssllooookkuupp to print the version number and imme‐
     diately exits.

IINNTTEERRAACCTTIIVVEE CCOOMMMMAANNDDSS
     eexxiitt    Exit the program.

     hhoosstt [_s_e_r_v_e_r]
             Look up information for host using the current default server or
             using server, if specified.  If host is an Internet address and
             the query type is A or PTR, the name of the host is returned.  If
             host is a name and does not have a trailing period, the search
             list is used to qualify the name.

             To look up a host not in the current domain, append a period to
             the name.

     llsseerrvveerr _d_o_m_a_i_n
     sseerrvveerr _d_o_m_a_i_n
             Change the default server to _d_o_m_a_i_n; llsseerrvveerr uses the initial
             server to look up information about ddoommaaiinn, while sseerrvveerr uses the
             current default server.  If an authoritative answer can't be
             found, the names of servers that might have the answer are re‐
             turned.

     sseett kkeeyywwoorrdd=_v_a_l_u_e
             This command is used to change state information that affects the
             lookups.  Some keywords may be abbreviated, as shown in parenthe‐
             ses.  Valid keywords are:

             aallll     Prints the current values of the frequently used options
                     to sseett.  Information about the  current default server
                     and host is also printed.

             ccllaassss=_v_a_l_u_e
                     (ccll) Change the query class to one of:

                     AANNYY     wildcard
                     CCHH      the Chaos class
                     HHSS      the Hesiod class
                     IINN      the Internet class

                     The class specifies the protocol group of the informa‐
                     tion.  The default is IINN.

             [nnoo]dd22  Turn debugging mode on or off.  This displays more about
                     what nslookup is doing.  The default is nnoodd22.

             [nnoo]ddeebbuugg
                     (nnooddeebb) Turn on or off the display of the full response
                     packet and any intermediate response packets when search‐
                     ing.  The default is nnooddeebbuugg.

             ddoommaaiinn=_n_a_m_e
                     Sets the search list to _n_a_m_e.

             [nnoo]ffaaiill
                     Try the next name server if a name server responds with
                     SERVFAIL or a referral (nnooffaaiill) or terminate query (ffaaiill)
                     on such a response.  The default is nnooffaaiill.

             nnddoottss=_n_u_m_b_e_r
                     Set the number of dots (label separators) in a domain
                     that will disable searching.  Absolute names always stop
                     searching.

             ppoorrtt=_v_a_l_u_e
                     (ppoo) Change the default TCP/UDP name server port to
                     _v_a_l_u_e.  The default is port 53.

             [qquueerryy]ttyyppee=_v_a_l_u_e
                     (qq, ttyy) Change the type of the information query.  The
                     default is ‘A’.

             [nnoo]rreeccuurrssee
                     (rreecc) Tell the name server to query other servers if it
                     does not have the information.  The default is rreeccuurrssee.

             rreettrryy=_n_u_m_b_e_r
                     Set the number of retries to number.

             [nnoo]sseeaarrcchh
                     If the lookup request contains at least one period but
                     doesn't end with a trailing period, append the domain
                     names in the domain search list to the request until an
                     answer is received.  The default is sseeaarrcchh.

             ttiimmeeoouutt=_n_u_m_b_e_r
                     Change the initial timeout interval for waiting for a re‐
                     ply to number seconds.

             [nnoo]vvcc  Always use a virtual circuit when sending requests to the
                     server.  The default is nnoovvcc.

     ??       not implemented
     ffiinnggeerr  not implemented
     hheellpp    not implemented
     llss      not implemented
     rroooott    not implemented
     vviieeww    not implemented

FFIILLEESS
     _/_e_t_c_/_r_e_s_o_l_v_._c_o_n_f

EEXXIITT SSTTAATTUUSS
     The nnssllooookkuupp command returns with an exit status of 1 if any query
     failed, and 0 otherwise.

SSEEEE AALLSSOO
     dig(1), host(1)

AAUUTTHHOORRSS
     Internet Systems Consortium, Inc.

BSD                            February 7, 2020                            BSD
