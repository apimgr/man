The  interface forms the backend part of the paravirtualized dri‐
vers used by domains to offer  network  connectivity.   When  the
guest domain is the endpoint of the interface is a interface.  In
the  XenStore, and are identified by (virtual interface) entries.
All interfaces follow the naming convention, where represents the
guest domain identifier, and an arbitrary identifier; most of the
time, it is the frontend interface identifier, e.g.   For  conve‐
nience, the MAC address of an interface is chosen by incrementing
the  third  byte of the MAC address of the frontend device.  Con‐
ceptually, frontends and backends drivers are similar to two Eth‐
ernet cards connected via a crossover cable.  The MAC address for
this interface could not be read from XenStore.  The MAC  address
specified  in  the  configuration file of the newly created guest
domain is invalid.  MAC address of  the  interface.   The  driver
first appeared in The driver was written by
