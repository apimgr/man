is  used  to modify or examine the attributes of the console bell
and bitmapped console display.  The console bell’s volume, pitch,
and count may be specified, as well as  the  bitmapped  display’s
width,  height,  horizontal and vertical offset, pixel depth, and
color map.  The following flags are interpreted by After process‐
ing all other arguments, print information  about  the  console’s
state.   Open  and  use the terminal named by rather than the de‐
fault console Set the volume of the console bell to which must be
between 0 and 63, inclusive.  Set the pitch of the  console  bell
to  which  must  be between 10 and 1399.  Set the duration of the
beep to milliseconds which must be between 1  and  5000  (5  sec‐
onds).   Set  the  width of the console display to pixel columns.
must be a positive integer.  Set the height of the  console  dis‐
play  to pixel rows.  must be a positive integer.  Set the number
of bitplanes the console view should use to For example, if is  3
then  8  colors  will  be used.  Set the horizontal offset of the
console view on the monitor to  pixel  columns.   The  horizontal
offset may be a positive or a negative integer, positive being an
offset to the right, negative to the left.  Set the vertical off‐
set of the console view on the monitor to pixel rows.  The verti‐
cal  offset may be a positive or a negative integer, positive be‐
ing an offset down, negative up.  Any additional  arguments  will
be  interpreted  as  colors  and will be used to supply the color
values for the console view’s color map, starting with the  first
entry  in  the map.  (See the section of this manual page for in‐
formation on how to specify colors.)  If more colors are supplied
than are usable by the console view, a warning is printed and the
extra colors are ignored.  Colors are hexadecimal  numbers  which
have  one of the following formats: and are taken to be eight‐bit
values specifying the intensities of the red, green and blue com‐
ponents, respectively, of the color to be used.  For example,  is
bright red, is white, and is dark cyan.  is taken to be an eight‐
bit  value  specifying the intensity of grey to be used.  A value
of is black, a value of is white, and a value of is  a  grey  ap‐
proximately  half  way  in  between.   is taken to be the one‐bit
monochrome value to be used.  A value of is black, and a value of
is white.  The command is only available on the and ports.
