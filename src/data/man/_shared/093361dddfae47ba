ACL_SET_QUALIFIER(3)     BSD Library Functions Manual     ACL_SET_QUALIFIER(3)

NNAAMMEE
     aaccll__sseett__qquuaalliiffiieerr — set ACL tag qualifier

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ttyyppeess..hh>>
     ##iinncclluuddee <<ssyyss//aaccll..hh>>

     _i_n_t
     aaccll__sseett__qquuaalliiffiieerr(_a_c_l___e_n_t_r_y___t _e_n_t_r_y___d, _c_o_n_s_t _v_o_i_d _*_t_a_g___q_u_a_l_i_f_i_e_r___p);

DDEESSCCRRIIPPTTIIOONN
     The aaccll__sseett__qquuaalliiffiieerr() function is a POSIX.1e call that sets the quali‐
     fier of the tag for the ACL entry _e_n_t_r_y___d to the value referred to by
     _t_a_g___q_u_a_l_i_f_i_e_r___p.

RREETTUURRNN VVAALLUUEESS
     The aaccll__sseett__qquuaalliiffiieerr() function returns the value 0 if successful; oth‐
     erwise the value -1 is returned and the global variable _e_r_r_n_o is set to
     indicate the error.

EERRRROORRSS
     The aaccll__sseett__qquuaalliiffiieerr() function fails if:

     [EINVAL]           Argument _e_n_t_r_y___d is not a valid descriptor for an ACL
                        entry.  The tag type of the ACL entry _e_n_t_r_y___d is not
                        ACL_USER or ACL_GROUP.  The value pointed to by
                        _t_a_g___q_u_a_l_i_f_i_e_r___p is not valid.

     [ENOMEM]           The value to be returned requires more memory than is
                        allowed by the hardware or system-imposed memory man‐
                        agement constraints.

SSEEEE AALLSSOO
     acl(3), acl_get_qualifier(3), posix1e(3)

SSTTAANNDDAARRDDSS
     POSIX.1e is described in IEEE POSIX.1e draft 17.

HHIISSTTOORRYY
     POSIX.1e support was introduced in FreeBSD 4.0.  The aaccll__ggeett__qquuaalliiffiieerr()
     function was added in FreeBSD 5.0.

AAUUTTHHOORRSS
     The aaccll__ggeett__qquuaalliiffiieerr() function was written by Chris D. Faulhaber
     <_j_e_d_g_a_r_@_f_x_p_._o_r_g>.

BSD                             March 10, 2001                             BSD
