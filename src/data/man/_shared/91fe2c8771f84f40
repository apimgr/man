_g_e_t_w(3)                    Library Functions Manual                    _g_e_t_w(3)

NNAAMMEE
       getw, putw - input and output of words (ints)

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssttddiioo..hh>>

       iinntt ggeettww((FFIILLEE **_s_t_r_e_a_m));;
       iinntt ppuuttww((iinntt _w,, FFIILLEE **_s_t_r_e_a_m));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       ggeettww(), ppuuttww():
           Since glibc 2.3.3:
               _XOPEN_SOURCE && ! (_POSIX_C_SOURCE >= 200112L)
                   || /* glibc >= 2.19: */ _DEFAULT_SOURCE
                   || /* glibc <= 2.19: */ _BSD_SOURCE || _SVID_SOURCE
           Before glibc 2.3.3:
               _SVID_SOURCE || _BSD_SOURCE || _XOPEN_SOURCE

DDEESSCCRRIIPPTTIIOONN
       ggeettww()  reads  a word (that is, an _i_n_t) from _s_t_r_e_a_m.  It’s provided for
       compatibility with SVr4.  We recommend you use ffrreeaadd(3) instead.

       ppuuttww() writes the word _w (that is, an _i_n_t) to _s_t_r_e_a_m.  It  is  provided
       for  compatibility  with  SVr4,  but we recommend you use ffwwrriittee(3) in‐
       stead.

RREETTUURRNN VVAALLUUEE
       Normally, ggeettww() returns the word read, and ppuuttww() returns 0.   On  er‐
       ror, they return EEOOFF.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       ggeettww(), ppuuttww() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       None.

HHIISSTTOORRYY
       SVr4, SUSv2.

BBUUGGSS
       The value returned on error is also a legitimate data value.  ffeerrrroorr(3)
       can be used to distinguish between the two cases.

SSEEEE AALLSSOO
       ffeerrrroorr(3), ffrreeaadd(3), ffwwrriittee(3), ggeettcc(3), ppuuttcc(3)

Linux man‐pages (unreleased)      2025‐05‐17                           _g_e_t_w(3)
