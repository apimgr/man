TIMER_DELETE(2)             BSD System Calls Manual            TIMER_DELETE(2)

NNAAMMEE
     ttiimmeerr__ddeelleettee — delete a per-process timer (REALTIME)

LLIIBBRRAARRYY
     POSIX Real-time Library (librt, -lrt)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ttiimmee..hh>>

     _i_n_t
     ttiimmeerr__ddeelleettee(_t_i_m_e_r___t _t_i_m_e_r_i_d);

DDEESSCCRRIIPPTTIIOONN
     The ttiimmeerr__ddeelleettee() system call deletes the specified timer, _t_i_m_e_r_i_d, pre‐
     viously created by the timer_create(2) system call.  If the timer is
     armed when ttiimmeerr__ddeelleettee() is called, the behavior is as if the timer is
     automatically disarmed before removal.  Pending signals for the deleted
     timer are cleared.

RREETTUURRNN VVAALLUUEESS
     Upon successful completion, the value 0 is returned; otherwise the
     value -1 is returned and the global variable _e_r_r_n_o is set to indicate the
     error.

EERRRROORRSS
     The ttiimmeerr__ddeelleettee() system call will fail if:

     [EINVAL]           The timer ID specified by _t_i_m_e_r_i_d is not a valid timer
                        ID.

SSEEEE AALLSSOO
     timer_create(2)

SSTTAANNDDAARRDDSS
     The ttiimmeerr__ddeelleettee() system call conforms to IEEE Std 1003.1-2004
     (“POSIX.1”).

HHIISSTTOORRYY
     Support for POSIX per-process timer first appeared in FreeBSD 7.0.

BSD                           September 11, 2000                           BSD
