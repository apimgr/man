The named semaphore named is removed.  If the semaphore is in use
by  other  processes,  then is immediately disassociated with the
semaphore, but the semaphore itself will not be removed until all
references to it have been closed.   Subsequent  calls  to  using
will refer to or create a new semaphore named If successful, will
return 0.  Otherwise, ‐1 is returned and is set, and the state of
the  semaphore  is unchanged.  succeeds unless: Permission is de‐
nied to be remove the semaphore.  exceeded characters.  The named
semaphore does not exist.  is specified in the POSIX Realtime Ex‐
tension (1003.1b‐1993/1003.1i‐1995).
