CAP_NETDB(3)             BSD Library Functions Manual             CAP_NETDB(3)

NNAAMMEE
     ccaapp__ggeettpprroottoobbyynnaammee, — library for getting network proto entry in capabil‐
     ity mode

LLIIBBRRAARRYY
     library “libcap_netdb”

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//nnvv..hh>>
     ##iinncclluuddee <<lliibbccaassppeerr..hh>>
     ##iinncclluuddee <<ccaassppeerr//ccaapp__nneettddbb..hh>>

     _s_t_r_u_c_t _p_r_o_t_o_e_n_t _*
     ccaapp__ggeettpprroottoobbyynnaammee(_c_o_n_s_t _c_a_p___c_h_a_n_n_e_l___t _*_c_h_a_n, _c_o_n_s_t _c_h_a_r _*_n_a_m_e);

DDEESSCCRRIIPPTTIIOONN
     The function ccaapp__ggeettpprroottoobbyynnaammee() is equivalent to getprotobyname(3) ex‐
     cept that the connection to the ssyysstteemm..nneettddbb service needs to be pro‐
     vided.  It is reentrant but not thread-safe.  That is, it may be called
     from separate threads only with different _c_a_p___c_h_a_n_n_e_l___t arguments or with
     synchronization.

EEXXAAMMPPLLEESS
     The following example first opens a capability to casper and then uses
     this capability to create the ssyysstteemm..nneettddbb casper service and uses it to
     look up a protocol by name.

     cap_channel_t *capcas, *capnetdb;
     struct protoent *ent;

     /* Open capability to Casper. */
     capcas = cap_init();
     if (capcas == NULL)
             err(1, "Unable to contact Casper");

     /* Enter capability mode sandbox. */
     if (caph_enter() < 0)
             err(1, "Unable to enter capability mode");

     /* Use Casper capability to create capability to the system.netdb service. */
     capnetdb = cap_service_open(capcas, "system.netdb");
     if (capnetdb == NULL)
             err(1, "Unable to open system.netdb service");

     /* Close Casper capability, we don't need it anymore. */
     cap_close(capcas);

     ent = cap_getprotobyname(capnetdb, "http");
     if (ent == NULL)
            errx(1, "cap_getprotobyname failed to find http proto");

SSEEEE AALLSSOO
     cap_enter(2), caph_enter(3), err(3), getprotobyname(3), capsicum(4),
     nv(9)

AAUUTTHHOORRSS
     The ccaapp__nneettddbb service was implemented by Ryan Moeller
     <_f_r_e_q_l_a_b_s_@_F_r_e_e_B_S_D_._o_r_g>.

BSD                            December 6, 2023                            BSD
