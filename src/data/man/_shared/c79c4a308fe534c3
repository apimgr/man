BRK(2)                      BSD System Calls Manual                     BRK(2)

NNAAMMEE
     bbrrkk, ssbbrrkk — change data segment size

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<uunniissttdd..hh>>

     _i_n_t
     bbrrkk(_c_o_n_s_t _v_o_i_d _*_a_d_d_r);

     _v_o_i_d _*
     ssbbrrkk(_i_n_t_p_t_r___t _i_n_c_r);

DDEESSCCRRIIPPTTIIOONN
     TThhee bbrrkk() aanndd ssbbrrkk() ffuunnccttiioonnss aarree lleeggaaccyy iinntteerrffaacceess ffrroomm bbeeffoorree tthhee aadd‐‐
     vveenntt ooff mmooddeerrnn vviirrttuuaall mmeemmoorryy mmaannaaggeemmeenntt..  TThheeyy aarree ddeepprreeccaatteedd aanndd nnoott
     pprreesseenntt oonn tthhee aarrmm6644 oorr rriissccvv aarrcchhiitteeccttuurreess..  TThhee mmap(22) iinntteerrffaaccee
     sshhoouulldd bbee uusseedd ttoo aallllooccaattee ppaaggeess iinnsstteeaadd..

     The bbrrkk() and ssbbrrkk() functions are used to change the amount of memory
     allocated in a process's data segment.  They do this by moving the loca‐
     tion of the “break”.  The break is the first address after the end of the
     process's uninitialized data segment (also known as the “BSS”).

     The bbrrkk() function sets the break to _a_d_d_r.

     The ssbbrrkk() function raises the break by _i_n_c_r bytes, thus allocating at
     least _i_n_c_r bytes of new memory in the data segment.  If _i_n_c_r is negative,
     the break is lowered by _i_n_c_r bytes.

NNOOTTEESS
     While the actual process data segment size maintained by the kernel will
     only grow or shrink in page sizes, these functions allow setting the
     break to unaligned values (i.e., it may point to any address inside the
     last page of the data segment).

     The current value of the program break may be determined by calling
     ssbbrrkk(_0).  See also end(3).

     The getrlimit(2) system call may be used to determine the maximum permis‐
     sible size of the data segment.  It will not be possible to set the break
     beyond “_e_t_e_x_t + _r_l_i_m_._r_l_i_m___m_a_x” where the _r_l_i_m_._r_l_i_m___m_a_x value is returned
     from a call to ggeettrrlliimmiitt(_R_L_I_M_I_T___D_A_T_A, _&_r_l_i_m).  (See end(3) for the defi‐
     nition of _e_t_e_x_t).

RREETTUURRNN VVAALLUUEESS
     The bbrrkk() function returns the value 0 if successful; otherwise the
     value -1 is returned and the global variable _e_r_r_n_o is set to indicate the
     error.

     The ssbbrrkk() function returns the prior break value if successful; other‐
     wise the value (_v_o_i_d _*)-1 is returned and the global variable _e_r_r_n_o is
     set to indicate the error.

EERRRROORRSS
     The bbrrkk() and ssbbrrkk() functions will fail if:

     [EINVAL]           The requested break value was beyond the beginning of
                        the data segment.

     [ENOMEM]           The data segment size limit, as set by setrlimit(2),
                        was exceeded.

     [ENOMEM]           Insufficient space existed in the swap area to support
                        the expansion of the data segment.

SSEEEE AALLSSOO
     execve(2), getrlimit(2), mmap(2), end(3), free(3), malloc(3)

HHIISSTTOORRYY
     The bbrrkk() function appeared in Version 7 AT&T UNIX.  FreeBSD 11.0 intro‐
     duced the arm64 and riscv architectures which do not support bbrrkk() or
     ssbbrrkk().

BBUUGGSS
     Mixing bbrrkk() or ssbbrrkk() with malloc(3), free(3), or similar functions will
     result in non-portable program behavior.

     Setting the break may fail due to a temporary lack of swap space.  It is
     not possible to distinguish this from a failure caused by exceeding the
     maximum size of the data segment without consulting getrlimit(2).

     ssbbrrkk() is sometimes used to monitor heap use by calling with an argument
     of 0.  The result is unlikely to reflect actual utilization in combina‐
     tion with an mmap(2) based malloc.

     bbrrkk() and ssbbrrkk() are not thread-safe.

BSD                              June 2, 2018                              BSD
