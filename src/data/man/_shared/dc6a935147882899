finds  the  server for a particular domain and stores information
about it in a This binding information includes the IP address of
the server associated with that particular domain and which  port
the server is using.  This information is stored in the directory
in a file named with the convention where is the relevant domain.
The system only supplies information on version 2.  If is started
without  the  option, steps through the list of servers specified
in and contacts each in turn attempting to bind to  that  server.
It  is  strongly  recommended  that  these hosts are in the local
hosts file, and that hosts are looked up in  local  files  before
the hosts map.  If is started with the option, or if does not ex‐
ist,  broadcasts  to find a process willing to serve maps for the
client’s domain.  Once a binding is established,  maintains  this
binding by periodically communicating with the server to which it
is  bound.  If the binding is somehow lost, e.g by server reboot,
marks the domain as unbound  and  attempts  to  re‐establish  the
binding.   If  a  binding cannot be re‐established within 60 sec‐
onds, backs off exponentially to trying only once per hour.   The
options  are as follows: sends a broadcast requesting a server to
which to bind.  do not require that the server is  running  on  a
reserved  port.   This  may  be  necessary  when connecting to or
servers.  may be used from anywhere to change the server to which
a domain is bound.  may be used only from this machine to  change
the  server  to which a domain is bound.  The and options are in‐
herently insecure and should be avoided.  responds to the follow‐
ing signals: causes to immediately retry any unbound domains that
are currently in exponential backoff.  Use this to resume immedi‐
ately after a long network outage is resolved.   ‐  binding  file
for  <domain>.   ‐  explicit  list of servers to bind to for <do‐
main>.  Messages are sent to using the facility.  This version of
was originally implemented by The ypservers  support  was  imple‐
mented by
