NG_VLAN(4)               BSD Kernel Interfaces Manual               NG_VLAN(4)

NNAAMMEE
     nngg__vvllaann — IEEE 802.1Q VLAN tagging netgraph node type

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ttyyppeess..hh>>
     ##iinncclluuddee <<nneettggrraapphh..hh>>
     ##iinncclluuddee <<nneettggrraapphh//nngg__vvllaann..hh>>

DDEESSCCRRIIPPTTIIOONN
     The vvllaann node type multiplexes frames tagged according to the IEEE 802.1Q
     standard between different hooks.

     Each node has two special hooks, _d_o_w_n_s_t_r_e_a_m and _n_o_m_a_t_c_h, and an arbitrary
     number of “vlan” hooks, each associated with a particular VLAN tag.

     An ETHERTYPE_VLAN frame received on the _d_o_w_n_s_t_r_e_a_m hook with a tag that
     the node has been configured to filter is sent out the corresponding
     “vlan” hook.  If it does not match any of the configured tags, or is not
     of a type ETHERTYPE_VLAN, it is sent out the _n_o_m_a_t_c_h hook.  If the
     _n_o_m_a_t_c_h hook is not connected, the packet is dropped.

     An Ethernet frame received on the _n_o_m_a_t_c_h hook is passed unmodified to
     the _d_o_w_n_s_t_r_e_a_m hook.

     An Ethernet frame received on any of the “vlan” hooks is tagged accord‐
     ingly and sent out the _d_o_w_n_s_t_r_e_a_m hook.

HHOOOOKKSS
     This node type supports the following hooks:

     _d_o_w_n_s_t_r_e_a_m            Typically this hook would be connected to a
                           ng_ether(4) node, using the _l_o_w_e_r hook.

     _n_o_m_a_t_c_h               Typically this hook would also be connected to an
                           ng_ether(4) type node using the _u_p_p_e_r hook.

     ⟨_a_n_y _v_a_l_i_d _n_a_m_e⟩      Any other hook name will be accepted and should
                           later be associated with a particular tag.  Typi‐
                           cally this hook would be attached to an
                           ng_eiface(4) type node using the _e_t_h_e_r hook.

CCOONNTTRROOLL MMEESSSSAAGGEESS
     This node type supports the generic control messages, plus the following:

     NGM_VLAN_ADD_FILTER (aaddddffiilltteerr)
          Associates a hook with the tag.

     NGM_VLAN_DEL_FILTER (ddeellffiilltteerr)
          Disassociates a hook from the tag.

     NGM_VLAN_GET_TABLE (ggeettttaabbllee)
          Returns a table of all hook/tag associations.

EEXXAAMMPPLLEESS
     #!/bin/sh

     ETHER_IF=rl0

     ngctl -f- <<EOF
     shutdown ${ETHER_IF}:
     mkpeer ${ETHER_IF}: vlan lower downstream
     name ${ETHER_IF}:lower vlan
     connect ${ETHER_IF}: vlan: upper nomatch
     EOF

     ngctl mkpeer vlan: eiface vlan123 ether
     ngctl msg vlan: addfilter '{ vlan=123 hook="vlan123" }'

SSHHUUTTDDOOWWNN
     This node shuts down upon receipt of a NGM_SHUTDOWN control message, or
     when all hooks have been disconnected.

SSEEEE AALLSSOO
     netgraph(4), ng_eiface(4), ng_ether(4), ngctl(8), nghook(8)

HHIISSTTOORRYY
     The nngg__vvllaann node type appeared in FreeBSD 4.10.

AAUUTTHHOORRSS
     Ruslan Ermilov <_r_u_@_F_r_e_e_B_S_D_._o_r_g>

BSD                              March 1, 2004                             BSD
