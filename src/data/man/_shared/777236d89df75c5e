SCTP_GETASSOCID(3)       BSD Library Functions Manual       SCTP_GETASSOCID(3)

NNAAMMEE
     ssccttpp__ggeettaassssoocciidd — return an association id for a specified socket address

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ttyyppeess..hh>>
     ##iinncclluuddee <<ssyyss//ssoocckkeett..hh>>
     ##iinncclluuddee <<nneettiinneett//ssccttpp..hh>>

     _s_c_t_p___a_s_s_o_c___t
     ssccttpp__ggeettaassssoocciidd(_i_n_t _s, _s_t_r_u_c_t _s_o_c_k_a_d_d_r _*_a_d_d_r);

DDEESSCCRRIIPPTTIIOONN
     The ssccttpp__ggeettaassssoocciidd() call attempts to look up the specified socket ad‐
     dress _a_d_d_r and find the respective association identification.

RREETTUURRNN VVAALLUUEESS
     The call returns the association id upon success and 0 is returned upon
     failure.

EERRRROORRSS
     The ssccttpp__ggeettaassssoocciidd() function can return the following errors:

     [ENOENT]           The address does not have an association setup to it.

     [EBADF]            The argument _s is not a valid descriptor.

     [ENOTSOCK]         The argument _s is not a socket.

SSEEEE AALLSSOO
     sctp(4)

BSD                            December 15, 2006                           BSD
