duplicates an existing object descriptor and returns its value to
the  calling process = The argument is a small non‐negative inte‐
ger index in the per‐process descriptor table.  The value must be
less than the size of the table, which is returned by The new de‐
scriptor returned by the call is the lowest  numbered  descriptor
currently  not  in  use by the process.  The object referenced by
the descriptor does not distinguish between and in any way.  Thus
if and are duplicate references to an open file,  and  calls  all
move  a single pointer into the file, and append mode, non‐block‐
ing I/O and asynchronous I/O options are shared between the  ref‐
erences.   If a separate pointer into the file is desired, a dif‐
ferent object reference to the file must be obtained  by  issuing
an  additional  call.  The close‐on‐exec flag on the new file de‐
scriptor is unset.  In the value of the new descriptor is  speci‐
fied.   If  and are equal, then just returns no other changes are
made to the existing descriptor.  Otherwise, if descriptor is al‐
ready in use, it is first deallocated as if a call had been  done
first.   Upon  successful  completion, the new file descriptor is
returned.  Otherwise, a value of ‐1 is returned  and  the  global
integer  variable  is  set to indicate the error.  The and system
calls will fail if: is not an active, valid file descriptor.  Ex‐
ecution is interrupted by a signal.  Too  many  file  descriptors
are active.  The system call will fail if: is negative or greater
than  the  maximum  allowable number (see getdtablesize(2)).  and
are expected to conform to
