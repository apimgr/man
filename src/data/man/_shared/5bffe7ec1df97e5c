The  interface  is  a  software  loopback  mechanism  that can be
loosely described as the network interface analog of the that is,
does for network interfaces what the driver does  for  terminals.
The  driver,  like the driver, provides two interfaces: an inter‐
face like the usual facility it is simulating a network interface
in the case of or a terminal for and a  character‐special  device
interface.   To use a device, the administrator must first create
the interface.  This can be done by using the command, or via the
ioctl.  An call on will also create a network interface with  the
same  unit  number  of  that device if it doesn’t exist yet.  The
network interfaces should be named etc.  Each interface  supports
the usual network‐interface such as and and thus can be used with
like any other interface.  At boot time, they are interfaces, but
this  can  be changed; see the description of the control device,
below.  When the system chooses to transmit a packet on the  net‐
work interface, the packet can be read from the control device it
appears there as writing a packet to the control device generates
an  input packet on the network interface, as if the hardware had
just received it.  The tunnel device, normally is  exclusive‐open
it  cannot  be  opened if it is already open and is restricted to
the super‐user A call will return an error if  the  interface  is
not  (which  means  that the interface address has not been set).
Once the interface is ready, will  return  a  packet  if  one  is
available;  if  not,  it will either block until one is or return
depending on whether non‐blocking I/O has been enabled.   If  the
packet  is longer than is allowed for in the buffer passed to the
extra data will be silently dropped.  Packets can  be  optionally
prepended  with  the destination address as presented to the net‐
work interface output routine The destination address is in  for‐
mat.  The actual length of the prepended address is in the member
The  packet  data follows immediately.  A call passes a packet in
to be on the pseudo‐interface.  Each call  supplies  exactly  one
packet;  the  packet length is taken from the amount of data pro‐
vided to Writes will not block; if the packet cannot be  accepted
for  a  transient reason it is silently dropped; if the reason is
not transient an error is returned.  If is on the  actual  packet
data must be preceded by a The driver currently only inspects the
field.   The following calls are supported The argument should be
a pointer to an this sets the internal debugging variable to that
value.  What, if anything, this variable controls  is  not  docu‐
mented  here;  see  the  source  code.   The argument should be a
pointer to an this stores the internal debugging variable’s value
into it.  The argument should be a pointer to an its  value  must
be  either or (optionally may be or’ed into the value).  The type
of the corresponding interface is set to the supplied  type.   If
the  value is anything else, an error occurs.  The interface must
be down at the time; if it is up, an error occurs.  The  argument
should  be  a  pointer  to an a non‐zero value turns off mode and
turns on mode, causing packets read from the tunnel device to  be
prepended  with network destination address.  The argument should
be a pointer to an the ioctl sets the value to one if the  device
is in mode, and zero otherwise.  The argument should be a pointer
to  an  a  non‐zero value turns off mode, and enables mode, where
every packet is preceded with a four byte address  family.   Turn
non‐blocking  I/O  for reads off or on, according as the argument
value is or isn’t zero.  Turn asynchronous I/O  for  reads  i.e.,
generation of when data is available to be read value is or isn’t
zero.   If  any  packets are queued to be read, store the size of
the first one into the argument otherwise, store zero.   Set  the
process  group  to  receive signals, when asynchronous I/O is en‐
abled, to the argument value.  Retrieve the process  group  value
for  signals  into  the  argument value.  The control device also
supports for read; selecting for write is pointless,  and  always
succeeds,  since  writes  are  always  non‐blocking.  On the last
close of the data device, by default, the  interface  is  brought
down  All queued packets are thrown away.  If the interface is up
when the data device is not open output packets are always thrown
away rather than letting them pile up.  When an  application  has
opened  the character device the link is considered up, otherwise
down.  As such, it is best to open the character device once con‐
nectivity has been established so that Duplicate  Address  Detec‐
tion,  if applicable, can be performed.  If connectivity is lost,
the character device should be closed.  IPv6 support comes mostly
from and was added in by
