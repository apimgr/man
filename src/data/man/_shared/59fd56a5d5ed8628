displays  the  kernel trace files produced with in human readable
format.  The file in the current directory is  displayed,  unless
either the option is used, or a file name is supplied as the last
argument.   The  options  are  as follows: Display all numbers in
decimal.  Display elapsed timestamps  (time  since  beginning  of
trace).   If an emulation of a process is unknown, interpret sys‐
tem call maps assuming the named  emulation  instead  of  default
"netbsd".  Display the specified file instead of Loop reading the
trace  file,  once  the  end‐of‐file is reached, waiting for more
data.  Display at most bytes when decoding I/O.  Suppress  system
call  number‐to‐name  translation.  Suppress ad hoc translations.
Normally tries to decode many system  calls  into  a  more  human
readable format.  For example, values are replaced with the macro
name  and  values are replaced with the string.  Suppressing this
feature yields a more consistent  output  format  and  is  easily
amenable  to  further  processing.  Only display records from the
trace file that are for  the  indicated  pid.   Display  relative
timestamps  (time  since previous entry).  Display absolute time‐
stamps for each entry (seconds since epoch).  Restrict display to
the specified set of kernel trace points.  The default is to dis‐
play everything in the file.  See the option of Display GIO  data
in hex and ascii instead of format.  Same as but display hex val‐
ues by groups of bytes.  Supported values are 1, 2, 4, 8, and 16.
The command appeared in
