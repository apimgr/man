RESIZEWIN(1)              BSD General Commands Manual             RESIZEWIN(1)

NNAAMMEE
     rreessiizzeewwiinn — update terminal size

SSYYNNOOPPSSIISS
     rreessiizzeewwiinn [--zz]

DDEESSCCRRIIPPTTIIOONN
     The rreessiizzeewwiinn utility queries the terminal emulator for the current win‐
     dow size and updates the size known to the kernel using the TIOCSWINSZ
     ioctl.

     The following options are available:

     --zz  Do nothing unless the current kernel terminal size is zero.  This is
         useful when run from a user's profile (shell startup) scripts: query‐
         ing the window size is required for serial lines, but not when log‐
         ging in over the network, as protocols like TELNET or SSH already
         handle the terminal size by themselves.

     After a terminal window has been resized, running rreessiizzeewwiinn updates the
     kernel's window size to match the new size.

     rreessiizzeewwiinn is functionally similar to resize(1), which is part of the
     xterm(1) (_p_o_r_t_s_/_x_1_1_/_x_t_e_r_m) distribution.  However, rreessiizzeewwiinn only works
     with VT100/ANSI-compatible terminals and does not emit commands to set
     environment variables.

     The terminal is assumed to be VT100/ANSI compatible.  The VT100/ANSI es‐
     cape sequences are supported by virtually all modern terminals, including
     xterm, konsole, gnome-terminal, iTerm, Terminal.app, and PuTTY.

SSEEEE AALLSSOO
     stty(1), tty(4)

HHIISSTTOORRYY
     The rreessiizzeewwiinn command first appeared in FreeBSD 11.0.

BSD                               May 9, 2017                              BSD
