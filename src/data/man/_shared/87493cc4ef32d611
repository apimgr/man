The  system  call  deletes the mappings for the specified address
range, causing further references to addresses within  the  range
to generate invalid memory references.  How handles a dirty page,
depends  on  what type of memory is being unmapped: If the memory
is anonymous memory and if the last reference is going away, then
the contents are discarded by definition of anonymous memory.  If
the memory mapping was created using System V shared memory, then
the contents persist until the System  V  memory  region  is  de‐
stroyed or the system is rebooted.  If the mapping maps data from
a  file  (MAP_SHARED), then the memory will eventually be written
back to disk if it’s dirty.  This will  happen  automatically  at
some  point  in  the future (implementation dependent).  Note: to
force the memory to be written back to the disk, use If there are
still other references to the memory when  the  munmap  is  done,
then  nothing  is done to the memory itself and it may be swapped
out if need be. The memory will continue  to  persist  until  the
last  reference  goes  away (except for System V shared memory in
which case, see  above).   Upon  successful  completion,  returns
zero.   Otherwise,  a value of ‐1 is returned and is set to indi‐
cate the error.  will fail if: The parameter was not page aligned
(i.e., a multiple of the page size).  The parameter was  negative
or  zero.   Some part of the region being unmapped is not part of
the currently valid address space.  The include  file  is  neces‐
sary.
The type of has changed.  The function first appeared in 4.4BSD.
