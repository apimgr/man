The  utility sets discretionary access control information on the
specified file(s).  If no files are specified, or the  list  con‐
sists  of the only the file names are taken from the standard in‐
put.  The following options are available: Modify the ACL on  the
specified  files by inserting new ACL entries specified in start‐
ing at position counting from zero.  This option is only applica‐
ble to NFSv4 ACLs.  Remove all ACL entries except  for  the  ones
synthesized  from  the file mode ‐ the three mandatory entries in
case of POSIX.1e ACL.  If the POSIX.1e ACL contains a entry,  the
permissions  of the entry in the resulting ACL will be set to the
permission associated with both the and entries  of  the  current
ACL.   The operations apply to the default ACL entries instead of
access ACL entries.  Currently only directories may have  default
ACL’s.   This option is not applicable to NFSv4 ACLs.  If the op‐
tion is specified, symbolic links on the command  line  are  fol‐
lowed  and  hence unaffected by the command.  (Symbolic links en‐
countered during tree traversal are not followed.)  If the target
of the operation is a symbolic link, perform the operation on the
symbolic link itself, rather than following the link.  Delete any
default ACL entries on the specified files.  It is not considered
an error if the specified files do not have any default  ACL  en‐
tries.   An  error will be reported if any of the specified files
cannot have a default entry (i.e., non‐directories).  This option
is not applicable to NFSv4 ACLs.  If the option is specified, all
symbolic links are followed.  Modify the ACL entries on the spec‐
ified files by adding new ACL entries and modifying existing  ACL
entries  with the ACL entries specified in the file If is the in‐
put is taken from stdin.  Modify the ACL on the  specified  file.
New  entries will be added, and existing entries will be modified
according to the argument.  For NFSv4 ACLs, it is recommended  to
use  the and options instead.  Do not recalculate the permissions
associated with the ACL mask entry.  This option is not  applica‐
ble to NFSv4 ACLs.  If the option is specified, no symbolic links
are  followed.   This  is the default.  Perform the action recur‐
sively on any specified directories.  When  modifying  or  adding
NFSv4 ACL entries, inheritance flags are applied only to directo‐
ries.   Remove the ACL entries specified in the file from the ac‐
cess or default ACL of the specified files.  If is specified, re‐
move the ACL entries specified there from the access  or  default
ACL  of  the  specified  files.  Otherwise, remove entry at index
counting from zero.  The above options are evaluated in the order
specified on the command‐line.  A  POSIX.1E  ACL  entry  contains
three  colon‐separated  fields: an ACL tag, an ACL qualifier, and
discretionary access permissions: The ACL tag specifies  the  ACL
entry  type  and  consists of one of the following: or specifying
the access granted to the owner of the file or a specified  user;
or  specifying  the  access granted to the file owning group or a
specified group; or specifying the access granted to any  process
that  does  not  match any user or group ACL entry; or specifying
the maximum access granted to any ACL entry except the ACL  entry
for  the  file  owner and the ACL entry.  The ACL qualifier field
describes the user or group associated with the  ACL  entry.   It
may  consist  of  one  of the following: UID or user name, GID or
group name, or empty.  For ACL entries, an empty field  specifies
access  granted  to  the  file  owner.  For ACL entries, an empty
field specifies access granted to the file owning group.  and ACL
entries do not use this field.  The access permissions field con‐
tains up to one of each of the following: and to set read, write,
and execute permissions, respectively.  Each of these may be  ex‐
cluded or replaced with a character to indicate no access.  A ACL
entry  is  required on a file with any ACL entries other than the
default and ACL entries.  If the option is not specified  and  no
ACL  entry was specified, the utility will apply a ACL entry con‐
sisting of the union of the permissions associated with  all  ACL
entries  in the resulting ACL.  Traditional POSIX interfaces act‐
ing on file system object modes have modified  semantics  in  the
presence of POSIX.1e extended ACLs.  When a mask entry is present
on the access ACL of an object, the mask entry is substituted for
the  group bits; this occurs in programs such as or When the mode
is modified on an object that has a mask entry, the  changes  ap‐
plied  to the group bits will actually be applied to the mask en‐
try.  These semantics provide for greater application compatibil‐
ity: applications modifying the mode instead of the ACL will  see
conservative  behavior,  limiting the effective rights granted by
all of the additional user and group entries; this occurs in pro‐
grams such as ACL entries applied from a file using  the  or  op‐
tions  shall be of the following form: one ACL entry per line, as
previously specified; whitespace is ignored; any text after a  is
ignored (comments).  When POSIX.1e ACL entries are evaluated, the
access  check  algorithm  checks the ACL entries in the following
order: file owner, ACL entries, file owning group,  ACL  entries,
and  ACL  entry.   Multiple  ACL entries specified on the command
line are separated by commas.  It is possible for files  and  di‐
rectories  to  inherit  ACL  entries from their parent directory.
This is accomplished through the use  of  the  default  ACL.   It
should  be  noted  that before you can specify a default ACL, the
mandatory ACL entries for user, group, other  and  mask  must  be
set.   For more details see the examples below.  Default ACLs can
be created by using An NFSv4 ACL  entry  contains  four  or  five
colon‐separated  fields:  an  ACL tag, an ACL qualifier (only for
and tags),  discretionary  access  permissions,  ACL  inheritance
flags, and ACL type: The ACL tag specifies the ACL entry type and
consists  of  one  of  the  following:  or  specifying the access
granted to the specified user; or specifying the  access  granted
to  the  specified  group;  specifying  the access granted to the
owner of the file; specifying the access granted to the file own‐
ing group; specifying everyone.  Note that is  not  the  same  as
traditional  Unix ‐ it means, literally, everyone, including file
owner and owning group.  The ACL qualifier  field  describes  the
user  or  group associated with the ACL entry.  It may consist of
one of the following: UID or user name, or GID or group name.  In
entries whose tag type is one of or this field is  omitted  alto‐
gether,  including the trailing comma.  Access permissions may be
specified in either short or long form.  Short and long forms may
not be mixed.  Permissions in long  form  are  separated  by  the
character;  in short form, they are concatenated together.  Valid
permissions are: Long read_data  write_data  execute  append_data
delete_child  delete  read_attributes write_attributes read_xattr
write_xattr read_acl write_acl write_owner synchronize  In  addi‐
tion,  the following permission sets may be used: Permissions all
permissions, as shown above all permissions except write_acl  and
write_owner  read_data,  read_attributes, read_xattr and read_acl
write_data, append_data, write_attributes and write_xattr Inheri‐
tance flags may be specified in either short or long form.  Short
and long forms may not be mixed.  Access flags in long  form  are
separated  by the character; in short form, they are concatenated
together.  Valid inheritance flags are: Long file_inherit dir_in‐
herit inherit_only no_propagate inherited Other than the  "inher‐
ited"  flag,  inheritance  flags  may be only set on directories.
The ACL type field is either or ACL entries applied from  a  file
using  the or options shall be of the following form: one ACL en‐
try per line, as previously specified; whitespace is ignored; any
text after a is ignored (comments).  NFSv4 ACL entries are evalu‐
ated in their visible order.  Multiple ACL entries  specified  on
the  command  line  are  separated by commas.  Note that the file
owner is always granted the read_acl, write_acl, read_attributes,
and write_attributes permissions, even if the ACL would deny  it.
The first command sets the mandatory elements of the POSIX.1e de‐
fault  ACL.  The second command specifies that users in group ad‐
mins can have read, write, and execute permissions for  directory
named  "dir".   It  should be noted that any files or directories
created underneath "dir" will inherit  these  default  ACLs  upon
creation.   Sets  read,  write,  and  execute permissions for the
owner’s POSIX.1e ACL entry and read  and  write  permissions  for
group  mail  on  Semantically equal to the example above, but for
NFSv4 ACL.  Sets/updates the ACL entries contained in  on  Remove
the  group  mail POSIX.1e ACL entry containing read/write permis‐
sions from Remove the first entry from the NFSv4 ACL from  Remove
all  ACL  entries except for the three required from Copy ACL en‐
tries from to The utility is expected to be Std  1003.2c  compli‐
ant.   Extended Attribute and Access Control List support was de‐
veloped as part of the Project and introduced in NFSv4  ACL  sup‐
port  was introduced in The utility was written by NFSv4 ACL sup‐
port was implemented by
