LOGIN_PASSWD(8)           BSD System Manager's Manual          LOGIN_PASSWD(8)

NNAAMMEE
     llooggiinn__ppaasssswwdd — provide standard password authentication type

SSYYNNOOPPSSIISS
     llooggiinn__ppaasssswwdd [--ss _s_e_r_v_i_c_e] [--vv wwhheeeell=yyeess|nnoo] [--vv llaassttcchhaannccee=yyeess|nnoo] _u_s_e_r
                  [_c_l_a_s_s]

DDEESSCCRRIIPPTTIIOONN
     The llooggiinn__ppaasssswwdd utility is called by login(1), su(1), ftpd(8), and oth‐
     ers to authenticate the _u_s_e_r with passwd-style authentication.

     The _u_s_e_r argument is the login name of the user to be authenticated.

     The _s_e_r_v_i_c_e argument specifies which protocol to use with the invoking
     program.  The allowed protocols are _l_o_g_i_n, _c_h_a_l_l_e_n_g_e, and _r_e_s_p_o_n_s_e.  (The
     _c_h_a_l_l_e_n_g_e protocol is silently ignored but will report success as passwd-
     style authentication is not challenge-response based).

     If the wwhheeeell argument is specified and is not set to yyeess, then the user
     will be rejected as not being in group “wheel”.  This is used by su(1).

     If the llaassttcchhaannccee argument is specified and is equal to yyeess, then if the
     user's password has expired, and it has not been expired longer than
     “password-dead” seconds (see login.conf(5)), the user will be able to log
     in one last time to change the password.

     llooggiinn__ppaasssswwdd will prompt the user for a password and report back to the
     invoking program whether or not the authentication was successful.

SSEEEE AALLSSOO
     login(1), passwd(1), su(1), login.conf(5), ftpd(8)

BSD                            January 25, 2019                            BSD
