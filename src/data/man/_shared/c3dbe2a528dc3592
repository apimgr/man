[  hostname-or-IP  ...]  can be used as an SNTP client to query a
NTP or SNTP server and either display the time or set  the  local
system’s  time  (given  suitable privilege).  It can be run as an
interactive command or from a job.  NTP (the Network Time  Proto‐
col)  and SNTP (the Simple Network Time Protocol) are defined and
described by RFC 5905.  The default is  to  write  the  estimated
correct local date and time (i.e. not UTC) to the standard output
in a format like: where the means that to get to UTC from the re‐
ported  local  time one must add 8 hours and 0 minutes, the indi‐
cates the local clock is 4.567 seconds behind  the  correct  time
(so  4.567  seconds must be added to the local clock to get it to
be correct).  Note that the number of decimals printed  for  this
value  will change based on the reported precision of the server.
is the reported (in seconds), which represents the maximum  error
due  to  all  causes.   If  the server does not report valid data
needed to calculate the synchronization distance,  this  will  be
reported  as If the is different from the both will be displayed.
Otherwise, only the is displayed.  Finally, the of  the  host  is
reported  and the leap indicator is decoded and displayed.  Force
IPv4 DNS name resolution.  This option must not appear in  combi‐
nation with any of the following options: ipv6.

Force  DNS  resolution of the following host names on the command
line to the IPv4 namespace.   Force  IPv6  DNS  name  resolution.
This  option  must not appear in combination with any of the fol‐
lowing options: ipv4.

Force DNS resolution of the following host names on  the  command
line  to  the IPv6 namespace.  Enable authentication with the key
aauutthh--kkeeyynnuummbbeerr.  This option takes an integer number as its argu‐
ment.

Enable authentication using the key specified  in  this  option’s
argument.   The  argument  of  this option is the kkeeyyiidd, a number
specified in the kkeeyyffiillee as this key’s identifier.  See the  kkeeyy‐‐
ffiillee  option (--kk) for more details.  Listen to the address speci‐
fied for broadcast time sync.  This option may appear  an  unlim‐
ited number of times.

If  specified  ssnnttpp  will listen to the specified address for NTP
broadcasts.  The default maximum  wait  time  can  (and  probably
should) be modified with --tt.  Concurrently query all IPs returned
for  host-name.   This  option  may appear an unlimited number of
times.

Requests from an NTP "client" to a "server" should never be  sent
more  rapidly  than one every 2 seconds.  By default, any IPs re‐
turned as part of a DNS lookup are assumed to be for a single in‐
stance of nnttppdd, and therefore ssnnttpp will send queries to these IPs
one after another, with a 2-second gap in between each query.

The --cc or ----ccoonnccuurrrreenntt flag says that any IPs  returned  for  the
DNS  lookup  of the supplied host-name are on different machines,
so we can send  concurrent  queries.   Increase  debug  verbosity
level.  This option may appear an unlimited number of times.

Set  the debug verbosity level.  This option may appear an unlim‐
ited number of times.  This option takes an integer number as its
argument.

The gap (in milliseconds) between  time  requests.   This  option
takes  an  integer  number as its argument.  The default for this
option is:
     50

Since we’re only going to use the first valid response we get and
there is benefit to specifying a good number of servers to query,
separate the queries we send out by the specified number of  mil‐
liseconds.   KoD  history  filename.  The default for this option
is:
     /var/db/ntp-kod

Specifies the filename to be used for the persistent  history  of
KoD responses received from servers.  If the file does not exist,
a  warning  message will be displayed.  The file will not be cre‐
ated.  Look in this file for the key specified with --aa.  The  de‐
fault for this option is:
     /etc/ntp.keys

This  option specifies the keyfile.  ssnnttpp will search for the key
specified with --aa _k_e_y_n_o in this file.  See nnttpp..kkeeyyss((55))  for  more
information.  Log to specified logfile.

This option causes the client to write log messages to the speci‐
fied  _l_o_g_f_i_l_e.   Adjustments  less  than  sstteepplliimmiitt  msec will be
slewed.  This option takes an integer  number  as  its  argument.
The value of is constrained to being:
    greater than or equal to 0

If the time adjustment is less than _s_t_e_p_l_i_m_i_t milliseconds, slew
the amount using aaddjjttiimmee((22)).  Otherwise, step the correction us‐
ing sseettttiimmeeooffddaayy((22)).  The default value is 0, which means all ad‐
justments will be stepped.  This is a feature, as different situ‐
ations demand different values.  Send iinntt as our NTP protocol
version.  This option takes an integer number as its argument.
The value of is constrained to being:
    in the range  0 through 7
The default for this option is:
     4

When sending requests to a remote server, tell them we are run‐
ning NTP protocol version _n_t_p_v_e_r_s_i_o_n .  Use the NTP Reserved Port
(port 123).

Use port 123, which is reserved for NTP, for our network communi‐
cations.  OK to ’step’ the time with sseettttiimmeeooffddaayy((22)).

OK to ’slew’ the time with aaddjjttiimmee((22)).

The number of seconds to wait for responses.  This option takes
an integer number as its argument.  The default for this option
is:
     5

When waiting for a reply, ssnnttpp will wait the number of seconds
specified before giving up.  The default should be more than
enough for a unicast response.  If ssnnttpp is only waiting for a
broadcast response a longer timeout is likely needed.  Wait for
pending replies (if not setting the time).  The _n_o_-_w_a_i_t form will
disable the option.  This option is enabled by default.

If we are not setting the time, wait for all pending responses.
Display usage information and exit.  Pass the extended usage in‐
formation through a pager.  Save the option state to _c_f_g_f_i_l_e.
The default is the _l_a_s_t configuration file listed in the OOPPTTIIOONN
PPRREESSEETTSS section, below.  The command will exit after updating the
config file.  Load options from _c_f_g_f_i_l_e.  The _n_o_-_l_o_a_d_-_o_p_t_s form
will disable the loading of earlier config/rc/ini files.
_-_-_n_o_-_l_o_a_d_-_o_p_t_s is handled early, out of order.  Output version of
program and exit.  The default mode is ‘v’, a simple version.
The ‘c’ mode will print copyright information and ‘n’ will print
the full copyright notice.  Any option that is not marked as _n_o_t
_p_r_e_s_e_t_t_a_b_l_e may be preset by loading values from configuration
("RC" or ".INI") file(s) and values from environment variables
named:
  SSNNTTPP__<<ooppttiioonn--nnaammee>> or SSNNTTPP
The  environmental  presets  take precedence (are processed later
than) the configuration files.  The _h_o_m_e_r_c files are "_$_H_O_M_E", and
"_.".  If any of these are directories, then the  file  _._n_t_p_r_c  is
searched  for  within  those directories.  is the simplest use of
this program and can be run as an unprivileged command  to  check
the  current  time  and  error in the local clock.  With suitable
privilege, run as a command or from a job, will request the  time
from  the  server, and if that server reports that it is synchro‐
nized then if the offset adjustment is less than 128 milliseconds
the correction will be slewed, and if the correction is more than
128 milliseconds the correction  will be stepped.  With  suitable
privilege,  run  as  a command or from a job, will set (step) the
local clock from a synchronized specified server, like the  (dep‐
recated) or commands.  See OOPPTTIIOONN PPRREESSEETTSS for configuration envi‐
ronment  variables.   See OOPPTTIIOONN PPRREESSEETTSS for configuration files.
One of the following exit values  will  be  returned:  Successful
program  execution.   The  operation failed or the command syntax
was not valid.  A  specified  configuration  file  could  not  be
loaded.   libopts  had an internal operational error.  Please re‐
port  it  to  autogen-users@lists.sourceforge.net.   Thank   you.
Copyright  (C)  1992-2020  The University of Delaware and Network
Time Foundation all rights reserved.  This  program  is  released
under  the  terms  of  the NTP license, <http://ntp.org/license>.
Please send bug  reports  to:  http://bugs.ntp.org,  bugs@ntp.org
This  manual page was _A_u_t_o_G_e_n-erated from the ssnnttpp option defini‐
tions.
