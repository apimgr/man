server  runs  on a server machine to service requests from client
machines.  At least one must be running for a machine to  operate
as a server.  Unless otherwise specified, four servers for trans‐
port  are  started.  The following options are available: Turn on
debugging, don’t fork and log to stderr.   Register  the  service
with without creating any servers.  This option can be used along
with  the  or options to re‐register NFS if the portmap server is
restarted.  Specifies how many server threads to create.  The de‐
fault is 4.  A server should run enough  threads  to  handle  the
maximum  level  of  concurrency from its clients.  Only listen to
IPv4 requests.  By default listens to  both  IPv6  and  IPv4  and
tries to register for both.  Only listen to IPv6 requests.  Serve
only  clients.   By  default  serves  both and clients Serve only
clients.  listens for service requests at the port  indicated  in
the  server specification; see RFC 1094 and serves and transports
using six threads.  The utility first appeared in
