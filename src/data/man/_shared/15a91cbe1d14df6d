The  program  displays statistics on the current state of the de‐
vice, and controls which sources are allowed to contribute to the
entropy pool maintained by The following options  are  available:
Disable  collection  of  data for the given device name or device
type.  Enable collection of data for the given device name or de‐
vice type.  Only the device named is altered or displayed.   This
is  mutually  exclusive with Ignore estimates of entropy from the
drivers for the given device name  or  type.   If  collection  is
still  enabled, data is still collected and mixed into the inter‐
nal entropy pool, but no entropy is assumed  to  be  present  and
data  from the selected devices will not unblock Accept estimates
of entropy from the drivers for the given device  name  or  type.
does  not change the estimate provided by the driver; if the dri‐
ver’s estimate is zero, as it generally is for devices  of  types
other  than  it remains zero after With the option to load a seed
from a file, ignore any estimate in the file of  the  entropy  of
the  seed.   This still loads the data into the kernel, but won’t
unblock even if the file claims to have adequate  entropy.   This
is  useful if the file is on a medium, such as an NFS share, that
the operator does not know to be secret.  Load a seed from gener‐
ated by Overwrite it with a seed derived by hashing  it  together
with  output  from  so that the new seed has at least as much en‐
tropy as either the old seed had or the system already  has.   If
interrupted,  either  the  old  seed  or  the new seed will be in
place.  List all sources, or, if the or flags are specified, only
those specified by the or specified.  Generate a  seed  from  the
system  entropy  pool  and save it to for later use with The file
format is specific to and includes an estimate of the  amount  of
saved  entropy  and  a checksum.  The prior internal state of the
system entropy pool cannot be recovered  from  so  disclosure  of
does  not compromise past secrets drawn from or equivalent.  Dis‐
play statistics on the current state of the  entropy  pool.   All
devices  of  type are altered or displayed.  This is mutually ex‐
clusive with The available types are: Physical hard drives.  Net‐
work interfaces.  Tape devices.  Terminal, mouse, or  other  user
input  devices.  Hardware random number generators.  Verbose out‐
put.  The program was first made available  in  The  program  was
written by
