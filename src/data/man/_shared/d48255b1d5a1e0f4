ELFTC_RELOC_TYPE_STR(3)  BSD Library Functions Manual  ELFTC_RELOC_TYPE_STR(3)

NNAAMMEE
     eellffttcc__rreelloocc__ttyyppee__ssttrr — return the type name for an ELF relocation

LLIIBBRRAARRYY
     library “libelftc”

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<lliibbeellffttcc..hh>>

     _c_o_n_s_t _c_h_a_r _*
     eellffttcc__rreelloocc__ttyyppee__ssttrr(_u_n_s_i_g_n_e_d _i_n_t _m_a_c_h, _u_n_s_i_g_n_e_d _i_n_t _t_y_p_e);

DDEESSCCRRIIPPTTIIOONN
     Function eellffttcc__rreelloocc__ttyyppee__ssttrr() returns the name for specified relocation
     type.

     Argument _m_a_c_h specifies the machine (architecture) type.  Argument _t_y_p_e
     specifies the relocation value.

RREETTUURRNN VVAALLUUEESS
     Function eellffttcc__pprrooggrraamm__vveerrssiioonn() returns a pointer to a string constant,
     or to an internal character buffer if the relocation type is unknown.

EEXXAAMMPPLLEESS
     To print ARM relocation type 7, use:

           #include <sys/types.h>
           #include <libelftc.h>
           #include <stdio.h>

           (void) printf("%s\n", elftc_reloc_type_str(EM_ARM, 7));

EERRRROORRSS
     Function eellffttcc__rreelloocc__ttyyppee__ssttrr() always succeeds.

BSD                            February 19, 2016                           BSD
