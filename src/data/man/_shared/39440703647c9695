parses  the  style input stream (stdin, or if given), and outputs
lines containing only the IPv4 address and host name.  is used by
other programs when creating some of the maps.  allows other  ad‐
dress types in the output, including IPv6 addresses.
