ZFS-LOAD-KEY(8)           BSD System Manager's Manual          ZFS-LOAD-KEY(8)

NNAAMMEE
     zzffss--llooaadd--kkeeyy — load, unload, or change encryption key of ZFS dataset

SSYYNNOOPPSSIISS
     zzffss llooaadd--kkeeyy [--nnrr] [--LL _k_e_y_l_o_c_a_t_i_o_n] --aa|_f_i_l_e_s_y_s_t_e_m
     zzffss uunnllooaadd--kkeeyy [--rr] --aa|_f_i_l_e_s_y_s_t_e_m
     zzffss cchhaannggee--kkeeyy [--ll] [--oo _k_e_y_l_o_c_a_t_i_o_n=_v_a_l_u_e] [--oo _k_e_y_f_o_r_m_a_t=_v_a_l_u_e]
         [--oo _p_b_k_d_f_2_i_t_e_r_s=_v_a_l_u_e] _f_i_l_e_s_y_s_t_e_m
     zzffss cchhaannggee--kkeeyy --ii [--ll] _f_i_l_e_s_y_s_t_e_m

DDEESSCCRRIIPPTTIIOONN
     zzffss llooaadd--kkeeyy [--nnrr] [--LL _k_e_y_l_o_c_a_t_i_o_n] --aa|_f_i_l_e_s_y_s_t_e_m
       Load the key for _f_i_l_e_s_y_s_t_e_m, allowing it and all children that inherit
       the kkeeyyllooccaattiioonn property to be accessed.  The key will be expected in
       the format specified by the kkeeyyffoorrmmaatt and location specified by the
       kkeeyyllooccaattiioonn property.  Note that if the kkeeyyllooccaattiioonn is set to pprroommpptt
       the terminal will interactively wait for the key to be entered.  Load‐
       ing a key will not automatically mount the dataset.  If that function‐
       ality is desired, zzffss mmoouunntt --ll will ask for the key and mount the
       dataset (see zfs-mount(8)).  Once the key is loaded the kkeeyyssttaattuuss prop‐
       erty will become aavvaaiillaabbllee.

       --rr  Recursively loads the keys for the specified filesystem and all de‐
           scendent encryption roots.

       --aa  Loads the keys for all encryption roots in all imported pools.

       --nn  Do a dry-run ("No-op") llooaadd--kkeeyy.  This will cause zzffss to simply
           check that the provided key is correct.  This command may be run
           even if the key is already loaded.

       --LL _k_e_y_l_o_c_a_t_i_o_n
           Use _k_e_y_l_o_c_a_t_i_o_n instead of the kkeeyyllooccaattiioonn property.  This will not
           change the value of the property on the dataset.  Note that if used
           with either --rr or --aa, _k_e_y_l_o_c_a_t_i_o_n may only be given as pprroommpptt.

     zzffss uunnllooaadd--kkeeyy [--rr] --aa|_f_i_l_e_s_y_s_t_e_m
       Unloads a key from ZFS, removing the ability to access the dataset and
       all of its children that inherit the kkeeyyllooccaattiioonn property.  This re‐
       quires that the dataset is not currently open or mounted.  Once the key
       is unloaded the kkeeyyssttaattuuss property will become uunnaavvaaiillaabbllee.

       --rr  Recursively unloads the keys for the specified filesystem and all
           descendent encryption roots.

       --aa  Unloads the keys for all encryption roots in all imported pools.

     zzffss cchhaannggee--kkeeyy [--ll] [--oo _k_e_y_l_o_c_a_t_i_o_n=_v_a_l_u_e] [--oo _k_e_y_f_o_r_m_a_t=_v_a_l_u_e] [--oo
       _p_b_k_d_f_2_i_t_e_r_s=_v_a_l_u_e] _f_i_l_e_s_y_s_t_e_m

     zzffss cchhaannggee--kkeeyy --ii [--ll] _f_i_l_e_s_y_s_t_e_m
       Changes the user's key (e.g. a passphrase) used to access a dataset.
       This command requires that the existing key for the dataset is already
       loaded.  This command may also be used to change the kkeeyyllooccaattiioonn,
       kkeeyyffoorrmmaatt, and ppbbkkddff22iitteerrss properties as needed.  If the dataset was
       not previously an encryption root it will become one.  Alternatively,
       the --ii flag may be provided to cause an encryption root to inherit the
       parent's key instead.

       If the user's key is compromised, zzffss cchhaannggee--kkeeyy does not necessarily
       protect existing or newly-written data from attack.  Newly-written data
       will continue to be encrypted with the same master key as the existing
       data.  The master key is compromised if an attacker obtains a user key
       and the corresponding wrapped master key.  Currently, zzffss cchhaannggee--kkeeyy
       does not overwrite the previous wrapped master key on disk, so it is
       accessible via forensic analysis for an indeterminate length of time.

       In the event of a master key compromise, ideally the drives should be
       securely erased to remove all the old data (which is readable using the
       compromised master key), a new pool created, and the data copied back.
       This can be approximated in place by creating new datasets, copying the
       data (e.g. using zzffss sseenndd | zzffss rreeccvv), and then clearing the free space
       with zzppooooll ttrriimm ----sseeccuurree if supported by your hardware, otherwise zzppooooll
       iinniittiiaalliizzee.

       --ll  Ensures the key is loaded before attempting to change the key.
           This is effectively equivalent to running zzffss llooaadd--kkeeyy _f_i_l_e_s_y_s_t_e_m;
           zzffss cchhaannggee--kkeeyy _f_i_l_e_s_y_s_t_e_m

       --oo _p_r_o_p_e_r_t_y=_v_a_l_u_e
           Allows the user to set encryption key properties (kkeeyyffoorrmmaatt,
           kkeeyyllooccaattiioonn, and ppbbkkddff22iitteerrss) while changing the key.  This is the
           only way to alter kkeeyyffoorrmmaatt and ppbbkkddff22iitteerrss after the dataset has
           been created.

       --ii  Indicates that zfs should make _f_i_l_e_s_y_s_t_e_m inherit the key of its
           parent.  Note that this command can only be run on an encryption
           root that has an encrypted parent.

   EEnnccrryyppttiioonn
     Enabling the eennccrryyppttiioonn feature allows for the creation of encrypted
     filesystems and volumes.  ZFS will encrypt file and volume data, file at‐
     tributes, ACLs, permission bits, directory listings, FUID mappings, and
     uusseerruusseedd/ggrroouuppuusseedd data.  ZFS will not encrypt metadata related to the
     pool structure, including dataset and snapshot names, dataset hierarchy,
     properties, file size, file holes, and deduplication tables (though the
     deduplicated data itself is encrypted).

     Key rotation is managed by ZFS.  Changing the user's key (e.g. a
     passphrase) does not require re-encrypting the entire dataset.  Datasets
     can be scrubbed, resilvered, renamed, and deleted without the encryption
     keys being loaded (see the llooaadd--kkeeyy subcommand for more info on key load‐
     ing).

     Creating an encrypted dataset requires specifying the eennccrryyppttiioonn and
     kkeeyyffoorrmmaatt properties at creation time, along with an optional kkeeyyllooccaattiioonn
     and ppbbkkddff22iitteerrss.  After entering an encryption key, the created dataset
     will become an encryption root.  Any descendant datasets will inherit
     their encryption key from the encryption root by default, meaning that
     loading, unloading, or changing the key for the encryption root will im‐
     plicitly do the same for all inheriting datasets.  If this inheritance is
     not desired, simply supply a kkeeyyffoorrmmaatt when creating the child dataset or
     use zzffss cchhaannggee--kkeeyy to break an existing relationship, creating a new en‐
     cryption root on the child.  Note that the child's kkeeyyffoorrmmaatt may match
     that of the parent while still creating a new encryption root, and that
     changing the eennccrryyppttiioonn property alone does not create a new encryption
     root; this would simply use a different cipher suite with the same key as
     its encryption root.  The one exception is that clones will always use
     their origin's encryption key.  As a result of this exception, some en‐
     cryption-related properties (namely kkeeyyssttaattuuss, kkeeyyffoorrmmaatt, kkeeyyllooccaattiioonn,
     and ppbbkkddff22iitteerrss) do not inherit like other ZFS properties and instead use
     the value determined by their encryption root.  Encryption root inheri‐
     tance can be tracked via the read-only eennccrryyppttiioonnrroooott property.

     Encryption changes the behavior of a few ZFS operations.  Encryption is
     applied after compression so compression ratios are preserved.  Normally
     checksums in ZFS are 256 bits long, but for encrypted data the checksum
     is 128 bits of the user-chosen checksum and 128 bits of MAC from the en‐
     cryption suite, which provides additional protection against maliciously
     altered data.  Deduplication is still possible with encryption enabled
     but for security, datasets will only deduplicate against themselves,
     their snapshots, and their clones.

     There are a few limitations on encrypted datasets.  Encrypted data cannot
     be embedded via the eemmbbeeddddeedd__ddaattaa feature.  Encrypted datasets may not
     have ccooppiieess=_3 since the implementation stores some encryption metadata
     where the third copy would normally be.  Since compression is applied be‐
     fore encryption, datasets may be vulnerable to a CRIME-like attack if ap‐
     plications accessing the data allow for it.  Deduplication with encryp‐
     tion will leak information about which blocks are equivalent in a dataset
     and will incur an extra CPU cost for each block written.

SSEEEE AALLSSOO
     zfsprops(7), zfs-create(8), zfs-set(8)

BSD                            January 13, 2020                            BSD
