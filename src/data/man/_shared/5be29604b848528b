The  and  files  list hosts and users which are by the local host
when a connection is made via or any other server that uses  This
mechanism  bypasses  password  checks, and is required for access
via Each line of these files has the format: hostname  [username]
The  may be specified as a host name (typically a fully qualified
host name in a DNS environment) or address, (from which only  the
host names are checked), or a wildcard (allow all hosts).  The if
specified,  may be given as a user name on the remote host, (from
which only the user names are checked), or a wildcard (allow  all
remote users).  If a is specified, only that user from the speci‐
fied host may login to the local machine.  If a is not specified,
any user may login with the same user name.  Global trusted host‐
user  pairs  list  Per‐user trusted host‐user pairs list A common
usage:  users on may login to the local host  as  the  same  user
name.   The user on may login to the local host.  If specified in
the user may login with only the same user name.   The  user  may
login to the local host from any machine listed in the netgroup +
+  +  Two  severe  security hazards.  In the first case, allows a
user on any machine to login to the local host as the  same  user
name.   In the second case, allows any user on any machine to lo‐
gin to the local host (as any user, if  in  The  username  checks
provided by this mechanism are secure, as the remote user name is
received  by  the  server unchecked for validity.  Therefore this
mechanism should only be used in an environment where  all  hosts
are completely trusted.  A numeric host address instead of a host
name  can  help  security considerations somewhat; the address is
then used directly by When a username  (or  netgroup,  or  +)  is
specified  in that user (or group of users, or all users, respec‐
tively) may login to the local host as Usernames in should there‐
fore be used with extreme caution, or not at all.  A file must be
owned by the user whose home directory it resides in, and must be
writable only by that user.  Logins as  root  only  check  root’s
file;  the  file  is  not checked for security.  Access permitted
through root’s file is typically only for as root must still  lo‐
gin on the console for an interactive login such as The file for‐
mat  appeared  in The implementation currently skips negative en‐
tries (preceded with a sign) and does not treat them as  ‘‘short‐
circuit’’ negative entries.
