MDMFS(8)                  BSD System Manager's Manual                 MDMFS(8)

NNAAMMEE
     mmddmmffss, mmoouunntt__mmffss — configure and mount an in-memory file system using the
     md(4) driver or the tmpfs(5) filesystem

SSYYNNOOPPSSIISS
     mmddmmffss [--DDLLllMMNNnnPPSSttTTUUXX] [--aa _m_a_x_c_o_n_t_i_g] [--bb _b_l_o_c_k_-_s_i_z_e]
           [--cc _b_l_o_c_k_s_-_p_e_r_-_c_y_l_i_n_d_e_r_-_g_r_o_u_p] [--dd _m_a_x_-_e_x_t_e_n_t_-_s_i_z_e]
           [--EE _p_a_t_h_-_m_d_c_o_n_f_i_g] [--ee _m_a_x_b_p_g] [--FF _f_i_l_e] [--ff _f_r_a_g_-_s_i_z_e] [--ii _b_y_t_e_s]
           [--kk _s_k_e_l] [--mm _p_e_r_c_e_n_t_-_f_r_e_e] [--OO _o_p_t_i_m_i_z_a_t_i_o_n] [--oo _m_o_u_n_t_-_o_p_t_i_o_n_s]
           [--pp _p_e_r_m_i_s_s_i_o_n_s] [--ss _s_i_z_e] [--TT _f_s_t_y_p_e] [--vv _v_e_r_s_i_o_n] [--ww _u_s_e_r:_g_r_o_u_p]
           _m_d_-_d_e_v_i_c_e _m_o_u_n_t_-_p_o_i_n_t

DDEESSCCRRIIPPTTIIOONN
     The mmddmmffss utility is designed to be a work-alike and look-alike of the
     deprecated mmoouunntt__mmffss.  The end result is essentially the same, but is ac‐
     complished in a completely different way.  Based on _m_d_-_d_e_v_i_c_e, the mmddmmffss
     utility either creates a tmpfs(5) filesystem, or it configures an md(4)
     disk using mdconfig(8), puts a UFS file system on it (unless --PP was spec‐
     ified) using newfs(8), and mounts it using mount(8).  It can handle
     geom_uzip(4) compressed disk images, as long as the kernel supports this
     GEOM class.  All the command line options are passed to the appropriate
     program at the appropriate stage in order to achieve the desired effect.

     When _m_d_-_d_e_v_i_c_e is `auto', mmddmmffss uses tmpfs(5) if it is present in the
     kernel or can be loaded as a module, otherwise it falls back to using
     md(4) auto-unit as if `md' had been specified.

     When _m_d_-_d_e_v_i_c_e is `tmpfs', mmddmmffss mounts a tmpfs(5) filesystem, translat‐
     ing the --ss size option, if present, into a `-o size=' mount option.  Any
     --oo options on the command line are passed through to the tmpfs(5) mount.
     Options specific to mdconfig(8) or newfs(8) are ignored.

     When _m_d_-_d_e_v_i_c_e does not result in tmpfs(5) being used, then an md(4) de‐
     vice is configured instead.  By default, mmddmmffss creates a swap-based
     (MD_SWAP) disk with soft-updates enabled and mounts it on _m_o_u_n_t_-_p_o_i_n_t.
     It uses the md(4) device specified by _m_d_-_d_e_v_i_c_e.  If _m_d_-_d_e_v_i_c_e is ‘md’
     (no unit number), it will use md(4)'s auto-unit feature to automatically
     select an unused device.  Unless otherwise specified with one of the op‐
     tions below, it uses the default arguments to all the helper programs.

     The following options are available.  Where possible, the option letter
     matches the one used by mmoouunntt__mmffss for the same thing.

     --aa _m_a_x_c_o_n_t_i_g
             Specify the maximum number of contiguous blocks that will be laid
             out before forcing a rotational delay (see the --dd option).

     --bb _b_l_o_c_k_-_s_i_z_e
             The block size of the file system, in bytes.

     --cc _b_l_o_c_k_s_-_p_e_r_-_c_y_l_i_n_d_e_r_-_g_r_o_u_p
             The number of blocks per cylinder group in the file system.

     --DD      If not using auto-unit, do not run mdconfig(8) to try to detach
             the unit before attaching it.

     --dd _m_a_x_-_e_x_t_e_n_t_-_s_i_z_e
             The file system may choose to store large files using extents.
             This parameter specifies the largest extent size that may be
             used.  It is presently limited to its default value which is 16
             times the file system blocksize.

     --EE _p_a_t_h_-_m_d_c_o_n_f_i_g
             Use _p_a_t_h_-_m_d_c_o_n_f_i_g as a location of the mdconfig(8) utility.

     --ee _m_a_x_b_p_g
             Indicate the maximum number of blocks any single file can allo‐
             cate out of a cylinder group before it is forced to begin allo‐
             cating blocks from another cylinder group.

     --FF _f_i_l_e
             Create a vnode-backed (MD_VNODE) memory disk backed by _f_i_l_e.

     --ff _f_r_a_g_-_s_i_z_e
             The fragment size of the file system in bytes.

     --ii _b_y_t_e_s
             Number of bytes per inode.

     --kk _s_k_e_l
             Copy the content of directory _s_k_e_l into _m_o_u_n_t_-_p_o_i_n_t.

     --ll      Enable multilabel MAC on the new file system.

     --LL      Show the output of the helper programs.  By default, it is sent
             to _/_d_e_v_/_n_u_l_l.

     --MM      Create a malloc(9) backed disk (MD_MALLOC) instead of a swap-
             backed disk.

     --mm _p_e_r_c_e_n_t_-_f_r_e_e
             The percentage of space reserved for the superuser.

     --NN      Do not actually run the helper programs.  This is most useful in
             conjunction with --XX.

     --nn      Do not create a _._s_n_a_p directory on the new file system.

     --OO _o_p_t_i_m_i_z_a_t_i_o_n
             Select the optimization preference; valid choices are ssppaaccee and
             ttiimmee, which will optimize for minimum space fragmentation and
             minimum time spent allocating blocks, respectively.

     --oo _m_o_u_n_t_-_o_p_t_i_o_n_s
             Specify the mount options with which to mount the file system.
             See mount(8) for more information.

     --PP      Preserve the existing file system; do not run newfs(8).  This
             only makes sense if --FF is specified to create a vnode-backed
             disk.

     --pp _p_e_r_m_i_s_s_i_o_n_s
             Set the file (directory) permissions of the mount point
             _m_o_u_n_t_-_p_o_i_n_t to _p_e_r_m_i_s_s_i_o_n_s.  The _p_e_r_m_i_s_s_i_o_n_s argument can be in
             any of the mode formats recognized by chmod(1).  If symbolic per‐
             missions are specified, the operation characters “+” and “-” are
             interpreted relative to the initial permissions of “a=rwx”.

     --SS      Do not enable soft-updates on the file system.

     --ss _s_i_z_e
             Specify the size of the disk to create.  This only makes sense if
             --FF is _n_o_t specified.  That is, this will work when the backing
             storage is some form of memory, as opposed to a fixed-size file.
             The size may include the usual SI suffixes (k, m, g, t, p).  A
             number without a suffix is interpreted as a count of 512-byte
             sectors.

     --tt      Turn on the TRIM enable flag for newfs(8).  When used with a file
             system that issue BIO_DELETE bio requests, md(4) returns deleted
             blocks to the system memory pool.

     --TT _f_s_t_y_p_e
             Specify a file system type for a vnode-backed memory disk.  Any
             file system supported by mount(8) command can be specified.  This
             option only makes sense when --FF and --PP are used.

     --UU      Enable soft-updates on the file system.  This is the default, and
             is accepted only for compatibility.  It is only really useful to
             negate the --SS flag, should such a need occur.

     --vv _v_e_r_s_i_o_n
             Specify the UFS version number for use on the file system; it may
             be either 1 or 2.  The default is derived from the default of the
             newfs(8) command.

     --ww _u_s_e_r:_g_r_o_u_p
             Set the owner and group to _u_s_e_r and _g_r_o_u_p, respectively.  The ar‐
             guments have the same semantics as with chown(8), but specifying
             just a _u_s_e_r or just a _g_r_o_u_p is not supported.

     --XX      Print what command will be run before running it, and other as‐
             sorted debugging information.

     The --FF and --ss options are passed to mdconfig(8) as --ff and --ss, respec‐
     tively.  The --aa, --bb, --cc, --dd, --ee, --ff, --ii, --mm and --nn options are passed to
     newfs(8) with the same letter.  The --OO option is passed to newfs(8) as
     --oo.  The --oo option is passed to mount(8) with the same letter.  The --TT
     option is passed to mount(8) as --tt.  For information on semantics, refer
     to the documentation of the programs that the options are passed to.

EEXXAAMMPPLLEESS
     Create and mount a 32 megabyte swap-backed file system on _/_t_m_p:

           mdmfs -s 32m md /tmp

     The same file system created as an entry in _/_e_t_c_/_f_s_t_a_b:

           md /tmp mfs rw,-s32m 2 0

     Create and mount a 16 megabyte malloc-backed file system on _/_t_m_p using
     the _/_d_e_v_/_m_d_1 device; furthermore, do not use soft-updates on it and mount
     it aassyynncc:

           mdmfs -M -S -o async -s 16m md1 /tmp

     Create and mount a geom_uzip(4) based compressed disk image:

           mdmfs -P -F foo.uzip -oro md.uzip /tmp/

     Mount the same image, specifying the _/_d_e_v_/_m_d_1 device:

           mdmfs -P -F foo.uzip -oro md1.uzip /tmp/

     Configure a vnode-backed file system and mount its first partition, using
     automatic device numbering:

           mdmfs -P -F foo.img mds1a /tmp/

     Mount a vnode-backed cd9660 file system using automatic device numbering:

           mdmfs -T cd9660 -P -F foo.iso md /tmp

CCOOMMPPAATTIIBBIILLIITTYY
     The mmddmmffss utility, while designed to be compatible with mmoouunntt__mmffss, can be
     useful by itself.  Since mmoouunntt__mmffss had some silly defaults, a
     “compatibility” mode is provided for the case where bug-to-bug compati‐
     bility is desired.

     Compatibility is enabled by starting mmddmmffss with the name mount_mfs or mfs
     (as returned by getprogname(3)).  In this mode, the following behavior,
     as done by mmoouunntt__mmffss, is duplicated:

           ••   The file mode of _m_o_u_n_t_-_p_o_i_n_t is set by default to 01777 as if
               --pp _1_7_7_7 was given on the command line.

SSEEEE AALLSSOO
     md(4), fstab(5), tmpfs(5), mdconfig(8), mount(8), newfs(8)

HHIISSTTOORRYY
     The mmddmmffss utility appeared in FreeBSD 5.0.

AAUUTTHHOORRSS
     Dima Dorfman

BSD                            October 31, 2019                            BSD
