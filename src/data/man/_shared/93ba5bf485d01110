_w_c_t_y_p_e(3)                  Library Functions Manual                  _w_c_t_y_p_e(3)

NNAAMMEE
       wctype - wide‐character classification

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<wwccttyyppee..hh>>

       wwccttyyppee__tt wwccttyyppee((ccoonnsstt cchhaarr **_n_a_m_e));;

DDEESSCCRRIIPPTTIIOONN
       The  _w_c_t_y_p_e___t  type represents a property which a wide character may or
       may not have.  In other words, it represents a class  of  wide  charac‐
       ters.   This type’s nature is implementation‐dependent, but the special
       value _(_w_c_t_y_p_e___t_) _0 denotes an invalid property.  Nonzero _w_c_t_y_p_e___t  val‐
       ues  can be passed to the iisswwccttyyppee(3) function to actually test whether
       a given wide character has the property.

       The wwccttyyppee() function returns a property, given by its name.   The  set
       of  valid names depends on the LLCC__CCTTYYPPEE category of the current locale,
       but the following names are valid in all locales.

              L1 Lx.  "alnum" - realizes the iissaallnnuumm(3)  classification  func‐
              tion  "alpha"  - realizes the iissaallpphhaa(3) classification function
              "blank" -     realizes the  iissbbllaannkk(3)  classification  function
              "cntrl"  -    realizes  the  iissccnnttrrll(3)  classification function
              "digit"  -   realizes  the  iissddiiggiitt(3)  classification  function
              "graph"   -  realizes  the  iissggrraapphh(3)  classification  function
              "lower"  - realizes  the  iisslloowweerr(3)   classification   function
              "print"  -     realizes  the  iisspprriinntt(3) classification function
              "punct" -    realizes  the  iissppuunncctt(3)  classification  function
              "space" -   realizes the iissssppaaccee(3) classification function "up‐
              per"   -     realizes  the  iissuuppppeerr(3)  classification  function
              "xdigit" -     realizes the iissxxddiiggiitt(3) classification function

RREETTUURRNN VVAALLUUEE
       The wwccttyyppee() function returns a property  descriptor  if  the  _n_a_m_e  is
       valid.  Otherwise, it returns _(_w_c_t_y_p_e___t_) _0.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       wwccttyyppee() T}   Thread safety  MT‐Safe locale

SSTTAANNDDAARRDDSS
       C11, POSIX.1‐2008.

HHIISSTTOORRYY
       POSIX.1‐2001, C99.

NNOOTTEESS
       The behavior of wwccttyyppee() depends on the LLCC__CCTTYYPPEE category of the
       current locale.

SSEEEE AALLSSOO
       iisswwccttyyppee(3)

Linux man‐pages (unreleased)      2025‐05‐17                         _w_c_t_y_p_e(3)
