contains  commonly  used shell script functions which are used by
various scripts such as and the periodic  system  services  which
are controlled by and The functions are accessed by sourcing into
the  current shell.  The following shell functions are available:
Make a backup copy of into If the variable is use to archive  the
previous version of otherwise save the previous version of as may
be  one  of  the following: is now being backed up by or possibly
re‐entered into this backup mechanism.  is created, and if neces‐
sary, the files are created as well.  has changed and needs to be
backed up.  If exists, it is copied to or checked  into  (if  the
repository file is old), and then is copied to is no longer being
tracked  by  this  backup  mechanism.  If is being used, an empty
file is checked in and is removed, otherwise  is  moved  to  Just
like except implemented using shell built‐in commands, and usable
before  the  direcory is available.  Return 0 if is defined to or
Return 1 if is defined to or Otherwise, warn that is not set cor‐
rectly.  The values are case insensitive.  Note that the  warning
message  shown by this function when is not set references a man‐
ual page where the user can find more information.  Its  name  is
picked  up from the variable.  Parses the first word of the first
line of for a PID, and ensures that the process with that PID  is
running and its first argument matches Prints the matching PID if
successful,  otherwise  nothing.  If is provided, parse the first
line of ensure that the line is of the form and use with its  op‐
tional  arguments  and  appended  as the process string to search
for.  Prints the PIDs of any processes that are  running  with  a
first  argument that matches is handled as per Copy input to out‐
put, collapsing to nothing, but leaving other backslashes  alone.
Just  like  except implemented using shell built‐in commands, and
usable before the direcory is available.  Display an  error  mes‐
sage  to log it to the system log using and with an exit value of
The error message consists of the script name (from  followed  by
and  then Source in the configuration files for First, is sourced
if it has not yet been read in.  Then, is sourced if it is an ex‐
isting file.  The latter may also contain other variable  assign‐
ments  to  override  arguments  defined by the calling script, to
provide an easy mechanism for an administrator  to  override  the
behaviour of a given script without requiring the editing of that
script.   Read the variable for and set in the current shell, us‐
ing in a sub‐shell to prevent unwanted side  effects  from  other
variable  assignments.   Go  through a list of critical file sys‐
tems, as found in the variable mounting each one that is not cur‐
rently mounted.  Execute the specified command with the specified
arguments, in such a way  that  its  output  bypasses  the  post‐
processor  that  uses  for  most commands.  This implies that the
output will not appear in the file, and will appear on  the  con‐
sole regardless of the value of This is expected to be useful for
interactive commands, and this mechanism is automatically used by
when a script contains the keyword If invoked from a context that
does  not  appear  to be under the control of then the command is
executed without special treatment.  Print the specified in  such
a  way that it should be handled as meta‐data by the post‐proces‐
sor.  If invoked from a context that does not appear to be  under
the control of then the is discarded.  Any script may invoke this
function  with  an argument that begins with followed by one line
of arbitrary text; the text will be logged by  but  will  not  be
displayed on the console.  The use of arguments that do not begin
with  is  reserved for internal use by and Print the specified in
such a way that it should be handled  as  normal  output  by  the
post‐processor.   If  invoked from a context that does not appear
to be under the control of then the is printed to  standard  out‐
put.   If the flag is specified, then the string is printed with‐
out a newline.  Intended use cases include: An  rc.d  script  can
use to print a partial line in such a way that it appears immedi‐
ately  instead  of  being  buffered  by  post‐processor.  An rc.d
script that is run via the function (so most of its output is in‐
visible to post‐processor) can use to force some of its output to
be seen by the post‐processor.  Print a usage  message  for  with
being  the  list of valid arguments prefixed by Print the list of
in reverse order.  Run the method for the current  script,  based
on  the settings of various shell variables.  is extremely flexi‐
ble, and allows fully functional scripts to be implemented  in  a
small  amount  of  shell code.  The optional set of parameters is
passed verbatim to the command, but not to  its  pre/post  hooks.
is  searched  for in the list of supported commands, which may be
one of: Start the service.  This should check that the service is
to be started as specified by Also checks if the service  is  al‐
ready  running  and refuses to start if it is.  This latter check
is not performed by standard scripts if the  system  is  starting
directly  to  multi‐user  mode, to speed up the boot process.  If
the service is to be started as specified by  stop  the  service.
This  should  check  that  the service is running and complain if
it’s not.  Perform a then a Defaults to displaying the process ID
of the program (if running).  Display which variables are used to
control the startup of the service (if any).  If or is set,  also
support:  Wait  for  the command to exit.  Show the status of the
process.  Other supported commands are  listed  in  the  optional
variable  may have one of the following prefixes which alters its
operation: Skip the check for an existing  running  process,  and
sets  Skip the checks for being set to yes, and sets This ignores
returning non‐zero, and ignores any of the tests failing, and al‐
ways returns a zero exit status.  Skip the checks for  being  set
to  yes, but performs all the other prerequisite tests.  uses the
following shell variables to control its behaviour.  Unless  oth‐
erwise  stated,  these  are  optional.   The name of this script.
This is not optional.  The value of is checked with to  determine
if  this method should be run.  The manual page containing infor‐
mation about It will be part of the warning message shown when is
undefined.  Defaults to Full path to the command.   Not  required
if  is  defined  for  each supported keyword.  Optional arguments
and/or shell directives for is started with which results in  its
command  being  so use that string to find the PID(s) of the run‐
ning command rather than Extra  commands/keywords/arguments  sup‐
ported.   Path  to pid file.  Used to determine the PID(s) of the
running command.  If is set, use to find the PID.  Otherwise,  if
is  set,  use  to  find the PID.  Process name to check for.  De‐
faults to the value of Check for the existence of the listed  di‐
rectories before running the default start method.  Check for the
readability  of the listed files before running the default start
method.  Perform on each of the list variables before running the
default start method.  Directory to to before running if  is  not
provided.  Directory to to before running Only supported after is
mounted.  List of additional or modified environment variables to
set when starting Arguments to call with.  This is usually set in
and  not  in the script.  The environment variable can be used to
override this.   level  to  run  as.   Only  supported  after  is
mounted.   User  to  run as, using if is set, otherwise uses Only
supported after is mounted.  Group to run the chrooted as.  Comma
separated list of supplementary groups to run the chrooted  with.
Shell  commands  which override the default method for Shell com‐
mands to run just before running or the  default  method  for  If
this  returns  a  non‐zero exit code, the main method is not per‐
formed.  If the default method is being executed, this  check  is
performed  after  the  checks and process (non‐)existence checks.
Shell commands to run if running or the default  method  for  re‐
turned a zero exit code.  Signal to send the processes to stop in
the  default method.  Defaults to Signal to send the processes to
reload in the default method.  Defaults to For a given method  if
is  not  defined,  then a default method is provided by If is not
running and succeeds, start Determine the PIDs of with or (as ap‐
propriate), those PIDs, and run on those PIDs.  Similar to except
that it uses instead, and doesn’t run Runs the method,  then  the
method.  Show the PID of or some other script specific status op‐
eration.   Wait  for to exit.  Display which variable is used (if
any).  This method always works, even if the appropriate variable
is set to The following variables are available  to  the  methods
(such as as well as after has completed: Argument provided to af‐
ter fast and force processing has been performed.  Flags to start
the  default  command with.  Defaults to unless overridden by the
environment variable This variable may be changed by the  method.
PID  of  (if  appropriate).   Not  empty if prefix was used.  Not
empty if prefix was used.  Start the script with an  argument  of
and handle the return value from the script.  Various shell vari‐
ables  are  unset before is started: The startup behaviour of de‐
pends upon the following checks: If ends in it  is  sourced  into
the  current  shell.   If  appears to be a backup or scratch file
(e.g., with a suffix of or ignore it.  If is not executable,  ig‐
nore it.  If the variable is empty, source in a sub shell, other‐
wise source into the current shell.  If contains the keyword then
the  command is executed using Prevent booting to multiuser mode.
If the variable is then a signal is sent to  the  parent  process
(which  is  assumed  to be Otherwise, the shell exits with status
Display one of the characters followed by a backspace.   Repeated
calls  to  this function will create the appearance of a spinning
symbol, as a different character is displayed on each call.  Out‐
put is to so this function may be useful  even  inside  a  script
whose output has been redirected.  Wait until all of the provided
don’t  exist any more, printing the list of outstanding every two
seconds.  Display a warning message to and log it to  the  system
log  using  The warning message consists of the script name (from
followed by and then Change the value of the  specified  variable
from  any of the forms acceptable to the function, to or The file
resides in appeared in The support functions appeared in  Support
for the post‐processor appeared in
