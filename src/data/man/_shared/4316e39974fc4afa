The utility copies the standard input to the standard output with
substitution  or  deletion of selected characters.  The following
options are available: Complements the set of characters in  that
is,  includes  every  character  except for and The option causes
characters to be deleted from the  input.   The  option  squeezes
multiple occurrences of the characters listed in the last operand
(either  or in the input into a single instance of the character.
This occurs after all deletion and translation is completed.   In
the  first  synopsis  form, the characters in are translated into
the characters in where the first character in is translated into
the first character in and so on.  If is  longer  than  the  last
character found in is duplicated until is exhausted.  In the sec‐
ond  synopsis form, the characters in are deleted from the input.
In the third synopsis form, the characters in are  compressed  as
described for the option.  In the fourth synopsis form, the char‐
acters  in  are deleted from the input, and the characters in are
compressed as described for the option.   The  following  conven‐
tions can be used in and to specify sets of characters: Any char‐
acter  not  described  by one of the following conventions repre‐
sents itself.  A backslash followed by 1, 2, or  3  octal  digits
represents a character with that encoded value.  To follow an oc‐
tal sequence with a digit as a character, left zero‐pad the octal
sequence  to  the  full  3 octal digits.  A backslash followed by
certain special characters maps to special values.


A backslash followed by any other character maps to that  charac‐
ter.   Represents  the range of characters between the range end‐
points, inclusively.  Represents all characters belonging to  the
defined character class.  Class names are:

With  the exception of the and classes, characters in the classes
are in unspecified order.  In the and classes, characters are en‐
tered in ascending order.  For specific information as  to  which
ASCII  characters  are included in these classes, see and related
manual pages.  Represents all characters or  collating  (sorting)
elements belonging to the same equivalence class as If there is a
secondary  ordering  within the equivalence class, the characters
are ordered in ascending sequence.  Otherwise, they  are  ordered
after  their  encoded values.  An example of an equivalence class
might be and in Spanish;  English  has  no  equivalence  classes.
Represents  repeated  occurrences of the character represented by
This expression is only valid when it occurs in If is omitted  or
is zero, it is interpreted as large enough to extend the sequence
to  the  length of If has a leading zero, it is interpreted as an
octal value; otherwise, it is interpreted  as  a  decimal  value.
The  following examples are shown as given to the shell: Create a
list of the words in one per line, where a word is taken to be  a
maximal string of letters:


Translate the contents of to upper‐case:


Strip out non‐printable characters from

has  historically  implemented  character ranges using the syntax
instead of the used by historic implementations and  standardized
by POSIX.  shell scripts should work under this implementation as
long  as  the range is intended to map in another range, i.e. the
command will work as it will map the character in to the  charac‐
ter  in  However,  if  the  shell script is deleting or squeezing
characters as in the command the characters and will be  included
in the deletion or compression list which would not have happened
under an historic implementation.  Additionally, any scripts that
depended  on  the  sequence to represent the three characters and
will have to be rewritten as The  utility  has  historically  not
permitted  the  manipulation of NUL bytes in its input and, addi‐
tionally, stripped NULs from its input stream.  This  implementa‐
tion has removed this behavior as a bug.  The utility has histor‐
ically  been  extremely  forgiving of syntax errors, for example,
the and options were ignored unless two strings  were  specified.
This  implementation will not permit illegal syntax.  The utility
is expected to be compatible.  It should be noted that  the  fea‐
ture  wherein  the  last  character  of is duplicated if has less
characters than is permitted by POSIX but is not required.  Shell
scripts attempting to be portable to other POSIX  systems  should
use  the  convention  instead  of  relying on this behavior.  was
originally designed to work with Its use with character sets that
do not share all the properties of e.g., a symmetric set of upper
and lower case characters that can be  algorithmically  converted
one to the other, may yield unpredictable results.  should be in‐
ternationalized.
