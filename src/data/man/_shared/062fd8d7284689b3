VFS_BUSY(9)              BSD Kernel Developer's Manual             VFS_BUSY(9)

NNAAMMEE
     vvffss__bbuussyy — marks a mount point as busy

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ppaarraamm..hh>>
     ##iinncclluuddee <<ssyyss//mmoouunntt..hh>>

     _i_n_t
     vvffss__bbuussyy(_s_t_r_u_c_t _m_o_u_n_t _*_m_p, _i_n_t _f_l_a_g_s);

DDEESSCCRRIIPPTTIIOONN
     The vvffss__bbuussyy() function marks a mount point as busy by incrementing the
     reference count of a mount point.  It also delays unmounting by sleeping
     on _m_p if the MNTK_UNMOUNT flag is set in _m_p_-_>_m_n_t___k_e_r_n___f_l_a_g and the
     MBF_NOWAIT flag is _n_o_t set.

     Its arguments are:

     _m_p     The mount point to busy.

     _f_l_a_g_s  Flags controlling how vvffss__bbuussyy() should act.

            MBF_NOWAIT      do not sleep if MNTK_UNMOUNT is set.

            MBF_MNTLSTLOCK  drop the mountlist_mtx in the critical path.

RREETTUURRNN VVAALLUUEESS
     A 0 value is returned on success.  If the mount point is being unmounted
     and MBF_NOWAIT flag is specified ENOENT will be returned.

EERRRROORRSS
     [ENOENT]           The mount point is being unmounted (MNTK_UNMOUNT is
                        set).

SSEEEE AALLSSOO
     vfs_unbusy(9)

AAUUTTHHOORRSS
     This manual page was written by Chad David <_d_a_v_i_d_c_@_a_c_n_s_._a_b_._c_a>.

BSD                            February 11, 2013                           BSD
