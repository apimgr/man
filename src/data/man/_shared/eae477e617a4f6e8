The  utility formats manual pages for display.  By default, reads
or text from stdin and produces output.  The options are as  fol‐
lows:  If  the  standard  output  is a terminal device and is not
specified, use to paginate the output, just like would.  Copy the
formatted manual pages to the standard output  without  using  to
paginate  them.   This  is  the  default.  It can be specified to
override Override the default operating system for  the  and  for
the  macro.  Specify the input encoding.  The supported arguments
are and If not specified, autodetection uses the first  match  in
the  following  list:  If the first three bytes of the input file
are the UTF‐8 byte order mark (BOM, 0xefbbbf),  input  is  inter‐
preted  as  If the first or second line of the input file matches
the mode line format then input is interpreted  according  to  If
the first non‐ASCII byte in the file introduces a valid UTF‐8 se‐
quence,  input  is interpreted as Otherwise, input is interpreted
as With all input files are interpreted as With all  input  files
are  interpreted  as  By default, the input language is automati‐
cally detected for each file: if the first macro is or the parser
is used; otherwise, the parser is used.  With other arguments, is
silently ignored.  Comma‐separated output options.  See  the  de‐
scriptions  of the individual output formats for supported Select
the output format.  Supported values for the argument are the de‐
fault of and The special mode only parses the input and  produces
no  output.  It implies and redirects parser messages, which usu‐
ally appear on standard error output, to standard output.   Spec‐
ify the minimum message to be reported on the standard error out‐
put and to affect the exit status.  The can be or The level auto‐
matically  derives  the operating system from the contents of the
macro, from the command line option, or from  the  return  value.
The  levels and are variants of that bypass autodetection and re‐
quest validation of base system conventions for a particular  op‐
erating system.  The level is an alias for By default, is silent.
See  and  for  details.   The  special option tells to exit after
parsing a file that causes warnings or errors of at least the re‐
quested level.  No formatted output will be  produced  from  that
file.   If  both  a  and are requested, they can be joined with a
comma, for example Read from the given input file.   If  multiple
files  are  specified, they are processed in the given order.  If
unspecified, reads from standard input.   The  options  are  also
supported  and  are documented in man(1).  In and mode, also sup‐
ports the options described in the manual.  The options are mutu‐
ally exclusive and override each other.  Use to force text output
in 7‐bit ASCII character encoding documented in the manual  page,
ignoring  the set in the environment.  Font styles are applied by
using back‐spaced encoding such that an underlined  character  is
rendered  as where is the back‐space character number 8.  Embold‐
ened characters are rendered as This  markup  is  typically  con‐
verted  to  appropriate terminal sequences by the pager or To re‐
move the markup, pipe the output to instead.  The special charac‐
ters documented in are rendered best‐effort in an  ASCII  equiva‐
lent.   In  particular,  opening  and  closing are represented as
characters number 0x60 and 0x27, respectively, which agrees  with
all  ASCII  standards from 1965 to the latest revision (2012) and
which matches the traditional way in which  formatters  represent
single  quotes in ASCII output.  This correct ASCII rendering may
look strange with modern Unicode‐compatible  fonts  because  con‐
trary  to ASCII, Unicode uses the code point U+0060 for the grave
accent only, never for an opening quote.  The following arguments
are accepted: The left margin for normal text  is  set  to  blank
characters  instead  of the default of five for and seven for In‐
creasing this is not recommended; it may result in degraded  for‐
matting,  for  example  overfull lines or ugly line breaks.  When
output is to a pager on a terminal that is less than  66  columns
wide,  the  default  is  reduced  to three columns.  Format input
files in output style.  Specifically, this suppresses the two ad‐
ditional blank lines near the top and the bottom  of  each  page,
and it implies One useful application is for checking that output
formats  in the same way as the source it was generated from.  If
the formatted manual page is opened in a pager, go to the defini‐
tion of the rather than showing the manual page from  the  begin‐
ning.   If no is specified, reuse the first command line argument
that is not a number.  If that argument is in format, only the is
used rather than the argument as a whole.   This  is  useful  for
commands like to search for a keyword and jump right to its defi‐
nition  in the matching manual pages.  The output width is set to
instead of the default of 78.  When output is to  a  pager  on  a
terminal  that  is  less than 79 columns wide, the default is re‐
duced to one less than the terminal width.  In  any  case,  lines
that  are output in literal mode are never wrapped and may exceed
the output width.  Output produced by conforms to HTML5 using op‐
tional self‐closing tags.  Default styles use only  CSS1.   Equa‐
tions rendered from blocks use MathML.  The file documents style‐
sheet classes available for customising output.  If a style‐sheet
is  not specified with defaults to simple output (via an embedded
style‐sheet) readable in any graphical or text‐based web browser.
Non‐ASCII characters are rendered as hexadecimal Unicode  charac‐
ter  references.   The following arguments are accepted: Omit the
<!DOCTYPE> declaration and the <html>, <head>,  and  <body>  ele‐
ments  and  only  emit the subtree below the <body> element.  The
argument will be ignored.  This is useful when  embedding  manual
content  within  existing  documents.  The string for example, is
used as a template for  linked  header  files  (usually  via  the
macro).   Instances  of  are  replaced with the include filename.
The default is not to present a hyperlink.  The string for  exam‐
ple,  is  used  as a template for linked manuals (usually via the
macro).  Instances of and are replaced with the  linked  manual’s
name  and section, respectively.  If no section is included, sec‐
tion 1 is assumed.  The default is not to  present  a  hyperlink.
If  two formats are given and a file exists in the current direc‐
tory, the first format is used; otherwise, the second  format  is
used.   The  file is used for an external style‐sheet.  This must
be a valid absolute or relative URI.  If an input  file  contains
at  least  two  non‐standard  sections, print a table of contents
near the beginning of the output.  By default, automatically  se‐
lects  UTF‐8  or  ASCII output according to the current If any of
the environment variables or are set and the first  one  that  is
set  selects the UTF‐8 character encoding, it produces otherwise,
it falls back to This output mode can also be selected explicitly
with Use to translate input into output format.  This  is  useful
for distributing manual sources to legacy systems lacking format‐
ters.   If  the  input format of a file is the input is copied to
the output, expanding any requests.  The parser is also run,  and
as  usual,  the level controls which are displayed before copying
the input to the output.  Use to translate input to the  markdown
format conforming to The output also almost conforms to the spec‐
ification.   The  character  set  used for the markdown output is
ASCII.  Non‐ASCII characters are encoded as HTML entities.  Since
that is not possible in literal font contexts, because these  are
rendered  as  code  spans and code blocks in the markdown output,
non‐ASCII characters are transliterated to  ASCII  approximations
in  these  contexts.  Markdown is a very weak markup language, so
all semantic markup is lost, and even part of the  presentational
markup  may  be lost.  Do not use this as an intermediate step in
converting to HTML; instead, use directly.  The  and  input  lan‐
guages  are  not supported by output mode.  PDF‐1.1 output may be
generated by See for arguments and defaults.  PostScript  Level‐2
pages  may  be  generated by Output pages default to letter sized
and are rendered in the Times font family, 11‐point.  Margins are
calculated as 1/9 the page  length  and  width.   Line‐height  is
1.4m.   Special characters are rendered as in The following argu‐
ments are accepted: The paper size may be one of or You may  also
manually  specify  dimensions  as width by height in millimetres.
If an unknown value is encountered, is used.  Use to  force  text
output  in UTF‐8 multi‐byte character encoding, ignoring the set‐
tings in the environment.  See regarding font  styles  and  argu‐
ments.   On  operating  systems  lacking locale or wide character
support, and on those where the internal character representation
is not UCS‐4, always falls back to Use to show a  human  readable
representation  of  the  syntax tree.  It is useful for debugging
the source code of manual pages.  The exact format is subject  to
change, so don’t write parsers for it.  The first paragraph shows
meta  data  found  in the prologue, on the line, or the fallbacks
used.  In the tree dump, each output line shows one  syntax  tree
node.   Child  nodes  are  indented  with respect to their parent
node.  The columns are: For macro nodes, the macro name; for text
and nodes, the content.  There is a  special  format  for  nodes.
Node  type  (text,  elem, block, head, body, body‐end, tail, tbl,
eqn).  Flags: An opening parenthesis if the node  is  an  opening
delimiter.  An asterisk if the node starts a new input line.  The
input  line number (starting at one).  A colon.  The input column
number (starting at one).  A closing parenthesis if the node is a
closing delimiter.  A full stop if  the  node  ends  a  sentence.
BROKEN  if the node is a block broken by another block.  NOSRC if
the node is not in the input file,  but  automatically  generated
from  macros.  NOPRT if the node is not supposed to generate out‐
put for any output format.  The following argument  is  accepted:
Skip  validation  and show the unvalidated syntax tree.  This can
help to find out whether a  given  behaviour  is  caused  by  the
parser  or  by the validator.  Meta data is not available in this
case.  The  character  encoding  When  is  selected,  it  decides
whether  to  use  ASCII or UTF‐8 output format.  It never affects
the interpretation of input files.  Any non‐empty  value  of  the
environment  variable  is used instead of the standard pagination
program, see for details.  Only used if or is specified.   Speci‐
fies  the pagination program to use when is not defined.  If nei‐
ther PAGER nor MANPAGER is defined, is used.  Only used if or  is
specified.   The  utility exits with one of the following values,
controlled by the message associated with  the  option:  No  base
system convention violations, style suggestions, warnings, or er‐
rors  occurred,  or those that did were ignored because they were
lower than the requested At least one base system convention vio‐
lation or style suggestion occurred, but no warning or error, and
or was specified.  At least one warning occurred, but  no  error,
and  or  a  lower  was requested.  At least one parsing error oc‐
curred, but no unsupported feature  was  encountered,  and  or  a
lower  was  requested.   At least one unsupported feature was en‐
countered, and or a lower was requested.   Invalid  command  line
arguments were specified.  No input files have been read.  An op‐
erating  system error occurred, for example exhaustion of memory,
file descriptors, or process table entries.  Such errors cause to
exit at once, possibly in the middle of parsing or  formatting  a
file.  Note that selecting output mode implies To page manuals to
the terminal: To produce HTML manuals with as the style‐sheet: To
check  over  a large set of manuals: To produce a series of Post‐
Script manuals for A4 paper: Convert a modern manual to the older
format, for use on systems lacking an parser: Messages  displayed
by  follow this format: The first three fields identify the name,
number, and number of the input file where the message was  trig‐
gered.  The line and column numbers start at 1.  Both are omitted
for  messages  referring  to  an  input file as a whole.  All and
strings are explained below.  The name of the triggering the mes‐
sage and its are omitted where meaningless.  The operating system
specifier is omitted for messages that are relevant for all oper‐
ating systems.  Fatal messages about invalid command  line  argu‐
ments  or operating system errors, for example when memory is ex‐
hausted, may also omit the and fields.  Message levels  have  the
following meanings: An input file uses unsupported low‐level fea‐
tures.   The output may be incomplete and/or misformatted, so us‐
ing GNU troff instead of to process the file may  be  preferable.
Indicates  a risk of information loss or severe misformatting, in
most cases caused by serious syntax  errors.   Indicates  a  risk
that the information shown or its formatting may mismatch the au‐
thor’s  intent  in  minor  ways.  Additionally, syntax errors are
classified at least as warnings, even  if  they  do  not  usually
cause  misformatting.   An input file uses dubious or discouraged
style.  This is not a complaint about the  syntax,  and  probably
neither  formatting  nor  portability are in danger.  While great
care is taken to avoid false positives on the higher message lev‐
els, the level tries to reduce the probability that issues go un‐
noticed, so it may occasionally issue bogus suggestions.   Please
use  your good judgement to decide whether any particular sugges‐
tion really justifies a change to the input file.   A  convention
used in the base system of a specific operating system is not ad‐
hered to.  These are not markup mistakes, and neither the quality
of  formatting  nor  portability  are in danger.  Messages of the
level are printed with the more intuitive tag.  Messages  of  the
and  levels  except  those about non‐existent or unreadable input
files are hidden unless their level, or a  lower  level,  is  re‐
quested  using  a option or output mode.  As indicated below, all
and some checks are only performed if a specific operating system
name occurs in the arguments of the command line option,  of  the
macro, of the command line option, or, if neither are present, in
the  return  value  of  the function.  The macro uses CVS keyword
substitution, which is not supported by the  base  system.   Con‐
sider  using the conventional format instead.  The macro does not
use CVS keyword substitution, but using it is conventionally  ex‐
pected  in the base system.  The third argument of the macro does
not match any of the architectures this operating system is  run‐
ning  on.   The macro has an argument.  In the base system, it is
conventionally left blank.  The manual  page  lacks  the  comment
line  with the RCS identifier generated by CVS or keyword substi‐
tution as conventionally used in  these  operating  systems.   An
macro references a manual page that is not found in the base sys‐
tem.  The path to look for base system manuals is configurable at
compile  time and defaults to The macro uses the legacy date for‐
mat Consider using the conventional date format instead.  The  or
macro  provides  an abbreviated month name or a day number with a
leading zero.  In the formatted output, the month name is written
out in full and the leading zero is omitted.  The title is  still
used as given in the or macro.  A single manual page contains two
copies of the RCS identifier for the same operating system.  Con‐
sider  deleting the later instance and moving the first one up to
the top of the page.  Fuzzy string matching revealed that the ar‐
gument of an macro is similar, but not identical  to  a  standard
section  name.   Macro  arguments can be enclosed in double quote
characters such that space characters and macro  names  contained
in the quoted argument need not be escaped.  The closing quote of
the  last  argument of a macro can be omitted.  However, omitting
it is not recommended because it makes the code harder  to  read.
A or macro was found.  Simply delete it: it serves no useful pur‐
pose.   A string was found in plain text or in a macro that could
be represented using or The items in a list are not in alphabeti‐
cal order.  A list contains two  consecutive  entries  describing
the  same  number.   The last argument of an or macro ends with a
trailing delimiter.  This is usually bad style  and  often  indi‐
cates  typos.   Most  likely,  the delimiter can be removed.  The
last argument of a macro that supports trailing  delimiter  argu‐
ments is longer than one byte and ends with a trailing delimiter.
Consider inserting a blank such that the delimiter becomes a sep‐
arate  argument, thus moving it out of the scope of the macro.  A
request occurs even though the document is still in fill mode, or
already switched back to fill mode.  It has no  effect.   An  re‐
quest occurs even though the document already switched to no‐fill
mode and did not switch back to fill mode yet.  It has no effect.
Even though the ASCII output device renders an em‐dash as that is
not  a  good  way to write it in an input file because it renders
poorly on all other output devices.  A word followed by an  empty
pair  of parentheses occurs on a text line.  Consider using an or
macro.  Whitespace at the end of input lines is almost never  se‐
mantically  significant  — but in the odd case where it might be,
it is extremely confusing when reviewing  and  maintaining  docu‐
ments.   Comment  lines start with a dot, a backslash, and a dou‐
ble‐quote character.  The utility treats the line  as  a  comment
line  even  without  the backslash, but leaving out the backslash
might not be portable.  A macro has no arguments, or there is  no
macro before the first non‐prologue macro.  There is no macro, or
it  has no arguments.  A or macro lacks the mandatory section ar‐
gument.  The section number in a line is invalid, but still used.
The document was parsed as and it has no macro, or the macro  has
no  arguments or only empty arguments; or the document was parsed
as and it has no macro, or the macro has less  than  three  argu‐
ments  or  its  third  argument is empty.  The date given in a or
macro does not follow the conventional format.  The date given in
a or macro is more than a day ahead of the current system The de‐
fault or current system is not shown in this case.   A  or  macro
occurs  after  some  non‐prologue  macro, but still takes effect.
The prologue macros are not given in the conventional  order  All
three  macros are used even when given in another order.  Includ‐
ing files only works when the parser program runs with  the  cor‐
rect  current working directory.  The document body contains nei‐
ther text nor macros.  An empty  document  is  shown,  consisting
only  of a header and a footer line.  Some macros or text precede
the first or section header.  The offending macros and  text  are
parsed and added to the top level of the syntax tree, outside any
section  block.   The argument of the first macro is not This may
confuse and The NAME section does not contain any child macro be‐
fore the first macro.  The NAME section lacks the mandatory child
macro.  The NAME section does contain an child macro,  but  other
content  follows  it.   The  NAME  section contains plain text or
macros other than and The NAME section contains an macro that  is
neither  the  first one nor preceded by a comma.  The macro lacks
the required argument.  The title line of the manual will end af‐
ter the dash.  An macro appears outside the  NAME  section.   The
arguments  are  printed anyway and the following text is used for
but none of that behaviour is portable.  A standard  section  oc‐
curs  after another section it usually precedes.  All section ti‐
tles are used as given, and the order of sections is not changed.
The same standard section title occurs more than once.   A  stan‐
dard  section  header  occurs in a section of the manual where it
normally isn’t useful.  An macro refers to  a  name  and  section
matching  the  section of the present manual page and a name men‐
tioned in an macro in the NAME or SYNOPSIS section, or in  an  or
macro  in  the SYNOPSIS.  Consider using or instead of In the SEE
ALSO section, an macro with a lower section  number  follows  one
with a higher number, or two macros referring to the same section
are out of alphabetical order.  In the SEE ALSO section, punctua‐
tion  between two macros differs from a single comma, or there is
trailing punctuation after the last macro.  An  AUTHORS  sections
contains  no macros, or only empty ones.  Probably, there are au‐
thor names lacking markup.  See the manual for replacements.  The
name of a macro that is not callable appears on a macro line.  It
is printed verbatim.  If the intention is to call it, move it  to
its  own  input line; otherwise, escape it by prepending In docu‐
ments, this happens at the beginning and end of sections and sub‐
sections right before non‐compact lists and displays at  the  end
of  items  in non‐column, non‐compact lists and for multiple con‐
secutive paragraph macros.  In documents, it  happens  for  empty
and  macros for macros having neither head nor body arguments for
or right after or A list item in a list contains a trailing para‐
graph macro.  The paragraph macro is moved after the end  of  the
list.   An  input line begins with an macro, or the next argument
after an macro is an isolated closing delimiter.   The  macro  is
ignored.   If two blocks intersect, one should completely contain
the other.  Otherwise, rendered output is likely to look  strange
in  any output format, and rendering in SGML‐based output formats
is likely to be outright wrong because such languages do not sup‐
port badly nested blocks  at  all.   Typical  examples  of  badly
nested blocks are and In these examples, breaks and respectively.
A  or  display  occurs nested inside another display.  This works
with but fails with most other  implementations.   A  list  block
contains  text  or  macros before the first macro.  The offending
children are moved before the beginning of the  list.   Inside  a
list,  a  macro occurs as the first macro on a line, which is not
portable.  While parsing the  next‐line  scope  of  the  previous
macro,  another  macro  is  found that prematurely terminates the
previous one.  The previous, interrupted macro  is  deleted  from
the  parse  tree.  The macro name is missing from a macro defini‐
tion request, or an control statement or operation keyword  lacks
its  required  argument.  A conditional request is only useful if
any of the following follows it on the same logical  input  line:
The  keyword  to  open a multi‐line scope.  A request or macro or
some text, resulting in a single‐line scope.  The  immediate  end
of the logical line without any intervening whitespace, resulting
in  next‐line  scope.  Here, a conditional request is followed by
trailing whitespace only, and there is no other  content  on  its
logical input line.  Note that it doesn’t matter whether the log‐
ical input line is split across multiple physical input lines us‐
ing  line continuation characters.  This is one of the rare cases
where trailing whitespace is syntactically significant.  The con‐
ditional request controls a scope containing whitespace only,  so
it  is  unlikely to have a significant effect, except that it may
control a following clause.  The indicated macro has no arguments
and hence no effect.  A or block contains nothing in its body and
will produce no output.  The required width is missing  after  or
or  The macro is invoked without the required display type.  In a
macro, at least one other argument precedes  the  type  argument.
The  utility copes with any argument order, but some other imple‐
mentations do not.  Every macro having the argument requires too.
The macro is called without an argument  before  has  first  been
called  with  an  argument.  The macro is called without an argu‐
ment.  No function name is printed.  In a or list, an macro lacks
the required argument.  The item head is left  empty.   In  a  or
list,  an block is empty.  An empty list item is shown.  An macro
in a list has no arguments.  While uses the text or macros of the
following line, if any, for the cell, other formatters  may  mis‐
format  the  list.   A macro has no argument.  It switches to the
default font.  The argument is invalid.  The default font is used
instead.  A macro has no argument, or only one  argument  and  no
macro  follows on the same input line.  This defeats its purpose;
in particular, spacing is  not  suppressed  before  the  text  or
macros following on the next input line.  An macro is immediately
followed by an macro on the next input line.  Such an empty block
does  not produce any output.  An macro lacks its second, section
number argument.  The first argument, i.e. the name, is  printed,
but  without  subsequent  parentheses.  An or macro lacks the re‐
quired argument.  The utility assumes even when it is not  speci‐
fied,  but  other  implementations may not.  The macro is invoked
without any argument.  An empty pair of square brackets is shown.
The or macro is invoked without any argument.  An empty  pair  of
angle  brackets  is shown.  A diacritic mark or a binary operator
is found, but there is nothing to the left of it.  An  empty  box
is  inserted.  A or macro has more than one more than one or more
than one argument.  All but the last instances of these arguments
are ignored.  An macro has more than one or  argument.   All  but
the  first of these arguments are ignored.  A macro has more than
one type argument; the first one is used.  A macro has more  than
one  type argument; the first one is used.  A or list has a argu‐
ment.  That has no effect.  In a line of a list,  the  number  of
tabs  or  macros  is  less than the number expected from the list
header line or exceeds the expected  number  by  more  than  one.
Missing cells remain empty, and all cells exceeding the number of
columns are joined into one single cell.  An macro has an invalid
argument.  It is used verbatim, with prefixed to it.  An argument
of an or macro contains a comma; it should probably be split into
two  arguments.   The  first  argument of an or macro contains an
opening or closing parenthesis; that’s probably wrong,  parenthe‐
ses  are added automatically.  An macro has an unknown name argu‐
ment and will be rendered as An  block  contains  plain  text  or
non‐%  macros.   The  bogus  content  is left in the syntax tree.
Formatting may be poor.  An macro has an argument other  than  or
The  invalid argument is moved out of the macro, which leaves the
macro empty, causing it to toggle the spacing mode.   The  second
argument  of  a  request  contains  more than one font escape se‐
quence.  A wrong font may remain active after using  the  charac‐
ter.   A request or a layout modifier has an unknown argument.  A
request contains an odd number of characters.  The last character
is mapped to the blank character.  The  meaning  of  blank  input
lines  is  only well‐defined in non‐fill mode: In fill mode, line
breaks of text input lines are not supposed  to  be  significant.
However,  for  compatibility with groff, blank lines in fill mode
are replaced with requests.  The meaning  of  tab  characters  is
only  well‐defined  in non‐fill mode: In fill mode, whitespace is
not supposed to be significant on text input lines.  As an imple‐
mentation dependent choice, tab  characters  on  text  lines  are
passed  through  to  the  formatters in any case.  Given that the
text before the tab character will be filled, it is hard to  pre‐
dict which tab stop position the tab will advance to.  A new sen‐
tence starts in the middle of a text line.  Start it on a new in‐
put  line  to help formatters produce correct spacing.  An escape
sequence has an invalid opening  argument  delimiter,  lacks  the
closing  argument  delimiter, the argument is of an invalid form,
or it is a character escape sequence with an  invalid  name.   If
the argument is incomplete, and expand to an empty string, to the
digit  and  to  the length of the incomplete argument.  All other
invalid escape sequences are ignored.  In an escape sequence, the
first character right after the  leading  backslash  is  invalid.
That  character  is printed literally, which is equivalent to ig‐
noring the backslash.  If a string is used without being  defined
before,  its  value  is implicitly set to the empty string.  How‐
ever, defining strings explicitly before use keeps the code  more
readable.   The first cell in a table layout line is a horizontal
span Data provided for this  cell  is  ignored,  and  nothing  is
printed in the cell.  The first line of a table layout specifica‐
tion  requests a vertical span Data provided for this cell is ig‐
nored, and nothing is printed in the cell.  A table layout speci‐
fication contains more than two  consecutive  vertical  bars.   A
double  bar  is  printed, all additional bars are discarded.  The
table options line contains a  character  other  than  a  letter,
blank,  or  comma  where  the  beginning of an option name is ex‐
pected.  The character is ignored.  The table options  line  con‐
tains  a  string  of letters that does not match any known option
name.  The word is ignored.  A table option that requires an  ar‐
gument  is not followed by an opening parenthesis, or the opening
parenthesis is immediately followed  by  a  closing  parenthesis.
The  option  is ignored.  A table option argument contains an in‐
valid number of characters.  Both the option and the argument are
ignored.  A table layout specification is completely empty, spec‐
ifying zero lines and zero columns.   As  a  fallback,  a  single
left‐justified column is used.  A table layout specification con‐
tains a character that can neither be interpreted as a layout key
character  nor  as  a layout modifier, or a modifier precedes the
first key.  The invalid character is discarded.  A  table  layout
specification  contains  an  opening parenthesis, but no matching
closing parenthesis.  The rest of the input line,  starting  from
the  parenthesis,  has  no  effect.  A table does not contain any
data cells.  It will probably produce no output.  A table cell is
marked as a horizontal span or vertical span in the table layout,
but it contains data.  The data is ignored.  A data line contains
more cells than the corresponding layout line.  The data  in  the
extra  cells  is  ignored.  A data block is opened with but never
closed with a matching The remaining data lines of the table  are
all  put  into one cell, and any remaining cells stay empty.  One
of the prologue macros occurs more than once.  The last  instance
overrides  all  previous ones.  The macro appears after the first
non‐prologue macro.  Traditional formatters  cannot  handle  this
because  they  write  the page header before parsing the document
body.  Even though this technical restriction does not  apply  to
traditional  semantics is preserved.  The late macro is discarded
including its arguments.  Explicit recursion  limits  are  imple‐
mented  for  the following features, in order to prevent infinite
loops: expansion of nested escape sequences  including  expansion
of strings and number registers, expansion of nested user‐defined
macros,  and  file inclusion.  When a limit is hit, the output is
incorrect, typically losing some content, but the parser can con‐
tinue.  The input file contains a byte that is  not  a  printable
character.   The  message mentions the character number.  The of‐
fending byte is replaced with a question  mark  Consider  editing
the  input file to replace the byte with an ASCII transliteration
of the intended character.  The first identifier on a request  or
macro  line is neither recognized as a request, nor as a user‐de‐
fined macro, nor, respectively,  as  an  or  macro.   It  may  be
mistyped  or  unsupported.  The request or macro is discarded in‐
cluding its arguments.  A or request occurs outside any macro de‐
finition and has no effect.  An input file  attempted  to  run  a
shell  command  or  to  read or write an external file.  Such at‐
tempts are denied for security reasons.  An macro occurs  outside
any  list,  or  an delimiter occurs outside any pile.  It is dis‐
carded including its  arguments.   A  macro  occurs  outside  any
block.   It is discarded including its arguments.  Various syntax
elements can only be used to explicitly close  blocks  that  have
previously  been  opened.  An block closing macro, a or macro, an
right delimiter or closing brace, or the end of an equation,  ta‐
ble,  or conditional request is encountered but no matching block
is open.  The offending request or macro is discarded.  The macro
is invoked with an argument, but less than the  specified  number
of  blocks  is  open.  The macro is discarded.  Various macros as
well as tables require explicit closing by dedicated  macros.   A
block  that  doesn’t  support  bad nesting ends before all of its
children are properly closed.  The open child  nodes  are  closed
implicitly.   At  the  end  of the document, an explicit block, a
next‐line scope or or block, an equation, table,  or  conditional
or  ignore block is still open.  The open block is closed implic‐
itly.  Macro, string and register identifiers consist  of  print‐
able,  non‐whitespace  ASCII  characters.   Escape  sequences and
characters and strings expressed in terms  of  them  cannot  form
part  of a name.  The first argument of an or request, or any ar‐
gument of an request, or the name of a request  or  user  defined
macro  being called, is terminated by an escape sequence.  In the
cases of and the request has no effect at all.  In the  cases  of
and  what was parsed up to this point is used as the arguments to
the request, and the rest of the input line is discarded  includ‐
ing  the  escape sequence.  When parsing for a request or a user‐
defined macro name to be called, only the escape sequence is dis‐
carded.  The characters preceding it are used as the  request  or
macro name, the characters following it are used as the arguments
to  the  request or macro.  The escape sequence \$ occurs outside
any macro definition and expands to the empty string.  The  argu‐
ment  of  the  escape  sequence \$ is not a digit; the escape se‐
quence expands to the empty string.  For  security  reasons,  the
macro does not support the argument.  By requesting the inclusion
of a sensitive file, a malicious document might otherwise trick a
privileged  user  into  inadvertently  displaying the file on the
screen, revealing the file content to bystanders.   The  argument
is  ignored  including the file name following it.  A block macro
does not have any arguments.  The block  is  discarded,  and  the
block content is displayed in whatever mode was active before the
block.   A macro fails to specify the list type.  The argument of
a request is not a number.  The first argument of  a  request  is
neither  a  single  ASCII character nor a single character escape
sequence.  The request is ignored including  all  its  arguments.
The  first call to or any call in the NAME section, lacks the re‐
quired argument.  The macro is called without arguments, and  the
system  call  failed.   As  a workaround, can be compiled with An
macro has an unknown argument and is discarded.  An request or an
or statement has a non‐numeric or negative argument or  no  argu‐
ment  at  all.  The invalid request or statement is ignored.  The
argument of a request is larger than the number of  arguments  of
the  macro that is currently being executed.  All macro arguments
are deleted and \n(.$ is set to zero.  For security reasons,  al‐
lows  file  inclusion  requests only with relative paths and only
without ascending to any parent directory.  By requesting the in‐
clusion of a sensitive file, a malicious document might otherwise
trick a privileged user into inadvertently displaying the file on
the screen, revealing the file content to bystanders.  only shows
the path as it appears behind Servicing a request requires  read‐
ing  an  external  file,  but the file could not be opened.  only
shows the path as it appears behind An or macro, an  macro  in  a
list that don’t support item heads, a or macro, an or macro, or a
or  request or block closing request is invoked with at least one
argument.  All arguments are ignored.  A macro or request is  in‐
voked  with  too  many  arguments: or with more than one argument
with another argument after or with more  than  one  argument  or
with  a non‐integer argument or a request of the family with more
than two arguments with more than three arguments with more  than
five arguments or with invalid arguments The excess arguments are
ignored.   Currently,  cannot  handle input files larger than its
arbitrary size limit of 2ˆ31 bytes (2 Gigabytes).   Since  useful
manuals  are  always  small,  this  is not a problem in practice.
Parsing is aborted as soon as  the  condition  is  detected.   An
ASCII  control  character  supported by other implementations but
not by was found in an input file.  It is replaced by a  question
mark.  An input file contains an escape sequence supported by GNU
troff  or  Heirloom  troff  but not by and it is likely that this
will cause information loss or  considerable  misformatting.   An
input  file contains a request supported by GNU troff or Heirloom
troff but not by and it is likely that this will  cause  informa‐
tion  loss  or considerable misformatting.  The options line of a
table defines equation delimiters.  Any equation source code con‐
tained in the table will be printed unformatted.  A table  layout
specification  contains  an modifier.  The modifier is discarded.
A table contains an invocation of an or macro or of an  undefined
macro.  The macro is ignored, and its arguments are handled as if
they  were a text line.  The utility first appeared in The option
appeared in and in The utility was written by and  is  maintained
by
