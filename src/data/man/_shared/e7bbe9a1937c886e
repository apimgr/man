When  the system has entropy demand, the driver, used with a com‐
patible hypervisor such as QEMU, KVM, or Google  Compute  Engine,
requests  entropy  using  the  VirtIO random number interface and
feeds it into the system entropy pool.  The  driver  appeared  in
The  driver  was  written by and reworked to request entropy upon
demand by VirtIO appears to support at least  8  pending  entropy
requests,  but  currently  supports only one pending request at a
time.
