The  program  adds,  removes, lists, and prioritizes swap devices
and files for the system.  The program acts the same as the  pro‐
gram  as  if  called  with the option, except if itself is called
with in which case acts as with the option.   The  following  op‐
tions  are available: This option causes to read the file for de‐
vices and files with a or type, and adds all type entries as swap
devices and sets the last type entry as the dump device.   If  no
swap  devices  are  configured, will exit with an error code.  If
used together with this option will not read but query the kernel
for all swap partitions on local hard disks.  The option requires
that a also be in the argument list.  The is added  to  the  ker‐
nel’s list of swap devices using the system call.  When using the
form  of  this command, the option is treated the same as the op‐
tion, for backwards compatibility.  The option changes the prior‐
ity of the listed swap device or file.  The option requires  that
a also be in the argument list.  The kernel dump device is set to
The  word  can  be used instead of a to disable the currently set
dump device.  This change is made via the system call.  The  dump
device  is  used when the system crashes to write a current snap‐
shot of real memory, to be saved later with at system reboot, and
analyzed to determine the problem.  The option removes the listed
from the kernel’s list of swap devices or files.  Used in  combi‐
nation  with the command and flag this option makes use the first
discovered swap device to also become the dump device.   The  op‐
tion  is  mutually  exclusive  with  the option.  The option uses
(1024 * 1024 * 1024) byte blocks instead of the default 512 byte.
The option uses to display the sizes.  The option uses 1024  byte
blocks  instead  of  the  default 512 byte.  The option lists the
current swap devices and files, and their usage statistics.   The
option  uses (1024 * 1024) byte blocks instead of the default 512
byte.  Used with the or command, the option makes print  the  ac‐
tion  it would take, but not actually change any swap or dump de‐
vices.  Similar to the flag, this option  makes  find  the  first
swap  device  and configure it as dump device.  No swap device is
changed.  This option needs to be used in combination with and is
mutually exclusive with The option sets the priority of swap  de‐
vices or files to the argument.  This works with the and options.
Query  checking for any defined swap or dump devices.  If any are
found, returns with an exit status of 0, if none  are  found  the
exit status will be 1.  The option displays a single line summary
of  current  swap statistics.  This flag modifies the function of
the and options.  The option allows the type of device to add  to
be  specified.   An argument of causes all block devices in to be
added.  An argument of causes all  non‐block  devices  in  to  be
added.   An  argument of causes all swap partitions on local hard
disks to be used.  This option is useful in early system startup,
where swapping may be needed before all file systems  are  avail‐
able, such as during disk checks of large file systems.  This op‐
tion  causes  to read the file for devices and files with a type,
and remove all these entries as swap devices.  If no swap devices
are unconfigured, will exit with an error code.  If used together
with this option will not read but  unconfigure  all  local  swap
partitions.   The  option  displays the current dump device.  The
swap system allows different swap devices and  files  to  be  as‐
signed  different priorities, to allow the faster resources to be
used first.  Swap devices at the same  priority  are  used  in  a
round‐robin  fashion  until  there  is no more space available at
this priority, when the next priority level will  be  used.   The
default  priority is 0, the highest.  This value can be any valid
integer, with higher values receiving less priority.  When  pars‐
ing  the  file for swap devices, the following options are recog‐
nized: This option sets the priority of the specified swap device
to N.  This option is useful for swapping to NFS files.  It spec‐
ifies the local mount point to  mount  an  NFS  filesystem.   The
mount  point  must  exist  as  a directory.  Typically, once this
mount has succeeded, the file to be used for swapping on will  be
available  under  this  point  mount.   For  example: server:/ex‐
port/swap/client none swap sw,nfsmntpt=/swap If the requested op‐
eration was successful, the utility exits with status 0.   If  an
error occurred, the exit status is 1.  The and operations (add or
remove  swap  devices listed in return an exit status of 2 to re‐
port that no suitable swap devices  were  found.   The  operation
(query  dump  device)  and  (list swap partitions) return an exit
status of 1 if no dump device or swap partition has been  config‐
ured.   If  any  swap  partition is available or a dump device is
set, the respective query returns 0.  The program was first  made
available in The original program, provided for backwards compat‐
ibility,  appeared  in The program was written by Using the auto‐
matic swap partition detection done by the option may be dangere‐
ous.  Depending on the on‐disk partitioning scheme used, the type
of a partition may not be accurately recognizable as a swap  par‐
tition.   The autodetection might recognize and use partitions on
removable media like USB sticks.  An easy way to test  the  auto‐
configuration  is to use with the option.  If no swap information
is specified in the system startup scripts (see will configure no
swap space and your machine  will  behave  very  badly  if  (more
likely  when)  it runs out of real memory.  Local and remote swap
files cannot be configured until after the file systems they  re‐
side  on are mounted read/write.  The system startup scripts need
to all local file systems before this can happen.   This  process
requires  substantial  amounts of memory on some systems.  If you
configure no local block swap devices on a machine that has local
file systems to check and rely only on swap  files,  the  machine
will  have  no swap space at all during system and may run out of
real memory, causing fsck to abnormally exit and startup  scripts
to fail.
