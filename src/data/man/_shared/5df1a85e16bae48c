KEYSERV(8)                BSD System Manager's Manual               KEYSERV(8)

NNAAMMEE
     kkeeyysseerrvv — server for storing private encryption keys

SSYYNNOOPPSSIISS
     kkeeyysseerrvv [--dd] [--DD] [--nn] [--pp _p_a_t_h] [--vv]

DDEEPPRREECCAATTIIOONN NNOOTTIICCEE
     kkeeyysseerrvv is deprecated and is not available as of FreeBSD 15.0.

DDEESSCCRRIIPPTTIIOONN
     The kkeeyysseerrvv utility is a daemon that is used for storing the private en‐
     cryption keys of each user logged into the system.  These encryption keys
     are used for accessing secure network services such as secure NFS.

     Normally, root's key is read from the file _/_e_t_c_/_._r_o_o_t_k_e_y when the daemon
     is started.  This is useful during power-fail reboots when no one is
     around to type a password.

     If a client with no secret key calls kkeeyysseerrvv, then the key of user _n_o_b_o_d_y
     is used instead as the default key.

     The following options are available:

     --dd      Disable the use of default keys for _n_o_b_o_d_y.

     --DD      Run in debugging mode and log all requests to kkeeyysseerrvv.

     --nn      Root's secret key is not read from _/_e_t_c_/_._r_o_o_t_k_e_y.  Instead,
             kkeeyysseerrvv prompts the user for the password to decrypt root's key
             stored in the _/_e_t_c_/_p_u_b_l_i_c_k_e_y database and then stores the de‐
             crypted key in _/_e_t_c_/_._r_o_o_t_k_e_y for future use.  This option is use‐
             ful if the _/_e_t_c_/_._r_o_o_t_k_e_y file ever gets out of date or corrupted.

     --pp _p_a_t_h
             Specify where to search for _l_i_b_d_e_s_._s_o_._3.  Default is _/_u_s_r_/_l_i_b.

     --vv      Display status of DES support (enabled/disabled).

FFIILLEESS
     /etc/.rootkey
     /usr/lib/libdes.so.3

SSEEEE AALLSSOO
     keylogin(1), keylogout(1), publickey(5)

HHIISSTTOORRYY
     The kkeeyysseerrvv utility was removed from FreeBSD 15.0.

BSD                           September 14, 1992                           BSD
