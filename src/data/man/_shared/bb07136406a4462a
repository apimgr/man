_e_r_f(3)                     Library Functions Manual                     _e_r_f(3)

NNAAMMEE
       erf, erff, erfl - error function

LLIIBBRRAARRYY
       Math library (_l_i_b_m, _-_l_m)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<mmaatthh..hh>>

       ddoouubbllee eerrff((ddoouubbllee _x));;
       ffllooaatt eerrffff((ffllooaatt _x));;
       lloonngg ddoouubbllee eerrffll((lloonngg ddoouubbllee _x));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       eerrff():
           _ISOC99_SOURCE || _POSIX_C_SOURCE >= 200112L || _XOPEN_SOURCE
               || /* Since glibc 2.19: */ _DEFAULT_SOURCE
               || /* glibc <= 2.19: */ _BSD_SOURCE || _SVID_SOURCE

       eerrffff(), eerrffll():
           _ISOC99_SOURCE || _POSIX_C_SOURCE >= 200112L
               || /* Since glibc 2.19: */ _DEFAULT_SOURCE
               || /* glibc <= 2.19: */ _BSD_SOURCE || _SVID_SOURCE

DDEESSCCRRIIPPTTIIOONN
       These functions return the error function of _x, defined as

           erf(x) = 2/sqrt(pi) * integral from 0 to x of exp(-t*t) dt

RREETTUURRNN VVAALLUUEE
       On  success,  these functions return the value of the error function of
       _x, a value in the range [-1, 1].

       If _x is a NaN, a NaN is returned.

       If _x is +0 (-0), +0 (-0) is returned.

       If _x is positive infinity (negative infinity), +1 (-1) is returned.

       If _x is subnormal, a range  error  occurs,  and  the  return  value  is
       2*x/sqrt(pi).

EERRRROORRSS
       See  mmaatthh__eerrrroorr(7) for information on how to determine whether an error
       has occurred when calling these functions.

       The following errors can occur:

       Range error: result underflow (_x is subnormal)
              An underflow floating‐point exception (FFEE__UUNNDDEERRFFLLOOWW) is raised.

       These functions do not set _e_r_r_n_o.

AATTTTRRIIBBUUTTEESS
       For an explanation of the terms  used  in  this  section,  see  aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{ eerrff(),
       eerrffff(), eerrffll() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       C11, POSIX.1‐2008.

HHIISSTTOORRYY
       C99, POSIX.1‐2001.

       The variant returning _d_o_u_b_l_e also conforms to SVr4, 4.3BSD.

SSEEEE AALLSSOO
       cceerrff(3), eerrffcc(3), eexxpp(3)

Linux man‐pages (unreleased)      2025‐05‐17                            _e_r_f(3)
