The  call enables, disables and manipulates filesystem quotas.  A
quota control command given by operates on the given filename for
the given user The address of an optional command  specific  data
structure,  may  be  given; its interpretation is discussed below
with each command.  A command is composed of  a  primary  command
(see  below)  and  a command type used to interpret the Types are
supported for interpretation of user identifiers and group  iden‐
tifiers.   The  specific commands are: Enable disk quotas for the
filesystem specified by The command type specifies  the  type  of
the  quotas  being  enabled.   The argument specifies a file from
which to take the quotas.  The quota file must exist; it is  nor‐
mally  created  with  the program.  The argument is unused.  Only
the super‐user may turn quotas on.  Disable disk quotas  for  the
filesystem  specified  by  The command type specifies the type of
the quotas being disabled.  The and arguments are  unused.   Only
the  super‐user  may  turn quotas off.  Get disk quota limits and
current usage for the user or group (as determined by the command
type) with identifier is a pointer  to  a  structure.   Set  disk
quota  limits for the user or group (as determined by the command
type) with identifier is a pointer to  a  structure.   The  usage
fields  of the structure are ignored.  This call is restricted to
the super‐user.  Set disk usage limits for the user or group  (as
determined by the command type) with identifier is a pointer to a
structure.   Only  the  usage  fields are used.  This call is re‐
stricted to the super‐user.  Update the on‐disk copy of quota us‐
ages.  The command type specifies which type of quotas are to  be
updated.   The and parameters are ignored.  Get the enable status
for the filesystem specified by The command  type  specifies  the
type  of  the quotas whose status is being queried.  is a pointer
to an integer.  Upon return, this integer will hold a zero  value
if quotas for the given type are not enabled and a non‐zero value
if  quotas  for the given type are enabled.  The parameter is ig‐
nored.  A successful call returns 0, otherwise the  value  ‐1  is
returned  and  the  global  variable indicates the reason for the
failure.  A call will fail if: The kernel has not  been  compiled
with  the  option.   The  quota table cannot be expanded.  or the
command type is invalid.  In the quota file is not a plain  file.
Search  permission is denied for a component of a path prefix.  A
component of a path prefix was not a directory.  A component of a
pathname exceeded characters, or an  entire  path  name  exceeded
characters.   A filename does not exist.  Too many symbolic links
were encountered in translating a pathname.  In  the  quota  file
resides on a read‐only filesystem.  An error occurred while read‐
ing  from or writing to a file containing quotas.  An invalid was
supplied; the associated structure could not be copied in or  out
of  the  kernel.   points outside the process’s allocated address
space.  The call was privileged and the caller was not the super‐
user.  There should be some way to integrate this call  with  the
resource  limit  interface  provided by and The function call ap‐
peared in
