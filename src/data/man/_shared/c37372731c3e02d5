OPTIONS(4)               BSD Kernel Interfaces Manual               OPTIONS(4)

NNAAMMEE
     ooppttiioonnss — kernel configuration options

SSYYNNOOPPSSIISS
     ooppttiioonn ......

DDEESSCCRRIIPPTTIIOONN
     This manual page describes a number of miscellaneous kernel configuration
     options that may be specified in a kernel config file.  See config(8) for
     information on how to configure and build kernels.  _N_o_t_e: options are
     passed to the compile process as --DD flags to the C compiler.

DDEEBBUUGGGGIINNGG OOPPTTIIOONNSS
     mmaakkeeooppttiioonnss DDEEBBUUGG==""""
     Do not build the debug kernel _b_s_d_._g_d_b.  Normally, _b_s_d_._g_d_b is built (in
     addition to the regular _b_s_d kernel) and is used for debugging kernels and
     their crash dumps with gdb(1).  A crash dump can be debugged by starting
     gdb with _b_s_d_._g_d_b as an argument (no core file) and then using the gdb
     command “target kvm COREFILE”.

     mmaakkeeooppttiioonnss PPRROOFF==""--ppgg""
     The --ppgg flag causes the kernel to be compiled with support for profiling.
     The ooppttiioonn GGPPRROOFF is required for the kernel compile to succeed.

     ooppttiioonn AACCCCOOUUNNTTIINNGG
     Adds support for the acct(2) system call.

     ooppttiioonn DDDDBB
     Compiles in a kernel debugger for diagnosing kernel problems.  See
     ddb(4).

     ooppttiioonn DDDDBB__SSAAFFEE__CCOONNSSOOLLEE
     Allows a break into the kernel debugger during boot.  Useful when debug‐
     ging problems that can cause init(8) to fail.

     ooppttiioonn DDEEBBUUGG
     Turns on miscellaneous kernel debugging.  Since options are turned into
     preprocessor defines (see above), ooppttiioonn DDEEBBUUGG is equivalent to doing a
     _#_d_e_f_i_n_e _D_E_B_U_G throughout the kernel.  Much of the kernel has _#_i_f_d_e_f _D_E_B_U_G
     conditional debugging code.  Note that many parts of the kernel (typi‐
     cally device drivers) include their own _#_i_f_d_e_f _X_X_X___D_E_B_U_G conditionals in‐
     stead.

     ooppttiioonn DDIIAAGGNNOOSSTTIICC
     Adds code to the kernel that does internal consistency checks.  This code
     will cause the kernel to panic if corruption of internal data structures
     is detected.

     ooppttiioonn GGPPRROOFF
     Adds code to the kernel for kernel profiling with kgmon(8).

     ooppttiioonn KKTTRRAACCEE
     Adds hooks for the system call tracing facility, which allows users to
     watch the system call invocation behavior of processes.  See ktrace(1).

     ooppttiioonn KKUUBBSSAANN
     Detect undefined behavior in the kernel.  See kubsan(4).

     ooppttiioonn NNOO__PPRROOPPOOLLIICCEE
     Do not compile the kernel with the ProPolice stack protection.  See
     gcc-local(1) for more information about ProPolice.

     ooppttiioonn PPTTRRAACCEE
     Adds hooks for the process tracing facility, allowing a process to con‐
     trol and observe another process.  See ptrace(2).

     ooppttiioonn SSMMAALLLL__KKEERRNNEELL
     Removes some features and some optimizations from the kernel to reduce
     the size of the resulting kernel binary.  This option is used on some in‐
     stallation media and should not be used for general purpose kernels.

     ooppttiioonn VVFFSSLLCCKKDDEEBBUUGG
     Turns on debugging for the Virtual File System interface.  See vfs(9).

     ooppttiioonn WWIITTNNEESSSS
     Compiles in a lock checker for detecting lock order violations in the
     kernel.  See witness(4).

     ooppttiioonn WWIITTNNEESSSS__CCOOUUNNTT==_i_n_t_e_g_e_r
     Maximum number of lock types that are tracked by witness(4).  It defaults
     to 1536.

     ooppttiioonn WWIITTNNEESSSS__LLOOCCKKTTRRAACCEE
     Enable witness(4) lock stack trace saving at boot.  The feature is dis‐
     abled by default and has to be enabled by setting the
     _k_e_r_n_._w_i_t_n_e_s_s_._l_o_c_k_t_r_a_c_e sysctl(8) variable.

     ooppttiioonn WWIITTNNEESSSS__WWAATTCCHH
     Enable witness(4) at boot.  By default, the subsystem is disabled and has
     to be enabled at runtime by raising the _k_e_r_n_._w_i_t_n_e_s_s_._w_a_t_c_h sysctl(8)
     variable.

FFIILLEE SSYYSSTTEEMMSS
     ooppttiioonn CCDD99666600
     Includes code for the ISO 9660 + Rock Ridge file system, which is the
     standard file system used on many CD-ROMs.  It also supports Joliet ex‐
     tensions.  See mount_cd9660(8).

     ooppttiioonn FFFFSS
     Includes code implementing the Berkeley Fast File System (_F_F_S).  Most ma‐
     chines need this if they are not running diskless.

     ooppttiioonn FFFFSS22
     Includes code implementing the enhanced Fast File System (_F_F_S_2).

     ooppttiioonn MMFFSS
     Include the memory file system (_M_F_S).  This file system stores files in
     swappable memory, and produces notable performance improvements when it
     is used as the file store for _/_t_m_p or similar mount points.  See
     mount_mfs(8).

     ooppttiioonn MMSSDDOOSSFFSS
     Includes support for the MS-DOS FAT file system.  The kernel also imple‐
     ments the Windows 95 extensions which permit the use of longer, mixed-
     case file names.  See mount_msdos(8) and fsck_msdos(8).

     ooppttiioonn NNFFSSCCLLIIEENNTT
     Include the client side of the _N_F_S (Network File System) remote file
     sharing protocol.  Although the bulk of the code implementing _N_F_S is ker‐
     nel based, several user level daemons are needed for it to work.  See
     mount_nfs(8) for details on NFS.

     ooppttiioonn NNTTFFSS
     Includes support for reading NTFS file systems.  See mount_ntfs(8).

     ooppttiioonn UUDDFF
     Includes code for the UDF file systems typically found on DVD discs.  See
     mount_udf(8).

     ooppttiioonn TTMMPPFFSS
     Includes code for the TMPFS efficient memory file system.  See
     mount_tmpfs(8).

FFIILLEE SSYYSSTTEEMM OOPPTTIIOONNSS
     ooppttiioonn BBUUFFCCAACCHHEEPPEERRCCEENNTT==_i_n_t_e_g_e_r
     The maximum percentage of DMA-reachable physical memory the buffer cache
     may use.

     ooppttiioonn EEXXTT22FFSS__SSYYSSTTEEMM__FFLLAAGGSS
     This option changes the behavior of the APPEND and IMMUTABLE flags for a
     file on an _E_X_T_2_F_S filesystem.  Without this option, the superuser or
     owner of the file can set and clear them.  With this option, only the su‐
     peruser can set them, and they can't be cleared if the securelevel is
     greater than 0.  See also chflags(1).

     ooppttiioonn FFIIFFOO
     Adds support for AT&T System V UNIX style FIFOs (i.e., “named pipes”).
     This option is recommended in almost all cases as many programs use
     these.

     ooppttiioonn NNFFSSSSEERRVVEERR
     Include the server side of the _N_F_S (Network File System) remote file
     sharing protocol.  Although the bulk of the code implementing _N_F_S is ker‐
     nel based, several user level daemons are needed for it to work.  See
     mountd(8) and nfsd(8).

     ooppttiioonn QQUUOOTTAA
     Enables kernel support for file system quotas.  See quotaon(8),
     edquota(8), repquota(8), and quota(1).  Note that quotas only work on
     “ffs” file systems, although rpc.rquotad(8) permits them to be accessed
     over _N_F_S.

     ooppttiioonn UUFFSS__DDIIRRHHAASSHH
     This option enables using an in memory hash table to speed lookups in
     large directories.

MMIISSCCEELLLLAANNEEOOUUSS OOPPTTIIOONNSS
     ooppttiioonn AAPPEERRTTUURREE
     Provide in-kernel support for controlling VGA framebuffer mapping and PCI
     configuration registers by user-processes (such as an X Window System
     server).  See xf86(4).  This option is supported on the _a_l_p_h_a, _a_m_d_6_4,
     _i_3_8_6, _m_a_c_p_p_c, and _s_p_a_r_c_6_4 architectures.

     ooppttiioonn BBOOOOTT__CCOONNFFIIGG
     Adds support for the --cc boot option (User Kernel Config).  Allows modifi‐
     cation of kernel settings (e.g., device parameters) before booting the
     system.  See boot_config(8).

     ooppttiioonn CCRRYYPPTTOO
     Enables support for the kernel cryptographic framework.  See crypto(9).
     While not IP specific, this option is usually used in conjunction with
     option _I_P_S_E_C.

     ooppttiioonn EEIISSAAVVEERRBBOOSSEE
     Makes the boot process more verbose for EISA peripherals.  See eisa(4).

     ooppttiioonn KKMMEEMMSSTTAATTSS
     The kernel memory allocator, malloc(9), will keep statistics on its per‐
     formance if this option is enabled.

     ooppttiioonn MMUULLTTIIPPRROOCCEESSSSOORR
     On those architectures that have it, this enables multiprocessor support.

     ooppttiioonn PPCCIIVVEERRBBOOSSEE
     Makes the boot process more verbose for PCI peripherals (vendor names and
     other information is printed, etc.).  See pci(4).

     ooppttiioonn PPCCMMCCIIAAVVEERRBBOOSSEE
     Makes the boot process more verbose for PCMCIA peripherals.  See
     pcmcia(4).

     ooppttiioonn UUSSEERR__PPCCIICCOONNFF
     Enables the user level access to the PCI bus configuration space through
     ioctls on the _/_d_e_v_/_p_c_i device.  It's used by Xorg(1) and pcidump(8).  See
     pci(4).

     ooppttiioonn UUVVMM__SSWWAAPP__EENNCCRRYYPPTT
     Enables kernel support for encrypting pages that are written out to swap
     storage.  Swap encryption prevents sensitive data from remaining on the
     disk even after the operating system has been shut down.  This option
     should be turned on if cryptographic filesystems are used.  The sysctl
     variable _v_m_._s_w_a_p_e_n_c_r_y_p_t_._e_n_a_b_l_e controls its behaviour.  See sysctl(8) and
     sysctl(2).

NNEETTWWOORRKKIINNGG OOPPTTIIOONNSS
     ooppttiioonn EENNCCDDEEBBUUGG
     This option permits the conditional logging of IPsec debugging informa‐
     tion, and requires the _I_P_S_E_C option.  Debug logging can be turned on/off
     through the use of the _n_e_t_._i_n_e_t_._i_p_._e_n_c_d_e_b_u_g sysctl variable.  If
     _n_e_t_._i_n_e_t_._i_p_._e_n_c_d_e_b_u_g is 1, debug logging is on.  See sysctl(8) and
     sysctl(2).

     ooppttiioonn IINNEETT66
     Includes support for the IPv6 protocol stack.  See inet6(4).  _I_N_E_T_6 en‐
     ables multicast routing code as well.

     ooppttiioonn IIPPSSEECC
     This option enables IP security protocol support.  See ipsec(4) for more
     details.

     ooppttiioonn MMRROOUUTTIINNGG
     Includes support for IP multicast routers.  Multicast routing is con‐
     trolled by the mrouted(8) daemon.

     ooppttiioonn PPIIPPEEXX
     Includes pipex in-kernel acceleration for PPPoE, L2TP or PPTP.  See
     pipex(4).

     ooppttiioonn PPPPPP__BBSSDDCCOOMMPP
     Enables BSD compressor for PPP connections.

     ooppttiioonn PPPPPP__DDEEFFLLAATTEE
     For use in conjunction with PPP_BSDCOMP; provides an interface to zlib
     for PPP for deflate compression/decompression.

     ooppttiioonn PPPPPPOOEE__TTEERRMM__UUNNKKNNOOWWNN__SSEESSSSIIOONNSS
     Send PADT to terminate open sessions before connecting.  See pppoe(4).

     ooppttiioonn SSOOCCKKEETT__SSPPLLIICCEE
     Enables zero-copy socket splicing in the kernel.  See SO_SPLICE in
     setsockopt(2) and sosplice(9).

     ooppttiioonn TTCCPP__EECCNN
     Turns on Explicit Congestion Notification (RFC 3168).  _E_C_N allows inter‐
     mediate routers to use the Congestion Experienced codepoint in the IP
     header as an indication of congestion, and allows TCP to adjust the
     transmission rate using this signal.  Both communication endpoints nego‐
     tiate enabling _E_C_N functionality at the TCP connection establishment.

     ooppttiioonn TTCCPP__SSIIGGNNAATTUURREE
     Turns on support for the TCP MD5 Signature option (RFC 2385).  This is
     used by Internet backbone routers to provide per-packet authentication
     for the TCP packets used to communicate BGP routing information.  You
     will also need a routing daemon that supports this option in order to ac‐
     tually use it.

OOPPEERRAATTIIOONN RREELLAATTEEDD OOPPTTIIOONNSS
     ooppttiioonn BBUUFFPPAAGGEESS==_v_a_l_u_e
     This option sets the number of pages available for the buffer cache.  The
     default value is machine dependent, often calculated as between 5% and
     10% of total available RAM.

     ooppttiioonn NNKKMMEEMMPPAAGGEESS==_v_a_l_u_e
     Size of kernel malloc area in PAGE_SIZE-sized logical pages.  This area
     is covered by the kernel submap _k_m_e_m___m_a_p.  The kernel attempts to auto-
     size this map based on the amount of physical memory in the system.
     Platform-specific code may place bounds on this computed size, which may
     be viewed with the sysctl(8) variable _v_m_._n_k_m_e_m_p_a_g_e_s.

SSCCSSII SSUUBBSSYYSSTTEEMM OOPPTTIIOONNSS
     See scsi(4).

     ooppttiioonn SSCCSSII__DDEELLAAYY==_v_a_l_u_e
     Delay for _v_a_l_u_e seconds before starting to probe the first SCSI bus.
     This can be used if a SCSI device needs extra time to get ready.

     ooppttiioonn SSCCSSIIDDEEBBUUGG
     Enable printing of SCSI subsystem debugging info to the console.  Debug‐
     ging info on the probing and attachment of all SCSI devices will be
     printed.

     Additional information can be requested by setting _S_C_S_I_D_E_B_U_G___B_U_S_E_S,
     _S_C_S_I_D_E_B_U_G___L_E_V_E_L, _S_C_S_I_D_E_B_U_G___L_U_N_S and _S_C_S_I_D_E_B_U_G___T_A_R_G_E_T_S.

     ooppttiioonn SSCCSSIIDDEEBBUUGG__BBUUSSEESS==_v_a_l_u_e
     Define which SCSI buses will print debug info.  Each bit enables debug‐
     ging info for the corresponding bus.  e.g. a value of 0x1 enables debug
     info for bus 0.

     ooppttiioonn SSCCSSIIDDEEBBUUGG__LLEEVVEELL==_v_a_l_u_e
     Define which of the four levels of debugging info are printed.  Each bit
     enables a level, and multiple levels are specified by setting multiple
     bits.

           0x0010  (SDEV_DB1) SCSI commands, errors, and data
           0x0020  (SDEV_DB2) routine flow
           0x0040  (SDEV_DB3) routine internals
           0x0080  (SDEV_DB4) miscellaneous addition debugging

     _S_C_S_I_D_E_B_U_G___L_E_V_E_L defaults to a value of 0x0030 (SDEV_DB1|SDEV_DB2).

     ooppttiioonn SSCCSSIIDDEEBBUUGG__LLUUNNSS==_v_a_l_u_e
     Define which SCSI luns will print debug info.  Each bit enables debugging
     info for the corresponding lun.

     ooppttiioonn SSCCSSIIDDEEBBUUGG__TTAARRGGEETTSS==_v_a_l_u_e
     Define which SCSI targets will print debug info.  Each bit enables debug‐
     ging info for the corresponding target.

     ooppttiioonn SSCCSSIITTEERRSSEE
     Terser SCSI error messages.  This omits the table for decoding ASC/ASCQ
     info, saving about 30KB.

SSYYSSTTEEMM VV IIPPCC OOPPTTIIOONNSS
     ooppttiioonn SSEEMMMMNNII==_v_a_l_u_e
     Number of semaphore identifiers (also called semaphore handles and sema‐
     phore sets) available in the system.  Default value is 10.  The kernel
     allocates memory for the control structures at startup, so arbitrarily
     large values should be avoided.

     ooppttiioonn SSEEMMMMNNSS==_v_a_l_u_e
     Maximum number of semaphores in all sets in the system.  Default value is
     60.

     ooppttiioonn SSEEMMMMNNUU==_v_a_l_u_e
     Maximum number of semaphore undo structures in the system.  Default value
     is 30.

     ooppttiioonn SSEEMMUUMMEE==_v_a_l_u_e
     Maximum number of per-process undo operation entries in the system.  Sem‐
     aphore undo operations are invoked by the kernel when semop(2) is called
     with the SEM_UNDO flag and the process holding the semaphores terminates
     unexpectedly.  Default value is 10.

     ooppttiioonn SSHHMMMMAAXXPPGGSS==_v_a_l_u_e
     Sets the maximum number of AT&T System V UNIX style shared memory pages
     that are available through the shmget(2) system call.  Default value is
     1024 on most architectures.  See _/_u_s_r_/_i_n_c_l_u_d_e_/_m_a_c_h_i_n_e_/_v_m_p_a_r_a_m_._h for the
     default.

     ooppttiioonn SSYYSSVVMMSSGG
     Includes support for AT&T System V UNIX style message queues.  See
     msgctl(2), msgget(2), msgrcv(2), msgsnd(2).

     ooppttiioonn SSYYSSVVSSEEMM
     Includes support for AT&T System V UNIX style semaphores.  See semctl(2),
     semget(2), semop(2).

     ooppttiioonn SSYYSSVVSSHHMM
     Includes support for AT&T System V UNIX style shared memory.  See
     shmat(2), shmctl(2), shmdt(2), shmget(2).

SSEEEE AALLSSOO
     intro(4), files.conf(5), config(8), sysctl(8)

HHIISSTTOORRYY
     The ooppttiioonnss man page first appeared in OpenBSD 2.3.

BSD                              June 1, 2025                              BSD
