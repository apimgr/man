The  driver provides support for the 53C710 controller chip.  For
the 53C700 host adapters, use the driver.  For  the  53C8xx  host
adapters,  use  the or driver.  The driver supports the following
for use in files: disable disconnect/reselect for the correspond‐
ing target disable synchronous negotiation for target disable DMA
interrupts is synonymous with ID number.  Note that  tape  drives
should  be  allowed to perform disconnect/reselect or performance
will suffer.  driver first appeared in The original 53C710 driver
appeared in 1.0 amiga port, and Izumi Tsutsui modified the driver
and made it machine‐independent.
