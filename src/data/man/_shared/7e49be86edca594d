A standard installation has the following user group names: Users
authorized  to elevate themselves to the super‐user privileges of
the root user, meaning uid 0.  Normally  the  group  has  gid  0.
Users  who are not in the group are never allowed by to gain root
privileges.  Used  by  the  set‐group‐id  programs  and  Historic
group.  Unused in modern Used by the set‐group‐id programs and to
allow  users  to  send messages to another tty even if they don’t
own it.  Static tty device nodes in are all in the group and  the
program  passes  the  gid  of the group to the kernel so that all
nodes in or equivalent are in the group too.  Users authorized to
take backups of disk devices and shut down the machine.  The disk
device nodes in such as are in the group  and  group‐readable  so
users  in  the group can read from disk devices, for example with
The tape device nodes in such as are in the group  and  are  both
group‐readable and group‐writable so users in the group can write
to tape devices.  The program is executable only by root and mem‐
bers  of  the  group.  Historic group.  Unused in modern Historic
group.  Unused in modern Historic group.  Unused in  modern  Used
by  the  set‐group‐id  programs  and to submit to and examine the
mail queue at and Primary group for the pseudo‐user used  by  the
mail transfer agent.  Used by various set‐group‐id games to main‐
tain  high‐scores  files  and other common files in Primary group
for the pseudo‐user used by the DNS nameserver  daemon.   Primary
group  for the pseudo‐user used by the network time protocol dae‐
mon.  Primary group for the pseudo‐user used by the secure  shell
daemon.  Primary group for the pseudo‐user used by the log daemon
with  the  packet filter.  Primary group for the pseudo‐user used
by the system status daemon.  Staff users, in contrast to regular
or guest users.  Not used by available  for  the  administrator’s
interpretation.   Primary  group  for the pseudo‐user used by the
and proxy daemons with packet filters such as  or  Primary  group
for the pseudo‐user used by the tool to communicate with the time
server  daemon.   Primary  group  for the pseudo‐user used by the
Bluetooth service discovery protocol daemon.  Primary  group  for
the  pseudo‐user  used  by the web server.  Primary group for the
pseudo‐user used by the multicast DNS and DNS  service  discovery
daemon.   Primary  group  for  the  pseudo‐user used by automatic
tests that request to run unprivileged.  Primary  group  for  the
pseudo‐user  used  by  the  network  traffic dumper and analyzer.
Primary group for the pseudo‐user used by the daemon to manage  a
TPM.   Users authorized to read and write GPIO pins; see and Pri‐
mary group for the pseudo‐user used by the  DHCP  Client  Daemon.
Primary group for the pseudo‐user used by the IPv6 network router
advertisement daemon.  Guest users, in contrast to staff or regu‐
lar  users.  Not used by available for the administrator’s inter‐
pretation.  Primary group for the pseudo‐user used by the  recur‐
sive DNS resolver.  Primary group for the pseudo‐user used by the
authoritative  DNS  nameserver.  Users authorized to use the Vir‐
tual Machine Monitor.  Primary group for the traditional  pseudo‐
user.   Modern practice is to assign to each different daemon its
own separate pseudo‐user account and group so that if one  daemon
is  compromised  it  does  not  compromise all the other daemons.
Group of login records.  Used by the set‐group‐id program to con‐
figure authenticated gateways.  Regular  users,  in  contrast  to
staff  or  guest  users.  Default primary group for new users, as
set in the default file.  Some administrators may instead  prefer
to  assign  to each user a unique group with the same name as the
user by passing the option to Users authorized to  make  outgoing
modem calls.  Unused in modern Pseudo‐group.
