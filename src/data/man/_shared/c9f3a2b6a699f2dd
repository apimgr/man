allows one user to become another user without logging out and in
as  the  new user.  If a is specified and is a member of then the
group is changed to rather than to primary group.  If is  omitted
and  is provided (form two above), then is assumed to be the cur‐
rent username.  When executed by a user, the user’s  password  is
requested.   When  using Kerberos, the password for (or for if no
login is provided) is requested, and switches to  that  user  and
group  ID  after  obtaining a Kerberos ticket granting ticket.  A
shell is then executed, and any additional after the  login  name
are  passed to the shell.  will resort to the local password file
to find the password for if there is a Kerberos error.  If is ex‐
ecuted by root, no password is requested and a shell with the ap‐
propriate user ID is executed; no additional Kerberos tickets are
obtained.   Alternatively,  if  the  user  enters  the   password
"s/key", authentication will use the S/Key one‐time password sys‐
tem  as  described  in  S/Key is a Trademark of Bellcore.  By de‐
fault, the environment is unmodified with the  exception  of  and
and are set to the target login’s default values.  and are set to
the  target login, unless the target login has a user ID of 0, in
which case they are unmodified.  is set to  the  caller’s  login.
The  invoked  shell is the target login’s.  With the exception of
this is the traditional behavior of The options are  as  follows:
Specify  a  login class.  You may only override the default class
if you’re already root.  See for details.  Same as but  does  not
change  the  current directory.  If the invoked shell is this op‐
tion prevents it from reading the file.  If the invoked shell  is
or  this  option  unsets thus preventing the shell from executing
the startup file pointed to by this variable.  Do not attempt  to
use  Kerberos  to  authenticate the user.  Simulate a full login.
The environment is discarded except for and and are  modified  as
above.   and  are  set  to  the target login.  is set to the path
specified in the file (or to the default of ).  is imported  from
your  current  environment.   The invoked shell is the target lo‐
gin’s, and will change directory to the target login’s  home  di‐
rectory.   The  and databases are not updated.  Same as Leave the
environment unmodified.  The invoked shell is your  login  shell,
and  no directory changes are made.  As a security precaution, if
the target user’s shell is a non‐standard shell  (as  defined  by
and  the  caller’s  real uid is non‐zero, will fail.  The and op‐
tions are mutually exclusive; the last  one  specified  overrides
any  previous  ones.   Only  users  in group (normally gid 0), as
listed in can to unless group wheel does not exist or has no mem‐
bers.  (If you do not want anybody to be able to to make the only
member of group which is the default.)  For sites with very large
user populations, group can contain the  names  of  other  groups
that  will  be considered authorized to to By default (unless the
prompt is reset by a startup file) the super‐user prompt  is  set
to  to  remind  one of its awesome power.  For the version of the
name of the required group can be changed by setting in auth req‐
uisite pam_group.so no_warn group=gname root_only  fail_safe  For
the non version of the same can be achieved by compiling with set
to  the desired group name.  can be configured so that users in a
particular group can supply their own password to become For  the
version  of  this  can  be done by adding a line to such as: auth
sufficient pam_group.so no_warn group=gname  root_only  authenti‐
cate where is the name of the desired group.  For the non version
of  the same can be achieved by compiling with set to the desired
group name.  This option is not available with the version of For
the non version of if is defined, the and groups are  treated  as
indirect  groups.   The  group  members  of  those two groups are
treated as groups themselves.  Environment variables used by  De‐
fault home directory of real user ID unless modified as specified
above.   The  user ID is always the effective ID (the target user
ID) after an unless the user ID is 0 (root).  Default search path
of real user ID unless modified  as  specified  above.   Provides
terminal  type which may be retained for the substituted user ID.
The user ID is always the effective ID (the target user ID) after
an unless the user ID is 0 (root).  returns the  exit  status  of
the executed subshell, or 1 if any error occurred while switching
privileges.  To become user username and use the same environment
as  in  original shell, execute: su username To become user user‐
name and use environment as if full login would be performed, ex‐
ecute: su ‐l username When a option is included the  name  it  is
not  a  option, because any arguments after the are passed to the
shell.  (See or for details.)  To execute arbitrary command  with
privileges  of  user  execute:  su  username ‐c "command args" An
utility appeared in
