RDMA_GET_SRC_PORT(3)     Librdmacm Programmer's Manual    RDMA_GET_SRC_PORT(3)



NNAAMMEE
       rdma_get_src_port   -   Returns  the  local  port  number  of  a  bound
       rdma_cm_id.

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<rrddmmaa//rrddmmaa__ccmmaa..hh>>

       uuiinntt1166__tt rrddmmaa__ggeett__ssrrcc__ppoorrtt ((ssttrruucctt rrddmmaa__ccmm__iidd **_i_d));;

AARRGGUUMMEENNTTSS
       id          RDMA identifier.

DDEESSCCRRIIPPTTIIOONN
       Returns the local port number for an rdma_cm_id that has been bound  to
       a local address.

RREETTUURRNN VVAALLUUEE
       Returns  the 16-bit port identifier associated with the local endpoint.
       If the rdma_cm_id is not bound to a port, the returned value is 0.

SSEEEE AALLSSOO
       rdma_bind_addr(3),     rdma_resolve_addr(3),      rdma_get_dst_port(3),
       rdma_get_local_addr(3), rdma_get_peer_addr(3)



librdmacm                         2007-05-15              RDMA_GET_SRC_PORT(3)
