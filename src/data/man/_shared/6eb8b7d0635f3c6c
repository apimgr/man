The  utility  manages certificates used by OpenSSL‐based applica‐
tions as trust anchors for certificate validation in TLS or other
purposes, for example by in HTTPS.  allows configuring the set of
certificates and persistently excluding individual  certificates.
For  trust anchors to validate TLS certificates, OpenSSL applica‐
tions typically use a directory at of hashed certificates in  PEM
format,  with  names like used for lookup; see scans all directo‐
ries in the certificate search path specified by  the  configura‐
tion  file  for  files  called or in PEM format, except for those
that have been excluded by and keeps populated with: symlinks  to
the  original  files in the certificate search path, for applica‐
tions that scan a directory for all files matching or hashed sym‐
links as in a single‐file bundle concatenating all  the  certifi‐
cates  in  PEM format.  treats and as configuration, and strictly
as a cache that can be safely deleted and rebuilt with  can  also
be  instructed  not  to  touch at all by putting in List absolute
paths to trusted certificates, one per line, in format to  encode
any  shell  metacharacters, that would use to populate the cache.
Populate with all trusted certificates, excluding any from  Allow
to  be included in the certificate cache if it is in the certifi‐
cate search path, and rehash the  certificate  cache.   In  other
words, reverse the persistent effect of must be the full absolute
path to a certificate that has been excluded by This does not add
a  new  certificate which is not in the search path.  To do that,
you can create a directory to hold it and put that  directory  in
the search path.  Persistently prevent from being included in the
certificate cache, and rehash the certificate cache.  must be the
full  absolute  path  to a certificate that is in the certificate
search path.  List absolute paths to untrusted certificates,  one
per line, in format to encode any shell metacharacters, that have
been  excluded  by so that will not put them in The configuration
file is a plain text file of lines separated by line feeds.   The
first  line  must  be:  Lines with only whitespace, or whitespace
followed by the comment character are ignored.  Each line  has  a
directive  and  arguments separated by whitespace, and may be ex‐
tended by to continuation lines.  Add to the  certificate  search
path.   must  be  an  absolute pathname, if it has any characters
outside the class All certificates must have  unique  base  names
across  all  directories  in the certificate search path.  Manual
override.  If specified, will modify but may still check  consis‐
tency  of the configuration when run and update Default directory
of hashed TLS CA certificates.  Default single‐file TLS  CA  cer‐
tificate  bundle.  Default configuration file for TLS CA certifi‐
cates.  Default directory of excluded TLS CA  certificates.   All
root  CA certificates published by Mozilla, including untrustwor‐
thy certificates.  All root CA certificates published by  Mozilla
for  use  in code‐signing.  All root CA certificates published by
Mozilla for use in email authentication.  All  root  CA  certifi‐
cates  published by Mozilla for use in TLS server authentication.
Example configuration file netbsd‐certctl 20230816

# Blank lines and comments are ignored.  # Comments begin with  a
‘#’ sign.

# Gather certificates from files called *.cer, *.crt, and *.pem #
under  these  directories.   path /usr/share/certs/mozilla/server
path /usr/pkg/share/chromium‐cacerts

# If the next line is uncommented, certctl(8) will decline  to  #
touch  /etc/openssl/certs.   #manual  Exclude  a  certificate:  $
certctl  untrust  /usr/share/certs/mozilla/server/GTS_Root_R1.pem
There  is no need to run explicitly after but if you do, the set‐
ting will persist.  Rebuild the hashed certificate cache at  from
and $ certctl ‐c /etc/myapplication/certs \
        ‐C /etc/myapplication/certs.conf \
        ‐u  /etc/myapplication/untrusted  The  utility  is mostly
compatible with a utility of the same name in  Differences:  sup‐
ports  destdir/metalog  handling;  does  not.  treats and both as
configuration as caches; treats and as configuration, and  treats
strictly as a cache.  will forget any settings on but will remem‐
ber  them.   takes  configuration  through environment variables;
takes configuration through a file  and  command‐line  arguments.
first  appeared in A utility of the same name previously appeared
in
