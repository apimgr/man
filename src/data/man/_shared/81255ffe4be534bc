The  driver  provides  support  for  the Am53c974 PCscsi‐PCI con‐
troller and boards using this chip, including the  host  adapter.
For  host  adapters,  use the driver.  For host adapters, use the
driver.  The driver currently ignores EEPROM settings, which  es‐
tablish per‐target parameters etc.
