The interface offers an abstraction layer used for communications
between  domains.   is  mainly used by split paravirtualized dri‐
vers, so backend and frontend devices can exchange  configuration
information,  properties,  and  statistics.  is not used for data
transfer (network frames, blocks, PCI commands, ...). This  func‐
tionality  is implemented by each paravirtualized driver indepen‐
dently, typically via shared memory pages and  an  event  channel
that  serves as a virtual interrupt, for signaling.  The abstrac‐
tion offers guests the possibility to read and write  information
directly  from and to XenStore, a centralized database accessible
to all domains.  For this reason, it also has  an  event  channel
associated  to  it, so that domains can post messages to the Xen‐
Store facility.  The event channel associated to  the  interface,
for  communication  with the XenStore database.  The driver first
appeared in The driver was written by
