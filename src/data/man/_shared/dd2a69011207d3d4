PFLOG(4)                 BSD Kernel Interfaces Manual                 PFLOG(4)

NNAAMMEE
     ppfflloogg — packet filter logging interface

SSYYNNOOPPSSIISS
     ddeevviiccee ppfflloogg

DDEESSCCRRIIPPTTIIOONN
     The ppfflloogg interface is a device which makes visible all packets logged by
     the packet filter, pf(4).  Logged packets can easily be monitored in real
     time by invoking tcpdump(1) on the ppfflloogg interface, or stored to disk us‐
     ing pflogd(8).

     The pflog0 interface is created when the ppfflloogg module is loaded; further
     instances can be created using ifconfig(8).  The ppfflloogg module is loaded
     automatically if both pf(4) and pflogd(8) are enabled.

     Each packet retrieved on this interface has a header associated with it
     of length PFLOG_HDRLEN.  This header documents the address family, inter‐
     face name, rule number, reason, action, and direction of the packet that
     was logged.  This structure, defined in ⟨_n_e_t_/_i_f___p_f_l_o_g_._h⟩ looks like

           struct pfloghdr {
                   u_int8_t        length;
                   sa_family_t     af;
                   u_int8_t        action;
                   u_int8_t        reason;
                   char            ifname[IFNAMSIZ];
                   char            ruleset[PF_RULESET_NAME_SIZE];
                   u_int32_t       rulenr;
                   u_int32_t       subrulenr;
                   uid_t           uid;
                   pid_t           pid;
                   uid_t           rule_uid;
                   pid_t           rule_pid;
                   u_int8_t        dir;
                   u_int8_t        pad[3];
                   u_int32_t       ridentifier;
           };

EEXXAAMMPPLLEESS
     Create a ppfflloogg interface and monitor all packets logged on it:

           # ifconfig pflog create
           pflog1
           # ifconfig pflog1 up
           # tcpdump -n -e -ttt -i pflog1

SSEEEE AALLSSOO
     tcpdump(1), inet(4), inet6(4), netintro(4), pf(4), ifconfig(8), pflogd(8)

HHIISSTTOORRYY
     The ppfflloogg device first appeared in OpenBSD 3.0.

BBUUGGSS
     FreeBSD does not set a process id in the _p_i_d field in pfloghdr.

BSD                            October 29, 2021                            BSD
