DPB-REPLAY(1)             BSD General Commands Manual            DPB-REPLAY(1)

NNAAMMEE
     ddppbb--rreeppllaayy — replays dpb terminal output

SSYYNNOOPPSSIISS
     ddppbb--rreeppllaayy [--ccCC] [--ee _r_e_g_e_x_p] [--ss _s_p_e_e_d_u_p] [--tt _t_i_m_e_s_t_a_m_p] _f_i_l_e

DDEESSCCRRIIPPTTIIOONN
     ddppbb--rreeppllaayy is used to replay dpb(1) terminal logs (_%_L_/_t_e_r_m_-_r_e_p_o_r_t_._l_o_g)
     later, usually at a much higher speed than the first run, for demonstra‐
     tion and optimization purposes.  The options are as follows

     --cc||CC         Color display.

     --ee _r_e_g_e_x_p    Start display only when information matches the perl(1) reg‐
                  ular expression _r_e_g_e_x_p.

     --ss _s_p_e_e_d_u_p   Sets the time acceleration to _s_p_e_e_d_u_p (defaults to 10).

     --tt _t_i_m_e_s_t_a_m_p
                  Starts display when the time matches timestamp, as either
                  seconds from the epoch, or a reasonable subset of ISO8601,
                  like ‘2023-07-05T12:00’.

SSEEEE AALLSSOO
     dpb(1)

BSD                            October 12, 2024                            BSD
