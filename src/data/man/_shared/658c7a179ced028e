PAM_LISTFILE(8)                Linux-PAM Manual                PAM_LISTFILE(8)



NNAAMMEE
       pam_listfile - deny or allow services based on an arbitrary file

SSYYNNOOPPSSIISS
       ppaamm__lliissttffiillee..ssoo item=[tty|user|rhost|ruser|group|shell]
                       sense=[allow|deny] file=_/_p_a_t_h_/_f_i_l_e_n_a_m_e
                       onerr=[succeed|fail] [apply=[_u_s_e_r|_@_g_r_o_u_p]] [quiet]

DDEESSCCRRIIPPTTIIOONN
       pam_listfile is a PAM module which provides a way to deny or allow
       services based on an arbitrary file.

       The module gets the iitteemm of the type specified -- _u_s_e_r specifies the
       username, _P_A_M___U_S_E_R; tty specifies the name of the terminal over which
       the request has been made, _P_A_M___T_T_Y; rhost specifies the name of the
       remote host (if any) from which the request was made, _P_A_M___R_H_O_S_T; and
       ruser specifies the name of the remote user (if available) who made the
       request, _P_A_M___R_U_S_E_R -- and looks for an instance of that item in the
       ffiillee==_f_i_l_e_n_a_m_e.  filename contains one line per item listed. If the item
       is found, then if sseennssee==_a_l_l_o_w, _P_A_M___S_U_C_C_E_S_S is returned, causing the
       authorization request to succeed; else if sseennssee==_d_e_n_y, _P_A_M___A_U_T_H___E_R_R is
       returned, causing the authorization request to fail.

       If an error is encountered (for instance, if filename does not exist,
       or a poorly-constructed argument is encountered), then if
       _o_n_e_r_r_=_s_u_c_c_e_e_d, _P_A_M___S_U_C_C_E_S_S is returned, otherwise if _o_n_e_r_r_=_f_a_i_l,
       _P_A_M___A_U_T_H___E_R_R or _P_A_M___S_E_R_V_I_C_E___E_R_R (as appropriate) will be returned.

       An additional argument, aappppllyy==, can be used to restrict the application
       of the above to a specific user (aappppllyy==_u_s_e_r_n_a_m_e) or a given group
       (aappppllyy==_@_g_r_o_u_p_n_a_m_e). This added restriction is only meaningful when used
       with the _t_t_y, _r_h_o_s_t and _s_h_e_l_l items.

       Besides this last one, all arguments should be specified; do not count
       on any default behavior.

       No credentials are awarded by this module.

OOPPTTIIOONNSS
       iitteemm==[[ttttyy||uusseerr||rrhhoosstt||rruusseerr||ggrroouupp||sshheellll]]
           What is listed in the file and should be checked for.

       sseennssee==[[aallllooww||ddeennyy]]
           Action to take if found in file, if the item is NOT found in the
           file, then the opposite action is requested.

       ffiillee==_/_p_a_t_h_/_f_i_l_e_n_a_m_e
           File containing one item per line. The file needs to be a plain
           file and not world writable.

       oonneerrrr==[[ssuucccceeeedd||ffaaiill]]
           What to do if something weird happens like being unable to open the
           file.

       aappppllyy==[[_u_s_e_r||_@_g_r_o_u_p]]
           Restrict the user class for which the restriction apply. Note that
           with iitteemm==[[uusseerr||rruusseerr||ggrroouupp]] this does not make sense, but for
           iitteemm==[[ttttyy||rrhhoosstt||sshheellll]] it have a meaning.

       qquuiieett
           Do not treat service refusals or missing list files as errors that
           need to be logged.

MMOODDUULLEE TTYYPPEESS PPRROOVVIIDDEEDD
       All module types (aauutthh, aaccccoouunntt, ppaasssswwoorrdd and sseessssiioonn) are provided.

RREETTUURRNN VVAALLUUEESS
       PAM_AUTH_ERR
           Authentication failure.

       PAM_BUF_ERR
           Memory buffer error.

       PAM_IGNORE
           The rule does not apply to the aappppllyy option.

       PAM_SERVICE_ERR
           Error in service module.

       PAM_SUCCESS
           Success.

EEXXAAMMPPLLEESS
       Classic 'ftpusers' authentication can be implemented with this entry in
       /etc/pam.d/ftpd:

           #
           # deny ftp-access to users listed in the /etc/ftpusers file
           #
           auth    required       pam_listfile.so \
                   onerr=succeed item=user sense=deny file=/etc/ftpusers


       Note, users listed in /etc/ftpusers file are (counterintuitively) _n_o_t
       allowed access to the ftp service.

       To allow login access only for certain users, you can use a
       /etc/pam.d/login entry like this:

           #
           # permit login to users listed in /etc/loginusers
           #
           auth    required       pam_listfile.so \
                   onerr=fail item=user sense=allow file=/etc/loginusers


       For this example to work, all users who are allowed to use the login
       service should be listed in the file /etc/loginusers. Unless you are
       explicitly trying to lock out root, make sure that when you do this,
       you leave a way for root to log in, either by listing root in
       /etc/loginusers, or by listing a user who is able to _s_u to the root
       account.

SSEEEE AALLSSOO
       ppaamm..ccoonnff(5), ppaamm..dd(5), ppaamm(7)

AAUUTTHHOORR
       pam_listfile was written by Michael K. Johnson <johnsonm@redhat.com>
       and Elliot Lee <sopwith@cuc.edu>.



Linux-PAM Manual                  09/03/2021                   PAM_LISTFILE(8)
