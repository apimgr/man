REL2ABS(3)               BSD Library Functions Manual               REL2ABS(3)

NNAAMMEE
     rreell22aabbss — make an absolute path name from a relative path name

SSYYNNOOPPSSIISS
     _c_h_a_r _*
     rreell22aabbss(_c_o_n_s_t _c_h_a_r _*_p_a_t_h, _c_o_n_s_t _c_h_a_r _*_b_a_s_e, _c_h_a_r _*_r_e_s_u_l_t, _s_i_z_e___t _s_i_z_e);

DDEESSCCRRIIPPTTIIOONN
     The rreell22aabbss() function makes an absolute path name from a relative path
     name _p_a_t_h based on a directory _b_a_s_e and copies the resulting path name
     into the memory referenced by _r_e_s_u_l_t.  The _r_e_s_u_l_t argument must refer to
     a buffer capable of storing at least _s_i_z_e character

     The resulting path name may include symbolic links.  aabbss22rreell() doesn't
     check whether or not any path exists.

RREETTUURRNN VVAALLUUEESS
     The rreell22aabbss() function returns absolute path name on success.  If an er‐
     ror occurs, it returns NULL.

EEXXAAMMPPLLEESS
         char result[MAXPATHLEN];
         char *path = rel2abs("../../src/sys", "/usr/local/lib", result, MAX‐
     PATHLEN);

     yields:

         path == "/usr/src/sys"

     Similarly,

         path1 = rel2abs("src/sys", "/usr", result, MAXPATHLEN);
         path2 = rel2abs(".", "/usr/src/sys", result, MAXPATHLEN);

     yields:

         path1 == "/usr/src/sys"
         path2 == "/usr/src/sys"

EERRRROORRSS
     The rreell22aabbss() function may fail and set the external variable _e_r_r_n_o to
     indicate the error.

     [EINVAL]           The _b_a_s_e directory isn't an absolute path name or the
                        _s_i_z_e argument is zero.

     [ERANGE]           The _s_i_z_e argument is greater than zero but smaller
                        than the length of the pathname plus 1

SSEEEE AALLSSOO
     abs2rel(3)

AAUUTTHHOORRSS
     Shigio Yamaguchi (shigio@tamacom.com)

BSD                             August 7, 2022                             BSD
