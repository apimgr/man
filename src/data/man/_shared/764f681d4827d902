_s_t_a_t_v_f_s(3)                 Library Functions Manual                 _s_t_a_t_v_f_s(3)

NNAAMMEE
       statvfs, fstatvfs - get filesystem statistics

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssyyss//ssttaattvvffss..hh>>

       iinntt ssttaattvvffss((ccoonnsstt cchhaarr **rreessttrriicctt _p_a_t_h,, ssttrruucctt ssttaattvvffss **rreessttrriicctt _b_u_f));;
       iinntt ffssttaattvvffss((iinntt _f_d,, ssttrruucctt ssttaattvvffss **_b_u_f));;

DDEESSCCRRIIPPTTIIOONN
       The  function ssttaattvvffss() returns information about a mounted filesystem.
       _p_a_t_h is the pathname of any file within the mounted filesystem.  _b_u_f is
       a pointer to a _s_t_a_t_v_f_s structure defined approximately as follows:

           struct statvfs {
               unsigned long  f_bsize;    /* Filesystem block size */
               unsigned long  f_frsize;   /* Fragment size */
               fsblkcnt_t     f_blocks;   /* Size of fs in f_frsize units */
               fsblkcnt_t     f_bfree;    /* Number of free blocks */
               fsblkcnt_t     f_bavail;   /* Number of free blocks for
                                             unprivileged users */
               fsfilcnt_t     f_files;    /* Number of inodes */
               fsfilcnt_t     f_ffree;    /* Number of free inodes */
               fsfilcnt_t     f_favail;   /* Number of free inodes for
                                             unprivileged users */
               unsigned long  f_fsid;     /* Filesystem ID */
               unsigned long  f_flag;     /* Mount flags */
               unsigned long  f_namemax;  /* Maximum filename length */
           };

       Here the types _f_s_b_l_k_c_n_t___t and _f_s_f_i_l_c_n_t___t are defined in  _<_s_y_s_/_t_y_p_e_s_._h_>.
       Both used to be _u_n_s_i_g_n_e_d _l_o_n_g.

       The field _f___f_l_a_g is a bit mask indicating various options that were em‐
       ployed  when mounting this filesystem.  It contains zero or more of the
       following flags:

       SSTT__MMAANNDDLLOOCCKK
              Mandatory locking is permitted on the filesystem (see ffccnnttll(2)).

       SSTT__NNOOAATTIIMMEE
              Do not update access times; see mmoouunntt(2).

       SSTT__NNOODDEEVV
              Disallow access to device special files on this filesystem.

       SSTT__NNOODDIIRRAATTIIMMEE
              Do not update directory access times; see mmoouunntt(2).

       SSTT__NNOOEEXXEECC
              Execution of programs is disallowed on this filesystem.

       SSTT__NNOOSSUUIIDD
              The set‐user‐ID and set‐group‐ID bits are ignored by eexxeecc(3) for
              executable files on this filesystem

       SSTT__RRDDOONNLLYY
              This filesystem is mounted read‐only.

       SSTT__RREELLAATTIIMMEE
              Update atime relative to mtime/ctime; see mmoouunntt(2).

       SSTT__SSYYNNCCHHRROONNOOUUSS
              Writes are synched to the filesystem immediately  (see  the  de‐
              scription of OO__SSYYNNCC in ooppeenn(2)).

       It is unspecified whether all members of the returned struct have mean‐
       ingful values on all filesystems.

       ffssttaattvvffss()  returns  the same information about an open file referenced
       by descriptor _f_d.

RREETTUURRNN VVAALLUUEE
       On success, zero is returned.  On error, -1 is returned, and  _e_r_r_n_o  is
       set to indicate the error.

EERRRROORRSS
       EEAACCCCEESS (ssttaattvvffss())  Search  permission is denied for a component of the
              path prefix of _p_a_t_h.  (See also ppaatthh__rreessoolluuttiioonn(7).)

       EEBBAADDFF  (ffssttaattvvffss()) _f_d is not a valid open file descriptor.

       EEFFAAUULLTT _B_u_f or _p_a_t_h points to an invalid address.

       EEIINNTTRR  This call was interrupted by a signal; see ssiiggnnaall(7).

       EEIIOO    An I/O error occurred while reading from the filesystem.

       EELLOOOOPP  (ssttaattvvffss()) Too many symbolic links were encountered  in  trans‐
              lating _p_a_t_h.

       EENNAAMMEETTOOOOLLOONNGG
              (ssttaattvvffss()) _p_a_t_h is too long.

       EENNOOEENNTT (ssttaattvvffss()) The file referred to by _p_a_t_h does not exist.

       EENNOOMMEEMM Insufficient kernel memory was available.

       EENNOOSSYYSS The filesystem does not support this call.

       EENNOOTTDDIIRR
              (ssttaattvvffss())  A component of the path prefix of _p_a_t_h is not a di‐
              rectory.

       EEOOVVEERRFFLLOOWW
              Some values were too large to be  represented  in  the  returned
              struct.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       ssttaattvvffss(), ffssttaattvvffss() T}   Thread safety  MT‐Safe

VVEERRSSIIOONNSS
       Only the SSTT__NNOOSSUUIIDD and SSTT__RRDDOONNLLYY flags of the _f___f_l_a_g field are
       specified in POSIX.1.  To obtain definitions of the remaining flags,
       one must define __GGNNUU__SSOOUURRCCEE.

NNOOTTEESS
       The Linux kernel has system calls ssttaattffss(2) and ffssttaattffss(2) to support
       this library call.

       The glibc implementations of

           pathconf(path, _PC_REC_XFER_ALIGN);
           pathconf(path, _PC_ALLOC_SIZE_MIN);
           pathconf(path, _PC_REC_MIN_XFER_SIZE);

       respectively use the _f___f_r_s_i_z_e, _f___f_r_s_i_z_e, and _f___b_s_i_z_e fields returned by
       a call to ssttaattvvffss() with the argument _p_a_t_h.

       Under Linux, _f___f_a_v_a_i_l is always the same as _f___f_f_r_e_e, and there’s no way
       for a filesystem to report otherwise.  This is not an issue, since no
       filesystems with an inode root reservation exist.

SSTTAANNDDAARRDDSS
       POSIX.1‐2008.

HHIISSTTOORRYY
       POSIX.1‐2001.

       Before glibc 2.13, ssttaattvvffss() populated the bits of the _f___f_l_a_g field by
       scanning the mount options shown in _/_p_r_o_c_/_m_o_u_n_t_s.  However, starting
       with Linux 2.6.36, the underlying ssttaattffss(2) system call provides the
       necessary information via the _f___f_l_a_g_s field, and since glibc 2.13, the
       ssttaattvvffss() function will use information from that field rather than
       scanning _/_p_r_o_c_/_m_o_u_n_t_s.

SSEEEE AALLSSOO
       ssttaattffss(2)

Linux man‐pages (unreleased)      2025‐09‐21                        _s_t_a_t_v_f_s(3)
