The  program allows the manipulation of Lua states in the kernel.
Lua states are created using the command (see below),  Lua  bind‐
ings are provided as modules.  To make a Lua binding available to
a state, it must be Once a module has been by a state, it can not
be  unloaded  from memory using the command until the state using
it has been destroyed.  Lua code can be loaded from the file sys‐
tem into a state at anytime, please note that code loaded into  a
state  is immediately executed.  When executed without a command,
reads information about the states and displays it.  The  options
are  as follows: Create a Lua state before executing the command.
This flag is used for the and commands only, it  is  ignored  for
all  other  commands.  Operate quietly i.e. nothing is printed to
stdout.  Create a Lua state with name  and  optional  description
Destroy  the  Lua  state Load Lua code in file into the Lua state
Note that the path name must contain at least one path separation
character Let the Lua state use the bindings provided  in  module
This is the equivalent of userland Lua code calling the function.
Lua  device  file.  The command first appeared in The program was
written by
