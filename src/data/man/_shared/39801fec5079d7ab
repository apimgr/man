The  utility  is  used to provide a rump kernel service.  Clients
can use the system calls provided by via The  difference  between
and  is  that  offers  only a minimalistic set of features, while
provides all rump kernel components which were available when the
system was built.  At execution time it is possible to load  com‐
ponents  from  the  command line as described in the options sec‐
tion.  Configure virtual CPUs on SMP‐capable archs.  By  default,
the  number  of  CPUs equals the number of CPUs on the host.  The
argument maps a host file in the rump kernel fs  namespace.   The
string  must  be  of  comma‐separated format and must contain the
following tokens: Block device path  in  rump  kernel  namespace.
This  must  be specified according to the rules for a key in Host
file used for storage.  If the file does not exist,  it  will  be
created.   Size of the mapping.  Similar to this argument accepts
a suffix as the multiplier for the number.  The special value in‐
dicates that the current size of will be used.  In this  case  it
is  assumed  that  exists and is a regular file.  Use a disklabel
partition identifier to specify the offset and size of  the  map‐
ping.   must  contain  an existing and valid disklabel within the
first 64k.  The following are optional: Offset  of  the  mapping.
The window into therefore is In case this parameter is not given,
the default value 0 is used.  The type of file that is exposed as
within  the rump kernel.  The possibilities are and for block de‐
vice, character device and regular file, respectively.   The  de‐
fault is a block device.  Note: the contents of block devices are
cached  in  the rump kernel’s buffer cache.  To avoid cache inco‐
herency, it is advisable not to access a file  through  the  host
namespace  while it is mapped as a block device in a rump kernel.
In case does not exist, it will be created as a regular file with
mode 0644 (plus any restrictions placed by umask).  In case is  a
regular file and is not large enough to accommodate the specified
size, it will be extended to the specified size.  Call on library
before  initializing  the rump kernel.  In case provides a kernel
module, it will appear as a builtin module in  the  rump  kernel.
Any  rump  kernel  component present in will also be initialized.
The argument can contain a full path or a filename, in which case
the standard dynamic library search path will be  used.   By  de‐
fault, lazy resolution is used, and may result in a runtime error
due  to  missing  components.   To test a configuration, run with
(see examples).  Load and link a kernel  module  after  the  rump
kernel  is  initialized.   For this to work, the rump kernel must
include the vfs faction, since the module is loaded using  kernel
vfs  code (see Sets the limit of kernel memory allocatable by the
server to as opposed to the default which allows  the  server  to
allocate as much memory as the host will give it.  This parameter
is  especially  useful for VFS servers, since by default the vir‐
tual file system will attempt to consume as  much  memory  as  it
can,  and  accessing large files can cause an excessive amount of
memory to be used as file system cache.  Do not detach  from  the
terminal.   By  default, detaches from the terminal once the ser‐
vice is running on Set bootverbose.  After use, can  be  made  to
exit  using Start a server and load the tmpfs file system module,
and  halt  the  server  immediately  afterwards:  $   rump_server
‐lrumpvfs    ‐m    /modules/tmpfs.kmod    unix://sock    $    env
RUMP_SERVER=unix://sock rump.halt Start a server with the one gi‐
gabyte host file mapped as the block device in the  rump  kernel.
$     rump_allserver    ‐d    key=/dev/dk,hostpath=dk.img,size=1g
unix://sock Start a server which listens on INADDR_ANY port  3755
$  rump_server  tcp://0:3755/  Test that a configuration contains
all of the necessary components: $ env LD_BIND_NOW=1  rump_server
‐lrumpvfs  ‐lrumpfs_ffs  unix://tsock  Start  a FFS server with a
16MB kernel memory limit.  $ rump_server  ‐lrumpfs_ffs  [...]  ‐r
16m unix://ffs
