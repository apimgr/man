is  a  server which will send a message to users currently logged
in to the system.  This server invokes the  command  to  actually
write  the  messages  to  the  system.  Messages are sent to this
server by the command.  The daemon is normally invoked by uses an
RPC protocol defined in
