is  a  touch panel calibration utility.  calibrates a touch panel
and saves and restores the calibration parameters into/from a pa‐
rameter database file.  Available command‐line flags are: Specify
display device name.  Specify touch panel device  name.   Specify
alternate parameter database file name.  Print brief description.
Do  not  change  the parameter database file.  Force calibration.
Without this flag, won’t do calibration if the database file  al‐
ready  contains  parameters  for  the touch panel.  Verbose mode.
You calibrate the touch panel the first time you run If you see a
cross cursor on the screen, you should tap the center of the cur‐
sor to calibrate the touch panel, or you can abort  the  calibra‐
tion  with  the  key.   Five cursors will appear on the screen in
turn.  Once calibration is done, saves the calibration parameters
into the database file and uses the saved parameters to calibrate
the touch panel.  You can run automatically with The default cal‐
ibration parameter database file.  The flag may be used to  spec‐
ify  an alternate database file name.  will create an empty data‐
base file if it doesn’t exist.  The default display device, which
is used to display the cursor during calibration.  The  flag  may
be used to specify an alternate display device name.  The display
device must provide the interface as defined in The default touch
panel device.  The flag may be used to specify an alternate touch
panel  device  name.  isn’t available on all ports because it re‐
quires a display device which provides the interface.
