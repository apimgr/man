can  be  used  to install, examine, or modify the label on a disk
drive or pack.  When writing the label, it can be used to  change
the drive identification, the disk partitions on the drive, or to
replace a damaged label.  The and options determine the basic op‐
eration.  If none are specified the label is displayed.  Edit the
existing label (using and write it back to the disk.  If is unde‐
fined, then is used.  Interactively update the existing label and
write  it  back  to  the  disk.  Show all known file system types
(those that can be specified along a partition within the  label)
and  exit.   Disallow writes to the disk sector that contains the
label.  This is the default state.  Write (restore)  a  label  by
reading  it from The file should be in the same format as the de‐
fault output.  Allow writes to the disk sector that contains  the
label.   This  state may not persist if no programs have the disk
open.  Write a standard label for the specified See The  majority
of  the rest of the options affect more than one form of the com‐
mand: Read all labels from the disk, including ones deleted  with
Implies  Specify  the  byteorder  of the label to be written.  It
should be: or Output the partition offset and size values in for‐
mat.  Note this format is always accepted on  input  with  either
the  or  flags.  Delete all existing labels (by 1’s complementing
the magic number) before writing any labels to their default  lo‐
cation.   Implies  If is specified without a request to write the
label, then existing labels are just deleted.  Treat as a regular
file.  This suppresses all calls, and is the default if is a reg‐
ular file.  is always opened using even if is specified.  Implies
Specify the name of a file to use instead of If a label cannot be
read from request the default one from the kernel.  Implies Spec‐
ify the relative sector number of the disklabel  similar  to  the
environment variable Specify the machine to generate a label for.
Defaults to the current machine it is compiled for.  Specific de‐
tails  can be overridden via environment and with the options and
Expect the label to have an MBR.  Expect the label not to have an
MBR.  Specify the relative byte offset of the  disklabel  similar
to the environment variable Specify the size of the partition ta‐
ble.   Read/write the disk directly rather than using requests on
the kernel.  When writing a label, the kernel will be told  about
the  label  before the label is written and asked to write after‐
wards.  This is the historic behaviour and can be  suppressed  by
specifying  Format  the  output as a entry.  Be verbose about the
operations being done, in particular the disk sectors being  read
and  written.   Specifying  more than once will increase the ver‐
bosity.  On systems that expect to have disks with MBR partitions
(see will find, and update if requested, labels in the  first  8k
of  type  169  MBR labels and within the first 8k of the physical
disk.  On other systems will only look at the start of the  disk.
The  offset at which the labels are written is also system depen‐
dent.  will detect byteswapped labels, but currently cannot  dis‐
play  them.  Previous versions of could update the bootstrap code
on some architectures.  This functionality has been  subsumed  by
The  exit  status  of  is set to indicate any errors or warnings.
The values used are: The utility has completed  successfully.   A
fatal  error  has occurred, such as unknown options passed on the
command line, or writing the disklabel failed.  An I/O  error  of
some  sort occurred.  One or more warnings occurred while reading
the disklabel.  Subtract 100 to get the number  of  warnings  de‐
tected.   Display  the in‐core label for sd0 as obtained via Read
the on‐disk label for sd0, edit it using the built‐in interactive
editor and reinstall in‐core as well as  on‐disk.   As  previous,
but  do  not  fail if there was no label on the disk yet; provide
some default values instead.  As previous, only edit using  $EDI‐
TOR  Create a label for sd0 based on information for found in us‐
ing as the disk pack label.  If you do not have an entry for your
disk in you can use this style to put an initial label onto a new
disk.  Then dump the label to a file (using editing the file, and
replacing the label with Restore the on‐disk  and  in‐core  label
for  sd0  from  information in The kernel device drivers will not
allow the size of a disk partition to be decreased or the  offset
of  a partition to be changed while it is open.  Some device dri‐
vers create a label containing only a single large partition if a
disk is unlabeled; thus, the label must be written to the  parti‐
tion  of  the disk while it is open.  This sometimes requires the
desired label to be set in two steps, the first one  creating  at
least  one  other  partition, and the second setting the label on
the new partition while shrinking the partition.  The utility ap‐
peared in The structure stored  on  disk  cannot  support  parti‐
tions/disks  greater  than  2TB.  Please use and to manage parti‐
tions and disks larger than 2TB.  If the disk  partition  is  not
specified  in  the disk name (i.e., instead of will construct the
full pathname of the disk and use the partition on i386, hpcmips,
or arc, and the partition on all others.  On the sparc,  sparc64,
sun2, and sun3 systems, the size of each partition must be a mul‐
tiple of the number of sectors per cylinder (i.e., each partition
must  be  an  integer number of cylinders), or the boot ROMs will
declare the label invalid and fail to boot the system.  In  addi‐
tion,  the option should never be used on a sparc, sparc64, sun2,
or sun3 system boot disk ‐ the kernel translates the  disk  label
into  a  SunOS  compatible  format (which is required by the boot
PROMs) when it writes the label.  Using the flag causes to  write
directly  to  disk, and bypass the format translation.  This will
result in a disk label that the PROMs  will  not  recognize,  and
that  therefore  cannot be booted from.  The flag previously tog‐
gled between using an MBR and not using an MBR, with the  default
being  implied  by  the  machine type.  It now always enables MBR
mode, independent of the machine default.  Use the flag  to  dis‐
able MBR mode.
