The  driver  provides support for wireless network adapters based
around the ZyDAS ZD1211 and ZD1211B USB  chips.   These  are  the
modes the driver can operate in: Also known as mode, this is used
when  associating with an access point, through which all traffic
passes.  This mode is the default.  In this mode  the  driver  is
able to receive packets without associating with an access point.
This disables the internal receive filter and enables the card to
capture packets from networks which it wouldn’t normally have ac‐
cess  to,  or  to scan for access points.  supports software WEP.
It can be typically configured in one of three modes: no  encryp‐
tion;  40‐bit  encryption; or 104‐bit encryption.  Unfortunately,
due to serious weaknesses in WEP protocol it is  strongly  recom‐
mended  that it not be used as the sole mechanism to secure wire‐
less communication.  WEP is not enabled by default.   The  driver
can  be configured at runtime with or on boot with using the fol‐
lowing parameters: Set the  desired  BSSID.   Unset  the  desired
BSSID.   The  interface will automatically select a BSSID in this
mode, which is the default.  Set the channel (radio frequency) to
be used by the driver based on the given channel ID Unset the de‐
sired channel to be used by the driver.  The driver will automat‐
ically select a channel in this mode, which is the default.   The
driver  supports the following types: Enable autoselection of the
media type and options.  Set 802.11b  DS  1Mbps  operation.   Set
802.11b  DS  2Mbps  operation.  Set 802.11b DS 5.5Mbps operation.
Set 802.11b DS 11Mbps operation.  The driver supports the follow‐
ing media options: Select Independent Basic  Service  Set  (IBSS)
operation.  Disable the specified media options on the driver and
return  it  to the default mode of operation (BSS).  Set the net‐
work ID.  The can either be any text string up to  32  characters
in length, or a series of hexadecimal digits up to 64 digits.  An
empty string allows the interface to connect to any available ac‐
cess  points.   By default the driver uses an empty string.  Note
that network ID is synonymous with Extended Service Set  ID  (ES‐
SID).  Set the network ID to the empty string to allow the inter‐
face  to  connect  to any available access point.  Enable WEP en‐
cryption using the specified The can either be a string, a series
of hexadecimal digits (preceded by or a set of keys of  the  form
where  specifies  which  of the keys will be used for transmitted
packets, and the four keys, through are configured as  WEP  keys.
If a set of keys is specified, a comma within the key must be es‐
caped  with  a  backslash.   Note that if multiple keys are used,
their order must be the same within the network.  is  capable  of
using  both  40‐bit  (5  characters  or 10 hexadecimal digits) or
104‐bit (13 characters or 26 hexadecimal digits)  keys.   Disable
WEP encryption.  This is the default mode of operation.  The fol‐
lowing  devices  are  known  to  be  supported by the driver: The
adapter needs some firmware files, which are loaded on demand  by
the driver when a device is attached: See for how to change this.
The following example configures zyd0 to join whatever network is
available  on  boot,  using  WEP key channel 11: inet 192.168.1.1
netmask 255.255.255.0 nwkey 0x1deadbeef1 chan 11  Configure  zyd0
for  WEP, using hex key # ifconfig zyd0 nwkey 0x1deadbeef1 Return
zyd0 to its default settings: # ifconfig zyd0 ‐bssid ‐chan  media
autoselect \         nwid "" ‐nwkey Join an existing BSS network,
#  ifconfig  zyd0  192.168.0.2 netmask 0xffffff00 nwid my_net For
some reason, the driver was unable to read the firmware file from
the filesystem.  The file might be missing or corrupted.  An  er‐
ror  occurred  while attempting to upload the firmware to the on‐
board microcontroller unit.  An attempt to send a command to  the
firmware failed.  Support for the specified radio chip is not yet
implemented  in  the  driver.  The device will not attach.  Early
revisions of the ZD1211 chipset are not supported by this driver.
The device will not attach.  A frame dispatched to  the  hardware
for transmission did not complete in time.  The driver will reset
the hardware.  This should not happen.  The driver was written by
and The driver does not support a lot of the functionality avail‐
able  in the hardware.  More work is required to properly support
the IBSS and power management features.
