POSIXMQCONTROL(1)         BSD General Commands Manual        POSIXMQCONTROL(1)

NNAAMMEE
     ppoossiixxmmqqccoonnttrrooll — Control POSIX mqueuefs message queues

SSYYNNOOPPSSIISS
     ppoossiixxmmqqccoonnttrrooll _c_r_e_a_t_e --qq _q_u_e_u_e --ss _s_i_z_e --dd _d_e_p_t_h [--mm _m_o_d_e] [--gg _g_r_o_u_p]
                    [--uu _u_s_e_r]
     ppoossiixxmmqqccoonnttrrooll _i_n_f_o --qq _q_u_e_u_e
     ppoossiixxmmqqccoonnttrrooll _r_e_c_v --qq _q_u_e_u_e
     ppoossiixxmmqqccoonnttrrooll _r_m --qq _q_u_e_u_e
     ppoossiixxmmqqccoonnttrrooll _s_e_n_d --qq _q_u_e_u_e --cc _c_o_n_t_e_n_t [--pp _p_r_i_o_r_i_t_y]

DDEESSCCRRIIPPTTIIOONN
     The ppoossiixxmmqqccoonnttrrooll command allows separating POSIX message queue adminis‐
     tration from application stack.  Defining and adjusting queue attributes
     can be done without touching application code.  It allows creating
     queues, inspecting queue metadata, altering group and user access to
     queues, dumping queue contents, and unlinking queues.

     Unlinking removes the name from the system and frees underlying memory.

     The maximum message size, maximum queue size, and current queue size are
     displayed by the iinnffoo subcommand. This output is similar to running ccaatt
     on a mqueuefs queue mounted under a mount point.  This utility requires
     the mmqquueeuueeffss kernel module to be loaded but does not require mmqquueeuueeffss to
     be mounted as a file system.

     The following subcommands are provided:

     ccrreeaattee    Create the named queues, if they do not already exist.  More
               than one queue name may be created. The same maximum queue
               depth and maximum message size are used to create all queues.
               If a queue exists, then depth and size are optional.

               The required _s_i_z_e and _d_e_p_t_h arguments specify the maximum mes‐
               sage size (bytes per message) and maximum queue size (depth or
               number of messages in the queue).  The optional numerical _m_o_d_e
               argument specifies the initial access mode.  If the queue ex‐
               ists but does not match the requested size and depth, this
               utility will attempt to recreate the queue by first unlinking
               and then creating it.  This will fail if the queue is not empty
               or is opened by other processes.

     rrmm        Unlink the queues specified - one attempt per queue.  Failure
               to unlink one queue does not stop this sub-command from at‐
               tempting to unlink the others.

     iinnffoo      For each named queue, dispay the maximum message size, maximum
               queue size, current queue depth, user owner id, group owner id,
               and mode permission bits.

     rreeccvv      Wait for a message from a single named queue and display the
               message to standard output.

     sseenndd      Send messages to one or more named queues.  If multiple mes‐
               sages and multiple queues are specified, the utility attempts
               to send all messages to all queues.  The optional -p priority,
               if omitted, defaults to MQ_PRIO_MAX / 2 or medium priority.

NNOOTTEESS
     A change of queue geometry (maximum message size and/or maximum number of
     messages) requires destroying and re-creating the queue.  As a safety
     feature, the create subcommand refuses to destroy a non-empty queue.  If
     you use the rm subcommand to destroy a queue, any queued messages are
     lost.  To avoid down-time when altering queue attributes, consider creat‐
     ing a new queue and configure reading applications to drain both new and
     old queues.  Retire the old queue once all writers have been updated to
     write to the new queue.

EEXXIITT SSTTAATTUUSS
     The ppoossiixxmmqqccoonnttrrooll utility exits 0 on success, and >0 if an error occurs.

     ••   EX_NOTAVAILABLE usually means the mqueuefs kernel module is not
         loaded.

     ••   EX_USAGE reports one or more incorrect parameters.

EEXXAAMMPPLLEESS
     ••   To retrieve the current message from a named queue, _/_1, use the com‐
         mand
               posixmqcontrol recv -q /1

     ••   To create a queue with the name _/_2 with maximum message size 100 and
         maximum queue depth 10, use the command
               posixmqcontrol create -q /2 -s 100 -d 10

     ••   To send a message to a queue with the name _/_3 use the command
               posixmqcontrol send -q /3 -c 'some choice words.'

     ••   To examine attributes of a queue named _/_4 use the command
               posixmqcontrol info -q /4

SSEEEE AALLSSOO
     mq_getattr(2), mq_open(2), mq_receive(2), mq_send(2), mq_setattr(2),
     mq_unlink(2), mqueuefs(5)

BBUUGGSS
     mq_timedsend and mq_timedrecv are not implemented.  info reports a worst-
     case estimate for QSIZE.

HHIISSTTOORRYY
     The ppoossiixxmmqqccoonnttrrooll command appeared in FreeBSD 15.0.

AAUUTTHHOORRSS
     The ppoossiixxmmqqccoonnttrrooll command and this manual page were written by Rick
     Parrish <_u_n_i_t_r_u_n_k_e_r_@_u_n_i_t_r_u_n_k_e_r_._n_e_t_.>

BSD                            February 19, 2024                           BSD
