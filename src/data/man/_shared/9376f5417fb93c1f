SCTP_PEELOFF(2)             BSD System Calls Manual            SCTP_PEELOFF(2)

NNAAMMEE
     ssccttpp__ppeeeellooffff — detach an association from a one-to-many socket to its own
     fd

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ttyyppeess..hh>>
     ##iinncclluuddee <<ssyyss//ssoocckkeett..hh>>
     ##iinncclluuddee <<nneettiinneett//ssccttpp..hh>>

     _i_n_t
     ssccttpp__ppeeeellooffff(_i_n_t _s, _s_c_t_p___a_s_s_o_c___t _i_d);

DDEESSCCRRIIPPTTIIOONN
     The ssccttpp__ppeeeellooffff() system call attempts detach the association specified
     by _i_d into its own separate socket.

RREETTUURRNN VVAALLUUEESS
     The call returns -1 on failure and the new socket descriptor upon suc‐
     cess.

EERRRROORRSS
     The ssccttpp__ppeeeellooffff() system call can return the following errors:

     [ENOTCONN]         The _i_d given to the call does not map to a valid asso‐
                        ciation.

     [E2BIG]            The size of the address list exceeds the amount of
                        data provided.

     [EBADF]            The argument _s is not a valid descriptor.

     [ENOTSOCK]         The argument _s is not a socket.

SSEEEE AALLSSOO
     sctp(4)

BSD                            October 30, 2007                            BSD
