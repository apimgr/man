When  executing  a program, with the or families of system calls,
reserves a region in the  new  program  image’s  virtual  address
space  for  the which stores return addresses and local variables
for nested procedure  calls  in  program  execution.   Similarly,
threads  created  with  have  regions  allocated  for  per‐thread
stacks.  The stack grows from the where information of the outer‐
most procedure call is stored, fixed at program start, to  the  a
register that points to information used by the current procedure
call, varying during execution as procedures are called.  On most
architectures,  the  stack base is at higher‐numbered virtual ad‐
dresses and the stack pointer is at  lower‐numbered  virtual  ad‐
dresses  —  on  these architectures, On some other architectures,
notably the stack base is at lower‐numbered virtual addresses and
the stack pointer is at higher‐numbered virtual addresses, so  on
those  architectures  In  the kernel, the C preprocessor macro is
defined in on architectures where the stack grows up.   For  sin‐
gle‐threaded  programs, and for the main thread of multi‐threaded
programs, reserves virtual addresses as follows on  architectures
where  the  stack  grows  down: +‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐+ USRSTACK |
stack gap          | +‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐+ stack base | accessi‐
ble pages   | | .                  | | .                   |  <‐‐
stack  pointer  (varies  during execution) | .                  |
+‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐+ (stack base) ‐ (soft stack rlimit) | inac‐
cessible pages |  +‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐+  (stack  base)  ‐  (hard
stack   rlimit)  |  guard/redzone       |  +‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐+
USRSTACK ‐ MAXSSIZ On architectures where the stack grows up, the
layout is: +‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐+ USRSTACK + MAXSSIZ | guard/red‐
zone      | +‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐+ (stack  base)  +  (hard  stack
rlimit)  |  inaccessible  pages  |  +‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐+ (stack
base)  +  (soft  stack  rlimit)  |  .                   |   |   .
|   <‐‐   stack   pointer   (varies   during   execution)   |   .
| | accessible pages    |  +‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐+  stack  base  |
stack gap          | +‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐+ USRSTACK The is allo‐
cated so that any access — read, write, or execute — will deliver
to  the  process.  This serves to detect stack overflow and crash
rather than silently overwrite other memory in the program’s vir‐
tual address space.  The size of the stack guard is tuned by  the
knob.  The stack guard is also sometimes known as the or although
the  term  is also sometimes used to mean a fixed space the stack
pointer (in the direction of stack growth) that the system  guar‐
antees it will not overwrite when calling a signal handler in the
of  some  architectures;  see  also to specify an alternate stack
base for the kernel to use when invoking signal handlers on  sig‐
nal  delivery.  The of the stack region are allocated so that any
access will also deliver to the process, but they can be made ac‐
cessible by changing the soft stack rlimit with The of the  stack
region  are  allocated  with read/write access permitted, and are
used to store the actual data in the program stack.  When address
space layout randomization, is enabled, the is an space of a size
chosen unpredictably at random at program startup time.  When  is
disabled,  the  stack  gap is empty.  All of the boundaries — the
stack base, and the boundaries between the accessible, inaccessi‐
ble, and guard pages — are page‐aligned, or rounded to  be  page‐
aligned  even  if  the  rlimits  are not themselves page‐aligned,
rounding so that the sizes of the regions do not exceed the rlim‐
its.  The stack base is exposed to  programs  via  the  auxiliary
info  vector  entry.   The per‐architecture constants and are de‐
fined in Threads created with have stacks  allocated  at  dynami‐
cally  chosen addresses outside the main thread’s stack region by
default, and their stacks cannot be resized after  creation.   On
architectures   where  the  stack  grows  down,  the  layout  is:
+‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐+ stack  base  =  stackaddr  +  stacksize  +
guardsize  |  stack               |  |  .                   | | .
|   <‐‐   stack   pointer   (varies   during   execution)   |   .
|   +‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐+   stackaddr   |  guard/redzone       |
+‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐+ stackaddr  ‐  guardsize  On  architectures
where  the  stack grows up, the layout is: +‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐+
stackaddr  +  stacksize  +  guardsize  |   guard/redzone        |
+‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐+     stackaddr     +    stacksize    |    .
| | .                  | <‐‐ stack pointer (varies during  execu‐
tion)    |    .                    |   |   stack                |
+‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐‐+ stack  base  =  stackaddr  The  parameters
stackaddr,  stacksize,  and guardsize can be obtained from an ex‐
isting thread using and the family of functions.  When creating a
thread, the stack can be manually allocated  and  the  parameters
can be set using and the family of functions.  However, the stack
parameters  cannot be changed after thread creation.  The default
guard size is tuned by the knob.  For the main thread, returns  a
of  the  parameters  as  they existed at program startup, so that
stackaddr and stacksize reflect the current accessible  pages  of
the  stack, and guardsize is the value of the knob at the time of
program startup.  Note that this  means  the  view  of  the  main
thread’s stack guard may not coincide with the actual stack guard
— it may overlap with, or lie entirely in, the inaccessible pages
of  the stack reserved on program start.  However, if the program
changes its soft stack  rlimit  with  this  snapshot  may  become
stale.   doesn’t  actually guarantee an accessible stack reserva‐
tion of length equal to the soft stack rlimit — owing  to  a  bug
(XXX which PR number?), may sometimes reserve less space than the
soft rlimit, in which case the accessible pages of the stack can‐
not  be extended.  There is a race between the kernel’s access of
at exec time, and userland’s access of in initialization.
