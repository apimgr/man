curs_getcchar(3X)                Library calls               curs_getcchar(3X)



NNAAMMEE
       ggeettcccchhaarr,  sseettcccchhaarr  -  convert  between  a wide-character string and a
       _c_u_r_s_e_s complex character

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ccuurrsseess..hh>>

       iinntt ggeettcccchhaarr((
               ccoonnsstt cccchhaarr__tt **_w_c_h,,
               wwcchhaarr__tt **_w_c,,
               aattttrr__tt **_a_t_t_r_s,,
               sshhoorrtt **_c_o_l_o_r___p_a_i_r,,
               vvooiidd **_o_p_t_s ));;

       iinntt sseettcccchhaarr((
               cccchhaarr__tt **_w_c_h,,
               ccoonnsstt wwcchhaarr__tt **_w_c,,
               ccoonnsstt aattttrr__tt _a_t_t_r_s,,
               sshhoorrtt _c_o_l_o_r___p_a_i_r,,
               ccoonnsstt vvooiidd **_o_p_t_s ));;

DDEESSCCRRIIPPTTIIOONN
   ggeettcccchhaarr
       The ggeettcccchhaarr function gets a wide-character string and rendition from a
       cccchhaarr__tt argument.  When _w_c is not a null pointer, the ggeettcccchhaarr function
       does the following:

       •   Extracts information from a cccchhaarr__tt value _w_c_h

       •   Stores the character attributes in the location pointed to by _a_t_t_r_s

       •   Stores the color pair in the location pointed to by _c_o_l_o_r___p_a_i_r

       •   Stores the wide-character string,  characters  referenced  by  _w_c_h,
           into the array pointed to by _w_c.

       When _w_c is a null pointer, the ggeettcccchhaarr function does the following:

       •   Obtains the number of wide characters pointed to by _w_c_h

       •   Does not change the data referenced by _a_t_t_r_s or _c_o_l_o_r___p_a_i_r

   sseettcccchhaarr
       The sseettcccchhaarr function initializes the location pointed to by _w_c_h by us‐
       ing:

       •   The character attributes in _a_t_t_r_s

       •   The color pair in _c_o_l_o_r___p_a_i_r

       •   The wide-character string pointed to by _w_c.   The  string  must  be
           L'\0' terminated, contain at most one spacing character, which must
           be the first.

           Up to CCCCHHAARRWW__MMAAXX-1 non-spacing characters may  follow.   Additional
           non-spacing characters are ignored.

           The string may contain a single control character instead.  In that
           case, no non-spacing characters are allowed.

RREETTUURRNN VVAALLUUEE
       When _w_c is a null pointer, ggeettcccchhaarr returns the number of wide  charac‐
       ters referenced by _w_c_h, including one for a trailing null.

       When _w_c is not a null pointer, ggeettcccchhaarr returns OOKK upon successful com‐
       pletion, and EERRRR otherwise.

       Upon successful completion, sseettcccchhaarr returns OOKK.  Otherwise, it returns
       EERRRR.

NNOOTTEESS
       The _w_c_h argument may be a value generated by a call to sseettcccchhaarr or by a
       function that has a cccchhaarr__tt output argument.  If _w_c_h is constructed  by
       any other means, the effect is unspecified.

EEXXTTEENNSSIIOONNSS
       X/Open  Curses  documents the _o_p_t_s argument as reserved for future use,
       saying that it must be null.  This implementation uses  that  parameter
       in ABI 6 for the functions which have a color pair parameter to support
       extended color pairs:

       •   For  functions  which modify the color, e.g., sseettcccchhaarr, if _o_p_t_s  is
           set it is treated as a pointer to iinntt, and used to  set  the  color
           pair instead of the sshhoorrtt pair parameter.

       •   For functions which retrieve the color, e.g., ggeettcccchhaarr, if _o_p_t_s  is
           set it is treated as a pointer to iinntt, and  used  to  retrieve  the
           color pair as an iinntt value, in addition retrieving it via the stan‐
           dard pointer to sshhoorrtt parameter.

PPOORRTTAABBIILLIITTYY
       The  CCCCHHAARRWW__MMAAXX  symbol is specific to _n_c_u_r_s_e_s.  X/Open Curses does not
       provide details for the layout of the cccchhaarr__tt structure.  It tells what
       data are stored in it:

       •   a spacing character (wwcchhaarr__tt, i.e., 32-bits).

       •   non-spacing characters (again, wwcchhaarr__tt's).

       •   attributes  (at  least  16 bits, inferred from the various ACS- and
           WACS-flags).

       •   color pair (at least 16 bits,  inferred  from  the  uunnssiiggnneedd  sshhoorrtt
           type).

       The non-spacing characters are optional, in the sense that zero or more
       may be stored in a cccchhaarr__tt.  XOpen/Curses specifies a limit:

           Implementations may limit the number of non-spacing characters that
           can  be  associated with a spacing character, provided any limit is
           at least 5.

       The Unix implementations at the time follow that limit:

       •   AIX 4 and OSF1 4 use the same declaration with an array of  5  non-
           spacing characters _z and a single spacing character _c.

       •   HP-UX 10  uses  an  opaque  structure with 28 bytes, which is large
           enough for the 6 wwcchhaarr__tt values.

       •   Solaris _x_p_g_4 curses uses a single array of 6 wwcchhaarr__tt values.

       This implementation's cccchhaarr__tt was defined in 1995 using 55 for the total
       of  spacing and non-spacing characters (CCCCHHAARRWW__MMAAXX).  That was probably
       due to a misreading of the  AIX 4  header  files,  because  the  X/Open
       Curses  document  was  not generally available at that time.  Later (in
       2002), this detail was overlooked when beginning to implement the func‐
       tions using the structure.

       In  practice, even four non-spacing characters may seem enough.  X/Open
       Curses documents possible uses for  non-spacing  characters,  including
       using  them  for ligatures between characters (a feature apparently not
       supported by any curses implementation).  Unicode does  not  limit  the
       (analogous) number of combining characters, so some applications may be
       affected.

SSEEEE AALLSSOO
       ccuurrsseess(3X), ccuurrss__aattttrr(3X), ccuurrss__ccoolloorr(3X), wwccwwiiddtthh(3)



ncurses 6.5                       2024-04-20                 curs_getcchar(3X)
