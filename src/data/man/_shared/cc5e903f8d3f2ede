SSH-KEYSIGN(8)            BSD System Manager's Manual           SSH-KEYSIGN(8)

NNAAMMEE
     sssshh--kkeeyyssiiggnn — OpenSSH helper for host-based authentication

SSYYNNOOPPSSIISS
     sssshh--kkeeyyssiiggnn

DDEESSCCRRIIPPTTIIOONN
     sssshh--kkeeyyssiiggnn is used by ssh(1) to access the local host keys and generate
     the digital signature required during host-based authentication.

     sssshh--kkeeyyssiiggnn is disabled by default and can only be enabled in the global
     client configuration file _/_e_t_c_/_s_s_h_/_s_s_h___c_o_n_f_i_g by setting EEnnaabblleeSSSSHHKKeeyyssiiggnn
     to “yes”.

     sssshh--kkeeyyssiiggnn is not intended to be invoked by the user, but from ssh(1).
     See ssh(1) and sshd(8) for more information about host-based authentica‐
     tion.

FFIILLEESS
     /etc/ssh/ssh_config
             Controls whether sssshh--kkeeyyssiiggnn is enabled.

     /etc/ssh/ssh_host_ecdsa_key
     /etc/ssh/ssh_host_ed25519_key
     /etc/ssh/ssh_host_rsa_key
             These files contain the private parts of the host keys used to
             generate the digital signature.  They should be owned by root,
             readable only by root, and not accessible to others.  Since they
             are readable only by root, sssshh--kkeeyyssiiggnn must be set-uid root if
             host-based authentication is used.

     /etc/ssh/ssh_host_ecdsa_key-cert.pub
     /etc/ssh/ssh_host_ed25519_key-cert.pub
     /etc/ssh/ssh_host_rsa_key-cert.pub
             If these files exist, they are assumed to contain public certifi‐
             cate information corresponding with the private keys above.

SSEEEE AALLSSOO
     ssh(1), ssh-keygen(1), ssh_config(5), sshd(8)

HHIISSTTOORRYY
     sssshh--kkeeyyssiiggnn first appeared in OpenBSD 3.2.

AAUUTTHHOORRSS
     Markus Friedl <_m_a_r_k_u_s_@_o_p_e_n_b_s_d_._o_r_g>

BSD                              June 17, 2024                             BSD
