The  system is a framework which provides several disciplines for
queuing outgoing network packets.  While traffic shaping is  per‐
haps the most prominent example, provides also other measures re‐
lated  to  QoS.   The framework has been integrated to the packet
filter since At the implementation level modifies  the  interface
packet  queues.   Therefore the driver modifications described in
are required  in  order  to  use  a  certain  network  card  with
/dev/altq  The  system first appeared in March 1997 and found its
home in the KAME project It was imported into  Please  note  that
you  must  compile in the kernel, using the PF alongside built in
the kernel will not work.
