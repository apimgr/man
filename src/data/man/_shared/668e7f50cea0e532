listens  on  for new devices and creates their device nodes using
if they are missing.  When is specified, does  not  go  into  the
background.   With the option it will also exit after the initial
setup of device nodes.  runs This script runs each  script  found
in  in a lexical order with two arguments: or and the device file
name.  The default installation supplies the script for  creating
a  device  node and the script for creating symlinks to wedge de‐
vices under /dev/wedges.  Additional scripts  may  be  added  for
other dynamic device actions.  A utility appeared in
