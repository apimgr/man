displays  kernel  I/O statistics on terminal, disk and CPU opera‐
tions.  By default, displays one line of statistics averaged over
the machine’s run time.  The use of or presents successive  lines
averaged  over  the  period.  The option causes to print raw, un‐
averaged values (totals).  Only the last disk option specified or
is used.  The options are as follows: Repeat the  display  times.
Unless  the  flag is in effect, the first display is for the time
since a reboot and each subsequent report is for the time  period
since the last display.  If no interval is specified, the default
is  1  second.   Show CPU statistics.  This is enabled by default
unless any of the or flags are used.  Show disk statistics.  This
is the default.  Displays number of transfers per  second,  kilo‐
bytes per transfer, and megabytes transferred per second.  Use of
this flag disables the default display of CPU and tty statistics.
Show  alternative disk statistics.  Displays number of transfers,
kilobytes transferred, and time spent in  transfers,  during  the
period  (or since boot with Use of this flag disables the default
display.  Set the page size (length, or  height)  explicitly,  as
the  number  of  lines, If not set, the page length is taken from
the environment variable if set, or from  the  terminal  (window)
size,  if output is to a terminal and its size is set, and other‐
wise defaults to 20.  If explicitly set to zero, pages  are  con‐
sidered  to  be  infinitely  long.  This parameter determines the
frequency at which repeated headers are output.  If the value  is
greater  than  zero, but too small for the header, along with one
output set, then a new header will be produced for  each  set  of
output.   Show  the running total values, rather than an average.
Show tty statistics.  This is enabled by default unless  one,  or
more,  of the or flags are used.  Pause seconds between each dis‐
play.  If no repeat is specified, the default is  infinity.   Set
the  page  width  explicitly, as the number of columns of charac‐
ters, If not set, the page width is taken  from  the  environment
variable if set, or from the terminal (window) size, if output is
to  a terminal and its size is set, and otherwise defaults to 80.
If explicitly set to 0, lines  are  considered  infinitely  long.
This width is used only to determine the number of drives to dis‐
play by default when no drive list is given.  In other cases out‐
put  will  be  as  wide  as needed to display the data requested.
Show extended disk statistics.  Each disk is displayed on a  line
of  its own with all available statistics.  This option overrides
all other display options, and all  disks  are  displayed  unless
specific  disk  names  are  provided as arguments.  Additionally,
separate read and write statistics are displayed.   The  and  op‐
tions  are  ignored  with this output format.  Shows the extended
statistics (as with and additional  queuing  statistics.   Output
does not fit in an 80 column window.  The and options are ignored
with  this  output format.  Replaces drive and CPU statistic out‐
puts that are exactly zero with spaces.  Note  that  zero  values
can  still appear – this indicates a value that was not zero, but
was rounded down so appears as zero.  Drive output is replaced by
spaces only when the drive did no input or output at all  in  the
interval,  or with has never done any I/O.  displays its informa‐
tion in the following  format:  characters  read  from  terminals
characters  written  to terminals Disk operations.  The header of
the field is the disk name and unit number.  If more  drives  are
configured  in  the  system  that fit across the current display,
displays only those drives that fit on the display.  To force  to
display  specific  drives,  they  may  be supplied on the command
line, either as names or patterns.  transfers  per  second  Kilo‐
bytes  transferred  per  disk  transfer Megabytes transferred per
second The alternative display format,  (selected  with  presents
the  following  values: Disk transfers Kilobytes transferred Sec‐
onds spent in disk activity With the flag, the following  queuing
measurements  are  added: Number of I/O requests queued up Number
of currently active I/O requests Average waiting time of  an  I/O
request  in  milliseconds  Average  duration of an I/O request in
milliseconds Seconds spent in the waiting  queue.   Queuing  data
might  not be available from all drivers and is then shown as ze‐
ros.  % of CPU time in user mode % of CPU time in user mode  run‐
ning  niced  processes % of CPU time in system mode % of CPU time
in interrupt mode % of CPU time in idle mode Note that because of
rounding, these percentages may appear to total more or less than
100.  The sections starting with ‘‘Interpreting system activity’’
in appeared in The option was added in Collection of queuing val‐
ues and the option were added in The archaic option format:
   remains supported (the first whose name starts with a digit is
taken to be the period) but is deprecated, and may be removed  in
a future version, so should not be used.
