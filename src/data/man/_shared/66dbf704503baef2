RESOLVCONF(8)             BSD System Manager's Manual            RESOLVCONF(8)

NNAAMMEE
     rreessoollvvccoonnff — a framework for managing multiple DNS configurations

SSYYNNOOPPSSIISS
     rreessoollvvccoonnff --II
     rreessoollvvccoonnff [--mm _m_e_t_r_i_c] [--pp] [--xx] --aa _i_n_t_e_r_f_a_c_e[_._p_r_o_t_o_c_o_l] <_f_i_l_e
     rreessoollvvccoonnff [--ff] --dd _i_n_t_e_r_f_a_c_e[_._p_r_o_t_o_c_o_l]
     rreessoollvvccoonnff [--xx] --iill _p_a_t_t_e_r_n
     rreessoollvvccoonnff --uu
     rreessoollvvccoonnff ----vveerrssiioonn

DDEESSCCRRIIPPTTIIOONN
     rreessoollvvccoonnff manages resolv.conf(5) files from multiple sources, such as
     DHCP and VPN clients.  Traditionally, the host runs just one client and
     that updates _/_e_t_c_/_r_e_s_o_l_v_._c_o_n_f.  More modern systems frequently have wired
     and wireless interfaces and there is no guarantee both are on the same
     network.  With the advent of VPN and other types of networking daemons,
     many things now contend for the contents of _/_e_t_c_/_r_e_s_o_l_v_._c_o_n_f.

     rreessoollvvccoonnff solves this by letting the daemon send their resolv.conf(5)
     file to rreessoollvvccoonnff via stdin(4) with the argument --aa _i_n_t_e_r_f_a_c_e[_._p_r_o_t_o_c_o_l]
     instead of the filesystem.  rreessoollvvccoonnff then updates _/_e_t_c_/_r_e_s_o_l_v_._c_o_n_f as
     it thinks best.  When a local resolver other than libc is installed, such
     as dnsmasq(8) or named(8), then rreessoollvvccoonnff will supply files that the re‐
     solver should be configured to include.

     rreessoollvvccoonnff assumes it has a job to do.  In some situations rreessoollvvccoonnff
     needs to act as a deterrent to writing to _/_e_t_c_/_r_e_s_o_l_v_._c_o_n_f.  Where this
     file cannot be made immutable or you just need to toggle this behaviour,
     rreessoollvvccoonnff can be disabled by adding rreessoollvvccoonnff=NO to resolvconf.conf(5).

     rreessoollvvccoonnff can mark an interfaces _r_e_s_o_l_v_._c_o_n_f as private.  This means
     that the name servers listed in that _r_e_s_o_l_v_._c_o_n_f are only used for
     queries against the domain/search listed in the same file.  This only
     works when a local resolver other than libc is installed.  See
     resolvconf.conf(5) for how to configure rreessoollvvccoonnff to use a local name
     server and how to remove the private marking.

     rreessoollvvccoonnff can mark an interfaces _r_e_s_o_l_v_._c_o_n_f as exclusive.  Only the
     latest exclusive interface is used for processing, otherwise all are.

     When an interface goes down, it should then call rreessoollvvccoonnff with --dd
     _i_n_t_e_r_f_a_c_e_._* arguments to delete the _r_e_s_o_l_v_._c_o_n_f file(s) for all the
     _p_r_o_t_o_c_o_l_s on the _i_n_t_e_r_f_a_c_e.

     Here are some options for the above commands:-

     --ff           Ignore non existent interfaces.  Only really useful for
                  deleting interfaces.

     --mm _m_e_t_r_i_c    Set the metric of the interface when adding it, default of
                  0.  Lower metrics take precedence.  This affects the default
                  order of interfaces when listed.

     --pp           Marks the interface _r_e_s_o_l_v_._c_o_n_f as private.

     --xx           Mark the interface _r_e_s_o_l_v_._c_o_n_f as exclusive when adding,
                  otherwise only use the latest exclusive interface.

     rreessoollvvccoonnff has some more commands for general usage:-

     --ii _p_a_t_t_e_r_n   List the interfaces and protocols, optionally matching
                  _p_a_t_t_e_r_n, we have _r_e_s_o_l_v_._c_o_n_f files for.

     --ll _p_a_t_t_e_r_n   List the _r_e_s_o_l_v_._c_o_n_f files we have.  If _p_a_t_t_e_r_n is specified
                  then we list the files for the interfaces and protocols that
                  match it.

     --uu           Force rreessoollvvccoonnff to update all its subscribers.  rreessoollvvccoonnff
                  does not update the subscribers when adding a resolv.conf
                  that matches what it already has for that interface.

     ----vveerrssiioonn    Echo the resolvconf version to _s_t_d_o_u_t.

     rreessoollvvccoonnff also has some commands designed to be used by it's subscribers
     and system startup:-

     --II           Initialise the state directory _/_v_a_r_/_r_u_n_/_r_e_s_o_l_v_c_o_n_f.  This
                  only needs to be called if the initial system boot sequence
                  does not automatically clean it out; for example the state
                  directory is moved somewhere other than _/_v_a_r_/_r_u_n.  If used,
                  it should only be called once as early in the system boot
                  sequence as possible and before rreessoollvvccoonnff is used to add
                  interfaces.

     --RR           Echo the command used to restart a service.

     --rr _s_e_r_v_i_c_e   If the _s_e_r_v_i_c_e is running then restart it.  If the service
                  does not exist or is not running then zero is returned, oth‐
                  erwise the result of restarting the service.

     --vv           Echo variables DOMAINS, SEARCH and NAMESERVERS so that the
                  subscriber can configure the resolver easily.

     --VV           Same as --vv except that only the information configured in
                  resolvconf.conf(5) is set.

IINNTTEERRFFAACCEE OORRDDEERRIINNGG
     For rreessoollvvccoonnff to work effectively, it has to process the resolv.confs
     for the interfaces in the correct order.  rreessoollvvccoonnff first processes in‐
     terfaces from the iinntteerrffaaccee__oorrddeerr list, then interfaces without a metic
     and that match the ddyynnaammiicc__oorrddeerr list, then interfaces with a metric in
     order and finally the rest in the operating systems lexical order.  See
     resolvconf.conf(5) for details on these lists.

PPRROOTTOOCCOOLLSS
     Here are some suggested protocol tags to use for each _r_e_s_o_l_v_._c_o_n_f file
     registered on an _i_n_t_e_r_f_a_c_e:-

     dhcp         Dynamic Host Configuration Protocol.  Initial versions of
                  rreessoollvvccoonnff did not recommend a _p_r_o_t_o_c_o_l tag be appended to
                  the _i_n_t_e_r_f_a_c_e name.  When the protocol is absent, it is as‐
                  sumed to be the DHCP protocol.

     ppp          Point-to-Point Protocol.

     ra           IPv6 Router Advertisement.

     dhcp6        Dynamic Host Configuration Protocol, version 6.

IIMMPPLLEEMMEENNTTAATTIIOONN NNOOTTEESS
     If a subscriber has the executable bit then it is executed otherwise it
     is assumed to be a shell script and sourced into the current environment
     in a subshell.  This is done so that subscribers can remain fast, but are
     also not limited to the shell language.

     Portable subscribers should not use anything outside of _/_b_i_n and _/_s_b_i_n
     because _/_u_s_r and others may not be available when booting.  Also, it
     would be unwise to assume any shell specific features.

EENNVVIIRROONNMMEENNTT
     _I_F___M_E_T_R_I_C
     If the --mm option is not present then we use _I_F___M_E_T_R_I_C for the metric.

     _I_F___P_R_I_V_A_T_E
     Marks the interface _r_e_s_o_l_v_._c_o_n_f as private.

     _I_F___E_X_C_L_U_S_I_V_E
     Marks the interface _r_e_s_o_l_v_._c_o_n_f as exclusive.

FFIILLEESS
     /etc/resolv.conf.bak
     Backup file of the original resolv.conf.

     /etc/resolvconf.conf
     Configuration file for rreessoollvvccoonnff.

     /libexec/resolvconf
     Directory of subscribers which are run every time rreessoollvvccoonnff adds,
     deletes or updates.

     /libexec/resolvconf/libc.d
     Directory of subscribers which are run after the libc subscriber is run.

     /var/run/resolvconf
     State directory for rreessoollvvccoonnff.

SSEEEE AALLSSOO
     resolver(3), stdin(4), resolv.conf(5), resolvconf.conf(5)

HHIISSTTOORRYY
     This implementation of rreessoollvvccoonnff is called openresolv and is fully com‐
     mand line compatible with Debian's resolvconf, as written by Thomas Hood.

AAUUTTHHOORRSS
     Roy Marples <_r_o_y_@_m_a_r_p_l_e_s_._n_a_m_e>

BBUUGGSS
     Please report them to hhttttpp::////rrooyy..mmaarrpplleess..nnaammee//pprroojjeeccttss//ooppeennrreessoollvv

     rreessoollvvccoonnff does not validate any of the files given to it.

     When running a local resolver other than libc, you will need to configure
     it to include files that rreessoollvvccoonnff will generate.  You should consult
     resolvconf.conf(5) for instructions on how to configure your resolver.

BSD                            November 29, 2016                           BSD
