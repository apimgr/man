builds  a 7th Edition(V7) file system on the specified If it is a
device, the size information will be taken from  the  disk  label
and  before  running  the  disk  must be labeled using the proper
fstype is Otherwise, the size must be specified  on  the  command
line.   V7  filesystem’s block size and sector size are 512 byte.
Disk address limits are 24 bit.  The following arguments are sup‐
ported: Specify the metadata byte order of the file system to  be
created.   Valid  byte  orders are and If no byte order is speci‐
fied, the file system is created in host byte order.  Create file
system to a regular file (needs the option).  This specifies  the
number of inodes for the filesystem.  If the number of inodes ex‐
ceeds  65536,  it  is  reduced to 65536.  Create file system with
specified number of disk sectors.  This controls  the  amount  of
information  written  to  stdout: No output.  Overall size, ilist
size, endian and filename length.  A progress bar.  More  verbose
message.   The  default  is  3.  Fill file with zeroes instead of
creating a sparse file.  A utility appeared in was written by
