and  are  used to transmit binary files over transmission mediums
that do not support other than simple data.   The  following  op‐
tions  are  available:  Use  base64 encoding.  For the historical
uuencode algorithm is the default.  For by default  the  encoding
is  automatically  detected.   Send the decoded output data to By
default, uses the recorded in the  header  of  the  encoded  data
stream.   Write the decoded file to standard output instead of to
a file.  reads (or by default the standard input) and  writes  an
encoded  version  to  (always) the standard output.  The encoding
uses only printing characters suitable  for  text‐only  transport
media.   The  string is inserted into the output header as the to
use at time.  The header also includes the mode (permissions)  of
the  file.   transforms files (or by default, the standard input)
into the original form.  The resulting file is named as  recorded
in the encoded file, or as specified by the option, and will have
the mode of the original file except that setuid and execute bits
are  not  retained.  If the option is specified, or if the output
file name is given as then the data will be written to the  stan‐
dard  output instead of to a named file.  ignores any leading and
trailing lines.  The encoded form of the file is expanded by 35%.
Every 3 bytes become 4 plus control information.  The and  utili‐
ties  exits 0 on success, and >0 if an error occurs.  The follow‐
ing example packages up a source tree, compresses  it,  uuencodes
it  and mails it to a user on another system.  tar czf - src_tree
| uuencode src_tree.tgz | mail user@example.com On the other sys‐
tem, if the user saves the mail to the file the following example
creates the file and extracts it to make a copy of  the  original
tree.   uudecode temp tar xzf src_tree.tgz The and utilities con‐
form to The and utilities appeared in When using with files  com‐
ing  from  dubious sources, always either explicitly pass the op‐
tion or check the header (the first line) of the encoded file for
safety.  Blindly using a from a hostile source can overwrite  im‐
portant files.
