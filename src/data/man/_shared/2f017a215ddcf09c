IF_NTB(4)                BSD Kernel Interfaces Manual                IF_NTB(4)

NNAAMMEE
     iiff__nnttbb — Virtual Ethernet interface for Non-Transparent Bridges

SSYYNNOOPPSSIISS
     To compile this driver into your kernel, place the following lines in
     your kernel configuration file:

           ddeevviiccee nnttbb
           ddeevviiccee nnttbb__ttrraannssppoorrtt
           ddeevviiccee iiff__nnttbb

     Or, to load the driver as a module at boot, place the following line in
     loader.conf(5):

           if_ntb_load="YES"

     The following tunables are settable from the loader(8):

     _h_w_._i_f___n_t_b_._n_u_m___q_u_e_u_e_s
     Limits maximal number of queues per interface.  Default is unlimited.

DDEESSCCRRIIPPTTIIOONN
     The iiff__nnttbb driver attaches on top of the ntb_transport(4) driver to uti‐
     lize one or more of its packet queues to create virtual Ethernet network
     interface between the systems.  Typical MTU for the interface is about
     64KB to reduce overhead.  Default MAC address for the interface is ran‐
     domly generated.

     The iiff__nnttbb driver does not implement any real hardware offload, but since
     PCIe link is protected by CRC32, in some situations it may be possible to
     save some CPU cycles by enabling fake checksum offload on both link sides
     via setting rrxxccssuumm and ttxxccssuumm interface options.

SSEEEE AALLSSOO
     ntb_transport(4)

AAUUTTHHOORRSS
     The iiff__nnttbb driver was developed by Intel and originally written by Carl
     Delsey <_c_a_r_l_@_F_r_e_e_B_S_D_._o_r_g>.  Later improvements were done by Conrad E.
     Meyer <_c_e_m_@_F_r_e_e_B_S_D_._o_r_g> and Alexander Motin <_m_a_v_@_F_r_e_e_B_S_D_._o_r_g>.

BBUUGGSS
     Linux supports only one queue per interface, so manual configuration may
     be required for compatibility.

BSD                            September 2, 2017                           BSD
