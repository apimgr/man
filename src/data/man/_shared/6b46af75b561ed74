ZPOOL-OFFLINE(8)          BSD System Manager's Manual         ZPOOL-OFFLINE(8)

NNAAMMEE
     zzppooooll--oofffflliinnee — take physical devices offline in ZFS storage pool

SSYYNNOOPPSSIISS
     zzppooooll oofffflliinnee [----ppoowweerr|[--fftt]] _p_o_o_l _d_e_v_i_c_eâ¦
     zzppooooll oonnlliinnee [----ppoowweerr] [--ee] _p_o_o_l _d_e_v_i_c_eâ¦

DDEESSCCRRIIPPTTIIOONN
     zzppooooll oofffflliinnee [----ppoowweerr|[--fftt]] _p_o_o_l _d_e_v_i_c_eâ¦
             Takes the specified physical device offline.  While the _d_e_v_i_c_e is
             offline, no attempt is made to read or write to the device.  This
             command is not applicable to spares.

             ----ppoowweerr
                     Power off the device's slot in the storage enclosure.
                     This flag currently works on Linux only

             --ff      Force fault.  Instead of offlining the disk, put it into
                     a faulted state.  The fault will persist across imports
                     unless the --tt flag was specified.

             --tt      Temporary.  Upon reboot, the specified physical device
                     reverts to its previous state.

     zzppooooll oonnlliinnee [----ppoowweerr] [--ee] _p_o_o_l _d_e_v_i_c_eâ¦
             Brings the specified physical device online.  This command is not
             applicable to spares.

             ----ppoowweerr
                     Power on the device's slot in the storage enclosure and
                     wait for the device to show up before attempting to on‐
                     line it.  Alternatively, you can set the
                     ZZPPOOOOLL__AAUUTTOO__PPOOWWEERR__OONN__SSLLOOTT environment variable to always
                     enable this behavior.  This flag currently works on Linux
                     only

             --ee      Expand the device to use all available space.  If the de‐
                     vice is part of a mirror or raidz then all devices must
                     be expanded before the new space will become available to
                     the pool.

SSEEEE AALLSSOO
     zpool-detach(8), zpool-remove(8), zpool-reopen(8), zpool-resilver(8)

BSD                             August 9, 2019                             BSD
