_f_r_e_x_p(3)                   Library Functions Manual                   _f_r_e_x_p(3)

NNAAMMEE
       frexp, frexpf, frexpl - convert floating‐point number to fractional and
       integral components

LLIIBBRRAARRYY
       Math library (_l_i_b_m, _-_l_m)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<mmaatthh..hh>>

       ddoouubbllee ffrreexxpp((ddoouubbllee _x,, iinntt **_e));;
       ffllooaatt ffrreexxppff((ffllooaatt _x,, iinntt **_e));;
       lloonngg ddoouubbllee ffrreexxppll((lloonngg ddoouubbllee _x,, iinntt **_e));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       ffrreexxppff(), ffrreexxppll():
           _ISOC99_SOURCE || _POSIX_C_SOURCE >= 200112L
               || /* Since glibc 2.19: */ _DEFAULT_SOURCE
               || /* glibc <= 2.19: */ _BSD_SOURCE || _SVID_SOURCE

DDEESSCCRRIIPPTTIIOONN
       These  functions are used to split the number _x into a normalized frac‐
       tion and an exponent which is stored in _e.

RREETTUURRNN VVAALLUUEE
       These functions return the normalized fraction.  If the argument  _x  is
       not  zero,  the  normalized fraction is _x times a power of two, and its
       absolute value is always in the range 1/2 (inclusive) to 1 (exclusive),
       that is, [0.5,1).

       If _x is zero, then the normalized fraction is zero and zero  is  stored
       in _e.

       If _x is a NaN, a NaN is returned, and the value of _*_e is unspecified.

       If _x is positive infinity (negative infinity), positive infinity (nega‐
       tive infinity) is returned, and the value of _*_e is unspecified.

EERRRROORRSS
       No errors occur.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       ffrreexxpp(), ffrreexxppff(), ffrreexxppll() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       C11, POSIX.1‐2008.

HHIISSTTOORRYY
       C99, POSIX.1‐2001.

       The variant returning _d_o_u_b_l_e also conforms to SVr4, 4.3BSD, C89.

EEXXAAMMPPLLEESS
       The program below produces results such as the following:

           $ ..//aa..oouutt 22556600
           frexp(2560, &e) = 0.625: 0.625 * 2^12 = 2560
           $ ..//aa..oouutt --44
           frexp(-4, &e) = -0.5: -0.5 * 2^3 = -4

   PPrrooggrraamm ssoouurrccee

       #include <float.h>
       #include <math.h>
       #include <stdio.h>
       #include <stdlib.h>

       int
       main(int argc, char *argv[])
       {
           double x, r;
           int e;

           x = strtod(argv[1], NULL);
           r = frexp(x, &e);

           printf("frexp(%g, &e) = %g: %g * %d^%d = %g\n", x, r, r, 2, e, x);
           exit(EXIT_SUCCESS);
       }

SSEEEE AALLSSOO
       llddeexxpp(3), mmooddff(3)

Linux man‐pages (unreleased)      2025‐07‐19                          _f_r_e_x_p(3)
