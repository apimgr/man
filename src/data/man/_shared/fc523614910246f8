allows a user or system administrator to manipulate and configure
disks in various ways.  It is used by specifying a disk to manip‐
ulate,  the command to perform, and any arguments the command may
require.  is the disk (wdN, sdN, ...) containing the  wedges  un‐
less  noted otherwise.  If is called without any command, it dis‐
plays strategy, cache, and all of the wedges of the specified de‐
vice.  The following commands are  supported:  Define  a  on  the
specified disk starting at block number and spanning blocks.  You
need  to  create  the  partition first with or will just name it.
The wedge will have the volume name and the partition type  Valid
choices  for  ptype  would  be and The device name of the virtual
block device assigned to the wedge will be  displayed  after  the
wedge  has  been  successfully created.  See for more information
about disk wedges.  Used for managing  the  kernel’s  bad  sector
list  for  devices.   The  software bad sector list is only main‐
tained if the  option  was  specified  on  kernel  configuration.
Clears the in kernel list of bad sectors.  Prints out the list of
bad  sector ranges recorded by the kernel.  Flushes the in kernel
list and then retries all of the previously recorded bad sectors,
causing the list to self update.  This option be used with  char‐
acter  devices.   Delete  the  wedge specified by its device name
from the specified disk.  Get and display the cache  enables  for
the  specified  device.   Display information about the specified
disk wedge.  in this case is the wedge name.  Specify to keep  or
drop  the in‐core disklabel on the last close of the disk device.
(Keep if is specified, drop if is specified.)  List  all  of  the
wedges  configured  on the specified disk.  With exit with a non‐
zero exit status if there are no wedges configured on that  disk.
With  (quiet  mode) there is no output related to the wedges that
do, or do not, exist.  Delete all wedges configured on the speci‐
fied disk, and autodiscover the wedges again.  Wedges that are in
use are not deleted and conflicting or overlapping wedges are not
created.  You need to list wedges to find out what  has  changed.
Set  the cache enables for the specified device.  The enables are
as follows: Disable all caches on  the  disk.   Enable  the  read
cache,  and  disable  all  other  caches on the disk.  Enable the
write cache, and disable all other caches on  the  disk.   Enable
both  the  read  and write caches on the disk.  If specified, and
the cache enables are savable, saves the  cache  enables  in  the
disk’s  non‐volatile parameter storage.  Get and set the disk I/O
scheduler (buffer queue strategy) on the drive.  If  you  do  not
provide  a  argument,  the  currently  selected  strategy will be
shown.  To set the bufq strategy, the argument must be specified.
must be the name of one of the built‐in kernel  disk  I/O  sched‐
ulers.   To get the list of supported schedulers, use the follow‐
ing command: $ sysctl kern.bufq.strategies Causes  the  cache  on
the  disk  to  be  synchronized,  flushing  all dirty write cache
blocks to the media.  If is specified, the cache  synchronization
command  will  be issued even if the kernel does not believe that
there are any dirty cache blocks in the disk’s cache.  Note:  The
and  commands  only modify the in‐kernel representation of disks;
for modifying information on the disks themselves,  refer  to  or
The  command  first  appeared  in  The  command was written by of
Wasabi Systems, Inc.
