The  function sets the file permission bits of the file specified
by the pathname to sets the permission bits of the specified file
descriptor verifies that the process owner (user) either owns the
file specified by (or or is the super‐user.  The is equivalent to
except in the case where specifies a relative path.  In this case
the file to be changed is determined relative  to  the  directory
associated  with the file descriptor instead of the current work‐
ing directory.  The values for the are constructed by a  bitwise‐
inclusive  OR  of  flags  from  the following list, defined in If
names a symbolic link, then the mode  of  the  symbolic  link  is
changed.  If names a symbolic link, then the mode of the symbolic
link  is changed and if if the path has any other symbolic links,
an error is returned.  If does not reside in  the  hierarchy  be‐
neath the starting directory, an error is returned.  If is passed
the special value in the parameter, the current working directory
is used.  If also is zero, the behavior is identical to a call to
A  mode  is  created from permission bit masks defined in #define
S_IRWXU 0000700    /* RWX  mask  for  owner  */  #define  S_IRUSR
0000400     /* R for owner */ #define S_IWUSR 0000200    /* W for
owner */ #define S_IXUSR 0000100    /* X for owner */

#define S_IRWXG 0000070    /* RWX mask for group */ #define S_IR‐
GRP 0000040    /* R for group */ #define S_IWGRP 0000020    /*  W
for group */ #define S_IXGRP 0000010    /* X for group */

#define  S_IRWXO  0000007     /*  RWX  mask  for other */ #define
S_IROTH 0000004    /* R for  other  */  #define  S_IWOTH  0000002
/* W for other */ #define S_IXOTH 0000001    /* X for other */

#define S_ISUID 0004000    /* set user id on execution */ #define
S_ISGID  0002000     /*  set  group  id  on  execution */ #define
S_ISVTX 0001000    /* save swapped text even  after  use  */  The
(the indicates to the system which executable files are shareable
(the  default)  and  the system maintains the program text of the
files in the swap area. The sticky bit may only be set by the su‐
per user on shareable executable files.   If  mode  (the  ‘sticky
bit’)  is set on a directory, an unprivileged user may not delete
or rename files of other users in that directory. The sticky  bit
may  be set by any user on a directory which the user owns or has
appropriate permissions.  For more details of the  properties  of
the sticky bit, see Writing or changing the owner of a file turns
off  the set‐user‐id and set‐group‐id bits unless the user is the
super‐user.  This makes the system somewhat more secure  by  pro‐
tecting set‐user‐id (set‐group‐id) files from remaining set‐user‐
id  (set‐group‐id)  if they are modified, at the expense of a de‐
gree of compatibility.  Upon successful completion, a value of  0
is  returned.  Otherwise, a value of ‐1 is returned and is set to
indicate the error.  The system call will fail and the file  mode
will be unchanged if: Search permission is denied for a component
of  the  path prefix.  points outside the process’s allocated ad‐
dress space.  Its execution was interrupted by a signal.  An  I/O
error  occurred while reading from or writing to the file system.
Too many symbolic links were encountered in translating the path‐
name.  This is taken to be indicative of a looping symbolic link.
If AT_SYMLINK_NOFOLLOW_ANY was passed and a symbolic link was en‐
countered in translating the pathname.  A component of a pathname
exceeded characters, or an entire path name exceeded  characters.
The named file does not exist.  A component of the path prefix is
not  a directory.  The effective user ID does not match the owner
of the file and the effective user ID is not the super‐user.  The
named file resides on a read‐only file system.  will fail if:  is
not  a valid file descriptor.  refers to a socket, not to a file.
is not a valid file mode.  Its execution  was  interrupted  by  a
signal.   An  I/O error occurred while reading from or writing to
the file system.  The effective user ID does not match the  owner
of the file and the effective user ID is not the super‐user.  The
file  resides on a read‐only file system.  In addition to the er‐
rors, fails if: The argument does not specify  an  absolute  path
and  the argument is neither nor a valid file descriptor open for
searching.  The value of the argument is not valid.  The argument
is not an absolute path and is neither nor a file descriptor  as‐
sociated  with a directory.  if AT_RESOLVE_BENEATH was passed and
does not reside in the directory hierarchy beneath  the  starting
directory.   The  include file is necessary.  The function is ex‐
pected to conform to The  function  is  expected  to  conform  to
POSIX.1‐2008 .  The function call appeared in The system call ap‐
peared in OS X 10.10
