ADJTIME_CONFIG(5)                File formats                ADJTIME_CONFIG(5)



NNAAMMEE
       adjtime_config - information about hardware clock setting and drift
       factor

SSYYNNOOPPSSIISS
       _/_e_t_c_/_a_d_j_t_i_m_e

DDEESSCCRRIIPPTTIIOONN
       The file _/_e_t_c_/_a_d_j_t_i_m_e contains descriptive information about the
       hardware mode clock setting and clock drift factor. The file is read
       and write by hhwwcclloocckk(8); and read by programs like rtcwake to get RTC
       time mode.

       The file is usually located in _/_e_t_c, but tools like hhwwcclloocckk(8) or
       rrttccwwaakkee(8) can use alternative location by command line options if
       write access to _/_e_t_c is unwanted. The default clock mode is "UTC" if
       the file is missing.

       The Hardware Clock is usually not very accurate. However, much of its
       inaccuracy is completely predictable - it gains or loses the same
       amount of time every day. This is called systematic drift. The util
       hhwwcclloocckk(8) keeps the file _/_e_t_c_/_a_d_j_t_i_m_e, that keeps some historical
       information. For more details see "TThhee AAddjjuusstt FFuunnccttiioonn" and "TThhee
       AAddjjttiimmee FFiillee" sections from hhwwcclloocckk(8) man page.

       The _a_d_j_t_i_m_e file is formatted in ASCII.

   FFiirrsstt lliinnee
       Three numbers, separated by blanks:

       ddrriifftt ffaaccttoorr
           the systematic drift rate in seconds per day (floating point
           decimal)

       llaasstt aaddjjuusstt ttiimmee
           the resulting number of seconds since 1969 UTC of most recent
           adjustment or calibration (decimal integer)

       aaddjjuussttmmeenntt ssttaattuuss
           zero (for compatibility with cclloocckk(8)) as a floating point decimal

   SSeeccoonndd lliinnee
       llaasstt ccaalliibbrraattiioonn ttiimmee
           The resulting number of seconds since 1969 UTC of most recent
           calibration. Zero if there has been no calibration yet or it is
           known that any previous calibration is moot (for example, because
           the Hardware Clock has been found, since that calibration, not to
           contain a valid time). This is a decimal integer.

   TThhiirrdd lliinnee
       cclloocckk mmooddee
           Supported values are UUTTCC or LLOOCCAALL. Tells whether the Hardware Clock
           is set to Coordinated Universal Time or local time. You can always
           override this value with options on the hhwwcclloocckk(8) command line.

FFIILLEESS
       _/_e_t_c_/_a_d_j_t_i_m_e

SSEEEE AALLSSOO
       hhwwcclloocckk(8), rrttccwwaakkee(8)

RREEPPOORRTTIINNGG BBUUGGSS
       For bug reports, use the issue tracker at
       https://github.com/util-linux/util-linux/issues.

AAVVAAIILLAABBIILLIITTYY
       aaddjjttiimmee__ccoonnffiigg is part of the util-linux package which can be
       downloaded from Linux Kernel Archive
       <https://www.kernel.org/pub/linux/utils/util-linux/>.



util-linux 2.38.1                 2022-05-11                 ADJTIME_CONFIG(5)
