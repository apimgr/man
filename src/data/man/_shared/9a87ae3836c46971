PT(4)                    BSD Kernel Interfaces Manual                    PT(4)

NNAAMMEE
     pptt — SCSI processor type driver

SSYYNNOOPPSSIISS
     ddeevviiccee pptt

DDEESSCCRRIIPPTTIIOONN
     The pptt driver provides support for a SCSI processor type device.  These
     are usually scanners and other devices using the SCSI link as a communi‐
     cation interface with device specific commands embedded in the data
     stream.

     A SCSI adapter must be separately configured into the system before this
     driver can be used.

     This device supports read(2) and write(2), and the ioctl(2) calls de‐
     scribed below.

IIOOCCTTLLSS
     The following ioctl(2) calls are supported by the pptt driver.  They are
     defined in the header file <_s_y_s_/_p_t_i_o_._h>.

     PTIOCGETTIMEOUT  This ioctl allows userland applications to fetch the
                      current pptt driver read and write timeout.  The value re‐
                      turned is in seconds.

     PTIOCSETTIMEOUT  This ioctl allows userland applications to set the cur‐
                      rent pptt driver read and write timeouts.  The value
                      should be in seconds.

FFIILLEESS
     /dev/pt_N    the _Nth processor device.

SSEEEE AALLSSOO
     cam(4)

HHIISSTTOORRYY
     The pptt driver appeared in FreeBSD 2.1.

BSD                              March 2, 1995                             BSD
