The  file  specifies  which  services  are  enabled during system
startup by the startup scripts invoked by (see and  the  shutdown
scripts  invoked by The file is a shell script that is sourced by
meaning that must contain valid shell commands.  Listed below are
the standard variables that may be set, the values to which  each
may be set, a brief description of what each variable does, and a
reference  to  relevant  manual  pages.  Third party packages may
test for additional variables.  By default, reads (if it is read‐
able) to obtain default values for  various  variables,  and  the
end‐user  may  override these by appending appropriate entries to
the end of scripts that use from also  support  sourcing  an  op‐
tional  end‐user  provided per‐script override file (where is the
contents of the variable in the script).  This may contain  vari‐
able overrides, including allowing the end‐user to override vari‐
ous  control  variables,  and  thus changing the operation of the
script without requiring editing of the script.   Most  variables
are one of two types: enabling variables or flags variables.  En‐
abling  variables,  such as are generally named after the program
or the system they enable, and have boolean values (specified us‐
ing or for true, and or for false, with the values being case in‐
sensitive).  Flags variables, such as have the same name with ap‐
pended, and determine what arguments are passed to the program if
it is enabled.  If a variable that expects to be set is not  set,
or  the value is not one of the allowed values, a warning will be
printed.  Boolean value.  If false, will not run A string.  Extra
arguments to the run by A number.  If non‐blank, use this as  the
number  of  seconds to run a watchdog timer for which will termi‐
nate if the timer expires before the shutdown  script  completes.
Boolean  value.   If false then the system will drop into single‐
user mode during boot.  If set to a non‐empty string, each script
in will be executed in the current shell rather than a sub shell.
This may be faster on slow machines that have an expensive opera‐
tion.  Use this at your own risk!  A rogue command or script  may
inadvertently  prevent boot to multiuser.  A string.  Extra argu‐
ments to the run by A string.  Space separated list  of  directo‐
ries  searched for rc scripts.  The default is All directories in
must be located in the root file system, otherwise they  will  be
silently  skipped.  Boolean value.  If true then the usual output
is suppressed, and invokes the command specified in the  variable
once for each line of suppressed output.  The default value of is
set  from  the flag in the kernel’s variable (see A command to be
executed once per line of suppressed output, when is  true.   The
default  value of is which will display a spinning symbol instead
of each line of output.  Another useful value is which will  dis‐
play  nothing at all.  A string.  Default IPv4 network route.  If
empty or not set, then the contents of (if it exists)  are  used.
A string.  Default IPv6 network route.  If empty or not set, then
the contents of (if it exists) are used.  A string.  NIS (YP) do‐
main  of  host.  If empty or not set, then the contents of (if it
exists) are used.  A space separated  list  of  interface  names.
These  interfaces  will be configured down when going from multi‐
user to single‐user mode or on system shutdown.  A string.   Sets
domain  in  A string.  Sets search in A string of space separated
domain name servers.  Sets nameserver for each value in A string.
Sets sortlist in A string.  Sets options in An unsigned  integer.
Sets the priority of the above DNS to other sources, lowest wins.
Defaults  to  0.  This is important for some stateful interfaces,
for example PPPoE interfaces which have no direct means of notic‐
ing events.  All active interfaces will be automatically added to
this list.  A string.  Name of host.  If empty or not  set,  then
the contents of (if it exists) are used.  A string.  File systems
mounted  very early in the system boot before networking services
are available.  Usually is part of this, because it is needed  by
services  such as which may be required to get the network opera‐
tional.  The default is where the prefix means that it’s  not  an
error  if  the file system is not present in A string.  File sys‐
tems such as that may require network services to be available to
mount, that must be available early in the system boot  for  gen‐
eral services to use.  The default is where the prefix means that
it is not an error if the file system is not present in A string.
Mount non‐legacy ZFS file systems right after mounting local file
systems  listed in variable.  An entry can be prefixed with which
means that it is not an error if the file system is  not  present
among  available  ZFS datasets.  The default is ’’.  A string.  A
file system is checked with during boot before mounting it.  This
option may be used to override the default  command‐line  options
passed  to the program.  When set to assumes yes as the answer to
all operator questions during file system checks.  This might  be
important with hosts where the administrator does not have access
to  the  console  and  an unsuccessful shutdown must not make the
host unbootable even if the file  system  checks  would  fail  in
preen mode.  Boolean value.  If true, loads the modules specified
in  Boolean  value.  Should be true if you have deliberately con‐
figured your system with no swap.  If false and no  swap  devices
are configured, the system will warn you.  Boolean value.  Set to
true  to  have the system resize the root file system to fill its
partition.  Will only attempt to resize the root file  system  if
it is of type ffs and does not have logging enabled.  Defaults to
false.   Boolean  value.  Remove block‐type swap devices at shut‐
down time.  Useful if swapping onto RAIDframe devices.  or Before
removing block‐type swap devices, it is  wise  to  unmount  tmpfs
filesystems to avoid having to swap their contents back into RAM.
By default all tmpfs filesystems that contain no device nodes are
unmounted.  Set to to explicitly specify which filesystems to un‐
mount  before  removing swap.  A space‐separated list of absolute
paths to tmpfs mount points.  If is set to these  tmpfs  filesys‐
tems  will  be forcibly unmounted before removing block‐type swap
devices.  A path.  If set, names a path that  will  be  symlinked
to.   The  path  needs to live on a tmpfs file system.  A typical
value (assuming is mounted on  tmpfs)  would  be  Boolean  value.
Configures  concatenated disk devices according to Boolean value.
Configures cryptographic disk devices.  Requires  See  for  addi‐
tional  details.   Boolean  value.  Configures the logical volume
manager.  See for additional details.  Boolean value.  Configures
RAIDframe disk devices.  See  for  additional  details.   Boolean
value.   Configures  ZFS  storage  pools  and  ZFS  file systems.
Boolean value.  Enables process accounting with Requires  to  ex‐
ist.  Boolean value.  Clear after reboot.  Boolean value.  Create
from  the  output  of Passes A string, either or (empty).  If set
and nonempty, then during boot‐up, after and check  for  or  wait
until  enough  entropy  before any networking is enabled.  If not
enough entropy is available, then: With stop multiuser  boot  and
enter  single‐user  mode instead.  With wait until enough entropy
is available.  Note that may cause the  system  to  hang  indefi‐
nitely  at  boot if it has neither a random seed nor any hardware
random number generators — use with care.  If empty or  not  set,
the system may come to multiuser without entropy, which is unsafe
to  use  on  the internet; it is the operator’s responsibility to
heed warnings from the kernel and the daily report to remedy  the
problem  —  see Boolean value.  Sets preferences for the environ‐
mental systems framework, Requires which is described in  Boolean
value.   Configure  devices.  See Boolean value.  Configures run‐
time link editor directory cache.  Boolean value.  Read  for  how
to  set  mixer values.  List in the devices whose settings are to
be saved at shutdown and restored at  start‐up.   Boolean  value.
Run to trim log files before syslogd starts.  Intended for laptop
users.  Passes Boolean value.  Enables a per‐user directory.  can
be  used to override the default location of the temporary direc‐
tories, See for additional details.  Boolean value.   Checks  and
enables  quotas  by  running  and Boolean value.  During boot‐up,
runs the utility with the flag to seed the random number  subsys‐
tem from an entropy file.  During shutdown, runs the utility with
the  flag  to  save  some random information to the entropy file.
The entropy file name is specified by the variable, and  defaults
to  The  entropy  file  must  be  on  a local file system that is
writable early during boot‐up (just after the file systems speci‐
fied in have been mounted), and correspondingly late during shut‐
down.  Boolean value.  Runs the utility one or more times accord‐
ing to the specification in If does not contain a semicolon  then
it  is expected to contain zero or more flags, followed by one or
more device or type names.  The command will be executed once for
each device or type name.  If the specified flags do not  include
any  of  or then the flags and are added, to specify that entropy
from the relevant device or type should be both collected and es‐
timated.  If the specified flags do not include either of or then
the flag is added, to specify that the non‐flag arguments are de‐
vice names, not type names.  may contain multiple semicolon‐sepa‐
rated segments, in which each segment contains flags  and  device
or type names as described above.  This allows different flags to
be  associated with different device or type names.  For example,
given the following commands will  be  executed:  Boolean  value.
Sets  the real time clock to local time by adjusting the value of
The offset from UTC is calculated automatically according to  the
time  zone information in the file Boolean value.  Runs the util‐
ity.  Passes The directory where crash dumps are stored is speci‐
fied by The default setting is  Boolean  value.   Builds  various
system  databases,  including and entries for Boolean value.  Run
to calibrate touch panel device.  Passes Boolean value.   Updates
the version string in the file to reflect the version of the run‐
ning  kernel.   See Boolean value.  If enabled in addition to up‐
dates a second version string in the file to reflect the version,
architecture, and Build ID of the  installed  userland.   An  op‐
tional  prefix can be provided for this version string in Boolean
value.  Send notification mail to users if any recoverable  files
exist  in  Read for more information.  Boolean value.  Configures
watchdog timers.  Passes Refer to for information on how to  con‐
figure  a timer.  A number.  The system securelevel is set to the
specified value early in the boot process,  before  any  external
logins,  or  other  programs that run users job, are started.  If
set to nothing, the default action is taken, as described in  and
which  contains  definitive  information  about  the  system  se‐
curelevel.  Note that setting to 0 in will actually result in the
system booting with securelevel set to 1, as will raise the level
when completes.  Boolean value.  Allow passwords to include  non‐
alpha  characters,  usually  to  allow NIS/YP netgroups.  Boolean
value.  Load Veriexec fingerprints during startup.  Read for more
information.  A number.  Controls the strict level  of  Veriexec.
Level 0 is learning mode, used when building the signatures file.
It  will  only  output  messages  but  will not enforce anything.
Level 1 will only prevent access to files with a fingerprint mis‐
match.  Level 2 will also deny writing to and removing  of  moni‐
tored  files, as well as enforce access type (as specified in the
signatures file).  Level 3 will take a step further  and  prevent
access  to files that are not monitored.  A number.  Controls the
verbosity of Veriexec.  Recommended operation is at level 0, ver‐
bose output (mostly used when building the signatures file) is at
level 1.  Level 2 is for debugging only and should not  be  used.
A  string.   Flags  to pass to the command.  Boolean value.  Dis‐
ables SMT (Simultaneous Multi‐Threading).  Boolean  value.   ALTQ
configuration/monitoring daemon.  Passes Boolean value.  Sets the
variable  (see  below)  to  the  output of with the flag and sup‐
presses warnings about interfaces in this list that do  not  have
an  ifconfig  file  or variable.  Boolean value.  Runs to dynami‐
cally block hosts on a DoS  according  to  configuration  set  in
Passes  Boolean value.  Set true to configure some or all network
interfaces using dhcpcd.  If you set true, then  must  be  in  or
must  be on the root file system.  If you need to restrict dhcpcd
to one or a number of interfaces, or need a  separate  configura‐
tion per interface, then this should be done in the configuration
file ‐ see for details.  dhcpcd presently ignores the variable in
rc.conf  and  will start wpa_supplicant if a suitable wpa_suppli‐
cant.conf is found  unless  otherwise  instructed  in  Passes  to
dhcpcd.  See for complete documentation.  Boolean value.  Flushes
the  route table on networking startup.  Useful when coming up to
multiuser mode after going down  to  single‐user  mode.   Boolean
value.  Runs the proxy daemon for the Internet File Transfer Pro‐
tocol.   Boolean  value.   Runs the authenticator for IEEE 802.11
networks.  A string.  List of pairs to configure additional  net‐
work  addresses  for  the given configured interface (e.g.  If is
then use the default netmask for the interface.   covers  limited
cases  only  and is considered unrecommended.  We recommend using
variables or files with multiple lines instead.   Boolean  value.
Monitor  dynamic  interfaces  and  perform  actions  upon address
changes.  Passes Boolean value.  Fine grain  control  of  address
and  routing priorities.  A string.  Can be: automatically deter‐
mine from system settings; will read priorities from or  if  that
file  does  not  exist  it will default to IPv6 first, then IPv4.
try IPv4 before IPv6.  try IPv6 before IPv4.  Boolean value.   If
set,  print the resulting prefixes and priorities map.  A string.
An IPv6 node can be a router or a host A host can be  autoconfig‐
ured  based  on  the  information  advertised  by  adjacent  IPv6
routers.  By setting to or you  can  configure  your  node  as  a
router,  a  non‐autoconfigured  host,  or an autoconfigured host.
Invalid values will be ignored, and the node will  be  configured
as a non‐autoconfigured host.  Boolean value.  If is equal to and
is  false, a reject route will be installed on boot to avoid mis‐
configuration relating to unique‐local addresses.   If  is  true,
the  reject  route  won’t  be installed.  Boolean value.  Runs to
load in packet filter specifications from at network  boot  time,
before  any interfaces are configured.  Passes See Boolean value.
Runs to save and restore information for ipnat and ipfilter state
tables.  The information is stored in and Passes  Boolean  value.
Runs  to  read packet log information and log it to a file or the
system log.  Passes A string.  Specifies arguments to  supply  to
Defaults  to A typical example would be to have log directly to a
file bypassing If the argument is used, remember  to  modify  ac‐
cordingly;  for  example:  Boolean value.  Runs to load in the IP
network address translation (NAT)  rules  from  at  network  boot
time,  before  any interfaces are configured.  See Boolean value.
Runs to load in IPsec manual keys and policies  from  at  network
boot  time, before any interfaces are configured.  Boolean value.
Loads at network boot time, and starts Boolean value.   Runs  the
NPF  packet  filter  logging  and  state  synchronization daemon.
Passes A string.  The list of network interfaces to be configured
at boot time.  For each interface "xxN", the system  first  looks
for  ifconfig  parameters in the variable and then in the file If
is false, and neither the variable nor the file is found, a warn‐
ing is printed.  Information in either the variable or  the  file
is  parsed  identically,  except  that, if an variable contains a
single line with embedded semicolons, then  the  value  is  split
into  multiple lines prior to further parsing, treating the semi‐
colon as a line separator.  One common case it to set  the  vari‐
able  to  a set of arguments to be passed to an command after the
interface name.  Refer to for more details  on  files,  and  note
that  the  information there also applies to variables (after the
variables are split into lines).  Boolean value.  Runs to set the
system time from one of the hosts in If is empty, it will attempt
to find a list of hosts in Passes Boolean value.  Enable at  net‐
work boot time: Load the initial configuration before the network
is  up.  After the network has been configured, then load the fi‐
nal rule set A string.  The path of the rule  set  that  will  be
used  when  loading  the final rule set.  Boolean value.  Run for
dumping packet filter logging information to a file.  A  boolean.
Toggles  starting  on startup.  See below.  A string.  If is true
and is not empty, then will check each word in for a  correspond‐
ing  ppp  configuration  file  in  and will call with the option.
Boolean value.  Runs the IKE (ISAKMP/Oakley) key management  dae‐
mon.   Boolean  value.   Run  WPA/802.11i Supplicant for wireless
network devices.  If you set true, then must be in or must be  on
the  root  file  system.   dhcpcd  ignores this variable, see the
variable for details.  Boolean value.  Runs the daemon  to  start
network  server processes (as listed in as necessary.  Passes The
flag turns on libwrap connection logging.   Boolean  value.   The
daemon is required for any services.  These include NFS, NIS, and
Passes  Boolean  value.   Run Boolean value.  Runs the daemon and
passes Boolean value.  Runs the daemon and passes A string.   The
WWW  root  directory.  Used only if is true.  The default setting
is A string.  If non‐blank and is  true,  run  and  cause  it  to
switch  to  the  specified user after initialization.  It is pre‐
ferred to because is requiring extra privileges to start  listen‐
ing  on  default  port 80.  The default setting is Boolean value.
Runs and passes The flag will turn  on  extra  logging.   Boolean
value.   Runs  Boolean value.  Runs and passes A string.  If non‐
blank and is true, run as the unprivileged user and group to will
be added to the list of log sockets  that  listens  to.   Boolean
value.   Runs and passes A string.  If non‐blank and is true, run
as the unprivileged user and group to will be added to  the  list
of  log  sockets  that listens to.  This option requires that the
kernel has compiled in, and  that  is  present.   Boolean  value.
Starts  mail  system.   Boolean  value.   Runs and passes Boolean
value.  Runs and passes Boolean value.  Runs and passes  The  op‐
tion  allows  to  be a master time source as well as a slave.  If
you are also running only one machine running  both  should  have
the  flag  given  to Boolean value.  Runs A string.  If non‐blank
and is true, run to Boolean  value.   Runs  the  DVMRP  multicast
routing  protocol  daemon.  Passes Boolean value.  Runs the RIPng
routing protocol daemon for IPv6.  Passes  Boolean  value.   Runs
the RIP routing protocol daemon.  Passes Boolean value.  Runs the
boot  parameter  server, with as options.  Used to boot and SunOS
4.x systems.  Boolean value.  Runs the Dynamic Host Configuration
Protocol (DHCP) daemon, for assigning IP addresses to  hosts  and
passing  boot  information.   Passes  Boolean value.  Runs Passes
Boolean value.  Runs the DEC MOP protocol daemon; used for  boot‐
ing  VAX and other DEC machines.  Passes Boolean value.  Runs the
Sun Network Disk (ND) Protocol  server.   Passes  Boolean  value.
Runs  the reverse ARP daemon, often used to boot and Sun worksta‐
tions.  Passes Boolean value.  Runs the HP boot protocol  daemon;
used  for  booting  HP workstations.  Passes Boolean value.  Runs
the IPv6 router advertisement daemon, which is used to  advertise
information  about  the subnet to IPv6 end hosts.  Passes This is
only for IPv6 routers, so set to if you use it.   Boolean  value.
Runs  the  X display manager.  These X daemons are available only
with the optional X distribution of Boolean value.  Runs the  X11
font  server,  which  supplies local X font files to X terminals.
Boolean value.  Runs which lets NIS (YP) clients use  information
from  a NIS server.  Passes Boolean value.  Runs which allows re‐
mote NIS users to  update  password  on  master  server.   Passes
Boolean  value.  Runs the NIS (YP) server for distributing infor‐
mation from certain files in Passes The flag causes it to use DNS
for lookups in that fail.  Boolean value.  Runs  the  automounter
daemon,  which  automatically  mounts NFS file systems whenever a
file or directory within that file system is accessed.  Passes  A
string.   The  mount  directory.   Used only if is true.  Boolean
value.  Runs if and/or are true.  Passes Boolean value.  Runs and
passes Boolean value.  The number of local NFS  asynchronous  I/O
server is now controlled via Boolean value.  Sets up a host to be
a NFS server by running and passing Boolean value.  Runs a status
monitoring  daemon  used  when  is  running,  if and/or are true.
Passes Boolean value.  Configure  Bluetooth  support,  comprising
the  following  tasks:  attach  serial  Bluetooth  controllers as
listed in the configuration file.  enable  Bluetooth  controllers
with  useful defaults, plus additional options as detailed below.
optionally, start the Bluetooth Link Key/PIN Code manager,  pass‐
ing configure local Bluetooth drivers as listed in the configura‐
tion file.  optionally, start the Service Discovery server, pass‐
ing  A string.  An optional list of Bluetooth controllers to con‐
figure.  A string.  Additional configuration options for specific
Bluetooth controllers.  A string.  Additional  configuration  op‐
tions  for  Bluetooth  controllers  without  specific  options as
above.  Boolean value.  If set to  false,  disable  starting  the
Bluetooth  Link  Key/PIN Code manager.  Boolean value.  If set to
false, disable starting the Bluetooth Service  Discovery  server.
Boolean  value.  Runs the daemon for the user identification pro‐
tocol.  Passes Boolean value.  Runs  the  server  for  iSCSI  re‐
quests,  Passes  Boolean  value.   Runs  the  Kerberos  v4 and v5
server.  This should be run on Kerberos master and slave servers.
Boolean value.  Runs to support the and commands.  Boolean value.
If set to start the utility and the and daemons at boot time.   A
string.  If is set to these are the flags to pass to the program.
By  default  no  flags are passed.  A string.  If is set to these
are the flags to pass to the daemon.  By  default  no  flags  are
passed.   A  string.  If is set to these are the flags to pass to
the daemon.  By default no  flags  are  passed.   Boolean  value.
Runs  and  passes  Boolean value.  Runs and passes Boolean value.
Runs to pass serial mouse data to the wscons mouse  mux.   Passes
Boolean  value.   Runs  and passes Boolean value.  Configures the
console driver, from the configuration file Boolean value.   Runs
to  provide  copy  and  paste  text  support  in wscons displays.
Passes The file resides in Default settings for sourced by before
the end‐user configuration section.   overrides.   The  file  ap‐
peared in
