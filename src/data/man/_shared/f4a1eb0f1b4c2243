SLEEPQUEUE(9)            BSD Kernel Developer's Manual           SLEEPQUEUE(9)

NNAAMMEE
     iinniitt__sslleeeeppqquueeuueess, sslleeeeppqq__aabboorrtt, sslleeeeppqq__aadddd, sslleeeeppqq__aalllloocc,
     sslleeeeppqq__bbrrooaaddccaasstt, sslleeeeppqq__ffrreeee, sslleeeeppqq__lloocckk, sslleeeeppqq__llooookkuupp,
     sslleeeeppqq__rreelleeaassee, sslleeeeppqq__rreemmoovvee, sslleeeeppqq__ssiiggnnaall, sslleeeeppqq__sseett__ttiimmeeoouutt,
     sslleeeeppqq__sseett__ttiimmeeoouutt__ssbbtt, sslleeeeppqq__sslleeeeppccnntt, sslleeeeppqq__ttiimmeeddwwaaiitt,
     sslleeeeppqq__ttiimmeeddwwaaiitt__ssiigg, sslleeeeppqq__ttyyppee, sslleeeeppqq__wwaaiitt, sslleeeeppqq__wwaaiitt__ssiigg — manage
     the queues of sleeping threads

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ppaarraamm..hh>>
     ##iinncclluuddee <<ssyyss//sslleeeeppqquueeuuee..hh>>

     _v_o_i_d
     iinniitt__sslleeeeppqquueeuueess(_v_o_i_d);

     _i_n_t
     sslleeeeppqq__aabboorrtt(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d);

     _v_o_i_d
     sslleeeeppqq__aadddd(_c_o_n_s_t _v_o_i_d _*_w_c_h_a_n, _s_t_r_u_c_t _l_o_c_k___o_b_j_e_c_t _*_l_o_c_k,
         _c_o_n_s_t _c_h_a_r _*_w_m_e_s_g, _i_n_t _f_l_a_g_s, _i_n_t _q_u_e_u_e);

     _s_t_r_u_c_t _s_l_e_e_p_q_u_e_u_e _*
     sslleeeeppqq__aalllloocc(_v_o_i_d);

     _i_n_t
     sslleeeeppqq__bbrrooaaddccaasstt(_c_o_n_s_t _v_o_i_d _*_w_c_h_a_n, _i_n_t _f_l_a_g_s, _i_n_t _p_r_i, _i_n_t _q_u_e_u_e);

     _v_o_i_d
     sslleeeeppqq__ffrreeee(_s_t_r_u_c_t _s_l_e_e_p_q_u_e_u_e _*_s_q);

     _s_t_r_u_c_t _s_l_e_e_p_q_u_e_u_e _*
     sslleeeeppqq__llooookkuupp(_c_o_n_s_t _v_o_i_d _*_w_c_h_a_n);

     _v_o_i_d
     sslleeeeppqq__lloocckk(_c_o_n_s_t _v_o_i_d _*_w_c_h_a_n);

     _v_o_i_d
     sslleeeeppqq__rreelleeaassee(_c_o_n_s_t _v_o_i_d _*_w_c_h_a_n);

     _v_o_i_d
     sslleeeeppqq__rreemmoovvee(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d, _c_o_n_s_t _v_o_i_d _*_w_c_h_a_n);

     _i_n_t
     sslleeeeppqq__ssiiggnnaall(_c_o_n_s_t _v_o_i_d _*_w_c_h_a_n, _i_n_t _f_l_a_g_s, _i_n_t _p_r_i, _i_n_t _q_u_e_u_e);

     _v_o_i_d
     sslleeeeppqq__sseett__ttiimmeeoouutt(_c_o_n_s_t _v_o_i_d _*_w_c_h_a_n, _i_n_t _t_i_m_o);

     _v_o_i_d
     sslleeeeppqq__sseett__ttiimmeeoouutt__ssbbtt(_c_o_n_s_t _v_o_i_d _*_w_c_h_a_n, _s_b_i_n_t_i_m_e___t _s_b_t, _s_b_i_n_t_i_m_e___t _p_r,
         _i_n_t _f_l_a_g_s);

     _u___i_n_t
     sslleeeeppqq__sslleeeeppccnntt(_c_o_n_s_t _v_o_i_d _*_w_c_h_a_n, _i_n_t _q_u_e_u_e);

     _i_n_t
     sslleeeeppqq__ttiimmeeddwwaaiitt(_c_o_n_s_t _v_o_i_d _*_w_c_h_a_n, _i_n_t _p_r_i);

     _i_n_t
     sslleeeeppqq__ttiimmeeddwwaaiitt__ssiigg(_c_o_n_s_t _v_o_i_d _*_w_c_h_a_n, _i_n_t _p_r_i);

     _i_n_t
     sslleeeeppqq__ttyyppee(_c_o_n_s_t _v_o_i_d _*_w_c_h_a_n);

     _v_o_i_d
     sslleeeeppqq__wwaaiitt(_c_o_n_s_t _v_o_i_d _*_w_c_h_a_n, _i_n_t _p_r_i);

     _i_n_t
     sslleeeeppqq__wwaaiitt__ssiigg(_c_o_n_s_t _v_o_i_d _*_w_c_h_a_n, _i_n_t _p_r_i);

DDEESSCCRRIIPPTTIIOONN
     Sleep queues provide a mechanism for suspending execution of a thread un‐
     til some condition is met.  Each queue is associated with a specific wait
     channel when it is active, and only one queue may be associated with a
     wait channel at any given point in time.  The implementation of each wait
     channel splits its sleepqueue into 2 sub-queues in order to enable some
     optimizations on threads' wakeups.  An active queue holds a list of
     threads that are blocked on the associated wait channel.  Threads that
     are not blocked on a wait channel have an associated inactive sleep
     queue.  When a thread blocks on a wait channel it donates its inactive
     sleep queue to the wait channel.  When a thread is resumed, the wait
     channel that it was blocked on gives it an inactive sleep queue for later
     use.

     The sslleeeeppqq__aalllloocc() function allocates an inactive sleep queue and is used
     to assign a sleep queue to a thread during thread creation.  The
     sslleeeeppqq__ffrreeee() function frees the resources associated with an inactive
     sleep queue and is used to free a queue during thread destruction.

     Active sleep queues are stored in a hash table hashed on the addresses
     pointed to by wait channels.  Each bucket in the hash table contains a
     sleep queue chain.  A sleep queue chain contains a spin mutex and a list
     of sleep queues that hash to that specific chain.  Active sleep queues
     are protected by their chain's spin mutex.  The iinniitt__sslleeeeppqquueeuueess() func‐
     tion initializes the hash table of sleep queue chains.

     The sslleeeeppqq__lloocckk() function locks the sleep queue chain associated with
     wait channel _w_c_h_a_n.

     The sslleeeeppqq__llooookkuupp() returns a pointer to the currently active sleep queue
     for that wait channel associated with _w_c_h_a_n or NULL if there is no active
     sleep queue associated with argument _w_c_h_a_n.  It requires the sleep queue
     chain associated with _w_c_h_a_n to have been locked by a prior call to
     sslleeeeppqq__lloocckk().

     The sslleeeeppqq__rreelleeaassee() function unlocks the sleep queue chain associated
     with wwcchhaann() and is primarily useful when aborting a pending sleep re‐
     quest before one of the wait functions is called.

     The sslleeeeppqq__aadddd() function places the current thread on the sleep queue
     associated with the wait channel _w_c_h_a_n.  The sleep queue chain associated
     with argument _w_c_h_a_n must be locked by a prior call to sslleeeeppqq__lloocckk() when
     this function is called.  If a lock is specified via the _l_o_c_k argument,
     and if the kernel was compiled with ooppttiioonnss IINNVVAARRIIAANNTTSS, then the sleep
     queue code will perform extra checks to ensure that the lock is used by
     all threads sleeping on _w_c_h_a_n.  The _w_m_e_s_g parameter should be a short de‐
     scription of _w_c_h_a_n.  The _f_l_a_g_s parameter is a bitmask consisting of the
     type of sleep queue being slept on and zero or more optional flags.  The
     _q_u_e_u_e parameter specifies the sub-queue, in which the contending thread
     will be inserted.

     There are currently three types of sleep queues:

     SLEEPQ_CONDVAR  A sleep queue used to implement condition variables.
     SLEEPQ_SLEEP    A sleep queue used to implement sleep(9), wakeup(9) and
                     wakeup_one(9).
     SLEEPQ_PAUSE    A sleep queue used to implement pause(9).

     There are currently two optional flag:

     SLEEPQ_INTERRUPTIBLE  The current thread is entering an interruptible
                           sleep.
     SLEEPQ_STOP_ON_BDRY  When thread is entering an interruptible sleep, do
                          not stop it upon arrival of stop action, like
                          SIGSTOP.  Wake it up instead.

     A timeout on the sleep may be specified by calling sslleeeeppqq__sseett__ttiimmeeoouutt()
     after sslleeeeppqq__aadddd().  The _w_c_h_a_n parameter should be the same value from
     the preceding call to sslleeeeppqq__aadddd(), and the sleep queue chain associated
     with _w_c_h_a_n must have been locked by a prior call to sslleeeeppqq__lloocckk().  The
     _t_i_m_o parameter should specify the timeout value in ticks.

     sslleeeeppqq__sseett__ttiimmeeoouutt__ssbbtt() function takes _s_b_t argument instead of _t_i_m_o.  It
     allows to specify relative or absolute wakeup time with higher resolution
     in form of _s_b_i_n_t_i_m_e___t.  The parameter _p_r allows to specify wanted abso‐
     lute event precision.  The parameter _f_l_a_g_s allows to pass additional
     ccaalllloouutt__rreesseett__ssbbtt() flags.

     Once the thread is ready to suspend, one of the wait functions is called
     to put the current thread to sleep until it is awakened and to context
     switch to another thread.  The sslleeeeppqq__wwaaiitt() function is used for non-in‐
     terruptible sleeps that do not have a timeout.  The sslleeeeppqq__ttiimmeeddwwaaiitt()
     function is used for non-interruptible sleeps that have had a timeout set
     via sslleeeeppqq__sseett__ttiimmeeoouutt().  The sslleeeeppqq__wwaaiitt__ssiigg() function is used for in‐
     terruptible sleeps that do not have a timeout.  The
     sslleeeeppqq__ttiimmeeddwwaaiitt__ssiigg() function is used for interruptible sleeps that do
     have a timeout set.  The _w_c_h_a_n argument to all of the wait functions is
     the wait channel being slept on.  The sleep queue chain associated with
     argument _w_c_h_a_n needs to have been locked with a prior call to
     sslleeeeppqq__lloocckk().  The _p_r_i argument is used to set the priority of the
     thread when it is awakened.  If it is set to zero, the thread's priority
     is left alone.

     When the thread is resumed, the wait functions return a non-zero value if
     the thread was awakened due to an interrupt other than a signal or a
     timeout.  If the sleep timed out, then EWOULDBLOCK is returned.  If the
     sleep was interrupted by something other than a signal, then some other
     return value will be returned.

     A sleeping thread is normally resumed by the sslleeeeppqq__bbrrooaaddccaasstt() and
     sslleeeeppqq__ssiiggnnaall() functions.  The sslleeeeppqq__ssiiggnnaall() function awakens the
     highest priority thread sleeping on a wait channel (if SLEEPQ_UNFAIR flag
     is set, thread that went to sleep recently) while sslleeeeppqq__bbrrooaaddccaasstt()
     awakens all of the threads sleeping on a wait channel.  The _w_c_h_a_n argu‐
     ment specifics which wait channel to awaken.  The _f_l_a_g_s argument must
     match the sleep queue type contained in the _f_l_a_g_s argument passed to
     sslleeeeppqq__aadddd() by the threads sleeping on the wait channel.  If the _p_r_i ar‐
     gument does not equal -1, then each thread that is awakened will have its
     priority raised to _p_r_i if it has a lower priority.  The sleep queue chain
     associated with argument _w_c_h_a_n must be locked by a prior call to
     sslleeeeppqq__lloocckk() before calling any of these functions.  The _q_u_e_u_e argument
     specifies the sub-queue, from which threads need to be woken up.

     A thread in an interruptible sleep can be interrupted by another thread
     via the sslleeeeppqq__aabboorrtt() function.  The _t_d argument specifies the thread to
     interrupt.  An individual thread can also be awakened from sleeping on a
     specific wait channel via the sslleeeeppqq__rreemmoovvee() function.  The _t_d argument
     specifies the thread to awaken and the _w_c_h_a_n argument specifies the wait
     channel to awaken it from.  If the thread _t_d is not blocked on the wait
     channel _w_c_h_a_n then this function will not do anything, even if the thread
     is asleep on a different wait channel.  This function should only be used
     if one of the other functions above is not sufficient.  One possible use
     is waking up a specific thread from a widely shared sleep channel.

     The sslleeeeppqq__sslleeeeppccnntt() function offer a simple way to retrieve the number
     of threads sleeping for the specified _q_u_e_u_e, given a _w_c_h_a_n.

     The sslleeeeppqq__ttyyppee() function returns the type of _w_c_h_a_n associated to a
     sleepqueue.

     The sslleeeeppqq__aabboorrtt(), sslleeeeppqq__bbrrooaaddccaasstt(), and sslleeeeppqq__ssiiggnnaall() functions all
     return a boolean value.  If the return value is true, then at least one
     thread was resumed that is currently swapped out.  The caller is respon‐
     sible for awakening the scheduler process so that the resumed thread will
     be swapped back in.  This is done by calling the kkiicckk__pprroocc00() function
     after releasing the sleep queue chain lock via a call to
     sslleeeeppqq__rreelleeaassee().

     The sleep queue interface is currently used to implement the sleep(9) and
     condvar(9) interfaces.  Almost all other code in the kernel should use
     one of those interfaces rather than manipulating sleep queues directly.

SSEEEE AALLSSOO
     callout(9), condvar(9), runqueue(9), scheduler(9), sleep(9)

BSD                              June 19, 2019                             BSD
