is  a  server  which  returns  information  about users currently
logged in to the system.   The  currently  logged  in  users  are
queried  using  the  command.   The daemon is normally invoked by
uses an RPC protocol defined in
