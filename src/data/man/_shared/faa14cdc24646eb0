The  driver supports ACPI Embedded Controllers.  An ACPI Embedded
Controller is typically a small microprocessor that is  responsi‐
ble  for  various  tasks related to The primary task is to handle
specific interrupts, which are mapped to so‐called  General  Pur‐
pose  Events  Other possible functions include embedded access to
other buses such as the The specific events range from user  ini‐
tiated  events to events triggered by the hardware.  When such an
event occurs, typically either a System Management Interrupt or a
System Control Interrupt is raised.  The  latter  is  an  active,
visible,  shareable,  level interrupt.  On most Intel chipsets is
hardwired to the interrupt number 9.  The main task of an  is  to
raise  a  system  control  interrupt.  All GPEs generate SCIs.  A
typical example of the internal wiring  of  could  involve  when,
e.g.,  the adapter is connected, a certain line becomes active, a
given is flagged, and a interrupt is raised by the leading to ex‐
ecution of machine code in order to locate the handler associated
with the event.  A corresponding driver, in this case,  will  fi‐
nally finish the processing of the event.  Due to the reasons de‐
scribed  above,  majority  of  specific drivers are dysfunctional
without It is therefore recommended that is always enabled,  even
though  it may not be required on some older systems.  The driver
appeared in Many machines depend on early attachment of  In  such
cases  the information required by should be available as a sepa‐
rate and optional Embedded Controller Descriptor Table If  an  is
not  available  or  early  attachment  can not be carried out due
other reasons, the initialization of the whole subsystem  may  be
problematic.
