is  used  to  distribute  a map from a master server to any slave
server in the domain. The list of servers of are fetched from the
map The options are as follows: domain to use instead of the  de‐
fault  domain.   Distribute  map  only to one host and not to the
hosts in the ypserver map.  Verbose. Announce what the program is
doing.
