Because  of the flexible nature of ELF, the structures describing
it are available both as 32bit and 64bit versions. This  document
uses  the  32bit  versions,  refer to for the corresponding 64bit
versions.  The four main types of an ELF object file are: A  file
suitable  for execution. It contains the information required for
creating a new process image.  Contains the necessary information
to be run through the link editor to create an  executable  or  a
shared library.  The shared object contains necessary information
which  can  be  used by either the link editor at link time or by
the dynamic loader at run time.  A file which describes the  vir‐
tual  address  space and register state of a process.  Core files
are typically used in conjunction  with  debuggers  such  as  ELF
files  have a dual nature. The toolchain, including tools such as
the and linker treats them as a  set  of  sections  described  by
their  section headers. The system loader treats them as a set of
segments described by the program headers.  The general format of
an ELF file is the following: The file starts with an ELF header.
This is followed by a table of program headers (optional for  re‐
locatable  and  shared  files). After this come the sections/seg‐
ments.  The file ends with a table of section  headers  (optional
for executable files).  A segment can be considered to consist of
several  sections. For example, all executable sections are typi‐
cally packed into one loadable segment which is read‐only and ex‐
ecutable (see in the program header). This enables the system  to
map  the  entire  file  with  just a few operations, one for each
loadable segment, instead of doing numerous  map  operations  for
each  section  separately.   Each  file  is  described by the ELF
header:       typedef       struct       {               unsigned
char   e_ident[ELF_NIDENT];               Elf32_Half      e_type;
        Elf32_Half      e_machine;         Elf32_Word      e_ver‐
sion;                                    Elf32_Addr      e_entry;
        Elf32_Off       e_phoff;         Elf32_Off       e_shoff;
        Elf32_Word      e_flags;            Elf32_Half      e_eh‐
size;                                Elf32_Half      e_phentsize;
        Elf32_Half      e_phnum;         Elf32_Half      e_shent‐
size;                                    Elf32_Half      e_shnum;
        Elf32_Half      e_shstrndx; } Elf32_Ehdr; The array  con‐
tains  the  following information in the indicated locations: The
elements ranging from to contain the ELF magic  number:  \0177ELF
Contains  the  address  size  of  the binary, either 32 or 64bit.
byte order Contains the ELF header version. This is currently al‐
ways set to 1.  Contains the operating system ABI identification.
Note that even though the definition exists, uses here, since the
ABI does not deviate from the standard.  ABI  version.   Contains
the  file  type  identification. It can be either or for relocat‐
able, executable, shared, or core,  respectively.   Contains  the
machine  type,  e.g.  SPARC,  Alpha, MIPS, ...  The program entry
point if the file is executable.  The  position  of  the  program
header  table in the file or 0 if it doesn’t exist.  The position
of the section header table in the file or 0 if it doesn’t exist.
Contains processor‐specific flags. For example,  the  SPARC  port
uses  this space to specify what kind of memory store ordering is
required.  The size of the ELF header.  The size of an  entry  in
the  program  header  table.  All entries are the same size.  The
number of entries in the program header table, or 0 if  none  ex‐
ists.   The size of an entry in the section header table. All en‐
tries are the same size.  The number of entries  in  the  section
header  table, or 0 if none exists.  Contains the index number of
the section which contains the section name  strings.   Each  ELF
section  in  turn  is  described  by  the section header: typedef
struct             {                     Elf32_Word      sh_name;
        Elf32_Word      sh_type;
        Elf32_Word      sh_flags;
        Elf32_Addr      sh_addr;          Elf32_Off       sh_off‐
set;                                     Elf32_Word      sh_size;
        Elf32_Word      sh_link;         Elf32_Word      sh_info;
        Elf32_Word      sh_addralign;
        Elf32_Word      sh_entsize; } Elf32_Shdr; Contains an in‐
dex  to  the  position in the section header string section where
the name of the current section can be found.  Contains the  sec‐
tion type indicator. The more important possible values are: Sec‐
tion  is  inactive.  The  other  fields contain undefined values.
Section contains program information. It can be for example code,
data, or debugger information.  Section contains a symbol  table.
This section usually contains all the symbols and is intended for
the regular link editor Section contains a string table.  Section
contains relocation information with an explicit addend.  Section
contains  a  symbol hash table.  Section contains dynamic linking
information.  Section contains some special information. The for‐
mat can be e.g.  vendor‐specific.  Sections contains  information
similar  to  but  takes up no space in the file. This can be used
for e.g. bss.  Section contains relocation information without an
explicit addend.  This section type is reserved but has  unspeci‐
fied semantics.  Section contains a symbol table. This symbol ta‐
ble  is  intended for the dynamic linker, and is kept as small as
possible to conserve space, since it must be loaded to memory  at
run time.  Contains the section flags, which can have the follow‐
ing  values or any combination of them: Section is writable after
it has been loaded.  Section will  occupy  memory  at  run  time.
Section  contains  executable  machine  instructions.  Address to
where the section will be loaded, or 0 if this section  does  not
reside in memory at run time.  The byte offset from the beginning
of  the  file to the beginning of this section. If the section is
of type this field specifies  the  conceptual  placement  in  the
file.   The  size of the section in the file for all types except
For that type the value may differ from  zero,  but  the  section
will  still  always  take up no space from the file.  Contains an
index to the section header table. The interpretation depends  on
the section type as follows: Section index of the associated sym‐
bol  table.   Section index of the associated string table.  Sec‐
tion index of the symbol table to which the hash  table  applies.
Section  index  of  of  the string table by which entries in this
section are used.  Contains extra information. The interpretation
depends on the type as follows: Section index of the  section  to
which  the  relocation information applies.  Contains a value one
greater that the last local symbol table index.  Marks  the  sec‐
tion alignment requirement. If, for example, the section contains
a  doubleword,  the  entire section must be doubleword aligned to
ensure proper alignment. Only 0 and integral powers  of  two  are
allowed. Values 0 and 1 denote that the section has no alignment.
Contains the entry size of an element for sections which are con‐
structed  of  a  table of fixed‐size entries. If the section does
not hold a table of fixed‐size entries, this value is  0.   Every
executable  object  must  contain  a  program header. The program
header contains information necessary in constructing  a  process
image.     typedef   struct   {           Elf32_Word      p_type;
        Elf32_Off       p_offset;
        Elf32_Addr      p_vaddr;         Elf32_Addr      p_paddr;
        Elf32_Word      p_filesz;
        Elf32_Word      p_memsz;         Elf32_Word      p_flags;
        Elf32_Word      p_align; } Elf32_Phdr; Contains the  seg‐
ment  type  indicator.  The possible values are: Segment is inac‐
tive. The other fields  contain  undefined  values.   Segment  is
loadable.  It is loaded to the address described by If is greater
than the memory range from + to + is zero‐filled when the segment
is loaded.  can not be greater than Segments  of  this  type  are
sorted  in  the header table by in ascending order.  Segment con‐
tains dynamic linking information.  Segment contains a  null‐ter‐
minated path name to the interpreter. This segment may be present
only  once in a file, and it must appear before any loadable seg‐
ments. This field  will  most  likely  contain  the  ELF  dynamic
loader:  Segment contains some special information. Format can be
e.g. vendor‐specific.  This segment type is reserved but has  un‐
specified  semantics.  Programs  which  contain a segment of this
type do not conform to the ABI, and must indicate this by setting
the appropriate ABI in the ELF header field.   The  values  in  a
program  header  of  this type specify the characteristics of the
program header table itself. For example, the field specifies the
program header table location  in  memory  once  the  program  is
loaded.  This  field may not occur more than once, may occur only
if the program header table is part of the file memory image, and
must come before any loadable segments.  Contains the byte offset
from the beginning of the file to the beginning of this  segment.
Contains  the  virtual  memory  address  to which this segment is
loaded.  Contains the physical address to which this  segment  is
loaded.  This  value  is  usually  ignored, but may be used while
bootstrapping or in embedded systems.   Contains  the  number  of
bytes this segment occupies in the file image.  Contains the num‐
ber of bytes this segment occupies in the memory image.  Contains
the  segment flags, which specify the permissions for the segment
after it has been loaded. The following values or any combination
of them is acceptable: Segment can be read.  Segment can be writ‐
ten.  Segment is executable.  Contains the segment alignment. Ac‐
ceptable values are 0 and 1 for no alignment, and integral powers
of two.  should equal modulo The ELF object file format first ap‐
peared in
