The  owner  ID and group ID of the file named by or referenced by
is changed as specified by the arguments and The owner of a  file
may change the to a group of which he or she is a member, but the
change  capability  is  restricted to the super‐user.  The system
call clears the set‐user‐id and set‐group‐id bits  on  the  file.
The  system  call follows symbolic links to operate on the target
of the link rather than the link itself.  The system call is par‐
ticularly useful when used in conjunction with the  file  locking
primitives (see The system call is similar to but does not follow
symbolic  links.  The system call is equivalent to the and except
in the case where specifies a relative path.  In  this  case  the
file  to be changed is determined relative to the directory asso‐
ciated with the file descriptor instead of  the  current  working
directory.   Values for are constructed by a bitwise‐inclusive OR
of flags from the following list, defined in If names a  symbolic
link, ownership of the symbolic link is changed.  If names a sym‐
bolic  link,  ownership  of the symbolic link is changed.  If any
any other symbolic link is encountered it is not followed and  an
error  is  returned instead.  If does not reside in the hierarchy
beneath the starting directory, an  error  is  returned.   If  is
passed  the  special  value in the parameter, the current working
directory is used and the behavior is identical to a call  to  or
respectively,  depending  on whether or not the bit is set in the
argument.  One of the owner or group id’s may be  left  unchanged
by  specifying  it  as  ‐1.   The  and system calls will fail if:
Search permission is denied for a component of the  path  prefix.
The  argument  points  outside  the  process’s  allocated address
space.  Too many symbolic links are  encountered  in  translating
the  pathname.   This is taken to be indicative of a looping sym‐
bolic link.  If AT_SYMLINK_NOFOLLOW_ANY is passed a symbolic link
was encountered in translating the pathname.  A  component  of  a
pathname exceeded 255 characters, or an entire path name exceeded
1023  characters.  A component of does not exist.  A component of
the path prefix is not a directory.  The system  call  will  fail
if: The argument does not refer to a valid descriptor.  The argu‐
ment  refers  to  a  socket, not a file.  Any of these calls will
fail if: Its execution is interrupted by a signal.  An I/O  error
occurs while reading from or writing to the file system.  The ef‐
fective  user  ID  does  not  match the owner of the file and the
calling process does not have  appropriate  (i.e.,  root)  privi‐
leges.   The  named  file resides on a read‐only file system.  In
addition to the errors specified for and the system call may fail
if: The argument does not specify an absolute path and the  argu‐
ment  is  neither nor a valid file descriptor open for searching.
The value of the argument is not valid.  The argument is  not  an
absolute  path  and  is  neither nor a file descriptor associated
with a directory.  if AT_RESOLVE_BENEATH was passed and does  not
reside in the directory hierarchy beneath the starting directory.
The  system call is expected to conform to The system call is ex‐
pected to conform to POSIX.1‐2008 .  The function appeared in The
system call appeared in The and system calls were changed to fol‐
low symbolic links in The system call was added in to  compensate
for  the loss of functionality.  The system call appeared in OS X
10.10
