I2C(8)                    BSD System Manager's Manual                   I2C(8)

NNAAMMEE
     ii22cc — test I2C bus and slave devices

SSYYNNOOPPSSIISS
     ii22cc --aa _a_d_d_r_e_s_s [--ff _d_e_v_i_c_e] [--dd _r_|_w] [--ww _0_|_8_|_1_6_|_1_6_L_E_|_1_6_B_E] [--oo _o_f_f_s_e_t]
         [--cc _c_o_u_n_t] [--mm _t_r_|_s_s_|_r_s_|_n_o] [--bb] [--vv]
     ii22cc --hh
     ii22cc --ii [--vv] [_c_m_d _._._.] [_-]
     ii22cc --rr [--ff _d_e_v_i_c_e] [--vv]
     ii22cc --ss [--ff _d_e_v_i_c_e] [--nn _s_k_i_p___a_d_d_r] [--vv]

DDEESSCCRRIIPPTTIIOONN
     The ii22cc utility can be used to perform raw data transfers (read or write)
     to devices on an I2C bus.  It can also scan the bus for available devices
     and reset the I2C controller.

     The options are as follows:

     --aa _a_d_d_r_e_s_s    7-bit address on the I2C device to operate on (hex).

     --bb            binary mode - when performing a read operation, the data
                   read from the device is output in binary format on stdout.

     --cc _c_o_u_n_t      number of bytes to transfer (decimal).

     --dd _r_|_w        transfer direction: r - read, w - write.  Data to be writ‐
                   ten is read from stdin as binary bytes.

     --ff _d_e_v_i_c_e     I2C bus to use (default is /dev/iic0).

     --ii            Interpreted mode

     --hh            Help

     --mm _t_r_|_s_s_|_r_s_|_n_o
                   addressing mode, i.e., I2C bus operations performed after
                   the offset for the transfer has been written to the device
                   and before the actual read/write operation.
                         _t_r      complete-transfer
                         _s_s      stop then start
                         _r_s      repeated start
                         _n_o      none
                   Some I2C bus hardware does not provide control over the in‐
                   dividual start, repeat-start, and stop operations.  Such
                   hardware can only perform a complete transfer of the offset
                   and the data as a single operation.  The _t_r mode creates
                   control structures describing the transfer and submits them
                   to the driver as a single complete transaction.  This mode
                   works on all types of I2C hardware.

     --nn _s_k_i_p___a_d_d_r  address(es) to be skipped during bus scan.  One or more ad‐
                   dresses ([0x]xx) or ranges of addresses ([0x]xx-[0x]xx or
                   [0x]xx..[0x]xx) separated by commas or colons.

     --oo _o_f_f_s_e_t     offset within the device for data transfer (hex).  The de‐
                   fault is zero.  Use “-w 0” to disable writing of the offset
                   to the slave.

     --rr            reset the controller.

     --ss            scan the bus for devices.

     --vv            be verbose.

     --ww _0_|_8_|_1_6_|_1_6_L_E_|_1_6_B_E
                   device offset width (in bits).  This is used to determine
                   how to pass _o_f_f_s_e_t specified with --oo to the slave.  Zero
                   means that the offset is ignored and not passed to the
                   slave at all.  The endianness defaults to little-endian.

IINNTTEERRPPRREETTEEDD MMOODDEE
     When started with --ii any remaining arguments are interpreted as commands,
     and if the last argument is '-', or there are no arguments, commands will
     (also) be read from stdin.

     Available commands:
     'r' bus address [0|8|16|16LE|16BE] offset count
             Read command, count bytes are read and hexdumped to stdout.
     'w' bus address [0|8|16|16LE|16BE] offset hexstring
             Write command, hexstring (white-space is allowed) is written to
             device.
     'p' anything
             Print command, the entire line is printed to stdout.  (This can
             be used for synchronization.)

     All numeric fields accept canonical decimal/octal/hex notation.

     Without the --vv option, all errors are fatal with non-zero exit status.

     With the --vv option, no errors are fatal, and all commands will return ei‐
     ther "OK\n" or "ERROR\n" on stdout.  In case of error, detailed diagnos‐
     tics will precede that on stderr.

     Blank lines and lines starting with '#' are ignored.

EEXXAAMMPPLLEESS
     ••   Scan the default bus (/dev/iic0) for devices:

         i2c -s

     ••   Scan the default bus (/dev/iic0) for devices and skip addresses 0x45
         to 0x47 (inclusive) and 0x56.

         i2c -s -n 0x56,45-47

     ••   Read 8 bytes of data from device at address 0x56 (e.g., an EEPROM):

         i2c -a 0x56 -d r -c 8

     ••   Write 16 bytes of data from file data.bin to device 0x56 at offset
         0x10:

         i2c -a 0x56 -d w -c 16 -o 0x10 -b < data.bin

     ••   Copy 4 bytes between two EEPROMs (0x56 on /dev/iic1 to 0x57 on
         /dev/iic0):

         i2c -a 0x56 -f /dev/iic1 -d r -c 0x4 -b | i2c -a 0x57 -f /dev/iic0 -d
         w -c 4 -b

     ••   Reset the controller:

         i2c -f /dev/iic1 -r

     ••   Read 8 bytes at address 24 in an EEPROM:

         i2c -i 'r 0 0x50 16BE 24 8'

     ••   Read 2x8 bytes at address 24 and 48 in an EEPROM:

         echo 'r 0 0x50 16BE 48 8' | i2c -i 'r 0 0x50 16BE 24 8' -

WWAARRNNIINNGG
     Many systems store critical low-level information in I2C memories, and
     may contain other I2C devices, such as temperature or voltage sensors.
     Reading these can disturb the firmware's operation and writing to them
     can "brick" the hardware.

SSEEEE AALLSSOO
     iic(4), iicbus(4), smbus(4)

HHIISSTTOORRYY
     The ii22cc utility appeared in FreeBSD 8.0.

AAUUTTHHOORRSS
     The ii22cc utility and this manual page were written by Bartlomiej Sieka
     <_t_u_r_@_s_e_m_i_h_a_l_f_._c_o_m> and Michal Hajduk <_m_i_h_@_s_e_m_i_h_a_l_f_._c_o_m>.

     Poul-Henning Kamp <_p_h_k_@_F_r_e_e_B_S_D_._o_r_g> added interpreted mode.

BSD                              May 22, 2019                              BSD
