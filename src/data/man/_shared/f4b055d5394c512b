TCSETSID(3)              BSD Library Functions Manual              TCSETSID(3)

NNAAMMEE
     ttccsseettssiidd — set session ID associated with a controlling terminal

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<tteerrmmiiooss..hh>>

     _i_n_t
     ttccsseettssiidd(_i_n_t _f_d, _p_i_d___t _p_i_d);

DDEESSCCRRIIPPTTIIOONN
     The ttccsseettssiidd() function sets associates a session identified by _p_i_d with
     a controlling terminal specified by _f_d.

     This implementation only allows the controlling terminal to be changed by
     the session leader itself.  This implies that _p_i_d always has to be equal
     to the process ID.

     It is unsupported to associate with a terminal that already has an asso‐
     ciated session.  Conversely, it is also unsupported to associate to a
     terminal when the session is already associated with a different termi‐
     nal.

EERRRROORRSS
     If an error occurs, ttccsseettssiidd() returns -1 and the global variable _e_r_r_n_o
     is set to indicate the error, as follows:

     [EBADF]            The _f_d argument is not a valid file descriptor.

     [ENOTTY]           The file descriptor represented by _f_d is not a termi‐
                        nal.

     [EINVAL]           The _p_i_d argument is not equal to the session ID of the
                        calling process.

     [EPERM]            The calling process is not a session leader.

     [EPERM]            The session already has an associated terminal or the
                        terminal already has an associated session.

SSEEEE AALLSSOO
     getsid(2), setsid(2), tcgetpgrp(3), tcgetsid(3)

HHIISSTTOORRYY
     A ttccsseettssiidd() function first appeared in QNX.  It does not comply to any
     standard.

BSD                            November 28, 2022                           BSD
