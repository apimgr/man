The command attaches an instance of the per‐process file descrip‐
tor  namespace  to  the global filesystem namespace.  The conven‐
tional mount point is and the filesystem should be union  mounted
in order to augment, rather than replace, the existing entries in
The  directory  specified by is converted to an absolute path be‐
fore use.  This command is normally executed  by  at  boot  time.
The  options  are  as  follows: Options are specified with a flag
followed by a comma separated string of  options.   See  the  man
page  for  possible  options and their meanings.  The contents of
the mount point are and is a directory whose contents appear as a
list of numbered files which correspond to the open files of  the
process  reading  the directory.  The files through refer to file
descriptors which can be accessed through the  file  system.   If
the file descriptor is open and the mode the file is being opened
with  is  a  subset  of  the mode of the existing descriptor, the
call: fd = open("/dev/fd/0", mode); and the call: fd  =  fcntl(0,
F_DUPFD, 0); are equivalent.  The files and appear as symlinks to
the relevant entry in the sub‐directory.  Opening them is equiva‐
lent  to  the following calls: fd = fcntl(STDIN_FILENO,  F_DUPFD,
0);  fd  =   fcntl(STDOUT_FILENO,   F_DUPFD,   0);   fd   =   fc‐
ntl(STDERR_FILENO,  F_DUPFD, 0); Flags to the call other than and
are ignored.  The entry is an indirect reference to  the  current
process’s  controlling  terminal.   It  appears  as  a named pipe
(FIFO) but behaves in exactly the same way as the  real  control‐
ling  terminal  device.   The  utility  first  appeared  in  This
filesystem may not be NFS‐exported.
