RDISTD(1)                 BSD General Commands Manual                RDISTD(1)

NNAAMMEE
     rrddiissttdd — remote file distribution server program

SSYYNNOOPPSSIISS
     rrddiissttdd [--DDVV] --SS

DDEESSCCRRIIPPTTIIOONN
     rrddiissttdd is the server program for the rdist(1) command.  It is normally
     run by _r_d_i_s_t via ssh(1).

     The options are as follows:

     --DD      Enable debugging messages.  Messages are logged via syslog(3).

     --SS      This argument must be specified for normal usage.  It exists to
             prevent the accidental execution of rrddiissttdd since it usually re‐
             sides somewhere in a normal user's PATH.

     --VV      Print version information and exit.

SSEEEE AALLSSOO
     rdist(1), ssh(1)

BSD                             April 19, 2014                             BSD
