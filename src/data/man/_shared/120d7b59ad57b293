is  an assembler for the TMS34010 and TMS34020 graphics processor
chips.  The supported options are: Create  a  pre‐initialized  C‐
structure with the hex code of the assembler output.  Create com‐
mented  (with the input file) hex code of the assembler output in
Create the output in If is used, C code will be written to other‐
wise uncommented hex code of the assembler output will  be  writ‐
ten.   If  no  option is given, output will be written to stdout.
If no is given, input will be read from stdin.  appeared  in  was
written by The mode was added by This man page was written by
