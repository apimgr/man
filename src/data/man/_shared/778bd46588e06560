is a server which returns quotas for a user of a local filesystem
which  is NFS‐mounted onto a remote machine.  uses the results to
display user quotas for remote filesystems.  is normally  invoked
by uses an RPC protocol defined in
