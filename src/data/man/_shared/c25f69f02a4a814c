The  driver  provides  support  for the ADW (AdvanSys) ABP‐940UW,
ASB‐3940UW, ASB‐3940U2W host adapters.  The following kernel con‐
figuration options are available: Disables tagged  command  queu‐
ing,  wide  data transfers and synchronous data transfers for all
devices controlled by the driver.   By  default,  tagged  command
queuing,  wide  data transfers and synchronous data transfers are
used if the devices support them.  The following options use a to
specify which SCSI peripherals the option applies to.  The  is  a
16  bit  bitfield value. Each bit corresponds to a peripheral ID.
The LSB (bit 0) corresponds to the peripheral with ID 0.  The MSB
(bit 15) corresponds to the peripheral with ID 15.  The following
features cannot be disabled for the host adapter,  which  by  de‐
fault has ID 7.  Disable data transfer for the peripherals speci‐
fied  by the mask value.  Disable data transfer for the peripher‐
als specified by the mask value.   Disable  for  the  peripherals
specified by the mask value.  The device driver appeared in
