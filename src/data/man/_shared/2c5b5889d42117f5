The  parameter  is  a  socket.   The  communication domain of the
socket determines the availability and behavior  of  For  connec‐
tion‐oriented  socket,  is analogous to issued on the socket. For
connectionless socket, it disassociates any existing  association
to  the  peer  socket.   The  parameter specifies the association
identifier.  It should be set to The parameter should be  set  to
is  currently  supported  only on AF_INET and AF_INET6 sockets of
type SOCK_DGRAM and SOCK_STREAM.  The function returns the  value
0  if  successful;  otherwise the value of ‐1 is returned and the
global integer variable is set to indicate the error.  The system
call succeeds unless: Operation already in progress.   is  not  a
valid  descriptor.   The or argument is invalid or the underlying
protocol is no longer attached to The is  not  connected.   is  a
file, not a socket.  The function call appeared in Darwin 15.0.0
