TRADCPP(1)                BSD General Commands Manual               TRADCPP(1)

NNAAMMEE
     ttrraaddccpppp — traditional (K&R-style) C macro preprocessor

SSYYNNOOPPSSIISS
     ttrraaddccpppp [--ooppttiioonnss] [_i_n_p_u_t_-_f_i_l_e [_o_u_t_p_u_t_-_f_i_l_e]]

DDEESSCCRRIIPPTTIIOONN
     The ttrraaddccpppp command provides a traditional K&R-style C macro preproces‐
     sor.  It is intended to be suitable for historical Unix uses of the pre‐
     processor, such as iimmaakkee, particularly those that depend on preservation
     of whitespace.

     The chief ways in which traditional cpp differs from Standard C are:

           ••   Macro arguments are expanded within quoted strings.  There is
               no stringize operator.

           ••   There is no token pasting operator; tokens can be concatenated
               by placing comments between them.  This process is also not
               limited to valid C language tokens.

           ••   Whitespace is preserved, and in particular tabs are not ex‐
               panded into spaces.  Furthermore, additional whitespace is not
               injected.

OOPPTTIIOONNSS
     ttrraaddccpppp has many options, many of which are defined for compatibility
     with gcc(1) or other compilers.  Many of the options are not yet imple‐
     mented.

   CCoommmmoonn OOppttiioonnss
     --CC            Retain comments in output.

     --DDmmaaccrroo[[==eexxppaannssiioonn]]
                   Provide a definition for the named macro.  If no expansion
                   is provided, the value “1” is used.  Note that like many
                   Unix compilers, ttrraaddccpppp does not accept a space between the
                   “D” and the macro name.

     --IIppaatthh        Add the specified path to the main list of include directo‐
                   ries.  Note that like many Unix compilers, ttrraaddccpppp does not
                   accept a space between the “I” and the directory name.

     --nnoossttddiinncc     Do not search the standard system include directories.

     --PP            Suppress line number information in the output.  Currently
                   line number information is not generated at all and this
                   option has no effect.

     --UUmmaaccrroo       Remove any existing definition for the named macro.  Note
                   that like many Unix compilers, ttrraaddccpppp does not accept a
                   space between the “U” and the macro name.

     --uunnddeeff        Remove all predefined macros.

   WWaarrnniinngg OOppttiioonnss
     Warning options can be disabled or enabled by inserting, or not, the
     string “no-” between the “W” and the warning name.  Herein the “--WWnnoo--”
     form is shown for options that are enabled by default.

     --WWaallll         Turn on all warnings.  The option --WWnnoo--aallll disables only
                   the warnings that are disabled by default.

     --ww            Turn off all warnings.

     --WWeerrrroorr       Make warnings into fatal errors.

     --WWccoommmmeenntt     Warn about nested comments.

     --WWnnoo--eennddiiff--llaabbeellss
                   Don't warn about symbols attached to #endif directives.
                   (The warning is currently not implemented.)

     --WWuunnddeeff       Warn about undefined symbols appearing in #if and #elif ex‐
                   pressions.

     --WWuunnuusseedd--mmaaccrrooss
                   Warn about macros that are defined and never used.  Not im‐
                   plemented.

   DDeeppeenndd OOppttiioonnss
     --MM            Generate dependency information for make(1) on the standard
                   output, instead of preprocessing.  Not implemented.

     --MMDD           Like --MM but skip system headers.  Not implemented.

     --MMMM           Like --MM but write the dependency information to a file
                   named after the input file with extension _._d and preprocess
                   normally to standard output.  Not implemented.

     --MMMMDD          Like --MMMM but skip system headers.  Not implemented.

     --MMFF _f_i_l_e      Send dependency output to the named file instead of the de‐
                   fault location.  Not implemented.

     --MMGG           When generating dependency information, assume that missing
                   files are generated instead of failing.  Not implemented.

     --MMPP           Issue dummy rules for all include files.  This prevents
                   make(1) from choking if an include file is removed.  Not
                   implemented.

     --MMQQ _t_a_r_g_e_t    Same as --MMTT except that any make(1) metacharacters appear‐
                   ing in the target are escaped.

     --MMTT _t_a_r_g_e_t    Set the name of the make(1) target appearing in the gener‐
                   ated dependency information.  The default is the name of
                   the input file with its suffix replaced with the suffix for
                   object files, normally _._o.

   MMoorree IInncclluuddee PPaatthh OOppttiioonnss
     --iiddiirraafftteerr _p_a_t_h
                   Add the specified path to the “afterwards” include path.
                   This path is searched after all directories specified with
                   --II and the standard system directories.  Directories on
                   this path are treated as containing system include files.

     --iimmaaccrrooss _f_i_l_e
                   Read in _f_i_l_e prior to reading the main input file, and pre‐
                   process it, but throw away the output and retain only the
                   macro definitions.

     --iinncclluuddee _f_i_l_e
                   Read in and preprocess _f_i_l_e prior to reading the main input
                   file.

     --iipprreeffiixx _p_r_e_f_i_x
                   Set the path prefix used with the --iiwwiitthhpprreeffiixx option.

     --iiqquuoottee _p_a_t_h  Add _p_a_t_h to the list of directories searched for include
                   directives written with quotes.  This list is not searched
                   for include directives written with angle brackets.

     --iirreemmaapp _s_t_r_i_n_g_:_r_e_p_l_a_c_e_m_e_n_t
                   Substitute _r_e_p_l_a_c_e_m_e_n_t for _s_t_r_i_n_g in the __FILE__ built-in
                   macro.  Not supported.

     --iissyyssrroooott _p_a_t_h
                   Use _p_a_t_h as the “system root”, that is, the directory under
                   which the standard system paths are found.

     --iissyysstteemm _p_a_t_h
                   Add _p_a_t_h to the list of system include directories.  This
                   list is searched after the list given with _I.  Files found
                   on this path are treated as system headers.

     --iiwwiitthhpprreeffiixx _d_i_r
                   Splice _d_i_r onto the prefix given with --iipprreeffiixx and add this
                   directory as if it were specified with --iiddiirraafftteerr.

     --iiwwiitthhpprreeffiixxbbeeffoorree _d_i_r
                   Like --iiwwiitthhpprreeffiixx but adds the result as if it were speci‐
                   fied with --II.

   DDiiaaggnnoossttiicc OOppttiioonnss
     --ddeebbuugglloogg _f_i_l_e
                   Write a trace of actions and operations to _f_i_l_e as the in‐
                   put is processed.  Meant for debugging problems in complex
                   substitution schemes fed to ttrraaddccpppp, such as those used by
                   iimmaakkee.

     --ddDD           Dump all macro definitions, except for the predefined
                   macros, after the normal preprocessing output.  Not imple‐
                   mented.

     --ddII           Dump all include directives along with the normal prepro‐
                   cessing output.  Not implemented.

     --ddMM           Dump all macro definitions instead of the normal prepro‐
                   cessing output.  Not implemented.

     --ddNN           Like --ddDD but emits only macro names and not the expansions.
                   Not implemented.

     --HH            Output a trace of the include tree as it gets processed.
                   Not implemented.

   OOtthheerr OOppttiioonnss
     --CCCC           Retain comments in output.  Same as --CC, accepted for com‐
                   patibility with gcc(1).

     --ffddoollllaarrss--iinn--iiddeennttiiffiieerrss, --ffnnoo--ddoollllaarrss--iinn--iiddeennttiiffiieerrss
                   Enable (or disable, respectively) the use of the dollar
                   sign in identifiers.  Not implemented.

     --ffttaabbssttoopp==nnuumm
                   Set the tab width to the specified value, for reporting
                   column positions in diagnostics.  The default is 8.  Not
                   implemented.

     --ssttdd==ssttaannddaarrdd
                   Ask ttrraaddccpppp to conform to the named standard.  The default,
                   and the only supported value, is “krc”.

     --ttrraaddiittiioonnaall  This option is accepted for compatibility with gcc(1) and
                   ignored.

     --xx _l_a_n_g       Adjust the preprocessor for the given language.  The only
                   values accepted for _l_a_n_g are “assembler-with-cpp” and “c”,
                   neither of which have any effect on the behavior of
                   ttrraaddccpppp.

FFIILLEESS
     The default list of directories searched for include files is:
           _/_u_s_r_/_l_o_c_a_l_/_i_n_c_l_u_d_e
           _/_u_s_r_/_i_n_c_l_u_d_e

SSEEEE AALLSSOO
     cc(1), cpp(1), make(1)

SSTTAANNDDAARRDDSS
     None.  The whole point of a traditional cpp is that it reflects practices
     in pre-standardization implementations of C.  Some information is avail‐
     able from the first edition of Kernighan and Ritchie.  Much of the rest
     of the behavior is based on lore, pragmatism, material encountered in the
     wild, and comparison to other implementations.

HHIISSTTOORRYY
     The original version of ttrraaddccpppp was written one evening in late 2010.
     This version had some problems and was put aside.  The first working ver‐
     sion was released in June 2013.

BBUUGGSS
     Probably plenty.

BSD                            September 2, 2018                           BSD
