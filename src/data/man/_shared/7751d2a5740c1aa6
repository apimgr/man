PCF8591(4)               BSD Kernel Interfaces Manual               PCF8591(4)

NNAAMMEE
     ppccff88559911 — driver for the PCF8591 8-bit A/D and D/A converter

SSYYNNOOPPSSIISS
     To compile this driver into the kernel, place the following lines in your
     kernel configuration file:

           ddeevviiccee ppccff88559911
           ddeevviiccee iiiiccbbuuss

     Alternatively, to load the driver as a module at boot time, place the
     following line in loader.conf(5):

           pcf8591_load="YES"

DDEESSCCRRIIPPTTIIOONN
     The ppccff88559911 driver supports reading four inputs and setting one output
     over I2C.  The hardware supports configuring the input lines as:

     ••   four single-ended inputs

     ••   three differential inputs (one input line is shared between all three
         inputs)

     ••   two single-ended inputs and one differential input

     ••   two differential inputs.

     The ppccff88559911 driver reports data via sysctl(8) entries in the device's
     node in the sysctl(8) tree:

     _i_n_p_u_t_s_._%_d  The input level of the corresponding input in steps between 0
                and 255.  Absolute voltage depends on an actual reference
                voltage.

     On an FDT(4) based system the following properties must be set:

     _c_o_m_p_a_t_i_b_l_e  Must be set to "nxp,pcf8591".

     _r_e_g         The I2C address of ppccff88559911.  It should be in the range from
                 0x40 to 0x4f (7-bit).

     The DTS part for a ppccff88559911 device usually looks like:

     / {

             ...
             pcf8591adc {
                     compatible = "nxp,pcf8591";
                     reg = <0x48>;
             };
     };

SSEEEE AALLSSOO
     fdt(4), iicbus(4), sysctl(8)

HHIISSTTOORRYY
     The ppccff88559911 driver and this manual page was written by Andriy Gapon
     <_a_v_g_@_F_r_e_e_B_S_D_._o_r_g>.

BBUUGGSS
     The ppccff88559911 driver does not support changing the input configuration.
     All input lines are configured as single-ended inputs.

     The ppccff88559911 driver does not support setting the output.  It is always
     disabled (tri-state).

BSD                            November 6, 2021                            BSD
