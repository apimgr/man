CDCEEM(4)                BSD Kernel Interfaces Manual                CDCEEM(4)

NNAAMMEE
     ccddcceeeemm — USB Communication Device Class Ethernet Emulation Model (CDC
     EEM) driver

SSYYNNOOPPSSIISS
     To compile this driver into the kernel, place the following lines in your
     kernel configuration file:

           ddeevviiccee uuhhccii
           ddeevviiccee oohhccii
           ddeevviiccee uussbb
           ddeevviiccee mmiiiibbuuss
           ddeevviiccee uueetthheerr
           ddeevviiccee ccddcceeeemm

     Alternatively, to load the driver as a module at boot time, place the
     following line in loader.conf(5):

           if_cdceem_load="YES"

DDEESSCCRRIIPPTTIIOONN
     The ccddcceeeemm driver provides support for USB devices based on the USB Com‐
     munication Device Class Ethernet Emulation Model (CDC EEM) specification.

     The driver works on both host, and device-side; see usb_template(4) for
     details.

     The USB device appears as a regular network interface on both sides,
     transporting Ethernet frames.

     For more information on configuring this device, see ifconfig(8).

     The ccddcceeeemm driver does not support different media types or options.

SSYYSSCCTTLL VVAARRIIAABBLLEESS
     The following variables are available as both sysctl(8) variables and
     loader(8) tunables:

     _h_w_._u_s_b_._c_d_c_e_e_m_._d_e_b_u_g
             Verbosity level for log messages from the ccddcceeeemm driver.  Set to
             0 to disable logging or 1 to warn about potential problems.
             Larger values enable debugging output.  Defaults to 1.

     _h_w_._u_s_b_._c_d_c_e_e_m_._s_e_n_d___e_c_h_o_e_s
             If set to 1, the driver will send an Echo EEM packet when the in‐
             terface is brought up.  While responding to Echo is mandatory,
             some devices cannot handle it.  Only use for debugging.  Defaults
             to 0.

     _h_w_._u_s_b_._c_d_c_e_e_m_._s_e_n_d___f_a_k_e___c_r_c
             If set to 1, the driver will use 0xdeadbeef as the CRC value for
             outgoing Data EEM packets.  Only use for debugging.  Defaults to
             0.

SSEEEE AALLSSOO
     arp(4), cdce(4), intro(4), ipheth(4), netintro(4), urndis(4), usb(4),
     usb_template(4), ifconfig(8)

     _U_n_i_v_e_r_s_a_l _S_e_r_i_a_l _B_u_s _C_o_m_m_u_n_i_c_a_t_i_o_n_s _C_l_a_s_s _S_u_b_c_l_a_s_s _S_p_e_c_i_f_i_c_a_t_i_o_n _f_o_r
     _E_t_h_e_r_n_e_t _E_m_u_l_a_t_i_o_n _M_o_d_e_l _D_e_v_i_c_e_s,
     https://usb.org/sites/default/files/CDC_EEM10.pdf.

HHIISSTTOORRYY
     The ccddcceeeemm device driver first appeared in FreeBSD 12.1.

AAUUTTHHOORRSS
     The ccddcceeeemm driver was written by Edward Tomasz Napierala
     <_t_r_a_s_z_@_F_r_e_e_B_S_D_._o_r_g> under sponsorship from Hewlett Packard Enterprise.

BSD                            November 7, 2019                            BSD
