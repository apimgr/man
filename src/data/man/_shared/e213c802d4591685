The  interface  allows  serial lines to be used as network inter‐
faces using the (PPP).  The interface can use  various  types  of
compression  and has many features over the SLIP protocol used by
the interface.  Supported options are: Enable  support  for  BSD‐
compress  (‘bsdcomp’) compression in ppp.  Enable support for de‐
flate compression in ppp.  This option turns on  based  filtering
for  ppp  connections.   This option is used by which needs to be
compiled with defined (the current default).  The  interface  was
handed  a  message  with addresses formatted in an unsuitable ad‐
dress family; the packet was dropped.   The  device  appeared  in
Currently, only the and protocols are supported.
