_r_t___s_i_g_q_u_e_u_e_i_n_f_o(2)            System Calls Manual           _r_t___s_i_g_q_u_e_u_e_i_n_f_o(2)

NNAAMMEE
       rt_sigqueueinfo, rt_tgsigqueueinfo - queue a signal and data

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<lliinnuuxx//ssiiggnnaall..hh>>     /* Definition of SSII__** constants */
       ##iinncclluuddee <<ssyyss//ssyyssccaallll..hh>>      /* Definition of SSYYSS__** constants */
       ##iinncclluuddee <<uunniissttdd..hh>>

       iinntt ssyyssccaallll((SSYYSS__rrtt__ssiiggqquueeuueeiinnffoo,, ppiidd__tt _t_g_i_d,,
                   iinntt _s_i_g,, ssiiggiinnffoo__tt **_i_n_f_o));;
       iinntt ssyyssccaallll((SSYYSS__rrtt__ttggssiiggqquueeuueeiinnffoo,, ppiidd__tt _t_g_i_d,, ppiidd__tt _t_i_d,,
                   iinntt _s_i_g,, ssiiggiinnffoo__tt **_i_n_f_o));;

       _N_o_t_e: There are no glibc wrappers for these system calls; see NOTES.

DDEESSCCRRIIPPTTIIOONN
       The rrtt__ssiiggqquueeuueeiinnffoo() and rrtt__ttggssiiggqquueeuueeiinnffoo() system calls are the low‐
       level  interfaces  used  to  send  a  signal  plus data to a process or
       thread.  The receiver of the signal can obtain the accompanying data by
       establishing a signal handler with the ssiiggaaccttiioonn(2) SSAA__SSIIGGIINNFFOO flag.

       These system calls are not intended for direct  application  use;  they
       are   provided   to   allow   the  implementation  of  ssiiggqquueeuuee(3)  and
       pptthhrreeaadd__ssiiggqquueeuuee(3).

       The rrtt__ssiiggqquueeuueeiinnffoo() system call sends the signal _s_i_g  to  the  thread
       group  with  the  ID _t_g_i_d.  (The term "thread group" is synonymous with
       "process", and _t_g_i_d corresponds to the traditional  UNIX  process  ID.)
       The signal will be delivered to an arbitrary member of the thread group
       (i.e., one of the threads that is not currently blocking the signal).

       The _i_n_f_o argument specifies the data to accompany the signal.  This ar‐
       gument  is  a  pointer  to  a structure of type _s_i_g_i_n_f_o___t, described in
       ssiiggaaccttiioonn(2) (and defined  by  including  _<_s_i_g_a_c_t_i_o_n_._h_>).   The  caller
       should set the following fields in this structure:

       _s_i___c_o_d_e
              This  should be one of the SSII__** codes in the Linux kernel source
              file _i_n_c_l_u_d_e_/_a_s_m_-_g_e_n_e_r_i_c_/_s_i_g_i_n_f_o_._h.  If the signal is being sent
              to any process other than the caller itself, the  following  re‐
              strictions apply:

              •  The  code can’t be a value greater than or equal to zero.  In
                 particular, it can’t be SSII__UUSSEERR, which is used by the  kernel
                 to  indicate  a  signal  sent  by  kkiillll(2), and nor can it be
                 SSII__KKEERRNNEELL, which is used to indicate a  signal  generated  by
                 the kernel.

              •  The  code  can’t  (since  Linux 2.6.39) be SSII__TTKKIILLLL, which is
                 used by the kernel to indicate a signal sent using ttggkkiillll(2).

       _s_i___p_i_d This should be set to a process ID, typically the process ID  of
              the sender.

       _s_i___u_i_d This  should  be set to a user ID, typically the real user ID of
              the sender.

       _s_i___v_a_l_u_e
              This field contains the user data to accompany the signal.   For
              more information, see the description of the last (_u_n_i_o_n _s_i_g_v_a_l)
              argument of ssiiggqquueeuuee(3).

       Internally,  the  kernel sets the _s_i___s_i_g_n_o field to the value specified
       in _s_i_g, so that the receiver of the signal can also obtain  the  signal
       number via that field.

       The  rrtt__ttggssiiggqquueeuueeiinnffoo()  system  call  is  like rrtt__ssiiggqquueeuueeiinnffoo(), but
       sends the signal and data to the single thread specified by the  combi‐
       nation  of  _t_g_i_d,  a  thread group ID, and _t_i_d, a thread in that thread
       group.

RREETTUURRNN VVAALLUUEE
       On success, these system calls return 0.  On error, they return -1  and
       _e_r_r_n_o is set to indicate the error.

EERRRROORRSS
       EEAAGGAAIINN The  limit of signals that may be queued has been reached.  (See
              ssiiggnnaall(7) for further information.)

       EEIINNVVAALL _s_i_g, _t_g_i_d, or _t_i_d was invalid.

       EEPPEERRMM  The caller does not have permission to send the  signal  to  the
              target.  For the required permissions, see kkiillll(2).

       EEPPEERRMM  _t_g_i_d specifies a process other than the caller and _i_n_f_o_-_>_s_i___c_o_d_e
              is invalid.

       EESSRRCCHH  rrtt__ssiiggqquueeuueeiinnffoo(): No thread group matching _t_g_i_d was found.

       rrtt__ttggssiiggqquueeiinnffoo(): No thread matching _t_g_i_d and _t_i_d was found.

SSTTAANNDDAARRDDSS
       Linux.

HHIISSTTOORRYY
       rrtt__ssiiggqquueeuueeiinnffoo()
              Linux 2.2.

       rrtt__ttggssiiggqquueeuueeiinnffoo()
              Linux 2.6.31.

NNOOTTEESS
       Since  these  system  calls are not intended for application use, there
       are no glibc wrapper functions; use ssyyssccaallll(2)  in  the  unlikely  case
       that you want to call them directly.

       As with kkiillll(2), the null signal (0) can be used to check if the speci‐
       fied process or thread exists.

SSEEEE AALLSSOO
       kkiillll(2), ppiiddffdd__sseenndd__ssiiggnnaall(2), ssiiggaaccttiioonn(2), ssiiggpprrooccmmaasskk(2), ttggkkiillll(2),
       pptthhrreeaadd__ssiiggqquueeuuee(3), ssiiggqquueeuuee(3), ssiiggnnaall(7)

Linux man‐pages (unreleased)      2025‐10‐01                _r_t___s_i_g_q_u_e_u_e_i_n_f_o(2)
