The  Protocol  Family provides presentation layer support for the
AppleTalk Datagram Delivery Protocol (DDP), using the  SOCK_DGRAM
socket  type.  In addition, access to in‐kernel AppleTalk routing
tables and network interface configurations is provided.  The Ap‐
pleTalk Protocol Suite provides support for five kinds of  physi‐
cal  media: LocalTalk (230kbps wire‐or’d serial), Ethernet, FDDI,
Token Ring, and asynchronous serial connections (using either Ap‐
pleTalk Remote Access or ).  Currently, AppleTalk  implementation
supports  Ethernet.   AppleTalk  packets  are encapsulated on the
Ethernet using the EtherTalk Link Access Protocol (ELAP).   Local
network address resolution is handled using the AppleTalk Address
Resolution  Protocol  (AARP).   Neither of these protocols is ex‐
posed to user‐mode applications.  AppleTalk addresses  are  three
byte  quantities, stored in network byte order.  The include file
defines the AppleTalk address format.  Sockets in  the  AppleTalk
protocol family use the following address structure: struct sock‐
addr_at      {              uint8_t sat_len;              sa_fam‐
ily_t     sat_family;          uint8_t sat_port;           struct
at_addr  sat_addr;          union  {                  struct  ne‐
trange   r_netrange;                   char            r_zero[8];
        }  sat_range; }; The port of a socket may be set with The
node for must always be The net must be corresponds  to  the  ma‐
chine’s address (the first configured).  The port of a socket and
the primary address are returned with The AppleTalk protocol fam‐
ily  comprises  the  datagram  delivery  protocol, AppleTalk Data
Stream Protocol AppleTalk Echo Protocol AppleTalk Filing Protocol
AppleTalk Session Protocol AppleTalk  Transaction  Protocol  Name
Binding  Protocol  Printer  Access  Protocol and Zone Information
Protocol is implemented in the kernel as sockets in  the  address
family.   implements  all other protocols using the package.  im‐
plements all functions except for and an  client.   and  services
are  provided by the daemon.  and services are provided by a user
library.  and services are provided by user programs and daemons.
