FACTOR(6)                      BSD Games Manual                      FACTOR(6)

NNAAMMEE
     ffaaccttoorr, pprriimmeess — factor a number, generate primes

SSYYNNOOPPSSIISS
     ffaaccttoorr [--hh] [_n_u_m_b_e_r _._._.]
     pprriimmeess [--hh] [_s_t_a_r_t [_s_t_o_p]]

DDEESSCCRRIIPPTTIIOONN
     The ffaaccttoorr utility will factor positive integers.  When a number is fac‐
     tored, it is printed, followed by a ‘:’, and the list of factors on a
     single line.  Factors are listed in ascending order, and are preceded by
     a space.  If a factor divides a value more than once, it will be printed
     more than once.

     When ffaaccttoorr is invoked with one or more arguments, each argument will be
     factored.

     When ffaaccttoorr is invoked with no arguments, ffaaccttoorr reads numbers, one per
     line, from standard input until end of file or 0 is entered or an error
     occurs.  Leading white-space and empty lines are ignored.

     Numbers may be preceded by a single ‘+’.  Numbers can be either decimal
     or hexadecimal strings where the longest leading substring is used.  Num‐
     bers are terminated by a non-digit character (such as a newline).  If the
     string contains only decimal digits, it is treated as a decimal represen‐
     tation for a number.  A hexadecimal string can contain an optional _0_x or
     _0_X prefix.  After a number is read, it is factored.

     The pprriimmeess utility prints primes in ascending order, one per line, start‐
     ing at or above _s_t_a_r_t and continuing until, but not including _s_t_o_p.  The
     _s_t_a_r_t value must be at least 0 and not greater than _s_t_o_p.  The _s_t_o_p value
     must not be greater than the maximum.  The default and maximum value of
     _s_t_o_p is 18446744073709551615.

     When the pprriimmeess utility is invoked with no arguments, _s_t_a_r_t is read from
     standard input and _s_t_o_p is taken to be the maximum.  The _s_t_a_r_t value may
     be preceded by a single ‘+’.  The _s_t_a_r_t value is terminated by a non-
     digit character (such as a newline).

DDIIAAGGNNOOSSTTIICCSS
     nneeggaattiivvee nnuummbbeerrss aarreenn''tt ppeerrmmiitttteedd
     iilllleeggaall nnuummeerriicc ffoorrmmaatt
     ssttaarrtt vvaalluuee mmuusstt bbee lleessss tthhaann ssttoopp vvaalluuee
     RReessuulltt ttoooo llaarrggee

BBUUGGSS
     ffaaccttoorr cannot handle the “10 most wanted” factor list, pprriimmeess will not
     get you a world record.

BSD                            January 12, 2020                            BSD
