_r_e_m_a_p___f_i_l_e___p_a_g_e_s(2)           System Calls Manual          _r_e_m_a_p___f_i_l_e___p_a_g_e_s(2)

NNAAMMEE
       remap_file_pages - create a nonlinear file mapping

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##ddeeffiinnee __GGNNUU__SSOOUURRCCEE         /* See feature_test_macros(7) */
       ##iinncclluuddee <<ssyyss//mmmmaann..hh>>

       [[[[ddeepprreeccaatteedd]]]] iinntt rreemmaapp__ffiillee__ppaaggeess((size_t size;
                                           vvooiidd _a_d_d_r[[_s_i_z_e]],, ssiizzee__tt _s_i_z_e,,
                                           iinntt _p_r_o_t,, ssiizzee__tt _p_g_o_f_f,, iinntt _f_l_a_g_s));;

DDEESSCCRRIIPPTTIIOONN
       NNoottee:  this  system  call  was marked as deprecated starting with Linux
       3.16.  In Linux 4.0, the implementation was replaced by  a  slower  in‐
       kernel  emulation.   Those  few  applications that use this system call
       should consider migrating to alternatives.  This change  was  made  be‐
       cause  the  kernel code for this system call was complex, and it is be‐
       lieved to be little used or perhaps even completely unused.   While  it
       had  some  use  cases in database applications on 32‐bit systems, those
       use cases don’t exist on 64‐bit systems.

       The rreemmaapp__ffiillee__ppaaggeess() system call is used to create a  nonlinear  map‐
       ping, that is, a mapping in which the pages of the file are mapped into
       a   nonsequential   order   in   memory.    The   advantage   of  using
       rreemmaapp__ffiillee__ppaaggeess() over using repeated calls to  mmmmaapp(2)  is  that  the
       former  approach  does  not require the kernel to create additional VMA
       (Virtual Memory Area) data structures.

       To create a nonlinear mapping we perform the following steps:

       1. Use mmmmaapp(2) to create a mapping (which is initially  linear).   This
          mapping must be created with the MMAAPP__SSHHAARREEDD flag.

       2. Use  one or more calls to rreemmaapp__ffiillee__ppaaggeess() to rearrange the corre‐
          spondence between the pages of the mapping  and  the  pages  of  the
          file.   It  is possible to map the same page of a file into multiple
          locations within the mapped region.

       The _p_g_o_f_f and _s_i_z_e arguments specify the region of the file that is  to
       be relocated within the mapping: _p_g_o_f_f is a file offset in units of the
       system page size; _s_i_z_e is the length of the region in bytes.

       The  _a_d_d_r  argument serves two purposes.  First, it identifies the map‐
       ping whose pages we want to rearrange.  Thus, _a_d_d_r must be  an  address
       that  falls  within  a  region  previously mapped by a call to mmmmaapp(2).
       Second, _a_d_d_r specifies the address at which the file  pages  identified
       by _p_g_o_f_f and _s_i_z_e will be placed.

       The values specified in _a_d_d_r and _s_i_z_e should be multiples of the system
       page size.  If they are not, then the kernel rounds _b_o_t_h values _d_o_w_n to
       the nearest multiple of the page size.

       The _p_r_o_t argument must be specified as 0.

       The  _f_l_a_g_s  argument has the same meaning as for mmmmaapp(2), but all flags
       other than MMAAPP__NNOONNBBLLOOCCKK are ignored.

RREETTUURRNN VVAALLUUEE
       On success, rreemmaapp__ffiillee__ppaaggeess() returns 0.  On error,  -1  is  returned,
       and _e_r_r_n_o is set to indicate the error.

EERRRROORRSS
       EEIINNVVAALL _a_d_d_r  does  not  refer  to  a  valid  mapping  created  with the
              MMAAPP__SSHHAARREEDD flag.

       EEIINNVVAALL _a_d_d_r, _s_i_z_e, _p_r_o_t, or _p_g_o_f_f is invalid.

SSTTAANNDDAARRDDSS
       Linux.

HHIISSTTOORRYY
       Linux 2.5.46, glibc 2.3.3.

NNOOTTEESS
       Since Linux 2.6.23, rreemmaapp__ffiillee__ppaaggeess() creates non‐linear mappings only
       on in‐memory filesystems such as  ttmmppffss(5),  hugetlbfs  or  ramfs.   On
       filesystems  with  a backing store, rreemmaapp__ffiillee__ppaaggeess() is not much more
       efficient than using mmmmaapp(2) to adjust which  parts  of  the  file  are
       mapped to which addresses.

SSEEEE AALLSSOO
       ggeettppaaggeessiizzee(2), mmmmaapp(2), mmmmaapp22(2), mmpprrootteecctt(2), mmrreemmaapp(2), mmssyynncc(2)

Linux man‐pages (unreleased)      2025‐09‐07               _r_e_m_a_p___f_i_l_e___p_a_g_e_s(2)
