ULED(4)                  BSD Kernel Interfaces Manual                  ULED(4)

NNAAMMEE
     uulleedd — USB LED driver

SSYYNNOOPPSSIISS
     To compile this driver into the kernel, place the following lines into
     your kernel configuration file:

           ddeevviiccee uulleedd
           ddeevviiccee uussbb

     Alternatively, to load the driver as a module at boot time, place the
     following line in loader.conf(5):

           uled_load="YES"

DDEESSCCRRIIPPTTIIOONN
     The uulleedd driver provides support for Dream Cheeky WebMail Notifier and
     ThingM blink(1) notification LED.

     Subsequently, the _/_d_e_v_/_u_l_e_d_0 device can be used by userland applications.

IIOOCCTTLLSS
     The following ioctl(2) commands can be performed on _/_d_e_v_/_u_l_e_d_0, which are
     defined in <_d_e_v_/_u_s_b_/_u_l_e_d___i_o_c_t_l_._h>:

     ULED_GET_COLOR
             The command returns LED colors with values for RGB.  This
             ioctl(2) takes the following structure:

             struct uled_color {
                     uint8_t red;
                     uint8_t green;
                     uint8_t blue;
             };

     ULED_SET_COLOR
             The command sets LED colors with values for RGB.  It uses the
             same structure as above.

FFIILLEESS
     /dev/uled0  blocking device node

SSEEEE AALLSSOO
     ohci(4), uhci(4), usb(4)

AAUUTTHHOORRSS
     The uulleedd driver was written by Kevin Lo <_k_e_v_l_o_@_F_r_e_e_B_S_D_._o_r_g>.

BSD                             March 31, 2017                             BSD
