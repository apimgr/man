The  function disassociates the shared memory object specified by
from that name.  The resources associated with the shared  memory
object  remain intact until the last file descriptor reference is
removed, e.g., by or at which point the resources  are  reclaimed
(if  no references exist at the time of the call to the resources
are reclaimed immediately).  The name can only be reused when  it
is  bound  to  a new shared memory object with a call to with the
flag.  Upon successful completion, a  value  of  0  is  returned.
Otherwise,  a  value of ‐1 is returned and is set to indicate the
error, and the named shared memory object will remain  unchanged.
The  succeeds  unless:  Permission is denied to be remove the ob‐
ject.  exceeded the name size limit.  This is  currently  charac‐
ters  (defined  in  but this may change in the future.  The named
object does not exist.  is specified in the POSIX Realtime Exten‐
sion (1003.1b‐1993/1003.1i‐1995).
