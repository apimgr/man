PTSNAME(3)               BSD Library Functions Manual               PTSNAME(3)

NNAAMMEE
     ggrraannttpptt, ppttssnnaammee, ppttssnnaammee__rr, uunnlloocckkpptt — pseudo-terminal access functions

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssttddlliibb..hh>>

     _i_n_t
     ggrraannttpptt(_i_n_t _f_i_l_d_e_s);

     _c_h_a_r _*
     ppttssnnaammee(_i_n_t _f_i_l_d_e_s);

     _i_n_t
     ppttssnnaammee__rr(_i_n_t _f_i_l_d_e_s, _c_h_a_r _*_b_u_f_f_e_r, _s_i_z_e___t _b_u_f_l_e_n);

     _i_n_t
     uunnlloocckkpptt(_i_n_t _f_i_l_d_e_s);

DDEESSCCRRIIPPTTIIOONN
     The ggrraannttpptt(), ppttssnnaammee(), and uunnlloocckkpptt() functions allow access to
     pseudo-terminal devices.  These three functions accept a file descriptor
     that references the master half of a pseudo-terminal pair.  This file de‐
     scriptor is created with posix_openpt(2).

     The ggrraannttpptt() function is used to establish ownership and permissions of
     the slave device counterpart to the master device specified with _f_i_l_d_e_s.
     The slave device's ownership is set to the real user ID of the calling
     process, and the permissions are set to user readable-writable and group
     writable.  The group owner of the slave device is also set to the group
     “tty”.

     The ppttssnnaammee() function returns the full pathname of the slave device
     counterpart to the master device specified with _f_i_l_d_e_s.  This value can
     be used to subsequently open the appropriate slave after posix_openpt(2)
     and ggrraannttpptt() have been called.

     The ppttssnnaammee__rr() function is the thread-safe version of ppttssnnaammee().  The
     caller must provide storage for the results of the full pathname of the
     slave device in the _b_u_f_f_e_r and _b_u_f_s_i_z_e arguments.

     The uunnlloocckkpptt() function clears the lock held on the pseudo-terminal pair
     for the master device specified with _f_i_l_d_e_s.

RREETTUURRNN VVAALLUUEESS
     The ggrraannttpptt() and uunnlloocckkpptt() functions return the value 0 if successful;
     otherwise the value -1 is returned and the global variable _e_r_r_n_o is set
     to indicate the error.

     The ppttssnnaammee() function returns a pointer to the name of the slave device
     on success; otherwise a NULL pointer is returned.

     The ppttssnnaammee__rr() function returns the value 0 if successful; otherwise a
     nonzero value is returned and the global variable _e_r_r_n_o is set to indi‐
     cate the error.  Note: ppttssnnaammee__rr() was previously documented as returning
     -1 on error.  In the future it will be changed to return an error number,
     for POSIX compatibility.  Therefore, callers should not check for -1.

EERRRROORRSS
     The ggrraannttpptt(), ppttssnnaammee(), ppttssnnaammee__rr() and uunnlloocckkpptt() functions may fail
     and set _e_r_r_n_o to:

     [EBADF]            _f_i_l_d_e_s is not a valid open file descriptor.

     [EINVAL]           _f_i_l_d_e_s is not a master pseudo-terminal device.

     In addition, the ppttssnnaammee__rr() function may set _e_r_r_n_o to:

     [ERANGE]           The buffer was too small.

     In addition, the ggrraannttpptt() function may set _e_r_r_n_o to:

     [EACCES]           The slave pseudo-terminal device could not be ac‐
                        cessed.

SSEEEE AALLSSOO
     posix_openpt(2), pts(4), tty(4)

SSTTAANNDDAARRDDSS
     The ppttssnnaammee() function conforms to IEEE Std 1003.1-2008 (“POSIX.1”).

     This implementation of ggrraannttpptt() and uunnlloocckkpptt() does not conform to IEEE
     Std 1003.1-2008 (“POSIX.1”), because it depends on posix_openpt(2) to
     create the pseudo-terminal device with proper permissions in place.  It
     only validates whether _f_i_l_d_e_s is a valid pseudo-terminal master device.
     Future revisions of the specification will likely allow this behaviour,
     as stated by the Austin Group.

HHIISSTTOORRYY
     The ggrraannttpptt(), ppttssnnaammee() and uunnlloocckkpptt() functions appeared in
     FreeBSD 5.0.

BSD                            October 14, 2023                            BSD
