_g_e_t_f_s_e_n_t(3)                Library Functions Manual                _g_e_t_f_s_e_n_t(3)

NNAAMMEE
       getfsent,  getfsspec,  getfsfile, setfsent, endfsent - handle fstab en‐
       tries

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ffssttaabb..hh>>

       iinntt sseettffsseenntt((vvooiidd));;
       ssttrruucctt ffssttaabb **ggeettffsseenntt((vvooiidd));;
       vvooiidd eennddffsseenntt((vvooiidd));;

       ssttrruucctt ffssttaabb **ggeettffssffiillee((ccoonnsstt cchhaarr **_m_o_u_n_t___p_o_i_n_t));;
       ssttrruucctt ffssttaabb **ggeettffssssppeecc((ccoonnsstt cchhaarr **_s_p_e_c_i_a_l___f_i_l_e));;

DDEESSCCRRIIPPTTIIOONN
       These functions read from the file _/_e_t_c_/_f_s_t_a_b.  The _s_t_r_u_c_t _f_s_t_a_b is de‐
       fined by:

           struct fstab {
               char       *fs_spec;       /* block device name */
               char       *fs_file;       /* mount point */
               char       *fs_vfstype;    /* filesystem type */
               char       *fs_mntops;     /* mount options */
               const char *fs_type;       /* rw/rq/ro/sw/xx option */
               int         fs_freq;       /* dump frequency, in days */
               int         fs_passno;     /* pass number on parallel dump */
           };

       Here the field _f_s___t_y_p_e contains (on a *BSD  system)  one  of  the  five
       strings  "rw",  "rq",  "ro",  "sw",  "xx"  (read‐write, read‐write with
       quota, read‐only, swap, ignore).

       The function sseettffsseenntt() opens the file when required and  positions  it
       at the first line.

       The  function  ggeettffsseenntt()  parses  the next line from the file.  (After
       opening it when required.)

       The function eennddffsseenntt() closes the file when required.

       The function ggeettffssssppeecc() searches the file from the start  and  returns
       the  first  entry  found  for  which the _f_s___s_p_e_c field matches the _s_p_e_‐
       _c_i_a_l___f_i_l_e argument.

       The function ggeettffssffiillee() searches the file from the start  and  returns
       the  first  entry  found  for  which  the  _f_s___f_i_l_e  field  matches  the
       _m_o_u_n_t___p_o_i_n_t argument.

RREETTUURRNN VVAALLUUEE
       Upon success, the functions ggeettffsseenntt(),  ggeettffssffiillee(),  and  ggeettffssssppeecc()
       return  a  pointer to a _s_t_r_u_c_t _f_s_t_a_b, while sseettffsseenntt() returns 1.  Upon
       failure or end‐of‐file, these functions  return  NULL  and  0,  respec‐
       tively.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lb lb lbx l l l.  Interface Attribute Value T{
       eennddffsseenntt(), sseettffsseenntt() T}   Thread safety  T{ MT‐Unsafe race:fsent T}
       T{ ggeettffsseenntt(), ggeettffssssppeecc(), ggeettffssffiillee() T}   Thread safety  T{ MT‐
       Unsafe race:fsent locale T}

VVEERRSSIIOONNSS
       Several operating systems have these functions, for example, *BSD,
       SunOS, Digital UNIX, AIX (which also has a ggeettffssttyyppee()).  HP‐UX has
       functions of the same names, that however use a _s_t_r_u_c_t _c_h_e_c_k_l_i_s_t
       instead of a _s_t_r_u_c_t _f_s_t_a_b, and calls these functions obsolete,
       superseded by ggeettmmnntteenntt(3).

SSTTAANNDDAARRDDSS
       None.

HHIISSTTOORRYY
       The ggeettffsseenntt() function appeared in 4.0BSD; the other four functions
       appeared in 4.3BSD.

NNOOTTEESS
       These functions are not thread‐safe.

       Since Linux allows mounting a block special device in several places,
       and since several devices can have the same mount point, where the last
       device with a given mount point is the interesting one, while
       ggeettffssffiillee() and ggeettffssssppeecc() only return the first occurrence, these two
       functions are not suitable for use under Linux.

SSEEEE AALLSSOO
       ggeettmmnntteenntt(3), ffssttaabb(5)

Linux man‐pages (unreleased)      2025‐09‐21                       _g_e_t_f_s_e_n_t(3)
