The  command changes its root directory to the supplied directory
and exec’s or, if not  supplied,  an  interactive  copy  of  your
shell.   If  the or options are given, the user, group, and group
list of the process are set to these values after the chroot  has
taken  place;  see  and  Note: or the shell are run as your real‐
user‐id.  The following environment variable is referenced by  If
set,  the  string  specified by is interpreted as the name of the
shell to exec.  If the variable is not set, is used.  The utility
first appeared in should never be installed setuid  root,  as  it
would then be possible to exploit the program to gain root privi‐
leges.
