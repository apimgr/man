The utility converts the codeset of (or from standard input if no
file  is  specified) from codeset to codeset and outputs the con‐
verted text on standard output.  The following options are avail‐
able: Prevent output of any invalid characters.  By default, out‐
puts an specified by the codeset when it encounters  a  character
which  is  valid in the codeset but does not have a corresponding
character in the codeset.  Specifies the source codeset  name  as
Lists available codeset names.  Note that not all combinations of
and  are  valid.   Silent.   By default, outputs the number of to
standard error if they exist.  This option prevents  this  behav‐
iour.   Specifies  the  destination  codeset  name as conforms to
first appeared in
