ARC4RANDOM(3)            BSD Library Functions Manual            ARC4RANDOM(3)

NNAAMMEE
     aarrcc44rraannddoomm, aarrcc44rraannddoomm__bbuuff, aarrcc44rraannddoomm__uunniiffoorrmm — random number generator

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssttddlliibb..hh>>

     _u_i_n_t_3_2___t
     aarrcc44rraannddoomm(_v_o_i_d);

     _v_o_i_d
     aarrcc44rraannddoomm__bbuuff(_v_o_i_d _*_b_u_f, _s_i_z_e___t _n_b_y_t_e_s);

     _u_i_n_t_3_2___t
     aarrcc44rraannddoomm__uunniiffoorrmm(_u_i_n_t_3_2___t _u_p_p_e_r___b_o_u_n_d);

DDEESSCCRRIIPPTTIIOONN
     This family of functions provides higher quality data than those de‐
     scribed in rand(3), random(3), and rand48(3).

     Use of these functions is encouraged for almost all random number con‐
     sumption because the other interfaces are deficient in either quality,
     portability, standardization, or availability.  These functions can be
     called in almost all coding environments, including pthread(3) and
     chroot(2).

     High quality 32-bit pseudo-random numbers are generated very quickly.  On
     each call, a cryptographic pseudo-random number generator is used to gen‐
     erate a new result.  One data pool is used for all consumers in a
     process, so that consumption under program flow can act as additional
     stirring.  The subsystem is re-seeded from the kernel random(4) subsystem
     using getentropy(3) on a regular basis, and also upon fork(2).

     The aarrcc44rraannddoomm() function returns a single 32-bit value.  The
     aarrcc44rraannddoomm() function returns pseudo-random numbers in the range of 0 to
     (2**32)−1, and therefore has twice the range of rand(3) and random(3).

     aarrcc44rraannddoomm__bbuuff() fills the region _b_u_f of length _n_b_y_t_e_s with random data.

     aarrcc44rraannddoomm__uunniiffoorrmm() will return a single 32-bit value, uniformly dis‐
     tributed but less than _u_p_p_e_r___b_o_u_n_d.  This is recommended over construc‐
     tions like “arc4random() % upper_bound” as it avoids "modulo bias" when
     the upper bound is not a power of two.  In the worst case, this function
     may consume multiple iterations to ensure uniformity; see the source code
     to understand the problem and solution.

RREETTUURRNN VVAALLUUEESS
     These functions are always successful, and no return value is reserved to
     indicate an error.

EEXXAAMMPPLLEESS
     The following produces a drop-in replacement for the traditional rraanndd()
     and rraannddoomm() functions using aarrcc44rraannddoomm():

           #define foo4random() (arc4random_uniform(RAND_MAX + 1))

SSEEEE AALLSSOO
     rand(3), rand48(3), random(3)

     Daniel J. Bernstein, _C_h_a_C_h_a_, _a _v_a_r_i_a_n_t _o_f _S_a_l_s_a_2_0,
     http://cr.yp.to/papers.html#chacha, 2008-01-28, Document ID:
     4027b5256e17b9796842e6d0f68b0b5e.

HHIISSTTOORRYY
     These functions first appeared in OpenBSD 2.1.

     The original version of this random number generator used the RC4 (also
     known as ARC4) algorithm.  In OpenBSD 5.5 it was replaced with the
     ChaCha20 cipher, and it may be replaced again in the future as crypto‐
     graphic techniques advance.  A good mnemonic is “A Replacement Call for
     Random”.

     The aarrcc44rraannddoomm() random number generator was first introduced in
     FreeBSD 2.2.6.  The ChaCha20 based implementation was introduced in
     FreeBSD 12.0, with obsolete stir and addrandom interfaces removed at the
     same time.

BSD                             April 13, 2020                             BSD
