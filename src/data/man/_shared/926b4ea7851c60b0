SYSLOGD(8)                BSD System Manager's Manual               SYSLOGD(8)

NNAAMMEE
     ssyyssllooggdd — log system messages

SSYYNNOOPPSSIISS
     ssyyssllooggdd [--4466ddFFhhnnrruuVVZZ] [--aa _p_a_t_h] [--CC _C_A_f_i_l_e] [--cc _c_e_r_t___f_i_l_e]
             [--ff _c_o_n_f_i_g___f_i_l_e] [--KK _C_A_f_i_l_e] [--kk _k_e_y___f_i_l_e] [--mm _m_a_r_k___i_n_t_e_r_v_a_l]
             [--pp _l_o_g___s_o_c_k_e_t] [--SS _l_i_s_t_e_n___a_d_d_r_e_s_s] [--ss _r_e_p_o_r_t_i_n_g___s_o_c_k_e_t]
             [--TT _l_i_s_t_e_n___a_d_d_r_e_s_s] [--UU _b_i_n_d___a_d_d_r_e_s_s]

DDEESSCCRRIIPPTTIIOONN
     ssyyssllooggdd writes system messages to log files or a user's terminal.  Output
     can be sent to other programs for further processing.  It can also se‐
     curely send and receive log messages to and from remote hosts.

     The options are as follows:

     --44      Forces ssyyssllooggdd to use only IPv4 addresses for UDP.

     --66      Forces ssyyssllooggdd to use only IPv6 addresses for UDP.

     --aa _p_a_t_h
             Specify a location where ssyyssllooggdd should place an additional log
             socket.  The primary use for this is to place additional log
             sockets in _/_d_e_v_/_l_o_g of various chroot filespaces, though the need
             for these is less urgent after the introduction of sendsyslog(2).

     --CC _C_A_f_i_l_e
             PEM encoded file containing CA certificates used for certificate
             validation of a remote loghost; the default is _/_e_t_c_/_s_s_l_/_c_e_r_t_._p_e_m.

     --cc _c_e_r_t___f_i_l_e
             PEM encoded file containing the client certificate for TLS con‐
             nections to a remote loghost.  The default is not to use a client
             certificate for the outgoing connection to a syslog server.  This
             option has to be used together with --kk _k_e_y___f_i_l_e.

     --dd      Enable debugging to the standard output, and do not disassociate
             from the controlling terminal.

     --FF      Run in the foreground instead of disassociating from the control‐
             ling terminal and running as a background daemon.

     --ff _c_o_n_f_i_g___f_i_l_e
             Specify the pathname of an alternate configuration file; the de‐
             fault is _/_e_t_c_/_s_y_s_l_o_g_._c_o_n_f.

     --hh      Include the hostname when sending messages to a remote loghost.

     --KK _C_A_f_i_l_e
             PEM encoded file containing CA certificates used for client cer‐
             tificate validation on the local listen socket.  By default in‐
             coming connections from any TLS client are allowed.

     --kk _k_e_y___f_i_l_e
             PEM encoded file containing the client private key for TLS con‐
             nections to a remote loghost.  This option has to be used to‐
             gether with --cc _c_e_r_t___f_i_l_e.

     --mm _m_a_r_k___i_n_t_e_r_v_a_l
             Select the number of minutes between “mark” messages; the default
             is 20 minutes.

     --nn      Print source addresses numerically rather than symbolically.
             This saves an address-to-name lookup for each incoming message,
             which can be useful when combined with the --uu option on a loghost
             with no DNS cache.  Messages from the local host will still be
             logged with the symbolic local host name.

     --pp _l_o_g___s_o_c_k_e_t
             Specify the pathname of an alternate log socket to be used in‐
             stead; the default is _/_d_e_v_/_l_o_g.

     --rr      Print duplicate lines immediately and suppress the "last message
             repeated" summary when piping to another program or forwarding to
             a remote loghost.  If given twice, this is done for all log ac‐
             tions.

     --SS _l_i_s_t_e_n___a_d_d_r_e_s_s
             Create a TLS listen socket for receiving encrypted messages and
             bind it to the specified address.  A port number may be specified
             using the _h_o_s_t:_p_o_r_t syntax.  The first _l_i_s_t_e_n___a_d_d_r_e_s_s is also
             used to find a suitable server key and certificate in _/_e_t_c_/_s_s_l_/.

     --ss _r_e_p_o_r_t_i_n_g___s_o_c_k_e_t
             Specify path to a UNIX-domain socket for use in reporting logs
             stored in memory buffers using syslogc(8).

     --TT _l_i_s_t_e_n___a_d_d_r_e_s_s
             Create a TCP listen socket for receiving messages and bind it to
             the specified address.  There is no well-known port for syslog
             over TCP, so a port number must be specified using the _h_o_s_t:_p_o_r_t
             syntax.

     --UU _b_i_n_d___a_d_d_r_e_s_s
             Create a UDP socket for receiving messages and bind it to the
             specified address.  This can be used, for example, with a pf di‐
             vert-to rule to receive packets when ssyyssllooggdd is bound to local‐
             host.  A port number may be specified using the _h_o_s_t:_p_o_r_t syntax.

     --uu      Select the historical “insecure” mode, in which ssyyssllooggdd will ac‐
             cept input from the UDP port.  Some software wants this, but you
             can be subjected to a variety of attacks over the network, in‐
             cluding attackers remotely filling logs.

     --VV      Do not perform remote server certificate and hostname validation
             when sending messages.

     --ZZ      Generate timestamps in ISO format.  This includes the year and
             the timezone, and all logging is done in UTC.

     The options --aa, --SS, --TT, and --UU can be given more than once to specify
     multiple input sources.

     When starting up, ssyyssllooggdd reads its configuration file, syslog.conf(5),
     and opens the configured logfiles and TCP and TLS connections.  The log‐
     files already have to exist with the correct permissions.  When receiving
     a SIGHUP signal, it closes all open logfiles and outgoing TCP and TLS
     connections and re-runs this initialization sequence.  Sending this sig‐
     nal is required both after editing the configuration file and after log
     rotation.

     ssyyssllooggdd opens a UDP socket, as specified in _/_e_t_c_/_s_e_r_v_i_c_e_s, for sending
     forwarded messages.  By default all incoming data on this socket is dis‐
     carded.  If insecure mode is switched on with --uu, it will also read mes‐
     sages from the socket.  ssyyssllooggdd also opens and reads messages from the
     UNIX-domain socket _/_d_e_v_/_l_o_g, and from the special device _/_d_e_v_/_k_l_o_g (to
     read kernel messages), and from sendsyslog(2) (to read messages from
     userland processes).

     The message sent to ssyyssllooggdd should consist of a single line.  Embedded
     new line characters are converted to spaces; binary data is encoded by
     vis(3), but no backslash is inserted.  The message can contain a priority
     code, which should be a preceding decimal number in angle braces, for ex‐
     ample, “<5>”.  This priority code should map into the priorities defined
     in the include file <_s_y_s_/_s_y_s_l_o_g_._h>.

     When sending syslog messages to a remote loghost via TLS, the server's
     certificate and hostname are validated to prevent malicious servers from
     reading messages.  If the server has a certificate with a matching host‐
     name signed by a CA in _/_e_t_c_/_s_s_l_/_c_e_r_t_._p_e_m, it is verified with that by de‐
     fault.  If the server has a certificate with a matching hostname signed
     by a private CA, use the --CC option and put that CA into _C_A_f_i_l_e.  Valida‐
     tion can be explicitly turned off using the --VV option.  If the server is
     accepting messages only from clients with a trusted client certificate,
     use the --kk and --cc options to authenticate ssyyssllooggdd with this certificate.

     When receiving syslog messages from a TLS client, there must be a server
     key and certificate in _/_e_t_c_/_s_s_l_/_p_r_i_v_a_t_e_/_h_o_s_t[:_p_o_r_t]_._k_e_y and
     _/_e_t_c_/_s_s_l_/_h_o_s_t[:_p_o_r_t]_._c_r_t.  If the client uses certificates to authenti‐
     cate, the CA of the client's certificate may be added to _C_A_f_i_l_e using the
     --KK option to protect from messages being spoofed by malicious senders.

FFIILLEESS
     /dev/log             Name of the UNIX-domain datagram log socket.
     /dev/klog            Kernel log device.
     /etc/ssl/            Private keys and public certificates.
     /etc/syslog.conf     Configuration file.
     /var/run/syslog.pid  Process ID of current ssyyssllooggdd.

SSEEEE AALLSSOO
     logger(1), syslog(3), services(5), syslog.conf(5), newsyslog(8),
     syslogc(8)

HHIISSTTOORRYY
     The ssyyssllooggdd command appeared in 4.3BSD.

CCAAVVEEAATTSS
     ssyyssllooggdd does not create files, it only logs to existing ones.

BSD                              June 16, 2022                             BSD
