_c_o_p_y___f_i_l_e___r_a_n_g_e(2)            System Calls Manual           _c_o_p_y___f_i_l_e___r_a_n_g_e(2)

NNAAMMEE
       copy_file_range - Copy a range of data from one file to another

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##ddeeffiinnee __GGNNUU__SSOOUURRCCEE
       ##ddeeffiinnee __FFIILLEE__OOFFFFSSEETT__BBIITTSS 6644
       ##iinncclluuddee <<uunniissttdd..hh>>

       ssssiizzee__tt ccooppyy__ffiillee__rraannggee((iinntt _f_d___i_n,, ooffff__tt **__NNuullllaabbllee _o_f_f___i_n,,
                               iinntt _f_d___o_u_t,, ooffff__tt **__NNuullllaabbllee _o_f_f___o_u_t,,
                               ssiizzee__tt _s_i_z_e,, uunnssiiggnneedd iinntt _f_l_a_g_s));;

DDEESSCCRRIIPPTTIIOONN
       The  ccooppyy__ffiillee__rraannggee()  system  call performs an in‐kernel copy between
       two file descriptors without the additional cost of  transferring  data
       from the kernel to user space and then back into the kernel.  It copies
       up  to  _s_i_z_e bytes of data from the source file descriptor _f_d___i_n to the
       target file descriptor _f_d___o_u_t, overwriting any data that exists  within
       the requested range of the target file.

       The  following semantics apply for _o_f_f___i_n, and similar statements apply
       to _o_f_f___o_u_t:

       •  If _o_f_f___i_n is NULL, then bytes are read from _f_d___i_n starting from  the
          file  offset, and the file offset is adjusted by the number of bytes
          copied.

       •  If _o_f_f___i_n is not NULL, then _o_f_f___i_n must point to a buffer that spec‐
          ifies the starting offset where bytes from _f_d___i_n will be read.   The
          file  offset  of _f_d___i_n is not changed, but _o_f_f___i_n is adjusted appro‐
          priately.

       _f_d___i_n and _f_d___o_u_t can refer to the same file.  If they refer to the same
       file, then the source and target ranges are not allowed to overlap.

       The _f_l_a_g_s argument is provided to allow for future extensions and  cur‐
       rently must be set to 0.

RREETTUURRNN VVAALLUUEE
       Upon successful completion, ccooppyy__ffiillee__rraannggee() will return the number of
       bytes  copied  between  files.  This could be less than the size origi‐
       nally requested.  If the file offset of _f_d___i_n is at or past the end  of
       file, no bytes are copied, and ccooppyy__ffiillee__rraannggee() returns zero.

       On error, ccooppyy__ffiillee__rraannggee() returns -1 and _e_r_r_n_o is set to indicate the
       error.

EERRRROORRSS
       EEBBAADDFF  One or more file descriptors are not valid.

       EEBBAADDFF  _f_d___i_n  is  not open for reading; or _f_d___o_u_t is not open for writ‐
              ing.

       EEBBAADDFF  The OO__AAPPPPEENNDD flag is set for  the  open  file  description  (see
              ooppeenn(2)) referred to by the file descriptor _f_d___o_u_t.

       EEFFBBIIGG  An attempt was made to write at a position past the maximum file
              offset the kernel supports.

       EEFFBBIIGG  An  attempt  was  made to write a range that exceeds the allowed
              maximum file  size.   The  maximum  file  size  differs  between
              filesystem implementations and can be different from the maximum
              allowed file offset.

       EEFFBBIIGG  An  attempt was made to write beyond the process’s file size re‐
              source limit.  This may also result in the process  receiving  a
              SSIIGGXXFFSSZZ signal.

       EEIINNVVAALL The _f_l_a_g_s argument is not 0.

       EEIINNVVAALL _f_d___i_n  and _f_d___o_u_t refer to the same file and the source and tar‐
              get ranges overlap.

       EEIINNVVAALL Either _f_d___i_n or _f_d___o_u_t is not a regular file.

       EEIIOO    A low‐level I/O error occurred while copying.

       EEIISSDDIIRR Either _f_d___i_n or _f_d___o_u_t refers to a directory.

       EENNOOMMEEMM Out of memory.

       EENNOOSSPPCC There is not enough space on the target filesystem  to  complete
              the copy.

       EEOOPPNNOOTTSSUUPPPP (since Linux 5.19)
              The filesystem does not support this operation.

       EEOOVVEERRFFLLOOWW
              The requested source or destination range is too large to repre‐
              sent in the specified data types.

       EEPPEERRMM  _f_d___o_u_t refers to an immutable file.

       EETTXXTTBBSSYY
              Either _f_d___i_n or _f_d___o_u_t refers to an active swap file.

       EEXXDDEEVV (before Linux 5.3)
              The  files  referred  to by _f_d___i_n and _f_d___o_u_t are not on the same
              filesystem.

       EEXXDDEEVV (since Linux 5.19)
              The files referred to by _f_d___i_n and _f_d___o_u_t are not  on  the  same
              filesystem, and the source and target filesystems are not of the
              same type, or do not support cross‐filesystem copy.

VVEERRSSIIOONNSS
       A major rework of the kernel implementation occurred in Linux 5.3.  Ar‐
       eas  of the API that weren’t clearly defined were clarified and the API
       bounds are much more strictly checked than on earlier kernels.

       Since Linux 5.19, cross‐filesystem copies can  be  achieved  when  both
       filesystems  are  of the same type, and that filesystem implements sup‐
       port for it.  See BUGS for behavior prior to Linux 5.19.

       Applications should target the  behaviour  and  requirements  of  Linux
       5.19, that was also backported to earlier stable kernels.

SSTTAANNDDAARRDDSS
       Linux, GNU.

HHIISSTTOORRYY
       Linux  4.5,  but  glibc 2.27 provides a user‐space emulation when it is
       not available.

NNOOTTEESS
       If _f_d___i_n is a sparse file, then ccooppyy__ffiillee__rraannggee() may expand any  holes
       existing  in  the  requested  range.   Users  may  benefit from calling
       ccooppyy__ffiillee__rraannggee() in a loop,  and  using  the  llsseeeekk(2)  SSEEEEKK__DDAATTAA  and
       SSEEEEKK__HHOOLLEE operations to find the locations of data segments.

       ccooppyy__ffiillee__rraannggee()  gives  filesystems an opportunity to implement "copy
       acceleration" techniques, such as the use of  reflinks  (i.e.,  two  or
       more  inodes that share pointers to the same copy‐on‐write disk blocks)
       or server‐side‐copy (in the case of NFS).

       __FFIILLEE__OOFFFFSSEETT__BBIITTSS should be defined to be 64 in code that uses non‐null
       _o_f_f___i_n or _o_f_f___o_u_t or that takes the address of ccooppyy__ffiillee__rraannggee, if  the
       code is intended to be portable to traditional 32‐bit x86 and ARM plat‐
       forms where ooffff__tt’s width defaults to 32 bits.

BBUUGGSS
       In Linux 5.3 to Linux 5.18, cross‐filesystem copies were implemented by
       the  kernel,  if the operation was not supported by individual filesys‐
       tems.  However, on some virtual filesystems, the call failed  to  copy,
       while still reporting success.

EEXXAAMMPPLLEESS
       #define _GNU_SOURCE
       #define _FILE_OFFSET_BITS 64
       #include <fcntl.h>
       #include <stdio.h>
       #include <stdlib.h>
       #include <sys/stat.h>
       #include <sys/types.h>
       #include <unistd.h>

       int
       main(int argc, char *argv[])
       {
           int          fd_in, fd_out;
           off_t        size, ret;
           struct stat  stat;

           if (argc != 3) {
               fprintf(stderr, "Usage: %s <source> <destination>\n", argv[0]);
               exit(EXIT_FAILURE);
           }

           fd_in = open(argv[1], O_RDONLY);
           if (fd_in == -1) {
               perror("open (argv[1])");
               exit(EXIT_FAILURE);
           }

           if (fstat(fd_in, &stat) == -1) {
               perror("fstat");
               exit(EXIT_FAILURE);
           }

           size = stat.st_size;

           fd_out = open(argv[2], O_CREAT | O_WRONLY | O_TRUNC, 0644);
           if (fd_out == -1) {
               perror("open (argv[2])");
               exit(EXIT_FAILURE);
           }

           do {
               ret = copy_file_range(fd_in, NULL, fd_out, NULL, size, 0);
               if (ret == -1) {
                   perror("copy_file_range");
                   exit(EXIT_FAILURE);
               }

               size -= ret;
           } while (size > 0 && ret > 0);

           close(fd_in);
           close(fd_out);
           exit(EXIT_SUCCESS);
       }

SSEEEE AALLSSOO
       llsseeeekk(2), sseennddffiillee(2), sspplliiccee(2)

Linux man‐pages (unreleased)      2025‐09‐21                _c_o_p_y___f_i_l_e___r_a_n_g_e(2)
