|  The  subsystem  allows  network management of passwd and group
file entries through the functions and also  provides  hooks  for
other  client  programs, such as and that can use maps.  Password
maps in standard YP are insecure, because the pw_passwd field  is
accessible  by any user. A common solution to this is to generate
a secure map (using which can only be accessed by a client  bound
to a privileged port.  To activate the secure map, see the appro‐
priate  comment  in The subsystem is conditionally started in See
the file for configuration variables.  The client  subsystem  was
originally  written  by Theo de Raadt to be compatible with Sun’s
implementation.  The server suite was originally written by  Mats
O  Jansson.  If cannot find a server, the system behaves the same
way as Sun’s code: it hangs.  The feature is not compatible  with
non‐BSD implementations as found e.g. in Solaris.
