BLUETOOTH(3)             BSD Library Functions Manual             BLUETOOTH(3)

NNAAMMEE
     bbtt__ggeetthhoossttbbyynnaammee, bbtt__ggeetthhoossttbbyyaaddddrr, bbtt__ggeetthhoosstteenntt, bbtt__sseetthhoosstteenntt,
     bbtt__eennddhhoosstteenntt, bbtt__ggeettpprroottoobbyynnaammee, bbtt__ggeettpprroottoobbyynnuummbbeerr, bbtt__ggeettpprroottooeenntt,
     bbtt__sseettpprroottooeenntt, bbtt__eennddpprroottooeenntt, bbtt__aattoonn, bbtt__nnttooaa, bbtt__ddeevvaaddddrr, bbtt__ddeevvnnaammee,
     bbtt__ddeevviinnffoo, bbtt__ddeevveennuumm, bbtt__ddeevvooppeenn, bbtt__ddeevvcclloossee, bbtt__ddeevvsseenndd, bbtt__ddeevvrreeccvv,
     bbtt__ddeevvrreeqq, bbtt__ddeevvffiilltteerr, bbtt__ddeevvffiilltteerr__ppkktt__sseett, bbtt__ddeevvffiilltteerr__ppkktt__ccllrr,
     bbtt__ddeevvffiilltteerr__ppkktt__ttsstt, bbtt__ddeevvffiilltteerr__eevvtt__sseett, bbtt__ddeevvffiilltteerr__eevvtt__ccllrr,
     bbtt__ddeevvffiilltteerr__eevvtt__ttsstt, bbtt__ddeevviinnqquuiirryy, bbtt__ddeevvrreemmoottee__nnaammee,
     bbtt__ddeevvrreemmoottee__nnaammee__ggeenn, bbddaaddddrr__ssaammee, bbddaaddddrr__aannyy, bbddaaddddrr__ccooppyy — Bluetooth
     routines

LLIIBBRRAARRYY
     Bluetooth Library (libbluetooth, -lbluetooth)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<bblluueettooootthh..hh>>

     _s_t_r_u_c_t _h_o_s_t_e_n_t _*
     bbtt__ggeetthhoossttbbyynnaammee(_c_o_n_s_t _c_h_a_r _*_n_a_m_e);

     _s_t_r_u_c_t _h_o_s_t_e_n_t _*
     bbtt__ggeetthhoossttbbyyaaddddrr(_c_o_n_s_t _c_h_a_r _*_a_d_d_r, _i_n_t _l_e_n, _i_n_t _t_y_p_e);

     _s_t_r_u_c_t _h_o_s_t_e_n_t _*
     bbtt__ggeetthhoosstteenntt(_v_o_i_d);

     _v_o_i_d
     bbtt__sseetthhoosstteenntt(_i_n_t _s_t_a_y_o_p_e_n);

     _v_o_i_d
     bbtt__eennddhhoosstteenntt(_v_o_i_d);

     _s_t_r_u_c_t _p_r_o_t_o_e_n_t _*
     bbtt__ggeettpprroottoobbyynnaammee(_c_o_n_s_t _c_h_a_r _*_n_a_m_e);

     _s_t_r_u_c_t _p_r_o_t_o_e_n_t _*
     bbtt__ggeettpprroottoobbyynnuummbbeerr(_i_n_t _p_r_o_t_o);

     _s_t_r_u_c_t _p_r_o_t_o_e_n_t _*
     bbtt__ggeettpprroottooeenntt(_v_o_i_d);

     _v_o_i_d
     bbtt__sseettpprroottooeenntt(_i_n_t _s_t_a_y_o_p_e_n);

     _v_o_i_d
     bbtt__eennddpprroottooeenntt(_v_o_i_d);

     _i_n_t
     bbtt__aattoonn(_c_o_n_s_t _c_h_a_r _*_s_t_r, _b_d_a_d_d_r___t _*_b_a);

     _c_o_n_s_t _c_h_a_r _*
     bbtt__nnttooaa(_c_o_n_s_t _b_d_a_d_d_r___t _*_b_a, _c_h_a_r _*_s_t_r);

     _i_n_t
     bbtt__ddeevvaaddddrr(_c_o_n_s_t _c_h_a_r _*_d_e_v_n_a_m_e, _b_d_a_d_d_r___t _*_a_d_d_r);

     _i_n_t
     bbtt__ddeevvnnaammee(_c_h_a_r _*_d_e_v_n_a_m_e, _c_o_n_s_t _b_d_a_d_d_r___t _*_a_d_d_r);

     _i_n_t
     ((bbtt__ddeevveennuumm__ccbb__tt))(_i_n_t _s, _s_t_r_u_c_t _b_t___d_e_v_i_n_f_o _c_o_n_s_t _*_d_i, _v_o_i_d _*_a_r_g);

     _i_n_t
     bbtt__ddeevviinnffoo(_s_t_r_u_c_t _b_t___d_e_v_i_n_f_o _*_d_i);

     _i_n_t
     bbtt__ddeevveennuumm(_b_t___d_e_v_e_n_u_m___c_b___t _*_c_b, _v_o_i_d _*_a_r_g);

     _i_n_t
     bbtt__ddeevvooppeenn(_c_h_a_r _c_o_n_s_t _*_d_e_v_n_a_m_e);

     _i_n_t
     bbtt__ddeevvcclloossee(_i_n_t _s);

     _i_n_t
     bbtt__ddeevvsseenndd(_i_n_t _s, _u_i_n_t_1_6___t _o_p_c_o_d_e, _v_o_i_d _*_p_a_r_a_m, _s_i_z_e___t _p_l_e_n);

     _s_s_i_z_e___t
     bbtt__ddeevvrreeccvv(_i_n_t _s, _v_o_i_d _*_b_u_f, _s_i_z_e___t _s_i_z_e, _t_i_m_e___t _t_o);

     _i_n_t
     bbtt__ddeevvrreeqq(_i_n_t _s, _s_t_r_u_c_t _b_t___d_e_v_r_e_q _*_r, _t_i_m_e___t _t_o);

     _i_n_t
     bbtt__ddeevvffiilltteerr(_i_n_t _s, _s_t_r_u_c_t _b_t___d_e_v_f_i_l_t_e_r _c_o_n_s_t _*_n_e_w,
         _s_t_r_u_c_t _b_t___d_e_v_f_i_l_t_e_r _*_o_l_d);

     _v_o_i_d
     bbtt__ddeevvffiilltteerr__ppkktt__sseett(_s_t_r_u_c_t _b_t___d_e_v_f_i_l_t_e_r _*_f_i_l_t_e_r, _u_i_n_t_8___t _t_y_p_e);

     _v_o_i_d
     bbtt__ddeevvffiilltteerr__ppkktt__cclltt(_s_t_r_u_c_t _b_t___d_e_v_f_i_l_t_e_r _*_f_i_l_t_e_r, _u_i_n_t_8___t _t_y_p_e);

     _i_n_t
     bbtt__ddeevvffiilltteerr__ppkktt__ttsstt(_s_t_r_u_c_t _b_t___d_e_v_f_i_l_t_e_r _c_o_n_s_t _*_f_i_l_t_e_r, _u_i_n_t_8___t _t_y_p_e);

     _v_o_i_d
     bbtt__ddeevvffiilltteerr__eevvtt__sseett(_s_t_r_u_c_t _b_t___d_e_v_f_i_l_t_e_r _*_f_i_l_t_e_r, _u_i_n_t_8___t _e_v_e_n_t);

     _v_o_i_d
     bbtt__ddeevvffiilltteerr__eevvtt__cclltt(_s_t_r_u_c_t _b_t___d_e_v_f_i_l_t_e_r _*_f_i_l_t_e_r, _u_i_n_t_8___t _e_v_e_n_t);

     _i_n_t
     bbtt__ddeevvffiilltteerr__eevvtt__ttsstt(_s_t_r_u_c_t _b_t___d_e_v_f_i_l_t_e_r _c_o_n_s_t _*_f_i_l_t_e_r, _u_i_n_t_8___t _e_v_e_n_t);

     _i_n_t
     bbtt__ddeevviinnqquuiirryy(_c_h_a_r _c_o_n_s_t _*_d_e_v_n_a_m_e, _t_i_m_e___t _l_e_n_g_t_h, _i_n_t _n_u_m___r_s_p,
         _s_t_r_u_c_t _b_t___d_e_v_i_n_q_u_i_r_y _*_*_i_i);

     _c_h_a_r _*
     bbtt__ddeevvrreemmoottee__nnaammee(_c_h_a_r _c_o_n_s_t _*_d_e_v_n_a_m_e, _c_o_n_s_t _b_d_a_d_d_r___t _*_r_e_m_o_t_e, _t_i_m_e___t _t_o,
         _u_i_n_t_1_6___t _c_l_k___o_f_f, _u_i_n_t_8___t _p_s___r_e_p___m_o_d_e, _u_i_n_t_8___t _p_s___m_o_d_e);

     _c_h_a_r _*
     bbtt__ddeevvrreemmoottee__nnaammee__ggeenn(_c_h_a_r _c_o_n_s_t _*_b_t_o_o_t_h___d_e_v_n_a_m_e,
         _c_o_n_s_t _b_d_a_d_d_r___t _*_r_e_m_o_t_e);

     _i_n_t
     bbddaaddddrr__ssaammee(_c_o_n_s_t _b_d_a_d_d_r___t _*_a, _c_o_n_s_t _b_d_a_d_d_r___t _*_b);

     _i_n_t
     bbddaaddddrr__aannyy(_c_o_n_s_t _b_d_a_d_d_r___t _*_a);

     _i_n_t
     bbddaaddddrr__ccooppyy(_c_o_n_s_t _b_d_a_d_d_r___t _*_d_s_t, _c_o_n_s_t _b_d_a_d_d_r___t _*_s_r_c);

DDEESSCCRRIIPPTTIIOONN
     The bbtt__ggeetthhoosstteenntt(), bbtt__ggeetthhoossttbbyynnaammee() and bbtt__ggeetthhoossttbbyyaaddddrr() functions
     each return a pointer to an object with the _h_o_s_t_e_n_t structure describing
     a Bluetooth host referenced by name or by address, respectively.

     The _n_a_m_e argument passed to bbtt__ggeetthhoossttbbyynnaammee() should point to a
     NUL-terminated hostname.  The _a_d_d_r argument passed to bbtt__ggeetthhoossttbbyyaaddddrr()
     should point to an address which is _l_e_n bytes long, in binary form (i.e.,
     not a Bluetooth BD_ADDR in human readable ASCII form).  The _t_y_p_e argument
     specifies the address family of this address and must be set to
     AF_BLUETOOTH.

     The structure returned contains the information obtained from a line in
     _/_e_t_c_/_b_l_u_e_t_o_o_t_h_/_h_o_s_t_s file.

     The bbtt__sseetthhoosstteenntt() function controls whether _/_e_t_c_/_b_l_u_e_t_o_o_t_h_/_h_o_s_t_s file
     should stay open after each call to bbtt__ggeetthhoossttbbyynnaammee() or
     bbtt__ggeetthhoossttbbyyaaddddrr().  If the _s_t_a_y_o_p_e_n flag is non-zero, the file will not
     be closed.

     The bbtt__eennddhhoosstteenntt() function closes the _/_e_t_c_/_b_l_u_e_t_o_o_t_h_/_h_o_s_t_s file.

     The bbtt__ggeettpprroottooeenntt(), bbtt__ggeettpprroottoobbyynnaammee() and bbtt__ggeettpprroottoobbyynnuummbbeerr() func‐
     tions each return a pointer to an object with the _p_r_o_t_o_e_n_t structure de‐
     scribing a Bluetooth Protocol Service Multiplexor referenced by name or
     number, respectively.

     The _n_a_m_e argument passed to bbtt__ggeettpprroottoobbyynnaammee() should point to a
     NUL-terminated Bluetooth Protocol Service Multiplexor name.  The _p_r_o_t_o
     argument passed to bbtt__ggeettpprroottoobbyynnuummbbeerr() should have numeric value of the
     desired Bluetooth Protocol Service Multiplexor.

     The structure returned contains the information obtained from a line in
     _/_e_t_c_/_b_l_u_e_t_o_o_t_h_/_p_r_o_t_o_c_o_l_s file.

     The bbtt__sseettpprroottooeenntt() function controls whether _/_e_t_c_/_b_l_u_e_t_o_o_t_h_/_p_r_o_t_o_c_o_l_s
     file should stay open after each call to bbtt__ggeettpprroottoobbyynnaammee() or
     bbtt__ggeettpprroottoobbyynnuummbbeerr().  If the _s_t_a_y_o_p_e_n flag is non-zero, the file will
     not be closed.

     The bbtt__eennddpprroottooeenntt() function closes the _/_e_t_c_/_b_l_u_e_t_o_o_t_h_/_p_r_o_t_o_c_o_l_s file.

     The bbtt__aattoonn() routine interprets the specified character string as a
     Bluetooth address, placing the address into the structure provided.  It
     returns 1 if the string was successfully interpreted, or 0 if the string
     is invalid.

     The routine bbtt__nnttooaa() takes a Bluetooth address and places an ASCII
     string representing the address into the buffer provided.  It is up to
     the caller to ensure that provided buffer has enough space.  If no buffer
     was provided then internal static buffer will be used.

     The bbtt__ddeevvaaddddrr() function interprets the specified _d_e_v_n_a_m_e string as the
     address or device name of a Bluetooth device on the local system, and
     places the device address in the provided _b_d_a_d_d_r, if any.  The function
     returns 1 if the string was successfully interpreted, or 0 if the string
     did not match any local device.  The bbtt__ddeevvnnaammee() function takes a Blue‐
     tooth device address and copies the local device name associated with
     that address into the buffer provided, if any.  Caller must ensure that
     provided buffer is at least HCI_DEVNAME_SIZE characters in size.  The
     function returns 1 when the device was found, otherwise 0.

     The bbtt__ddeevviinnffoo() function populates provided _b_t___d_e_v_i_n_f_o structure with
     the information about given Bluetooth device.  The caller is expected to
     pass Bluetooth device name in the _d_e_v_n_a_m_e field of the passed _b_t___d_e_v_i_n_f_o
     structure.  The function returns 0 when successful, otherwise -1.  The
     _b_t___d_e_v_i_n_f_o structure is defined as follows

           struct bt_devinfo
           {
                   char            devname[HCI_DEVNAME_SIZE];

                   uint32_t        state;

                   bdaddr_t        bdaddr;
                   uint16_t        _reserved0;

                   uint8_t         features[HCI_DEVFEATURES_SIZE];

                   /* buffer info */
                   uint16_t        _reserved1;
                   uint16_t        cmd_free;
                   uint16_t        sco_size;
                   uint16_t        sco_pkts;
                   uint16_t        sco_free;
                   uint16_t        acl_size;
                   uint16_t        acl_pkts;
                   uint16_t        acl_free;

                   /* stats */
                   uint32_t        cmd_sent;
                   uint32_t        evnt_recv;
                   uint32_t        acl_recv;
                   uint32_t        acl_sent;
                   uint32_t        sco_recv;
                   uint32_t        sco_sent;
                   uint32_t        bytes_recv;
                   uint32_t        bytes_sent;

                   /* misc/specific */
                   uint16_t        link_policy_info;
                   uint16_t        packet_type_info;
                   uint16_t        role_switch_info;
                   uint16_t        debug;

                   uint8_t         _padding[20];
           };

     The bbtt__ddeevveennuumm() function enumerates Bluetooth devices present in the
     system.  For every device found, the function will call provided _c_b call‐
     back function which should be of _b_t___d_e_v_e_n_u_m___c_b___t type.  The callback
     function is passed a HCI socket _s, fully populated _b_t___d_e_v_i_n_f_o structure
     _d_i and _a_r_g argument provided to the bbtt__ddeevveennuumm().  The callback function
     can stop enumeration by returning a value that is greater than zero.  The
     function returns number of successfully enumerated devices, or -1 if an
     error occurred.

     The bbtt__ddeevvooppeenn() function opens a Bluetooth device with the given _d_e_v_n_a_m_e
     and returns a connected and bound HCI socket handle.  The function re‐
     turns -1 if an error has occurred.

     The bbtt__ddeevvcclloossee() closes the passed HCI socket handle _s, previously ob‐
     tained with bt_devopen(3).

     The bbtt__ddeevvsseenndd() function sends a Bluetooth HCI command with the given
     _o_p_c_o_d_e to the provided socket _s, previously obtained with bt_devopen(3).
     The _o_p_c_o_d_e parameter is expected to be in the host byte order.  The _p_a_r_a_m
     and _p_l_e_n parameters specify command parameters.  The bbtt__ddeevvsseenndd() func‐
     tion does not modify the HCI filter on the provided socket _s.  The func‐
     tion returns 0 on success, or -1 if an error occurred.

     The bbtt__ddeevvrreeccvv() function receives one Bluetooth HCI packet from the
     socket _s, previously obtained with bt_devopen(3).  The packet is placed
     into the provided buffer _b_u_f of size _s_i_z_e.  The _t_o parameter specifies
     receive timeout in seconds.  Infinite timeout can be specified by passing
     negative value in the _t_o parameter.  The bbtt__ddeevvrreeccvv() function does not
     modify the HCI filter on the provided socket _s.  The function returns to‐
     tal number of bytes received, or -1 if an error occurred.

     The bbtt__ddeevvrreeqq() function makes a Bluetooth HCI request to the socket _s,
     previously obtained with bt_devopen(3).  The function will send the spec‐
     ified command and will wait for the specified event, or timeout _t_o sec‐
     onds to occur.  The _b_t___d_e_v_r_e_q structure is defined as follows

           struct bt_devreq
           {
                   uint16_t        opcode;
                   uint8_t         event;
                   void            *cparam;
                   size_t          clen;
                   void            *rparam;
                   size_t          rlen;
           };

     The _o_p_c_o_d_e field specifies the command and is expected to be in the host
     byte order.  The _c_p_a_r_a_m and _c_l_e_n fields specify command parameters data
     and command parameters data size respectively.  The _e_v_e_n_t field specifies
     which Bluetooth HCI event ID the function should wait for, otherwise it
     should be set to zero.  The HCI Command Complete and Command Status
     events are enabled by default.  The _r_p_a_r_a_m and _r_l_e_n parameters specify
     buffer and buffer size respectively where return parameters should be
     placed.  The bbtt__ddeevvrreeqq() function temporarily modifies filter on the pro‐
     vided HCI socket _s.  The function returns 0 on success, or -1 if an error
     occurred.

     The bbtt__ddeevvffiilltteerr() controls the local HCI filter associated with the
     socket _s, previously obtained with bt_devopen(3).  Filtering can be done
     on packet types, i.e.  ACL, SCO or HCI, command and event packets, and,
     in addition, on HCI event IDs.  Before applying the _n_e_w filter (if pro‐
     vided) the function will try to obtain the current filter from the socket
     _s and place it into the _o_l_d parameter (if provided).  The function re‐
     turns 0 on success, or -1 if an error occurred.

     The bbtt__ddeevvffiilltteerr__ppkktt__sseett(), bbtt__ddeevvffiilltteerr__ppkktt__ccllrr() and
     bbtt__ddeevvffiilltteerr__ppkktt__ttsstt() functions can be used to modify and test the HCI
     filter _f_i_l_t_e_r.  The _t_y_p_e parameter specifies HCI packet type.

     The bbtt__ddeevvffiilltteerr__eevvtt__sseett(), bbtt__ddeevvffiilltteerr__eevvtt__ccllrr() and
     bbtt__ddeevvffiilltteerr__eevvtt__ttsstt() functions can be used to modify and test the HCI
     event filter _f_i_l_t_e_r.  The _e_v_e_n_t parameter specifies HCI event ID.

     The bbtt__ddeevviinnqquuiirryy() function performs Bluetooth inquiry.  The _d_e_v_n_a_m_e pa‐
     rameter specifies which local Bluetooth device should perform an inquiry.
     If not specified, i.e.  NULL, then first available device will be used.
     The _l_e_n_g_t_h parameters specifies the total length of an inquiry in sec‐
     onds.  If not specified, i.e. 0, default value will be used.  The _n_u_m___r_s_p
     parameter specifies the number of responses that can be received before
     the inquiry is halted.  If not specified, i.e. 0, default value will be
     used.  The _i_i parameter specifies where to place inquiry results.  On
     success, the function will return total number of inquiry results, will
     allocate, using calloc(3), buffer to store all the inquiry results and
     will return pointer to the allocated buffer in the _i_i parameter.  It is
     up to the caller of the function to dispose of the buffer using free(3)
     call.  The function returns -1 if an error has occurred.  The
     _b_t___d_e_v_i_n_q_u_i_r_y structure is defined as follows

           struct bt_devinquiry {
                   bdaddr_t        bdaddr;
                   uint8_t         pscan_rep_mode;
                   uint8_t         pscan_period_mode;
                   uint8_t         dev_class[3];
                   uint16_t        clock_offset;
                   int8_t          rssi;
                   uint8_t         data[240];
           };

     The bbtt__ddeevvrreemmoottee__nnaammee() function performs Bluetooth Remote Name Request
     procedure to obtain the user-friendly name of another Bluetooth unit.
     The _d_e_v_n_a_m_e parameter specifies which local Bluetooth device should per‐
     form the request.  If not specified (NULL), the first available device is
     used.  The _r_e_m_o_t_e parameter specifies the Bluetooth BD_ADDR of the remote
     device to query.  The _t_o parameter specifies response timeout in seconds.
     If not specified (0), the default value is taken from the net.blue‐
     tooth.hci.command_timeout sysctl(8) value.  The _c_l_k___o_f_f, _p_s___r_e_p___m_o_d_e, and
     _p_s___m_o_d_e parameters specify Clock_Offset, Page_Scan_Repetition_Mode, and
     Page_Scan_Mode fields of HCI_Remote_Name_Request respectively.  On suc‐
     cess, the function returns a pointer to dynamically allocated NUL-termi‐
     nated string or NULL if an error occurred.  It is up to the caller to re‐
     lease returned string using free(3).

     The bbtt__ddeevvrreemmoottee__nnaammee__ggeenn() function is a shortcut to bbtt__ddeevvrreemmoottee__nnaammee()
     that passes generic defaults for _t_o, _c_l_k___o_f_f, _p_s___r_e_p___m_o_d_e, and _p_s___m_o_d_e
     parameters.

     The bbddaaddddrr__ssaammee(), bbddaaddddrr__aannyy(), and bbddaaddddrr__ccooppyy() are handy shorthand
     Bluetooth address utility functions.  The bbddaaddddrr__ssaammee() function will
     test if two provided BD_ADDRs are the same.  The bbddaaddddrr__aannyy() function
     will test if provided BD_ADDR is ANY BD_ADDR.  The bbddaaddddrr__ccooppyy() function
     will copy provided _s_r_c BD_ADDR into provided _d_s_t BD_ADDR.

FFIILLEESS
     /etc/bluetooth/hosts
     /etc/bluetooth/protocols

EEXXAAMMPPLLEESS
     Print out the hostname associated with a specific BD_ADDR:

           const char *bdstr = "00:01:02:03:04:05";
           bdaddr_t bd;
           struct hostent *hp;

           if (!bt_aton(bdstr, &bd))
                   errx(1, "can't parse BD_ADDR %s", bdstr);

           if ((hp = bt_gethostbyaddr((const char *)&bd,
               sizeof(bd), AF_BLUETOOTH)) == NULL)
                   errx(1, "no name associated with %s", bdstr);

           printf("name associated with %s is %s\n", bdstr, hp->h_name);

DDIIAAGGNNOOSSTTIICCSS
     Error return status from bbtt__ggeetthhoosstteenntt(), bbtt__ggeetthhoossttbbyynnaammee() and
     bbtt__ggeetthhoossttbbyyaaddddrr() is indicated by return of a NULL pointer.  The exter‐
     nal integer _h___e_r_r_n_o may then be checked to see whether this is a tempo‐
     rary failure or an invalid or unknown host.  The routine herror(3) can be
     used to print an error message describing the failure.  If its argument
     _s_t_r_i_n_g is non-NULL, it is printed, followed by a colon and a space.  The
     error message is printed with a trailing newline.

     The variable _h___e_r_r_n_o can have the following values:

     HOST_NOT_FOUND  No such host is known.

     NO_RECOVERY     Some unexpected server failure was encountered.  This is
                     a non-recoverable error.

     The bbtt__ggeettpprroottooeenntt(), bbtt__ggeettpprroottoobbyynnaammee() and bbtt__ggeettpprroottoobbyynnuummbbeerr() re‐
     turn NULL on EOF or error.

SSEEEE AALLSSOO
     gethostbyaddr(3), gethostbyname(3), getprotobyname(3),
     getprotobynumber(3), herror(3), inet_aton(3), inet_ntoa(3), ng_hci(4)

CCAAVVEEAATT
     The bbtt__ggeetthhoosstteenntt() function reads the next line of _/_e_t_c_/_b_l_u_e_t_o_o_t_h_/_h_o_s_t_s,
     opening the file if necessary.

     The bbtt__sseetthhoosstteenntt() function opens and/or rewinds the
     _/_e_t_c_/_b_l_u_e_t_o_o_t_h_/_h_o_s_t_s file.

     The bbtt__ggeettpprroottooeenntt() function reads the next line of
     _/_e_t_c_/_b_l_u_e_t_o_o_t_h_/_p_r_o_t_o_c_o_l_s, opening the file if necessary.

     The bbtt__sseettpprroottooeenntt() function opens and/or rewinds the
     _/_e_t_c_/_b_l_u_e_t_o_o_t_h_/_p_r_o_t_o_c_o_l_s file.

     The bbtt__ddeevveennuumm() function enumerates up to HCI_DEVMAX Bluetooth devices.
     During enumeration the bbtt__ddeevveennuumm() function uses the same HCI socket.
     The function guarantees that the socket, passed to the callback function,
     will be bound and connected to the Bluetooth device being enumerated.

AAUUTTHHOORRSS
     Maksim Yevmenkin <_m___e_v_m_e_n_k_i_n_@_y_a_h_o_o_._c_o_m>

BBUUGGSS
     Some of those functions use static data storage; if the data is needed
     for future use, it should be copied before any subsequent calls overwrite
     it.

BSD                               May 1, 2018                              BSD
