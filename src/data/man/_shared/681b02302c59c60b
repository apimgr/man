GELF_GETCLASS(3)         BSD Library Functions Manual         GELF_GETCLASS(3)

NNAAMMEE
     ggeellff__ggeettccllaassss — retrieve the class of an ELF descriptor

LLIIBBRRAARRYY
     ELF Access Library (libelf, -lelf)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ggeellff..hh>>

     _i_n_t
     ggeellff__ggeettccllaassss(_E_l_f _*_e_l_f);

DDEESSCCRRIIPPTTIIOONN
     Function ggeellff__ggeettccllaassss() returns the ELF class of the descriptor supplied
     in argument _e_l_f.

RREETTUURRNN VVAALLUUEESS
     Function ggeellff__ggeettccllaassss() will return one of ELFCLASS32 or ELFCLASS64 if
     the argument _e_l_f is a descriptor for an ELF file.  The value ELFCLASSNONE
     is returned if argument _e_l_f was null, or if it was not a descriptor for
     an ELF file.

SSEEEE AALLSSOO
     elf(3), elf_kind(3), gelf(3)

BSD                              July 3, 2006                              BSD
