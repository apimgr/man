is  used to prepare a set of for use by the a.out run‐time linker
to facilitate quick lookup of shared libraries available in  mul‐
tiple  directories.   is  only  available on systems that use the
format for executables and libraries – on ELF  systems,  all  the
work is done by By default, it scans a set of built‐in system di‐
rectories, directories listed in and any specified on the command
line (in the given order) looking for shared libraries and stores
the results in the file to forestall the overhead that would oth‐
erwise  result from the directory search operations would have to
perform to load required shared libraries.  The shared  libraries
so found will be automatically available for loading if needed by
the program being prepared for execution.  This obviates the need
for  storing search paths within the executable.  The environment
variable can be used to override the use of directories  (or  the
order  thereof)  from the cache or to specify additional directo‐
ries where shared libraries might be found.  is a separated  list
of  directory  paths that are searched by when it needs to load a
shared library.  It can be viewed as the run‐time  equivalent  of
the switch of is typically run as part of the boot sequence.  The
following  options  are  recognized  by  Do  not scan directories
listed in for shared libraries.  Merge the result of the scan  of
the  directories given as arguments into the existing hints file.
The default action is to build the hints file afresh.  Lists  the
current  contents of on the standard output.  The hints file will
not be modified.  Do not scan the built‐in system  directory  nor
any  directories listed in for shared libraries.  Do not scan the
built‐in system directory  for  shared  libraries.   (Directories
listed in are still scanned.)  Switch on verbose mode.  A utility
first  appeared  in SunOS 4.0, it appeared in its current form in
Special care must be taken when loading shared libraries into the
address space of programs.  Whenever such a program is run,  will
only load shared libraries from the file.  In particular, the and
is  not used to search for libraries.  Thus, the role of ldconfig
is dual.  In addition to  building  a  set  of  hints  for  quick
lookup,  it  also serves to specify the trusted collection of di‐
rectories from which shared objects can be safely loaded.  It  is
presumed  that  the set of directories specified to is under con‐
trol of the system’s administrator.  further assists  set‐user‐ID
programs by erasing the and from the environment.
