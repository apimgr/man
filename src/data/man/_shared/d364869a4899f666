divides  its  into  fixed‐size  groups  and  runs in turn on each
group.  On each execution of each character sequence of the  form
in  where  is  a digit from 1 to 9, is replaced with the argument
from the current argument group.  The argument group size is  set
to the largest such found.  Any given argument number can be used
arbitrarily  many  times.  (Including zero.)  If no explicit sub‐
stitution sequences are found in the current  argument  group  is
substituted  after  delimited  by  spaces, and the argument group
size defaults to 1 and can be set with the option.  If the  argu‐
ment  group size is set to 0, one argument from is taken for each
execution of anyway, but is discarded and not substituted;  thus,
is run verbatim once for every argument.  The options are as fol‐
lows: Set the argument group size.  Ignored if explicit substitu‐
tions are used.  Change the magic substitution character from the
default  to The following environment variable affects the execu‐
tion of Pathname of the shell to use to execute If this  variable
is not defined, the Bourne shell is used.  Default shell.  Prints
the  name  of  every file in the current directory.  Compares the
‘a’ files to the ‘b’ files.  Runs 5 times.  Hard‐links all  files
in the current directory into the directory with their names pre‐
fixed  with  "joe.".  Diff all C sources in the current directory
against the last checked‐in version and store each result in  its
own output file.  The command appeared in The complete command to
be  executed  on  each iteration is assembled as a string without
additional quoting and then passed to a copy  of  the  shell  for
parsing  and execution.  Thus, commands or arguments that contain
spaces or shell metacharacters may behave in unexpected ways.  To
protect a shell metacharacter fully it must be quoted twice, once
against the current shell and once against the subshell used  for
execution.   Similarly,  for  a  shell metacharacter to be inter‐
preted by the subshell it must be quoted to protect it  from  the
current  shell.   A simple rule of thumb is to enclose the entire
in single quotes so that the current shell does not interpret any
of it.  There is no easy way to produce the literal string in un‐
conditionally inserts "exec" at the beginning of each copy of  so
compound commands may not behave as intended.
