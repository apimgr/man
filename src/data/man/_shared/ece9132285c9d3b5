RANDOM(9)                BSD Kernel Developer's Manual               RANDOM(9)

NNAAMMEE
     aarrcc44rraanndd, aarrcc44rraannddoomm, aarrcc44rraannddoomm__bbuuff, iiss__rraannddoomm__sseeeeddeedd, rraannddoomm,
     rreeaadd__rraannddoomm, rreeaadd__rraannddoomm__uuiioo — supply pseudo-random numbers

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//lliibbkkeerrnn..hh>>

     _u_i_n_t_3_2___t
     aarrcc44rraannddoomm(_v_o_i_d);

     _v_o_i_d
     aarrcc44rraannddoomm__bbuuff(_v_o_i_d _*_p_t_r, _s_i_z_e___t _l_e_n);

     _v_o_i_d
     aarrcc44rraanndd(_v_o_i_d _*_p_t_r, _u___i_n_t _l_e_n_g_t_h, _i_n_t _r_e_s_e_e_d);

     ##iinncclluuddee <<ssyyss//rraannddoomm..hh>>

     _b_o_o_l
     iiss__rraannddoomm__sseeeeddeedd(_v_o_i_d);

     _v_o_i_d
     rreeaadd__rraannddoomm(_v_o_i_d _*_b_u_f_f_e_r, _i_n_t _c_o_u_n_t);

     _i_n_t
     rreeaadd__rraannddoomm__uuiioo(_s_t_r_u_c_t _u_i_o _*_u_i_o, _b_o_o_l _n_o_n_b_l_o_c_k);

   LLEEGGAACCYY RROOUUTTIINNEESS
     ##iinncclluuddee <<ssyyss//lliibbkkeerrnn..hh>>

     _u___l_o_n_g
     rraannddoomm(_v_o_i_d);

DDEESSCCRRIIPPTTIIOONN
     The aarrcc44rraannddoomm() and aarrcc44rraannddoomm__bbuuff() functions will return very good
     quality random numbers, suited for security-related purposes.  Both are
     wrappers around the underlying aarrcc44rraanndd() interface.  aarrcc44rraannddoomm() re‐
     turns a 32-bit random value, while aarrcc44rraannddoomm__bbuuff() fills _p_t_r with _l_e_n
     bytes of random data.

     The aarrcc44rraanndd() CSPRNG is seeded from the random(4) kernel abstract en‐
     tropy device.  Automatic reseeding happens at unspecified time and bytes
     (of output) intervals.  A reseed can be forced by passing a non-zero
     _r_e_s_e_e_d value.

     The rreeaadd__rraannddoomm() function is used to read entropy directly from the ker‐
     nel abstract entropy device.  rreeaadd__rraannddoomm() blocks if and until the en‐
     tropy device is seeded.  The provided _b_u_f_f_e_r is filled with no more than
     _c_o_u_n_t bytes.  It is strongly advised that rreeaadd__rraannddoomm() is not used di‐
     rectly; instead, use the aarrcc44rraanndd() family of functions.

     The iiss__rraannddoomm__sseeeeddeedd() function can be used to check in advance if
     rreeaadd__rraannddoomm() will block.  (If random is seeded, it will not block.)

     The rreeaadd__rraannddoomm__uuiioo() function behaves identically to read(2) on
     _/_d_e_v_/_r_a_n_d_o_m.  The _u_i_o argument points to a buffer where random data
     should be stored.  If _n_o_n_b_l_o_c_k is true and the random device is not
     seeded, this function does not return any data.  Otherwise, this function
     may block interruptibly until the random device is seeded.  If the func‐
     tion is interrupted before the random device is seeded, no data is re‐
     turned.

     The deprecated rraannddoomm() function will return a 31-bit value.  It is obso‐
     lete and scheduled to be removed in FreeBSD 15.0.  Consider prng(9) in‐
     stead and see _S_E_C_U_R_I_T_Y _C_O_N_S_I_D_E_R_A_T_I_O_N_S.

RREETTUURRNN VVAALLUUEESS
     The aarrcc44rraanndd() function uses the Chacha20 algorithm to generate a pseudo-
     random sequence of bytes.  The aarrcc44rraannddoomm() function uses aarrcc44rraanndd() to
     generate pseudo-random numbers in the range from 0 to (2**32)−1.

     The rreeaadd__rraannddoomm() function returns the number of bytes placed in _b_u_f_f_e_r.

     rreeaadd__rraannddoomm__uuiioo() returns zero when successful, otherwise an error code
     is returned.

     rraannddoomm() returns numbers in the range from 0 to (2**31)−1.


EERRRROORRSS
     rreeaadd__rraannddoomm__uuiioo() may fail if:

     [EFAULT]           _u_i_o points to an invalid memory region.

     [EWOULDBLOCK]      The random device is unseeded and _n_o_n_b_l_o_c_k is true.

AAUUTTHHOORRSS
     Dan Moschuk wrote aarrcc44rraannddoomm().
     Mark R V Murray wrote rreeaadd__rraannddoomm().

SSEECCUURRIITTYY CCOONNSSIIDDEERRAATTIIOONNSS
     Do not use rraannddoomm() in new code.

     It is important to remember that the rraannddoomm() function is entirely pre‐
     dictable.  It is easy for attackers to predict future output of rraannddoomm()
     by recording some generated values.  We cannot emphasize strongly enough
     that rraannddoomm() must not be used to generate values that are intended to be
     unpredictable.

BSD                             March 18, 2024                             BSD
