The  utility  prints  a  sequence of numbers, one per line by de‐
fault, from to as near as possible,  in  increments  of  When  is
larger  than  the  default is ‐1.  All numbers are interpreted as
floating point.  Normally integer values are printed  as  decimal
integers.  The utility accepts the following options: Use a style
to  print  each  number.   Only the and conversion characters are
valid, along with any optional flags and an optional numeric min‐
imum field width or precision.  The can contain character  escape
sequences  in backslash notation as defined in The default is Use
to separate numbers.  The can contain character escape  sequences
in  backslash notation as defined in The default is Use to termi‐
nate sequence of numbers.  The can contain character  escape  se‐
quences in backslash notation as defined in The default is Equal‐
ize the widths of all numbers by padding with zeros as necessary.
This  option has no effect with the option.  If any sequence num‐
bers will be printed in exponential notation, the default conver‐
sion is changed to Generate a sequence from 1  to  3  (inclusive)
with  a  default  increment  of 1: $ seq 1 3 1 2 3 Generate a se‐
quence from 3 to 1 (inclusive) with a default increment of ‐1:  $
seq  3 1 3 2 1 Generate a sequence from 0 to 0.1 (inclusive) with
an increment of 0.05 and padding with leading zeroes: $ seq ‐w  0
.05 .1 0.00 0.05 0.10 Generate a sequence from 1 to 3 (inclusive)
with  a  default increment of 1, and a custom separator string: $
seq ‐s "," 1 3 1,2,3 Generate a sequence from 1 to 2  (inclusive)
with  an  increment  of 0.2 and print the results with two digits
after the decimal point (using a style format): $ seq ‐f  %.2f  1
0.2 2 1.00 1.20 1.40 1.60 1.80 2.00 The command first appeared in
Version  8  A  command  appeared in This command was based on the
command of the same name in and the core utilities.  The  command
first  appeared  in the 1.13 shell utilities release.  The option
does not handle the transition from pure floating point to  expo‐
nent  representation  very  well.  The command is not bug for bug
compatible with other implementations.
