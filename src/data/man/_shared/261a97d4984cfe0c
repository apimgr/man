is  a  helper  program  to  allow a user to update his own entry.
does some consistency checks on the and then updates the database
of currently logged in users.  should not be called directly, but
will normally only be called by if the privileges of the  calling
user are not sufficient.  returns 0 on success, and 1 if an error
occurred.  A utility appeared in
