The  command  is used to dump out information for packages, which
may be either packed up in files or already installed on the sys‐
tem with the command.  The may be the name of an installed  pack‐
age  (with  or  without  version), a pattern matching several in‐
stalled packages (see the section for a description  of  possible
patterns), the pathname to a binary package, a filename belonging
to  an installed package (with or a URL to an FTP‐available pack‐
age.  The following command line options are available: Show  in‐
formation  for  all  currently installed packages.  See also When
neither nor is given, the former is assumed.  Show  some  of  the
important  definitions used when building the binary package (the
for each package.   Additionally,  any  installation  information
variables  (lowercase) can be queried, too.  In particular, tells
if a package was installed automatically as a dependency  of  an‐
other  package.   Show  the RCS Id strings from the files used in
the construction of the binary package  (the  for  each  package.
These files are the package Makefile, any patch files, any check‐
sum  files, and the packing list file.  Show the one‐line comment
field for each package.  Show the install‐message file  (if  any)
for each package.  Show the long‐description field for each pack‐
age.  This option allows you to test for the existence of a given
package.   If a package identified by is currently installed, re‐
turn code is 0, otherwise 1.  The name of the best matching pack‐
age found installed is printed to stdout unless turned off  using
the option.  can contain wildcards (see the section below).  This
option  allows  you to test for the existence of a given package.
If a package identified by is currently installed, return code is
0, otherwise 1.  The names of any package(s) found installed  are
printed  to  stdout unless turned off using the option.  can con‐
tain wildcards (see the section below).  Interpret any  given  as
filename,  and query information on the package that file belongs
to.  This can be used to query information on a  per‐file  basis.
See  the  section  below  for more information.  Show the packing
list instructions for each  package.   Print  usage  message  and
exit.  Show the index entry for each package.  This option is as‐
sumed  when  no  arguments or relevant flags are specified.  Show
the install script (if any) for each package.  Override the value
of the configuration option with the value  Show  the  de‐install
script  (if  any)  for  each package.  Show the files within each
package.  This is different from just viewing the  packing  list,
since  full  pathnames  for everything are generated.  Files that
were created dynamically during installation of the  package  are
not  listed.   Prefix each information category header (see shown
with This is primarily of use to front‐end programs that want  to
request a lot of different information fields at once for a pack‐
age,  but  don’t necessary want the output intermingled in such a
way that they can’t organize it.  This lets you add a special to‐
ken to the start of each field.  Show the mtree file (if any) for
each package.  Show which packages each package  was  built  with
(exact  dependencies),  if any.  Show which packages each package
needs (depends upon), if any.  Show the installation  prefix  for
each  package.  Show the definition of from the build information
for each package.  An empty string is returned if no  such  vari‐
able  definition is found for the package(s).  Be in emitting re‐
port headers and such, just dump the raw info (basically,  assume
a  non‐human  reading).  For each package, show the packages that
require it.  For each package, show the packages that require it.
Continue recursively to show all dependents.  Show  the  size  of
this  package  and  all the packages it requires, in bytes.  Show
the size of this package in bytes.  The  size  is  calculated  by
adding up the size of each file of the package.  Show information
for all user‐installed packages: automatically installed packages
(as  dependencies of other packages) are not displayed.  See also
Print version number and exit.  Turn on  verbose  output.   Print
summary  information for each package.  The summary format is de‐
scribed in Its primary use is to contain  all  information  about
the  contents  of  a (remote) binary package repository needed by
package managing software.  Package info is either extracted from
package files named on the command  line,  or  from  already  in‐
stalled  package  information in When the option is used, a file‐
name can be given instead of a package name to query  information
on the (installed) package that file belongs to.  The filename is
resolved to a package name using the package database.  The file‐
name  must  be  absolute, as in the output of For example, can be
used to display the package the given file belongs to, and can be
used to display all files belonging to the package the given file
belongs to.  In the places where a package  name/version  is  ex‐
pected,  e.g., for the option, several forms can be used.  Either
use a package name with or without version, or specify a  package
wildcard that gets matched against all installed packages.  Pack‐
age wildcards use In addition, style {,} alternates have been im‐
plemented.   Package version numbers can also be matched in a re‐
lational manner using the and operators.  For example, will match
versions 1.3 and later of the package.  (Make sure to  use  shell
quoting.)   Additionally, ranges can be defined, by giving both a
lower bound or as well as an upper bound or The lower  bound  has
to  come first.  For example, will match versions 1.3 (inclusive)
to 2.0 (exclusive) of package The collating sequence of the vari‐
ous package version numbers is unusual, but strives to be consis‐
tent.  The magic string equates to and sorts before a  beta  ver‐
sion.   The  magic  string  equates to and sorts before a release
candidate.  The magic string equates to and sorts  before  a  re‐
lease.   The magic string short for is a synonym for For example,
will sort before and they both sort before Similarly,  will  sort
before  and  after  The  magic string equates to and has the same
value as a dot in the dewey‐decimal ordering schemes, as does the
underscore Additionally, alphabetic characters sort in  the  same
place as their numeric counterparts, so that has the same sorting
value  as  See  for options, that can also be specified using the
environment.  most of the work refined it for wildcard dependency
processing, pkgdb, depends  displaying,  pkg  size  display,  and
more.
