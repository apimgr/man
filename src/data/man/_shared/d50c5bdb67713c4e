CTFCONVERT(1)             BSD General Commands Manual            CTFCONVERT(1)

NNAAMMEE
     ccttffccoonnvveerrtt — convert debug data to CTF data

SSYYNNOOPPSSIISS
     ccttffccoonnvveerrtt [--ggiiss] --ll _l_a_b_e_l --LL _l_a_b_e_l_e_n_v [--oo _o_u_t_f_i_l_e] object_file

DDEESSCCRRIIPPTTIIOONN
     The ccttffccoonnvveerrtt utility converts debug information from a binary file to
     CTF (Compact C Type Format) data and replaces the debug section of that
     file with a CTF section called SUNW_ctf.  This new section is added to
     the input file, unless the -o option is present.  You can also opt to
     keep the original debugging section with the -g option.

     The following options are available:

     --ll _l_a_b_e_l
             Sets the label as _l_a_b_e_l.

     --LL _l_a_b_e_l_e_n_v
             Instructs ccttffccoonnvveerrtt to read the label from the environment vari‐
             able _l_a_b_e_l_e_n_v.

     --gg      Don't delete the original debugging section.

     --ii      Ignore object files built from other languages than C.

     --ss      Use the .dynsym ELF section instead of the .symtab ELF section.

     --oo _o_u_t_f_i_l_e
             Write the output to file in _o_u_t_f_i_l_e.

EEXXIITT SSTTAATTUUSS
     The ccttffccoonnvveerrtt utility exits 0 on success, and >0 if an error occurs.

SSEEEE AALLSSOO
     ctfdump(1), ctfmerge(1), ctf(5)

HHIISSTTOORRYY
     The ccttffccoonnvveerrtt utility first appeared in FreeBSD 7.0.

AAUUTTHHOORRSS
     The CTF utilities came from OpenSolaris.

BSD                             April 21, 2022                             BSD
