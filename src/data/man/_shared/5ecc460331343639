curs_terminfo(3X)                Library calls               curs_terminfo(3X)



NNAAMMEE
       ddeell__ccuurrtteerrmm,   mmvvccuurr,   ppuuttpp,   rreessttaarrtttteerrmm,   sseett__ccuurrtteerrmm,  sseettuupptteerrmm,
       ttiiggeettffllaagg,  ttiiggeettnnuumm,  ttiiggeettssttrr,  ttiippaarrmm,  ttiippaarrmm__ss,  ttiissccaann__ss,  ttppaarrmm,
       ttppuuttss,  vviidd__aattttrr,  vviidd__ppuuttss,  vviiddaattttrr,  vviiddppuuttss  - _c_u_r_s_e_s interfaces to
       _t_e_r_m_i_n_f_o database

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ccuurrsseess..hh>>
       ##iinncclluuddee <<tteerrmm..hh>>

       TTEERRMMIINNAALL **ccuurr__tteerrmm;;

       ccoonnsstt cchhaarr ** ccoonnsstt bboooollnnaammeess[[]];;
       ccoonnsstt cchhaarr ** ccoonnsstt bboooollccooddeess[[]];;
       ccoonnsstt cchhaarr ** ccoonnsstt bboooollffnnaammeess[[]];;
       ccoonnsstt cchhaarr ** ccoonnsstt nnuummnnaammeess[[]];;
       ccoonnsstt cchhaarr ** ccoonnsstt nnuummccooddeess[[]];;
       ccoonnsstt cchhaarr ** ccoonnsstt nnuummffnnaammeess[[]];;
       ccoonnsstt cchhaarr ** ccoonnsstt ssttrrnnaammeess[[]];;
       ccoonnsstt cchhaarr ** ccoonnsstt ssttrrccooddeess[[]];;
       ccoonnsstt cchhaarr ** ccoonnsstt ssttrrffnnaammeess[[]];;

       iinntt sseettuupptteerrmm((ccoonnsstt cchhaarr **_t_e_r_m,, iinntt _f_i_l_e_d_e_s,, iinntt **_e_r_r_r_e_t));;
       TTEERRMMIINNAALL **sseett__ccuurrtteerrmm((TTEERRMMIINNAALL **_n_t_e_r_m));;
       iinntt ddeell__ccuurrtteerrmm((TTEERRMMIINNAALL **_o_t_e_r_m));;
       iinntt rreessttaarrtttteerrmm((ccoonnsstt cchhaarr **_t_e_r_m,, iinntt _f_i_l_e_d_e_s,, iinntt **_e_r_r_r_e_t));;

       cchhaarr **ttppaarrmm((ccoonnsstt cchhaarr **_s_t_r,, ...));;
            _/_* _o_r _*_/
       cchhaarr **ttppaarrmm((ccoonnsstt cchhaarr **_s_t_r,, lloonngg _p_1 ... lloonngg _p_9));;

       iinntt ttppuuttss((ccoonnsstt cchhaarr **_s_t_r,, iinntt _a_f_f_c_n_t,, iinntt ((**_p_u_t_c))((iinntt))));;
       iinntt ppuuttpp((ccoonnsstt cchhaarr **_s_t_r));;

       iinntt vviiddppuuttss((cchhttyyppee _a_t_t_r_s,, iinntt ((**_p_u_t_c))((iinntt))));;
       iinntt vviiddaattttrr((cchhttyyppee _a_t_t_r_s));;
       iinntt vviidd__ppuuttss((aattttrr__tt _a_t_t_r_s,, sshhoorrtt _p_a_i_r,, vvooiidd **_o_p_t_s,, iinntt ((**_p_u_t_c))((iinntt))));;
       iinntt vviidd__aattttrr((aattttrr__tt _a_t_t_r_s,, sshhoorrtt _p_a_i_r,, vvooiidd **_o_p_t_s));;

       iinntt mmvvccuurr((iinntt _o_l_d_r_o_w,, iinntt _o_l_d_c_o_l,, iinntt _n_e_w_r_o_w,, iinntt _n_e_w_c_o_l));;

       iinntt ttiiggeettffllaagg((ccoonnsstt cchhaarr **_c_a_p_-_c_o_d_e));;
       iinntt ttiiggeettnnuumm((ccoonnsstt cchhaarr **_c_a_p_-_c_o_d_e));;
       cchhaarr **ttiiggeettssttrr((ccoonnsstt cchhaarr **_c_a_p_-_c_o_d_e));;

       cchhaarr **ttiippaarrmm((ccoonnsstt cchhaarr **_s_t_r,, ...));;

       _/_* _e_x_t_e_n_s_i_o_n_s _*_/
       cchhaarr **ttiippaarrmm__ss((iinntt _e_x_p_e_c_t_e_d,, iinntt _m_a_s_k,, ccoonnsstt cchhaarr **_s_t_r,, ......));;
       iinntt ttiissccaann__ss((iinntt **_e_x_p_e_c_t_e_d,, iinntt **_m_a_s_k,, ccoonnsstt cchhaarr **_s_t_r));;

       _/_* _d_e_p_r_e_c_a_t_e_d _*_/
       iinntt sseetttteerrmm((ccoonnsstt cchhaarr **_t_e_r_m));;

DDEESSCCRRIIPPTTIIOONN
       These low-level functions must be called by programs that deal directly
       with  the  _t_e_r_m_i_n_f_o  database  to handle certain terminal capabilities,
       such as programming function keys.  For all other functionality, _c_u_r_s_e_s
       functions are more suitable and their use is recommended.

       None  of  these  functions  use  (or  are aware of) multibyte character
       strings such as UTF-8.

       •   Capability names and codes use the POSIX portable character set.

       •   Capability string values have  no  associated  encoding;  they  are
           strings of 8-bit characters.

   IInniittiiaalliizzaattiioonn
       Initially, sseettuupptteerrmm should be called.  The high-level _c_u_r_s_e_s functions
       iinniittssccrr and nneewwtteerrmm call sseettuupptteerrmm to initialize the low-level  set  of
       terminal-dependent variables listed in tteerrmm__vvaarriiaabblleess(3X).

       Applications  can  use  the  terminal capabilities either directly (via
       header  definitions),  or  by  special  functions.   The  header  files
       _c_u_r_s_e_s_._h and _t_e_r_m_._h should be included (in that order) to get the defi‐
       nitions for these strings, numbers, and flags.

       The _t_e_r_m_i_n_f_o variables lliinneess and ccoolluummnnss are initialized  by  sseettuupptteerrmm
       as follows.

       •   If  uussee__eennvv((FFAALLSSEE))  has  been  called, values for lliinneess and ccoolluummnnss
           specified in _t_e_r_m_i_n_f_o are used.

       •   Otherwise, if the environment variables _L_I_N_E_S  and  _C_O_L_U_M_N_S  exist,
           their values are used.  If these environment variables do not exist
           and the program is running in a window, the current window size  is
           used.   Otherwise,  if  the environment variables do not exist, the
           values for lliinneess and ccoolluummnnss specified in the _t_e_r_m_i_n_f_o database are
           used.

       Parameterized  strings  should  be  passed through ttppaarrmm to instantiate
       them.  All _t_e_r_m_i_n_f_o strings (including the output of ttppaarrmm)  should  be
       sent  to the terminal device with ttppuuttss or ppuuttpp.  Call rreesseett__sshheellll__mmooddee
       to restore the terminal modes before exiting; see ccuurrss__kkeerrnneell(3X).

       Programs that use cursor addressing should

       •   output eenntteerr__ccaa__mmooddee upon startup and

       •   output eexxiitt__ccaa__mmooddee before exiting.

       Programs that execute shell subprocesses should

       •   call rreesseett__sshheellll__mmooddee and output eexxiitt__ccaa__mmooddee before the  shell  is
           called and

       •   output  eenntteerr__ccaa__mmooddee and call rreesseett__pprroogg__mmooddee after returning from
           the shell.

       sseettuupptteerrmm reads in the _t_e_r_m_i_n_f_o  database,  initializing  the  _t_e_r_m_i_n_f_o
       structures,  but  does  not set up the output virtualization structures
       used by _c_u_r_s_e_s.  Its parameters follow.

          _t_e_r_m is the terminal type, a character string.  If _t_e_r_m is null, the
               environment variable _T_E_R_M is read.

          _f_i_l_e_d_e_s
               is  the  file  descriptor used for getting and setting terminal
               I/O modes.

               Higher-level applications use  nneewwtteerrmm(3X)  to  initialize  the
               terminal,  passing  an  output _s_t_r_e_a_m rather than a _d_e_s_c_r_i_p_t_o_r.
               In  _c_u_r_s_e_s,  the  two  are  the  same  because  nneewwtteerrmm   calls
               sseettuupptteerrmm,  passing the file descriptor derived from its output
               stream parameter.

          _e_r_r_r_e_t
               points to an optional location where an error status can be re‐
               turned  to  the  caller.  If _e_r_r_r_e_t is not null, then sseettuupptteerrmm
               returns OOKK or EERRRR and stores a  status  value  in  the  integer
               pointed  to by _e_r_r_r_e_t.  A return value of OOKK combined with sta‐
               tus of 11 in _e_r_r_r_e_t is normal.

               If EERRRR is returned, examine _e_r_r_r_e_t_:

               11    means that the terminal is hardcopy, and  cannot  be  used
                    for _c_u_r_s_e_s applications.

                    sseettuupptteerrmm  determines  if  the entry is a hardcopy type by
                    checking the hhaarrddccooppyy (hhcc) capability.

               00    means that the terminal could not be found, or that it  is
                    a  generic  type, having too little information for _c_u_r_s_e_s
                    applications to run.

                    sseettuupptteerrmm determines if the entry is  a  generic  type  by
                    checking the ggeenneerriicc__ttyyppee (ggnn) capability.

               --11   means that the _t_e_r_m_i_n_f_o database could not be found.

               If  _e_r_r_r_e_t  is  null,  sseettuupptteerrmm  reports an error message upon
               finding an error and exits.  Thus, the simplest call is:

                      setupterm((char *)0, 1, (int *)0);

               which uses all the defaults and sends the output to ssttddoouutt.

   TThhee TTeerrmmiinnaall SSttaattee
       sseettuupptteerrmm stores its information  about  the  terminal  in  a  _T_E_R_M_I_N_A_L
       structure pointed to by the global variable ccuurr__tteerrmm.  If it detects an
       error,  or  decides  that  the  terminal  is  unsuitable  (hardcopy  or
       generic),  it discards this information, making it not available to ap‐
       plications.

       If sseettuupptteerrmm is called repeatedly for the same terminal type,  it  will
       reuse  the  information.   It maintains only one copy of a given termi‐
       nal's capabilities in memory.  If it is called for  different  terminal
       types,  sseettuupptteerrmm  allocates new storage for each set of terminal capa‐
       bilities.

       sseett__ccuurrtteerrmm sets ccuurr__tteerrmm to _n_t_e_r_m, and makes all of the _t_e_r_m_i_n_f_o Bool‐
       ean,  numeric,  and string variables use the values from _n_t_e_r_m.  It re‐
       turns the old value of ccuurr__tteerrmm.

       ddeell__ccuurrtteerrmm frees the space pointed to by _o_t_e_r_m and makes it  available
       for  further  use.  If _o_t_e_r_m is the same as ccuurr__tteerrmm, references to any
       of the _t_e_r_m_i_n_f_o Boolean, numeric, and string variables  thereafter  may
       refer  to  invalid  memory  locations  until another sseettuupptteerrmm has been
       called.

       rreessttaarrtttteerrmm is similar to sseettuupptteerrmm and  iinniittssccrr,  except  that  it  is
       called  after  restoring  memory to a previous state (for example, when
       reloading a game saved as a core image dump).  rreessttaarrtttteerrmm assumes that
       the  windows and the input and output options are the same as when mem‐
       ory was saved, but the terminal type and baud rate  may  be  different.
       Accordingly,  rreessttaarrtttteerrmm  saves  various  terminal  state  bits, calls
       sseettuupptteerrmm, and then restores the bits.

   FFoorrmmaattttiinngg OOuuttppuutt
       ttppaarrmm instantiates the string _s_t_r with parameters _p_i.  A pointer is re‐
       turned  to  the result of _s_t_r with the parameters applied.  Application
       developers should keep in mind these quirks of the interface:

       •   Although ttppaarrmm's actual parameters may be integers or strings,  the
           prototype expects _l_o_n_g (integer) values.

       •   Aside from the sseett__aattttrriibbuutteess (ssggrr) capability, most terminal capa‐
           bilities require no more than one or two parameters.

       •   Padding information is ignored  by  ttppaarrmm;  it  is  interpreted  by
           ttppuuttss.

       •   The  capability  string  is  null-terminated.   Use “\200” where an
           ASCII NUL is needed in the output.

       ttiippaarrmm is a newer form of ttppaarrmm  which  uses  _s_t_d_a_r_g_._h  rather  than  a
       fixed-parameter  list.   Its  numeric  parameters  are _i_n_ts rather than
       _l_o_n_gs.

       Both ttppaarrmm and ttiippaarrmm assume that  the  application  passes  parameters
       consistent  with the terminal description.  Two extensions are provided
       as alternatives to deal with untrusted data.

       •   ttiippaarrmm__ss is an extension which is a safer formatting function  than
           ttppaarrmm or ttiippaarrmm, because it allows the developer to tell the _c_u_r_s_e_s
           library how many parameters to expect in the  parameter  list,  and
           which may be string parameters.

           The  _m_a_s_k  parameter has one bit set for each of the parameters (up
           to 9) passed as _c_h_a_r pointers rather than numbers.

       •   The extension ttiissccaann__ss allows the application to inspect a  format‐
           ting capability to see what the _c_u_r_s_e_s library would assume.

   OOuuttppuutt FFuunnccttiioonnss
       String  capabilities can contain padding information, a time delay (ac‐
       commodating performance limitations of hardware terminals) expressed as
       $$<<_n>>,  where  _n  is a nonnegative integral count of milliseconds.  If _n
       exceeds 30,000 (thirty seconds), it is capped at that value.

       ttppuuttss interprets time-delay information in the string _s_t_r  and  outputs
       it, executing the delays:

       •   The  _s_t_r parameter must be a _t_e_r_m_i_n_f_o string variable or the return
           value of ttppaarrmm, ttiippaarrmm, ttggeettssttrr, or ttggoottoo.

           The ttggeettssttrr and ttggoottoo functions are part of the _t_e_r_m_c_a_p  interface,
           which happens to share these function names with the _t_e_r_m_i_n_f_o API.

       •   _a_f_f_c_n_t is the number of lines affected, or 11 if not applicable.

       •   _p_u_t_c is a _p_u_t_c_h_a_r-like function to which the characters are passed,
           one at a time.

           If ttppuuttss processes a time-delay, it uses the ddeellaayy__oouuttppuutt(3X) func‐
           tion,  routing  any resulting padding characters through this func‐
           tion.

       ppuuttpp calls “ttppuuttss((_s_t_r,, 11,, ppuuttcchhaarr))”.  The output of ppuuttpp always goes to
       ssttddoouutt, rather than the _f_i_l_e_d_e_s specified in sseettuupptteerrmm.

       vviiddppuuttss displays the string on the terminal in the video attribute mode
       _a_t_t_r_s, which is any combination of the attributes listed in ccuurrsseess(3X).
       The characters are passed to the _p_u_t_c_h_a_r-like function _p_u_t_c.

       vviiddaattttrr is like vviiddppuuttss, except that it outputs through _p_u_t_c_h_a_r(3).

       vviidd__aattttrr  and vviidd__ppuuttss correspond to vviiddaattttrr and vviiddppuuttss, respectively.
       They use multiple parameters to represent the character attributes  and
       color; namely,

       •   _a_t_t_r_s, of type _a_t_t_r___t, for the attributes and

       •   _p_a_i_r, of type _s_h_o_r_t, for the color pair number.

       Use  the  attribute  constants  prefixed  with  “WWAA__” with vviidd__aattttrr and
       vviidd__ppuuttss.

       X/Open Curses reserves the _o_p_t_s argument for future  use,  saying  that
       applications  must  provide  a  null pointer for that argument; but see
       section “EXTENSIONS” below.

       mmvvccuurr provides low-level cursor motion.  It  takes  effect  immediately
       (rather  than  at the next refresh).  Unlike the other low-level output
       functions, which either write to the standard output or pass an  output
       function  parameter,  mmvvccuurr uses an output file descriptor derived from
       the output stream parameter of nneewwtteerrmm(3X).

       While ppuuttpp and mmvvccuurr are low-level functions that do not use high-level
       _c_u_r_s_e_s  state,  _n_c_u_r_s_e_s  declares them in _c_u_r_s_e_s_._h because System V did
       this (see section “HISTORY” below).

   TTeerrmmiinnaall CCaappaabbiilliittyy FFuunnccttiioonnss
       ttiiggeettffllaagg, ttiiggeettnnuumm, and ttiiggeettssttrr return the value  of  the  capability
       corresponding  to  the _t_e_r_m_i_n_f_o _c_a_p_-_c_o_d_e, such as xxeennll, passed to them.
       The _c_a_p_-_c_o_d_e for each capability is given in the table column  entitled
       _c_a_p_-_c_o_d_e code in the capabilities section of tteerrmmiinnffoo(5).

       These functions return special values to denote errors.

       ttiiggeettffllaagg returns

       --11     if _c_a_p_-_c_o_d_e is not a Boolean capability, or

       00      if it is canceled or absent from the terminal description.

       ttiiggeettnnuumm returns

       --22     if _c_a_p_-_c_o_d_e is not a numeric capability, or

       --11     if it is canceled or absent from the terminal description.

       ttiiggeettssttrr returns

       ((cchhaarr **))--11
              if _c_a_p_-_c_o_d_e is not a string capability, or

       00      if it is canceled or absent from the terminal description.

   TTeerrmmiinnaall CCaappaabbiilliittyy NNaammeess
       These null-terminated arrays contain

       •   the short _t_e_r_m_i_n_f_o names (“codes”),

       •   the _t_e_r_m_c_a_p names (“names”), and

       •   the long _t_e_r_m_i_n_f_o names (“fnames”)

       for each of the predefined _t_e_r_m_i_n_f_o variables:

              ccoonnsstt cchhaarr **bboooollnnaammeess[[]], **bboooollccooddeess[[]], **bboooollffnnaammeess[[]]
              ccoonnsstt cchhaarr **nnuummnnaammeess[[]], **nnuummccooddeess[[]], **nnuummffnnaammeess[[]]
              ccoonnsstt cchhaarr **ssttrrnnaammeess[[]], **ssttrrccooddeess[[]], **ssttrrffnnaammeess[[]]

   RReelleeaassiinngg MMeemmoorryy
       Each successful call to sseettuupptteerrmm allocates memory to hold the terminal
       description.  As a side effect, it sets ccuurr__tteerrmm to point to this  mem‐
       ory.  If an application calls

              del_curterm(cur_term);

       the memory will be freed.

       The  formatting functions ttppaarrmm and ttiippaarrmm extend the storage allocated
       by sseettuupptteerrmm as follows.

       •   They add the “static” _t_e_r_m_i_n_f_o  variables  [a-z].   Before  _n_c_u_r_s_e_s
           6.3, those were shared by all screens.  With _n_c_u_r_s_e_s 6.3, those are
           allocated per screen.  See tteerrmmiinnffoo(5).

       •   To improve performance, _n_c_u_r_s_e_s 6.3 caches the result of  analyzing
           _t_e_r_m_i_n_f_o  strings  for  their parameter types.  That is stored as a
           binary tree referenced from the _T_E_R_M_I_N_A_L structure.

       The higher-level iinniittssccrr and nneewwtteerrmm functions use sseettuupptteerrmm.  Normally
       they  do  not free this memory, but it is possible to do that using the
       ddeellssccrreeeenn(3X) function.

RREETTUURRNN VVAALLUUEE
       X/Open Curses defines no failure conditions.  In _n_c_u_r_s_e_s,

       ddeell__ccuurrtteemm
            fails if its terminal parameter is null.

       ppuuttpp calls ttppuuttss, returning the same error codes.

       rreessttaarrtttteerrmm
            fails if the associated call to sseettuupptteerrmm returns an error.

       sseettuupptteerrmm
            fails if it cannot allocate enough memory, or create  the  initial
            windows  (ssttddssccrr,  ccuurrssccrr,  and nneewwssccrr) Other error conditions are
            documented above.

       ttppaarrmm
            returns a null pointer if the capability would require  unexpected
            parameters;  that  is,  too  many,  too  few,  or  incorrect types
            (strings where integers are expected, or vice versa).

       ttppuuttss
            fails if the string parameter is null.  It does not detect I/O er‐
            rors:  X/Open Curses states that ttppuuttss ignores the return value of
            the output function _p_u_t_c.

NNOOTTEESS
       The vviidd__aattttrr function in _n_c_u_r_s_e_s is a special case.  It was  originally
       implemented based on a draft of X/Open Curses, as a macro, before other
       parts of the _n_c_u_r_s_e_s wide-character API were developed, and unlike  the
       other  wide-character functions, is also provided in the non-wide-char‐
       acter configuration.

EEXXTTEENNSSIIOONNSS
       The functions marked as extensions were designed for _n_c_u_r_s_e_s,  and  are
       not  found  in SVr4 _c_u_r_s_e_s, 4.4BSD _c_u_r_s_e_s, or any other previous _c_u_r_s_e_s
       implementation.

       _n_c_u_r_s_e_s allows _o_p_t_s to be a pointer to _i_n_t, which  overrides  the  _p_a_i_r
       (_s_h_o_r_t) argument.

PPOORRTTAABBIILLIITTYY
       sseetttteerrmm is not described by X/Open and must be considered non-portable.
       All other functions are as described by X/Open.

   CCoommppaattiibbiilliittyy MMaaccrrooss
       This implementation provides a few macros for compatibility  with  sys‐
       tems  before SVr4 (see section “HISTORY” below).  They include BBccrrmmooddee,
       BBffiixxtteerrmm, BBggeettttmmooddee, BBnnooccrrmmooddee, BBrreesseetttteerrmm, BBssaavveetteerrmm, and BBsseetttteerrmm.

       In SVr4, these are found in _c_u_r_s_e_s_._h, but except for sseetttteerrmm, are like‐
       wise  macros.   The  one  function, sseetttteerrmm, is mentioned in the manual
       page.  It further notes that sseetttteerrmm was replaced by sseettuupptteerrmm, stating
       that the call
              setupterm(_t_e_r_m, 1, (int *)0)
       provides the same functionality as sseetttteerrmm((_t_e_r_m)), discouraging the lat‐
       ter for new programs.  _n_c_u_r_s_e_s implements  each  of  these  symbols  as
       macros for BSD _c_u_r_s_e_s compatibility.

   LLeeggaaccyy DDaattaa
       sseettuupptteerrmm  copies  the terminal name to the array ttttyyttyyppee.  This is not
       part of X/Open Curses, but is assumed by some applications.

       Other implementions may not declare the capability name  arrays.   Some
       provide  them  without  declaring them.  X/Open Curses does not specify
       them.

       Extended terminal capability names, as defined by  “ttiicc  --xx”,  are  not
       stored in the arrays described here.

   OOuuttppuutt BBuuffffeerriinngg
       Older  versions  of  _n_c_u_r_s_e_s assumed that the file descriptor passed to
       sseettuupptteerrmm from iinniittssccrr or nneewwtteerrmm uses buffered I/O, and would write to
       the  corresponding stream.  In addition to the limitation that the ter‐
       minal was left in block-buffered mode on exit (like  System V  _c_u_r_s_e_s),
       it  was  problematic  because  _n_c_u_r_s_e_s  did not allow a reliable way to
       clean up on receiving SSIIGGTTSSTTPP.

       The current version (ncurses6) uses output buffers managed directly  by
       _n_c_u_r_s_e_s.  Some of the low-level functions described in this manual page
       write to the standard output.  They are  not  signal-safe.   The  high-
       level functions in _n_c_u_r_s_e_s employ alternate versions of these functions
       using the more reliable buffering scheme.

   FFuunnccttiioonn PPrroottoottyyppeess
       The X/Open Curses prototypes are based on the SVr4 _c_u_r_s_e_s header decla‐
       rations,  which  were defined at the same time the C language was first
       standardized in the late 1980s.

       •   X/Open Curses uses _c_o_n_s_t  less  effectively  than  a  later  design
           might,  sometimes applying it needlessly to values that are already
           constant, and in most cases overlooking  parameters  that  normally
           would  use  _c_o_n_s_t.  Passing _c_o_n_s_t-qualified parameters to functions
           that do not declare them _c_o_n_s_t may prevent the program from compil‐
           ing.  On the other hand, “writable strings” are an obsolescent fea‐
           ture.

           As an extension, this implementation can be  configured  to  change
           the  function prototypes to use the _c_o_n_s_t keyword.  The _n_c_u_r_s_e_s ABI
           6 enables this feature by default.

       •   X/Open Curses prototypes ttppaarrmm with a fixed number  of  parameters,
           rather than a variable argument list.

           This  implementation uses a variable argument list, but can be con‐
           figured to use the  fixed-parameter  list.   Portable  applications
           should  provide  nine  parameters after the format; zeroes are fine
           for this purpose.

           In response to review comments by Thomas E. Dickey,  X/Open  Curses
           Issue 7 proposed the ttiippaarrmm function in mid-2009.

           While  ttiippaarrmm is always provided in _n_c_u_r_s_e_s, the older form is only
           available as a build-time configuration option.  If  not  specially
           configured, ttppaarrmm is the same as ttiippaarrmm.

       Both forms of ttppaarrmm have drawbacks:

       •   Most of the calls to ttppaarrmm use only one or two parameters.  Passing
           nine on each call is awkward.

           Using _l_o_n_g for the numeric parameter type is a workaround  to  make
           the  parameter use the same amount of stack as a pointer.  That ap‐
           proach dates back to the  mid-1980s,  before  C  was  standardized.
           Since  then,  there is a standard (and pointers are not required to
           fit in a _l_o_n_g).

       •   Providing the right number of parameters for  a  variadic  function
           such  as  ttiippaarrmm can be a problem, in particular for string parame‐
           ters.  However, only a few _t_e_r_m_i_n_f_o capabilities use string parame‐
           ters (for instance, the ones used for programmable function keys).

           The _n_c_u_r_s_e_s library checks usage of these capabilities, and returns
           an error if the capability mishandles string  parameters.   But  it
           cannot  check  if  a  calling program provides strings in the right
           places for the ttppaarrmm calls.

           The ttppuutt(1) program checks its use of these capabilities with a ta‐
           ble, so that it calls ttppaarrmm correctly.

   SSppeecciiaall _T_E_R_M ttrreeaattmmeenntt
       If configured to use the terminal driver, as with the MinGW port,

       •   sseettuupptteerrmm  interprets  a missing/empty _T_E_R_M variable as the special
           value “unknown”.

           SVr4 _c_u_r_s_e_s uses the special value “dumb”.

           The difference  between  the  two  is  that  the  former  uses  the
           ggeenneerriicc__ttyyppee  (ggnn)  _t_e_r_m_i_n_f_o capability, while the latter does not.
           A generic terminal is unsuitable for full-screen applications.

       •   sseettuupptteerrmm allows explicit use of the the windows console driver  by
           checking  if $$TTEERRMM is set to “#win32con” or an abbreviation of that
           string.

   OOtthheerr PPoorrttaabbiilliittyy IIssssuueess
       In SVr4, sseett__ccuurrtteerrmm returns an _i_n_t, OOKK or EERRRR.  We have chosen to  im‐
       plement the X/Open Curses semantics.

       In SVr4, the third argument of ttppuuttss has the type “iinntt ((**ppuuttcc))((cchhaarr))”.

       At  least one implementation of X/Open Curses (Solaris) returns a value
       other than OOKK or EERRRR from ttppuuttss.  It instead returns the length of  the
       string, and does no error checking.

       X/Open  Curses notes that after calling mmvvccuurr, the _c_u_r_s_e_s state may not
       match the actual terminal state, and that an application  should  touch
       and  refresh  the  window  before  resuming  normal _c_u_r_s_e_s calls.  Both
       _n_c_u_r_s_e_s and SVr4 _c_u_r_s_e_s implement mmvvccuurr using the _S_C_R_E_E_N data allocated
       in either iinniittssccrr or nneewwtteerrmm.  So though it is documented as a _t_e_r_m_i_n_f_o
       function, mmvvccuurr is really a _c_u_r_s_e_s function that is not well specified.

       X/Open Curses states that the old location must be given for  mmvvccuurr  to
       accommodate  terminals  that lack absolute cursor positioning.  _n_c_u_r_s_e_s
       allows the caller to use -1 for either or both old coordinates.  The -1
       tells  _n_c_u_r_s_e_s  that  the old location is unknown, and that it must use
       only absolute motion, as  with  the  ccuurrssoorr__aaddddrreessss  (ccuupp)  capability,
       rather  than  the least costly combination of absolute and relative mo‐
       tion.

HHIISSTTOORRYY
       SVr2 (1984) introduced the _t_e_r_m_i_n_f_o feature.   Its  programming  manual
       mentioned the following low-level functions.

       lB lB lB lx.  Function  Description _ fixterm   restore terminal to “in
       _c_u_r_s_e_s” state gettmode  establish current terminal modes  mvcur     low
       level  cursor  motion putp use ttppuuttss to send characters via _p_u_t_c_h_a_r re‐
       setterm set terminal modes to “out  of  _c_u_r_s_e_s”  state  resetty   reset
       terminal  flags  to  stored  value  saveterm  save current modes as “in
       _c_u_r_s_e_s” state savetty   store current terminal  flags  setterm   estab‐
       lish  terminal  with given type setupterm establish terminal with given
       type   tparm     interpolate   parameters   into   string    capability
       tputs     apply padding information to a string vidattr   like vviiddppuuttss,
       but output through _p_u_t_c_h_a_r vidputs   T{ write string to  terminal,  ap‐
       plying specified attributes T}

       The  programming  manual  also mentioned functions provided for _t_e_r_m_c_a_p
       compatibility (commenting that they “may go away at a later date”).

       lB lB lB lx.  Function  Description _ tgetent   look up  _t_e_r_m_c_a_p  entry
       for  given  _n_a_m_e tgetflag  get Boolean entry for given _i_d tgetnum   get
       numeric entry for given _i_d tgetstr   get  string  entry  for  given  _i_d
       tgoto     apply parameters to given capability tputs     T{ write char‐
       acters via a function parameter, applying padding T}

       Early _t_e_r_m_i_n_f_o programs obtained capability values  from  the  _T_E_R_M_I_N_A_L
       structure initialized by sseettuupptteerrmm.

       SVr3  (1987) extended _t_e_r_m_i_n_f_o by adding functions to retrieve capabil‐
       ity values (like the _t_e_r_m_c_a_p interface), and reusing ttggoottoo and ttppuuttss.

       lB lB lB lx.  Function  Description _ tigetflag get Boolean  entry  for
       given  _i_d tigetnum  get numeric entry for given _i_d tigetstr  get string
       entry for given _i_d

       SVr3 also replaced several of the SVr2 _t_e_r_m_i_n_f_o functions that  had  no
       counterpart in the _t_e_r_m_c_a_p interface, documenting them as obsolete.

       lB  lB  l   lx.  Function  Replaced by _ crmode    cbreak fixterm   re‐
       set_prog_mode    gettmode  _n_/_a     nocrmode  nocbreak     resetterm re‐
       set_shell_mode saveterm  def_prog_mode setterm   setupterm

       SVr3  kept  the mmvvccuurr, vviiddaattttrr, and vviiddppuuttss functions, along with ppuuttpp,
       ttppaarrmm, and ttppuuttss.  The latter were needed to support  padding,  and  to
       handle  capabilities  accessed by functions such as vviiddaattttrr (which used
       more than the two parameters supported by ttggoottoo).

       SVr3 introduced the functions for switching between  terminal  descrip‐
       tions;  for  example,  sseett__ccuurrtteerrmm.  Some changes reflected incremental
       improvements to the SVr2 library.

       •   The _T_E_R_M_I_N_A_L type definition was introduced  in  SVr3.01,  for  the
           _t_e_r_m structure provided in SVr2.

       •   Various  global  variables  such as bboooollnnaammeess were mentioned in the
           programming manual at this point, though  the  variables  had  been
           provided in SVr2.

       SVr4 (1989) added the vviidd__aattttrr and vviidd__ppuuttss functions.

       Other  low-level  functions  are declared in the _c_u_r_s_e_s header files of
       Unix systems, but none are documented.  Those noted  as  “obsolete”  by
       SVr3 remained in use by System V's _v_i(1) editor.

SSEEEE AALLSSOO
       ccuurrsseess(3X),   ccuurrss__iinniittssccrr(3X),   ccuurrss__kkeerrnneell(3X),   ccuurrss__mmeemmlleeaakkss(3X),
       ccuurrss__tteerrmmccaapp(3X),  ccuurrss__vvaarriiaabblleess(3X),   ppuuttcc(3),   tteerrmm__vvaarriiaabblleess(3X),
       tteerrmmiinnffoo(5)



ncurses 6.5                       2024-04-13                 curs_terminfo(3X)
