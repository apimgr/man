is a simple protocol based on RFC 1288 that provides an interface
to the program at several network sites.  The program is supposed
to  return a friendly, human‐oriented status report on either the
system at the moment or a particular person in depth.   There  is
no required format and the protocol consists mostly of specifying
a  single  is  started  by which listens for requests at port 79.
Once handed a connection, reads a single command line  terminated
by  a  which  it then passes to closes its connections as soon as
the output is finished.  If the line is null  (i.e.,  just  a  is
sent) then returns a report that lists all people logged into the
system  at  that moment.  If a user name is specified (e.g., then
the response lists more extended information for only  that  par‐
ticular user, whether logged in or not.  Allowable in the command
line  include  both  and If a name is ambiguous, all possible de‐
rivations are returned.  The following options may be  passed  to
as  server program arguments in Enable 8‐bit output.  Do not show
any gecos information besides the users’ real names.  Display the
name of the remote host in short mode, instead of the office  lo‐
cation  and  office phone.  Enable logging.  The name of the host
originating the query, and the actual request is reported via  at
LOG_NOTICE  priority.   A request of the form or will return long
output.  Empty requests  will  return  all  currently  logged  in
users.  All other requests look for specific users.  See RFC 1288
for  details.   Prevent  matching  of  names.  is usually a login
name; however, matching will also be  done  on  the  users’  real
names,  unless  the option is supplied.  Use an alternate program
as the local information provider.  The default local program ex‐
ecuted by is By specifying a customized local server, this option
allows a system manager to have more control over  what  informa‐
tion  is  provided to remote sites.  Prevents from displaying the
contents of the and files.   Prints  user  information  in  short
mode,  one  line per user.  This overrides the that may be passed
in from the remote client.   Disable  forwarding  of  queries  to
other  remote  hosts.   Queries without a user name are rejected.
The command appeared in Connecting directly to the server from  a
or  an  equally narrow‐minded user program can result in meaning‐
less attempts at option negotiation being  sent  to  the  server,
which  will  foul  up the command line interpretation.  should be
taught to filter out and perhaps even respond negatively  to  all
option commands received.
