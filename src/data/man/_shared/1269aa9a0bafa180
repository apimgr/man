and  read  commands from standard input or a specified file which
are to be executed at a later time, using Executes commands at  a
specified  time.   Lists the user’s pending jobs, unless the user
is the superuser.  In that case,  everybody’s  jobs  are  listed.
Deletes  jobs.  Executes commands when system load levels permit.
In other words, when the load average drops  below  1.5,  or  the
value  specified in the invocation of allows some moderately com‐
plex specifications.  It accepts times of the form or  to  run  a
job  at  a  specific time of day.  (If that time is already past,
the next day is assumed.)  You may also specify or (4pm) and  you
can  have a time‐of‐day suffixed with or for running in the morn‐
ing or the evening.  You can also say what day the  job  will  be
run,  by  giving  a date in the form with an optional or giving a
date of the form or or The specification of a  date  must  follow
the  specification  of  the time of day.  You can also give times
like or where the time‐units can be or and you can  tell  to  run
the  job  today by suffixing the time with and to run the job to‐
morrow by suffixing the time with For example, to run  a  job  at
4pm  three days from now, you would do to run a job at 10:00am on
July 31, you would do and to run a job at 1am tomorrow, you would
do Alternatively the time may be specified in a  language‐neutral
fashion  by  using  the  options.  For both and commands are read
from standard input or the file specified with the option and ex‐
ecuted.  The working directory, the environment (except  for  the
variables  and  and the are retained from the time of invocation.
An or command invoked  from  a  shell  will  retain  the  current
userid.  The user will be mailed standard error and standard out‐
put  from his commands, if any.  Mail will be sent using the com‐
mand If is executed from a shell, the owner of  the  login  shell
will  receive  the mail.  The superuser may use these commands in
any case.  For other users, permission to use  is  determined  by
the  files and If the file exists, only usernames mentioned in it
are allowed to use If does not exist, is checked; every  username
not  mentioned  in  it  is then allowed to use If neither exists,
only the superuser is allowed use of An empty  means  that  every
user is allowed use these commands.  This is the default configu‐
ration.  Is an alias for Cats the jobs listed on the command line
to  standard  output.   Is an alias for Reads the job from rather
than standard input.  Is an alias for Send mail to the user  when
the  job  has  completed  even  if there was no output.  Uses the
specified queue.  A queue designation consists of a  single  let‐
ter.   Valid queue designations range from to and to The queue is
the default for and the queue for Queues with higher letters  run
with increased niceness.  If a job is submitted to a queue desig‐
nated  with  an uppercase letter, it is treated as if it had been
submitted to batch at that time.  If is given a  specific  queue,
it  will  only  show jobs pending in that queue.  Is an alias for
For both and the time may be specified in a language‐neutral for‐
mat consisting of: The first two digits of  the  year  (the  cen‐
tury).   The second two digits of the year.  If is specified, but
is not, a value for between 69 and 99 results in a value  of  19.
Otherwise, a value of 20 is used.  The month of the year, from 01
to  12.   The  day  of the month, from 01 to 31.  The hour of the
day, from 00 to 23.  The minute of the hour, from 00 to 59.   The
second  of  the minute, from 00 to 61.  Prints the version number
to standard error.  For shows completed but not yet deleted  jobs
in the queue.  Otherwise shows the time the job will be executed.
Directory  containing job files Directory containing output spool
files Login records Allow permission control Deny permission con‐
trol Job‐creation lock file.  The and utilities  conform  to  was
mostly written by The time parsing routines are implemented by If
the  file  is  not  available or corrupted, or if the user is not
logged on at the time is invoked, the mail is sent to the  userid
found  in the environment variable If that is undefined or empty,
the current userid is assumed.  and as presently implemented  are
not  suitable when users are competing for resources.  If this is
the case for your site, you might want to consider another  batch
system, such as
