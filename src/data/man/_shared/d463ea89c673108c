MKFS.CRAMFS(8)               System Administration              MKFS.CRAMFS(8)



NNAAMMEE
       mkfs.cramfs - make compressed ROM file system

SSYYNNOOPPSSIISS
       mmkkffss..ccrraammffss [options] _d_i_r_e_c_t_o_r_y _f_i_l_e

DDEESSCCRRIIPPTTIIOONN
       Files on cramfs file systems are zlib-compressed one page at a time to
       allow random read access. The metadata is not compressed, but is
       expressed in a terse representation that is more space-efficient than
       conventional file systems.

       The file system is intentionally read-only to simplify its design;
       random write access for compressed files is difficult to implement.
       cramfs ships with a utility (mmkkccrraammffss(8)) to pack files into new cramfs
       images.

       File sizes are limited to less than 16 MB.

       Maximum file system size is a little under 272 MB. (The last file on
       the file system must begin before the 256 MB block, but can extend past
       it.)

AARRGGUUMMEENNTTSS
       The _d_i_r_e_c_t_o_r_y is simply the root of the directory tree that we want to
       generate a compressed filesystem out of.

       The _f_i_l_e will contain the cram file system, which later can be mounted.

OOPPTTIIOONNSS
       --vv
           Enable verbose messaging.

       --EE
           Treat all warnings as errors, which are reflected as command exit
           status.

       --bb _b_l_o_c_k_s_i_z_e
           Use defined block size, which has to be divisible by page size.

       --ee _e_d_i_t_i_o_n
           Use defined file system edition number in superblock.

       --NN _b_i_g_, _l_i_t_t_l_e_, _h_o_s_t
           Use defined endianness. Value defaults to _h_o_s_t.

       --ii _f_i_l_e
           Insert a _f_i_l_e to cramfs file system.

       --nn _n_a_m_e
           Set name of the cramfs file system.

       --pp
           Pad by 512 bytes for boot code.

       --ss
           This option is ignored. Originally the --ss turned on directory entry
           sorting.

       --zz
           Make explicit holes.

       --hh, ----hheellpp
           Display help text and exit.

       --VV, ----vveerrssiioonn
           Print version and exit.

EEXXIITT SSTTAATTUUSS
       00
           success

       88
           operation error, such as unable to allocate memory

SSEEEE AALLSSOO
       ffsscckk..ccrraammffss(8), mmoouunntt(8)

RREEPPOORRTTIINNGG BBUUGGSS
       For bug reports, use the issue tracker at
       https://github.com/util-linux/util-linux/issues.

AAVVAAIILLAABBIILLIITTYY
       The mmkkffss..ccrraammffss command is part of the util-linux package which can be
       downloaded from Linux Kernel Archive
       <https://www.kernel.org/pub/linux/utils/util-linux/>.



util-linux 2.38.1                 2022-05-11                    MKFS.CRAMFS(8)
