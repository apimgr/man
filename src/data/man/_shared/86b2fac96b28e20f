_m_p_o_o_l(3)                   Library Functions Manual                   _m_p_o_o_l(3)

NNAAMMEE
       mpool - shared memory buffer pool

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ddbb..hh>>
       ##iinncclluuddee <<mmppooooll..hh>>

       MMPPOOOOLL **mmppooooll__ooppeenn((DDBBTT **_k_e_y,, iinntt _f_d,, ppggnnoo__tt _p_a_g_e_s_i_z_e,, ppggnnoo__tt _m_a_x_c_a_c_h_e));;

       vvooiidd mmppooooll__ffiilltteerr((MMPPOOOOLL **_m_p,,
                         ttyyppeeooff((vvooiidd ((vvooiidd **,, ppggnnoo__tt,, vvooiidd **)))) **_p_g_i_n,,
                         ttyyppeeooff((vvooiidd ((vvooiidd **,, ppggnnoo__tt,, vvooiidd **)))) **_p_g_o_u_t,,
                         vvooiidd **_p_g_c_o_o_k_i_e));;

       vvooiidd **mmppooooll__nneeww((MMPPOOOOLL **_m_p,, ppggnnoo__tt **_p_g_n_o_a_d_d_r));;
       vvooiidd **mmppooooll__ggeett((MMPPOOOOLL **_m_p,, ppggnnoo__tt _p_g_n_o,, uunnssiiggnneedd iinntt _f_l_a_g_s));;
       iinntt mmppooooll__ppuutt((MMPPOOOOLL **_m_p,, vvooiidd **_p_g_a_d_d_r,, uunnssiiggnneedd iinntt _f_l_a_g_s));;

       iinntt mmppooooll__ssyynncc((MMPPOOOOLL **_m_p));;
       iinntt mmppooooll__cclloossee((MMPPOOOOLL **_m_p));;

DDEESSCCRRIIPPTTIIOONN
       _N_o_t_e  _w_e_l_l: This page documents interfaces provided up until glibc 2.1.
       Since glibc 2.2, glibc no longer provides these interfaces.   Probably,
       you are looking for the APIs provided by the _l_i_b_d_b library instead.

       _M_p_o_o_l is the library interface intended to provide page oriented buffer
       management of files.  The buffers may be shared between processes.

       The  function mmppooooll__ooppeenn() initializes a memory pool.  The _k_e_y argument
       is the byte string used to negotiate between multiple processes wishing
       to share buffers.  If the file buffers are mapped in shared memory, all
       processes using the same key will share the buffers.  If _k_e_y  is  NULL,
       the  buffers are mapped into private memory.  The _f_d argument is a file
       descriptor for the underlying file, which must be seekable.  If _k_e_y  is
       non‐NULL  and  matches  a file already being mapped, the _f_d argument is
       ignored.

       The _p_a_g_e_s_i_z_e argument is the size, in bytes, of the  pages  into  which
       the  file is broken up.  The _m_a_x_c_a_c_h_e argument is the maximum number of
       pages from the underlying file to cache at any one time.  This value is
       not relative to the number of processes which share a  file’s  buffers,
       but will be the largest value specified by any of the processes sharing
       the file.

       The  mmppooooll__ffiilltteerr()  function is intended to make transparent input and
       output processing of the pages possible.  If the _p_g_i_n function is spec‐
       ified, it is called each time a buffer is read  into  the  memory  pool
       from  the  backing  file.   If  the  _p_g_o_u_t function is specified, it is
       called each time a buffer is written into the backing file.  Both func‐
       tions are called with the _p_g_c_o_o_k_i_e  pointer,  the  page  number  and  a
       pointer to the page to being read or written.

       The function mmppooooll__nneeww() takes an _M_P_O_O_L pointer and an address as argu‐
       ments.   If  a  new page can be allocated, a pointer to the page is re‐
       turned and the page number is stored into the _p_g_n_o_a_d_d_r address.  Other‐
       wise, NULL is returned and _e_r_r_n_o is set.

       The function mmppooooll__ggeett() takes an _M_P_O_O_L pointer and a  page  number  as
       arguments.   If  the  page  exists,  a pointer to the page is returned.
       Otherwise, NULL is returned and _e_r_r_n_o is set.  The  _f_l_a_g_s  argument  is
       not currently used.

       The  function mmppooooll__ppuutt() unpins the page referenced by _p_g_a_d_d_r.  _p_g_a_d_d_r
       must be an address previously returned by mmppooooll__ggeett()  or  mmppooooll__nneeww().
       The flag value is specified by ORing any of the following values:

       MMPPOOOOLL__DDIIRRTTYY
              The  page has been modified and needs to be written to the back‐
              ing file.

       mmppooooll__ppuutt() returns 0 on success and -1 if an error occurs.

       The function mmppooooll__ssyynncc() writes all modified pages associated with the
       _M_P_O_O_L pointer to the backing file.  mmppooooll__ssyynncc() returns 0  on  success
       and -1 if an error occurs.

       The  mmppooooll__cclloossee()  function  free’s up any allocated memory associated
       with the memory pool cookie.  Modified pages are  nnoott  written  to  the
       backing  file.   mmppooooll__cclloossee()  returns 0 on success and -1 if an error
       occurs.

EERRRROORRSS
       The mmppooooll__ooppeenn() function may fail and set _e_r_r_n_o for any of the  errors
       specified for the library routine mmaalllloocc(3).

       The mmppooooll__ggeett() function may fail and set _e_r_r_n_o for the following:

       EEIINNVVAALL         The requested record doesn’t exist.

       The  mmppooooll__nneeww()  and  mmppooooll__ggeett() functions may fail and set _e_r_r_n_o for
       any of the errors specified for the library routines rreeaadd(2), wwrriittee(2),
       and mmaalllloocc(3).

       The mmppooooll__ssyynncc() function may fail and set _e_r_r_n_o for any of the  errors
       specified for the library routine wwrriittee(2).

       The mmppooooll__cclloossee() function may fail and set _e_r_r_n_o for any of the errors
       specified for the library routine ffrreeee(3).

SSTTAANNDDAARRDDSS
       BSD.

SSEEEE AALLSSOO
       bbttrreeee(3), ddbbooppeenn(3), hhaasshh(3), rreeccnnoo(3)

4.4 Berkeley Distribution         2025‐05‐17                          _m_p_o_o_l(3)
