SOCKET(9)                BSD Kernel Developer's Manual               SOCKET(9)

NNAAMMEE
     ssoocckkeett — kernel socket interface

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ssoocckkeett..hh>>
     ##iinncclluuddee <<ssyyss//ssoocckkeettvvaarr..hh>>

     _v_o_i_d
     ssooaabboorrtt(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o);

     _i_n_t
     ssooaacccceepptt(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _s_t_r_u_c_t _s_o_c_k_a_d_d_r _*_*_n_a_m);

     _i_n_t
     ssoocchheecckkuuiidd(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _u_i_d___t _u_i_d);

     _i_n_t
     ssoobbiinndd(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _s_t_r_u_c_t _s_o_c_k_a_d_d_r _*_n_a_m, _s_t_r_u_c_t _t_h_r_e_a_d _*_t_d);

     _v_o_i_d
     ssoocclloossee(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o);

     _i_n_t
     ssooccoonnnneecctt(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _s_t_r_u_c_t _s_o_c_k_a_d_d_r _*_n_a_m, _s_t_r_u_c_t _t_h_r_e_a_d _*_t_d);

     _i_n_t
     ssooccrreeaattee(_i_n_t _d_o_m, _s_t_r_u_c_t _s_o_c_k_e_t _*_*_a_s_o, _i_n_t _t_y_p_e, _i_n_t _p_r_o_t_o,
         _s_t_r_u_c_t _u_c_r_e_d _*_c_r_e_d, _s_t_r_u_c_t _t_h_r_e_a_d _*_t_d);

     _i_n_t
     ssooddiissccoonnnneecctt(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o);

     _v_o_i_d
     ssooddttoorr__sseett(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _v_o_i_d _(_*_f_u_n_c_)_(_s_t_r_u_c_t _s_o_c_k_e_t _*_));

     _s_t_r_u_c_t _s_o_c_k_a_d_d_r _*
     ssoodduuppssoocckkaaddddrr(_c_o_n_s_t _s_t_r_u_c_t _s_o_c_k_a_d_d_r _*_s_a, _i_n_t _m_f_l_a_g_s);

     _v_o_i_d
     ssooffrreeee(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o);

     _v_o_i_d
     ssoohhaassoouuttooffbbaanndd(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o);

     _i_n_t
     ssoolliisstteenn(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _i_n_t _b_a_c_k_l_o_g, _s_t_r_u_c_t _t_h_r_e_a_d _*_t_d);

     _v_o_i_d
     ssoolliisstteenn__pprroottoo(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _i_n_t _b_a_c_k_l_o_g);

     _i_n_t
     ssoolliisstteenn__pprroottoo__cchheecckk(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o);

     _s_t_r_u_c_t _s_o_c_k_e_t _*
     ssoonneewwccoonnnn(_s_t_r_u_c_t _s_o_c_k_e_t _*_h_e_a_d, _i_n_t _c_o_n_n_s_t_a_t_u_s);

     _i_n_t
     ssooppoollll(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _i_n_t _e_v_e_n_t_s, _s_t_r_u_c_t _u_c_r_e_d _*_a_c_t_i_v_e___c_r_e_d,
         _s_t_r_u_c_t _t_h_r_e_a_d _*_t_d);

     _i_n_t
     ssooppoollll__ggeenneerriicc(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _i_n_t _e_v_e_n_t_s, _s_t_r_u_c_t _u_c_r_e_d _*_a_c_t_i_v_e___c_r_e_d,
         _s_t_r_u_c_t _t_h_r_e_a_d _*_t_d);

     _i_n_t
     ssoorreecceeiivvee(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _s_t_r_u_c_t _s_o_c_k_a_d_d_r _*_*_p_s_a, _s_t_r_u_c_t _u_i_o _*_u_i_o,
         _s_t_r_u_c_t _m_b_u_f _*_*_m_p_0, _s_t_r_u_c_t _m_b_u_f _*_*_c_o_n_t_r_o_l_p, _i_n_t _*_f_l_a_g_s_p);

     _i_n_t
     ssoorreecceeiivvee__ssttrreeaamm(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _s_t_r_u_c_t _s_o_c_k_a_d_d_r _*_*_p_a_d_d_r,
         _s_t_r_u_c_t _u_i_o _*_u_i_o, _s_t_r_u_c_t _m_b_u_f _*_*_m_p_0, _s_t_r_u_c_t _m_b_u_f _*_*_c_o_n_t_r_o_l_p,
         _i_n_t _*_f_l_a_g_s_p);

     _i_n_t
     ssoorreecceeiivvee__ddggrraamm(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _s_t_r_u_c_t _s_o_c_k_a_d_d_r _*_*_p_a_d_d_r,
         _s_t_r_u_c_t _u_i_o _*_u_i_o, _s_t_r_u_c_t _m_b_u_f _*_*_m_p_0, _s_t_r_u_c_t _m_b_u_f _*_*_c_o_n_t_r_o_l_p,
         _i_n_t _*_f_l_a_g_s_p);

     _i_n_t
     ssoorreecceeiivvee__ggeenneerriicc(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _s_t_r_u_c_t _s_o_c_k_a_d_d_r _*_*_p_a_d_d_r,
         _s_t_r_u_c_t _u_i_o _*_u_i_o, _s_t_r_u_c_t _m_b_u_f _*_*_m_p_0, _s_t_r_u_c_t _m_b_u_f _*_*_c_o_n_t_r_o_l_p,
         _i_n_t _*_f_l_a_g_s_p);

     _i_n_t
     ssoorreesseerrvvee(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _u___l_o_n_g _s_n_d_c_c, _u___l_o_n_g _r_c_v_c_c);

     _v_o_i_d
     ssoorrfflluusshh(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o);

     _i_n_t
     ssoosseenndd(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _s_t_r_u_c_t _s_o_c_k_a_d_d_r _*_a_d_d_r, _s_t_r_u_c_t _u_i_o _*_u_i_o,
         _s_t_r_u_c_t _m_b_u_f _*_t_o_p, _s_t_r_u_c_t _m_b_u_f _*_c_o_n_t_r_o_l, _i_n_t _f_l_a_g_s,
         _s_t_r_u_c_t _t_h_r_e_a_d _*_t_d);

     _i_n_t
     ssoosseenndd__ddggrraamm(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _s_t_r_u_c_t _s_o_c_k_a_d_d_r _*_a_d_d_r, _s_t_r_u_c_t _u_i_o _*_u_i_o,
         _s_t_r_u_c_t _m_b_u_f _*_t_o_p, _s_t_r_u_c_t _m_b_u_f _*_c_o_n_t_r_o_l, _i_n_t _f_l_a_g_s,
         _s_t_r_u_c_t _t_h_r_e_a_d _*_t_d);

     _i_n_t
     ssoosseenndd__ggeenneerriicc(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _s_t_r_u_c_t _s_o_c_k_a_d_d_r _*_a_d_d_r, _s_t_r_u_c_t _u_i_o _*_u_i_o,
         _s_t_r_u_c_t _m_b_u_f _*_t_o_p, _s_t_r_u_c_t _m_b_u_f _*_c_o_n_t_r_o_l, _i_n_t _f_l_a_g_s,
         _s_t_r_u_c_t _t_h_r_e_a_d _*_t_d);

     _i_n_t
     ssoosshhuuttddoowwnn(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _i_n_t _h_o_w);

     _v_o_i_d
     ssoottooxxssoocckkeett(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _s_t_r_u_c_t _x_s_o_c_k_e_t _*_x_s_o);

     _v_o_i_d
     ssoouuppccaallll__cclleeaarr(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _i_n_t _w_h_i_c_h);

     _v_o_i_d
     ssoouuppccaallll__sseett(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _i_n_t _w_h_i_c_h,
         _i_n_t _(_*_f_u_n_c_)_(_s_t_r_u_c_t _s_o_c_k_e_t _*_, _v_o_i_d _*_, _i_n_t_), _v_o_i_d _*_a_r_g);

     _v_o_i_d
     ssoowwaakkeeuupp(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _s_t_r_u_c_t _s_o_c_k_b_u_f _*_s_b);

     ##iinncclluuddee <<ssyyss//ssoocckkoopptt..hh>>

     _i_n_t
     ssoosseettoopptt(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _s_t_r_u_c_t _s_o_c_k_o_p_t _*_s_o_p_t);

     _i_n_t
     ssooggeettoopptt(_s_t_r_u_c_t _s_o_c_k_e_t _*_s_o, _s_t_r_u_c_t _s_o_c_k_o_p_t _*_s_o_p_t);

     _i_n_t
     ssooooppttccooppyyiinn(_s_t_r_u_c_t _s_o_c_k_o_p_t _*_s_o_p_t, _v_o_i_d _*_b_u_f, _s_i_z_e___t _l_e_n, _s_i_z_e___t _m_i_n_l_e_n);

     _i_n_t
     ssooooppttccooppyyoouutt(_s_t_r_u_c_t _s_o_c_k_o_p_t _*_s_o_p_t, _c_o_n_s_t _v_o_i_d _*_b_u_f, _s_i_z_e___t _l_e_n);

DDEESSCCRRIIPPTTIIOONN
     The kernel ssoocckkeett programming interface permits in-kernel consumers to
     interact with local and network socket objects in a manner similar to
     that permitted using the socket(2) user API.  These interfaces are appro‐
     priate for use by distributed file systems and other network-aware kernel
     services.  While the user API operates on file descriptors, the kernel
     interfaces operate directly on _s_t_r_u_c_t _s_o_c_k_e_t pointers.  Some portions of
     the kernel API exist only to implement the user API, and are not expected
     to be used by kernel code.  The portions of the socket API used by socket
     consumers and implementations of network protocols will differ; some rou‐
     tines are only useful for protocol implementors.

     Except where otherwise indicated, ssoocckkeett functions may sleep, and are not
     appropriate for use in an interrupt thread context or while holding non-
     sleepable kernel locks.

   CCrreeaattiinngg aanndd DDeessttrrooyyiinngg SSoocckkeettss
     A new socket may be created using ssooccrreeaattee().  As with socket(2), argu‐
     ments specify the requested domain, type, and protocol via _d_o_m, _t_y_p_e, and
     _p_r_o_t_o.  The socket is returned via _a_s_o on success.  In addition, the cre‐
     dential used to authorize operations associated with the socket will be
     passed via _c_r_e_d (and will be cached for the lifetime of the socket), and
     the thread performing the operation via _t_d.  _W_a_r_n_i_n_g: authorization of
     the socket creation operation will be performed using the thread creden‐
     tial for some protocols (such as raw sockets).

     Sockets may be closed and freed using ssoocclloossee(), which has similar seman‐
     tics to close(2).

     In certain circumstances, it is appropriate to destroy a socket without
     waiting for it to disconnect, for which ssooaabboorrtt() is used.  This is only
     appropriate for incoming connections which are in a partially connected
     state.  It must be called on an unreferenced socket, by the thread which
     removed the socket from its listen queue, to prevent races.  It will call
     into protocol code, so no socket locks may be held over the call.  The
     caller of ssooaabboorrtt() is responsible for setting the VNET context.  The
     normal path to freeing a socket is ssooffrreeee(), which handles reference
     counting on the socket.  It should be called whenever a reference is re‐
     leased, and also whenever reference flags are cleared in socket or proto‐
     col code.  Calls to ssooffrreeee() should not be made from outside the socket
     layer; outside callers should use ssoocclloossee() instead.

   CCoonnnneeccttiioonnss aanndd AAddddrreesssseess
     The ssoobbiinndd() function is equivalent to the bind(2) system call, and binds
     the socket _s_o to the address _n_a_m.  The operation would be authorized us‐
     ing the credential on thread _t_d.

     The ssooccoonnnneecctt() function is equivalent to the connect(2) system call, and
     initiates a connection on the socket _s_o to the address _n_a_m.  The opera‐
     tion will be authorized using the credential on thread _t_d.  Unlike the
     user system call, ssooccoonnnneecctt() returns immediately; the caller may
     msleep(9) on _s_o_-_>_s_o___t_i_m_e_o while holding the socket mutex and waiting for
     the SS_ISCONNECTING flag to clear or _s_o_-_>_s_o___e_r_r_o_r to become non-zero.  If
     ssooccoonnnneecctt() fails, the caller must manually clear the SS_ISCONNECTING
     flag.

     A call to ssooddiissccoonnnneecctt() disconnects the socket without closing it.

     The ssoosshhuuttddoowwnn() function is equivalent to the shutdown(2) system call,
     and causes part or all of a connection on a socket to be closed down.

     Sockets are transitioned from non-listening status to listening with
     ssoolliisstteenn().

   SSoocckkeett OOppttiioonnss
     The ssooggeettoopptt() function is equivalent to the getsockopt(2) system call,
     and retrieves a socket option on socket _s_o.  The ssoosseettoopptt() function is
     equivalent to the setsockopt(2) system call, and sets a socket option on
     socket _s_o.

     The second argument in both ssooggeettoopptt() and ssoosseettoopptt() is the _s_o_p_t pointer
     to a _s_t_r_u_c_t _s_o_p_t describing the socket option operation.  The caller-al‐
     located structure must be zeroed, and then have its fields initialized to
     specify socket option operation arguments:

     _s_o_p_t___d_i_r      Set to SOPT_SET or SOPT_GET depending on whether this is a
                   get or set operation.

     _s_o_p_t___l_e_v_e_l    Specify the level in the network stack the operation is
                   targeted at; for example, SOL_SOCKET.

     _s_o_p_t___n_a_m_e     Specify the name of the socket option to set.

     _s_o_p_t___v_a_l      Kernel space pointer to the argument value for the socket
                   option.

     _s_o_p_t___v_a_l_s_i_z_e  Size of the argument value in bytes.

   SSoocckkeett UUppccaallllss
     In order for the owner of a socket to be notified when the socket is
     ready to send or receive data, an upcall may be registered on the socket.
     The upcall is a function that will be called by the socket framework when
     a socket buffer associated with the given socket is ready for reading or
     writing.  ssoouuppccaallll__sseett() is used to register a socket upcall.  The func‐
     tion _f_u_n_c is registered, and the pointer _a_r_g will be passed as its second
     argument when it is called by the framework.  The possible values for
     _w_h_i_c_h are SO_RCV and SO_SND, which register upcalls for receive and send
     events, respectively.  The upcall function ffuunncc() must return either
     SU_OK or SU_ISCONNECTED, depending on whether or not a call to
     soisconnected should be made by the socket framework after the upcall re‐
     turns.  The upcall _f_u_n_c cannot call soisconnected itself due to lock or‐
     dering with the socket buffer lock.  Only SO_RCV upcalls should return
     SU_ISCONNECTED.  When a SO_RCV upcall returns SU_ISCONNECTED, the upcall
     will be removed from the socket.

     Upcalls are removed from their socket by ssoouuppccaallll__cclleeaarr().  The _w_h_i_c_h ar‐
     gument again specifies whether the sending or receiving upcall is to be
     cleared, with SO_RCV or SO_SND.

   SSoocckkeett DDeessttrruuccttoorr CCaallllbbaacckk
     A kernel system can use the ssooddttoorr__sseett() function to set a destructor for
     a socket.  The destructor is called when the socket is about to be freed.
     The destructor is called before the protocol detach routine.  The de‐
     structor can serve as a callback to initiate additional cleanup actions.

   SSoocckkeett II//OO
     The ssoorreecceeiivvee() function is equivalent to the recvmsg(2) system call, and
     attempts to receive bytes of data from the socket _s_o, optionally blocking
     awaiting for data if none is ready to read.  Data may be retrieved di‐
     rectly to kernel or user memory via the _u_i_o argument, or as an mbuf chain
     returned to the caller via _m_p_0, avoiding a data copy.  The _u_i_o must al‐
     ways be non-NULL.  If _m_p_0 is non-NULL, only the _u_i_o___r_e_s_i_d of _u_i_o is used.
     The caller may optionally retrieve a socket address on a protocol with
     the PR_ADDR capability by providing storage via non-NULL _p_s_a argument.
     The caller may optionally retrieve control data mbufs via a non-NULL
     _c_o_n_t_r_o_l_p argument.  Optional flags may be passed to ssoorreecceeiivvee() via a
     non-NULL _f_l_a_g_s_p argument, and use the same flag name space as the
     recvmsg(2) system call.

     The ssoosseenndd() function is equivalent to the sendmsg(2) system call, and
     attempts to send bytes of data via the socket _s_o, optionally blocking if
     data cannot be immediately sent.  Data may be sent directly from kernel
     or user memory via the _u_i_o argument, or as an mbuf chain via _t_o_p, avoid‐
     ing a data copy.  Only one of the _u_i_o or _t_o_p pointers may be non-NULL.
     An optional destination address may be specified via a non-NULL _a_d_d_r ar‐
     gument, which may result in an implicit connect if supported by the pro‐
     tocol.  The caller may optionally send control data mbufs via a non-NULL
     _c_o_n_t_r_o_l argument.  Flags may be passed to ssoosseenndd() using the _f_l_a_g_s argu‐
     ment, and use the same flag name space as the sendmsg(2) system call.

     Kernel callers running in an interrupt thread context, or with a mutex
     held, will wish to use non-blocking sockets and pass the MSG_DONTWAIT
     flag in order to prevent these functions from sleeping.

     A socket can be queried for readability, writability, out-of-band data,
     or end-of-file using ssooppoollll().  The possible values for _e_v_e_n_t_s are as for
     poll(2), with symbolic values POLLIN, POLLPRI, POLLOUT, POLLRDNORM,
     POLLWRNORM, POLLRDBAND, and POLLINGEOF taken from <_s_y_s_/_p_o_l_l_._h>.

     Calls to ssooaacccceepptt() pass through to the protocol's accept routine to ac‐
     cept an incoming connection.

   SSoocckkeett UUttiilliittyy FFuunnccttiioonnss
     The uid of a socket's credential may be compared against a _u_i_d with
     ssoocchheecckkuuiidd().

     A copy of an existing _s_t_r_u_c_t _s_o_c_k_a_d_d_r may be made using ssoodduuppssoocckkaaddddrr().

     Protocol implementations notify the socket layer of the arrival of out-
     of-band data using ssoohhaassoouuttooffbbaanndd(), so that the socket layer can notify
     socket consumers of the available data.

     An “external-format” version of a _s_t_r_u_c_t _s_o_c_k_e_t can be created using
     ssoottooxxssoocckkeett(), suitable for isolating user code from changes in the ker‐
     nel structure.

   PPrroottooccooll IImmpplleemmeennttaattiioonnss
     Protocols must supply an implementation for ssoolliisstteenn(); such protocol im‐
     plementations can call back into the socket layer using
     ssoolliisstteenn__pprroottoo__cchheecckk() and ssoolliisstteenn__pprroottoo() to check and set the socket-
     layer listen state.  These callbacks are provided so that the protocol
     implementation can order the socket layer and protocol locks as neces‐
     sary.  Protocols must supply an implementation of ssoorreecceeiivvee(); the func‐
     tions ssoorreecceeiivvee__ssttrreeaamm(), ssoorreecceeiivvee__ddggrraamm(), and ssoorreecceeiivvee__ggeenneerriicc() are
     supplied for use by such implementations.

     Protocol implementations can use ssoonneewwccoonnnn() to create a socket and at‐
     tach protocol state to that socket.  This can be used to create new sock‐
     ets available for ssooaacccceepptt() on a listen socket.  The returned socket has
     a reference count of zero.

     Protocols must supply an implementation for ssooppoollll(); ssooppoollll__ggeenneerriicc() is
     provided for the use by protocol implementations.

     The functions ssoosseenndd__ddggrraamm() and ssoosseenndd__ggeenneerriicc() are supplied to assist
     in protocol implementations of ssoosseenndd().

     When a protocol creates a new socket structure, it is necessary to re‐
     serve socket buffer space for that socket, by calling ssoorreesseerrvvee().  The
     rough inverse of this reservation is performed by ssoorrfflluusshh(), which is
     called automatically by the socket framework.

     When a protocol needs to wake up threads waiting for the socket to become
     ready to read or write, variants of ssoowwaakkeeuupp() are used.  The ssoowwaakkeeuupp()
     function should not be called directly by protocol code, instead use the
     wrappers ssoorrwwaakkeeuupp(), ssoorrwwaakkeeuupp__lloocckkeedd(), ssoowwwwaakkeeuupp(), and
     ssoowwwwaakkeeuupp__lloocckkeedd() for readers and writers, with the corresponding socket
     buffer lock not already locked, or already held, respectively.

     The functions ssooooppttccooppyyiinn() and ssooooppttccooppyyoouutt() are useful for transfer‐
     ring _s_t_r_u_c_t _s_o_c_k_o_p_t data between user and kernel code.

SSEEEE AALLSSOO
     bind(2), close(2), connect(2), getsockopt(2), recv(2), send(2),
     setsockopt(2), shutdown(2), socket(2), ng_ksocket(4), intr_event(9),
     msleep(9), ucred(9)

HHIISSTTOORRYY
     The socket(2) system call appeared in 4.2BSD.  This manual page was in‐
     troduced in FreeBSD 7.0.

AAUUTTHHOORRSS
     This manual page was written by Robert Watson and
     Benjamin Kaduk.

BBUUGGSS
     The use of explicitly passed credentials, credentials hung from explic‐
     itly passed threads, the credential on curthread, and the cached creden‐
     tial from socket creation time is inconsistent, and may lead to unex‐
     pected behaviour.  It is possible that several of the _t_d arguments should
     be _c_r_e_d arguments, or simply not be present at all.

     The caller may need to manually clear SS_ISCONNECTING if ssooccoonnnneecctt() re‐
     turns an error.

     The MSG_DONTWAIT flag is not implemented for ssoosseenndd(), and may not always
     work with ssoorreecceeiivvee() when zero copy sockets are enabled.

     This manual page does not describe how to register socket upcalls or mon‐
     itor a socket for readability/writability without using blocking I/O.

     The ssoorreeff() and ssoorreellee() functions are not described, and in most cases
     should not be used, due to confusing and potentially incorrect interac‐
     tions when ssoorreellee() is last called after ssoocclloossee().

BSD                            September 6, 2022                           BSD
