ELF_FLAGDATA(3)          BSD Library Functions Manual          ELF_FLAGDATA(3)

NNAAMMEE
     eellff__ffllaaggaarrhhddrr, eellff__ffllaaggddaattaa, eellff__ffllaaggeehhddrr, eellff__ffllaaggeellff, eellff__ffllaaggpphhddrr,
     eellff__ffllaaggssccnn, eellff__ffllaaggsshhddrr — manipulate flags associated with ELF data
     structures

LLIIBBRRAARRYY
     ELF Access Library (libelf, -lelf)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<lliibbeellff..hh>>

     _u_n_s_i_g_n_e_d _i_n_t
     eellff__ffllaaggaarrhhddrr(_E_l_f___A_r_h_d_r _*_a_r_h_d_r, _E_l_f___C_m_d _c_m_d, _u_n_s_i_g_n_e_d _i_n_t _f_l_a_g_s);

     _u_n_s_i_g_n_e_d _i_n_t
     eellff__ffllaaggddaattaa(_E_l_f___D_a_t_a _*_d_a_t_a, _E_l_f___C_m_d _c_m_d, _u_n_s_i_g_n_e_d _i_n_t _f_l_a_g_s);

     _u_n_s_i_g_n_e_d _i_n_t
     eellff__ffllaaggeehhddrr(_E_l_f _*_e_l_f, _E_l_f___C_m_d _c_m_d, _u_n_s_i_g_n_e_d _i_n_t _f_l_a_g_s);

     _u_n_s_i_g_n_e_d _i_n_t
     eellff__ffllaaggeellff(_E_l_f _*_e_l_f, _E_l_f___C_m_d _c_m_d, _u_n_s_i_g_n_e_d _i_n_t _f_l_a_g_s);

     _u_n_s_i_g_n_e_d _i_n_t
     eellff__ffllaaggpphhddrr(_E_l_f _*_e_l_f, _E_l_f___C_m_d _c_m_d, _u_n_s_i_g_n_e_d _i_n_t _f_l_a_g_s);

     _u_n_s_i_g_n_e_d _i_n_t
     eellff__ffllaaggssccnn(_E_l_f___S_c_n _*_s_c_n, _E_l_f___C_m_d _c_m_d, _u_n_s_i_g_n_e_d _i_n_t _f_l_a_g_s);

     _u_n_s_i_g_n_e_d _i_n_t
     eellff__ffllaaggsshhddrr(_E_l_f___S_c_n _*_s_c_n, _E_l_f___C_m_d _c_m_d, _u_n_s_i_g_n_e_d _i_n_t _f_l_a_g_s);

DDEESSCCRRIIPPTTIIOONN
     These functions are used to query, set or reset flags on data structures
     associated with an ELF file.

     Arguments _a_r_h_d_r, _d_a_t_a, _e_l_f and _s_c_n denote the data structures whose flags
     need to be changed.  These values should have been returned by prior
     calls to functions in the elf(3) API set:
     ••   Argument _a_r_h_d_r should have been returned by a prior call to
         elf_getarhdr(3).
     ••   Argument _d_a_t_a should have been returned by a prior call to one of
         elf_newdata(3), elf_getdata(3) or elf_rawdata(3).
     ••   Argument _e_l_f should have been allocated by a prior call to one of
         elf_begin(3) or elf_memory(3).
     ••   Argument _s_c_n should have been returned by a prior call to one of
         elf_getscn(3), elf_newscn(3) or elf_nextscn(3).
     These values are allowed to be NULL to simplify error handling in appli‐
     cation code.

     Argument _c_m_d may have the following values:

     ELF_C_CLR  The argument _f_l_a_g_s specifies the flags to be cleared.

     ELF_C_SET  The argument _f_l_a_g_s specifies the flags to be set.

     The argument _f_l_a_g_s is allowed to have the following flags set:

     ELF_F_ARCHIVE       This flag is only valid with the eellff__ffllaaggeellff() API.
                         It informs the library that the application desires
                         to create an ar(1) archive.  Argument _e_l_f should have
                         been opened for writing using the ELF_C_WRITE command
                         to function eellff__bbeeggiinn().

     ELF_F_ARCHIVE_SYSV  This flag is used in conjunction with the
                         ELF_F_ARCHIVE flag to indicate that library should
                         create archives that conform to System V layout
                         rules.  The default is to create BSD style archives.

     ELF_F_DIRTY         Mark the associated data structure as needing to be
                         written back to the underlying file.  A subsequent
                         call to elf_update(3) will resynchronize the li‐
                         brary's internal data structures.

     ELF_F_LAYOUT        This flag is only valid with the eellff__ffllaaggeellff() API.
                         It informs the library that the application will take
                         responsibility for the layout of the file and that
                         the library is not to insert any padding in between
                         sections.

     Marking a given data structure as “dirty” affects all of its contained
     elements.  Thus marking an ELF descriptor _e_l_f with eellff__ffllaaggeellff(_e_l_f,
     _E_L_F___C___S_E_T, _E_L_F___F___D_I_R_T_Y) means that the entire contents of the descriptor
     are “dirty”.

     Using a value of zero for argument _f_l_a_g_s will return the current set of
     flags for the data structure being queried.

RREETTUURRNN VVAALLUUEESS
     These functions return the updated flags if successful, or zero if an er‐
     ror is detected.

CCOOMMPPAATTIIBBIILLIITTYY
     The eellff__ffllaaggaarrhhddrr() function and the ELF_F_ARCHIVE and ELF_F_ARCHIVE_SYSV
     flags are an extension to the elf(3) API.

EERRRROORRSS
     These functions may fail with the following errors:

     [ELF_E_ARGUMENT]  An unsupported value was used for the _c_m_d argument.

     [ELF_E_ARGUMENT]  Argument _f_l_a_g_s had unsupported flags set.

     [ELF_E_ARGUMENT]  The argument _e_l_f was not a descriptor for an ELF ob‐
                       ject.

     [ELF_E_MODE]      The ELF_F_ARCHIVE flag was used with an ELF descriptor
                       that had not been opened for writing.

     [ELF_E_SEQUENCE]  Function eellff__ffllaaggeehhddrr() was called without an exe‐
                       cutable header being allocated.

     [ELF_E_SEQUENCE]  Function eellff__ffllaaggpphhddrr() was called without a program
                       header being allocated.

SSEEEE AALLSSOO
     elf(3), elf32_newehdr(3), elf32_newphdr(3), elf64_newehdr(3),
     elf64_newphdr(3), elf_newdata(3), elf_update(3), gelf(3),
     gelf_newehdr(3), gelf_newphdr(3), gelf_update_dyn(3),
     gelf_update_move(3), gelf_update_rel(3), gelf_update_rela(3),
     gelf_update_sym(3), gelf_update_syminfo(3)

BSD                              June 12, 2019                             BSD
