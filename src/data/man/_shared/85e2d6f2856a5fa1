The  system call writes modified whole pages back to the filesys‐
tem and updates the file modification  time.   Only  those  pages
containing  and succeeding locations will be examined.  The argu‐
ment may be specified as follows: MS_ASYNC        Return  immedi‐
ately   MS_SYNC         Perform   synchronous  writes  MS_INVALI‐
DATE   Invalidate all cached data The flag is not permitted to be
combined with other flags.  If any errors occur, ‐1  is  returned
and  errno is set to indicate the error.  Otherwise, a 0 value is
returned.  will fail if: Some  of  the  specified  addresses  are
locked  and MS_INVALIDATE is specified.  is not a multiple of the
hardware page size.  is too large, or less than  1.   is  invalid
(e.g.,  it combines MS_ASYNC with another flag, which is not per‐
mitted).  An I/O error occurs while writing to the  file  system.
The  specified  address  range is outside of the address range of
the process or includes an unmapped page.  The  include  file  is
necessary.  The function first appeared in
