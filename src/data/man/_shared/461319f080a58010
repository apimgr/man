_g_e_t_w_c_h_a_r(3)                Library Functions Manual                _g_e_t_w_c_h_a_r(3)

NNAAMMEE
       getwchar - read a wide character from standard input

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<wwcchhaarr..hh>>

       wwiinntt__tt ggeettwwcchhaarr((vvooiidd));;

DDEESSCCRRIIPPTTIIOONN
       The  ggeettwwcchhaarr()  function  is  the  wide‐character  equivalent  of  the
       ggeettcchhaarr(3) function.  It reads a wide character from _s_t_d_i_n and  returns
       it.  If the end of stream is reached, or if _f_e_r_r_o_r_(_s_t_d_i_n_) becomes true,
       it  returns WWEEOOFF.  If a wide‐character conversion error occurs, it sets
       _e_r_r_n_o to EEIILLSSEEQQ and returns WWEEOOFF.

       For a nonlocking counterpart, see uunnlloocckkeedd__ssttddiioo(3).

RREETTUURRNN VVAALLUUEE
       The ggeettwwcchhaarr() function returns the next wide‐character  from  standard
       input, or WWEEOOFF.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       ggeettwwcchhaarr() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       C11, POSIX.1‐2008.

HHIISSTTOORRYY
       C99, POSIX.1‐2001.

NNOOTTEESS
       The behavior of ggeettwwcchhaarr() depends on the LLCC__CCTTYYPPEE category of the
       current locale.

       It is reasonable to expect that ggeettwwcchhaarr() will actually read a
       multibyte sequence from standard input and then convert it to a wide
       character.

SSEEEE AALLSSOO
       ffggeettwwcc(3), uunnlloocckkeedd__ssttddiioo(3)

Linux man‐pages (unreleased)      2025‐05‐17                       _g_e_t_w_c_h_a_r(3)
