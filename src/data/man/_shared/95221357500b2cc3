The  driver  allows  the creation and use of virtual Ethernet de‐
vices.  Those interfaces appear just as any real Ethernet NIC  to
the  kernel, but can also be accessed by userland through a char‐
acter device node in order to read frames being sent by the  sys‐
tem  or  to  inject frames. In that respect it is very similar to
what provides.  Interfaces may be created in two different  ways:
using  the command with a specified device number, or its equiva‐
lent, or using the special cloning device The  former  works  the
same  as  any  other cloning network interface: the administrator
can create and destroy interfaces at any time,  notably  at  boot
time.   This  is the easiest way of combining and Later, userland
will actually access the interfaces through the  specific  device
nodes  The  latter  is  aimed at applications that need a virtual
Ethernet device for the duration of their execution.  A  new  in‐
terface  is created at the opening of and is later destroyed when
the last process using the file descriptor  closes  it.   Whether
the  devices  are  accessed through the special cloning device or
through the specific devices the possible actions to control  the
matching  interface  are the same.  When using though, as the in‐
terface is created on‐the‐fly, its name is not known  immediately
by  the application.  Therefore the ioctl is provided.  It should
be the first action an application using the special cloning  de‐
vice  will do.  It takes a pointer to a as an argument.  Ethernet
frames sent out by the kernel on a interface can be  obtained  by
the controlling application with It can also inject frames in the
kernel  with  There is absolutely no validation of the content of
the injected frame, it can be any data, of any length.  One  call
of  will inject a single frame in the kernel, as one call of will
retrieve a single frame from the queue, to the extent of the pro‐
vided buffer.  If the buffer is not large enough, the frame  will
be  truncated.  character devices support the ioctl which returns
the size of the next available frame, or 0 if there is no  avail‐
able  frame  in  the  queue.   They also support non‐blocking I/O
through the ioctl.  In that mode, is returned by when no data  is
available.  Asynchronous I/O is supported through the and ioctls.
The  first  will enable generation, while the two other configure
the process group that will  receive  the  signal  when  data  is
ready.   Synchronisation  may also be achieved through the use of
or When a device is created, it is assigned an  Ethernet  address
of the form f2:0b:a4:xx:xx:xx.  This address can later be changed
using  to  add  an active link layer address, or directly via the
ioctl on a socket, as it is not available on the ioctl handler of
the character device interface.  When an application  has  opened
the  character  device the link is considered up, otherwise down.
As such, it is best to open the character device once  connectiv‐
ity  has been established so that Duplicate Address Detection, if
applicable, can be performed.  If connectivity is lost, the char‐
acter device should be closed.  cloning device individual charac‐
ter device nodes The driver first appeared in Starting  from  the
driver  can no longer be used as a endpoint because it supports a
link state based on if it has been opened or not.  Use the driver
instead as it’s been explicitly designed for this purpose.
