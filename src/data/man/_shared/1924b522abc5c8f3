NVM  Express  (NVMe) is a storage protocol standard, for SSDs and
other high‐speed storage devices over  PCI  Express.   The  power
command controls the supported power states.  will list supported
power states.  will set the power state to specified value.  will
set  workload value.  With the specified power state and workload
value is saved, so it persists any autonomous transitions and re‐
sets.  Support for saving the values is optional, and depends  on
the controller.  The logpage command knows how to print log pages
of  various types.  It also knows about vendor specific log pages
from hgst/wdc and intel.  Page 0xc1 for hgst/wdc contains the ad‐
vanced smart information about the drive.  Page 0xc1 is read  la‐
tency  stats for intel.  Page 0xc2 is write latency stats for in‐
tel.  Page 0xc5 is temperature stats for intel.  Page 0xca is ad‐
vanced smart information for intel.   Specifying  will  list  all
valid vendors and pages.  will print the page as hex.  will print
the  binary data for the page.  The various wdc commands retrieve
log data from the wdc/hgst drives.  The  flag  specifies  a  path
template  to  use  to output the files.  Each file takes the path
template (which defaults to nothing), appends the drive’s  serial
number  and the type of dump it is followed by These logs must be
sent to the vendor for analysis.  This tool only provides  a  way
to  extract  them.   Display a list of NVMe controllers and name‐
spaces along with their device nodes.  Display  a  human‐readable
summary  of the nvme0 IDENTIFY_CONTROLLER data.  Display an hexa‐
decimal dump of the nvme0 data for namespace 1.  Display a human‐
readable summary of the nvme0 controller’s Error Information Log.
Log pages defined by the NVMe specification include Error  Infor‐
mation  Log  (ID=1),  SMART/Health  Information  Log  (ID=2), and
Firmware Slot Log (ID=3).  Display a  human‐readable  summary  of
the nvme0’s wdc‐specific advanced SMART data.  Display a hexadec‐
imal dump of the nvme0 controller’s Error Information Log.  Print
the contents of vendor specific page 0xcb as binary data on stan‐
dard out.  Redirect it to a temporary file.  List all the current
power  modes.  Set the current power mode.  Get the current power
mode.  The nvmecontrol utility appeared in nvmecontrol was devel‐
oped by Intel and originally written by This man page was written
by
