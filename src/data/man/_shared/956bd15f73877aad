and  cause the file named by or referenced by to be truncated (or
extended) to bytes in size. If the file size  exceeds  any  extra
data  is  discarded. If the file size is smaller than the file is
extended and filled with zeros to the indicated length.  The form
requires the file to be open for writing.  Note: and do not  mod‐
ify  the current file offset for any open file descriptions asso‐
ciated with the file.  A value of 0 is returned if the call  suc‐
ceeds.   If the call fails a ‐1 is returned, and the global vari‐
able specifies the error.  The system call will fail if: is not a
valid descriptor open for writing.  The file is  a  regular  file
and  is  greater  than the offset maximum established in the open
file description associated with references a socket, not a file.
is not open for writing.  The named file has its append‐only flag
set, see the manual page for more information.   The  named  file
resides  on  a  read‐only file system.  The system call will fail
if: Search permission is denied for a component of the path  pre‐
fix.  The named file is not writable by the user.  points outside
the  process’s  allocated address space.  The named file is a di‐
rectory.  Too many symbolic links are encountered in  translating
the  pathname.   This is taken to be indicative of a looping sym‐
bolic link.  A component of a pathname exceeds characters, or  an
entire path name exceeds characters.  The named file does not ex‐
ist.   A component of the path prefix is not a directory.  A com‐
ponent of the pathname refers to a directory that requires  mate‐
rialization  and  the I/O policy of the current thread or process
disallows dataless directory materialization The named  file  re‐
sides  on  a read‐only file system.  The file is a pure procedure
(shared text) file that is being executed.  The and system  calls
will  fail  if:  The length argument was greater than the maximum
file size.  A signal is caught during execution.  The argument is
less than 0.  An I/O error occurred while reading from or writing
to a file system.  The file is a file that requires  materializa‐
tion  and  the I/O policy of the current thread or process disal‐
lows dataless file materialization These calls should be general‐
ized to allow ranges of bytes in a file to be discarded.  Use  of
to  extend  a  file  is not portable.  The and function calls ap‐
peared in
