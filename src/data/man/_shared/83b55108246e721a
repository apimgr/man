The  driver  supports  the  IEEE488 general‐purpose interface bus
(GPIB).  GPIB device special file  The  driver  appeared  in  The
user‐level  interface  to the gpib driver current doesn’t do any‐
thing useful.  The GPIB subsystem  places  the  unfortunate  con‐
straint  that  the local GPIB controller is always the controller
in charge (CIC).
