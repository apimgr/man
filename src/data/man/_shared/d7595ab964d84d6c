The  utility reads each operand and searches for sequences of the
form as inserted by SCCS and SCCS‐compatible version control sys‐
tems.  It prints the  remainder  of  the  string  following  this
marker, up to a null character, newline, double quote, backslash,
or  character.  If the option is specified, only the first occur‐
rence of an identification string in each file is  printed.   The
utility  exits 0 if any matches were found, and 1 otherwise.  The
utility conforms to The command appeared in As is not licensed to
distribute this is a rewrite of the command which is part of  and
may not behave exactly the same as that command does.
