SEM(4)                   BSD Kernel Interfaces Manual                   SEM(4)

NNAAMMEE
     sseemm — POSIX semaphores

SSYYNNOOPPSSIISS
     To compile this driver into the kernel, place the following line in your
     kernel configuration file:

           ooppttiioonnss PP11000033__11BB__SSEEMMAAPPHHOORREESS

     Alternatively, to load the driver as a module at boot time, place the
     following line in loader.conf(5):

           sem_load="YES"

     To load the driver as a module at run-time, run the following command as
     root:

           kldload sem

DDEESSCCRRIIPPTTIIOONN
     The sseemm facility provides system calls used by the standard C library
     (_l_i_b_c) to implement POSIX semaphores.  This facility offers support for
     such functions as sseemm__iinniitt() and sseemm__wwaaiitt().  It is available both as a
     kernel option for static inclusion and as a dynamic kernel module.

SSEEEE AALLSSOO
     sem_destroy(3), sem_getvalue(3), sem_init(3), sem_open(3), sem_post(3),
     sem_wait(3), config(8), kldload(8), kldunload(8)

HHIISSTTOORRYY
     The sseemm facility appeared in FreeBSD 5.0.

BSD                            February 7, 2014                            BSD
