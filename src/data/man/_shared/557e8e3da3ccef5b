PMC_ENABLE(3)            BSD Library Functions Manual            PMC_ENABLE(3)

NNAAMMEE
     ppmmcc__ddiissaabbllee, ppmmcc__eennaabbllee — administrative control of hardware performance
     counters

LLIIBBRRAARRYY
     Performance Counters Library (libpmc, -lpmc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ppmmcc..hh>>

     _i_n_t
     ppmmcc__ddiissaabbllee(_i_n_t _c_p_u, _i_n_t _p_m_c);

     _i_n_t
     ppmmcc__eennaabbllee(_i_n_t _c_p_u, _i_n_t _p_m_c);

DDEESSCCRRIIPPTTIIOONN
     These functions allow specific hardware performance monitoring counters
     in a system to be disabled and enabled administratively.  The hardware
     performance counters available on each CPU are numbered using small non-
     negative integers, in a system dependent manner.  Disabled counters will
     not be available to applications for use.

     The invoking process needs to have the PRIV_PMC_MANAGE privilege to per‐
     form these operations.

     Function ppmmcc__ddiissaabbllee() disables the hardware counter numbered by argument
     _p_m_c on CPU number _c_p_u.

     Function ppmmcc__eennaabbllee() enables the hardware counter numbered by argument
     _p_m_c on CPU number _c_p_u.

IIMMPPLLEEMMEENNTTAATTIIOONN NNOOTTEESS
     Hardware PMCs that are currently in use by applications cannot be dis‐
     abled.  Allocation of a process scope software PMC marks all hardware
     PMCs in the system with the same pmc number as being in-use.

RREETTUURRNN VVAALLUUEESS
     Upon successful completion, the value 0 is returned; otherwise the
     value -1 is returned and the global variable _e_r_r_n_o is set to indicate the
     error.

EERRRROORRSS
     A call to these functions may fail with the following errors:

     [EBUSY]            Function ppmmcc__ddiissaabbllee() specified a hardware PMC is
                        currently in use.

     [EINVAL]           Arguments _c_p_u or _p_m_c were invalid.

     [ENXIO]            Argument _c_p_u specified a disabled or absent CPU.

     [EPERM]            The current process lacks sufficient privilege to per‐
                        form this operation.

SSEEEE AALLSSOO
     pmc(3), pmc_cpuinfo(3), pmc_pmcinfo(3), hwpmc(4), pmccontrol(8),
     priv_check(9)

BSD                           September 22, 2008                           BSD
