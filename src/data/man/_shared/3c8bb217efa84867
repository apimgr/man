is  a program that runs on Windows CE.  It loads and executes the
specified kernel.  supports hpcarm,  hpcmips,  and  hpcsh  ports.
Click  on  the button to start the boot process with selected op‐
tions.  Click on the button to exit On this tab  you  can  select
the  kernel  to  boot and options to pass to the kernel.  In this
combobox you specify the directory.  The kernel and miniroot  im‐
age  pathnames  are  taken to be relative to this directory.  can
load kernel and miniroot from FAT and UFS  filesystems,  and  via
HTTP.   In  this text field you specify the name of the kernel to
load.  Kernels compressed with are supported.  Select  your  H/PC
model  in this combobox.  This group of controls lets you specify
the desired root file system type.  You can select and NFS  root.
If  you  select  memory disk root file system, you should specify
the path name of the file system image in the text  field  below.
Miniroot  images  compressed  with  are supported.  This group of
controls is used to pass boot flags to the kernel.  On  this  tab
you  can  specify  miscellaneous  options that mostly control the
program itself.  If this option is  selected  will  automatically
boot  after the specified timeout.  Tells kernel if it should use
the framebuffer in reverse video mode.  If  selected,  a  warning
dialog will be presented anything is done, right after the button
is  pressed.  This option currently does nothing.  If selected, a
warning dialog will be presented the kernel has been  loaded  and
prepared  to be started.  This will be your last chance to cancel
the boot.  In this text field you can specify additional  options
to  pass to the kernel.  This tab gets its name from the big text
area that uses as the to report its progress.   If  checked,  the
progress log will be sent to the specified file instead.  The row
of 8 checkboxes controls debugging options for itself.  They con‐
trol  the bits of an internal variable, the leftmost checkbox be‐
ing the 7th bit.  The buttons to control 4 a developer might want
to use during development.  The utility first appeared  in  reads
the entire kernel image at once, and requires enough free area on
the main memory.
