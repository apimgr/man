compat_freebsd  is  not maintained anymore, and new FreeBSD bina‐
ries cannot be expected to work.  The compat_freebsd  feature  is
available  in  NetBSD  only to support the FreeBSD tw_cli driver.
supports running binaries.  Most  binaries  should  work,  except
programs  that  use features.  These include i386‐specific calls,
such as syscons utilities.  The compatibility feature  is  active
for  kernels compiled with the option enabled.  A lot of programs
are dynamically linked.  This means, that you will also need  the
shared  libraries  that  the  program depends on, and the runtime
linker.  Also, you will need to create a directory  for  binaries
on your system.  This directory is named Any file operations done
by  programs run under will look in this directory first.  So, if
a program opens, for example, will first try to open and if  that
does not exist open the file.  It is recommended that you install
packages  that  include  configuration  files, etc under to avoid
naming conflicts with possible  counterparts.   Shared  libraries
should also be installed in the shadow tree.  Generally, you will
need  to  look  for  the shared libraries that binaries depend on
only the first few times that you install a program on your  sys‐
tem.  After a while, you will have a sufficient set of shared li‐
braries  on your system to be able to run newly imported binaries
without any extra work.  How to get  to  know  which  shared  li‐
braries  binaries  need,  and where to get them? Basically, there
are 2 possibilities (when following these instructions: you  will
need  to  be root on your system to do the necessary installation
steps).  You have access to a system.  In this case you can  tem‐
porarily  install  the binary there, see what shared libraries it
needs, and copy them to your system.  Example: you have just ftp‐
ed the binary of SimCity.  Put it on the system you  have  access
to,  and  check  which  shared  libraries  it  needs  by  running
me@freebsd%    ldd    /usr/local/lib/SimCity/res/sim     /usr/lo‐
cal/lib/SimCity/res/sim:         ‐lXext.6 => /usr/X11R6/lib/libX‐
ext.so.6.0         (0x100c1000)                 ‐lX11.6        =>
/usr/X11R6/lib/libX11.so.6.0   (0x100c9000)           ‐lc.2    =>
/usr/lib/libc.so.2.1      (0x10144000)              ‐lm.2      =>
/usr/lib/libm.so.2.0    (0x101a7000)             ‐lgcc.261     =>
/usr/lib/libgcc.so.261.0  (0x101bf000)  You would need go get all
the files from the last column, and put them under This means you
eventually have these files on your system: Note that if you  al‐
ready have a shared library with a matching major revision number
to  the  first  column  of the output, you won’t need to copy the
file named in the last column to your system, the one you already
have should work.  It is advisable to  copy  the  shared  library
anyway  if it is a newer version, though.  You can remove the old
one.  So, if you have these libraries on  your  system:  and  you
find that the ldd output for a new binary you want to install is:
-lc.2  =>  /usr/lib/libc.so.2.1  (0x10144000)  You  won’t need to
worry about copying too, because the  program  should  work  fine
with  the  slightly older version.  You can decide to replace the
libc.so anyway, and that should leave you with: Finally, you must
make sure that you have the runtime linker and its  config  files
on  your  system.  You should copy these files from the system to
their appropriate place on your system (in the tree):  You  don’t
have  access to a system.  In that case, you should get the extra
files you need from various ftp sites.  Information on  where  to
look for the various files is appended below.  For now, let’s as‐
sume  you  know  where  to get the files.  Retrieve the following
files (from _one_ ftp site to avoid any version mismatches),  and
install  them  under (i.e.  is installed as and don’t necessarily
need to be under you can install them  elsewhere  in  the  system
too.  Just make sure they don’t conflict with their counterparts.
A  good  idea would be to install them in as and Run the ldconfig
program with directory arguments  in  which  the  runtime  linker
should  look  for  shared libs.  are standard, you could run like
the  following:   me@netbsd%   mkdir   ‐p   /emul/freebsd/var/run
me@netbsd% touch /emul/freebsd/var/run/ld.so.hints me@netbsd% ld‐
config‐freebsd  /usr/X11R6/lib  /usr/local/lib Note that argument
directories of ldconfig are mapped to by compat code, and  should
exist as such on your system.  Make sure is existing when you run
ldconfig, if not, you may lose should be statically linked, so it
doesn’t  need any shared libraries by itself.  It will create the
file You should rerun the version of the  ldconfig  program  each
time  you add a new shared library.  You should now be set up for
binaries which only need a shared libc.  You  can  test  this  by
running  the on itself.  Suppose that you have it installed as it
should produce something like: me@netbsd% ldd‐freebsd ‘which ldd‐
freebsd‘     /usr/local/bin/ldd‐freebsd:             ‐lc.2     =>
/usr/lib/libc.so.2.1  (0x1001a000) This being done, you are ready
to install new binaries.  Whenever you install a new program, you
should check if it needs shared libraries, and if so, whether you
have them installed in the tree.  To do this, you run the version
on the new program, and watch its output.  (see also  the  manual
page  for  will print a list of shared libraries that the program
depends on, in the  form  ‐l<majorname>  =>  <fullname>.   If  it
prints  instead of <fullname> it means that you need an extra li‐
brary.  Which library this is, is  shown  in  <majorname>,  which
will   be   of  the  form  XXXX.<N>  You  will  need  to  find  a
libXXXX.so.<N>.<mm> on a ftp site, and install it on your system.
The XXXX (name) and <N> (major revision number) should match; the
minor number(s) <mm> are less important, though it is advised  to
take the most recent version.  In some cases, binary needs access
to certain device file.  For example, X server software needs for
ioctls.   In  this  case, create a symbolic link from to a device
file like You will need to have at least  and  probably  also  in
your  kernel  (see  and  the information below is valid as of the
time this document was written (June, 1995), but certain  details
such  as  names  of ftp sites, directories and distribution names
may have changed by the time you read this.  The distribution  is
available  on  a  lot  of ftp sites.  Sometimes the files are un‐
packed, and you can get the individual files you need, but mostly
they are stored in distribution sets, usually consisting of  sub‐
directories with gzipped tar files in them.  The ftp site for the
distributions  is: This distribution consists of a number of tar‐
ed and gzipped files, Normally, they’re controlled by an  install
program,  but  you can retrieve files too.  The way to look some‐
thing up is to retrieve all the files in  the  distribution,  and
‘‘tar  ztvf’’ through them for the file you need.  Here is an ex‐
ample  of  a  list  of  files  that  you  might   need.    Needed
Files

ld.so                   2.0‐RELEASE/bindist/bindist.??   ldconfig
2.0‐RELEASE/bindist/bindist.??   ldd                      2.0‐RE‐
LEASE/bindist/bindist.??       libc.so.2                  2.0‐RE‐
LEASE/bindist/bindist.??      libX11.so.6.0               2.0‐RE‐
LEASE/XFree86‐3.1/XFree86‐3.1‐bin.tar.gz            libX11.so.6.0
XFree86‐3.1.1/X311bin.tgz     libXt.so.6.0                2.0‐RE‐
LEASE/XFree86‐3.1/XFree86‐3.1‐bin.tar.gz             libXt.so.6.0
XFree86‐3.1.1/X311bin.tgz The files called  are  tar‐ed,  gzipped
and  split, so you can extract contents by Extract the files from
these gzipped tarfiles in your directory  (possibly  omitting  or
afterwards removing files you don’t need), and you are done.  The
information about distributions may become outdated.
