which  NIS server process to use tells the process on the current
machine which server process to communicate with.  If is down  or
is  not  running  a  server process, it is not discovered until a
client process attempts to access a map, at which time tests  the
binding and takes appropriate action.  is most useful for binding
a client that is not on the same broadcast network as the closest
server, but can also be used for debugging a local network’s con‐
figuration,  testing  specific  client  programs, or binding to a
specific server when there are many servers on the local  network
supplying  maps.   The options are as follows: Set the binding on
instead of the local machine.  Use the domain instead of the  de‐
fault domain as returned by
