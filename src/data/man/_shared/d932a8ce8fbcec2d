sets  the  user  ID  and/or  the group ID of the specified files.
Symbolic links named by arguments are silently left unchanged un‐
less is used.  The options are as follows: If the option is spec‐
ified, symbolic links on the command line  are  followed.   (Sym‐
bolic  links encountered in the tree traversal are not followed.)
If the option is specified, all symbolic links are followed.   If
the  option is specified, no symbolic links are followed.  Change
the user ID and/or the group ID for the file  hierarchies  rooted
in the files instead of just the files themselves.  Do not report
any  failure  to  change file owner or group, nor modify the exit
status to reflect such failures.  If  is  a  symbolic  link,  the
owner  and/or group of the link is changed.  Cause to be verbose,
showing files as they are processed.  The and options are ignored
unless the option is specified.  In addition, these options over‐
ride each other and the command’s actions are determined  by  the
last  one  specified.   The  default is as if the option had been
specified.  The option cannot be used together with  the  option.
The  and  operands are both optional, however, one must be speci‐
fied; alternatively, both the owner and group  may  be  specified
using  a  reference specified using the argument.  If the operand
is specified, it must be preceded by a colon  (‘‘:’’)  character.
The  may  be either a user name or a numeric user ID.  The may be
either a group name or a numeric group ID.  Since it is valid  to
have  a user or group name that is numeric (and does not have the
numeric ID that matches its name) the name lookup is always  done
first.   Preceding  an ID with a ‘‘#’’ character will force it to
be taken as a number.  The ownership of a file may  only  be  al‐
tered  by  a super‐user for obvious security reasons.  Unless in‐
voked by the super‐user, clears the set‐user‐id and  set‐group‐id
bits  on  a file to prevent accidental or mischievous creation of
set‐user‐id and set‐group‐id programs.  Previous versions of  the
utility  used  the dot (‘‘.’’) character to distinguish the group
name.  This has been changed to be a colon (‘‘:’’)  character  so
that  user  and  group  names may contain the dot character.  The
command is expected to be POSIX 1003.2 compliant.  The option and
the use of ‘‘#’’ to force a numeric lookup are  extensions  to  A
utility appeared in
