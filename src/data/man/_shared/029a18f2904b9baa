PCAP_LIST_TSTAMP_TYPES(3)  Library Functions Manual  PCAP_LIST_TSTAMP_TYPES(3)



NNAAMMEE
       pcap_list_tstamp_types,  pcap_free_tstamp_types  -  get  a list of time
       stamp types supported by a capture device, and free that list

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ppccaapp//ppccaapp..hh>>

       iinntt ppccaapp__lliisstt__ttssttaammpp__ttyyppeess((ppccaapp__tt **pp,, iinntt ****ttssttaammpp__ttyyppeesspp));;
       vvooiidd ppccaapp__ffrreeee__ttssttaammpp__ttyyppeess((iinntt **ttssttaammpp__ttyyppeess));;

DDEESSCCRRIIPPTTIIOONN
       ppccaapp__lliisstt__ttssttaammpp__ttyyppeess() is used to get a list of  the  supported  time
       stamp  types  of  the  interface  associated  with the pcap descriptor.
       ppccaapp__lliisstt__ttssttaammpp__ttyyppeess() allocates an array to hold the list  and  sets
       _*_t_s_t_a_m_p___t_y_p_e_s_p to point to the array.  See ppccaapp--ttssttaammpp(7) for a list of
       all the time stamp types.

       The   caller   is   responsible   for   freeing    the    array    with
       ppccaapp__ffrreeee__ttssttaammpp__ttyyppeess(),   which   frees   the   list  pointed  to  by
       _t_s_t_a_m_p___t_y_p_e_s.

RREETTUURRNN VVAALLUUEE
       ppccaapp__lliisstt__ttssttaammpp__ttyyppeess() returns the number of time stamp types in  the
       array  on  success  and  PPCCAAPP__EERRRROORR  on failure.  A return value of one
       means that the only time stamp type supported is the one in  the  list,
       which  is the capture device's default time stamp type.  A return value
       of  zero  means  that  the  only   time   stamp   type   supported   is
       PPCCAAPP__TTSSTTAAMMPP__HHOOSSTT, which is the capture device's default time stamp type
       (only older versions of libpcap will return that; newer  versions  will
       always   return  one  or  more  types).   If  PPCCAAPP__EERRRROORR  is  returned,
       ppccaapp__ggeetteerrrr(3) or ppccaapp__ppeerrrroorr(3) may be called with _p as an argument to
       fetch or display the error text.

BBAACCKKWWAARRDD CCOOMMPPAATTIIBBIILLIITTYY
       These functions became available in libpcap release 1.2.1.  In previous
       releases, the time stamp type cannot be  set;  only  the  default  time
       stamp type offered by a capture source is available.

SSEEEE AALLSSOO
       ppccaapp(3), ppccaapp__ttssttaammpp__ttyyppee__vvaall__ttoo__nnaammee(3), ppccaapp--ttssttaammpp(7)



                               8 September 2019      PCAP_LIST_TSTAMP_TYPES(3)
