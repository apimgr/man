file ...  file ...  file ...  The utility merges several CTF data
sections from several files into one output file, unifying common
data.   The following options are available: Match global symbols
to global CTF data.  Don’t delete  the  original  debugging  sec‐
tions.   Use  the  .dynsym ELF section instead of the .symtab ELF
section.  Make sure that all object files  have  a  CTF  section.
Enable verbose mode.  Sets the label as Instructs to read the la‐
bel  from  the  environment  variable Use to store the merged CTF
data.  Uniquify against Uniquify  against  label  Additive  merge
with  Copy  CTF  data from into The utility first appeared in The
CTF utilities came from OpenSolaris.
