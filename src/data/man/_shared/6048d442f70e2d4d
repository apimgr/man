The  driver  attaches  to USB modems, serial ports, and other de‐
vices that need to look like a tty.  The driver shows a behaviour
like a This means that normal programs such as or can be used  to
access  the device.  The locator can be used to decide which port
to use for device that have multiple external ports.   Note  that
while  supports  the (undocumented) pulse‐per‐second API normally
used on conventional serial ports, USB serial  devices  typically
have  a  varying  latency around 1 ms due to the USB frame struc‐
ture.  The ttyXX devices are  traditional  dial‐in  devices;  the
dtyXX devices are used for dial‐out.  (See The driver appeared in
