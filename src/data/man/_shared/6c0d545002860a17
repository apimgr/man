_l_o_g_b(3)                    Library Functions Manual                    _l_o_g_b(3)

NNAAMMEE
       logb, logbf, logbl - get exponent of a floating‐point value

LLIIBBRRAARRYY
       Math library (_l_i_b_m, _-_l_m)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<mmaatthh..hh>>

       ddoouubbllee llooggbb((ddoouubbllee _x));;
       ffllooaatt llooggbbff((ffllooaatt _x));;
       lloonngg ddoouubbllee llooggbbll((lloonngg ddoouubbllee _x));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       llooggbb():
           _ISOC99_SOURCE || _POSIX_C_SOURCE >= 200112L
               || _XOPEN_SOURCE >= 500
               || /* Since glibc 2.19: */ _DEFAULT_SOURCE
               || /* glibc <= 2.19: */ _BSD_SOURCE || _SVID_SOURCE

       llooggbbff(), llooggbbll():
           _ISOC99_SOURCE || _POSIX_C_SOURCE >= 200112L
               || /* Since glibc 2.19: */ _DEFAULT_SOURCE
               || /* glibc <= 2.19: */ _BSD_SOURCE || _SVID_SOURCE

DDEESSCCRRIIPPTTIIOONN
       These  functions  extract the exponent from the internal floating‐point
       representation of _x and return it as a floating‐point value.  The inte‐
       ger constant FFLLTT__RRAADDIIXX, defined in _<_f_l_o_a_t_._h_>, indicates the radix  used
       for  the  system’s  floating‐point  representation.  If FFLLTT__RRAADDIIXX is 2,
       llooggbb((_x)) is similar to fflloooorr((lloogg22((ffaabbss((_x)))))), except that the latter  may
       give an incorrect integer due to intermediate rounding.

       If  _x is subnormal, llooggbb() returns the exponent _x would have if it were
       normalized.

RREETTUURRNN VVAALLUUEE
       On success, these functions return the exponent of _x.

       If _x is a NaN, a NaN is returned.

       If _x is zero, then a  pole  error  occurs,  and  the  functions  return
       -HHUUGGEE__VVAALL, -HHUUGGEE__VVAALLFF, or -HHUUGGEE__VVAALLLL, respectively.

       If  _x is negative infinity or positive infinity, then positive infinity
       is returned.

EERRRROORRSS
       See mmaatthh__eerrrroorr(7) for information on how to determine whether an  error
       has occurred when calling these functions.

       The following errors can occur:

       Pole error: _x is 0
              A  divide‐by‐zero  floating‐point  exception  (FFEE__DDIIVVBBYYZZEERROO)  is
              raised.

       These functions do not set _e_r_r_n_o.

AATTTTRRIIBBUUTTEESS
       For an explanation of the terms  used  in  this  section,  see  aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       llooggbb(), llooggbbff(), llooggbbll() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       C11, POSIX.1‐2008.

HHIISSTTOORRYY
       C99, POSIX.1‐2001.

       llooggbb() 4.3BSD (see IEEE.3 in the 4.3BSD manual).

SSEEEE AALLSSOO
       iillooggbb(3), lloogg(3)

Linux man‐pages (unreleased)      2025‐05‐17                           _l_o_g_b(3)
