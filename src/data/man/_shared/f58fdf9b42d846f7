_P_R_O_V_I_D_E_R(7)                         OpenSSL                        _P_R_O_V_I_D_E_R(7)

NNAAMMEE
       provider - OpenSSL operation implementation providers

LLIIBBRRAARRYY
       libcrypto, ‐lcrypto

SSYYNNOOPPSSIISS
       #include <openssl/provider.h>

DDEESSCCRRIIPPTTIIOONN
   GGeenneerraall
       This page contains information useful to provider authors.

       A _p_r_o_v_i_d_e_r, in OpenSSL terms, is a unit of code that provides one or
       more implementations for various operations for diverse algorithms that
       one might want to perform.

       An _o_p_e_r_a_t_i_o_n is something one wants to do, such as encryption and
       decryption, key derivation, MAC calculation, signing and verification,
       etc.

       An _a_l_g_o_r_i_t_h_m is a named method to perform an operation.  Very often,
       the algorithms revolve around cryptographic operations, but may also
       revolve around other types of operation, such as managing certain types
       of objects.

       See ccrryyppttoo(7) for further details.

   PPrroovviiddeerr
       A _p_r_o_v_i_d_e_r offers an initialization function, as a set of base
       functions in the form of an OOSSSSLL__DDIISSPPAATTCCHH(3) array, and by extension, a
       set of OOSSSSLL__AALLGGOORRIITTHHMM(3)s (see ooppeennssssll--ccoorree..hh(7)).  It may be a
       dynamically loadable module, or may be built‐in, in OpenSSL libraries
       or in the application.  If it’s a dynamically loadable module, the
       initialization function must be named "OSSL_provider_init" and must be
       exported.  If it’s built‐in, the initialization function may have any
       name.

       The initialization function must have the following signature:

        int NAME(const OSSL_CORE_HANDLE *handle,
                 const OSSL_DISPATCH *in, const OSSL_DISPATCH **out,
                 void **provctx);

       _h_a_n_d_l_e is the OpenSSL library object for the provider, and works as a
       handle for everything the OpenSSL libraries need to know about the
       provider.  For the provider itself, it is passed to some of the
       functions given in the dispatch array _i_n.

       _i_n is a dispatch array of base functions offered by the OpenSSL
       libraries, and the available functions are further described in
       pprroovviiddeerr--bbaassee(7).

       _*_o_u_t must be assigned a dispatch array of base functions that the
       provider offers to the OpenSSL libraries.  The functions that may be
       offered are further described in pprroovviiddeerr--bbaassee(7), and they are the
       central means of communication between the OpenSSL libraries and the
       provider.

       _*_p_r_o_v_c_t_x should be assigned a provider specific context to allow the
       provider multiple simultaneous uses.  This pointer will be passed to
       various operation functions offered by the provider.

       Note that the provider will not be made available for applications to
       use until the initialization function has completed and returned
       successfully.

       One of the functions the provider offers to the OpenSSL libraries is
       the central mechanism for the OpenSSL libraries to get access to
       operation implementations for diverse algorithms.  Its referred to with
       the number OOSSSSLL__FFUUNNCC__PPRROOVVIIDDEERR__QQUUEERRYY__OOPPEERRAATTIIOONN and has the following
       signature:

        const OSSL_ALGORITHM *provider_query_operation(void *provctx,
                                                       int operation_id,
                                                       const int *no_store);

       _p_r_o_v_c_t_x is the provider specific context that was passed back by the
       initialization function.

       _o_p_e_r_a_t_i_o_n___i_d is an operation identity (see "Operations" below).

       _n_o___s_t_o_r_e is a flag back to the OpenSSL libraries which, when nonzero,
       signifies that the OpenSSL libraries will not store a reference to the
       returned data in their internal store of implementations.

       The returned OOSSSSLL__AALLGGOORRIITTHHMM(3) is the foundation of any OpenSSL library
       API that uses providers for their implementation, most commonly in the
       _f_e_t_c_h_i_n_g type of functions (see "ALGORITHM FETCHING" in ccrryyppttoo(7)).

   OOppeerraattiioonnss
       Operations are referred to with numbers, via macros with names starting
       with "OSSL_OP_".

       With each operation comes a set of defined function types that a
       provider may or may not offer, depending on its needs.

       Currently available operations are:

       Digests
           In  the  OpenSSL  libraries,  the  corresponding  method  object is
           EEVVPP__MMDD.  The number for  this  operation  is  OOSSSSLL__OOPP__DDIIGGEESSTT.   The
           functions    the    provider    can    offer   are   described   in
           pprroovviiddeerr--ddiiggeesstt(7).

       Symmetric ciphers
           In the  OpenSSL  libraries,  the  corresponding  method  object  is
           EEVVPP__CCIIPPHHEERR.   The number for this operation is OOSSSSLL__OOPP__CCIIPPHHEERR.  The
           functions   the   provider   can    offer    are    described    in
           pprroovviiddeerr--cciipphheerr(7).

       Message Authentication Code (MAC)
           In  the  OpenSSL  libraries,  the  corresponding  method  object is
           EEVVPP__MMAACC.  The  number  for  this  operation  is  OOSSSSLL__OOPP__MMAACC.   The
           functions the provider can offer are described in pprroovviiddeerr--mmaacc(7).

       Key Derivation Function (KDF)
           In  the  OpenSSL  libraries,  the  corresponding  method  object is
           EEVVPP__KKDDFF.  The  number  for  this  operation  is  OOSSSSLL__OOPP__KKDDFF.   The
           functions the provider can offer are described in pprroovviiddeerr--kkddff(7).

       Key Exchange
           In  the  OpenSSL  libraries,  the  corresponding  method  object is
           EEVVPP__KKEEYYEEXXCCHH.  The number for  this  operation  is  OOSSSSLL__OOPP__KKEEYYEEXXCCHH.
           The   functions   the   provider   can   offer   are  described  in
           pprroovviiddeerr--kkeeyyeexxcchh(7).

       Asymmetric Ciphers
           In the  OpenSSL  libraries,  the  corresponding  method  object  is
           EEVVPP__AASSYYMM__CCIIPPHHEERR.     The    number    for    this    operation   is
           OOSSSSLL__OOPP__AASSYYMM__CCIIPPHHEERR.  The functions  the  provider  can  offer  are
           described in pprroovviiddeerr--aassyymm__cciipphheerr(7).

       Asymmetric Key Encapsulation
           In  the  OpenSSL  libraries,  the  corresponding  method  object is
           EEVVPP__KKEEMM.  The  number  for  this  operation  is  OOSSSSLL__OOPP__KKEEMM.   The
           functions the provider can offer are described in pprroovviiddeerr--kkeemm(7).

       Encoding
           In  the  OpenSSL  libraries,  the  corresponding  method  object is
           OOSSSSLL__EENNCCOODDEERR.  The number for this  operation  is  OOSSSSLL__OOPP__EENNCCOODDEERR.
           The   functions   the   provider   can   offer   are  described  in
           pprroovviiddeerr--eennccooddeerr(7).

       Decoding
           In the  OpenSSL  libraries,  the  corresponding  method  object  is
           OOSSSSLL__DDEECCOODDEERR.   The  number  for this operation is OOSSSSLL__OOPP__DDEECCOODDEERR.
           The  functions  the   provider   can   offer   are   described   in
           pprroovviiddeerr--ddeeccooddeerr(7).

       Random Number Generation
           The  number  for this operation is OOSSSSLL__OOPP__RRAANNDD.  The functions the
           provider can offer for random number generation  are  described  in
           pprroovviiddeerr--rraanndd(7).

       Key Management
           The  number  for  this operation is OOSSSSLL__OOPP__KKEEYYMMGGMMTT.  The functions
           the  provider  can  offer  for  key  management  are  described  in
           pprroovviiddeerr--kkeeyymmggmmtt(7).

       Signing and Signature Verification
           The  number for this operation is OOSSSSLL__OOPP__SSIIGGNNAATTUURREE.  The functions
           the provider can offer for  digital  signatures  are  described  in
           pprroovviiddeerr--ssiiggnnaattuurree(7).

       Store Management
           The  number for this operation is OOSSSSLL__OOPP__SSTTOORREE.  The functions the
           provider  can  offer  for  store  management   are   described   in
           pprroovviiddeerr--ssttoorreemmggmmtt(7).

       _A_l_g_o_r_i_t_h_m _n_a_m_i_n_g

       Algorithm names are case insensitive. Any particular algorithm can have
       multiple  aliases  associated  with  it.  The  canonical OpenSSL naming
       scheme follows this format:

       ALGNAME[VERSION?][-SUBNAME[VERSION?]?][-SIZE?][-MODE?]

       VERSION is only present if there are multiple versions of an  algorithm
       (e.g.  MD2, MD4, MD5).  It may be omitted if there is only one version.

       SUBNAME may be present where multiple algorithms are combined together,
       e.g. MD5-SHA1.

       SIZE  is  only  present if multiple versions of an algorithm exist with
       different sizes (e.g. AES-128-CBC, AES-256-CBC)

       MODE is only present where applicable.

       Other aliases may exist for example where standards  bodies  or  common
       practice   use  alternative  names  or  names  that  OpenSSL  has  used
       historically.

OOPPEENNSSSSLL PPRROOVVIIDDEERRSS
       OpenSSL provides a number of its own providers. These are the  default,
       base, fips, legacy and null providers. See ccrryyppttoo(7) for an overview of
       these providers.

SSEEEE AALLSSOO
       EEVVPP__DDiiggeessttIInniitt__eexx(3),      EEVVPP__EEnnccrryyppttIInniitt__eexx(3),      OOSSSSLL__LLIIBB__CCTTXX(3),
       EEVVPP__sseett__ddeeffaauulltt__pprrooppeerrttiieess(3),  EEVVPP__MMDD__ffeettcchh(3),   EEVVPP__CCIIPPHHEERR__ffeettcchh(3),
       EEVVPP__KKEEYYMMGGMMTT__ffeettcchh(3),        ooppeennssssll--ccoorree..hh(7),       pprroovviiddeerr--bbaassee(7),
       pprroovviiddeerr--ddiiggeesstt(7), pprroovviiddeerr--cciipphheerr(7), pprroovviiddeerr--kkeeyyeexxcchh(7)

HHIISSTTOORRYY
       The concept of providers and everything surrounding them was introduced
       in OpenSSL 3.0.

CCOOPPYYRRIIGGHHTT
       Copyright 2019-2022 The OpenSSL Project Authors. All Rights Reserved.

       Licensed under the Apache License 2.0 (the "License").  You may not use
       this file except in compliance with the License.  You can obtain a copy
       in   the   file   LICENSE   in   the   source   distribution   or    at
       <https://www.openssl.org/source/license.html>.

3.0.12                            2023‐05‐07                       _P_R_O_V_I_D_E_R(7)
