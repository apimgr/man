The  utility looks up records in the databases maintained by sev‐
eral Network Information Centers The options are as follows:  Use
the  Asia/Pacific  Network  Information Center database.  It con‐
tains network numbers used in East Asia, Australia, New  Zealand,
and  the Pacific islands.  Use the American Registry for Internet
Numbers database.  It contains  network  numbers  used  in  those
parts  of the world covered neither by nor by (Hint: All point of
contact handles in the whois database end with This is the equiv‐
alent of using the option with an argument of Use the US  Depart‐
ment of Defense database.  It contains points of contact for sub‐
domains  of  Use the African Network Information Center database.
It contains network numbers used in Africa.  Use the US non‐mili‐
tary federal government database, which contains points  of  con‐
tact  for subdomains of Use the specified host instead of the de‐
fault NIC (whois.crsnic.net).  Either a host name or  an  IP  ad‐
dress  may  be  specified.   By  default constructs the name of a
whois server to use from the top‐level  domain  of  the  supplied
(single)  argument.   For newer generic domains (gTLDs), a lookup
for whois.nic.tld is attempted.   For  other  TLDs,  or  if  this
lookup  fails, is appended to the TLD.  This effectively allows a
suitable whois server to be selected automatically  for  a  large
number of In the event that an IP address is specified, the whois
server will default to the American Registry for Internet Numbers
If  a  query  to  references or that server will be queried also,
provided that the option is not specified.  If the query is not a
domain name or IP address, will fall back to Use the Internet As‐
signed Numbers Authority root zone database.  It contains  infor‐
mation  about  top‐level domains.  Use the Network Solutions Reg‐
istry for Internet Numbers database.  Historically, it  contained
network  numbers  and  domain contact information for most of and
domains.  However, the registration of these domains is now  done
by  a  number  of  independent  and competing registrars and this
database holds no information on the domains registered by  orga‐
nizations other than Network Solutions, Inc.  Also, note that the
database is no longer handled by Network Solutions, Inc.  For de‐
tails, see (Hint: Contact information, identified by the term can
be  looked  up  by prefixing or to the handle in the query.)  Use
the Latin American and Caribbean  IP  address  Regional  Registry
database.   It  contains  network  numbers  used in much of Latin
America and the Caribbean.  Use the Route Arbiter Database  data‐
base.  It contains route policy specifications for a large number
of  operators’  networks.   Use the PeeringDB database of AS num‐
bers.  It contains details about  presence  at  internet  peering
points  for  many network operators.  Connect to the whois server
on If this option is not specified, defaults to the  port  listed
in  (port  43).  Do a quick lookup.  This means that will not at‐
tempt to lookup the name in the authoritative  whois  server  (if
one  is  listed)  nor will it contact InterNic if a lookup fails.
This flag has no effect when combined with any other  flag.   Use
the Russia Network Information Center database.  It contains net‐
work  numbers  and  domain  contact information for subdomains of
This option is deprecated; use the option with an argument of in‐
stead.  Use the R´eseaux IP  Europ´eens  database.   It  contains
network  numbers  and domain contact information for Europe.  The
default action, unless directed otherwise with a special is to do
a very broad search, looking for  matches  to  in  all  types  of
records  and most fields (name, nicknames, hostname, net address,
etc.) in the database.  For more information as to what  operands
have  special  meaning, and how to guide the search, use the spe‐
cial name Queries beginning with an exclamation point are assumed
to be contact handles.  Unless a host or domain is  specified  on
the  command  line,  will  be  used  as the database.  Similarly,
queries beginning with are assumed to be contact handles.  Unless
a host or domain is specified on the command line, will  be  used
as  the  database.   Most types of data, such as domain names and
addresses, can be used as arguments to without any  options,  and
will  choose the correct whois server to query.  Some exceptions,
where will not be able to handle data correctly, are detailed be‐
low.  To obtain contact information about  an  administrator  lo‐
cated  in  the Russian domain use the option as shown in the fol‐
lowing example, where is  substituted  with  the  actual  contact
identifier.  (Note: This example is specific to the but other can
be  queried  by  using  a similar syntax.)  The following example
demonstrates how to query a whois  server  using  a  non‐standard
port,  where  is the query to be sent to on port (written numeri‐
cally as 4321).  The command appeared in
