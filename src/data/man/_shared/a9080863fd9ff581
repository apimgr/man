REMOVE(3)                BSD Library Functions Manual                REMOVE(3)

NNAAMMEE
     rreemmoovvee — remove directory entry

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssttddiioo..hh>>

     _i_n_t
     rreemmoovvee(_c_o_n_s_t _c_h_a_r _*_p_a_t_h);

DDEESSCCRRIIPPTTIIOONN
     The rreemmoovvee() function removes the file or directory specified by _p_a_t_h.

     If _p_a_t_h specifies a directory, rreemmoovvee(_p_a_t_h) is the equivalent of
     rrmmddiirr(_p_a_t_h).  Otherwise, it is the equivalent of uunnlliinnkk(_p_a_t_h).

RREETTUURRNN VVAALLUUEESS
     The rreemmoovvee() function returns the value 0 if successful; otherwise the
     value -1 is returned and the global variable _e_r_r_n_o is set to indicate the
     error.

EERRRROORRSS
     The rreemmoovvee() function may fail and set _e_r_r_n_o for any of the errors speci‐
     fied for the routines lstat(2), rmdir(2) or unlink(2).

SSEEEE AALLSSOO
     rmdir(2), unlink(2)

SSTTAANNDDAARRDDSS
     The rreemmoovvee() function conforms to ISO/IEC 9899:1990 (“ISO C90”) and
     X/Open Portability Guide Issue 4, Version 2 (“XPG4.2”).

BSD                              June 4, 1993                              BSD
