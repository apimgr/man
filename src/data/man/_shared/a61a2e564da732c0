Jpeg2ktopam User Manual(1)  General Commands Manual Jpeg2ktopam User Manual(1)



NNAAMMEE
       jpeg2ktopam - convert JPEG-2000 code stream to PAM/PNM


SSYYNNOOPPSSIISS
       jjppeegg22kkttooppaamm [--vveerrbboossee] [--ddeebbuugglleevveell==_n_u_m_b_e_r] _f_i_l_e_n_a_m_e


OOPPTTIIOONN UUSSAAGGEE
       Minimum  unique abbreviation of option is acceptable.  You may use dou‐
       ble hyphens instead of single hyphen to denote options.   You  may  use
       white space in place of the equals sign to separate an option name from
       its value.


DDEESSCCRRIIPPTTIIOONN
       This program is part of NNeettppbbmm(1).

       jjppeegg22kkttooppaamm converts the named JPEG-2000 file (JP2 or JPC), or Standard
       Input  if no file is named, to a PBM, PGM, PPM, or PAM file on Standard
       Output.

       The JPEG-2000 specification specifies two different  formats:  JP2  and
       JPEG-2000  code  stream  (JPC).   JP2  represents  a visual image quite
       specifically, whereas JPC is a more or less arbitrary array  of  codes.
       A  JP2  image  contains  a  JPC stream and metadata describing how that
       stream represents a visual image.  jjppeegg22kkttooppaamm converts both.

       If the color space  identified  in  the  image  is  grayscale  (JAS_IM‐
       AGE_CS_GRAY),  jjppeegg22kkttooppaamm generates a PGM image, unless the image con‐
       tains only one bit per pixel, in which case jjppeegg22kkttooppaamm generates  PBM.
       If  the  color space is RGB (JAS_IMAGE_CS_RGB), jjppeegg22kkttooppaamm generates a
       PPM image.  If the input image is anything else, jjppeegg22kkttooppaamm  generates
       a PAM image with no tuple type identified.

       In  the  PGM and PPM cases, jjppeegg22kkttooppaamm assumes the intensity values in
       the input image have the same meaning as for PGM and  PPM.   One  thing
       that  implies  is  the  ITU-R  Recommendation BT.709 color space, which
       means the assumption is false for JP2 input.  JP2 input uses SRGB color
       encoding.   So if you use jjppeegg22kkttooppaamm to convert a JP2 image to PPM, it
       changes the visual image (slightly) -- the colors in the output are not
       the same as in the input.

       In  the  PAM image, the output samples are numerically identical to the
       input samples.  If the input samples are signed, they  are  represented
       in  two's complement form in the output (because PAM samples are always
       unsigned).  The PAM plane numbers are identical to  the  JPC  component
       numbers.

       A  JPEG-2000  image has a "precision," which is the number of bits used
       for each code (in Netpbm lingo, "sample").   Actually,  each  component
       has  a  separate precision.  The maxval of a PGM, PPM, or PAM output is
       the largest number you can represent in the JPEG-2000 precision of  the
       JPEG-2000  component  with  the greatest precision.  The samples in all
       components are scaled to that maxval.  So if the red  component  has  a
       precision  of 4 bits and the green component has a precision of 6 bits,
       the maxval is 63 and the red component codes from the  JPEG-2000  image
       are multiplied by 63/15 to generate the output samples.

       jjppeegg22kkttooppaamm  interprets  the  JPEG-2000 input with the Jasper JPEG-2000
       library ⟨http://www.ece.uvic.ca/~mdadams/jasper/⟩ .  See  documentation
       of  the library for details on what jjppeegg22kkttooppaamm handles.  Note that the
       Jasper  library  contains  facilities  for  writing  PNM  images,   but
       jjppeegg22kkttooppaamm  does  not  use those.  It uses the Netpbm library instead.
       Note that the makers of the  Jasper  library  write  it  "JasPer,"  but
       Netpbm  documentation  follows  standard  American  English  typography
       rules, which don't allow that kind of capitalization.

       Use ppaammttoojjppeegg22kk to convert in the other direction.

       The program jjaassppeerr, which is packaged with  the  Jasper  JPEG-2000  li‐
       brary,  also  converts between JPEG-2000 and PNM formats.  Because it's
       packaged with the library, it may exploit  it  better,  especially  re‐
       cently  added  features.  However, since it does not use the Netpbm li‐
       brary to read and write the Netpbm formats, it doesn't do as good a job
       on that side.


OOPPTTIIOONNSS
       In  addition  to  the options common to all programs based on libnetpbm
       (most notably --qquuiieett, see
        Common Options ⟨index.html#commonoptions⟩  ),  jjppeegg22kkttooppaamm  recognizes
       the following command line options:




       --vveerrbboossee
              This  option  causes jjppeegg22kkttooppaamm to issue informational messages
              about the conversion process.


       --ddeebbuugglleevveell=_n_u_m_b_e_r
              This option controls debug messages  from  the  Jasper  library.
              jjppeegg22kkttooppaamm  passes  _n_u_m_b_e_r  as  the  debug  level to the Jasper
              JPEG-2000 decoder.




EEXXAAMMPPLLEESS
         jpeg2ktopam myimg.jpc >myimg.ppm



AABBOOUUTT JJPPEEGG--22000000
       See tthhee ppaammttoojjppeegg22kk mmaannuuaall(1) for general information on JPEG-2000 com‐
       pression and the JPEG-2000 formats.


SSEEEE AALLSSOO
       ppaammttoojjppeegg22kk(1), jjppeeggttooppnnmm(1), ppppmm(1), ppggmm(1), ppbbmm(1), ppaamm(1),


HHIISSTTOORRYY
       jjppeegg22kkttooppaamm was added to Netpbm in Release 10.12 (November 2002).

       Before  Netpbm  10.49  (December 2009), jjppeegg22kkttooppaamm could not convert a
       JP2 file -- only JPC.

DDOOCCUUMMEENNTT SSOOUURRCCEE
       This manual page was generated by the Netpbm tool 'makeman'  from  HTML
       source.  The master documentation is at

              hhttttpp::////nneettppbbmm..ssoouurrcceeffoorrggee..nneett//ddoocc//jjppeegg22kkttooppaamm..hhttmmll

netpbm documentation            08 October 2009     Jpeg2ktopam User Manual(1)
