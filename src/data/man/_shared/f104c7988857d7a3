mount requests is the server for mount requests from other client
machines.   listens for service requests at the port indicated in
the server specification; see RFC 1094, Appendix A  and  Appendix
I.   Options  and  operands  available for Enable debugging mode.
will not detach from the controlling terminal and will print  de‐
bugging  messages to stderr.  Do not require privileged ports for
mount or NFS RPC calls.  This option is equivalent to  specifying
on  every export.  See for more information.  Some operating sys‐
tems (notably Mac OS X) require this option.   IPsec  string,  as
described  in  Multiple  IPsec policy strings may be specified by
using a semicolon as a separator.  If conflicting policy  strings
are found in a single line, the last string will take effect.  If
an  invalid IPsec policy string is used logs an error message and
terminates itself.  Force to bind to the given port.  If this op‐
tion is not given, may bind to every anonymous port (in the range
600‐1023) which causes trouble when trying to use NFS  through  a
firewall.  The argument specifies an alternative location for the
exports  file.   Multiple  exports files can be defined.  When is
started, it loads the export host addresses and options into  the
kernel using the system call.  After changing the exports file, a
hangup  signal  should  be sent to the daemon to get it to reload
the export information.  After sending the SIGHUP  (kill  -s  HUP
‘cat  /var/run/mountd.pid‘),  check  the  syslog output to see if
logged any parsing errors in the exports file.   After  receiving
SIGTERM,  sends a broadcast request to remove the mount list from
all the clients.  This can take a long time, since the  broadcast
request  waits  for each client to respond.  the list of exported
filesystems the pid of the currently running the list of remotely
mounted filesystems The utility first appeared in
