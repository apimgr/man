VM_MAP_SUBMAP(9)         BSD Kernel Developer's Manual        VM_MAP_SUBMAP(9)

NNAAMMEE
     vvmm__mmaapp__ssuubbmmaapp — create a subordinate map

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ppaarraamm..hh>>
     ##iinncclluuddee <<vvmm//vvmm..hh>>
     ##iinncclluuddee <<vvmm//vvmm__mmaapp..hh>>

     _i_n_t
     vvmm__mmaapp__ssuubbmmaapp(_v_m___m_a_p___t _m_a_p, _v_m___o_f_f_s_e_t___t _s_t_a_r_t, _v_m___o_f_f_s_e_t___t _e_n_d,
         _v_m___m_a_p___t _s_u_b_m_a_p);

DDEESSCCRRIIPPTTIIOONN
     The vvmm__mmaapp__ssuubbmmaapp() function marks the range bounded by _s_t_a_r_t and _e_n_d
     within the map _m_a_p as being handled by a subordinate map _s_u_b___m_a_p.

     It is generally called by the kernel memory allocator.

IIMMPPLLEEMMEENNTTAATTIIOONN NNOOTTEESS
     This function is for internal use only.

     Both maps must exist.  The range must have been created with
     vm_map_find(9) previously.

     No other operations may have been performed on this range before calling
     this function.  Only the vvmm__ffaauulltt() operation may be performed within
     this range after calling this function.

     To remove a submapping, one must first remove the range from the parent
     _m_a_p, and then destroy the _s_u_b___m_a_p.  This procedure is not recommended.

RREETTUURRNN VVAALLUUEESS
     The vvmm__mmaapp__ssuubbmmaapp() function returns KERN_SUCCESS if successful.

     Otherwise, it returns KERN_INVALID_ARGUMENT if the caller requested copy-
     on-write flags, or if the range specified for the sub-map was out of
     range for the parent map, or if a NULL backing object was specified.

SSEEEE AALLSSOO
     vm_map(9), vm_map_find(9)

AAUUTTHHOORRSS
     This manual page was written by Bruce M Simpson <_b_m_s_@_s_p_c_._o_r_g>.

BSD                              July 19, 2003                             BSD
