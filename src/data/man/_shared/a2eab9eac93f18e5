NG_ETHER_ECHO(4)         BSD Kernel Interfaces Manual         NG_ETHER_ECHO(4)

NNAAMMEE
     nngg__eetthheerr__eecchhoo — netgraph ether_echo node type

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<nneettggrraapphh//nngg__eetthheerr__eecchhoo..hh>>

DDEESSCCRRIIPPTTIIOONN
     The eetthheerr__eecchhoo node type reflects all data and control messages back to
     the sender.  It assumes (and does not check) that the packet is an ether‐
     net frame, and swaps the source and destination addresses before echoing
     it.  This node type is used for testing and debugging.

HHOOOOKKSS
     A eetthheerr__eecchhoo node accepts any request to connect, regardless of the hook
     name, as long as the name is unique.

CCOONNTTRROOLL MMEESSSSAAGGEESS
     This node type supports only the generic control messages.  Any other
     control messages are reflected back to the sender.

SSHHUUTTDDOOWWNN
     This node shuts down upon receipt of a NGM_SHUTDOWN control message, or
     when all hooks have been disconnected.

SSEEEE AALLSSOO
     netgraph(4), ng_echo(4), ng_ether(4), ng_hole(4), ngctl(8)

HHIISSTTOORRYY
     The nngg__eetthheerr__eecchhoo node type was implemented in FreeBSD 8.0.

AAUUTTHHOORRSS
     Julian Elischer <_j_u_l_i_a_n_@_F_r_e_e_B_S_D_._o_r_g>

BSD                            December 24, 2008                           BSD
