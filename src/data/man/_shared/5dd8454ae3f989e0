The  system call sends a regular file specified by descriptor out
a stream socket specified by descriptor  The  argument  specifies
where  to begin in the file.  Should fall beyond the end of file,
the system will return success and report 0  bytes  sent  as  de‐
scribed  below.   The  argument is a value‐result parameter, that
specifies how many bytes of the file should be  sent  and/or  how
many bytes have been sent.  Initially the value pointed to by the
argument  specifies  how  many bytes should be sent with 0 having
the special meaning to send  until  the  end  of  file  has  been
reached.   On  return  the value pointed to by the argument indi‐
cates how many bytes have been sent,  except  when  a  header  or
trailer  is  specified  as  shown  below.  The pointer may not be
NULL.  An optional header and/or trailer can be sent  before  and
after  the  file  data by specifying a pointer to a which has the
following structure: struct sf_hdtr {         struct iovec *head‐
ers;  /*   pointer    to    header    iovecs    */            int
hdr_cnt;            /*  number of header iovecs */         struct
iovec *trailers; /* pointer  to  trailer  iovecs  */          int
trl_cnt;            /*  number  of  trailer  iovecs */ }; The and
pointers, if point to arrays of structures.  See the system  call
for  information on the iovec structure.  The number of iovecs in
these arrays is specified by and When  a  header  or  trailer  is
specified,  the value of argument indicates the maximum number of
bytes in the header and/or file to be sent.  It does not  control
the trailer; if a trailer exists, all of it will be sent.  If the
value  of  argument  is  0, all of the header and/or file will be
sent before the entire trailer is sent.  On return, the  argument
specifies  the  total number of bytes sent.  The parameter is re‐
served for future expansion and must be set to 0. Any other value
will cause to return When using a socket marked for  non‐blocking
I/O, may send fewer bytes than requested.  In this case, the num‐
ber of bytes successfully sent is returned in the via the parame‐
ters  and  the error is returned.  When a signal causes to return
the error the argument may return 0 without  necessarily  meaning
the  end  of  file  has  been reached as the signal may have been
caught before any data was sent.  The Mac OS X implementation  of
uses  64 bits types for size and offset parameters so there is no
need for a 64 bits version as found on some other operating  sys‐
tems.   The  number of bytes sent is returned via the parameter A
value of 0 means the end of the file specified by descriptor  has
been  reached or that the value passed in falls beyond the end of
file.  The socket is marked for non‐blocking I/O and not all data
was sent due to the socket buffer being full.  If specified,  the
number  of  bytes successfully sent will be returned in The argu‐
ment is not a valid file descriptor.  The argument does not refer
to a regular file.  The argument is not a valid  socket  descrip‐
tor.  The argument does not refer stream oriented socket.  An in‐
valid  address  was  specified  for an argument.  A signal inter‐
rupted before it could be completed.  If specified, the number of
bytes successfully sent will be returned in The argument is nega‐
tive.  The argument is NULL.  The argument is not set to  0.   An
error  occurred  while reading from The argument points to an un‐
connected socket.  The argument is not a socket.  The file system
for descriptor does not support The socket peer  has  closed  the
connection.  The system call first appeared in Darwin 9.0 (Mac OS
X  version 10.5) .  This manual page is based on the FreeBSD ver‐
sion written by
