This document attempts to list items for the system administrator
to  check  and  set  up after the installation and first complete
boot of the system.  The idea is to create a list of  items  that
can  be  checked  off  so that you have a warm fuzzy feeling that
something obvious has not been missed.  A basic knowledge  of  is
assumed.   Complete  instructions for correcting and fixing items
is not provided.  There are manual pages and other  methodologies
available  for doing that.  For example, to view the man page for
the command, type: Administrators will rapidly become more famil‐
iar with if they get used to using the manual pages.  On a  fresh
install  with  no  other user accounts, login as You can do so on
the console, or over the network using If you  have  enabled  the
SSH  daemon  (see and wish to allow root logins over the network,
edit the file and set to (see The default is to not  permit  root
logins  over  the  network after fresh install in Upon successful
login on the console, you may see the message For  security  rea‐
sons,  it is bad practice to login as root during regular use and
maintenance of the system.  In fact, the system will only let you
login as root on a secure terminal.  By default, only the console
is considered to be a secure terminal.   Instead,  administrators
are  encouraged  to  add a user, add said user to the group, then
use the command when root privileges  are  required:  Change  the
password for the root user.  (Note that throughout the documenta‐
tion,  the  term is a synonym for the root user.)  Choose a pass‐
word that has numbers, digits, and special characters (not space)
as well as from the upper and lower case alphabet.  Do not choose
any word in any language.  It is common for an  intruder  to  use
dictionary attacks.  Type the command to change it.  It is a good
idea  to  always specify the full path name for both the and com‐
mands as this inhibits the possibility of files  placed  in  your
execution  for  most shells.  Furthermore, the superuser’s should
never contain the current directory Check the  system  date  with
the  command.  If needed, change the date, and/or change the sym‐
bolic link of to the correct time zone in the  directory.   Exam‐
ples:  Set the current date to October 5th, 2020 6:20pm.  Set the
time zone to Eastern Europe Summer Time.  One of the first things
you will likely need to do is to set up your  keyboard  map  (and
maybe  some  other  aspects about the system console).  To change
your keyboard layout, edit the variable found  in  contains  more
information  about  this  file.  All significant and easily fixed
problems will be reported at It is  recommended  that  you  check
this  page  regularly.   Additionally, you should set in to allow
your system to automatically update the local database  of  known
vulnerable packages to the latest version available on‐line.  The
system  will  later  check,  on a daily basis, if any of your in‐
stalled packages are vulnerable based on  the  contents  of  this
database.   See  and  for more details.  If your machine does not
have a hardware random number generator, it may not  be  safe  to
use  on  the internet until it has enough entropy to generate un‐
predictable secrets for programs like web browsers  and  You  can
use to list the entropy sources with or save entropy from another
machine  running  with  and  load it on this one with (as long as
there are no eavesdroppers on the  medium  between  the  two  ma‐
chines).   See  for more details.  Use the command to verify that
the name of your machine is correct.  See the man page for if  it
needs  to  be changed.  You will also need to change the contents
of the variable in or edit the file to have it stick  around  for
the next reboot.  Note that is supposed include a domainname, and
that  this  should not be confused with YP (NIS) If you are using
to configure network interfaces, it might  override  these  local
hostname settings if your DHCP server specifies client’s hostname
with  other  network configurations.  The first thing to do is an
to see if the network interfaces are properly  configured.   Cor‐
rect  by  editing  or the corresponding variable in (where is the
interface name, e.g., and then using to manually configure it  if
you  do  not  wish to reboot.  Alternatively, many networks allow
interfaces to be configured automatically via DHCP.   To  get  to
start  automatically  on boot, you will need to have this line in
See and for more information on setting up a  DHCP  client.   For
information  on  setting  up Wi‐Fi, see You can add new by adding
the required entries to Read the man page for more information on
the format of files.  The loopback interface will look  something
like:    lo0:    flags=8009<UP,LOOPBACK,MULTICAST>    mtu   32972
        inet   127.0.0.1   netmask    0xff000000            inet6
fe80::1%lo0  prefixlen 64 scopeid 0x3         inet6 ::1 prefixlen
128    an    Ethernet    interface    something    like:     le0:
flags=9863<UP,BROADCAST,NOTRAILERS,RUNNING,SIMPLEX,MULTICAST>
        inet    192.168.4.52    netmask    0xffffff00   broadcast
192.168.4.255          inet6  fe80::5ef0:f0f0%le0  prefixlen   64
scopeid   0x1   and   a   PPP  interface  something  like:  ppp0:
flags=8051<UP,POINTOPOINT,RUNNING,MULTICAST>
        inet 203.3.131.108 ‐‐> 198.181.0.253  netmask  0xffff0000
See  for  instructions on configuring multicast routing.  Issue a
command.  The output will look something like: Routing tables

Internet: Destination    Gateway            Flags   Refs      Use
Mtu    Interface   default         192.168.4.254      UGS       0
11098028    ‐  le0 127             127.0.0.1          UGRS      0
0     ‐  lo0 127.0.0.1      127.0.0.1         UH       3       24
‐  lo0 192.168.4      link#1            UC       0        0     ‐
le0  192.168.4.52    8:0:20:73:b8:4a    UHL       1     6707    ‐
le0 192.168.4.254  0:60:3e:99:67:ea   UHL       1         0     ‐
le0

Internet6:  Destination         Gateway        Flags   Refs   Use
Mtu  Interface ::/96              ::1           UGRS     0      0
32972   lo0  => ::1                ::1           UH       4     0
32972  lo0  ::ffff:0.0.0.0/96   ::1            UGRS      0      0
32972   lo0  fc80::/10           ::1            UGRS      0     0
32972  lo0  fe80::/10           ::1            UGRS      0      0
32972   lo0  fe80::%le0/64       link#1         UC        0     0
1500   le0  fe80::%lo0/64       fe80::1%lo0    U         0      0
32972   lo0  ff01::/32           ::1            U         0     0
32972  lo0  ff02::%le0/32       link#1         UC        0      0
1500   le0  ff02::%lo0/32       fe80::1%lo0    UC        0      0
32972  lo0 The default gateway address is stored in the  variable
in  or  in the file If you need to edit this file, a painless way
to reconfigure the network afterwards is to  issue  Or,  you  may
prefer  to manually configure using a series of and commands (see
If you run you will have to kill it by running before  you  flush
the routes.  If you wish to route packets between interfaces, add
one  or  both  of  the following directives (depending on whether
IPv4 or IPv6 routing is required) to As an alternative, compile a
new kernel with the option.  Packets are  not  forwarded  by  de‐
fault, due to RFC requirements.  By default, nodes are created in
for  a fairly typical number of devices.  However, if this system
has a large number of devices connected  (e.g.  for  large  scale
storage), you may want to enable to ensure a sufficient number of
nodes are available.  Set in to create nodes automatically during
system  runtime.   You  can  also run the node creation script by
hand: By default, all services are disabled in a fresh  installa‐
tion,  and SSH is no exception.  You may wish to enable it so you
can remotely control your system.  Set in and then  starting  the
server  with the command The first time the server is started, it
will generate a new keypair, which will be stored inside the  di‐
rectory  The  system  resolves host names according the rules for
hosts in the name service switch configuration at By default,  it
will  query  first, and then the DNS resolver specified in Multi‐
cast DNS and DNS Service Discovery are usually not enabled by de‐
fault on a fresh system, and can be enabled by setting in and ei‐
ther rebooting or running the following  command:  You  may  also
wish  to enable mdnsd as a source for host lookups in see If your
network does not have a usable DNS resolver, e.g. one provided by
DHCP, you can run a local caching recursive resolver  by  setting
in and either rebooting or running the following command: is con‐
figured  in  by  default  to run as a local caching recursive re‐
solver.  Then, to make the system use it, put  the  following  in
nameserver  127.0.0.1 To configure the system to connect to a Wi‐
Fi network with a password using WPA: To to configure the  system
to connect to an open wireless network with no password, edit in‐
stead     of     using    network={            ssid="Public‐WiFi"
        key_mgmt=NONE         priority=100 } Then  bring  up  the
interface  and start the necessary daemons: To automatically con‐
nect at boot, add the following to While using you can easily re‐
trieve network scan results with Or  trigger  a  rescan:  Several
services depend on the RPC portmapper ‐ formerly known as ‐ being
running for proper operation.  This includes YP (NIS) and NFS ex‐
ports,  among other services.  To get the RPC portmapper to start
automatically on boot, you will need to have this line  in  Check
the YP domain name with the command.  If necessary, correct it by
editing  the  file or by setting the variable in The script reads
this file on bootup to determine and set the  domain  name.   You
may  also  set the running system’s domain name with the command.
To start YP client services, simply run then perform the  remain‐
ing YP activation as described in and In particular, to enable YP
passwd  support, you’ll need to update to include for the and en‐
tries.  A traditional way to accomplish the same thing is to  add
following  entry to local passwd database via Note this entry has
to be the very last one.  This traditional way works with the de‐
fault setting of which is There are many more YP man pages avail‐
able to help you.  You can find more information by starting with
Check that the disks are mounted correctly by comparing the  file
against the output of the and commands.  Example: /dev/sd0a / ffs
rw              1 1 /dev/sd0b none swap sw /dev/sd0e /usr ffs  rw
1  2  /dev/sd0f  /var ffs  rw              1 3 /dev/sd0g /tmp ffs
rw              1 4 /dev/sd0h /home ffs rw              1 5

/dev/sd0a on / type ffs (local) /dev/sd0e on /usr type  ffs  (lo‐
cal)  /dev/sd0f  on  /var type ffs (local) /dev/sd0g on /tmp type
ffs (local) /dev/sd0h on /home type ffs (local)

Filesystem  1024‐blocks     Used    Avail  Capacity   Mounted  on
/dev/sd0a          22311     14589     6606    69%    / /dev/sd0e
203399   150221    43008    78%    /usr  /dev/sd0f          10447
682      9242      7%     /var  /dev/sd0g          18823        2
17879     0%    /tmp /dev/sd0h           7519      5255      1888
74%    /home

Device        512‐blocks      Used     Avail  Capacity   Priority
/dev/sd0b       131072    84656    46416    65%    0 Edit and use
the and commands as appropriate.  Refer to the above example  and
for  information  on the format of this file.  You may wish to do
NFS mounts now too, or you can do them later.  In order  to  make
sure  the  system clock is synchronized to that of a publicly ac‐
cessible NTP server, make sure that contains the  following:  See
and for more information on setting the system’s date.  The pack‐
ages  collection,  pkgsrc,  includes  a  large set of third‐party
software.  A lot of it is available as binary packages  that  you
can  download  from  or a mirror.  For most users, using pkgin to
manage binary packages is recommended.  To install pkgin,  if  it
was  not  done  by  the installer: See and for more details.  The
system should be usable now, but you may wish  to  do  more  cus‐
tomizing,  such as adding users, etc.  Many of the following sec‐
tions may be skipped if you are not using that package (for exam‐
ple, skip the section if you won’t be using Kerberos).   We  sug‐
gest that you and edit most of the files in that directory.  Note
that  the  file is modified by whenever the system is booted.  To
keep any custom message intact, ensure that you leave  two  blank
lines  at  the  top, or your message will be overwritten.  To add
new users and groups, there are and see also for further programs
for user and group manipulation.  You may use to add users to the
file and edit by hand to add new groups.   The  manual  page  for
tells  you  to  make sure to put people in the group if they need
root access (non‐Kerberos).  For  example:  wheel:*:0:root,myself
Follow  instructions  for  if  using Kerberos for authentication.
and the scripts are invoked at boot time after single  user  mode
has  exited, and at shutdown.  The whole process is controlled by
the master script This script should not be changed  by  adminis‐
trators.   The  directory  contains  a  series of scripts used at
startup/shutdown, called by is in turn influenced by the configu‐
ration variables present in The script is run as the  last  thing
during  multiuser  boot, and is provided to allow any other local
hooks necessary for the system.  To  enable  or  disable  various
services  on system startup, corresponding entries can be made in
You can take a look at to see a list of default system variables,
which you can override in Note you are  supposed  to  change  di‐
rectly,  edit only See for further information.  To use the auto‐
mounter, create the directory, copy example config files from  to
and  customize  them  as needed.  Alternatively, you can get your
maps with YP.  If you are using concatenated disks, edit You  may
wish to take a look to for more information about this file.  Use
the  command  to unload and the command to create tables internal
to the kernel for the concatenated disks.  You then and  edit  as
needed.   is  the default firewall used on You may wish to enable
it if your machine is connected directly to the internet.  To  do
this, edit and set in Configuration examples for NPF can be found
in Before installing a configuration, you can validate it with If
you’ve  installed  X,  you may want to turn on the X Display Man‐
ager.  To do this, set in Edit and to get any  printers  set  up.
Consult  and if needed.  Various internet services can be enabled
in including and Note that by default all services  are  disabled
for  security  reasons.   Only add things that are really needed.
If you are going to use Kerberos for authentication, see and  for
more  information.  If you already have a Kerberos master, change
directory to and configure.  Remember to get a from the master so
that the remote commands work.  Check and update appropriately if
you want e‐mail to be routed to non‐local addresses or to differ‐
ent users.  Run after changes.  uses Postfix as its Mail Transfer
Agent.  Postfix is started by default, but its initial configura‐
tion does not cause it to listen on the network for incoming con‐
nections.  To configure Postfix, see and If you  wish  to  use  a
different  MTA  (e.g.,  sendmail), install your MTA of choice and
edit to point to the proper binaries.  If this is a DHCP  server,
edit  and as needed.  You will have to make sure has or run manu‐
ally.  If this is a Bootparam server, edit as needed.   You  will
have  to  turn  it on in by adding If this is an NFS server, make
sure has: nfs_server=YES mountd=YES rpcbind=YES Edit and  get  it
correct.   After this, you can start the server by issuing: which
will also start dependencies.  Edit if needed for remote booting.
If you do not have HP computers doing remote booting, do not  en‐
able  this.   Look  at  and  possibly  edit the and configuration
files.  You can check which values you  can  set  by  looking  to
their  matching files in Your site specific things should go into
and These scripts have been limited so as to keep the system run‐
ning without filling up disk space from normal running  processes
and  database  updates.   (You probably do not need to understand
them.)  Look at the other files in and edit them as needed.   (Do
not  edit  files  ending  in — like nor nor nor any directories.)
Check what is running by typing as root and see if anything unex‐
pected is present.  Do you need anything else?  Do  you  wish  to
change  things?   For  example,  if  you do not like root getting
standard output of the daily scripts, and want only the  security
scripts  that are mailed internally, you can type and change some
of the lines to read: 30  1  *  *  *   /bin/sh /etc/daily 2>&1  >
/var/log/daily.out  30   3   *  *  6   /bin/sh /etc/weekly 2>&1 >
/var/log/weekly.out 30  5  1  *  *   /bin/sh /etc/monthly 2>&1  >
/var/log/monthly.out  See  After  the first night’s security run,
change ownerships and permissions on files, directories, and  de‐
vices;  root  should have received mail with subject: "<hostname>
daily insecurity output.".  This mail contains a set of  security
recommendations, presented as a list looking like this: var/mail:
        permissions (0755, 0775) etc/daily:
        user  (0, 3) The best bet is to follow the advice in that
list.  The recommended setting is the first item in  parentheses,
while the current setting is the second one.  This list is gener‐
ated  by  using  Use  and  as  needed.  At this point, the system
should be fully configured to your liking.  It is now a good time
to ensure that the system behaves according to its specifications
and that it is stable on your hardware.  Please refer to for  de‐
tails  on  how  to  do  so.   You can use and to check on running
processes, network connections, and opened  files,  respectively.
Other  tools  you may find useful are and This document first ap‐
peared in It has been adapted to and first appeared in
