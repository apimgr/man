The command is a simple interface to the services startup system.
Its  purpose is to list the services or invoke actions on them as
provided by the scripts.  When used to invoke scripts,  will  set
the  same environment that is used at boot time.  The options are
as follows: List services that are enabled.  The list of  scripts
is compiled using the same way as is done in each script is first
checked for an assignment and if present, checked to see if it is
enabled.   If  one or more are specified, only those are checked.
The arguments are always specified without a path  prefix.   List
all  files  in  (the default of A script will be listed unless it
has the keyword enabled.  Report what are defined or in which di‐
rectory the was found when an is invoked.  When  used  to  invoke
command  sets  to  to and to which is how they are set in at boot
time.  The is typically one of or any other argument supported by
the These are typical usages  of  the  command:  #  service  sshd
restart Stopping sshd.  Starting sshd.  $ service ‐v inetd status
inetd  is  located  in /etc/rc.d inetd is running as pid 1713.  $
service ‐ve ccd motd hostapd my_pkg rc_directories  is  /etc/rc.d
/usr/pkg/etc/rc.d      /etc/rc.d/ccd     /usr/pkg/etc/rc.d/my_pkg
/etc/rc.d/motd In the last example, was apparently left  disabled
and  the  additional  directory  was configured by redefining the
variable in further, must have also been set.  The utility  first
appeared in Written for by Adapted and extended to by
