The  reconstructs  the  first  available definition found for and
prints the result in a format.  Capability types are grouped  to‐
gether  and new types start new lines, first flags, then numbers,
then strings.  Capabilities are sorted by their name.  If a  sec‐
ond  is  given  then  the  capabilities are compared against each
other.  The following options are available: Print one capability
per line.  Use this database to load the first  terminal  defini‐
tion.  Use this database to load subsequent terminal definitions.
Include commented out capabilities.  This only works if the data‐
base was compiled with the flag passed to This also sets the flag
as retains commented out capabilities as non standard.  Print ca‐
pabilities common to each definition.  Print capabilities that do
not  exist  in  either  definition.   Make the comparison listing
shorter by omitting subheadings and using ‐ for absent  capabili‐
ties,  @ for canceled capabilities rather than Build a new termi‐
nal description for the first terminal description, using  subse‐
quent terminal descriptions.  This also sets the flag.  Limit the
width  to  Include  non‐standard capabilities.  More only handles
non‐standard capabilities.  This only works if the  database  was
compiled with the flag passed to Override columns returned by the
output  terminal.  supersedes this.  uses the contents of the en‐
vironment variable if no terminal name is given  on  the  command
line.  The utility outputs information that conforms to the stan‐
dard.
