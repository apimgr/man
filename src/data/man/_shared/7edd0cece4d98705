PCAP_SET_TIMEOUT(3)        Library Functions Manual        PCAP_SET_TIMEOUT(3)



NNAAMMEE
       pcap_set_timeout  -  set  the packet buffer timeout for a not-yet-acti‐
       vated capture handle

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ppccaapp//ppccaapp..hh>>

       iinntt ppccaapp__sseett__ttiimmeeoouutt((ppccaapp__tt **pp,, iinntt ttoo__mmss));;

DDEESSCCRRIIPPTTIIOONN
       ppccaapp__sseett__ttiimmeeoouutt() sets the packet buffer timeout that will be used  on
       a  capture  handle  when  the handle is activated to _t_o___m_s, which is in
       units of milliseconds.  (See ppccaapp(3) for an explanation of  the  packet
       buffer timeout.)

       The  behavior,  if the timeout isn't specified, is undefined, as is the
       behavior if the timeout is set to zero or to a negative value.  We rec‐
       ommend  always setting the timeout to a non-zero value unless immediate
       mode is set, in which case the timeout has no effect.

RREETTUURRNN VVAALLUUEE
       ppccaapp__sseett__ttiimmeeoouutt() returns 00  on  success  or  PPCCAAPP__EERRRROORR__AACCTTIIVVAATTEEDD  if
       called on a capture handle that has been activated.

SSEEEE AALLSSOO
       ppccaapp__ccrreeaattee(3), ppccaapp__aaccttiivvaattee(3), ppccaapp__sseett__iimmmmeeddiiaattee__mmooddee(3)



                                6 December 2017            PCAP_SET_TIMEOUT(3)
