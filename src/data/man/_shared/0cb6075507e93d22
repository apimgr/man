_g_c_v_t(3)                    Library Functions Manual                    _g_c_v_t(3)

NNAAMMEE
       gcvt - convert a floating‐point number to a string

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssttddlliibb..hh>>

       cchhaarr **ggccvvtt((ddoouubbllee _n_u_m_b_e_r,, iinntt _n_d_i_g_i_t,, cchhaarr **_b_u_f));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       ggccvvtt():
           Since glibc 2.17
               (_XOPEN_SOURCE >= 500 && ! (_POSIX_C_SOURCE >= 200809L))
                   || /* glibc >= 2.20 */ _DEFAULT_SOURCE
                   || /* glibc <= 2.19 */ _SVID_SOURCE
           glibc 2.12 to glibc 2.16:
               (_XOPEN_SOURCE >= 500 && ! (_POSIX_C_SOURCE >= 200112L))
                   || _SVID_SOURCE
           Before glibc 2.12:
               _SVID_SOURCE || _XOPEN_SOURCE >= 500

DDEESSCCRRIIPPTTIIOONN
       The ggccvvtt() function converts _n_u_m_b_e_r to a minimal length null‐terminated
       ASCII string and stores the result in _b_u_f.  It produces _n_d_i_g_i_t signifi‐
       cant digits in either pprriinnttff(3) F format or E format.

RREETTUURRNN VVAALLUUEE
       The ggccvvtt() function returns _b_u_f.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{ ggccvvtt()
       T}   Thread safety   MT‐Safe

SSTTAANNDDAARRDDSS
       None.

HHIISSTTOORRYY
       Marked as LEGACY in POSIX.1‐2001.  POSIX.1‐2008 removed it,
       recommending the use of sspprriinnttff(3) instead (though ssnnpprriinnttff(3) may be
       preferable).

SSEEEE AALLSSOO
       eeccvvtt(3), ffccvvtt(3), sspprriinnttff(3)

Linux man‐pages (unreleased)      2025‐05‐17                           _g_c_v_t(3)
