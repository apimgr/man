VOP_READDIR(9)           BSD Kernel Developer's Manual          VOP_READDIR(9)

NNAAMMEE
     VVOOPP__RREEAADDDDIIRR — read contents of a directory

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ppaarraamm..hh>>
     ##iinncclluuddee <<ssyyss//ddiirreenntt..hh>>
     ##iinncclluuddee <<ssyyss//vvnnooddee..hh>>

     _i_n_t
     VVOOPP__RREEAADDDDIIRR(_s_t_r_u_c_t _v_n_o_d_e _*_v_p, _s_t_r_u_c_t _u_i_o _*_u_i_o, _s_t_r_u_c_t _u_c_r_e_d _*_c_r_e_d,
         _i_n_t _*_e_o_f_f_l_a_g, _i_n_t _*_n_c_o_o_k_i_e_s, _u_i_n_t_6_4___t _*_*_c_o_o_k_i_e_s);

DDEESSCCRRIIPPTTIIOONN
     Read directory entries.

     _v_p        The vnode of the directory.

     _u_i_o       Where to read the directory contents.

     _c_r_e_d      The caller's credentials.

     _e_o_f_f_l_a_g   Return end of file status (NULL if not wanted).

     _n_c_o_o_k_i_e_s  Number of directory cookies generated for NFS (NULL if not
               wanted).

     _c_o_o_k_i_e_s   Directory seek cookies generated for NFS (NULL if not wanted).
     The directory contents are read into _s_t_r_u_c_t _d_i_r_e_n_t structures.  If the
     on-disc data structures differ from this then they should be translated.

LLOOCCKKSS
     The directory should be locked on entry and will still be locked on exit.

RREETTUURRNN VVAALLUUEESS
     Zero is returned on success, otherwise an error code is returned.

     If this is called from the NFS server, the extra arguments _e_o_f_f_l_a_g,
     _n_c_o_o_k_i_e_s and _c_o_o_k_i_e_s are given.  The value of _*_e_o_f_f_l_a_g should be set to
     TRUE if the end of the directory is reached while reading.  The directory
     seek cookies are returned to the NFS client and may be used later to
     restart a directory read part way through the directory.  There should be
     one cookie returned per directory entry.  The value of the cookie should
     be the offset within the directory where the on-disc version of the ap‐
     propriate directory entry starts.  Memory for the cookies should be allo‐
     cated using:

             ...;
             *ncookies = number of entries read;
             *cookies = malloc(*ncookies * sizeof(**cookies), M_TEMP, M_WAITOK);

EERRRROORRSS
     [EINVAL]           An attempt was made to read from an illegal offset in
                        the directory.

     [EIO]              A read error occurred while reading the directory.

     [EINTEGRITY]       Corrupted data was detected while reading the direc‐
                        tory.

SSEEEE AALLSSOO
     vnode(9)

AAUUTTHHOORRSS
     This manual page was written by Doug Rabson.

BSD                            December 13, 2021                           BSD
