GMOUNTVER(8)              BSD System Manager's Manual             GMOUNTVER(8)

NNAAMMEE
     ggmmoouunnttvveerr — control utility for disk mount verification GEOM class

SSYYNNOOPPSSIISS
     ggmmoouunnttvveerr ccrreeaattee [--vv] _p_r_o_v _._._.
     ggmmoouunnttvveerr ddeessttrrooyy [--ffvv] _n_a_m_e
     ggmmoouunnttvveerr lliisstt
     ggmmoouunnttvveerr ssttaattuuss [--ss _n_a_m_e]
     ggmmoouunnttvveerr llooaadd [--vv]
     ggmmoouunnttvveerr uunnllooaadd [--vv]

DDEESSCCRRIIPPTTIIOONN
     The ggmmoouunnttvveerr utility is used to control the mount verification GEOM
     class.  When configured, it passes all the I/O requests to the underlying
     provider.  When the underlying provider disappears - for example because
     the disk device got disconnected - it queues all the I/O requests and
     waits for the provider to reappear.  When that happens, it attaches to it
     and sends the queued requests.

     The first argument to ggmmoouunnttvveerr indicates an action to be performed:

     ccrreeaattee   Enable mount verification for the given provider.  If the opera‐
              tion succeeds, a new GEOM provider will be created using the
              given provider's name with a ‘.mountver’ suffix.  The kernel
              module _g_e_o_m___m_o_u_n_t_v_e_r_._k_o will be loaded if it is not loaded al‐
              ready.

     ddeessttrrooyy  Destroy _n_a_m_e.

     lliisstt     See geom(8).

     ssttaattuuss   See geom(8).

     llooaadd     See geom(8).

     uunnllooaadd   See geom(8).

     Additional options:

     --ff      Force the removal of the specified mountver device.

     --vv      Be more verbose.

SSYYSSCCTTLL VVAARRIIAABBLLEESS
     The following sysctl(8) variables can be used to control the behavior of
     the MMOOUUNNTTVVEERR GEOM class.  The default value is shown next to each vari‐
     able.

     _k_e_r_n_._g_e_o_m_._m_o_u_n_t_v_e_r_._d_e_b_u_g: 0
             Debug level of the MMOOUUNNTTVVEERR GEOM class.  This can be set to a
             number between 0 and 3 inclusive.  If set to 0 minimal debug in‐
             formation is printed, and if set to 3 the maximum amount of debug
             information is printed.

     _k_e_r_n_._g_e_o_m_._m_o_u_n_t_v_e_r_._c_h_e_c_k___i_d_e_n_t: 1
             This can be set to 0 or 1.  If set to 0, ggmmoouunnttvveerr will reattach
             to the device even if the device reports a different disk ID.

EEXXIITT SSTTAATTUUSS
     Exit status is 0 on success, and 1 if the command fails.

SSEEEE AALLSSOO
     geom(4), geom(8)

HHIISSTTOORRYY
     The ggmmoouunnttvveerr utility appeared in FreeBSD 9.0.

AAUUTTHHOORRSS
     Edward Tomasz Napierala <_t_r_a_s_z_@_F_r_e_e_B_S_D_._o_r_g>

BSD                              May 18, 2015                              BSD
