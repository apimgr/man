gives information on previously executed commands.  With no argu‐
ments,  prints information about all the commands recorded during
the current accounting file’s lifetime.  Option: Read from rather
than the default accounting file.  Use as many columns as  needed
to print the output instead of limiting it to 80.  If called with
arguments, only accounting entries with a matching name, name, or
name  are  printed.   So, for example: would produce a listing of
all the executions of commands named by user on the terminal  For
each  process  entry, the following are printed.  The name of the
user who ran the process.  Flags, as accumulated by the  account‐
ing  facilities  in the system.  The command name under which the
process was called.  The amount of cpu time used by  the  process
(in seconds).  The time the process started.  The elapsed time of
the  process.   The  flags  are encoded as follows: indicates the
command was executed by the super‐user, indicates the command ran
after a fork, but without a following indicates the  command  was
run  in  PDP‐11 compatibility mode (VAX only), indicates the com‐
mand terminated with the generation of a file, and indicates  the
command  was  terminated  with  a  signal.   The and flags are no
longer recorded by the system, but will be reported by when read‐
ing from an accounting file generated by an older version of  the
system.  Default accounting file.  The command appeared in
