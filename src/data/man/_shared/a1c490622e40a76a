FREEBSD-UPDATE(8)         BSD System Manager's Manual        FREEBSD-UPDATE(8)

NNAAMMEE
     ffrreeeebbssdd--uuppddaattee — fetch and install binary updates to FreeBSD

SSYYNNOOPPSSIISS
     ffrreeeebbssdd--uuppddaattee [--FF] [--bb _b_a_s_e_d_i_r] [----ccuurrrreennttllyy--rruunnnniinngg _r_e_l_e_a_s_e]
                    [--dd _w_o_r_k_d_i_r] [--ff _c_o_n_f_f_i_l_e] [--jj _j_a_i_l] [--kk _K_E_Y]
                    [----nnoott--rruunnnniinngg--ffrroomm--ccrroonn] [--rr _n_e_w_r_e_l_e_a_s_e] [--ss _s_e_r_v_e_r]
                    [--tt _a_d_d_r_e_s_s] _c_o_m_m_a_n_d _._._.

DDEESSCCRRIIPPTTIIOONN
     The ffrreeeebbssdd--uuppddaattee tool is used to fetch, install, and rollback binary
     updates to the FreeBSD base system.

BBIINNAARRYY UUPPDDAATTEESS AAVVAAIILLAABBIILLIITTYY
     Binary updates are not available for every single FreeBSD version and ar‐
     chitecture.

     In general, binary updates are available for ALPHA, BETA, RC, and RELEASE
     versions of FreeBSD, e.g.:
           FreeBSD 13.1-ALPHA3
           FreeBSD 13.1-BETA2
           FreeBSD 13.1-RC1
           FreeBSD 13.1-RELEASE
     They are not available for branches such as PRERELEASE, STABLE, and CUR‐
     RENT, e.g.:
           FreeBSD 13.0-PRERELEASE
           FreeBSD 13.1-STABLE
           FreeBSD 14.0-CURRENT

     In particular, the FreeBSD Security Team only builds updates for releases
     shipped in binary form by the FreeBSD Release Engineering Team.

OOPPTTIIOONNSS
     The following options are supported:

     --bb _b_a_s_e_d_i_r     Operate on a system mounted at _b_a_s_e_d_i_r.  (default: _/, or
                    as given in the configuration file.)

     --dd _w_o_r_k_d_i_r     Store working files in _w_o_r_k_d_i_r.  (default:
                    _/_v_a_r_/_d_b_/_f_r_e_e_b_s_d_-_u_p_d_a_t_e_/, or as given in the configuration
                    file.)

     --ff _c_o_n_f_f_i_l_e    Read configuration options from _c_o_n_f_f_i_l_e.  (default:
                    _/_e_t_c_/_f_r_e_e_b_s_d_-_u_p_d_a_t_e_._c_o_n_f)

     --FF             Force ffrreeeebbssdd--uuppddaattee ffeettcchh to proceed in the case of an
                    unfinished upgrade.

     --jj _j_a_i_l        Operate on the given jail specified by _j_i_d or _n_a_m_e.  (The
                    version of the installed userland is detected and the
                    ----ccuurrrreennttllyy--rruunnnniinngg option is no more required.)

     --kk _K_E_Y         Trust an RSA key with SHA256 of _K_E_Y.  (default: read value
                    from configuration file.)

     --rr _n_e_w_r_e_l_e_a_s_e  Specify the new release (e.g., 11.2-RELEASE) to which
                    ffrreeeebbssdd--uuppddaattee should upgrade (uuppggrraaddee command only).

     --ss _s_e_r_v_e_r      Fetch files from the specified server or server pool.
                    (default: read value from configuration file.)

     --tt _a_d_d_r_e_s_s     Mail output of ccrroonn command, if any, to _a_d_d_r_e_s_s.  (de‐
                    fault: root, or as given in the configuration file.)

     ----nnoott--rruunnnniinngg--ffrroomm--ccrroonn
                    Force ffrreeeebbssdd--uuppddaattee ffeettcchh to proceed when there is no
                    controlling tty(4).  This is for use by automated scripts
                    and orchestration tools.  Please do not run ffrreeeebbssdd--uuppddaattee
                    ffeettcchh from crontab(5) or similar using this flag, see:
                    ffrreeeebbssdd--uuppddaattee ccrroonn

     ----ccuurrrreennttllyy--rruunnnniinngg _r_e_l_e_a_s_e
                    Do not detect the currently-running release; instead, as‐
                    sume that the system is running the specified _r_e_l_e_a_s_e.
                    This is most likely to be useful when upgrading jails.

CCOOMMMMAANNDDSS
     The ccoommmmaanndd can be any one of the following:

     ffeettcchh     Based on the currently installed world and the configuration
               options set, fetch all available binary updates.

     ccrroonn      Sleep a random amount of time between 1 and 3600 seconds, then
               download updates as if the ffeettcchh command was used.  If updates
               are downloaded, an email will be sent (to root or a different
               address if specified via the --tt option or in the configuration
               file).  As the name suggests, this command is designed for run‐
               ning from cron(8); the random delay serves to minimize the
               probability that a large number of machines will simultaneously
               attempt to fetch updates.

     uuppggrraaddee   Fetch files necessary for upgrading to a new release.  Before
               using this command, make sure that you read the announcement
               and release notes for the new release in case there are any
               special steps needed for upgrading.  Note that this command may
               require up to 500 MB of space in _w_o_r_k_d_i_r depending on which
               components of the FreeBSD base system are installed.

     uuppddaatteessrreeaaddyy
               Check if there are fetched updates ready to install.  Returns
               exit code 2 if there are no updates to install.

     iinnssttaallll   Install the most recently fetched updates or upgrade.  Returns
               exit code 2 if there are no updates to install and the ffeettcchh
               command wasn't passed as an earlier argument in the same invo‐
               cation.

     rroollllbbaacckk  Uninstall the most recently installed updates.

     IIDDSS       Compare the system against a "known good" index of the in‐
               stalled release.

     sshhoowwccoonnffiigg
               Show configuration options after parsing conffile and command
               line options.

TTIIPPSS
     ••   If your clock is set to local time, adding the line

               0 3 * * * root /usr/sbin/freebsd-update cron

         to _/_e_t_c_/_c_r_o_n_t_a_b will check for updates every night.  If your clock is
         set to UTC, please pick a random time other than 3AM, to avoid overly
         imposing an uneven load on the server(s) hosting the updates.

     ••   In spite of its name, ffrreeeebbssdd--uuppddaattee IDS should not be relied upon as
         an "Intrusion Detection System", since if the system has been tam‐
         pered with it cannot be trusted to operate correctly.  If you intend
         to use this command for intrusion-detection purposes, make sure you
         boot from a secure disk (e.g., a CD).

EENNVVIIRROONNMMEENNTT
     PAGER  The pager program used to present various reports during the exe‐
            cution.  (Default: “_/_u_s_r_/_b_i_n_/_l_e_s_s”.)

            PAGER can be set to “cat” when a non-interactive pager is desired.

FFIILLEESS
     /etc/freebsd-update.conf  Default location of the ffrreeeebbssdd--uuppddaattee configu‐
                               ration file.

     /var/db/freebsd-update/   Default location where ffrreeeebbssdd--uuppddaattee stores
                               temporary files, downloaded updates, and files
                               required for rollback.  All files under
                               _/_v_a_r_/_d_b_/_f_r_e_e_b_s_d_-_u_p_d_a_t_e_/ may be deleted if an
                               upgrade is not in progress and rollback will
                               not be required.

SSEEEE AALLSSOO
     freebsd-version(1), uname(1), freebsd-update.conf(5), nextboot(8)

AAUUTTHHOORRSS
     Colin Percival <_c_p_e_r_c_i_v_a_@_F_r_e_e_B_S_D_._o_r_g>

BBUUGGSS
     In patch level situations â for example, 13.2-RELEASE-p1 up to 13.2-RE‐
     LEASE-p2: if any previous modification to a file in _/_e_t_c_/ will conflict
     with an available update, then ffrreeeebbssdd--uuppddaattee will make no attempt to
     merge.  Instead: ffrreeeebbssdd--uuppddaattee will print a list of affected locally-
     modified files.

BSD                             October 4, 2023                            BSD
