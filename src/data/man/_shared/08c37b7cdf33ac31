X99TOKEN(1)               BSD General Commands Manual              X99TOKEN(1)

NNAAMMEE
     xx9999ttookkeenn — X9.9 software token calculator

SSYYNNOOPPSSIISS
     xx9999ttookkeenn [--dd] [--kk _k_e_y_f_i_l_e] [--nn _c_o_u_n_t]
     xx9999ttookkeenn --ii [--kk _k_e_y_f_i_l_e]

DDEESSCCRRIIPPTTIIOONN
     The xx9999ttookkeenn program is a simple X9.9 software token calculator.  The to‐
     ken is initialized by using the --ii option.  In this mode, xx9999ttookkeenn re‐
     quests a key.  The key consists of 8 bytes encoded in either hex or oc‐
     tal.  When encoded in hex the key must consist of 16 hex digits, no spa‐
     ces (e.g., "0123456789abcdef").  When encoded in octal the key must con‐
     sist of 8 3-digit octets, each separated by exactly one space (e.g., "001
     010 100 002 020 200 003 030").  Once the key is entered, a PIN number is
     requested.  The PIN number is used to perturb the key before it is stored
     in a keyfile.  This perturbation is not terribly secure.  You should as‐
     sume that anyone with access to your keyfile will also have access to
     your key.

     When --ii is not specified, xx9999ttookkeenn is in calculator mode.  In this mode
     you must enter the same PIN as used in the initialization step.  The PIN
     is used to decode the key read from the keyfile.  Next you enter the
     challenge you have been presented with.  The xx9999ttookkeenn program will pro‐
     vide you with a response to the challenge.  If the --dd option is used, the
     response will fold any hex digits to decimal.  If a _c_o_u_n_t is specified by
     --nn, it indicates the number of challenges to predict.  The first chal‐
     lenge is always the entered challenge.  By default the value of _c_o_u_n_t is
     1, which implies only the entered challenge is resolved.

     Future challenges are predicted by the following algorithm:

           ••   Encrypt the current challenge with the shared secret key

           ••   AND each byte of the response with 0x0f

           ••   Modulo each byte by 10 (0x0a)

           ••   ADD 0x30 (ASCII value of '0') to each byte

     The resulting 8 bytes are all ASCII decimal digits and are the predicted
     challenge.  This process may be repeated.

FFIILLEESS
     $HOME/.keyfile.des  default keyfile

SSEEEE AALLSSOO
     tokenadm(8), tokeninit(8)

AAUUTTHHOORRSS
     Paul Borman <_p_r_b_@_k_r_y_s_t_a_l_._c_o_m>

CCAAVVEEAATTSS
     This is a very simple-minded program.  The keyfile should be placed on a
     flash disk which is kept physically secure.

     Having not read the X9.9 standard, this may not be a complete or accurate
     implementation of the standard.  This software is provided AS IS with no
     express or implied warranty.

BSD                              July 9, 2014                              BSD
