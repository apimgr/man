The  program  is  used to give commands to a magnetic tape drive.
By default performs the requested operation once.  Operations may
be performed multiple times by specifying Note that  must  refer‐
ence  a raw (not block) tape device.  If is of the form or writes
to the named tape device on the remote host using The process  on
the  remote host is typically initiated via although an alternate
method such as can be specified  via  the  environment  variable.
The available commands are listed below.  Only as many characters
as are required to uniquely identify a command need be specified.
Move  forward  files from the beginning of the tape.  This is ac‐
complished by a rewind followed by fsf Write end‐of‐file marks at
the current position on the tape.  Forward space files.   Forward
space  records.   Back  space files.  Back space records.  Rewind
the tape.  (The is ignored.)  Rewind the tape and place the  tape
unit  off‐line.   Where supported, this ejects the tape.  (The is
ignored.)  Print status information about the tape unit.  (The is
ignored.)  Retensions the tape.  Not all tape drives support this
feature.  (The is ignored.)  Erases the tape Not all tape  drives
support this feature.  (The is ignored.)  Enable or disable early
warning  EOM  behaviour.   Set to nonzero to enable, zero to dis‐
able.  Forward space to the end of recorded media.  (The  is  ig‐
nored.)   Set  the tape blocksize to bytes.  A of zero sets vari‐
able blocksize.  Set the tape density code to as specified in the
specification.  See the section for a list of codes for  commonly
used  media  types.  Read the logical block position of the tape.
Not all tape drives support  this  feature.   (The  is  ignored.)
Read  the hardware block position of the tape.  Not all tape dri‐
ves support this feature.  (The is  ignored.)   Set  the  logical
block  position  of  the tape to Not all tape drives support this
feature.  Set the hardware block position of the tape to Not  all
tape  drives  support this feature.  If is zero, disable compres‐
sion.  Otherwise enable compression.  Not all tape drives support
this feature.  Enable controller buffering.   Disable  controller
buffering.   If a tape name is not specified, and the environment
variable is not set, then uses the device If the following  envi‐
ronment  variables  exist,  they are used by uses device filename
given in the environment variable if the argument is  not  given.
will  use  rather  than  to invoke on a remote machine.  The full
path name must be specified.  Raw tape device Raw  magnetic  tape
device  returns  a  0 exit status when the operation(s) were suc‐
cessful, 1 if the command was unrecognized, and 2 if an operation
failed.  The SCSI‐3 specification defines  a  number  of  density
codes  for  various  tape  media,  some of which are listed here.
Note that many tape  drive  vendors  also  define  model‐specific
codes.  The utility appeared in
