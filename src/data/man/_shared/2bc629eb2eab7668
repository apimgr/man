IWI(4)                   BSD Kernel Interfaces Manual                   IWI(4)

NNAAMMEE
     iiwwii — Intel PRO/Wireless 2200BG/2225BG/2915ABG IEEE 802.11a/b/g wireless
     network device

SSYYNNOOPPSSIISS
     iiwwii** aatt ppccii??

DDEESSCCRRIIPPTTIIOONN
     The iiwwii driver provides support for Intel PRO/Wireless 2200BG/2915ABG
     Mini PCI and 2225BG PCI network adapters.

     These are the modes the iiwwii driver can operate in:

     BSS mode       Also known as _i_n_f_r_a_s_t_r_u_c_t_u_r_e mode, this is used when asso‐
                    ciating with an access point, through which all traffic
                    passes.  This mode is the default.

     IBSS mode      Also known as _I_E_E_E _a_d_-_h_o_c mode or _p_e_e_r_-_t_o_-_p_e_e_r mode.  This
                    is the standardized method of operating without an access
                    point.  Stations associate with a service set.  However,
                    actual connections between stations are peer-to-peer.

     monitor mode   In this mode the driver is able to receive packets without
                    associating with an access point.  This disables the in‐
                    ternal receive filter and enables the card to capture
                    packets from networks which it wouldn't normally have ac‐
                    cess to, or to scan for access points.

     The iiwwii driver can be configured to use Wired Equivalent Privacy (WEP) or
     Wi-Fi Protected Access (WPA1 and WPA2).  WPA2 is currently the most se‐
     cure encryption standard for wireless networks supported by OpenBSD.  It
     is strongly recommended that neither WEP nor WPA1 are used as the sole
     mechanism to secure wireless communication, due to serious weaknesses.
     WPA1 is disabled by default and may be enabled using the option
     "wwppaapprroottooss _w_p_a_1_,_w_p_a_2".  For standard WPA networks which use pre-shared
     keys (PSK), keys are configured using the "wwppaakkeeyy" option.  WPA-Enter‐
     prise networks require use of the wpa_supplicant package.  The iiwwii driver
     relies on the software 802.11 stack for both encryption and decryption of
     data frames.

     The iiwwii driver can be configured at runtime with ifconfig(8) or on boot
     with hostname.if(5).

FFIILLEESS
     The driver needs at least version 3.1 of the following firmware files,
     which are loaded when an interface is brought up:

           /etc/firmware/iwi-bss
           /etc/firmware/iwi-ibss
           /etc/firmware/iwi-monitor

     These firmware files are not free because Intel refuses to grant distri‐
     bution rights without contractual obligations.  As a result, even though
     OpenBSD includes the driver, the firmware files cannot be included and
     users have to download these files on their own.  The official person to
     state your views to about this issue is _m_a_j_i_d_._a_w_a_d_@_i_n_t_e_l_._c_o_m.

     A prepackaged version of the firmware can be installed using
     fw_update(8).

EEXXAAMMPPLLEESS
     The following example scans for available networks:

           # ifconfig iwi0 scan

     The following hostname.if(5) example configures iwi0 to join network
     “mynwid”, using WPA key “mywpakey”, obtaining an IP address using DHCP:

           join mynwid wpakey mywpakey
           inet autoconf

DDIIAAGGNNOOSSTTIICCSS
     iiwwii00:: ddeevviiccee ttiimmeeoouutt  The driver will reset the hardware.  This should
     not happen.

     iiwwii00:: eerrrroorr NN,, ccoouulldd nnoott rreeaadd ffiirrmmwwaarree ......  For some reason, the driver
     was unable to read the firmware image from the filesystem.  The file
     might be missing or corrupted.

SSEEEE AALLSSOO
     arp(4), ifmedia(4), intro(4), netintro(4), pci(4), hostname.if(5),
     ifconfig(8)

HHIISSTTOORRYY
     The iiwwii driver first appeared in OpenBSD 3.7.

AAUUTTHHOORRSS
     The iiwwii driver was written by Damien Bergamini
     <_d_a_m_i_e_n_._b_e_r_g_a_m_i_n_i_@_f_r_e_e_._f_r>.

BSD                             March 27, 2025                             BSD
