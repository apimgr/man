PSIGNAL(9)               BSD Kernel Developer's Manual              PSIGNAL(9)

NNAAMMEE
     ppssiiggnnaall, kkeerrnn__ppssiiggnnaall, ppggssiiggnnaall, ttddssiiggnnaall — post signal to a thread,
     process, or process group

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ttyyppeess..hh>>
     ##iinncclluuddee <<ssyyss//ssiiggnnaallvvaarr..hh>>

     _v_o_i_d
     kkeerrnn__ppssiiggnnaall(_s_t_r_u_c_t _p_r_o_c _*_p, _i_n_t _s_i_g_n_u_m);

     _v_o_i_d
     ppggssiiggnnaall(_s_t_r_u_c_t _p_g_r_p _*_p_g_r_p, _i_n_t _s_i_g_n_u_m, _i_n_t _c_h_e_c_k_c_t_t_y);

     _v_o_i_d
     ttddssiiggnnaall(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d, _i_n_t _s_i_g_n_u_m);

DDEESSCCRRIIPPTTIIOONN
     These functions post a signal to a thread or one or more processes.  The
     argument _s_i_g_n_u_m common to all three functions should be in the range
     [1-NSIG].

     The kkeerrnn__ppssiiggnnaall() function posts signal number _s_i_g_n_u_m to the process
     represented by the process structure _p.  The kkeerrnn__ppssiiggnnaall() function used
     to be called ppssiiggnnaall() but was renamed in order to eliminate a name col‐
     lision with the libc function of that name and facilitate code reuse.
     With a few exceptions noted below, the target process signal disposition
     is updated and is marked as runnable, so further handling of the signal
     is done in the context of the target process after a context switch.
     Note that kkeerrnn__ppssiiggnnaall() does not by itself cause a context switch to
     happen.

     The target process is not marked as runnable in the following cases:

           ••   The target process is sleeping uninterruptibly.  The signal
               will be noticed when the process returns from the system call
               or trap.

           ••   The target process is currently ignoring the signal.

           ••   If a stop signal is sent to a sleeping process that takes the
               default action (see sigaction(2)), the process is stopped with‐
               out awakening it.

           ••   SIGCONT restarts a stopped process (or puts them back to sleep)
               regardless of the signal action (e.g., blocked or ignored).

     If the target process is being traced kkeerrnn__ppssiiggnnaall() behaves as if the
     target process were taking the default action for _s_i_g_n_u_m.  This allows
     the tracing process to be notified of the signal.

     The ppggssiiggnnaall() function posts signal number _s_i_g_n_u_m to each member of the
     process group described by _p_g_r_p.  If _c_h_e_c_k_c_t_t_y is non-zero, the signal
     will be posted only to processes that have a controlling terminal.
     ppggssiiggnnaall() is implemented by walking along the process list headed by the
     field pg_members of the process group structure pointed at by _p_g_r_p and
     calling kkeerrnn__ppssiiggnnaall() as appropriate.  If _p_g_r_p is NULL no action is
     taken.

     The ttddssiiggnnaall() function posts signal number _s_i_g_n_u_m to the thread repre‐
     sented by the thread structure _t_d.

SSEEEE AALLSSOO
     sigaction(2), signal(9), tsleep(9)

HHIISSTTOORRYY
     The ppssiiggnnaall() function was renamed to kkeerrnn__ppssiiggnnaall() in FreeBSD 9.0.

BSD                              July 14, 2023                             BSD
