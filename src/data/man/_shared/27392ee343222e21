curs_move(3X)                    Library calls                   curs_move(3X)



NNAAMMEE
       mmoovvee, wwmmoovvee - move cursor in a _c_u_r_s_e_s window

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ccuurrsseess..hh>>

       iinntt mmoovvee((iinntt _y,, iinntt _x));;
       iinntt wwmmoovvee((WWIINNDDOOWW **_w_i_n,, iinntt _y,, iinntt _x));;

DDEESSCCRRIIPPTTIIOONN
       wwmmoovvee  relocates  the  cursor  associated with the _c_u_r_s_e_s window _w_i_n to
       line _y and column  _x.   The  terminal's  cursor  does  not  move  until
       rreeffrreesshh(3X)  is  called.   The position (_y, _x) is relative to the upper
       left-hand  corner  of  the  window,  which  has   coordinates   (0, 0).
       nnccuurrsseess(3X) describes the mmoovvee variant of this function.

RREETTUURRNN VVAALLUUEE
       These functions return OOKK on success and EERRRR on failure.

       They fail if the position (_y, _x) is outside the window boundaries.

       wwmmoovvee fails if its _W_I_N_D_O_W pointer argument is NNUULLLL.

NNOOTTEESS
       mmoovvee may be implemented as a macro.

PPOORRTTAABBIILLIITTYY
       X/Open Curses, Issue 4 describes these functions.

SSEEEE AALLSSOO
       ccuurrsseess(3X), ccuurrss__rreeffrreesshh(3X)



ncurses 6.5                       2024-04-27                     curs_move(3X)
