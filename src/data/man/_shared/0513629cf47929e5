LOCKF(1)                  BSD General Commands Manual                 LOCKF(1)

NNAAMMEE
     lloocckkff — execute a command while holding a file lock

SSYYNNOOPPSSIISS
     lloocckkff [--kknnssww] [--tt _s_e_c_o_n_d_s] _f_i_l_e _c_o_m_m_a_n_d [_a_r_g_u_m_e_n_t_s]
     lloocckkff [--ss] [--tt _s_e_c_o_n_d_s] _f_d

DDEESSCCRRIIPPTTIIOONN
     The lloocckkff utility acquires an exclusive lock on a _f_i_l_e, creating it if
     necessary, _a_n_d _r_e_m_o_v_i_n_g _t_h_e _f_i_l_e _o_n _e_x_i_t _u_n_l_e_s_s _e_x_p_l_i_c_i_t_l_y _t_o_l_d _n_o_t _t_o_.
     While holding the lock, it executes a _c_o_m_m_a_n_d with optional _a_r_g_u_m_e_n_t_s.
     After the _c_o_m_m_a_n_d completes, lloocckkff releases the lock, and removes the
     _f_i_l_e unless the --kk option is specified.  BSD-style locking is used, as
     described in flock(2); the mere existence of the _f_i_l_e is not considered
     to constitute a lock.

     lloocckkff may also be used to operate on a file descriptor instead of a file.
     If no _c_o_m_m_a_n_d is supplied, then _f_d must be a file descriptor.  The ver‐
     sion with a _c_o_m_m_a_n_d may also be used with a file descriptor by supplying
     it as a path _/_d_e_v_/_f_d_/_N, where N is the desired file descriptor.  The --kk
     option is implied when a file descriptor is in use, and the --nn and --ww op‐
     tions are silently ignored.  This can be used to lock inside a shell
     script.

     If the lloocckkff utility is being used to facilitate concurrency between a
     number of processes, it is recommended that the --kk option be used.  This
     will guarantee lock ordering, as well as implement a performance enhanced
     algorithm which minimizes CPU load associated with concurrent unlink,
     drop and re-acquire activity.  It should be noted that if the --kk option
     is not used, then no guarantees around lock ordering can be made.

     The following options are supported:

     --kk          Causes the lock file to be kept (not removed) after the com‐
                 mand completes.

     --ss          Causes lloocckkff to operate silently.  Failure to acquire the
                 lock is indicated only in the exit status.

     --nn          Causes lloocckkff to fail if the specified lock _f_i_l_e does not ex‐
                 ist.  If --nn is not specified, lloocckkff will create _f_i_l_e if nec‐
                 essary.

     --tt _s_e_c_o_n_d_s  Specifies a timeout for waiting for the lock.  By default,
                 lloocckkff waits indefinitely to acquire the lock.  If a timeout
                 is specified with this option, lloocckkff will wait at most the
                 given number of _s_e_c_o_n_d_s before giving up.  A timeout of 0 may
                 be given, in which case lloocckkff will fail unless it can acquire
                 the lock immediately.  When a lock times out, _c_o_m_m_a_n_d is _n_o_t
                 executed.

     --ww          Causes lloocckkff to open _f_i_l_e for writing rather than reading.
                 This is necessary on filesystems (including NFSv4) where a
                 file which has been opened read-only cannot be exclusively
                 locked.

     In no event will lloocckkff break a lock that is held by another process.

EEXXIITT SSTTAATTUUSS
     If lloocckkff successfully acquires the lock, it returns the exit status pro‐
     duced by _c_o_m_m_a_n_d.  Otherwise, it returns one of the exit codes defined in
     sysexits(3), as follows:

     EX_TEMPFAIL   The specified lock file was already locked by another
                   process.

     EX_CANTCREAT  The lloocckkff utility was unable to create the lock file, e.g.,
                   because of insufficient access privileges.

     EX_UNAVAILABLE
                   The --nn option is specified and the specified lock file does
                   not exist.

     EX_USAGE      There was an error on the lloocckkff command line.

     EX_OSERR      A system call (e.g., fork(2)) failed unexpectedly.

     EX_SOFTWARE   The _c_o_m_m_a_n_d did not exit normally, but may have been sig‐
                   naled or stopped.

EEXXAAMMPPLLEESS
     The first job takes a lock and sleeps for 5 seconds in the background.
     The second job tries to get the lock and timeouts after 1 second (PID
     numbers will differ):

           $ lockf mylock sleep 5 & lockf -t 1 mylock echo "Success"
           [1] 94410
           lockf: mylock: already locked

     The first job takes a lock and sleeps for 1 second in the background.
     The second job waits up to 5 seconds to take the lock and echoes the mes‐
     sage on success (PID numbers will differ):

           $ lockf mylock sleep 1 & lockf -t 5 mylock echo "Success"
           [1] 19995
           Success
           [1]+  Done                    lockf mylock sleep 1
     Lock a file and run a script, return immediately if the lock is not
     available. Do not delete the file afterward so lock order is guaranteed.

           $ lockf -t 0 -k /tmp/my.lock myscript

     Protect a section of a shell script with a lock, wait up to 5 seconds for
     it to become available.  Note that the shell script has opened the lock
     file _/_t_m_p_/_m_y_._l_o_c_k, and lloocckkff is performing the lock call exclusively via
     the passed in file descriptor (9).  In this case --kk is implied, and --ww
     has no effect because the file has already been opened by the shell.
     This example assumes that ‘>’ is implemented in the shell by opening and
     truncating _/_t_m_p_/_m_y_._l_o_c_k, rather than by replacing the lock file.

           (
                   lockf -s -t 5 9
                   if [ $? -ne 0 ]; then
                           echo "Failed to obtain lock"
                           exit 1
                   fi

                   echo Start
                   # Do some stuff
                   echo End
           ) 9>/tmp/my.lock

SSEEEE AALLSSOO
     flock(2), lockf(3), sysexits(3)

HHIISSTTOORRYY
     A lloocckkff utility first appeared in FreeBSD 2.2.

AAUUTTHHOORRSS
     John Polstra <_j_d_p_@_p_o_l_s_t_r_a_._c_o_m>

BSD                            November 25, 2023                           BSD
