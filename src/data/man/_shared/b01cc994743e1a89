The  system  call  changes the specified pages to have protection
Not all implementations will guarantee protection on a page basis
but Mac OS X’s current implementation does.  When a program  vio‐
lates the protections of a page, it gets a SIGBUS or SIGSEGV sig‐
nal.   Currently  can be one or more of the following: No permis‐
sions at all.  The pages can be read.  The pages can be  written.
The  pages  can be executed.  Upon successful completion, a value
of 0 is returned.  Otherwise, a value of ‐1 is  returned  and  is
set  to  indicate the error.  will fail if: The requested protec‐
tion conflicts with the access permissions of the process on  the
specified  address  range.   is  not  a multiple of the page size
(i.e.  is not page‐aligned).  The specified address range is out‐
side of the address range of the process or includes an  unmapped
page.  The combination of accesses requested in is not supported.
The include file is necessary.
The type of has changed.  The function first appeared in 4.4BSD.
