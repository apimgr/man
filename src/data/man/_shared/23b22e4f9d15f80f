GROUPMEMS(8)              System Management Commands              GROUPMEMS(8)



NNAAMMEE
       groupmems - administer members of a user's primary group

SSYYNNOOPPSSIISS
       ggrroouuppmmeemmss -a _u_s_e_r___n_a_m_e | -d _u_s_e_r___n_a_m_e | [-g _g_r_o_u_p___n_a_m_e] | -l | -p

DDEESSCCRRIIPPTTIIOONN
       The ggrroouuppmmeemmss command allows a user to administer their own group
       membership list without the requirement of superuser privileges. The
       ggrroouuppmmeemmss utility is for systems that configure its users to be in
       their own name sake primary group (i.e., guest / guest).

       Only the superuser, as administrator, can use ggrroouuppmmeemmss to alter the
       memberships of other groups.

OOPPTTIIOONNSS
       The options which apply to the ggrroouuppmmeemmss command are:

       --aa, ----aadddd _u_s_e_r___n_a_m_e
           Add a user to the group membership list.

           If the /etc/gshadow file exist, and the group has no entry in the
           /etc/gshadow file, a new entry will be created.

       --dd, ----ddeelleettee _u_s_e_r___n_a_m_e
           Delete a user from the group membership list.

           If the /etc/gshadow file exist, the user will be removed from the
           list of members and administrators of the group.

           If the /etc/gshadow file exist, and the group has no entry in the
           /etc/gshadow file, a new entry will be created.

       --gg, ----ggrroouupp _g_r_o_u_p___n_a_m_e
           The superuser can specify which group membership list to modify.

       --hh, ----hheellpp
           Display help message and exit.

       --ll, ----lliisstt
           List the group membership list.

       --pp, ----ppuurrggee
           Purge all users from the group membership list.

           If the /etc/gshadow file exist, and the group has no entry in the
           /etc/gshadow file, a new entry will be created.

       --RR, ----rroooott _C_H_R_O_O_T___D_I_R
           Apply changes in the _C_H_R_O_O_T___D_I_R directory and use the configuration
           files from the _C_H_R_O_O_T___D_I_R directory. Only absolute paths are
           supported.

SSEETTUUPP
       The ggrroouuppmmeemmss executable should be in mode 2710 as user _r_o_o_t and in
       group _g_r_o_u_p_s. The system administrator can add users to group _g_r_o_u_p_s to
       allow or disallow them using the ggrroouuppmmeemmss utility to manage their own
       group membership list.

                $ groupadd -r groups
                $ chmod 2710 groupmems
                $ chown root.groups groupmems
                $ groupmems -g groups -a gk4


CCOONNFFIIGGUURRAATTIIOONN
       The following configuration variables in /etc/login.defs change the
       behavior of this tool:

       MMAAXX__MMEEMMBBEERRSS__PPEERR__GGRROOUUPP (number)
           Maximum members per group entry. When the maximum is reached, a new
           group entry (line) is started in /etc/group (with the same name,
           same password, and same GID).

           The default value is 0, meaning that there are no limits in the
           number of members in a group.

           This feature (split group) permits to limit the length of lines in
           the group file. This is useful to make sure that lines for NIS
           groups are not larger than 1024 characters.

           If you need to enforce such limit, you can use 25.

           Note: split groups may not be supported by all tools (even in the
           Shadow toolsuite). You should not use this variable unless you
           really need it.

FFIILLEESS
       /etc/group
           Group account information.

       /etc/gshadow
           secure group account information

SSEEEE AALLSSOO
       cchhffnn(1), cchhsshh(1), ppaasssswwdd(1), ggrroouuppaadddd(8), ggrroouuppddeell(8), uusseerraadddd(8),
       uusseerrddeell(8), uusseerrmmoodd(8).



shadow-utils 4.13                 12/14/2025                      GROUPMEMS(8)
