fills  a  buffer with random data, which can be used as input for
process‐context pseudorandom generators like The  maximum  buffer
size  permitted  is 256 bytes.  If exceeds this, an error of will
be indicated.  should be used as a replacement  for  when  random
data  derived  directly  from the kernel random byte generator is
required.  Unlike the pseudo‐devices, it  is  not  vulnerable  to
file  descriptor  exhaustion  attacks and is available when sand‐
boxed or in a chroot, making it more reliable for security‐criti‐
cal applications.  However, it should be noted that is  primarily
intended  for  use  in  the construction and seeding of userspace
PRNGs like or Clients who simply require random data  should  use
from  or  from  the Security framework instead of or will succeed
unless: The parameter points to an  invalid  address.   Too  many
bytes  requested,  or some other fatal error occurred.  The func‐
tion appeared in OSX 10.12
