The  command configures vnode pseudo disk devices.  It will asso‐
ciate the vnode disk with the regular file allowing the latter to
be accessed as though it were  a  disk.   Hence  a  regular  file
within  the  filesystem can be used for swapping or can contain a
filesystem that is mounted in the name space.  The is  a  special
file of raw partition or name of vnode disk like Options indicate
an action to be performed: Configures the device.  If successful,
references  to  will  access the contents of If is specified, the
vnode device will emulate the specified disk geometry.  The  for‐
mat  of the argument is: If geometry is not specified, the kernel
will choose a default based on 1MB cylinders.  is the  number  of
bytes  per  sector.  It must be a power of two, and at least 512.
is the number of sectors per track.  is the number of tracks  per
cylinder.   is  the number of cylinders in the device.  Force un‐
configuration if the device is in use.  Does not imply  Specifies
that the option should look up in instead of in Configure the de‐
vice  to  use  regular  file  I/O  even  when  direct  I/O  using
bmap/strategy would be possible.  List the vnd devices and  indi‐
cate  which  ones are in use.  If one or more specific are given,
then only those will be described.  Together with and if no  spe‐
cific  devices  are  given, causes at least devices to be listed.
The default for is 4, but all vnd devices up  to  (and  sometimes
just beyond) the highest numbered vnd device configured since the
system last booted will be listed.  If is set to 0, then only vnd
devices  currently in use will be shown.  Configure the device as
read‐only.  If configuring the device, look up  in  and  use  the
geometry  specified  in  the entry.  This option and the argument
are mutually exclusive.  Unconfigures the device.  Print messages
to stdout describing actions taken.  Assume that is a  compressed
disk image in cloop2 format, and configure it read‐only.  See the
manpage  on  how to create such an image.  If no action option is
given, is assumed.  will exit with status 0 if the operation  re‐
quested  completed  successfully,  or  1 otherwise.  Unsuccessful
completion can be caused by unknown or incorrectly used  options;
attempting  to configure a vnd that is already configured; or un‐
configure one that is not, or without one which is still in  use;
or if devices are specified that do not exist or are not devices,
giving  an  improper geometry, etc.  or Configures the vnode disk
Please note that use of the second form of the  command  is  dis‐
couraged because it requires knowledge of the raw partition which
varies  between architectures.  For the first form, be aware that
there must not be a file in the current directory, or it will  be
assumed to be the vnd device to be configured (which will usually
fail.)   Configures  the vnode disk emulating the geometry of 512
bytes per sector, 18 sectors per track, 2  tracks  per  cylinder,
and 80 cylinders total.  Configures the vnode disk for an UDF im‐
age  to be used with Configures the vnode disk using the geometry
specified in the entry in Unconfigures  the  device.   To  obtain
status  on all vnd devices listed in (assuming a system where the
partition is the whole device use: Using will omit those  devices
that  are  not in use, whereas will list all devices known to the
kernel (at least 4 without regardless of what might appear in (or
elsewhere.)  The command appeared in It was  renamed  to  in  for
consistency  with other similar commands.  (The original name was
also retained as an alternative for backwards compatibility.)
