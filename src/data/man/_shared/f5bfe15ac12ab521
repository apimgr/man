and some of the architectures can run executables.  Most executa‐
bles will work.  The exceptions include programs that use the kvm
library,  and  various system calls, or kernel semantics that are
difficult to emulate.  The number of reasons why a program  might
fail  to  work is (thankfully) longer than the number of programs
that fail to run.  Static executables will normally  run  without
any  extra  setup.   This  procedure  details the directories and
files that must be set up to allow dynamically linked executables
to work.  The files you need are on your machine.   You  need  to
worry about the legal issues of ensuring that you have a right to
use  the required files on your machine.  On your machine, do the
following: If you ever expect to use YP, you will want to  create
a  link:  ln  ‐s  /var/run/ypbind.lock  /etc/ypbind.lock Alterna‐
tively, you can use an NFS mount to accomplish the  same  effect.
On  your machine, do the following: This will place the libraries
on your machine in a location where the compatibility  code  will
look  for first, where they do not conflict with the standard li‐
braries.  When using on the option must also be used.  A list  of
things  which  fail to work in compatibility mode should be here.
executables can not handle directory offset cookies  >  32  bits.
Should  such an offset occur, you will see the message Currently,
this can only happen on NFS  mounted  filesystems,  mounted  from
servers  that  return  offsets  with  information in the upper 32
bits.  These errors should rarely happen, but can be  avoided  by
mounting  this  filesystem  with offset translation enabled.  See
the option to The option to will also have  the  desired  effect,
but  is  less  preferable.  The support is less complete than the
other ports.
