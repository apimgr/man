The  file  specifies the additional addresses (aliases) that each
interface has.  is processed by at system boot time.   Each  line
of  the  file  is  of  the  form: The is a network address of the
alias.  This must be a number, or must be in since the nameserver
is not running at this point.  The is the network  interface  the
alias  will  be  configured on.  The is the netmask of the alias’
network address.  Although this is optional, omitting the netmask
is discouraged.  Omission results in the use of the classful net‐
mask associated with The file appeared in assumes IPv4, and  does
not  support  other protocol families.  Please check for alterna‐
tives like
