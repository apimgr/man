Extended  attributes extend the basic attributes of files and di‐
rectories in the file system.  They are stored as name:data pairs
associated with file system  objects  (files,  directories,  sym‐
links, etc).  The function retrieves up to bytes of data from the
extended attribute identified by associated with into the pre‐al‐
located  buffer  pointed to by The function returns the number of
bytes of data retrieved.  An extended  attribute’s  is  a  simple
NULL‐terminated UTF‐8 string.  specifies an offset within the ex‐
tended  attribute.   In the current implementation, this argument
is only used with the resource fork attribute.  For all other ex‐
tended attributes, this parameter is reserved and should be zero.
On success, contains the data associated  with  When  is  set  to
NULL, returns current size of the named attribute.  This facility
can  be used to determine the size of a buffer sufficiently large
to hold the data currently associated with the attribute.   spec‐
ify  options  for  retrieving  extended attributes: do not follow
symbolic links.  normally returns information from the target  of
if it is a symbolic link.  With this option, will return extended
attribute data from the symbolic link instead.  do not follow any
symbolic  links  encountered during pathname resolution. An error
is returned if a symlink is encountered before the last component
of path.  If does not reside in the hierarchy beneath the  start‐
ing  directory,  an  error is returned.  and will return HFS Plus
Compression extended attribute (if present) for the file referred
to by or is identical to except that it  retrieves  extended  at‐
tribute data from the open file referenced by the file descriptor
On  success, the size of the extended attribute data is returned.
On failure, ‐1 is returned and the global variable is set as fol‐
lows.  The extended attribute does not exist.   The  file  system
does not support extended attributes or has the feature disabled.
(as  indicated  by  is  too  small to hold the extended attribute
data.  The named attribute is not permitted for this type of  ob‐
ject.  is invalid or has an unsupported bit set.  XATTR_NOFOLLOW,
XATTR_NOFOLLOW_ANY  or XATTR_RESOLVE_BENEATH option was passed in
or do not refer to a regular file and the attribute  in  question
is  only  applicable to files.  Similar to EPERM.  A component of
prefix is not a directory.  The length of exceeds UTF‐8 bytes, or
a component of exceeds characters, or the entire exceeds  charac‐
ters.   Search permission is denied for a component of or the at‐
tribute is not allowed to be read (e.g. an ACL prohibits  reading
the  attributes  of this file).  Too many symbolic links were en‐
countered in translating the pathname.  If XATTR_NOFOLLOW_ANY op‐
tion was passed and a symbolic link was encountered in  translat‐
ing  the  pathname.  If XATTR_RESOLVE_BENEATH was passed and does
not reside in the directory hierarchy beneath the starting direc‐
tory.  or points to an invalid address.  An  I/O  error  occurred
while  reading from or writing to the file system.  and first ap‐
peared in Mac OS X 10.4.
