VOP_READ_PGCACHE(9)      BSD Kernel Developer's Manual     VOP_READ_PGCACHE(9)

NNAAMMEE
     VVOOPP__RREEAADD__PPGGCCAACCHHEE — read a file, fast

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ppaarraamm..hh>>
     ##iinncclluuddee <<ssyyss//vvnnooddee..hh>>
     ##iinncclluuddee <<ssyyss//uuiioo..hh>>

     _i_n_t
     VVOOPP__RREEAADD__PPGGCCAACCHHEE(_s_t_r_u_c_t _v_n_o_d_e _*_v_p, _s_t_r_u_c_t _u_i_o _*_u_i_o, _i_n_t _i_o_f_l_a_g,
         _s_t_r_u_c_t _u_c_r_e_d _*_c_r_e_d);

DDEESSCCRRIIPPTTIIOONN
     This entry point reads the contents of a file.  The intent is to provide
     the data from caches, which do not require expensive operations or any
     disk IO.  For instance, if filesystem uses normal VM page cache and main‐
     tains v_object lifetime, it can use vn_read_from_obj(9) helper to return
     data from the resident vp->v_object pages.

     The filesystem indicates support for the VVOOPP__RREEAADD__PPGGCCAACCHHEE on specific vn‐
     ode by setting the VIRF_PGREAD flag in vp->v_irflag.

     The function does not need to satisfy the whole request; it also might
     choose to not provide any data.  In these cases, the _u_i_o must be advanced
     by the amount of read data, VVOOPP__RREEAADD__PPGGCCAACCHHEE should return EJUSTRETURN,
     and VFS would handle the rest of the read operation using the
     VOP_READ(9).

     The VFS layer does the same deadlock avoidance for accessing userspace
     pages from VVOOPP__RREEAADD__PPGGCCAACCHHEE as for VOP_READ(9).

     Vnode is not locked on the call entry and should not be locked on return.
     For a filesystem that requires vnode lock to return any data, it does not
     make sense to implement VVOOPP__RREEAADD__PPGGCCAACCHHEE (and set VIRF_PGREAD flag) since
     VFS arranges the call to VOP_READ(9) as needed.

     The arguments are:

     _v_p      The vnode of the file.

     _u_i_o     The location of the data to be read.

     _i_o_f_l_a_g  Various flags, see VOP_READ(9) for the list.

     _c_r_e_d    The credentials of the caller.

     VVOOPP__RREEAADD__PPGGCCAACCHHEE does not handle non-zero _i_o_f_l_a_g argument.

LLOOCCKKSS
     The file should be referenced on entry on entry and will still be refer‐
     enced on exit.  Rangelock covering the whole read range should be owned
     around the call.

RREETTUURRNN VVAALLUUEESS
     Zero is returned on success, when the whole request is satisfied, and no
     more data cannot be provided for it by any means.  If more data can be
     returned, but VVOOPP__RREEAADD__PPGGCCAACCHHEE was unable to provide it, EJUSTRETURN must
     be returned.  The uio records should be updated according to the partial
     operation done.

     Otherwise an error code is returned, same as from VOP_READ(9)

SSEEEE AALLSSOO
     uiomove(9), vnode(9), VOP_READ(9)

BSD                            February 28, 2021                           BSD
