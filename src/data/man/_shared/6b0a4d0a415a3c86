_m_a_l_l_o_c___t_r_i_m(3)             Library Functions Manual             _m_a_l_l_o_c___t_r_i_m(3)

NNAAMMEE
       malloc_trim - release free memory from the heap

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<mmaalllloocc..hh>>

       iinntt mmaalllloocc__ttrriimm((ssiizzee__tt _p_a_d));;

DDEESSCCRRIIPPTTIIOONN
       The  mmaalllloocc__ttrriimm()  function  attempts  to release free memory from the
       heap (by calling ssbbrrkk(2) or mmaaddvviissee(2) with suitable arguments).

       The _p_a_d argument specifies the amount of free space to leave  untrimmed
       at the top of the heap.  If this argument is 0, only the minimum amount
       of  memory  is  maintained  at  the  top of the heap (i.e., one page or
       less).  A nonzero argument can be used to maintain some trailing  space
       at  the top of the heap in order to allow future allocations to be made
       without having to extend the heap with ssbbrrkk(2).

RREETTUURRNN VVAALLUUEE
       The mmaalllloocc__ttrriimm() function returns 1 if memory  was  actually  released
       back to the system, or 0 if it was not possible to release any memory.

EERRRROORRSS
       No errors are defined.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       mmaalllloocc__ttrriimm() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       GNU.

VVEERRSSIIOONNSS
       glibc 2.0.

NNOOTTEESS
       Only the main heap (using ssbbrrkk(2)) honors the _p_a_d argument; thread
       heaps do not.

       Since glibc 2.8 this function frees memory in all arenas and in all
       chunks with whole free pages.

       Before glibc 2.8 this function only freed memory at the top of the heap
       in the main arena.

SSEEEE AALLSSOO
       ssbbrrkk(2), mmaalllloocc(3), mmaalllloopptt(3)

Linux man‐pages (unreleased)      2025‐09‐21                    _m_a_l_l_o_c___t_r_i_m(3)
