A  file  contains instructions to the daemon of the general form:
There may be a system and each user may have their  own  Commands
in any given will be executed either as the user who owns the or,
in  the  case  of the system as the user specified on the command
line.  While a is a text file, it is not intended to be  directly
edited.   Creation, modification, and removal of a should be done
using Blank lines, leading spaces, and tabs are  ignored.   Lines
whose first non‐space character is a pound sign are comments, and
are ignored.  Note that comments are not allowed on the same line
as  commands, since they will be taken to be part of the command.
Similarly, comments are not allowed on the same line as  environ‐
ment  variable  settings.  An active line in a is either an envi‐
ronment variable setting or a command.  Environment variable set‐
tings create the environment any command in the is  run  in.   An
environment  variable  setting  is of the form: The spaces around
the equal sign  are  optional,  and  any  subsequent  non‐leading
spaces in will be part of the value assigned to The string may be
placed  in  quotes to preserve leading or trailing blanks.  Lines
in the system have six fixed fields plus a command, in the  form:
While  lines  in a user have five fixed fields plus a command, in
the form: Fields are separated by blanks or  tabs.   The  command
may  be  one  or  more  fields  long.  The allowed values for the
fields are: Lists are allowed.  A list is a set  of  numbers  (or
ranges)  separated  by commas.  For example, or Ranges of numbers
are allowed.  Ranges are two numbers  separated  with  a  hyphen.
The specified range is inclusive.  For example, 8–11 for an entry
specifies  execution at hours 8, 9, 10 and 11.  A field may begin
with a question mark which indicates a single value randomly  se‐
lected when the crontab file is read.  If the field contains only
a question mark, the value is randomly selected from the range of
all possible values for the field.  If the question mark precedes
a  range, the value is randomly selected from the range.  For ex‐
ample, specifies that a task  will  be  performed  daily  between
2:00am  and  and  5:59am  at  a  time  randomly selected when the
crontab file is first read.  As just one  example,  this  feature
can  be used to prevent a large number of hosts from contacting a
server simultaneously and overloading it by staggering  the  time
at  which a download script is executed.  Step values can be used
in conjunction with ranges (but not random ranges which represent
a single number).  Following a  range  with  specifies  skips  of
through  the  range.   For  example,  can be used in the field to
specify command execution every other hour.  Steps are also  per‐
mitted after an asterisk, so to say just use An asterisk is short
form for a range of all allowed values.  Names can be used in the
and fields.  Use the first three letters of the particular day or
month  (case  doesn’t  matter).  Ranges or lists of names are not
allowed.  The field (the rest of the line) is the command  to  be
run.   The entire command portion of the line, up to a newline or
% character, will be executed by or by the shell specified in the
variable of the Percent signs in the command, unless escaped with
a backslash will be changed into newline characters, and all data
after the first will be sent to the command  as  standard  input.
Commands may be modified as follows: No mail is sent after a suc‐
cessful  run.   The  execution  output will only be mailed if the
command exits with a non‐zero exit code.  The option  is  an  at‐
tempt to cure potentially copious volumes of mail coming from Ex‐
ecution  will  not  be logged.  Only a single instance of will be
run concurrently.  Additional instances of will not be  scheduled
until  the  earlier one completes.  Commands are executed by when
the and fields match the current time, when at least one  of  the
two  day  fields  or  match the current time.  Note: The day of a
command’s execution can be specified by two fields — and If  both
fields  are  restricted  (i.e. aren’t *), the command will be run
when field matches the current time.  For example, would cause  a
command  to  be run at 4:30 am on the 1st and 15th of each month,
plus every Friday.  Instead of the  first  five  fields,  one  of
eight  special  strings may appear: The variable can be set to an
alternate time zone in order to affect when the job is run.  Note
that this only affects the scheduling of the job,  not  the  time
zone  that  the  job perceives when it is run.  If is defined but
empty jobs are scheduled with respect to  the  local  time  zone.
The variable should indicate the number of seconds within a job’s
scheduled time that it should still be run.  For example if a job
is  scheduled  for  12:30pm and is (2 minutes), then the job will
not be run if the system attempts to start it past 12:32pm.  On a
heavily loaded system, or on a system that  has  just  been  jobs
will sometimes start later than originally intended, and by skip‐
ping  non‐critical  jobs  because  of  delays, system load can be
lightened.  If is defined but empty or set to  some  non‐positive
value  (0,  a  negative  number,  or a non‐numeric string), it is
treated as if it was unset, that is the job will always run, even
if it is going to start at a time long past its  scheduled  time.
Set  from the user’s entry.  May be overridden by settings in the
Set from the user’s entry.  May not be overridden by settings  in
the  If  is  defined  and  non‐empty, mail is sent to the user so
named.  If is defined but empty no mail will be sent.   Otherwise
mail  is sent to the owner of the This is useful for pseudo‐users
that lack an alias that would otherwise redirect the  mail  to  a
real  person.   Set  to  May be overridden by settings in the Set
from the user’s entry.  May not be overridden by settings in  the
System crontab.  User crontab.  # use /bin/sh to run commands, no
matter  what  /etc/passwd says SHELL=/bin/sh # mail any output to
‘paul’, no matter whose crontab this is MAILTO=paul # # run  five
minutes    after    midnight,    every    day   5   0   *   *   *
$HOME/bin/daily.job >> $HOME/tmp/out 2>&1 # run at 2:15pm on  the
first  of  every  month  ‐‐  output  mailed  to  paul 15 14 1 * *
$HOME/bin/monthly # run at 10 pm on weekdays, annoy Joe 0 22 *  *
1‐5    mail  ‐s  "It’s  10pm"  joe%Joe,%%Where are your kids?% 23
0‐23/2 * * * echo "run 23  minutes  after  midn,  2am,  4am  ...,
everyday"  5  4  * * sun     echo "run at 5 after 4 every sunday"
The file format is compliant with the specification.  The  behav‐
iours  described  below  are all extensions to that standard: The
field may use 7 to represent Sunday.  Ranges may  include  Months
or  days  of  the week can be specified by name.  Mailing after a
successful run can be suppressed with Logging can  be  suppressed
with Environment variables can be set in a crontab.  Command out‐
put  can  be  mailed to a person other than the crontab owner, or
the feature can be turned off and no mail will be  sent  at  all.
All  of  the  commands that can appear in place of the first five
fields.  was written by
