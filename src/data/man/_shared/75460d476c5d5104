With  an argument naming an existing causes system accounting in‐
formation for every process executed to be placed at the  end  of
the  file.   If  no  argument is given, accounting is turned off.
The default accounting file is Typically, accounting  is  enabled
by  rc  scripts  during  the boot process.  In one may enable ac‐
counting by setting the variable to in Note that,  traditionally,
the  system accounting log file can not be rotated cleanly by In‐
stead, a default installation of rotates using the  script.   De‐
fault accounting file.
