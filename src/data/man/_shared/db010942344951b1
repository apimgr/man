The   driver   provides   support   for  NS8250‐,  NS16450‐,  and
NS16550‐based communications interfaces.  The NS8250 and  NS16450
have single character buffers, and the NS16550 has a 16 character
buffer.  Input and output for each line may set to one of follow‐
ing  baud  rates;  50, 75, 110, 134.5, 150, 300, 600, 1200, 1800,
2400, 4800, 9600, 19200, 38400, 57600, or 115200,  or  any  other
baud  rate  which  is  a factor of 115200.  The ttyXX devices are
traditional dial‐in devices; the dtyXX devices are used for dial‐
out.  (See adds support for the Hayes ESP serial board.   enables
code to use the Data Carrier Detect (DCD) signal line for attach‐
ment  to an external precision clock source e.g., which generates
a Pulse Per Second (PPS) signal.  This is used by  to  discipline
the  system  clock,  and more accurately count/measure time.  See
for more discussion.  With enabled, the driver  can  be  used  to
collect  entropy  for the entropy pool.  The entropy is generated
from interrupt randomness.  If is specified, the driver will  not
set the bit on the UART.  This is mainly for use on AST multiport
boards,  where  the bit is used to control whether or not the de‐
vices use a shared interrupt.  The input has overflowed  and  in‐
coming  data  has  been  lost.  The device has generated an unex‐
pected interrupt with the code listed.  The driver was originally
derived from the HP9000/300 driver.  Data  loss  is  possible  on
busy  systems  with  unbuffered UARTs at high speed.  The name of
this driver and the constants which define the locations  of  the
various serial ports are holdovers from
