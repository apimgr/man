_m_q___s_e_n_d(3)                 Library Functions Manual                 _m_q___s_e_n_d(3)

NNAAMMEE
       mq_send, mq_timedsend - send a message to a message queue

LLIIBBRRAARRYY
       Real‐time library (_l_i_b_r_t, _-_l_r_t)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<mmqquueeuuee..hh>>

       iinntt mmqq__sseenndd((size_t msg_len;
                     mmqqdd__tt _m_q_d_e_s,, ccoonnsstt cchhaarr _m_s_g___p_t_r[[_m_s_g___l_e_n]],,
                     ssiizzee__tt _m_s_g___l_e_n,, uunnssiiggnneedd iinntt _m_s_g___p_r_i_o));;

       ##iinncclluuddee <<ttiimmee..hh>>
       ##iinncclluuddee <<mmqquueeuuee..hh>>

       iinntt mmqq__ttiimmeeddsseenndd((size_t msg_len;
                     mmqqdd__tt _m_q_d_e_s,, ccoonnsstt cchhaarr _m_s_g___p_t_r[[_m_s_g___l_e_n]],,
                     ssiizzee__tt _m_s_g___l_e_n,, uunnssiiggnneedd iinntt _m_s_g___p_r_i_o,,
                     ccoonnsstt ssttrruucctt ttiimmeessppeecc **_a_b_s___t_i_m_e_o_u_t));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       mmqq__ttiimmeeddsseenndd():
           _POSIX_C_SOURCE >= 200112L

DDEESSCCRRIIPPTTIIOONN
       mmqq__sseenndd() adds the message pointed to by _m_s_g___p_t_r to the message queue
       referred to by the message queue descriptor _m_q_d_e_s.  The _m_s_g___l_e_n argu‐
       ment specifies the length of the message pointed to by _m_s_g___p_t_r; this
       length must be less than or equal to the queue’s _m_q___m_s_g_s_i_z_e attribute.
       Zero‐length messages are allowed.

       The _m_s_g___p_r_i_o argument is a nonnegative integer that specifies the pri‐
       ority of this message.  Messages are placed on the queue in decreasing
       order of priority, with newer messages of the same priority being
       placed after older messages with the same priority.  See mmqq__oovveerrvviieeww(7)
       for details on the range for the message priority.

       If the message queue is already full (i.e., the number of messages on
       the queue equals the queue’s _m_q___m_a_x_m_s_g attribute), then, by default,
       mmqq__sseenndd() blocks until sufficient space becomes available to allow the
       message to be queued, or until the call is interrupted by a signal han‐
       dler.  If the OO__NNOONNBBLLOOCCKK flag is enabled for the message queue descrip‐
       tion, then the call instead fails immediately with the error EEAAGGAAIINN.

       mmqq__ttiimmeeddsseenndd() behaves just like mmqq__sseenndd(), except that if the queue is
       full and the OO__NNOONNBBLLOOCCKK flag is not enabled for the message queue de‐
       scription, then _a_b_s___t_i_m_e_o_u_t points to a structure which specifies how
       long the call will block.  This value is an absolute timeout in seconds
       and nanoseconds since the Epoch, 1970‐01‐01 00:00:00 +0000 (UTC), spec‐
       ified in a ttiimmeessppeecc(3) structure.

       If the message queue is full, and the timeout has already expired by
       the time of the call, mmqq__ttiimmeeddsseenndd() returns immediately.

RREETTUURRNN VVAALLUUEE
       On success, mmqq__sseenndd() and mmqq__ttiimmeeddsseenndd() return zero; on error, -1 is
       returned, with _e_r_r_n_o set to indicate the error.

EERRRROORRSS
       EEAAGGAAIINN The queue was full, and the OO__NNOONNBBLLOOCCKK flag was set for the mes‐
              sage queue description referred to by _m_q_d_e_s.

       EEBBAADDFF  The  descriptor specified in _m_q_d_e_s was invalid or not opened for
              writing.

       EEIINNTTRR  The call was interrupted by a signal handler; see ssiiggnnaall(7).

       EEIINNVVAALL The call would have blocked, and _a_b_s___t_i_m_e_o_u_t was invalid, either
              because _t_v___s_e_c was less than zero, or because _t_v___n_s_e_c  was  less
              than zero or greater than 1000 million.

       EEMMSSGGSSIIZZEE
              _m_s_g___l_e_n was greater than the _m_q___m_s_g_s_i_z_e attribute of the message
              queue.

       EETTIIMMEEDDOOUUTT
              The call timed out before a message could be transferred.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       mmqq__sseenndd(), mmqq__ttiimmeeddsseenndd() T}   Thread safety  MT‐Safe

VVEERRSSIIOONNSS
       On Linux, mmqq__ttiimmeeddsseenndd() is a system call, and mmqq__sseenndd() is a library
       function layered on top of that system call.

SSTTAANNDDAARRDDSS
       POSIX.1‐2008.

HHIISSTTOORRYY
       POSIX.1‐2001.

SSEEEE AALLSSOO
       mmqq__cclloossee(3), mmqq__ggeettaattttrr(3), mmqq__nnoottiiffyy(3), mmqq__ooppeenn(3), mmqq__rreecceeiivvee(3),
       mmqq__uunnlliinnkk(3), ttiimmeessppeecc(3), mmqq__oovveerrvviieeww(7), ttiimmee(7)

Linux man‐pages (unreleased)      2025‐09‐21                        _m_q___s_e_n_d(3)
