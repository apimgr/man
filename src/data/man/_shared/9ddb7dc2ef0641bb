takes two arguments, a name to be resolved and a string, known as
a  It  then  prints  the information returned by the Hesiod name‐
server.  The value returned by is of  the  type  understands  the
following  options: Selects long format.  Prints the fully‐quali‐
fied string passed to the nameserver.   The  following  types  of
identifiers  may  be used in the argument to These values will be
resolved by  accessing  the  database.   the  8‐character‐or‐less
string  used  to  identify  users or classes (e.g. joeuser, root,
1.00, etc).  Used with the and the id number assigned to a  user.
the  id  number assigned to a group.  a name identifying a unique
group.  the name of an athena file system.  the name of an  rvd’s
server  and pack separated by a colon.  the name of an nfs server
and its partition separated by a colon.  the machine name  of  an
Athena  workstation  (e.g. E40‐343‐3).  name of an Athena service
(e.g. Zephyr).  name of Unix service (valid entries  are  defined
in  name  of  a  printer.  name of an Athena print cluster.  some
calls (e.g., do not require a specific  argument.   However,  you
must  include  a  dummy  string (e.g., for to work properly.  The
following symbols are valid substitutions for the argument to re‐
turns string suitable for inclusion in searching with returns in‐
formation on the pobox assigned to the user specified by  search‐
ing  with returns string suitable for inclusion in searching with
returns string suitable for inclusion in searching  with  returns
string suitable for inclusion in searching with returns subgroups
included  in superset defined by returns file system type, export
point, server, mount mode, and import  point  for  the  following
valid (see above) ‐ and returns information about the local clus‐
ter  the  workstation, specified by Included is information about
the local file and print servers.  This information  is  accesses
by  at  boot  time.  returns network name of service host for re‐
turns Internet protocol type and protocol service  port  for  re‐
turns  a  valid  entry  for for returns a list of print clusters.
returns a list of printers in a cluster specified by  IBM/Project
Athena  Copyright 1987, 1988, 1996 by the Massachusetts Institute
of Technology.
