This  manual page describes features specific to the file server.
Please see for a full description of the available  command  line
options.   The utility can be used to mount nfs file systems.  It
uses and to facilitate running the file system  as  a  server  in
userspace.   As  opposed  to does not use file system code within
the kernel and therefore does not require kernel  support  except
Apart  from  a  minor speed penalty there is no downside with re‐
spect to in‐kernel code.  Even though the file system client runs
within a virtual rump kernel in userspace, it uses  host  network
services  This means that regardless of whether using or the same
network configurations will be used.  Currently,  supports  IPv4.
To use via the flags and should be given.  Similarly, is used in‐
stead  of  if  is added to the options field of The utility first
appeared in
