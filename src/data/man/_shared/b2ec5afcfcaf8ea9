SPAMD-SETUP(8)            BSD System Manager's Manual           SPAMD-SETUP(8)

NNAAMMEE
     ssppaammdd--sseettuupp — parse and load file of spammer addresses

SSYYNNOOPPSSIISS
     ssppaammdd--sseettuupp [--bbDDddnn]

DDEESSCCRRIIPPTTIIOONN
     The ssppaammdd--sseettuupp utility sends blacklist data to spamd(8), as well as con‐
     figuring mail rejection messages for blacklist entries.

     When ssppaammdd--sseettuupp is run in blacklist only mode, it also sends blacklist
     data to the pf(4) table <_s_p_a_m_d>.  The <_s_p_a_m_d> table must then be used in
     conjunction with a pf(4) redirection rule to selectively redirect mail
     connections to spamd(8).

     The options are as follows:

     --bb      Blacklisting only mode.  Blacklist data is normally stored only
             in spamd(8).  With this flag, data is stored in both spamd(8) and
             pf(4).  Use this flag if spamd(8) is running with the --bb flag
             too.

     --DD      Daemonize; run ssppaammdd--sseettuupp in the background.

     --dd      Debug mode reports a few pieces of information.

     --nn      Dry-run mode.  No data is shipped.

     Lists are specified in the configuration file _/_e_t_c_/_m_a_i_l_/_s_p_a_m_d_._c_o_n_f and
     are processed in the order specified in the _a_l_l tag.  Output is concate‐
     nated and sent to a running spamd(8).  Addresses are sent along with the
     message spamd will give on mail rejection when a matching client con‐
     nects.  The configuration port for spamd(8) is found from services(5), by
     looking for the named service _s_p_a_m_d_-_c_f_g.

     ssppaammdd--sseettuupp reads all configuration information from the spamd.conf(5)
     file.

FFIILLEESS
     _/_e_t_c_/_m_a_i_l_/_s_p_a_m_d_._c_o_n_f

SSEEEE AALLSSOO
     pf.conf(5), services(5), spamd.conf(5), spamd(8)

BBUUGGSS
     Blacklists removed from _/_e_t_c_/_m_a_i_l_/_s_p_a_m_d_._c_o_n_f are not automatically re‐
     moved from the running spamd(8).  If an entry is removed from
     _/_e_t_c_/_m_a_i_l_/_s_p_a_m_d_._c_o_n_f that is currently in use, it is necessary to restart
     spamd(8).  This applies only to blacklists that are removed entirely, not
     those that are simply modified.

BSD                             March 31, 2016                             BSD
