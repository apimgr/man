obtains service tickets for the specified Kerberos principals and
prints  out  the  version number of the each.  Supported options:
the credential cache to use.  encryption type to  use.   use  the
named  file  as  a  keytab file.  suppress printing.  specify the
service name used to obtain a principal.  print a help message.
