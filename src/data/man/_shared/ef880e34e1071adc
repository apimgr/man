MKNOD(8)                  BSD System Manager's Manual                 MKNOD(8)

NNAAMMEE
     mmkknnoodd — build special file

SSYYNNOOPPSSIISS
     mmkknnoodd _n_a_m_e
     mmkknnoodd _n_a_m_e [bb | cc] _m_a_j_o_r _m_i_n_o_r [_o_w_n_e_r:_g_r_o_u_p]

DDEESSCCRRIIPPTTIIOONN
     TThhee mmkknnoodd uuttiilliittyy iiss ddeepprreeccaatteedd oonn mmooddeerrnn FFrreeeeBBSSDD ssyysstteemmss..

     The mmkknnoodd utility creates device special files.  To make nodes manually,
     the arguments are:

     _n_a_m_e    Device name, for example _/_d_e_v_/_d_a_0 for a SCSI disk or _/_d_e_v_/_p_t_s_/_0
             for pseudo-terminals.

     bb | cc   Type of device.  If the device is a block type device such as a
             tape or disk drive which needs both cooked and raw special files,
             the type is bb.  All other devices are character type devices,
             such as terminal and pseudo devices, and are type cc.

     _m_a_j_o_r   The major device number is an integer number which tells the ker‐
             nel which device driver entry point to use.

     _m_i_n_o_r   The minor device number tells the kernel which subunit the node
             corresponds to on the device; for example, a subunit may be a
             file system partition or a tty line.

     _o_w_n_e_r:_g_r_o_u_p
             The _o_w_n_e_r _g_r_o_u_p operand pair is optional, however, if one is
             specified, they both must be specified.  The _o_w_n_e_r may be either
             a numeric user ID or a user name.  If a user name is also a nu‐
             meric user ID, the operand is used as a user name.  The _g_r_o_u_p may
             be either a numeric group ID or a group name.  Similar to the
             user name, if a group name is also a numeric group ID, the oper‐
             and is used as a group name.

     Major and minor device numbers can be given in any format acceptable to
     strtoul(3), so that a leading ‘0x’ indicates a hexadecimal number, and a
     leading ‘0’ will cause the number to be interpreted as octal.

     The mmkknnoodd utility can be used to recreate deleted device nodes under a
     devfs(5) mount point by invoking it with only a filename as an argument.
     Example:

           mknod /dev/cd0

     where _/_d_e_v_/_c_d_0 is the name of the deleted device node.

CCOOMMPPAATTIIBBIILLIITTYY
     The chown(8)-like functionality is specific to FreeBSD.

     As of FreeBSD 4.0, block devices were deprecated in favour of character
     devices.  As of FreeBSD 5.0, device nodes are managed by the device file
     system devfs(5), making the mmkknnoodd utility superfluous.  As of FreeBSD 6.0
     device nodes may be created in regular file systems but such nodes cannot
     be used to access devices.

SSEEEE AALLSSOO
     mkfifo(1), mknod(2), devfs(5), chown(8)

HHIISSTTOORRYY
     A mmkknnoodd utility appeared in Version 4 AT&T UNIX.

BSD                             October 3, 2016                            BSD
