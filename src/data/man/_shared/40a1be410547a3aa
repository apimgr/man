creates  the  directories  named as operands, in the order speci‐
fied, using mode as modified by the current The  options  are  as
follows: Set the file permission bits of the final created direc‐
tory  to  the specified mode.  The mode argument can be in any of
the formats specified to the utility.   If  a  symbolic  mode  is
specified,  the operation characters and are interpreted relative
to an initial mode of  Create  intermediate  directories  as  re‐
quired.  If this option is not specified, the full path prefix of
each  operand  must  already exist.  Intermediate directories are
created with permission bits of as modified by the current umask,
plus write and search permission for the owner.  Do not  consider
it  an  error if the argument directory already exists.  The user
must have write permission in the parent directory.  The  utility
is expected to be compatible.  A utility appeared in
