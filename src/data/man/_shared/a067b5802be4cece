The  module allows to run executables, to run executables, to run
executables, and to run executables.  On the default userland  is
N32 which is a handled by framework, and 64‐bit binaries are han‐
dled  similarly  to  the setup for 32‐bit compatibility.  It also
provides compatibility between OABI and EABI binaries  on  32‐bit
ports.   To  use  one must either have and in the kernel, or load
the compat_netbsd32 and exec_elf32 kernel modules.   Static  exe‐
cutables  typically  need  no additional setup.  Dynamic binaries
require the dynamic linker plus shared libraries.  Since the base
system has directly included support for 32‐bit compatibility  by
installing 32‐bit libraries and dynamic linker into This includes
compiler  support  for compiling 32‐bit applications on platforms
where this is supported.  For a.out compatibility, from a  32‐bit
distribution is required to exist, and the a.out shared libraries
must  be  found in as normal for a.out compatibility.  For 32‐bit
(64‐bit on ELF compatibility, the relevant needs to be  found  in
Note  that  the  kernel handles rewriting the builting ELF inter‐
preter to the above path.  Before all of these files needed to be
placed under The shared libraries for a.out binaries do not  live
under the directory, but under the directory, where the a.out dy‐
namic linker will find them.  A list of things which fail to work
in  compatibility  mode  should be here.  is not supported.  Some
commands are not supported, including
