This  filter  displays  the  first lines of each of the specified
files, or of the standard input if no files are specified.  If is
omitted it defaults to 10.  If is specified, counts bytes instead
of lines.  If more than a single file is specified, or the option
is used, each file is preceded by  a  header  consisting  of  the
string  where  is  the  name  of the file.  The flag disables the
printing of the header in all cases.  The historic  command  line
syntax  of  is supported by this implementation.  This command is
mostly compatible with GNU extensions to The utility conforms  to
The  utility  appeared  in It was enhanced to include the and op‐
tions for
