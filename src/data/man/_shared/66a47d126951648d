DWARF_GET_ABBREV_ENTR... BSD Library Functions Manual DWARF_GET_ABBREV_ENTR...

NNAAMMEE
     ddwwaarrff__ggeett__aabbbbrreevv__eennttrryy — retrieve attribute information from an abbrevia‐
     tion descriptor

LLIIBBRRAARRYY
     DWARF Access Library (libdwarf, -ldwarf)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<lliibbddwwaarrff..hh>>

     _i_n_t
     ddwwaarrff__ggeett__aabbbbrreevv__eennttrryy(_D_w_a_r_f___A_b_b_r_e_v _a_b_b_r_e_v, _D_w_a_r_f___S_i_g_n_e_d _n_d_x,
         _D_w_a_r_f___H_a_l_f _*_c_o_d_e, _D_w_a_r_f___S_i_g_n_e_d _*_f_o_r_m, _D_w_a_r_f___O_f_f _*_o_f_f_s_e_t,
         _D_w_a_r_f___E_r_r_o_r _*_e_r_r);

DDEESSCCRRIIPPTTIIOONN
     Function ddwwaarrff__ggeett__aabbbbrreevv__eennttrryy() retrieves attribute information from a
     DWARF abbreviation descriptor.

     Argument _a_b_b_r_e_v should be a valid abbreviation descriptor, as returned by
     function dwarf_get_abbrev(3).

     Argument _n_d_x specifies the 0-based index of the attribute.  The total
     count of the attributes contained in the abbreviation entry can be re‐
     trieved using the function dwarf_get_abbrev(3).

     Argument _c_o_d_e should point to a location which will hold a returned at‐
     tribute code.

     Argument _f_o_r_m should point to a location which will hold the returned
     form of the attribute.

     Argument _o_f_f_s_e_t should point to a location which will hold a returned
     offset, relative to the “.debug_abbrev” section, for the specified attri‐
     bute.

     If argument _e_r_r is not NULL, it will be used to return an error descrip‐
     tor in case of an error.

RREETTUURRNN VVAALLUUEESS
     Function ddwwaarrff__ggeett__aabbbbrreevv__eennttrryy() returns DW_DLV_OK when it succeeds.  It
     returns DW_DLV_NO_ENTRY if the attribute index specified by argument _n_d_x
     is out of range.  In case of an error, it returns DW_DLV_ERROR and sets
     the argument _e_r_r.

EEXXAAMMPPLLEESS
     To loop through all the attribute entries contained in the abbreviation
     section, use:

           Dwarf_Debug dbg;
           Dwarf_Abbrev ab;
           Dwarf_Off aboff, atoff;
           Dwarf_Signed form;
           Dwarf_Half attr;
           Dwarf_Unsigned length, attr_count;
           Dwarf_Error de;
           int i, ret;

           /* ...allocate 'dbg' using dwarf_init(3) ... */

           while ((ret = dwarf_next_cu_header(dbg, NULL, NULL, &aboff,
               NULL, NULL, &de)) ==  DW_DLV_OK) {
                   while ((ret = dwarf_get_abbrev(dbg, aboff, &ab, &length,
                       &attr_count, &de)) == DW_DLV_OK) {
                           if (length == 1)        /* Last entry. */
                                   break;
                           aboff += length;
                           for (i = 0; (Dwarf_Unsigned) i < attr_count; i++) {
                                   if (dwarf_get_abbrev_entry(ab, i,
                                       &attr, &form, &atoff, &de) != DW_DLV_OK) {
                                           warnx("dwarf_get_abbrev_entry failed:"
                                               " %s", dwarf_errmsg(de));
                                           continue;
                                   }
                                   /* .. use the retrieved information ... */
                           }
                   }

                   if (ret != DW_DLV_OK)
                           warnx("dwarf_get_abbrev: %s", dwarf_errmsg(de));
           }

           if (ret == DW_DLV_ERROR)
                   warnx("dwarf_next_cu_header: %s", dwarf_errmsg(de));

EERRRROORRSS
     Function ddwwaarrff__ggeett__aabbbbrreevv__eennttrryy() can fail with:

     [DW_DLE_ARGUMENT]  One of the arguments _a_b_b_r_e_v, _c_o_d_e, _f_o_r_m or _o_f_f_s_e_t was
                        NULL.

     [DW_DLE_NO_ENTRY]  The attribute index specified by argument _n_d_x was out
                        of range.

SSEEEE AALLSSOO
     dwarf(3), dwarf_get_abbrev(3)

BSD                             April 02, 2011                             BSD
