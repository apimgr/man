An  array  of  strings  called  the  is  made available by when a
process begins.  By convention these strings have  the  form  The
following names are used by various commands: The name of the au‐
dio control device to be used by and The name of the audio device
to  be  used  by  and The size of the block units used by several
commands, most notably and may be specified in units of a byte by
specifying a number, in units of a kilobyte by specifying a  num‐
ber  followed by or in units of a megabyte by specifying a number
followed by or and in units of a gigabyte by specifying a  number
followed  by or Sizes less than 512 bytes or greater than a giga‐
byte are ignored.  Gives the path name of the file used  by  when
command line editing is enabled in various programs.  See for in‐
formation  on the format of the file.  A startup list of commands
read by and A user’s login directory, set by  from  the  password
file  Default  for all NLS categories.  Only used if or the envi‐
ronment variable for a particular NLS category is not provided or
Override for all NLS categories.  If set, overrides the values of
and NLS string‐collation order information.  NLS character  clas‐
sification, case conversion, and other character attributes.  NLS
format  for  affirmative  and  negative responses.  NLS rules and
symbols for formatting monetary numeric information.   NLS  rules
and  symbols for formatting nonmonetary numeric information.  NLS
rules and symbols for formatting time and date information.  Con‐
trol how the macro (from behaves once the  assertion  is  raised.
Refer to for more information.  The login name of the user.  Con‐
trol  the  behaviour of the function.  Refer to for more informa‐
tion.  The name of the audio mixer device to be used by The  pro‐
gram  used  for paginating the output of several commands such as
If null or not set, the standard pagination program will be used.
The sequence of directories, separated  by  colons,  searched  by
etc, when looking for an executable file.  is set to initially by
The  name of the default printer to be used by and When using the
function, this variable is used as the program to run instead  of
The  full pathname of the user’s login shell.  The kind of termi‐
nal for which output is to be prepared.  This information is used
by commands, such as which may exploit special terminal capabili‐
ties.  See for a list of terminal types.  The  string  describing
the  terminal  in or, if it begins with a the name of the termcap
file.  This is only checked if is not set.  The string describing
the terminal in or, if it begins with a the name of the  terminfo
file.   A  format string that may be used by programs such as for
formatting timestamps.  The directory in which to store temporary
files.  Most applications use either  or  Setting  this  variable
will  make  them use another directory.  The timezone to use when
displaying dates.  The normal format is a  pathname  relative  to
For example, the command displays the current time in California.
See  for  more  information.   The login name of the user.  It is
recommended that  portable  applications  use  instead.   Further
names  may  be placed in the environment by the command and argu‐
ments in or by the command if you use It is unwise to change cer‐
tain variables that are frequently exported by files, such as and
unless you know what you are doing.  The manual page appeared in
