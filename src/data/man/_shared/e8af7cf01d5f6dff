The  program  works with the pseudo‐driver to allow the rescan of
busses and to detach drivers from devices.  The following options
are available: Give the interface attribute where children are to
be attached to (and which defines the interpretation of the loca‐
tor information).  This will only be needed in rare  cases  where
the  bus  has multiple attributes.  If there are multiple attrib‐
utes, and one is not specified, will return an Invalid  argument.
In  such cases, the option can be used to determine the available
interface attributes.  Detach the device driver from  the  device
given by the argument.  List the children of the device specified
by  the  argument.  If is not specified, list roots of the device
tree instead.  Output comes in two columns.  The first column  is
or  if  is  not specified.  The second column is the child.  Sup‐
press first column in output.  Suppress non‐XML headers  in  out‐
put.   Get  properties  for the device specified by the argument.
If is specified, the value of that property is printed, otherwise
the properties are displayed as an XML property list.  The  prop‐
erty  can be given as a path of dictionary keys and numeric array
indexes separated by slashes.  Resume the  ancestors  of  itself,
and all of its descendants.  Resume both the ancestors of and it‐
self.   Rescan the bus given by the argument.  The scan range can
be restricted by an optional list.  Suspend both the  descendants
of  and  itself.  Print a tree of devices in output.  To scan for
IDE/SATA/eSATA disks on which need to use the attribute: # drvctl
‐r ‐a ata_hl atabus1 To scan for devices on a USB hub  that  have
not  been discovered by normal methods (or were detached with the
option for need to use the or attributes,  try  one  or  both  of
these: # drvctl ‐r ‐a usbifif uhub3 # drvctl ‐r ‐a usbdevif uhub3
A  utility  appeard in Currently, there is no good way to get in‐
formation about locator lengths  and  default  values  (which  is
present  at  kernel  configuration time) out of a running kernel.
Thus the locator handling is less intelligent than it could be.
