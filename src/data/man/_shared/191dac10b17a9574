obtains  a ticket for the given service principal.  Usually tick‐
ets for services are obtained automatically when needed but some‐
times for some odd reason you want to obtain a particular  ticket
or of a special type.  If is given then the given service princi‐
pal  name will be canonicalized (see below).  The third form con‐
structs a host‐based principal from the given  service  name  and
hostname.   The  service name "host" is used if the given name in
the third usage is the empty string.  For host‐based  names,  the
local host’s hostname is used if the given is the empty string or
if the has a single component.  Any additional components will be
included,  even for host‐based service principal names, but there
are no defaults nor local canonicalization rules  for  additional
components.  Local name canonicalization rules are applied unless
the option is given.  Currently local name canonicalization rules
are  supported only for host‐based principal names’ hostname com‐
ponent.  The principal’s realm name may be canonicalized by  fol‐
lowing  Kerberos referrals from the client principal’s home realm
if the option is given or  if  the  local  name  canonicalization
rules  are  configured  to use referrals.  Supported options: re‐
quests that the KDC canonicalize the principal.   Currently  this
only canonicalizes the realm by chasing referrals from the user’s
start  realm,  but  in the future this may also enable the KDC to
canonicalize the complete principal name.  turns off local canon‐
icalization of the principal name.  the  name‐type  to  use  when
parsing the principal name.  is short for the credential cache to
use.   the  credential  cache  to use for delegation.  encryption
type to use.  requests that the KDC doesn’t do transit  checking.
do  not store tickets in the ccache.  do not talk the TGS, search
only the ccache.  obtain an anonymous  service  ticket.   enables
debug  output  to stderr.  If the option is used, then no further
canonicalization should be done locally by the client (for  exam‐
ple,  DNS), but if is used, then the client will ask that the KDC
canonicalize the name.  If the option is used with  a  host‐based
name‐type,  and is not used, then the hostname will be canonical‐
ized according to the name canonicalization rules in GSS‐API ini‐
tiator applications with host‐based services will  get  the  same
behavior as using the options here.
