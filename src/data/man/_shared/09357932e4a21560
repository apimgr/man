The  RADIUS  service  module for PAM provides authentication ser‐
vices based upon the RADIUS (Remote Authentication Dial  In  User
Service) protocol.  The module accepts these optional parameters:
causes  to use a previously entered password instead of prompting
for a new one.  If no password has been entered then  authentica‐
tion  fails.  causes to use a previously entered password, if one
is available.  If no password has been entered, prompts  for  one
as  usual.   causes  echoing to be left on if prompts for a pass‐
word.  specifies a non‐standard location for  the  RADIUS  client
configuration  file  (normally located in specifies a NAS identi‐
fier to send instead of the hostname.  specifies a user whose en‐
try will be used as a template to create the session  environment
if  the  supplied  username  does not exist in the local password
database.  The user will be authenticated with the supplied user‐
name and password, but his credentials to the system will be pre‐
sented as the ones for i.e., his login class, home directory, re‐
source limits, etc. will be set to ones defined for If  this  op‐
tion is omitted, and there is no username in the system databases
equal to the supplied one (as determined by call to the authenti‐
cation will fail.  specifies a NAS IP address to be sent.  If op‐
tion  is  present, but there is no value provided then IP address
corresponding to the current hostname will be used.  The standard
RADIUS client configuration file for The module first appeared in
The manual page first appeared in The manual page was written  by
The module was written by
