PMC_GET_MSR(3)           BSD Library Functions Manual           PMC_GET_MSR(3)

NNAAMMEE
     ppmmcc__ggeett__mmssrr — x86 architecture-specific PMC operations

LLIIBBRRAARRYY
     Performance Counters Library (libpmc, -lpmc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ppmmcc..hh>>

     _i_n_t
     ppmmcc__ggeett__mmssrr(_p_m_c___i_d___t _p_m_c, _u_i_n_t_3_2___t _*_m_s_r);

DDEESSCCRRIIPPTTIIOONN
     The function ppmmcc__ggeett__mmssrr() returns the processor model specific register
     number associated with a PMC for subsequent use with RDPMC instructions.
     Argument _p_m_c specifies a process scope counting PMC.  The function will
     write the model specific register number associated with the PMC to the
     location pointed to by argument _m_s_r.

     After successful completion of this function, applications can directly
     read the contents of PMC hardware using RDPMC instructions.

RREETTUURRNN VVAALLUUEESS
     The ppmmcc__ggeett__mmssrr() function returns the value 0 if successful; otherwise
     the value -1 is returned and the global variable _e_r_r_n_o is set to indicate
     the error.

EERRRROORRSS
     A call to ppmmcc__ggeett__mmssrr() may fail with the following errors:

     [EINVAL]           The PMC handle specified was invalid.

     [EINVAL]           The PMC specified did not have process scope or count‐
                        ing mode.

     [EINVAL]           The PMC specified was allocated with the
                        PMC_F_DESCENDANTS flag.

     [EINVAL]           The specified PMC is already attached to target pro‐
                        cesses other than the owner.

     [ENOSYS]           The underlying hardware does not support an RDPMC in‐
                        struction.

SSEEEE AALLSSOO
     pmc(3), hwpmc(4)

BSD                            November 25, 2007                           BSD
