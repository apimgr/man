ELF_GETIDENT(3)          BSD Library Functions Manual          ELF_GETIDENT(3)

NNAAMMEE
     eellff__ggeettiiddeenntt — return the initial bytes of a file

LLIIBBRRAARRYY
     ELF Access Library (libelf, -lelf)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<lliibbeellff..hh>>

     _c_h_a_r _*
     eellff__ggeettiiddeenntt(_E_l_f _*_e_l_f, _s_i_z_e___t _*_s_z);

DDEESSCCRRIIPPTTIIOONN
     Function eellff__ggeettiiddeenntt() returns a pointer to the initial bytes of the
     file for descriptor _e_l_f.

     If argument _s_z is non-null, the size of the identification area returned
     is written to the location pointed to by _s_z.  This location is set to
     zero on errors.

RREETTUURRNN VVAALLUUEESS
     Function eellff__ggeettiiddeenntt() will return a non-NULL pointer to the initial
     bytes of the file if successful, or NULL if an error condition is de‐
     tected.

EERRRROORRSS
     Function eellff__ggeettiiddeenntt() can fail with the following errors:

     [ELF_E_ARGUMENT]  A NULL value was passed in for argument _e_l_f.

     [ELF_E_SEQUENCE]  ELF descriptor _e_l_f was opened for writing and function
                       eellff__ggeettiiddeenntt() was called before a call to
                       elf_update(3).

SSEEEE AALLSSOO
     elf(3), elf32_getehdr(3), elf64_getehdr(3), elf_getarhdr(3),
     elf_getbase(3), elf_getflags(3), elf_kind(3), elf_rawfile(3),
     elf_update(3), gelf(3), gelf_getclass(3), gelf_getehdr(3)

BSD                              July 3, 2006                              BSD
