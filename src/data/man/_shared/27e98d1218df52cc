NG_RFC1490(4)            BSD Kernel Interfaces Manual            NG_RFC1490(4)

NNAAMMEE
     nngg__rrffcc11449900 — RFC 1490 netgraph node type

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<nneettggrraapphh//nngg__rrffcc11449900..hh>>

DDEESSCCRRIIPPTTIIOONN
     The rrffcc11449900 node type performs protocol encapsulation, de-encapsulation,
     and multiplexing according to RFC 1490 (which has since been updated by
     RFC 2427).  This particular type of encapsulation is often used on top of
     frame relay DLCI channels.

     The downstream hook is used to transmit and receive encapsulated frames.
     On the other side of the node, the inet and ppp hooks are used to trans‐
     mit and receive raw IP frames and PPP frames, respectively.  PPP frames
     are transmitted and received according to RFC 1973; in particular, frames
     appearing on the ppp hook begin with the PPP protocol number.  The
     ethernet hook can be used to transmit and receive Ethernet frames (with‐
     out a checksum) in RFC 1490's bridging format.

     Typically the inet hook is connected to the inet hook of an ng_iface(4)
     node.

HHOOOOKKSS
     This node type supports the following hooks:

     _d_o_w_n_s_t_r_e_a_m  Connects to the RFC 1490 peer entity.

     _e_t_h_e_r_n_e_t    Transmits and receives bridged raw Ethernet frames, without a
                 checksum.

     _i_n_e_t        Transmits and receives raw IP frames.

     _p_p_p         Transmits and receives PPP frames.

CCOONNTTRROOLL MMEESSSSAAGGEESS
     This node type supports the generic control messages, plus the following:

     NGM_RFC1490_SET_ENCAP (sseetteennccaapp)
          This command sets encapsulation method for the node.  The desired
          method must be passed as a string message argument, and must be one
          of the following supported encapsulation modes:

          "ietf-ip"    IP packets are sent using simple RFC1490/2427 encapsu‐
                       lation.

          "ietf-snap"  IP packets are sent inside SNAP frames.  Also conforms
                       to RFC1490/2427.

          "cisco"      IP packets are sent and received using proprietary
                       Cisco encapsulation method.

     NGM_RFC1490_GET_ENCAP (ggeetteennccaapp)
          This command returns current encapsulation method on the node.

SSHHUUTTDDOOWWNN
     This node shuts down upon receipt of a NGM_SHUTDOWN control message, or
     when all hooks have been disconnected.

SSEEEE AALLSSOO
     netgraph(4), ng_frame_relay(4), ng_iface(4), ngctl(8)

     C. Brown and A. Malis, _M_u_l_t_i_p_r_o_t_o_c_o_l _I_n_t_e_r_c_o_n_n_e_c_t _o_v_e_r _F_r_a_m_e _R_e_l_a_y, RFC
     2427.

     W. Simpson, _P_P_P _i_n _F_r_a_m_e _R_e_l_a_y, RFC 1973.

     _h_t_t_p_:_/_/_w_w_w_._c_i_s_c_o_._c_o_m_/_w_a_r_p_/_p_u_b_l_i_c_/_1_2_1_/_f_r_f_8_m_o_d_e_s_._p_d_f

HHIISSTTOORRYY
     The nngg__rrffcc11449900 node type was implemented in FreeBSD 4.0.

AAUUTTHHOORRSS
     Julian Elischer <_j_u_l_i_a_n_@_F_r_e_e_B_S_D_._o_r_g>

BBUUGGSS
     Not all of RFC 1490 is implemented.

BSD                            January 19, 1999                            BSD
