is  a  program  to  query  Internet domain name servers.  has two
modes: interactive and non‐interactive.  Interactive mode  allows
the  user  to  query  name  servers for information about various
hosts and domains or to print a list of hosts in a domain.   Non‐
interactive mode is used to print just the name and requested in‐
formation  for  a host or domain.  Interactive mode is entered in
the following cases: when no arguments  are  given  (the  default
name  server  will  be used), when the first argument is a hyphen
and the second argument is the host name or Internet address of a
name server.  Non‐interactive mode is used when the name  or  In‐
ternet  address of the host to be looked up is given as the first
argument.  The optional second argument specifies the  host  name
or  address  of a name server.  The options listed under the com‐
mand below can be specified in the file in the user’s home direc‐
tory if they are listed one per line.  Options can also be speci‐
fied on the command line if they precede the  arguments  and  are
prefixed with a hyphen.  For example, to change the default query
type  to host information, and the initial timeout to 10 seconds,
type:         nslookup ‐query=hinfo  ‐timeout=10 Commands may  be
interrupted  at  any time by typing a control‐C.  To exit, type a
control‐D or type The command line length must be less  than  256
characters.   To treat a built‐in command as a host name, precede
it with an escape character Look up  information  for  using  the
current  default server or using if specified.  If is an Internet
address and the query type is or the name  of  the  host  is  re‐
turned.   If  is  a name and does not have a trailing period, the
default domain name is appended to the name.  (This behavior  de‐
pends  on  the  state of the options and To look up a host not in
the current domain, append a period to the name.  Change the  de‐
fault  server  to  uses the initial server to look up information
about while uses the current default server.  If an authoritative
answer can’t be found, the names of servers that might  have  the
answer  are  returned.   Changes the default server to the server
for the root of the domain name space.  Currently,  the  host  is
used.  (This command is a synonym for The name of the root server
can be changed with the command.  Connects with the finger server
on the current host.  The current host is defined when a previous
lookup for a host was successful and returned address information
(see the command).  The is optional.  and can be used to redirect
output  in  the usual manner.  List the information available for
optionally creating or appending to The default  output  contains
host  names and their Internet addresses.  can be one of the fol‐
lowing: lists all records of  the  specified  type  (see  below).
lists  aliases  of  hosts  in  the  domain; synonym for lists all
records for the domain; synonym for lists CPU and operating  sys‐
tem information for the domain; synonym for lists well‐known ser‐
vices of hosts in the domain; synonym for When output is directed
to  a  file, hash marks are printed for every 50 records received
from the server.  Sorts and lists the  output  of  previous  com‐
mand(s)  with Prints a brief summary of commands.  Exits the pro‐
gram.  This command is used to change state information that  af‐
fects the lookups.  Valid keywords are: Prints the current values
of  the frequently‐used options to Information about the  current
default server and host is also printed.  Change the query  class
to one of: the Internet class the Chaos class the MIT Athena Hes‐
iod  class  wildcard  (any  of the above) The class specifies the
protocol group of the information.   (Default  =  abbreviation  =
Turn  debugging mode on.  A lot more information is printed about
the packet sent to the server and the resulting answer.  (Default
= abbreviation = Turn exhaustive debugging mode on.   Essentially
all  fields  of  every packet are printed.  (Default = Change the
default domain name to The default domain name is appended  to  a
lookup  request  depending  on the state of the and options.  The
domain search list contains the parents of the default domain  if
it  has at least two components in its name.  For example, if the
default domain is CC.Berkeley.EDU, the search list  is  CC.Berke‐
ley.EDU and Berkeley.EDU.  Use the command to specify a different
list.   Use  the  command  to display the list.  (Default = value
from or abbreviation = Change the default domain name to and  the
domain  search  list  to  etc.  A maximum of 6 names separated by
slashes   can   be   specified.    For   example,    set    srch‐
list=lcs.MIT.EDU/ai.MIT.EDU/MIT.EDU    sets    the    domain   to
lcs.MIT.EDU and the search list to the three names.  This command
overrides the default domain name and search list of the command.
Use the command to display the list.  (Default = value  based  on
or  abbreviation  =  If  set, append the default domain name to a
single‐component lookup request (i.e., one that does not  contain
a  period).  (Default = abbreviation = If the lookup request con‐
tains at least one period but end with a trailing period,  append
the  domain  names in the domain search list to the request until
an answer is received.  (Default = abbreviation = Change the  de‐
fault  TCP/UDP  name server port to (Default = 53; abbreviation =
Change the type of information query to one of: the host’s Inter‐
net address.  the canonical name for an alias.  the host CPU  and
operating  system  type.   the  mailbox or mail list information.
the mail exchanger.  the name server for  the  named  zone.   the
host  name  if  the  query is an Internet address; otherwise, the
pointer to other information.   the  domain’s  information.   the
text  information.   the  user  information.  the supported well‐
known services.  Other types are described in the RFC‐1035  docu‐
ment.   (Default  = abbreviations = Tell the name server to query
other servers if it does not have the  information.   (Default  =
abbreviation = Set the number of retries to When a reply to a re‐
quest  is  not  received within a certain amount of time (changed
with the timeout period is doubled and  the  request  is  resent.
The  retry  value controls how many times a request is resent be‐
fore giving up.  (Default = 4, abbreviation = This  keyword  sets
the  number  of dots (label separators) in a domain that disables
searching.  Absolute names always  stop  searching.   Change  the
name  of the root server to This affects the command.  (Default =
abbreviation = Change the initial timeout  interval  for  waiting
for  a  reply to seconds.  Each retry doubles the timeout period.
(Default = 5 seconds; abbreviation = Always use a virtual circuit
when sending requests to the server.  (Default =  abbreviation  =
Ignore  packet truncation errors.  (Default = abbreviation = This
keyword  tries  the next nameserver if a nameserver responds with
or a referral (nofail), or terminates the query (fail) on such  a
response.   The  default  is  nofail.  If has been built with IDN
(internationalized domain name) support, it can accept  and  dis‐
play  non‐ASCII  domain  names.  appropriately converts character
encoding of a domain name before  sending  a  request  to  a  DNS
server  or  displaying  a reply from the server.  To turn off IDN
support, define the IDN_DISABLE environment variable.   IDN  sup‐
port  is  disabled  if the variable is set when runs, or when the
standard output is not a tty.  file containing host aliases over‐
rides default domain turns off IDN support  initial  domain  name
and  name  server addresses returns with an exit status of if any
query failed, and otherwise.  If the lookup request was not  suc‐
cessful,  an  error message is printed.  Possible errors are: The
server did not respond to a request after  a  certain  amount  of
time  (changed with and a certain number of retries (changed with
No name server is running on the server machine.  The server does
not have resource records of the current query type for the host,
although the host name is valid.  The  query  type  is  specified
with  the  command.  The host or domain name does not exist.  The
connection to the name or finger server could not be made at  the
current time.  This error commonly occurs with and requests.  The
name  server  found an internal inconsistency in its database and
could not return a valid answer.  The name server refused to ser‐
vice the request.  The name server found that the request  packet
was not in the proper format.  It may indicate an error in
