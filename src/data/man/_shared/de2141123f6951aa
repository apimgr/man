The  utility displays the manual pages named on the command line.
Its options are as follows: Display all of the man  pages  for  a
specified  and  combination.   (Normally, only the first man page
found is displayed.)  Use the specified instead  of  the  default
configuration  file.   This  permits users to configure their own
man environment.  See for a description of the contents  of  this
file.   Copy the man page to the standard output instead of using
to paginate it.  This is done by default if the  standard  output
is  not a terminal device.  Synonym for It searches man pages for
in their names and displays header lines from all matching pages.
Display only the lines of the requested man pages.  For commands,
this is typically the command line usage  information.   For  li‐
brary functions, this usually contains the required include files
and function prototypes.  Search man pages for in the same manner
as  Override  the list of standard directories which searches for
man pages.  The supplied must be a colon separated list of direc‐
tories.  This search path may also be set using  the  environment
variable  The subdirectories to be searched, and their search or‐
der, is specified by the line in the configuration file.  Augment
the list of standard directories which searches  for  man  pages.
The  supplied  must  be  a  colon  separated list of directories.
These directories will be searched before the  standard  directo‐
ries  or  the directories specified using the option or the envi‐
ronment variable.  The subdirectories to be searched,  and  their
search order, is specified by the line in the configuration file.
Print  the search path for the manual pages.  Restrict the direc‐
tories that will search to the specified section.  The configura‐
tion file (see specifies the possible values that  are  currently
available.  Display only man pages that have the specified string
in  the  directory  part of their filenames.  This allows the man
page search process criteria to be  narrowed  without  having  to
change  the  MANPATH or variables.  List the pathnames of the man
pages which would display for the specified and combination.   If
the option is not specified, there is more than one argument, the
option  is  not  used, and the first argument is a valid section,
then that argument will be used as if specified  by  the  option.
If  is  given with a full path (beginning with or a relative path
that begins with or then interprets it as a  file  specification,
so  that you can do or even If contains but does not match one of
the above cases, then the search path is used; this allows you to
request machine‐specific man pages, such as As some man pages are
intended only for specific architectures, searches any  subdirec‐
tories,  with the same name as the current architecture, in every
directory which it searches.  Machine‐specific areas are  checked
before general areas.  The current machine type may be overridden
by setting the environment variable to the name of a specific ar‐
chitecture.   Machine‐specific man pages may also be requested by
prepending the relevant subdirectory name to the page name, sepa‐
rated by (See also the line in the configuration file, which  de‐
fines additional supplemental paths related to a specific machine
type.)   The  standard  search  path used by may be overridden by
specifying a path in the environment variable.  The format of the
path is a colon separated list of directories.   The  subdirecto‐
ries to be searched as well as their search order is specified by
the  line in the configuration file.  The pagination command used
for writing the output.  If the environment variable is  null  or
not  set,  the standard pagination program will be used.  default
man configuration file.  conforms to The on‐line man  pages  are,
by  necessity, forgiving toward stupid display devices, causing a
few man pages to be not as  nicely  formatted  as  their  typeset
counterparts.
