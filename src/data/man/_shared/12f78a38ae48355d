_p_i_d_f_d___g_e_t_f_d(2)                System Calls Manual               _p_i_d_f_d___g_e_t_f_d(2)

NNAAMMEE
       pidfd_getfd - obtain a duplicate of another process’s file descriptor

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssyyss//ssyyssccaallll..hh>>      /* Definition of SSYYSS__** constants */
       ##iinncclluuddee <<uunniissttdd..hh>>

       iinntt ssyyssccaallll((SSYYSS__ppiiddffdd__ggeettffdd,, iinntt _p_i_d_f_d,, iinntt _t_a_r_g_e_t_f_d,,
                   uunnssiiggnneedd iinntt _f_l_a_g_s));;

       _N_o_t_e:  glibc  provides  no wrapper for ppiiddffdd__ggeettffdd(), necessitating the
       use of ssyyssccaallll(2).

DDEESSCCRRIIPPTTIIOONN
       The ppiiddffdd__ggeettffdd() system call allocates a new file  descriptor  in  the
       calling  process.  This new file descriptor is a duplicate of an exist‐
       ing file descriptor, _t_a_r_g_e_t_f_d, in the process referred to  by  the  PID
       file descriptor _p_i_d_f_d.

       The  duplicate file descriptor refers to the same open file description
       (see ooppeenn(2)) as the original file descriptor in the  process  referred
       to by _p_i_d_f_d.  The two file descriptors thus share file status flags and
       file  offset.   Furthermore,  operations  on the underlying file object
       (for example, assigning an address to a socket  object  using  bbiinndd(2))
       can equally be performed via the duplicate file descriptor.

       The  close‐on‐exec  flag  (FFDD__CCLLOOEEXXEECC; see ffccnnttll(2)) is set on the file
       descriptor returned by ppiiddffdd__ggeettffdd().

       The _f_l_a_g_s argument is reserved for future use.  Currently, it  must  be
       specified as 0.

       Permission  to  duplicate another process’s file descriptor is governed
       by  a  ptrace  access  mode  PPTTRRAACCEE__MMOODDEE__AATTTTAACCHH__RREEAALLCCRREEDDSS  check   (see
       ppttrraaccee(2)).

RREETTUURRNN VVAALLUUEE
       On  success, ppiiddffdd__ggeettffdd() returns a file descriptor (a nonnegative in‐
       teger).  On error, -1 is returned and _e_r_r_n_o is set to indicate the  er‐
       ror.

EERRRROORRSS
       EEBBAADDFF  _p_i_d_f_d is not a valid PID file descriptor.

       EEBBAADDFF  _t_a_r_g_e_t_f_d  is not an open file descriptor in the process referred
              to by _p_i_d_f_d.

       EEIINNVVAALL _f_l_a_g_s is not 0.

       EEMMFFIILLEE The per‐process limit on the number of open file descriptors has
              been reached (see the  description  of  RRLLIIMMIITT__NNOOFFIILLEE  in  ggeettrr‐‐
              lliimmiitt(2)).

       EENNFFIILLEE The system‐wide limit on the total number of open files has been
              reached.

       EEPPEERRMM  The  calling  process  did not have PPTTRRAACCEE__MMOODDEE__AATTTTAACCHH__RREEAALLCCRREEDDSS
              permissions (see ppttrraaccee(2)) over  the  process  referred  to  by
              _p_i_d_f_d.

       EESSRRCCHH  The  process  referred  to by _p_i_d_f_d does not exist (i.e., it has
              terminated and been waited on).

SSTTAANNDDAARRDDSS
       Linux.

HHIISSTTOORRYY
       Linux 5.6.

NNOOTTEESS
       For a description of PID file descriptors, see ppiiddffdd__ooppeenn(2).

       The effect of ppiiddffdd__ggeettffdd() is similar to the use  of  SSCCMM__RRIIGGHHTTSS  mes‐
       sages described in uunniixx(7), but differs in the following respects:

       •  In  order to pass a file descriptor using an SSCCMM__RRIIGGHHTTSS message, the
          two processes must first establish a UNIX domain socket connection.

       •  The use of SSCCMM__RRIIGGHHTTSS  requires  cooperation  on  the  part  of  the
          process whose file descriptor is being copied.  By contrast, no such
          cooperation is necessary when using ppiiddffdd__ggeettffdd().

       •  The  ability to use ppiiddffdd__ggeettffdd() is restricted by a PPTTRRAACCEE__MMOODDEE__AATT‐‐
          TTAACCHH__RREEAALLCCRREEDDSS ptrace access mode check.

SSEEEE AALLSSOO
       cclloonnee33(2), dduupp(2), kkccmmpp(2), ppiiddffdd__ooppeenn(2)

Linux man‐pages (unreleased)      2025‐08‐29                    _p_i_d_f_d___g_e_t_f_d(2)
