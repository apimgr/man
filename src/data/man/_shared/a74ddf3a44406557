GELF_GETPHDR(3)          BSD Library Functions Manual          GELF_GETPHDR(3)

NNAAMMEE
     eellff3322__ggeettpphhddrr, eellff6644__ggeettpphhddrr, ggeellff__ggeettpphhddrr — retrieve an ELF program
     header table

LLIIBBRRAARRYY
     ELF Access Library (libelf, -lelf)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<lliibbeellff..hh>>

     _E_l_f_3_2___P_h_d_r _*
     eellff3322__ggeettpphhddrr(_E_l_f _*_e_l_f);

     _E_l_f_6_4___P_h_d_r _*
     eellff6644__ggeettpphhddrr(_E_l_f _*_e_l_f);

     ##iinncclluuddee <<ggeellff..hh>>

     _G_E_l_f___P_h_d_r _*
     ggeellff__ggeettpphhddrr(_E_l_f _*_e_l_f, _i_n_t _i_n_d_e_x, _G_E_l_f___P_h_d_r _*_d_s_t);

DDEESSCCRRIIPPTTIIOONN
     These functions retrieve and translate ELF program header information
     from an ELF descriptor, if this information exists.

     Functions eellff3322__ggeettpphhddrr() and eellff6644__ggeettpphhddrr() return a pointer to an ar‐
     ray of translated _E_l_f_3_2___P_h_d_r and _E_l_f_6_4___P_h_d_r descriptors respectively.
     These descriptors are described in elf(5).  The number of entries in this
     array may be determined using the elf_getphnum(3) function.

     Function ggeellff__ggeettpphhddrr() will retrieve the program header table entry at
     index _i_n_d_e_x from ELF descriptor _e_l_f.  The translated program header table
     entry will be written to the address pointed to be argument _d_s_t.

     Applications may inform the library of modifications to a program header
     table entry by using the elf_flagphdr(3) API.  Applications using the
     gelf(3) interface need to use the gelf_update_phdr(3) API to copy modifi‐
     cations to a program header entry back to the underlying ELF descriptor.

RREETTUURRNN VVAALLUUEESS
     The functions a valid pointer if successful, or NULL in case an error was
     encountered.

EERRRROORRSS
     These functions may fail with the following errors:

     [ELF_E_ARGUMENT]  Argument _e_l_f was NULL.

     [ELF_E_ARGUMENT]  Argument _e_l_f was not a descriptor for an ELF object.

     [ELF_E_ARGUMENT]  Argument _d_s_t was NULL.

     [ELF_E_ARGUMENT]  Index _i_n_d_e_x was out of range.

     [ELF_E_CLASS]     The class of ELF descriptor _e_l_f did not match the ex‐
                       pected class of the function being called.

     [ELF_E_HEADER]    ELF descriptor _e_l_f did not possess an executable
                       header.

     [ELF_E_HEADER]    ELF descriptor _e_l_f had a corrupt executable header.

     [ELF_E_RESOURCE]  An out of memory condition was detected.

     [ELF_E_SECTION]   The ELF descriptor in argument _e_l_f did not adhere to
                       the conventions used for extended numbering.

     [ELF_VERSION]     ELF descriptor _e_l_f was of an unsupported version.

SSEEEE AALLSSOO
     elf(3), elf32_getehdr(3), elf32_newphdr(3), elf64_getehdr(3),
     elf64_newphdr(3), elf_flagphdr(3), elf_getphnum(3), gelf(3),
     gelf_getehdr(3), gelf_newphdr(3), gelf_update_phdr(3), elf(5)

BSD                            October 21, 2007                            BSD
