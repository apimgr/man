PW_UTIL(3)               BSD Library Functions Manual               PW_UTIL(3)

NNAAMMEE
     ppww__ccooppyy, ppww__dduupp, ppww__eeddiitt, ppww__eeqquuaall, ppww__ffiinnii, ppww__iinniitt, ppww__mmaakkee,
     ppww__mmaakkee__vv77, ppww__mmkkddbb, ppww__lloocckk, ppww__ssccaann, ppww__tteemmppnnaammee, ppww__ttmmpp — functions
     for passwd file handling

LLIIBBRRAARRYY
     System Utilities Library (libutil, -lutil)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ppwwdd..hh>>
     ##iinncclluuddee <<lliibbuuttiill..hh>>

     _i_n_t
     ppww__ccooppyy(_i_n_t _f_f_d, _i_n_t _t_f_d, _c_o_n_s_t _s_t_r_u_c_t _p_a_s_s_w_d _*_p_w, _s_t_r_u_c_t _p_a_s_s_w_d _*_o_l_d_p_w);

     _s_t_r_u_c_t _p_a_s_s_w_d _*
     ppww__dduupp(_c_o_n_s_t _s_t_r_u_c_t _p_a_s_s_w_d _*_p_w);

     _i_n_t
     ppww__eeddiitt(_i_n_t _n_o_s_e_t_u_i_d);

     _i_n_t
     ppww__eeqquuaall(_c_o_n_s_t _s_t_r_u_c_t _p_a_s_s_w_d _*_p_w_1, _c_o_n_s_t _s_t_r_u_c_t _p_a_s_s_w_d _*_p_w_2);

     _v_o_i_d
     ppww__ffiinnii(_v_o_i_d);

     _i_n_t
     ppww__iinniitt(_c_o_n_s_t _c_h_a_r _*_d_i_r, _c_o_n_s_t _c_h_a_r _*_m_a_s_t_e_r);

     _v_o_i_d
     ppww__iinniittppwwdd(_s_t_r_u_c_t _p_a_s_s_w_d _*_p_w);

     _c_h_a_r _*
     ppww__mmaakkee(_c_o_n_s_t _s_t_r_u_c_t _p_a_s_s_w_d _*_p_w);

     _c_h_a_r _*
     ppww__mmaakkee__vv77(_c_o_n_s_t _s_t_r_u_c_t _p_a_s_s_w_d _*_p_w);

     _i_n_t
     ppww__mmkkddbb(_c_o_n_s_t _c_h_a_r _*_u_s_e_r);

     _i_n_t
     ppww__lloocckk(_v_o_i_d);

     _s_t_r_u_c_t _p_a_s_s_w_d _*
     ppww__ssccaann(_c_o_n_s_t _c_h_a_r _*_l_i_n_e, _i_n_t _f_l_a_g_s);

     _c_o_n_s_t _c_h_a_r _*
     ppww__tteemmppnnaammee(_v_o_i_d);

     _i_n_t
     ppww__ttmmpp(_i_n_t _m_f_d);

DDEESSCCRRIIPPTTIIOONN
     The ppww__ccooppyy() function reads a password file from _f_f_d and writes it back
     out to _t_f_d possibly with modifications:

     --   If _p_w is NULL and _o_l_d_p_w is not NULL, then the record represented by
         _o_l_d_p_w will not be copied (corresponding to user deletion).

     --   If _p_w and _o_l_d_p_w are not NULL then the record corresponding to _p_w will
         be replaced by the record corresponding to _o_l_d_p_w.

     --   If _p_w is set and _o_l_d_p_w is NULL then the record corresponding to _p_w
         will be appended (corresponding to user addition).

     The ppww__ccooppyy() function returns -1 in case of failure otherwise 0.

     The ppww__dduupp() function duplicates the _s_t_r_u_c_t _p_a_s_s_w_d pointed to by _p_w and
     returns a pointer to the copy, or NULL in case of failure.  The new
     _s_t_r_u_c_t _p_a_s_s_w_d is allocated with malloc(3), and it is the caller's respon‐
     sibility to free it with free(3).

     The ppww__eeddiitt() function invokes the command specified by the EDITOR envi‐
     ronment variable (or _/_u_s_r_/_b_i_n_/_v_i if EDITOR is not defined) on a temporary
     copy of the master password file created by ppww__ttmmpp().  If the file was
     modified, ppww__eeddiitt() installs it and regenerates the password database.
     The ppww__eeddiitt() function returns -1 in case of failure, 0 if the file was
     not modified, and a non-zero positive number if the file was modified and
     successfully installed.

     The ppww__eeqquuaall() function compares two _s_t_r_u_c_t _p_a_s_s_w_d and returns 0 if they
     are equal.

     The ppww__ffiinnii() function destroy the temporary file created by ppww__ttmmpp() if
     any, kills any running instance of EDITOR executed by ppww__eeddiitt() if any,
     and closes the lock created by ppww__lloocckk() if any.

     The ppww__iinniitt() initializes the static variable representing the path to a
     password file.  _d_i_r is the directory where the password file is located.
     If set to NULL, it will default to _/_e_t_c.  _m_a_s_t_e_r is the name of the pass‐
     word file.  If set to NULL?  it will default to _m_a_s_t_e_r_._p_a_s_s_w_d

     The ppww__iinniittppwwdd() function initializes the _p_a_s_s_w_d struct to canonical val‐
     ues.  The entire structure is zeroed, then _p_w___u_i_d and _p_w___g_i_d are set to
     -1, and all string pointers are set to point at an internally-defined
     zero-length string.

     The ppww__mmaakkee() function creates a properly formatted BSD passwd(5) line
     from a _s_t_r_u_c_t _p_a_s_s_w_d, and returns a pointer to the resulting string.  The
     string is allocated with malloc(3), and it is the caller's responsibility
     to free it with free(3).

     The ppww__mmaakkee__vv77() function creates a properly formatted UNIX V7 passwd(5)
     line from a _s_t_r_u_c_t _p_a_s_s_w_d, and returns a pointer to the resulting string.
     The string is allocated with malloc(3), and it is the caller's responsi‐
     bility to free it with free(3).

     The ppww__mmkkddbb() function regenerates the password database by running
     pwd_mkdb(8).  If _u_s_e_r only the record corresponding to that user will be
     updated.  The ppww__mmkkddbb() function returns 0 in case of success and -1 in
     case of failure.

     The ppww__lloocckk() function locks the master password file.  It returns a file
     descriptor to the master password file on success and -1 on failure.

     The ppww__ssccaann() function is a wrapper around the internal libc function
     ____ppww__ssccaann().  It scans the master password file for a line corresponding
     to the _l_i_n_e provided and return a _s_t_r_u_c_t _p_a_s_s_w_d if it matched an existing
     record.  In case of failure, it returns NULL.  Otherwise, it returns a
     pointer to a _s_t_r_u_c_t _p_a_s_s_w_d containing the matching record.  The _s_t_r_u_c_t
     _p_a_s_s_w_d is allocated with malloc(3), and it is the caller's responsibility
     to free it with free(3).

     The ppww__tteemmppnnaammee() function returns the temporary name of the masterfile
     created via ppww__ttmmpp().

     The ppww__ttmmpp() creates and opens a presumably safe temporary password file.
     If _m_f_d is a file descriptor to an open password file, it will be read and
     written back to the temporary password file.  Otherwise if should be set
     -1.  The ppww__ttmmpp() returns an open file descriptor to the temporary pass‐
     word file or -1 in case of failure.

HHIISSTTOORRYY
     The functions for passwd file handling first appeared in 4.4BSD.

AAUUTTHHOORRSS
     Portions of this software were developed for the FreeBSD Project by
     ThinkSec AS and Network Associates Laboratories, the Security Research
     Division of Network Associates, Inc. under DARPA/SPAWAR contract
     N66001-01-C-8035 (“CBOSS”), as part of the DARPA CHATS research program.

     This manual page was written by Baptiste Daroussin <_b_a_p_t_@_F_r_e_e_B_S_D_._o_r_g>.

BSD                              May 10, 2020                              BSD
