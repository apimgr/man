is a self‐contained, position independent program image providing
run‐time support for loading and link‐editing shared objects into
a  process’  address space.  It uses the data structures see con‐
tained within dynamically  linked  programs  to  determine  which
shared  libraries  are needed and loads them at a convenient vir‐
tual address using the system call.  After all  shared  libraries
have  been successfully loaded, proceeds to resolve external ref‐
erences from both the main program and  all  objects  loaded.   A
mechanism  is  provided for initialization routines to be called,
on a per‐object basis, giving a shared object an  opportunity  to
perform  any extra set‐up, before execution of the program proper
begins.  looks for a symbol named in each object’s symbol  table.
If  present, this symbol is assumed to represent a C‐function de‐
clared as which is then called.  Similarly, a function is  called
just  before an object is unloaded from the process address space
as a result of calling Note that  while  an  object’s  is  always
called,  whether  the  object  is loaded automatically at program
startup or programmatically by using the function is called  only
on  This  mechanism  is exploited by the system‐supplied C++ con‐
structor initialization code located in This file should  be  in‐
cluded in the list of object‐code files passed to when building a
shared  C++ library.  is itself a shared object that is initially
loaded by the startup module Since formats do  not  provide  easy
access to the file header from within a running process, uses the
special  symbol to determine whether a program is in fact dynami‐
cally linked or not.  Whenever the linker has relocated this sym‐
bol to a location other than  0,  assumes  the  services  of  are
needed  see  for details passes control to entry point before the
program’s routine is called.  Thus, can complete the link‐editing
process before the dynamic program calls upon services of any dy‐
namic library.  To quickly locate the required shared objects  in
the filesystem, may use a file, prepared by the utility, in which
the  full  path specification of the shared objects can be looked
up by hashing on the 3‐tuple library‐name,  major‐version‐number,
minor‐version‐number recognizes a number of environment variables
that can be used to modify its behavior as follows: A colon sepa‐
rated list of directories, overriding the default search path for
shared  libraries.  A colon separated list of shared object file‐
names to be loaded the main program but its shared object  depen‐
dencies.  When set, issue a warning whenever a link‐editing oper‐
ation  requires  modification  of the text segment of some loaded
object.  This is usually indicative of an incorrectly  built  li‐
brary.   When  set,  no  warning messages of any kind are issued.
Normally, a warning is given if satisfactorily versioned  library
could  not  be found.  When set, causes to exit after loading the
shared objects and printing a summary which includes the absolute
pathnames of all objects, to standard output.   When  set,  these
variables are interpreted as format strings a la to customize the
trace  output and are used by option and allows to be operated as
a filter more conveniently.  The  following  conversions  can  be
used:  The main program’s name The value of the environment vari‐
able The library name.  The library’s major version number.   The
library’s  minor version number.  The full pathname as determined
by library search rules.  The library’s load address.   Addition‐
ally, and are recognized and have their usual meaning.  When set,
does  not process any internal search paths that were recorded in
the executable.  When set, do not include a set of built‐in stan‐
dard directory paths for searching.  This might  be  useful  when
running  on  a  system  with a completely non‐standard filesystem
layout.  library location hints built by The shared library model
employed first appeared in SunOS 4.0.  The environment  variables
and are not honored when executing in a set‐user‐ID or set‐group‐
ID  environment.   This action is taken to prevent malicious sub‐
stitution of shared object dependencies or interposition of  sym‐
bols.
