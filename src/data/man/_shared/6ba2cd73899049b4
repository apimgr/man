KLDLOAD(8)                BSD System Manager's Manual               KLDLOAD(8)

NNAAMMEE
     kkllddllooaadd — load a file into the kernel

SSYYNNOOPPSSIISS
     kkllddllooaadd [--nnqqvv] _f_i_l_e _._._.

DDEESSCCRRIIPPTTIIOONN
     The kkllddllooaadd utility loads _f_i_l_e_._k_o into the kernel using the kernel
     linker.  Note that if multiple modules are specified then an attempt will
     be made to load them all, even if some fail.  The _._k_o extension name is
     not mandatory when loading a given module using kkllddllooaadd.  It does not
     hurt to specify it though.

     If a bare filename is requested it will only be loaded if it is found
     within the module path as defined by the sysctl _k_e_r_n_._m_o_d_u_l_e___p_a_t_h.  To
     load a module from the current directory it must be specified as a full
     or relative path.  The kkllddllooaadd utility will warn if a module is requested
     as a bare filename and is present in the current directory.

     The following options are available:

     --nn      Do not try to load module if already loaded.

     --vv      Be more verbose.

     --qq      Silence any extraneous warnings.

NNOOTTEESS
     The kernel security level settings may prevent a module from being loaded
     or unloaded by giving _O_p_e_r_a_t_i_o_n _n_o_t _p_e_r_m_i_t_t_e_d.

FFIILLEESS
     /boot/kernel  directory containing loadable modules.  Modules must have
                   an extension of _._k_o.

EEXXIITT SSTTAATTUUSS
     The kkllddllooaadd utility exits 0 on success, and >0 if an error occurs.

EEXXAAMMPPLLEESS
     To load by module name:

           > kldload foo

     To load by file name within the module path:

           > kldload foo.ko

     To load by relative path:

           > kldload ./foo.ko

     To load by full path:

           > kldload /boot/kernel/foo.ko

AAUUTTOOMMAATTIICCAALLLLYY LLOOAADDIINNGG MMOODDUULLEESS
     Some modules (pf, ipfw, ipf, etc.) may be automatically loaded at boot
     time when the corresponding rc.conf(5) statement is used.  Modules may
     also be auto-loaded through their addition to loader.conf(5) or kld_list
     in rc.conf(5).

     Only those modules necessary for booting the system, including those re‐
     quired for mounting the root filesystem, should be handled by
     loader.conf(5).

SSEEEE AALLSSOO
     kenv(1), kldload(2), loader.conf(5), rc.conf(5), security(7),
     kldconfig(8), kldstat(8), kldunload(8), kldxref(8)

HHIISSTTOORRYY
     The kkllddllooaadd utility first appeared in FreeBSD 3.0, replacing the llkkmm in‐
     terface.

AAUUTTHHOORRSS
     Doug Rabson <_d_f_r_@_F_r_e_e_B_S_D_._o_r_g>

BSD                            December 20, 2023                           BSD
