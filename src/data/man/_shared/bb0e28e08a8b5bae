The utility verifies and repairs filesystems (more commonly known
as filesystems).  The first form of preens the specified filesys‐
tems.   It  is  normally started by run from during automatic re‐
boot, when a FAT filesystem is detected.  When preening file sys‐
tems, will fix common inconsistencies non‐interactively.  If more
serious problems are found, does not try to fix  them,  indicates
that it was not successful, and exits.  The second form of checks
the  specified  file systems and tries to repair all detected in‐
consistencies, requesting confirmation before making any changes.
The options are as follows: This option  is  ignored  by  and  is
present  only  for  compatibility  with programs that check other
file system types for consistency, such as Causes to assume no as
the answer to all operator questions, except Preen the  specified
filesystems.  Use a snapshot with as backup to check a read‐write
mounted filesystem.  Must be used with See for more details.  The
point  is  to  check  an  internally‐consistent  version  of  the
filesystem to find out if it is damaged; on  failure  one  should
unmount  the  filesystem  and repair it.  Causes to assume yes as
the answer to all operator questions.  A utility appeared  in  is
still under construction.
