The  command  copies  the disk image specified by into the memory
disk storage area in The file system present in will typically be
used by the kernel as the root file system.  To recognize  kernel
executable  format,  the  flag specifies BFD name of kernel.  The
and flags specify the symbol names of image and  size  of  memory
disk  drivers  respectively.   For  a.out  kernels only, the flag
specifies the starting address of kernel text.  This flag is  ig‐
nored  for  other  kernel  executable  formats.   If the flags is
given, will write back the actual disk image size  back  into  If
the  flag  is  given,  will print out status information as it is
copying the image.  If the flag is given, will extract  the  disk
image  from into the file This is the opposite of the default be‐
havior.
