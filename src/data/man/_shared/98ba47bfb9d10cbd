software  based  on configuration file Special.  See below.  Once
upon time, the only Mail Transfer Agent software easily available
was This famous was written by and first appeared in  The  legacy
of this affected most Mail User Agents such as the path and call‐
ing conventions expected by were compiled in.  But times changed.
On a modern system, the administrator may wish to use one of sev‐
eral available It would be difficult to modify all software typi‐
cally  available  on a system, so most of the authors of alterna‐
tive have written their front  end  message  submission  programs
that may appear in the place of but still follow the same calling
conventions as The also typically has aliases named and linked to
it.   The  program knows to behave differently when its is or and
behaves appropriately.  Typically,  replacement  provide  similar
functionality, either through a program that also switches behav‐
ior based on calling name, or through a set of programs that pro‐
vide similar functionality.  Although having replacement programs
that  plug replace helps in installing alternative it essentially
makes the configuration of the system depend on  hand  installing
new programs in This leads to configuration problems for many ad‐
ministrators, since they may wish to install a new without alter‐
ing  the system provided (This may be, for example, to avoid hav‐
ing upgrade problems when a new version  of  the  system  is  in‐
stalled over the old.)  They may also have a shared among several
machines,  and  may  wish to avoid placing implicit configuration
information in a read‐only The program is designed to replace and
to invoke  an  appropriate  based  on  configuration  information
placed in This permits the administrator to configure which is to
be  invoked on the system at run time.  Configuration for is kept
in is typically set up as a symlink to which is not  usually  in‐
voked  on  its own.  will print a diagnostic if its configuration
file is missing or malformed, or does not contain a  mapping  for
the name under which it was invoked.  The program appeared in The
entire reason this program exists is a crock.  Instead, a command
for how to submit mail should be standardized, and all the behav‐
ior of things like should go away.
