_i_s_f_d_t_y_p_e(3)                Library Functions Manual                _i_s_f_d_t_y_p_e(3)

NNAAMMEE
       isfdtype - test file type of a file descriptor

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssyyss//ssttaatt..hh>>
       ##iinncclluuddee <<ssyyss//ssoocckkeett..hh>>

       iinntt iissffddttyyppee((iinntt _f_d,, iinntt _f_d_t_y_p_e));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       iissffddttyyppee():
           Since glibc 2.20:
               _DEFAULT_SOURCE
           Before glibc 2.20:
               _BSD_SOURCE || _SVID_SOURCE

DDEESSCCRRIIPPTTIIOONN
       The  iissffddttyyppee() function tests whether the file descriptor _f_d refers to
       a file of type _f_d_t_y_p_e.  The _f_d_t_y_p_e argument specifies one of the  SS__IIFF**
       constants  defined  in  _<_s_y_s_/_s_t_a_t_._h_>  and  documented in ssttaatt(2) (e.g.,
       SS__IIFFRREEGG).

RREETTUURRNN VVAALLUUEE
       The iissffddttyyppee() function returns 1 if the file descriptor _f_d is of  type
       _f_d_t_y_p_e and 0 if it is not.  On failure, -1 is returned and _e_r_r_n_o is set
       to indicate the error.

EERRRROORRSS
       The  iissffddttyyppee()  function  can  fail with any of the same errors as ffss‐‐
       ttaatt(2).

VVEERRSSIIOONNSS
       Portable applications should use ffssttaatt(2) instead.

SSTTAANNDDAARRDDSS
       None.

HHIISSTTOORRYY
       It appeared in the draft POSIX.1g standard.  It is present  on  OpenBSD
       and  Tru64  UNIX  (where the required header file in both cases is just
       _<_s_y_s_/_s_t_a_t_._h_>, as shown in the POSIX.1g draft).

SSEEEE AALLSSOO
       ffssttaatt(2)

Linux man‐pages (unreleased)      2025‐05‐17                       _i_s_f_d_t_y_p_e(3)
