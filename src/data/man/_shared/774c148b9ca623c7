Extended  attributes  extend the basic attributes associated with
files and directories in the file system.   They  are  stored  as
name:data  pairs  associated with file system objects (files, di‐
rectories, symlinks, etc).  associates and  together  as  an  at‐
tribute  of  An  extended attribute’s is a simple NULL‐terminated
UTF‐8 string.  is a pointer to a data buffer of bytes  containing
textual  or  binary  data  to be associated with the extended at‐
tribute.  specifies the offset within the extended attribute.  In
the current implementation, only the resource fork  extended  at‐
tribute  makes use of this argument.  For all others, is reserved
and should be set to zero.  controls how the attribute is set: do
not follow symbolic links.  normally sets attributes on the  tar‐
get  of  if it is a symbolic link.  With this option, will act on
the link itself.  do not follow any  symbolic  links  encountered
during  pathname resolution. An error is returned if a symlink is
encountered before the last component of path.  If does  not  re‐
side in the hierarchy beneath the starting directory, an error is
returned.   fail  if the named attribute already exists.  fail if
the named attribute does not exist.  Failure to specify or allows
creation and replacement.  is identical to except that it sets an
extended attribute on an open file referenced by file  descriptor
On  some filesystems, such as setting the extended attribute will
update the modification time (‘‘mtime’’) of  the  file.   Due  to
historical reasons, the (defined to be extended attribute must be
32  bytes;  see  the section in The maximum supported size of ex‐
tended attribute can be found out using with  _PC_XATTR_SIZE_BITS
option.   On  success, 0 is returned.  On failure, ‐1 is returned
and the global variable is set  as  follows.   contains  and  the
named  attribute  already  exists.   is  set to and the named at‐
tribute does not exist.  The file system  does  not  support  ex‐
tended  attributes  or  has  them  disabled.   The file system is
mounted read‐only.  The data size of  the  attribute  is  out  of
range  (some attributes have size restrictions).  Attributes can‐
not be associated with this type of object.  For example, attrib‐
utes are not allowed for resource forks.  or is invalid.  must be
valid UTF‐8 and must make  sense.   XATTR_NOFOLLOW,  XATTR_NOFOL‐
LOW_ANY or XATTR_RESOLVE_BENEATH option was passed in A component
of  is  not a directory.  exceeded UTF‐8 bytes, or a component of
exceeded characters, or the entire exceeded  characters.   Search
permission  is denied for a component of or permission to set the
attribute is denied.  Too many symbolic  links  were  encountered
resolving  If XATTR_NOFOLLOW_ANY option was passed and a symbolic
link was encountered in translating the pathname.   If  XATTR_RE‐
SOLVE_BENEATH was passed and does not reside in the directory hi‐
erarchy  beneath the starting directory.  or points to an invalid
address.  An I/O error occurred while reading from or writing  to
the  file system.  The data size of the extended attribute is too
large.  Not enough space left on the file system.  and first  ap‐
peared in Mac OS X 10.4.
