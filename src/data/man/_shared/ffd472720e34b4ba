_u_t_i_m_e_n_s_a_t(2)                  System Calls Manual                 _u_t_i_m_e_n_s_a_t(2)

NNAAMMEE
       utimensat, futimens - change file timestamps with nanosecond precision

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ffccnnttll..hh>>            /* Definition of AATT__** constants */
       ##iinncclluuddee <<ssyyss//ssttaatt..hh>>

       iinntt uuttiimmeennssaatt((iinntt _d_i_r_f_d,, ccoonnsstt cchhaarr **_p_a_t_h,,
                     ccoonnsstt ssttrruucctt ttiimmeessppeecc _t_i_m_e_s[[__NNuullllaabbllee 22]],, iinntt _f_l_a_g_s));;
       iinntt ffuuttiimmeennss((iinntt _f_d,, ccoonnsstt ssttrruucctt ttiimmeessppeecc _t_i_m_e_s[[__NNuullllaabbllee 22]]));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       uuttiimmeennssaatt():
           Since glibc 2.10:
               _POSIX_C_SOURCE >= 200809L
           Before glibc 2.10:
               _ATFILE_SOURCE

       ffuuttiimmeennss():
           Since glibc 2.10:
               _POSIX_C_SOURCE >= 200809L
           Before glibc 2.10:
               _GNU_SOURCE

DDEESSCCRRIIPPTTIIOONN
       uuttiimmeennssaatt()  and  ffuuttiimmeennss()  update  the  timestamps  of  a  file with
       nanosecond precision.  This contrasts with the historical uuttiimmee(2)  and
       uuttiimmeess(2),  which permit only second and microsecond precision, respec‐
       tively, when setting file timestamps.

       With uuttiimmeennssaatt() the file is specified via the pathname given in  _p_a_t_h.
       With  ffuuttiimmeennss()  the file whose timestamps are to be updated is speci‐
       fied via an open file descriptor, _f_d.

       For both calls, the new file timestamps  are  specified  in  the  array
       _t_i_m_e_s:  _t_i_m_e_s_[_0_] specifies the new "last access time" (_a_t_i_m_e); _t_i_m_e_s_[_1_]
       specifies the new "last modification time" (_m_t_i_m_e).  Each of  the  ele‐
       ments  of  _t_i_m_e_s specifies a time as the number of seconds and nanosec‐
       onds since the Epoch, 1970‐01‐01 00:00:00 +0000 (UTC).   This  informa‐
       tion is conveyed in a ttiimmeessppeecc(3) structure.

       Updated  file timestamps are set to the greatest value supported by the
       filesystem that is not greater than the specified time.

       If the _t_v___n_s_e_c field of one of the _t_i_m_e_s_p_e_c structures has the  special
       value  UUTTIIMMEE__NNOOWW,  then  the corresponding file timestamp is set to the
       current time.  If the _t_v___n_s_e_c field of one of the  _t_i_m_e_s_p_e_c  structures
       has the special value UUTTIIMMEE__OOMMIITT, then the corresponding file timestamp
       is  left  unchanged.   In  both of these cases, the value of the corre‐
       sponding _t_v___s_e_c field is ignored.

       If _t_i_m_e_s is NULL, then both timestamps are set to the current time.

       The status change time (ctime) will be set to the current time, even if
       the other time stamps don’t actually change.

   PPeerrmmiissssiioonnss rreeqquuiirreemmeennttss
       To set both file timestamps to the current time (i.e., _t_i_m_e_s  is  NULL,
       or both _t_v___n_s_e_c fields specify UUTTIIMMEE__NNOOWW), either:

       •  the caller must have write access to the file;

       •  the caller’s effective user ID must match the owner of the file; or

       •  the caller must have appropriate privileges.

       To  make  any  change other than setting both timestamps to the current
       time (i.e., _t_i_m_e_s is not NULL, and neither _t_v___n_s_e_c field  is  UUTTIIMMEE__NNOOWW
       and neither _t_v___n_s_e_c field is UUTTIIMMEE__OOMMIITT), either condition 2 or 3 above
       must apply.

       If both _t_v___n_s_e_c fields are specified as UUTTIIMMEE__OOMMIITT, then no file owner‐
       ship  or  permission  checks are performed, and the file timestamps are
       not modified, but other error conditions may still be detected.

   uuttiimmeennssaatt(()) ssppeecciiffiiccss
       If _p_a_t_h is relative, then by default it is interpreted relative to  the
       directory  referred  to by the open file descriptor, _d_i_r_f_d (rather than
       relative to the current working directory of the calling process, as is
       done by uuttiimmeess(2) for a relative pathname).  See ooppeennaatt(2) for  an  ex‐
       planation of why this can be useful.

       If  _p_a_t_h is relative and _d_i_r_f_d is the special value AATT__FFDDCCWWDD, then _p_a_t_h
       is interpreted relative to the current working directory of the calling
       process (like uuttiimmeess(2)).

       If _p_a_t_h is absolute, then _d_i_r_f_d is ignored.

       The _f_l_a_g_s argument is a bit mask created by ORing together zero or more
       of the following values defined in _<_f_c_n_t_l_._h_>:

       AATT__EEMMPPTTYY__PPAATTHH (since Linux 5.8)
              If _p_a_t_h is an empty string, operate on the file referred  to  by
              _d_i_r_f_d  (which  may  have  been obtained using the ooppeenn(2) OO__PPAATTHH
              flag).  In this case, _d_i_r_f_d can refer to any type of  file,  not
              just  a  directory.   If _d_i_r_f_d is AATT__FFDDCCWWDD, the call operates on
              the current working directory.  This flag is Linux‐specific; de‐
              fine __GGNNUU__SSOOUURRCCEE to obtain its definition.

       AATT__SSYYMMLLIINNKK__NNOOFFOOLLLLOOWW
              If _p_a_t_h specifies a symbolic link, then update the timestamps of
              the link, rather than the file to which it refers.

RREETTUURRNN VVAALLUUEE
       On success, uuttiimmeennssaatt() and ffuuttiimmeennss() return 0.  On error, -1  is  re‐
       turned and _e_r_r_n_o is set to indicate the error.

EERRRROORRSS
       EEAACCCCEESS _t_i_m_e_s is NULL, or both _t_v___n_s_e_c values are UUTTIIMMEE__NNOOWW, and the ef‐
              fective  user  ID  of the caller does not match the owner of the
              file, the caller does not have write access to the file, and the
              caller is not  privileged  (Linux:  does  not  have  either  the
              CCAAPP__FFOOWWNNEERR or the CCAAPP__DDAACC__OOVVEERRRRIIDDEE capability).

       EEBBAADDFF  (ffuuttiimmeennss()) _f_d is not a valid file descriptor.

       EEBBAADDFF  (uuttiimmeennssaatt()) _p_a_t_h is relative but _d_i_r_f_d is neither AATT__FFDDCCWWDD nor
              a valid file descriptor.

       EEFFAAUULLTT _t_i_m_e_s pointed to an invalid address; or, _d_i_r_f_d was AATT__FFDDCCWWDD, and
              _p_a_t_h is NULL or an invalid address.

       EEIINNVVAALL Invalid value in _f_l_a_g_s.

       EEIINNVVAALL Invalid  value in one of the _t_v___n_s_e_c fields (value outside range
              [0, 999,999,999], and not UUTTIIMMEE__NNOOWW or UUTTIIMMEE__OOMMIITT);  or  an  in‐
              valid value in one of the _t_v___s_e_c fields.

       EEIINNVVAALL _p_a_t_h  is NULL, _d_i_r_f_d is not AATT__FFDDCCWWDD, and _f_l_a_g_s contains AATT__SSYYMM‐‐
              LLIINNKK__NNOOFFOOLLLLOOWW.

       EELLOOOOPP  (uuttiimmeennssaatt()) Too many symbolic links were  encountered  in  re‐
              solving _p_a_t_h.

       EENNAAMMEETTOOOOLLOONNGG
              (uuttiimmeennssaatt()) _p_a_t_h is too long.

       EENNOOEENNTT (uuttiimmeennssaatt())  A component of _p_a_t_h does not refer to an existing
              directory or file, or _p_a_t_h is an empty string.

       EENNOOTTDDIIRR
              (uuttiimmeennssaatt()) _p_a_t_h is relative, but _d_i_r_f_d  is  neither  AATT__FFDDCCWWDD
              nor  a  file descriptor referring to a directory; or, one of the
              prefix components of _p_a_t_h is not a directory.

       EEPPEERRMM  The caller attempted to change one or both timestamps to a value
              other than the current time, or to change one of the  timestamps
              to the current time while leaving the other timestamp unchanged,
              (i.e.,  _t_i_m_e_s  is  not NULL, neither _t_v___n_s_e_c field is UUTTIIMMEE__NNOOWW,
              and neither _t_v___n_s_e_c field is UUTTIIMMEE__OOMMIITT) and either:

              •  the caller’s effective user ID does not match  the  owner  of
                 file,  and the caller is not privileged (Linux: does not have
                 the CCAAPP__FFOOWWNNEERR capability); or,

              •  the file is marked append‐only or immutable (see cchhaattttrr(1)).

       EERROOFFSS  The file is on a read‐only filesystem.

       EESSRRCCHH  (uuttiimmeennssaatt()) Search permission is denied for one of the  prefix
              components of _p_a_t_h.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       uuttiimmeennssaatt(), ffuuttiimmeennss() T}   Thread safety  MT‐Safe

VVEERRSSIIOONNSS
   CC lliibbrraarryy//kkeerrnneell AABBII ddiiffffeerreenncceess
       On Linux, ffuuttiimmeennss() is a library function implemented on top of the
       uuttiimmeennssaatt() system call.  To support this, the Linux uuttiimmeennssaatt() system
       call implements a nonstandard feature: if _p_a_t_h is NULL, then the call
       modifies the timestamps of the file referred to by the file descriptor
       _d_i_r_f_d (which may refer to any type of file).  Using this feature, the
       call _f_u_t_i_m_e_n_s_(_f_d_, _t_i_m_e_s_) is implemented as:

           utimensat(fd, NULL, times, 0);

       Note, however, that the glibc wrapper for uuttiimmeennssaatt() disallows passing
       NULL as the value for _p_a_t_h: the wrapper function returns the error
       EEIINNVVAALL in this case.

SSTTAANNDDAARRDDSS
       POSIX.1‐2024.

HHIISSTTOORRYY
       uuttiimmeennssaatt()
              Linux 2.6.22, glibc 2.6.  POSIX.1‐2008.

       ffuuttiimmeennss()
              glibc 2.6.  POSIX.1‐2008.

NNOOTTEESS
       uuttiimmeennssaatt() obsoletes ffuuttiimmeessaatt(2).

       On Linux, timestamps cannot be changed for a file marked immutable, and
       the only change permitted for files marked append‐only is  to  set  the
       timestamps   to  the  current  time.   (This  is  consistent  with  the
       historical behavior of uuttiimmee(2) and uuttiimmeess(2) on Linux.)

       If both _t_v___n_s_e_c fields are specified  as  UUTTIIMMEE__OOMMIITT,  then  the  Linux
       implementation  of uuttiimmeennssaatt() succeeds even if the file referred to by
       _d_i_r_f_d and _p_a_t_h does not exist.

BBUUGGSS
       Several bugs afflict uuttiimmeennssaatt() and ffuuttiimmeennss()  before  Linux  2.6.26.
       These   bugs   are   either  nonconformances  with  the  POSIX.1  draft
       specification or inconsistencies with historical Linux behavior.

       •  POSIX.1 specifies that if one of the _t_v___n_s_e_c fields  has  the  value
          UUTTIIMMEE__NNOOWW  or UUTTIIMMEE__OOMMIITT, then the value of the corresponding _t_v___s_e_c
          field should be ignored.  Instead, the value of the _t_v___s_e_c field  is
          required to be 0 (or the error EEIINNVVAALL results).

       •  Various  bugs mean that for the purposes of permission checking, the
          case where both _t_v___n_s_e_c fields are set  to  UUTTIIMMEE__NNOOWW  isn’t  always
          treated the same as specifying _t_i_m_e_s as NULL, and the case where one
          _t_v___n_s_e_c value is UUTTIIMMEE__NNOOWW and the other is UUTTIIMMEE__OOMMIITT isn’t treated
          the  same as specifying _t_i_m_e_s as a pointer to an array of structures
          containing arbitrary time values.  As a result, in  some  cases:  a)
          file  timestamps  can  be  updated  by a process that shouldn’t have
          permission to perform updates; b) file timestamps can’t  be  updated
          by  a process that should have permission to perform updates; and c)
          the wrong _e_r_r_n_o value is returned in case of an error.

       •  POSIX.1 says that a process that has _w_r_i_t_e _a_c_c_e_s_s _t_o  _t_h_e  _f_i_l_e  can
          make  a  call with _t_i_m_e_s as NULL, or with _t_i_m_e_s pointing to an array
          of structures in which both _t_v___n_s_e_c fields are UUTTIIMMEE__NNOOWW,  in  order
          to  update both timestamps to the current time.  However, ffuuttiimmeennss()
          instead checks whether the _a_c_c_e_s_s _m_o_d_e _o_f _t_h_e _f_i_l_e _d_e_s_c_r_i_p_t_o_r _a_l_l_o_w_s
          _w_r_i_t_i_n_g.

SSEEEE AALLSSOO
       cchhaattttrr(1),  ttoouucchh(1),  ffuuttiimmeessaatt(2),  ooppeennaatt(2),  ssttaatt(2),   uuttiimmeess(2),
       ffuuttiimmeess(3), ttiimmeessppeecc(3), iinnooddee(7), ppaatthh__rreessoolluuttiioonn(7), ssyymmlliinnkk(7)

Linux man‐pages (unreleased)      2025‐10‐29                      _u_t_i_m_e_n_s_a_t(2)
