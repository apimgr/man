The  authentication service module for PAM provides functionality
for two PAM categories: authentication  and  account  management.
In  terms  of  the parameter, they are the and features.  It also
provides a null function for session management.  The authentica‐
tion component provides functions to verify  the  identity  of  a
user which obtains the relevant entry.  It prompts the user for a
password and verifies that this is correct with The following op‐
tions  may  be passed to the authentication module: debugging in‐
formation at level.  If the  authentication  module  is  not  the
first  in  the  stack,  and a previous module obtained the user’s
password, that password is used to  authenticate  the  user.   If
this  fails,  the  authentication  module returns failure without
prompting the user for a password.  This option has no effect  if
the  authentication  module  is  the first in the stack, or if no
previous modules obtained the user’s password.   This  option  is
similar  to  the  option,  except that if the previously obtained
password fails, the user is prompted for another password.   This
option  will require the user to authenticate himself as the user
given by not as the account they are attempting to access.   This
is primarily for services like where the user’s ability to retype
their  own  password might be deemed sufficient.  If the password
database has no password for the entity being authenticated, then
this option will forgo password prompting, and silently allow au‐
thentication to succeed.  The account management  component  pro‐
vides a function to perform account management, The function ver‐
ifies  that the authenticated user is allowed to login to the lo‐
cal user account by checking the password expiry date.  The  fol‐
lowing  options may be passed to the management module: debugging
information at level.  The password management component provides
a function to perform account management,  The  function  changes
the  user’s password.  The following options may be passed to the
password module: debugging information at level.  suppress  warn‐
ing messages to the user.  These messages include reasons why the
user’s  authentication  attempt  was declined.  Change the user’s
password only the specified password  database.   Valid  password
database names are: local password file NIS password database de‐
fault password database.
