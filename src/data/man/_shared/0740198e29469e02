RFORK_THREAD(3)          BSD Library Functions Manual          RFORK_THREAD(3)

NNAAMMEE
     rrffoorrkk__tthhrreeaadd — create a rfork-based process thread

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<uunniissttdd..hh>>

     _p_i_d___t
     rrffoorrkk__tthhrreeaadd(_i_n_t _f_l_a_g_s, _v_o_i_d _*_s_t_a_c_k, _i_n_t _(_*_f_u_n_c_)_(_v_o_i_d _*_a_r_g_), _v_o_i_d _*_a_r_g);

DDEESSCCRRIIPPTTIIOONN
     TThhee rrffoorrkk__tthhrreeaadd() ffuunnccttiioonn hhaass bbeeeenn ddeepprreeccaatteedd iinn ffaavvoorr ooff
     pthread_create(33)..

     The rrffoorrkk__tthhrreeaadd() function is a helper function for rfork(2).  It ar‐
     ranges for a new process to be created and the child process will call
     the specified function with the specified argument, while running on the
     supplied stack.

     Using this function should avoid the need to implement complex stack swap
     code.

RREETTUURRNN VVAALLUUEESS
     Upon successful completion, rrffoorrkk__tthhrreeaadd() returns the process ID of the
     child process to the parent process.  Otherwise, a value of -1 is re‐
     turned to the parent process, no child process is created, and the global
     variable _e_r_r_n_o is set to indicate the error.

     The child process context is not aware of a return from the
     rrffoorrkk__tthhrreeaadd() function as it begins executing directly with the supplied
     function.

EERRRROORRSS
     See rfork(2) for error return codes.

SSEEEE AALLSSOO
     fork(2), intro(2), minherit(2), rfork(2), vfork(2), pthread_create(3)

HHIISSTTOORRYY
     The rrffoorrkk__tthhrreeaadd() function first appeared in FreeBSD 4.3.

BSD                            February 6, 2011                            BSD
