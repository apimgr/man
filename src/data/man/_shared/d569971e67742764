Computers  need  random unpredictable secrets for the security of
software such as web browsers and Computers are designed  to  be‐
have  in highly predictable ways, so they rely on observations of
random physical phenomena around them, called  to  derive  unpre‐
dictable secrets for cryptography.  While some computers have re‐
liable  entropy sources such as hardware random number generators
based on thermal noise in silicon circuits,  others  may  require
operator  intervention  for  security.  Web browsers and programs
such as rely on unpredictable secrets in cryptography to  prevent
eavesdropping  and detect tampering of sessions over the network.
relies on unpredictable secrets to create keys that allow you  to
log  in but keep out malicious adversaries; if an adversary could
guess the key then they could impersonate you.  relies on  unpre‐
dictable  secrets  to  make sure that private user data stored on
nonvolatile media when memory is scarce using see cannot  be  re‐
covered  by  forensic tools after shutdown.  gathers samples from
various kinds of entropy sources, including: hardware random num‐
ber generators network traffic  timing  user  input  (keystrokes,
mouse  movements, etc.)  disk I/O latency environment sensors The
samples are mixed together  with  cryptography  to  yield  unpre‐
dictable  secrets through and related interfaces used by programs
like Firefox, and so on.  also stores a random seed at  to  carry
unpredictable  secrets over from one boot to the next, as long as
the medium remains secret and can be updated on boot.   The  seed
is  maintained  automatically by Entropy is measured in bits, and
only 256 bits of entropy are needed for security, thanks to  mod‐
ern  cryptography.  To detect potentially insecure systems, takes
measures to alert the operator if there isn’t  definitely  enough
for  security:  issues  warnings  on  the  console if there’s not
enough entropy when programs need it; see The has  a  warning  if
there  was  not enough entropy when network daemons such as first
generated keys.  The daily security report includes an  alert  if
there’s  still  not  enough entropy; see Since it is hard to know
how unpredictable most physical systems are, only devices specif‐
ically designed to be hardware random  number  generators,  or  a
seed  file  stored  on disk, count toward these alerts.  At boot,
will wait, when is set in or fail to single‐user  mode,  when  is
set,  if  there is not enough entropy from sources, including de‐
vices not designed to be unpredictable, such  as  the  CPU  cycle
counter  sampled by a periodic timer, provided the samples pass a
simple filter called the like other operating  systems.   Sources
known  to be predictable, which could give a false sense of secu‐
rity, can be disabled from unblocking boot by setting in Many new
computers  have  hardware  random  number  generators,  such   as
RDRAND/RDSEED  in  Intel/AMD CPUs, or ARMv8.5‐RNDRRS; virtualiza‐
tion platforms such as QEMU can expose entropy from the host with
bootloader firmware such as UEFI may also  expose  an  underlying
platform’s  random number generator.  However, many older comput‐
ers have no reliable entropy sources.  Some  have  the  hardware,
but  have it off by default, such as a disabled On computers with
no built‐in reliable entropy source, you may wish to  transfer  a
seed  from another computer with or manually enter samples into —
see below.  You can manually save and load seeds with  the  tool.
For  example,  you  might  use  to  save a seed from one machine,
transfer it — over a medium where you are confident there are  no
eavesdroppers  — to another machine, and load it with on the tar‐
get machine; then run on the target machine to  ensure  that  the
entropy  will  be  saved  for next boot, even if the system later
crashes or otherwise shuts down uncleanly.  records the number of
bits of entropy in the seed so that  can  count  it.   Users  can
write  data  to to be mixed together with all other samples.  For
example, no matter what entropy sources are  built  into  a  com‐
puter, you can ensure it has enough entropy (as long as there are
no  surveillance  cameras  watching  you)  by flipping a coin 256
times and running: Then run to ensure that  the  effort  will  be
saved  for next boot.  Inputs from the superuser (uid 0) to count
toward the system’s entropy estimate, at the maximum rate of  one
bit  of  entropy  per bit of data; inputs from unprivileged users
will affect subsequent outputs but will be counted as having zero
entropy.  After adding entropy, that might be predictable because
they were previously generated with too little entropy.  For  ex‐
ample, if is enabled in then will automatically generate ssh host
keys  on  boot;  if  they were generated with too little entropy,
then you may wish to delete them and create new ones  before  al‐
lowing  anyone  to log in via may print the following warnings to
the console: Some process tried to draw use entropy from e.g.  to
generate  a key for cryptography, before enough inputs from reli‐
able entropy sources have been obtained.  The entropy may be  low
enough  that  an adversary could guess keys by brute force.  This
message is rate‐limited, so if you have added entropy and want to
verify that the problem is resolved, you should consult the vari‐
able to confirm it is zero, rather than just look for the absence
of this message; see for details.
