The  is  a  routing daemon which supports RIP over IPv6.  Options
are: Enables aging of the statically defined routes.   With  this
option, any statically defined routes will be removed unless cor‐
responding  updates  arrive  as if the routes are received at the
startup of This option makes log route  changes  (add/delete)  to
the  file  This option is used for aggregating routes.  specifies
the prefix and the prefix length of the aggregated  route.   When
advertising routes, filters specific routes covered by the aggre‐
gate and advertises the aggregated route to the interfaces speci‐
fied in the comma‐separated interface list creates a static route
to  with  the  flag  set, into the kernel routing table.  Enables
output of debugging messages.  This option also instructs to  run
in  foreground  mode  Enables  extensive output of debugging mes‐
sages.  This option also instructs to run in foreground mode Dis‐
ables split horizon processing.  By default,  will  not  exchange
site local routes for safety reasons.  This is because the seman‐
tics  of site local address space are rather vague, as the speci‐
fication is still being worked on, and there is no  good  way  to
define  the  site  local boundary.  With will exchange site local
routes as well.  It must not be used on  site  boundary  routers,
since  assumes  that all interfaces are in the same site.  Filter
incoming routes from interfaces will accept incoming routes  that
are  in  If multiple options are specified, all routes that match
any of the options are accepted.  is treated specially as default
route, not any route that has longer prefix length than, or equal
to 0 If you would like to accept any route,  specify  no  option.
For example, with will accept the default route and routes in the
6bone  test address range, but no others.  Do not update the ker‐
nel routing table.  Do not listen to, or advertise, route from/to
interfaces specified by Restrict route advertisement  toward  in‐
terfaces specified by With this option will only advertise routes
that match Makes use listen‐only mode.  No advertisement is sent.
Makes  advertise the statically defined routes which exist in the
kernel routing table when is  invoked.   Announcements  obey  the
regular  split  horizon  rule.  This option is the same as except
that the split horizon rule does apply.  Advertise only  the  de‐
fault  route  toward Attach the route tag to originated route en‐
tries.  can be decimal, octal prefixed by or hexadecimal prefixed
by Upon receipt of signal or will dump the current internal state
into contains the internal state dumps created if receives  a  or
signal  uses the advanced IPv6 API, defined in RFC 3542, for com‐
municating with peers using link‐local addresses.  Internally em‐
beds interface identifiers into bits 32 to 63 of  link‐local  ad‐
dresses  and  so  they will be visible in the internal state dump
file Routing table manipulation differs from IPv6  implementation
to  implementation.  Currently obeys the WIDE Hydrangea/KAME IPv6
kernel, and will not be able to run  on  other  platforms.   Cur‐
rently,  does  not  reduce the rate of the triggered updates when
consecutive updates arrive.
