The  driver  provides  support for video capture devices based on
the Auvitek AU0828 bridge.  This hybrid analog/digital device re‐
quires a hi‐speed USB host controller (such as to function  prop‐
erly.   For devices with analog audio capture interfaces, the de‐
vice driver provides access to  the  audio  stream.   Application
software  can  find a device’s device by comparing the field with
the audio device’s field.  The following cards are  supported  by
the  driver: Cards with an XC5000 tuner require the firmware pro‐
vided by the package to function properly.  The device driver ap‐
peared in The driver was written by
