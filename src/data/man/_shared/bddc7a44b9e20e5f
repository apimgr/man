The  utility writes the user’s login name to standard output fol‐
lowed by a newline.  The utility explicitly ignores the and envi‐
ronment variables because the environment cannot be trusted.  The
utility conforms to The command appeared in
