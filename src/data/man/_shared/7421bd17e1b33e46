URTW(4)                  BSD Kernel Interfaces Manual                  URTW(4)

NNAAMMEE
     uurrttww — Realtek RTL8187L/RTL8187B USB IEEE 802.11b/g wireless network de‐
     vice

SSYYNNOOPPSSIISS
     uurrttww** aatt uuhhuubb?? ppoorrtt ??

DDEESSCCRRIIPPTTIIOONN
     The uurrttww driver supports USB 802.11b/g wireless adapters based on the Re‐
     altek RTL8187L and RTL8187B.

     These are the modes the uurrttww driver can operate in:

     BSS mode       Also known as _i_n_f_r_a_s_t_r_u_c_t_u_r_e mode, this is used when asso‐
                    ciating with an access point, through which all traffic
                    passes.  This mode is the default.

     monitor mode   In this mode the driver is able to receive packets without
                    associating with an access point.  This disables the in‐
                    ternal receive filter and enables the card to capture
                    packets from networks which it wouldn't normally have ac‐
                    cess to, or to scan for access points.

     The uurrttww driver can be configured to use Wired Equivalent Privacy (WEP)
     or Wi-Fi Protected Access (WPA1 and WPA2).  WPA2 is currently the most
     secure encryption standard for wireless networks supported by OpenBSD.
     It is strongly recommended that neither WEP nor WPA1 are used as the sole
     mechanism to secure wireless communication, due to serious weaknesses.
     WPA1 is disabled by default and may be enabled using the option
     "wwppaapprroottooss _w_p_a_1_,_w_p_a_2".  For standard WPA networks which use pre-shared
     keys (PSK), keys are configured using the "wwppaakkeeyy" option.  WPA-Enter‐
     prise networks require use of the wpa_supplicant package.  The uurrttww
     driver relies on the software 802.11 stack for both encryption and de‐
     cryption of data frames.

     The uurrttww driver can be configured at runtime with ifconfig(8) or on boot
     with hostname.if(5).

HHAARRDDWWAARREE
     The following adapters should work:

           Alfa AWUS036H
           ASUS P5B Deluxe
           Belkin F5D7050E
           Digitus DN-7003GT
           Linksys WUSB54GC v2
           Netgear WG111v2
           Netgear WG111v3
           Shuttle XPC Accessory PN20
           Sitecom WL-168 v1
           Sitecom WL-168 v4
           Surecom EP-9001-g rev 2A
           TRENDnet TEW-424UB V3.xR

EEXXAAMMPPLLEESS
     The following example scans for available networks:

           # ifconfig urtw0 scan

     The following hostname.if(5) example configures urtw0 to join network
     “mynwid”, using WPA key “mywpakey”, obtaining an IP address using DHCP:

           join mynwid wpakey mywpakey
           inet autoconf

SSEEEE AALLSSOO
     arp(4), ifmedia(4), intro(4), netintro(4), usb(4), hostname.if(5),
     ifconfig(8)

     Realtek Semiconductor: hhttttppss::////wwwwww..rreeaalltteekk..ccoomm//eenn

HHIISSTTOORRYY
     The uurrttww device driver first appeared in OpenBSD 4.5.

AAUUTTHHOORRSS
     The uurrttww driver was written by Weongyo Jeong <_w_e_o_n_g_y_o_@_F_r_e_e_B_S_D_._o_r_g>.

BSD                             March 27, 2025                             BSD
