The  command  can be used to control and inspect the state of in‐
terrupts in the system.  The first argument, specifies the action
to take.  Valid commands are: set an interrupt’s affinity to  en‐
able  to  set  an  interrupt’s affinity to If is already enabled,
this command has no effect.  for each intrid in the system,  dis‐
play  interrupt  counts per CPU.  The intrid is an interrupt name
such as "ioapic0 pin 22" for x86.  If is specified, display  com‐
pact  list  with  total counts per interrupt, and CPU affinity as
list of CPU indexes.  If is specified then display the data  con‐
tinuously  with  a  seconds  delay between each iteration.  If is
specified then rows with all CPUs having zero interrupts will  be
skipped.  disable to set an interrupt’s affinity to If is already
disabled, this command has no effect.  first appeared in
