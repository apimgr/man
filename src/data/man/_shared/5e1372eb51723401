_g_e_t_u_t_e_n_t(3)                Library Functions Manual                _g_e_t_u_t_e_n_t(3)

NNAAMMEE
       getutent, getutid, getutline, pututline, setutent, endutent, utmpname -
       access utmp file entries

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<uuttmmpp..hh>>

       ssttrruucctt uuttmmpp **ggeettuutteenntt((vvooiidd));;
       ssttrruucctt uuttmmpp **ggeettuuttiidd((ccoonnsstt ssttrruucctt uuttmmpp **_u_t));;
       ssttrruucctt uuttmmpp **ggeettuuttlliinnee((ccoonnsstt ssttrruucctt uuttmmpp **_u_t));;

       ssttrruucctt uuttmmpp **ppuuttuuttlliinnee((ccoonnsstt ssttrruucctt uuttmmpp **_u_t));;

       vvooiidd sseettuutteenntt((vvooiidd));;
       vvooiidd eenndduutteenntt((vvooiidd));;

       iinntt uuttmmppnnaammee((ccoonnsstt cchhaarr **_p_a_t_h));;

DDEESSCCRRIIPPTTIIOONN
       New  applications  should use the POSIX.1‐specified "utmpx" versions of
       these functions; see STANDARDS.

       uuttmmppnnaammee() sets the pathname of the utmp‐format file for the other utmp
       functions to access.  If uuttmmppnnaammee() is not used to set the pathname be‐
       fore the other functions are used, they assume __PPAATTHH__UUTTMMPP,  as  defined
       in _<_p_a_t_h_s_._h_>.

       sseettuutteenntt()  rewinds the file pointer to the beginning of the utmp file.
       It is generally a good idea to call it before any of  the  other  func‐
       tions.

       eenndduutteenntt()  closes  the  utmp  file.  It should be called when the user
       code is done accessing the file with the other functions.

       ggeettuutteenntt() reads a line from the current  file  position  in  the  utmp
       file.  It returns a pointer to a structure containing the fields of the
       line.  The definition of this structure is shown in uuttmmpp(5).

       ggeettuuttiidd()  searches  forward from the current file position in the utmp
       file based upon _u_t.  If  _u_t_-_>_u_t___t_y_p_e  is  one  of  RRUUNN__LLVVLL,  BBOOOOTT__TTIIMMEE,
       NNEEWW__TTIIMMEE,  or  OOLLDD__TTIIMMEE,  ggeettuuttiidd()  will  find  the  first entry whose
       _u_t___t_y_p_e  field  matches  _u_t_-_>_u_t___t_y_p_e.   If  _u_t_-_>_u_t___t_y_p_e   is   one   of
       IINNIITT__PPRROOCCEESSSS,  LLOOGGIINN__PPRROOCCEESSSS,  UUSSEERR__PPRROOCCEESSSS, or DDEEAADD__PPRROOCCEESSSS, ggeettuuttiidd()
       will find the first entry whose _u_t___i_d field matches _u_t_-_>_u_t___i_d.

       ggeettuuttlliinnee() searches forward from the current file position in the utmp
       file.  It scans entries whose _u_t___t_y_p_e is UUSSEERR__PPRROOCCEESSSS or  LLOOGGIINN__PPRROOCCEESSSS
       and returns the first one whose _u_t___l_i_n_e field matches _u_t_-_>_u_t___l_i_n_e.

       ppuuttuuttlliinnee()  writes  the _u_t_m_p structure _u_t into the utmp file.  It uses
       ggeettuuttiidd() to search for the proper place in the file to insert the  new
       entry.   If it cannot find an appropriate slot for _u_t, ppuuttuuttlliinnee() will
       append the new entry to the end of the file.

RREETTUURRNN VVAALLUUEE
       ggeettuutteenntt(),  ggeettuuttiidd(),  and  ggeettuuttlliinnee()  return  a   pointer   to   a
       _s_t_r_u_c_t _u_t_m_p on success, and NULL on failure (which includes the "record
       not found" case).  This _s_t_r_u_c_t _u_t_m_p is allocated in static storage, and
       may be overwritten by subsequent calls.

       On success ppuuttuuttlliinnee() returns _u_t; on failure, it returns NULL.

       uuttmmppnnaammee()  returns 0 if the new name was successfully stored, or -1 on
       failure.

       On failure, these functions _e_r_r_n_o set to indicate the error.

EERRRROORRSS
       EENNOOMMEEMM Out of memory.

       EESSRRCCHH  Record not found.

       sseettuutteenntt(), ppuuttuuttlliinnee(), and the ggeettuutt**() functions can also  fail  for
       the reasons described in ooppeenn(2).

FFIILLEESS
       _/_v_a_r_/_r_u_n_/_u_t_m_p
              database of currently logged‐in users

       _/_v_a_r_/_l_o_g_/_w_t_m_p
              database of past user logins

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lb lb lbx l l l.  Interface Attribute Value T{
       ggeettuutteenntt() T}   Thread safety  T{ MT‐Unsafe init race:utent
       race:utentbuf sig:ALRM timer T} T{ ggeettuuttiidd(), ggeettuuttlliinnee() T}   Thread
       safety  T{ MT‐Unsafe init race:utent sig:ALRM timer T} T{ ppuuttuuttlliinnee()
       T}   Thread safety   T{ MT‐Unsafe race:utent sig:ALRM timer T} T{
       sseettuutteenntt(), eenndduutteenntt(), uuttmmppnnaammee() T}   Thread safety  MT‐Unsafe
       race:utent

       In the above table, _u_t_e_n_t in _r_a_c_e_:_u_t_e_n_t signifies that if any of the
       functions sseettuutteenntt(), ggeettuutteenntt(), ggeettuuttiidd(), ggeettuuttlliinnee(), ppuuttuuttlliinnee(),
       uuttmmppnnaammee(), or eenndduutteenntt() are used in parallel in different threads of
       a program, then data races could occur.

SSTTAANNDDAARRDDSS
       None.

HHIISSTTOORRYY
       XPG2, SVr4.

       In XPG2 and SVID 2 the function ppuuttuuttlliinnee() is documented to return
       void, and that is what it does on many systems (AIX, HP‐UX).  HP‐UX
       introduces a new function __ppuuttuuttlliinnee() with the prototype given above
       for ppuuttuuttlliinnee().

       All these functions are obsolete now on non‐Linux systems.
       POSIX.1‐2001 and POSIX.1‐2008, following SUSv1, does not have any of
       these functions, but instead uses

           ##iinncclluuddee <<uuttmmppxx..hh>>

           ssttrruucctt uuttmmppxx **ggeettuuttxxeenntt((vvooiidd));;
           ssttrruucctt uuttmmppxx **ggeettuuttxxiidd((ccoonnsstt ssttrruucctt uuttmmppxx **));;
           ssttrruucctt uuttmmppxx **ggeettuuttxxlliinnee((ccoonnsstt ssttrruucctt uuttmmppxx **));;
           ssttrruucctt uuttmmppxx **ppuuttuuttxxlliinnee((ccoonnsstt ssttrruucctt uuttmmppxx **));;
           vvooiidd sseettuuttxxeenntt((vvooiidd));;
           vvooiidd eenndduuttxxeenntt((vvooiidd));;

       These functions are provided by glibc, and perform the same task as
       their equivalents without the "x", but use _s_t_r_u_c_t _u_t_m_p_x, defined on
       Linux to be the same as _s_t_r_u_c_t _u_t_m_p.  For completeness, glibc also
       provides uuttmmppxxnnaammee(), although this function is not specified by
       POSIX.1.

       On some other systems, the _u_t_m_p_x structure is a superset of the _u_t_m_p
       structure, with additional fields, and larger versions of the existing
       fields, and parallel files are maintained, often _/_v_a_r_/_*_/_u_t_m_p_x and
       _/_v_a_r_/_*_/_w_t_m_p_x.

       Linux glibc on the other hand does not use a parallel _u_t_m_p_x file since
       its _u_t_m_p structure is already large enough.  The "x" functions listed
       above are just aliases for their counterparts without the "x" (e.g.,
       ggeettuuttxxeenntt() is an alias for ggeettuutteenntt()).

NNOOTTEESS
   gglliibbcc nnootteess
       The above functions are not thread‐safe.  glibc adds reentrant versions

       ##iinncclluuddee <<uuttmmpp..hh>>

       iinntt ggeettuutteenntt__rr((ssttrruucctt uuttmmpp **_u_b_u_f,, ssttrruucctt uuttmmpp ****_u_b_u_f_p));;
       iinntt ggeettuuttiidd__rr((ssttrruucctt uuttmmpp **_u_t,,
                     ssttrruucctt uuttmmpp **_u_b_u_f,, ssttrruucctt uuttmmpp ****_u_b_u_f_p));;
       iinntt ggeettuuttlliinnee__rr((ssttrruucctt uuttmmpp **_u_t,,
                       ssttrruucctt uuttmmpp **_u_b_u_f,, ssttrruucctt uuttmmpp ****_u_b_u_f_p));;

       Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       ggeettuutteenntt__rr(), ggeettuuttiidd__rr(), ggeettuuttlliinnee__rr():
           _GNU_SOURCE
               || /* Since glibc 2.19: */ _DEFAULT_SOURCE
               || /* glibc <= 2.19: */    _SVID_SOURCE || _BSD_SOURCE

       These functions are GNU extensions, analogs of the functions of the
       same name without the _r suffix.  The _u_b_u_f argument gives these
       functions a place to store their result.  On success, they return 0,
       and a pointer to the result is written in _*_u_b_u_f_p.  On error, these
       functions return -1.  There are no utmpx equivalents of the above
       functions.  (POSIX.1 does not specify such functions.)

EEXXAAMMPPLLEESS
       The following example adds and removes a utmp record, assuming it is
       run from within a pseudo terminal.  For usage in a real application,
       you should check the return values of ggeettppwwuuiidd(3) and ttttyynnaammee(3).

       #include <err.h>
       #include <pwd.h>
       #include <stdlib.h>
       #include <string.h>
       #include <time.h>
       #include <unistd.h>
       #include <utmp.h>

       int
       main(void)
       {
           struct utmp entry;

           if (system("echo before adding entry:;who") == -1)
               err(EXIT_FAILURE, "system");

           entry.ut_type = USER_PROCESS;
           entry.ut_pid = getpid();
           strcpy(entry.ut_line, ttyname(STDIN_FILENO) + strlen("/dev/"));
           /* only correct for ptys named /dev/tty[pqr][0-9a-z] */
           strcpy(entry.ut_id, ttyname(STDIN_FILENO) + strlen("/dev/tty"));
           entry.ut_time = time(NULL);
           strcpy(entry.ut_user, getpwuid(getuid())->pw_name);
           memset(entry.ut_host, 0, UT_HOSTSIZE);
           entry.ut_addr = 0;
           setutent();
           if (pututline(&entry) == NULL)
               err(EXIT_FAILURE, "pututline");

           if (system("echo after adding entry:;who") == -1)
               err(EXIT_FAILURE, "system");

           entry.ut_type = DEAD_PROCESS;
           memset(entry.ut_line, 0, UT_LINESIZE);
           entry.ut_time = 0;
           memset(entry.ut_user, 0, UT_NAMESIZE);
           setutent();
           if (pututline(&entry) == NULL)
               err(EXIT_FAILURE, "pututline");

           if (system("echo after removing entry:;who") == -1)
               err(EXIT_FAILURE, "system");

           endutent();
           exit(EXIT_SUCCESS);
       }

SSEEEE AALLSSOO
       ggeettuuttmmpp(3), uuttmmpp(5)

Linux man‐pages (unreleased)      2025‐09‐21                       _g_e_t_u_t_e_n_t(3)
