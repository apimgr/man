MAKEWHATIS(8)             BSD System Manager's Manual            MAKEWHATIS(8)

NNAAMMEE
     mmaakkeewwhhaattiiss — index UNIX manuals

SSYYNNOOPPSSIISS
     mmaakkeewwhhaattiiss [--aaDDnnppQQ] [--TT uuttff88] [--CC _f_i_l_e]
     mmaakkeewwhhaattiiss [--aaDDnnppQQ] [--TT uuttff88] _d_i_r _._._.
     mmaakkeewwhhaattiiss [--DDnnppQQ] [--TT uuttff88] --dd _d_i_r [_f_i_l_e _._._.]
     mmaakkeewwhhaattiiss [--DDnnpp] [--TT uuttff88] --uu _d_i_r [_f_i_l_e _._._.]
     mmaakkeewwhhaattiiss [--DDQQ] --tt _f_i_l_e _._._.

DDEESSCCRRIIPPTTIIOONN
     The mmaakkeewwhhaattiiss utility extracts keywords from UNIX manuals and indexes
     them in a database for fast retrieval by apropos(1), whatis(1), and
     man(1)'s --kk option.

     By default, mmaakkeewwhhaattiiss creates a database in each _d_i_r using the files
     mmaann_s_e_c_t_i_o_n/[_a_r_c_h/]_t_i_t_l_e._s_e_c_t_i_o_n and ccaatt_s_e_c_t_i_o_n/[_a_r_c_h/]_t_i_t_l_e.00 in that di‐
     rectory.  Existing databases are replaced.  If a directory contains no
     manual pages, no database is created in that directory.  If _d_i_r is not
     provided, mmaakkeewwhhaattiiss uses the default paths stipulated by man.conf(5).

     The arguments are as follows:

     --aa       Use all directories and files found below _d_i_r _._._..

     --CC _f_i_l_e  Specify an alternative configuration _f_i_l_e in man.conf(5) format.

     --DD       Display all files added or removed to the index.  With a second
              --DD, also show all keywords added for each file.

     --dd _d_i_r   Merge (remove and re-add) _f_i_l_e _._._. to the database in _d_i_r.

     --nn       Do not create or modify any database; scan and parse only, and
              print manual page names and descriptions to standard output.

     --pp       Print warnings about potential problems with manual pages to the
              standard error output.

     --QQ       Quickly build reduced-size databases by reading only the NAME
              sections of manuals.  The resulting databases will usually con‐
              tain names and descriptions only.

     --TT uuttff88  Use UTF-8 encoding instead of ASCII for strings stored in the
              databases.

     --tt _f_i_l_e _._._.
              Check the given _f_i_l_e_s for potential problems.  Implies --aa, --nn,
              and --pp.  All diagnostic messages are printed to the standard
              output; the standard error output is not used.

     --uu _d_i_r   Remove _f_i_l_e _._._. from the database in _d_i_r.  If that causes the
              database to become empty, also delete the database file.

     If fatal parse errors are encountered while parsing, the offending file
     is printed to stderr, omitted from the index, and the parse continues
     with the next input file.

EENNVVIIRROONNMMEENNTT
     MANPATH  A colon-separated list of directories to create databases in.
              Ignored if a _d_i_r argument or the --tt option is specified.

FFIILLEESS
     mandoc.db
             A database of manpages relative to the directory of the file.
             This file is portable across architectures and systems, so long
             as the manpage hierarchy it indexes does not change.

     /etc/man.conf
             The default man(1) configuration file.

EEXXIITT SSTTAATTUUSS
     The mmaakkeewwhhaattiiss utility exits with one of the following values:

     0       No errors occurred.
     5       Invalid command line arguments were specified.  No input files
             have been read.
     6       An operating system error occurred, for example memory exhaustion
             or an error accessing input files.  Such errors cause mmaakkeewwhhaattiiss
             to exit at once, possibly in the middle of parsing or formatting
             a file.  The output databases are corrupt and should be removed.

SSEEEE AALLSSOO
     apropos(1), man(1), whatis(1), man.conf(5)

HHIISSTTOORRYY
     A mmaakkeewwhhaattiiss utility first appeared in 2BSD.  It was rewritten in perl(1)
     for OpenBSD 2.7 and in C for OpenBSD 5.6.

     The _d_i_r argument first appeared in NetBSD 1.0; the options --ddpptt in
     OpenBSD 2.7; the option --uu in OpenBSD 3.4; and the options --aaCCDDnnQQTT in
     OpenBSD 5.6.

AAUUTTHHOORRSS
     Bill Joy wrote the original BSD mmaakkeewwhhaattiiss in February 1979, Marc Espie
     started the Perl version in 2000, and the current version of mmaakkeewwhhaattiiss
     was written by Kristaps Dzonsons <_k_r_i_s_t_a_p_s_@_b_s_d_._l_v> and Ingo Schwarze
     <_s_c_h_w_a_r_z_e_@_o_p_e_n_b_s_d_._o_r_g>.

BSD                              May 17, 2017                              BSD
