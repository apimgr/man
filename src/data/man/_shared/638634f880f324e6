The  utility  is  a  filter  which displays each specified or the
standard input if no arguments are specified, in a user specified
format.  The options are as follows: Specify  the  input  address
base.  may be one of or which specify decimal, octal, hexadecimal
addresses  or no address, respectively.  Display the input offset
in octal, followed  by  sixteen  space‐separated,  three  column,
space‐filled, characters of input data per line.  Control charac‐
ters  are  printed  as their names instead of as C‐style escapes.
Same as Display the input offset in octal,  followed  by  sixteen
space‐separated,  three column, zero‐filled, bytes of input data,
in octal, per line.  This is the default output style if no other
is selected.  Display the input offset in octal, followed by six‐
teen space‐separated, three column, space‐filled,  characters  of
input  data  per line.  Control characters are printed at C‐style
escapes, or as three octal digits, if no C escape exists for  the
character.   Display the input offset in octal, followed by eight
space‐separated, five column, zero‐filled, two‐byte units of  in‐
put  data, in unsigned decimal, per line.  Display the input off‐
set in octal, followed by two space‐separated, twenty‐one column,
space filled, eight byte units of input data, in floating  point,
per line.  Same as Display the input offset in octal, followed by
four space‐separated, 14 column, space filled, four byte units of
input  data, in floating point, per line.  Display the input off‐
set in octal, followed by  four  space‐separated,  eight  column,
zero  filled,  four  byte  units of input data, in hex, per line.
Display the input offset in octal, followed by eight  space‐sepa‐
rated, four column, zero filled, two byte units of input data, in
hex,  per  line.   Display the input offset in octal, followed by
four space‐separated, eleven  column,  space  filled,  four  byte
units  of  input  data,  in decimal, per line.  Display the input
offset in octal, followed by eight space‐separated,  six  column,
space filled, two‐byte units of input data, in decimal, per line.
Skip  bytes  from the beginning of the input.  By default, is in‐
terpreted as a decimal number.  With a leading or is  interpreted
as a hexadecimal number; otherwise, with a leading is interpreted
as  an  octal number.  Appending the character or to causes it to
be interpreted as a multiple of or respectively.  Same as Same as
Interpret only bytes of input.  Display the input offset  in  oc‐
tal,  followed  by  four  space‐separated,  eleven  column, zero‐
filled, four‐byte units of input data, in octal, per line.   Dis‐
play  the  input  offset  in octal, followed by eight space‐sepa‐
rated, six column, zero‐filled, two‐byte units of input data,  in
octal,  per line.  Specify one or more output types.  The option‐
argument must be a string specifying the types to  be  used  when
writing  the  input  data.   The  string must consist of the type
specification characters: selects US‐ASCII output,  with  control
characters  replaced  with their names instead of as C escape se‐
quences.  See also the conversion provided by selects a  standard
character  based conversion.  See also the conversion provided by
selects the floating point output format.   This  type  character
can  be  optionally followed by the characters or to specify four
byte floating point output, or or to specify eight byte  floating
point  output.   The  default output format is eight byte floats.
See also the conversion provided by or select decimal, octal, un‐
signed decimal, or hex output respectively.  These types can  op‐
tionally  be followed by to specify output, to specify output, to
specify output, to specify output, to specify one‐byte output, to
specify two‐byte output, to specify four‐byte output, or to spec‐
ify eight‐byte output.  The default output format is in four‐byte
quantities.  See also the and conversions provided by The  option
causes to display all input data.  Without the option, any number
of  groups of output lines, which would be identical to the imme‐
diately preceding group of output lines  (except  for  the  input
offsets), are replaced with a line comprised of a single asterisk
Same  as Same as For each input file, sequentially copies the in‐
put to standard output, transforming the data  according  to  the
options  given.  If no options are specified, the default display
is equivalent to specifying the option.  An command  appeared  in
This  man  page  was initially written in February 2001 by Andrew
Brown, shortly after he augmented the deprecated  syntax  to  in‐
clude things he felt had been missing for a long time.
