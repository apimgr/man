executes after modifying the environment as specified on the com‐
mand  line.  The option specifies an environmental variable, with
a value of The option causes to completely ignore the environment
it inherits.  The option causes removal of the environment  vari‐
able if it is in the environment.  This is similar to the command
in The value for must not include the character.  If no is speci‐
fied, prints out the names and values of the variables in the en‐
vironment.  Each pair is separated by a new line unless is speci‐
fied,  in which case name/value pairs are separated by NUL.  Both
and must not be specified together.  exits with one of  the  fol‐
lowing  values:  was invoked and completed successfully.  In this
case the exit code is returned by the utility itself, not  If  no
utility  was  specified, then completed successfully and returned
the exit code itself.  An invalid command line option was  passed
to  was  invoked, but failed in some way; see its manual page for
more information.  In this case the exit code is returned by  the
utility  itself, not was specified together with the option.  was
found, but could not be invoked.  could not be found.   The  his‐
toric  option  has been deprecated but is still supported in this
implementation.  The and  options  are  non‐standard  extensions.
The  utility  conforms to The command appeared in The and options
first appeared in doesn’t handle commands  with  equal  signs  in
their names, for obvious reasons.
