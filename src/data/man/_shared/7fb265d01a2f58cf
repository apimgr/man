is  used to configure Bluetooth devices.  If the is given, but no
parameters, then will print information about the device.  If  no
is given, a basic list of devices will be printed.  When the flag
is  used,  just  the device names will be printed.  The following
parameters may be specified with Enable Bluetooth  Device.   Dis‐
able  Bluetooth Device.  Enable Page Scan.  This enables incoming
connections to the device.  Disable Page  Scan.   Enable  Inquiry
Scan.   This  puts  the device in Discoverable mode.  Disable In‐
quiry Scan.  Enable encryption.  This will cause  the  device  to
request  encryption  on  all  baseband connections, and will only
work if authentication is also enabled.  Disable encryption.  En‐
able authentication.  This will cause the device to  request  au‐
thentication  for  all baseband connections.  Disable authentica‐
tion.  Enable Role Switching.  In a Bluetooth  piconet  there  is
one  Master  and up to seven Slaves, and normally the device that
initiates a connection will take the Master role.  Enabling  this
option allows remote devices to switch connection roles.  Disable
Role  Switching.   Request the Master role when accepting connec‐
tions.  Do not request Master role.  Enable Hold  Mode.   Disable
Hold  Mode.  Enable Sniff Mode.  Disable Sniff Mode.  Enable Park
Mode.  Disable Park Mode.  Set human  readable  name  of  device.
Set  packet types.  is a 16 bit hex value specifying packet types
that will be requested by outgoing ACL connections.  By  default,
all  packet types that the device supports are enabled, see blue‐
tooth specifications for more information if you want  to  change
this.   Set class of device.  is a 6 digit hex value the value of
which declares the device capabilities.  See the document at  for
details  of  constructing a Class of Device value.  As a starter,
0x020104 means Desktop Computer, with Networking available.   Set
fixed pin type.  Set variable pin type.  Perform device Discovery
from  the  specified  device and print details.  Set inquiry mode
type to control which event formats are generated during a device
inquiry.  The can be: Standard Inquiry Result Event format.   En‐
able  Remote Signal Strength Indicator (RSSI) in inquiry results.
This will only work if the device features indicate  Inquiry  Re‐
sult  with  RSSI  format or Extended Inquiry Result format.  This
will only work where the device features  indicate  and  the  Ex‐
tended Inquiry Result will only occur when the remote device pro‐
vides  the extended information.  Perform a hard reset on the de‐
vice and re‐initialise system state.  Set Voice  Setting.   [This
should be 0x0060 for now] Set Page Timeout value.  This is a dec‐
imal  value  in milliseconds, indicating how long the device will
attempt to page another device when opening baseband connections.
Change SCO mtu value.  This is a decimal value, see  for  reasons
why  you  may  need to do this.  All parameters are parsed before
any device operations take place.  Each time the flag  is  given,
verbosity  levels  will  be increased.  Super‐user privileges are
required to change device  configurations.   Messages  indicating
the specified device does not exist, the requested address is un‐
known,  or  the  user  is not privileged and tried to alter a de‐
vice’s configuration.  The command was written for by  under  the
sponsorship of Itronix, Inc.  The output is very messy.
