_i_o_c_t_l___e_v_e_n_t_p_o_l_l(2)            System Calls Manual           _i_o_c_t_l___e_v_e_n_t_p_o_l_l(2)

NNAAMMEE
       ioctl_eventpoll,  EPIOCSPARAMS,  EPIOCGPARAMS  - ioctl() operations for
       epoll file descriptors

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssyyss//eeppoollll..hh>>  /* Definition of EEPPIIOOCC** constants */
       ##iinncclluuddee <<ssyyss//iiooccttll..hh>>

       iinntt iiooccttll((iinntt _f_d,, EEPPIIOOCCSSPPAARRAAMMSS,, ccoonnsstt ssttrruucctt eeppoollll__ppaarraammss **_a_r_g_p));;
       iinntt iiooccttll((iinntt _f_d,, EEPPIIOOCCGGPPAARRAAMMSS,, ssttrruucctt eeppoollll__ppaarraammss **_a_r_g_p));;

       ##iinncclluuddee <<ssyyss//eeppoollll..hh>>

       ssttrruucctt eeppoollll__ppaarraammss {{
           uuiinntt3322__tt  bbuussyy__ppoollll__uusseeccss;;   /* Number of usecs to busy poll */
           uuiinntt1166__tt  bbuussyy__ppoollll__bbuuddggeett;;  /* Max packets per poll */
           uuiinntt88__tt   pprreeffeerr__bbuussyy__ppoollll;;  /* Boolean preference  */

           /* pad the struct to a multiple of 64bits */
           uuiinntt88__tt   ____ppaadd;;             /* Must be zero */
       }};;

DDEESSCCRRIIPPTTIIOONN
       EEPPIIOOCCSSPPAARRAAMMSS
              Set the _e_p_o_l_l___p_a_r_a_m_s structure to  configure  the  operation  of
              epoll.   Refer  to the structure description below to learn what
              configuration is supported.

       EEPPIIOOCCGGPPAARRAAMMSS
              Get the current _e_p_o_l_l___p_a_r_a_m_s configuration settings.

       All operations documented above must be performed on an epoll file  de‐
       scriptor,  which  can  be  obtained  with  a call to eeppoollll__ccrreeaattee(2) or
       eeppoollll__ccrreeaattee11(2).

   TThhee eeppoollll__ppaarraammss ssttrruuccttuurree
       _a_r_g_p_._b_u_s_y___p_o_l_l___u_s_e_c_s denotes the number of microseconds that  the  net‐
       work stack will busy poll.  During this time period, the network device
       will  be  polled  repeatedly  for  packets.   This  value cannot exceed
       IINNTT__MMAAXX.

       _a_r_g_p_._b_u_s_y___p_o_l_l___b_u_d_g_e_t denotes the maximum number of  packets  that  the
       network  stack  will  retrieve on each poll attempt.  This value cannot
       exceed NNAAPPII__PPOOLLLL__WWEEIIGGHHTT (which is 64  as  of  Linux  6.9),  unless  the
       process is run with CCAAPP__NNEETT__AADDMMIINN.

       _a_r_g_p_._p_r_e_f_e_r___b_u_s_y___p_o_l_l  is  a  boolean  field and must be either 0 (dis‐
       abled) or 1 (enabled).  If enabled, this indicates to the network stack
       that busy poll is the preferred method of processing network  data  and
       the  network  stack should give the application the opportunity to busy
       poll.  Without this option, very busy systems may continue to  do  net‐
       work  processing  via  the normal method of IRQs triggering softIRQ and
       NAPI.

       _a_r_g_p_._____p_a_d must be zero.

RREETTUURRNN VVAALLUUEE
       On success, 0 is returned.  On failure, -1 is returned,  and  _e_r_r_n_o  is
       set to indicate the error.

EERRRROORRSS
       EEOOPPNNOOTTSSUUPPPP
              The kernel was not compiled with busy poll support.

       EEIINNVVAALL _f_d is not a valid file descriptor.

       EEIINNVVAALL _a_r_g_p_._____p_a_d is not zero.

       EEIINNVVAALL _a_r_g_p_._b_u_s_y___p_o_l_l___u_s_e_c_s exceeds IINNTT__MMAAXX.

       EEIINNVVAALL _a_r_g_p_._p_r_e_f_e_r___b_u_s_y___p_o_l_l is not 0 or 1.

       EEPPEERRMM  The process is being run without CCAAPP__NNEETT__AADDMMIINN and the specified
              _a_r_g_p_._b_u_s_y___p_o_l_l___b_u_d_g_e_t exceeds NNAAPPII__PPOOLLLL__WWEEIIGGHHTT.

       EEFFAAUULLTT _a_r_g_p is an invalid address.

SSTTAANNDDAARRDDSS
       Linux.

HHIISSTTOORRYY
       Linux 6.9.  glibc 2.40.

EEXXAAMMPPLLEESS
       /* Code to set the epoll params to enable busy polling */

       int epollfd = epoll_create1(0);
       struct epoll_params params;

       if (epollfd == -1) {
           perror("epoll_create1");
           exit(EXIT_FAILURE);
       }

       memset(&params, 0, sizeof(struct epoll_params));

       params.busy_poll_usecs = 25;
       params.busy_poll_budget = 8;
       params.prefer_busy_poll = 1;

       if (ioctl(epollfd, EPIOCSPARAMS, &params) == -1) {
           perror("ioctl");
           exit(EXIT_FAILURE);
       }

       /* Code to show how to retrieve the current settings */

       memset(&params, 0, sizeof(struct epoll_params));

       if (ioctl(epollfd, EPIOCGPARAMS, &params) == -1) {
           perror("ioctl");
           exit(EXIT_FAILURE);
       }

       /* params struct now contains the current parameters */

       fprintf(stderr, "epoll usecs: %lu\n", params.busy_poll_usecs);
       fprintf(stderr, "epoll packet budget: %u\n", params.busy_poll_budget);
       fprintf(stderr, "epoll prefer busy poll: %u\n", params.prefer_busy_poll);

SSEEEE AALLSSOO
       iiooccttll(2), eeppoollll__ccrreeaattee(2), eeppoollll__ccrreeaattee11(2), eeppoollll(7)

       _l_i_n_u_x_._g_i_t_/_D_o_c_u_m_e_n_t_a_t_i_o_n_/_n_e_t_w_o_r_k_i_n_g_/_n_a_p_i_._r_s_t

       _l_i_n_u_x_._g_i_t_/_D_o_c_u_m_e_n_t_a_t_i_o_n_/_a_d_m_i_n_‐_g_u_i_d_e_/_s_y_s_c_t_l_/_n_e_t_._r_s_t

Linux man‐pages (unreleased)      2025‐05‐17                _i_o_c_t_l___e_v_e_n_t_p_o_l_l(2)
