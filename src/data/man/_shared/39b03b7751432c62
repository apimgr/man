GETBOOTFILE(3)           BSD Library Functions Manual           GETBOOTFILE(3)

NNAAMMEE
     ggeettbboooottffiillee — get kernel boot file name

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ppaatthhss..hh>>

     _c_o_n_s_t _c_h_a_r _*
     ggeettbboooottffiillee(_v_o_i_d);

DDEESSCCRRIIPPTTIIOONN
     The ggeettbboooottffiillee() function retrieves the full pathname of the file from
     which the current kernel was loaded, and returns a static pointer to the
     name.  A read/write interface to this information is available via the
     sysctl(3) MIB variable “kern.bootfile”.

RREETTUURRNN VVAALLUUEESS
     If the call succeeds a string giving the pathname is returned.  If it
     fails, a null pointer is returned and an error code is placed in the
     global location _e_r_r_n_o.

SSEEEE AALLSSOO
     sysctl(3)

HHIISSTTOORRYY
     The ggeettbboooottffiillee() function appeared in FreeBSD 2.0.

BBUUGGSS
     If the boot blocks have not been modified to pass this information into
     the kernel at boot time, the static string “_/_b_o_o_t_/_k_e_r_n_e_l_/_k_e_r_n_e_l” is re‐
     turned instead of the real boot file.

BSD                           September 23, 1994                           BSD
