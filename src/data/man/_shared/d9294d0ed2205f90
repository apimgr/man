shows  status  information  about the NFS server on By default it
prints the names of all hosts that have NFS file systems  mounted
on the host.  See RFC 1094, Appendix A, and Appendix I, for a de‐
tailed description of the protocol.  List all mount points in the
form:  List directory paths of mount points instead of hosts Show
the exports list in a script‐friendly format.   Client  addresses
and the header are not shown, and special characters are escaped.
Show  the  exports  list Use mount protocol Version 3, compatible
with NFS Version 3.  The utility first appeared in The mount dae‐
mon running on the server only has an idea of the actual  mounts,
since  the NFS server is stateless.  will only display the infor‐
mation as accurately as the mount daemon reports it.
