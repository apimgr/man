LIBXO(3)                 BSD Library Functions Manual                 LIBXO(3)

NNAAMMEE
     xxoo__ooppeenn__ccoonnttaaiinneerr, xxoo__ooppeenn__ccoonnttaaiinneerr__hh, xxoo__ooppeenn__ccoonnttaaiinneerr__hhdd,
     xxoo__ooppeenn__ccoonnttaaiinneerr__dd xxoo__cclloossee__ccoonnttaaiinneerr, xxoo__cclloossee__ccoonnttaaiinneerr__hh,
     xxoo__cclloossee__ccoonnttaaiinneerr__hhdd, xxoo__cclloossee__ccoonnttaaiinneerr__dd — open (and close) container
     constructs

LLIIBBRRAARRYY
     library “libxo”

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<lliibbxxoo//xxoo..hh>>

     _x_o___s_s_i_z_e___t
     xxoo__ooppeenn__ccoonnttaaiinneerr(_c_o_n_s_t _c_h_a_r _*_n_a_m_e);

     _x_o___s_s_i_z_e___t
     xxoo__ooppeenn__ccoonnttaaiinneerr__hh(_x_o___h_a_n_d_l_e___t _*_h_a_n_d_l_e, _c_o_n_s_t _c_h_a_r _*_n_a_m_e);

     _x_o___s_s_i_z_e___t
     xxoo__ooppeenn__ccoonnttaaiinneerr__hhdd(_x_o___h_a_n_d_l_e___t _*_h_a_n_d_l_e, _c_o_n_s_t _c_h_a_r _*_n_a_m_e);

     _x_o___s_s_i_z_e___t
     xxoo__ooppeenn__ccoonnttaaiinneerr__dd(_c_o_n_s_t _c_h_a_r _*_n_a_m_e);

     _x_o___s_s_i_z_e___t
     xxoo__cclloossee__ccoonnttaaiinneerr(_c_o_n_s_t _c_h_a_r _*_n_a_m_e);

     _x_o___s_s_i_z_e___t
     xxoo__cclloossee__ccoonnttaaiinneerr__hh(_x_o___h_a_n_d_l_e___t _*_h_a_n_d_l_e, _c_o_n_s_t _c_h_a_r _*_n_a_m_e);

     _x_o___s_s_i_z_e___t
     xxoo__cclloossee__ccoonnttaaiinneerr__hhdd(_x_o___h_a_n_d_l_e___t _*_h_a_n_d_l_e);

     _x_o___s_s_i_z_e___t
     xxoo__cclloossee__ccoonnttaaiinneerr__dd(_v_o_i_d);

DDEESSCCRRIIPPTTIIOONN
     lliibbxxoo represents two types of hierarchy: “containers” and “lists”.  A
     container appears once under a given parent where a list contains in‐
     stances that can appear multiple times.  A container is used to hold re‐
     lated fields and to give the data organization and scope.  The container
     has no value, but serves to contain other nodes.

     To open a container, call xxoo__ooppeenn__ccoonnttaaiinneerr() or xxoo__ooppeenn__ccoonnttaaiinneerr__hh().
     The former uses the default handle and the latter accepts a specific han‐
     dle.

     To close a level, use the xxoo__cclloossee__ccoonnttaaiinneerr() or xxoo__cclloossee__ccoonnttaaiinneerr__hh()
     functions.

     Each open call should have a matching close call.  If the XOF_WARN flag
     is set and the name given does not match the name of the currently open
     container, a warning will be generated.
               Example:

                   xo_open_container("top");
                   xo_open_container("system");
                   xo_emit("{:host-name/%s%s%s", hostname,
                           domainname ? "." : "", domainname ?: "");
                   xo_close_container("system");
                   xo_close_container("top");

               Sample Output:
                 Text:
                   my-host.example.org
                 XML:
                   <top>
                     <system>
                         <host-name>my-host.example.org</host-name>
                     </system>
                   </top>
                 JSON:
                   "top" : {
                     "system" : {
                         "host-name": "my-host.example.org"
                     }
                   }
                 HTML:
                   <div class="data"
                        data-tag="host-name">my-host.example.org</div>

EEMMIITTTTIINNGG HHIIEERRAARRCCHHYY
     To create a container, use the xxoo__ooppeenn__ccoonnttaaiinneerr() and
     xxoo__cclloossee__ccoonnttaaiinneerr() set of functions.  The _h_a_n_d_l_e parameter contains a
     handle such as returned by xo_create(3) or NULL to use the default han‐
     dle.  The _n_a_m_e parameter gives the name of the container, encoded in
     _U_T_F_-_8.  Since _A_S_C_I_I is a proper subset of _U_T_F_-_8, traditional C strings
     can be used directly.

     The close functions with the “_d” suffix are used in “Do The Right Thing”
     mode, where the name of the open containers, lists, and instances are
     maintained internally by lliibbxxoo to allow the caller to avoid keeping track
     of the open container name.

     Use the XOF_WARN flag to generate a warning if the name given on the
     close does not match the current open container.

     For TEXT and HTML output, containers are not rendered into output text,
     though for HTML they are used when the XOF_XPATH flag is set.

               EXAMPLE:
                  xo_open_container("system");
                  xo_emit("The host name is {:host-name}\n", hn);
                  xo_close_container("system");
               XML:
                  <system><host-name>foo</host-name></system>

DDTTRRTT MMOODDEE
     Some users may find tracking the names of open containers, lists, and in‐
     stances inconvenient.  lliibbxxoo offers a “Do The Right Thing” mode, where
     lliibbxxoo will track the names of open containers, lists, and instances so
     the close function can be called without a name.  To enable _D_T_R_T mode,
     turn on the XOF_DTRT flag prior to making any other lliibbxxoo output.
               xo_set_flags(NULL, XOF_DTRT);
     Each open and close function has a version with the suffix “_d”, which
     will close the open container, list, or instance:
               xo_open_container("top");
               ...
               xo_close_container_d();
     Note that the XOF_WARN flag will also cause lliibbxxoo to track open contain‐
     ers, lists, and instances.  A warning is generated when the name given to
     the close function and the name recorded do not match.

SSEEEE AALLSSOO
     xo_emit(3), libxo(3)

HHIISSTTOORRYY
     The lliibbxxoo library first appeared in FreeBSD 11.0.

AAUUTTHHOORRSS
     lliibbxxoo was written by Phil Shafer <_p_h_i_l_@_f_r_e_e_b_s_d_._o_r_g>.


BSD                            December 4, 2014                            BSD
