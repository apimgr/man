mlx5tool(8)               BSD System Manager's Manual              mlx5tool(8)

NNAAMMEE
     mmllxx55ttooooll — Utility for managing Connect-X 4/5/6 Mellanox network adapters

SSYYNNOOPPSSIISS
     mmllxx55ttooooll --dd _d_o_m_a_i_n_:_b_u_s_:_s_l_o_t_:_f_u_n_c --EE
     mmllxx55ttooooll --dd _d_o_m_a_i_n_:_b_u_s_:_s_l_o_t_:_f_u_n_c --ee
     mmllxx55ttooooll --dd _d_o_m_a_i_n_:_b_u_s_:_s_l_o_t_:_f_u_n_c --ff _f_i_l_e_._m_f_a_2
     mmllxx55ttooooll --dd _d_o_m_a_i_n_:_b_u_s_:_s_l_o_t_:_f_u_n_c --oo _f_i_l_e --ww
     mmllxx55ttooooll --dd _d_o_m_a_i_n_:_b_u_s_:_s_l_o_t_:_f_u_n_c --rr
     mmllxx55ttooooll --dd _d_o_m_a_i_n_:_b_u_s_:_s_l_o_t_:_f_u_n_c --zz

DDEESSCCRRIIPPTTIIOONN
     The mmllxx55ttooooll utility is provided for management of the Connect-X4, 5 and
     6 network adapters in the aspects not covered by the generic ifconfig(8)
     command, mostly related to the PCIe attachment and internal card working.
     The utility executes commands on specific adapter, which is addressed us‐
     ing _d_e_v_i_c_e_:_b_u_s_:_s_l_o_t_:_f_u_n_c_t_i_o_n conventions of the PCIe buses.  You can
     match adapters ethernet name and addresses using the pciconf(8) utility.
     The address is passed as an argument of the --dd option, which must be
     specified for each invocation.

     When the driver detects the malfunctioning of the hardware, or by user
     request, it is possible to create _f_i_r_m_w_a_r_e _d_u_m_p, which contains debugging
     information about internal device state, for analysis of the failure by
     the Mellanox support team.

     The following commands are currently implemented:

     --EE      Print EEPROM information

     --ee      Take the snapshot of the firmware registers state and store it in
             the kernel buffer.  The buffer must be empty, in other words, no
             dumps should be written so far, or existing dump cleared with the

     --ff      Flashes the firmware image _f_i_l_e_._m_f_a_2 to the specified adapter.
             Image must be in MFA2 pack format and contain a component suit‐
             able for the adapter hardware.

             Typically, PCIe link-level reset is required to activate the
             newly flashed image, which can be performed by the system reboot
             or using the --zz option.  --rr command for the specified device.

     --rr      Clear the stored firmware dump, preparing the kernel buffer for
             the next dump.

     --ww      Fetches the stored firmware dump and writes it into the file
             specified by the --oo option argument.

     --zz      Performs PCIe link-level reset on the specified device.

FFIILLEESS
     The _/_d_e_v_/_m_l_x_5_c_t_l devfs(5) node is used to pass commands to the driver.

SSEEEE AALLSSOO
     mlx5en(4), mlx5ib(4), mlx5io(4), ifconfig(8) and pciconf(8).

BSD                               May 7, 2019                              BSD
