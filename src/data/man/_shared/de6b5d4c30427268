initializes  the  files  and  directories that are required for a
client or server.  If isn’t specified, the default domain (as re‐
turned by is used.  The following options are available: Create a
client.  Initializes to contain a list of ypservers for  to  con‐
nect  to.   Set  the list of client servers from the command line
rather than prompting for them interactively.  The  format  is  a
comma  separated  list  of server names with no spaces.  Create a
master server.  Generates map data from local files etc.).   Cre‐
ate  a slave server.  Downloads the maps from which should be the
active master server.  To rebuild or refresh the maps for the do‐
main <domain>, change to the directory and run master  directory;
contains  the  template  makefiles.   directory to store maps for
<domain>.  list of servers to bind  to.   Originally  written  by
Modified by
