DWARF_ERRMSG(3)          BSD Library Functions Manual          DWARF_ERRMSG(3)

NNAAMMEE
     ddwwaarrff__eerrrrmmssgg — retrieve a human-readable string corresponding to a
     _D_w_a_r_f___E_r_r_o_r instance

LLIIBBRRAARRYY
     DWARF Access Library (libdwarf, -ldwarf)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<lliibbddwwaarrff..hh>>

     _c_o_n_s_t _c_h_a_r _*
     ddwwaarrff__eerrrrmmssgg(_D_w_a_r_f___E_r_r_o_r _e_r_r);

DDEESSCCRRIIPPTTIIOONN
     Function ddwwaarrff__eerrrrmmssgg() returns a NUL-terminated string for the error de‐
     noted by argument _e_r_r.

     Argument _e_r_r should be a valid handle to a _D_w_a_r_f___E_r_r_o_r instance.

MMeemmoorryy MMaannaaggeemmeenntt
     The returned pointer should not be freed using free(3) or
     dwarf_dealloc(3).

RREETTUURRNN VVAALLUUEESS
     Function ddwwaarrff__eerrrrmmssgg() returns a pointer to a NUL-terminated string.

SSEEEE AALLSSOO
     dwarf(3), dwarf_errno(3)

BSD                            December 12, 2009                           BSD
