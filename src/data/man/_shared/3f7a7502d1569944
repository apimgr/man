This  manual page describes a number of miscellaneous kernel con‐
figuration options that may be specified in a kernel config file.
See and for information on how to configure  and  build  kernels.
The  form  removes  a previously specified option.  The following
keywords are recognized in a kernel  configuration  file:  Condi‐
tionally includes another kernel configuration file whose name is
which  may  be double‐quoted and may be an explicit path or rela‐
tive to the kernel source directory.  Failure to open  the  named
file is ignored.  Defines a configuration whose kernel executable
is named normally with its root file system of type on the device
and  optionally  specifying  the location of kernel core dumps on
the device or and may be specified as which is a wild card.   The
root  and  are optional and assumed to be wild carded if they are
not specified.  Define an instance of the device driver that  at‐
taches  to  the bus or device named An may require additional in‐
formation on where the device can be found, such as  an  address,
channel,  function, offset, and/or slot, referred to as whose of‐
ten may be a wild card, Some device drivers have one or more that
can be adjusted to affect the way they operate.  Include  support
for  the  file‐system  Sets  the  kernel identification string to
Functions the same as except failure to open produces a fatal er‐
ror.  Defines a macro with the value in the kernel Makefile.  Set
the maxusers  variable  in  the  kernel.   For  the  file‐system,
makeoptions, options, and pseudo‐device, removes the file‐system,
makeoption, options, or pseudo‐device, This is useful when a ker‐
nel  configuration  file includes another which has undesired op‐
tions.  For example, a local configuration file that  wanted  the
kitchen  sink,  but  not COMPAT_09 or bridging, might be: include
"arch/i386/conf/GENERIC" no options  COMPAT_09  no  pseudo‐device
bridge Specifies (or sets) the option, or comma‐separated list of
options, Some options expect to be assigned a value, which may be
an integer, a double‐quoted word, a bare word, or an empty string
Note  that those are eventually handled by the C compiler, so the
rules of that language apply.  Options that are  not  defined  by
device  definition  files  are  passed  to the compile process as
flags to the C compiler.  Includes support for the  pseudo‐device
Some  pseudo‐devices  can  have multiple or instances.  Note that
compatibility options for older  releases  includes  support  for
newer  releases  as  well.  This means that typically only one of
these is necessary, with the option enabling  all  compatibility.
This  does not include the or options.  Enable binary compatibil‐
ity with This enables support for 16‐bit user, group, and process
IDs (following revisions support 32‐bit  identifiers).   It  also
allows  the use of the deprecated and syscalls.  This option also
allows using numeric file system identifiers rather than strings.
Post versions use string identifiers.  Enable binary  compatibil‐
ity with This option allows the use of the file system name of as
an alias for The name should be used post 1.0 in and other files.
It  also adds old syscalls for the shared memory interface.  This
was changed post 1.0 to work on 64‐bit architectures.   This  op‐
tion also enables compatibility, without which programs using the
old  interface  produce an error, and only works when this option
is set in the kernel, see on ports that support it.   Enable  bi‐
nary  compatibility with This allows binaries running on the i386
port to gain direct access to the io ports by opening read/write.
This functionality was replaced by post 1.1.  On the Atari  port,
the location of the disk label was moved after 1.1.  When the op‐
tion  is set, the kernel will read (pre) 1.1 style disk labels as
a last resort.  When a disk label is re‐written,  the  old  style
label  will  be  replaced with a post 1.1 style label.  This also
enables the option.  Enable binary compatibility with This allows
the use of old syscalls for and The syscall numbers were  changed
post  1.2 to add functionality to the syscall, and the new inter‐
face was introduced.  This also enables the option.   Enable  bi‐
nary  compatibility  with This allows the use of old syscalls for
and also enables the old command (now called which does  not  in‐
clude  the member of Enable binary compatibility with This allows
some old on to be performed, and allows the mode  of  the  system
call to be used for compatibility with the deprecated nfsiod pro‐
gram.   Enable  binary compatibility with Since there were no API
changes from and this option does nothing.  Enable binary compat‐
ibility with This allows the use of old  signal  trampoline  code
which has been deprecated with the addition of Enable binary com‐
patibility with This allows the use of old syscalls for and which
have  been  deprecated with the addition of the and system calls.
Enable binary  compatibility  with  See  for  details  about  the
changes made after the release.  Enable binary compatibility with
This  allows  the use of old calls for the SH3 platform.  It also
enables the old system call that did not include the data  length
parameter.   The  power_event_t  structure’s pev_switch is filled
in.  Enables compatibility with This adds an old syscall  for  It
also  adds  the ioctls for and The return values for and syscalls
are modified as well, to return the parent’s PID and UID as  well
as  the current process’s.  It also enables the deprecated termi‐
nal line discipline.  It also  provides  backwards  compatibility
with  SIOC[GS]IF{ADDR,DSTADDR,BRDADDR,NETMASK}  interface ioctls,
including binary compatibility with code written before  the  in‐
troduction  of  the  sa_len  field in sockaddrs.  It also enables
support for some older pre socket calls.  Enable binary  compati‐
bility with This enables support for the old and types as 32 bit,
and all the associated kernel interface changes.  It also enables
old  and  interfaces.  Enable binary compatibility with This pro‐
vides old interfaces, enables support for  old  microcode  inter‐
faces, and support for the old structure.  Enable binary compati‐
bility with This provides support for old interfaces.  Enable bi‐
nary compatibility with Enable binary compatibility with This op‐
tion  is  currently on by default and enables the pty multiplexer
and to find and use ptys named (master) and (slave).   Eventually
this  option  will become optional as ptyfs based pseudo‐ttys be‐
come the default, see On those  architectures  that  support  it,
this enables binary compatibility with Linux ELF and applications
built for the same architecture.  This currently includes the al‐
pha,  arm,  i386, m68k, mips, powerpc and x86_64 ports.  On those
64 bit architectures that support it, this enables binary compat‐
ibility with 32 bit Linux binaries.  For now this is  limited  to
running i386 ELF Linux binaries on amd64.  On those architectures
that support it, this enables binary compatibility with SunOS 4.1
applications built for the same architecture.  This currently in‐
cludes  the sparc, sparc64 and most or all m68k ports.  Note that
the sparc64 requires the option for 64‐bit kernels,  in  addition
to this option.  On those architectures that support it, this en‐
ables binary compatibility with ULTRIX applications built for the
same  architecture.   This currently is limited to the pmax.  The
functionality of this option is unknown.  On those  architectures
that  support it, this enables binary compatibility with applica‐
tions built for the same architecture.  At  the  moment  this  is
limited  to the i386 port.  Enable compatibility with executables
that lack a machine ID.  This includes ZMAGIC format, and  386BSD
and  BSDI’s  QMAGIC, NMAGIC, and OMAGIC formats.  On those archi‐
tectures that support it, this enables binary compatibility  with
32‐bit  applications  built  for  the same architecture.  This is
currently limited to the amd64 and sparc64 ports,  and  only  ap‐
plicable  for  64‐bit  kernels.  On m68k architectures which have
switched to ELF, this enables binary compatibility with  executa‐
bles  on  ELF kernels.  This handles alignment incompatibility of
m68k ABI between a.out and ELF which causes the structure padding
differences.  Currently only some system calls which use are  ad‐
justed  and  some  binaries which use to retrieve network details
would not work properly.  Just like emulated binaries  first  try
looking  up files in an emulation root (e.g.  before looking them
up in real root, this option causes native binaries to first look
up files in an "emulation" directory too.  This can be useful  to
test  an amd64 kernel on top of an i386 system before full migra‐
tion: by unpacking the amd64 distribution in e.g.  and specifying
that location as native amd64 binaries can be run while the  root
file  system remains populated with i386 binaries.  Beware of in‐
compatibilities between i386 and  amd64  if  you  do  this.   Run
unidentified  ELF binaries as binaries.  This might be needed for
very old ELF binaries on some archs.  These old  binaries  didn’t
contain  an  appropriate section, and thus can’t be identified by
the kernel as binaries otherwise.  Beware ‐ if this option is on,
the kernel would run unknown ELF binaries as if they  were  bina‐
ries.   Compiles in a kernel debugger for diagnosing kernel prob‐
lems.  See for details.  not available on all architectures.   If
set  to non‐zero, DDB may be entered by sending a break on a ser‐
ial console or by a special key sequence on a  graphics  console.
A  value  of "0" ignores console breaks or key sequences.  If not
explicitly specified, the default value is "1".  Note  that  this
sets the value of the variable which may be changed at run time —
see  for details.  If this is non‐zero, enable history editing in
the kernel debugger and set the  size  of  the  history  to  this
value.   The  default  if not specified is ‐ just enter into DDB.
If set to the kernel will attempt to print out a stack trace  and
reboot  the  system.   If  set  to  then neither a stack trace is
printed or DDB entered ‐ it is as if DDB were not  compiled  into
the  kernel.  Note that this sets the value of the variable which
may be changed at run time — see for details.  This option  spec‐
ify  commands  which will be executed on each entry to DDB.  This
sets the default value of the variable which may  be  changed  at
run  time.  This option overrides using break to enter the kernel
debugger on the serial console.  The value  given  is  the  ASCII
value  to  be  used instead.  This is currently only supported by
the com driver.  This option overrides the string used  to  enter
the  kernel debugger.  This option adds more verbose descriptions
to the command.  Number of stack  frames  to  display  on  panic.
Useful  to avoid scrolling away the interesting frames on a glass
tty.  Default value is (all frames), useful value around Compiles
in a remote kernel debugger stub for diagnosing  kernel  problems
using the feature of gdb.  See for details.  not available on all
architectures.   Device  number  as a of kgdb device.  Memory ad‐
dress of kgdb device.  Permissions of kgdb device.   Device  name
of kgdb device.  Baud rate of kgdb device.  The flag causes to be
built  in  addition to is useful for debugging kernel crash dumps
with gdb.  See for details.  Turns on miscellaneous kernel debug‐
ging.  Since options are turned into  preprocessor  defines  (see
above),  is equivalent to doing a throughout the kernel.  Much of
the kernel has conditionalized debugging code.   Note  that  many
parts  of the kernel (typically device drivers) include their own
conditionals instead.  This option also turns  on  certain  other
options,  which  may  decrease  system performance.  Systems with
this option are not suitable for regular use,  and  are  intended
only  for debugging or looking for bugs.  Adds code to the kernel
that does internal consistency checks.  This code will cause  the
kernel  to panic if corruption of internal data structures is de‐
tected.  Historically,  the  performance  degradation  is  suffi‐
ciently  small  that  it  is reasonable for systems with to be in
production use, with the real consideration not being performance
but instead a preference for more panics versus continued  opera‐
tion with undetected problems.  Adds code to the kernel to detect
incorrect  use  of locking primitives (mutex, rwlock).  This code
will cause the kernel to check for dead lock conditions.  It will
also check for memory being freed to not contain initialised lock
primitives.  Functions for use in to check lock chains  etc.  are
also  enabled.   These checks are very expensive and can decrease
performance on multi‐processor machines by  a  factor  of  three.
Adds hooks for the DTrace tracing facility, which allows users to
analyze many aspects of system and application behavior.  See for
details.  Check kernel stack usage and panic if stack overflow is
detected.   This  check is performance sensitive because it scans
stack on each context switch.  Add  hooks  for  the  system  call
tracing facility, which allows users to watch the system call in‐
vocation  behavior  of  processes.  See for details.  This option
sets the size of the kernel message buffer in bytes.  This buffer
holds the kernel output of when not (yet) read by This is partic‐
ularly useful when the system has crashed and you wish to  lookup
the  kernel  output  from just before the crash.  Also, since the
autoconfig output becomes more and  more  verbose,  it  sometimes
happens that the message buffer overflows before was able to read
it.   Note  that not all systems are capable of obtaining a vari‐
able sized message buffer.  There are also some systems on  which
memory  contents  are  not preserved across reboots.  Enables the
kernel history logs, which create  in‐memory  traces  of  various
kernel  activities.   These  logs  can be displayed by using from
DDB.  See the kernel source file  and  the  manual  for  details.
Prints  the  kernel history logs on the system console as entries
are added.  Note that the output is voluminous, so this option is
really only useful for debugging the very earliest parts of  ker‐
nel initialization.  Like it enables the UVM history logs.  These
logs  can  be displayed by using from DDB.  See the kernel source
file for details.  Like it prints the UVM  history  logs  on  the
system console as entries are added.  Note that the output is vo‐
luminous,  so this option is really only useful for debugging the
very earliest parts of kernel initialization.  Set  the  size  of
the  kernel  history.   The  default is 100.  This option depends
upon the option.  Set the size of the kernel  history.   The  de‐
fault  is 100.  This option depends upon the option.  Like it en‐
ables the BIO history logs.  These logs can be displayed by using
from DDB, and can help in debugging problems  with  Buffered  I/O
operations.   See  the  kernel  source file for details.  Like it
prints the BIO history logs on the system console as entries  are
added.  Note that the output is voluminous, so this option is re‐
ally  only useful for debugging the very earliest parts of kernel
initialization.  Set the size of the kernel history.  The default
is 500.  This option depends upon the option.  Includes code  im‐
plementing  the Berkeley Fast File System Most machines need this
if they are not running diskless.  Includes code implementing the
Second Extended File System revision 0 and revision  1  with  the
and  options.   This is the most commonly used file system on the
Linux operating system, and is provided here  for  compatibility.
Some  of  the  specific features of like the "behavior on errors"
are not implemented.  See for details.   Include  the  Log‐struc‐
tured  File  System See and for details.  Include the Memory File
System This file system stores files  in  swappable  memory,  and
produces  notable performance improvements when it is used as the
file store for and similar file systems.  See for  details.   In‐
clude  the  client  side  of the Network File System (NFS) remote
file sharing protocol.  Although the bulk of the code  implement‐
ing  NFS  is  kernel based, several user level daemons are needed
for it to work.  See for details.  Includes code for the ISO 9660
+ Rock Ridge file system, which is the standard  file  system  on
many  CD‐ROM discs.  Useful primarily if you have a CD‐ROM drive.
See for details.  Includes the MS‐DOS FAT file system,  which  is
reportedly  still  used  by unfortunate people who have not heard
about Also implements the Windows  95  extensions  to  the  same,
which  permit  the use of longer, mixed case file names.  See and
for details.  Includes code for the  Microsoft  Windows  NT  file
system.   See for details.  Includes code for a file system, con‐
ventionally mounted on which permits access  to  the  per‐process
file  descriptor space via special files in the file system.  See
for details.  Note that this facility is redundant, and thus  un‐
needed  on  most  systems, since the pseudo‐device driver already
provides identical functionality.  On most systems, instances  of
are  mknoded  under  and  on  and Includes code which permits the
mounting of a special file system (normally mounted on  in  which
files representing various kernel variables and parameters may be
found.   See for details.  Includes code for a loopback file sys‐
tem.  This permits portions of  the  file  hierarchy  to  be  re‐
mounted  in  other  places.  The code really exists to provide an
example of a stackable file system layer.  See for details.   In‐
cludes  code  for a file system filter.  This permits the overlay
file system to intercept all access to an underlying file system.
This file system is intended to serve as an example of a stacking
file system which has a need to interpose itself between  an  un‐
derlying file system and all other access.  See for details.  In‐
cludes  code for a special file system (conventionally mounted on
in which the process space becomes visible in  the  file  system.
Among other things, the memory spaces of processes running on the
system are visible as files, and signals may be sent to processes
by  writing  to  files in the procfs namespace.  See for details.
Includes code for the UDF file system commonly found  on  CD  and
DVD  media  but  also on USB sticks.  Currently supports read and
write access up to UDF 2.01 and somewhat  limited  write  support
for  UDF  2.50.   It is marked experimental since there is no See
for details.  Includes a loopback file system in which  user  and
group  IDs  may  be  remapped  — this can be useful when mounting
alien file systems with different UIDs and GIDs  than  the  local
system.   See for details.  Includes code for the union file sys‐
tem, which permits directories to be mounted on top of each other
in such a way that both file systems remain visible —  this  per‐
mits  tricks like allowing writing (and the deleting of files) on
a read‐only file  system  like  a  CD‐ROM  by  mounting  a  local
writable  file  system  on top of the read‐only file system.  See
for details.  Includes code for the Coda file system.  Coda is  a
distributed  file  system  like NFS and AFS.  It is freely avail‐
able, like NFS, but it functions much like AFS in  being  a  file
system.   Both  Coda and AFS cache files on your local machine to
improve performance.  Then Coda goes a step further than  AFS  by
letting  you  access  the cached files when there is no available
network, viz. disconnected laptops and network outages.  In Coda,
both the client and server are outside  the  kernel  which  makes
them  easier  to  experiment with.  Coda is available for several
UNIX and non‐UNIX platforms.  See for  more  details.   You  also
need to enable the pseudo‐device, vcoda, for the Coda file system
to  work.   Includes  code  for  a  special file system (normally
mounted on in which pseudo‐terminal slave devices become  visible
in  the file system.  See for details.  Includes code for the ef‐
ficient memory file system, normally used over See  for  details.
Includes  kernel support for the pass‐to‐userspace framework file
system.  It can be used to implement file system functionality in
userspace.  See for  more  details.   This  enables  for  example
sshfs:  Enable  support.   This  allows  a  system to recognize a
disklabel written in the other byte order.  For writing,  when  a
label  already exists, its byte order is preserved.  Otherwise, a
new label is written in the native byte order.   To  specify  the
byte  order explicitly, the option of should be used with the op‐
tion in order to avoid using which results in the default  behav‐
ior  explained above.  At the moment this option is restricted to
the following ports: amd64, bebox, emips, epoc32,  evbarm,  i386,
ibmnws,  landisk, mvmeppc, prep, rs6000, sandpoint, xen, and zau‐
rus; also to machines of the evbmips and evbppc ports  that  sup‐
port  Master Boot Record (MBR).  Enables the expansion of special
strings beginning with when traversing symbolic links.  See for a
list of supported strings.  Note that this option  only  controls
the enabling of this feature by the kernel at boot‐up.  This fea‐
ture  can still be manipulated with the command regardless of the
setting of this option.  Include the server side of the  (Network
File  System) remote file sharing protocol.  Although the bulk of
the code implementing is kernel based, several user level daemons
are needed for it to work.  See and  for  details.   This  option
sets  the size of the cache used by the name‐to‐inode translation
routines, (a.k.a. the cache, though called by many other names in
the kernel source).  By default, this cache  has  entries  (NPROC
set  as  20 + 16 * MAXUSERS and NTEXT as 80 + NPROC / 8).  A rea‐
sonable way to derive a value of should you notice a large number
of namei cache misses with a tool such as is to examine your sys‐
tem’s current computed value with  (which  calls  this  parameter
"kern.maxvnodes")  and  to  increase  this value until either the
namei cache hit rate improves or it is determined that your  sys‐
tem  does  not benefit substantially from an increase in the size
of the namei cache.  Causes the namei cache to always enter a re‐
verse mapping (vnode ‐> name) as well as a normal one.  Normally,
this is already done for directory vnodes, to speed up the getcwd
operation.  This option will cause longer hash chains in the  re‐
verse  cache,  and  thus  slow down getcwd somewhat.  However, it
does make vnode ‐> path translations possible in some cases.  For
now, only useful if strict emulation for Linux  binaries  is  re‐
quired.  Enable support for UFS file systems created on Mac OS X.
Enable  FFS  support.   This allows a system to mount an FFS file
system created for another architecture, at a  small  performance
cost for all FFS file systems.  See also for file system byte or‐
der status and manipulation.  Disable support for the creation of
file  system internal snapshot of FFS file systems.  Maybe useful
for install media kernels, small memory systems and embedded sys‐
tems which don’t require the snapshot  support.   Enables  kernel
support  for traditional quotas in FFS.  Traditional quotas store
the quota information in external files and require and  at  boot
time.   Traditional quotas are limited to 32‐bit sizes and are at
this point considered a legacy feature.  Enables  kernel  support
for  in‐volume quotas in FFS.  The quota information is file sys‐
tem metadata maintained by and/or WAPBL journaling.  MFS  volumes
can  also  use quotas; see for more information.  Increase lookup
performance by maintaining in‐core hash tables for large directo‐
ries.  Enable extended attribute support for UFS1  file  systems.
Enable This provides rapid file system consistency checking after
a system outage.  It also provides better general use performance
over  regular FFS.  See also Enable LFS support.  This allows (at
a small performance cost) mounting an LFS file system created for
another architecture.  Increase lookup performance by maintaining
in‐core hash tables for large directories.  This  option  changes
the  behavior  of the APPEND and IMMUTABLE flags for a file on an
file system.  Without this option, the superuser or owner of  the
file  can  set  and clear them.  With this option, only the supe‐
ruser can set them, and they can’t be cleared if the  securelevel
is greater than 0.  See also and Enable use of the BOOTP protocol
(RFCs  951  and  1048) to get configuration information if NFS is
used to mount the root file system.  See for details.  Enable use
of static values defined as and in kernel options to get configu‐
ration information if NFS is used to mount the root file  system.
Same  as  but  use the DHCP extensions to the BOOTP protocol (RFC
1541).  Specifies the string sent in the  bp_file  field  of  the
BOOTP/DHCP request packet.  Enable use of the BOOTPARAM protocol,
consisting of RARP and BOOTPARAM RPC, to get configuration infor‐
mation if NFS is used to mount the root file system.  See for de‐
tails.   Set  the  initial NFS read and write sizes for diskless‐
boot requests.  The normal default is 8Kbytes.  This option  pro‐
vides  a  way  to  lower  the  value  (e.g.,  to 1024 bytes) as a
workaround for buggy network interface cards or boot PROMs.  Once
booted, the read and write request sizes can be increased by  re‐
mounting  the  file system.  See for details.  Reduce the size of
the NFS client code by omitting code  that’s  only  required  for
NFSv3  and  NQNFS support, leaving only that code required to use
NFSv2 servers.  Use NFS over UDP instead of the default TCP,  for
mounting  root.   The following options enable alternative buffer
queue strategies.  Enable alternate  buffer  queue  strategy  for
disk I/O.  In the default strategy, outstanding disk requests are
ordered  by  sector  number  and  sent to the disk, regardless of
whether the operation is a read or write; this option gives  pri‐
ority to issuing read requests over write requests.  Although re‐
quests  may therefore be issued out of sector‐order, causing more
seeks and thus lower overall throughput, interactive  system  re‐
sponsiveness  under  heavy  disk  I/O  load  may  be improved, as
processes blocking on disk reads are serviced sooner (file writes
typically don’t cause applications to  block).   The  performance
effect  varies greatly depending on the hardware, drive firmware,
file system  configuration,  workload,  and  desired  performance
trade‐off.   Systems  using  drive  write‐cache  (most modern IDE
disks, by default) are unlikely to benefit and may  well  suffer;
such disks acknowledge writes very quickly, and optimize them in‐
ternally  according  to  physical  layout.  Giving these disks as
many requests to work with as possible  (the  standard  strategy)
will  typically produce the best results, especially if the drive
has a large cache; the drive will silently complete  writes  from
cache  as  it seeks for reads.  Disks that support a large number
of concurrent tagged requests (SCSI disks and many hardware  RAID
controllers)  expose  this  internal  scheduling  with tagged re‐
sponses, and don’t block for reads; such disks may not see a  no‐
ticeable  difference with either strategy.  However, if IDE disks
are run with write‐cache disabled for safety, writes are not  ac‐
knowledged  until actually completed, and only one request can be
outstanding; a large number of small writes in one  locality  can
keep  the  disk busy, starving reads elsewhere on the disk.  Such
systems are likely to see the most benefit from this option.  Fi‐
nally, the performance interaction of this option with  ffs  soft
dependencies can be subtle, as that mechanism can drastically al‐
ter the workload for file system metadata writes.  Enable another
buffer  queue  strategy for disk I/O, per‐priority cyclical scan.
Synonym of Support cpu microcode loading via  This  option  makes
the  RAM  disk  size  dynamically sized.  It is incompatible with
This option allows for some machine  dependent  functions  to  be
called  when  the RAM disk driver is configured.  This can result
in automatically loading a RAM disk from floppy  on  open  (among
other  things).  Forces the RAM disk to be the root device.  This
can only be overridden when the kernel is booted in the ’ask‐for‐
root’ mode.  Allocates the given number of  512  byte  blocks  as
memory  for the RAM disk, to be populated with Do not include the
interface to a userland memory disk server process.  Per default,
this option is set to 1, including the support code.  Useful  for
install  media  kernels.   This  option  sets the flags used when
booting with a memory disk as root file system.  Possible  values
include (boot in the usual fashion ‐ default value), and (boot in
single‐user mode).  Enables the framework for kernel modules (see
Enables  the autoloading of kernel modules by default.  This sets
the default value of the variable which may  be  changed  at  run
time.   Enables  verbose  debug messages of kernel modules by de‐
fault.  This sets the default value of the variable which may  be
changed  at  run  time.   Enables  the driver to also handle com‐
pressed images.  See and for more information.  Make  the  kernel
able  to  self relocate at bootstrap, so that it can run whatever
its load address is.  This is intented to be used withe the boos‐
trap command documented in to workaround UEFI bugs, and  is  only
available on amd64.  Help the kernel programmer find bugs related
to  the  interrupt  priority  level.  When or changes the current
CPU’s interrupt priority level to or  from  record  a  backtrace.
Read  for  caveats  about collecting backtraces.  This feature is
experimental, and it is only available on i386.  See Download the
root memory disk through TFTP at root mount time.   This  enables
the use of a root RAM disk without requiring it to be embedded in
the kernel using The RAM disk name is obtained using DHCP’s file‐
name parameter.  This option requires and It is incompatible with
On  ports that support it, set the system clock frequency (see to
the supplied value.  Handle with care.  Turns on in‐kernel preci‐
sion timekeeping support used by software  implementing  (Network
Time  Protocol,  RFC  1305).  The option adds an in‐kernel Phase‐
Locked Loop (PLL) for normal operation,  and  a  Frequency‐Locked
Loop (FLL) for intermittently‐connected operation.  will employ a
user‐level  PLL  when  kernel support is unavailable, but the in‐
kernel version has lower latency and more precision, and so typi‐
cally keeps much better time.  The interface to the  kernel  sup‐
port  is provided by the and system calls, which are intended for
use by and are enabled by the option.  On  systems  with  sub‐mi‐
crosecond resolution timers, or where (HZ/100000) is not an inte‐
ger,  the  option  also  enables extended‐precision arithmetic to
keep track of fractional clock ticks at  NTP  time‐format  preci‐
sion.   This  option  enables a kernel serial line discipline for
receiving time phase signals from  an  external  reference  clock
such  as a radio clock.  The option (which see) must be on if the
option is used Some reference clocks generate a Pulse Per  Second
(PPS)  signal  in  phase with their time source.  The line disci‐
pline receives this signal on either the data leads  or  the  DCD
control lead of a serial port.  uses the PPS signal to discipline
the  local  clock oscillator to a high degree of precision (typi‐
cally less than 50 microseconds in time and 0.1 ppm in accuracy).
can also generate a serial output pulse when the system  receives
a  PPS  interrupt.  This can be used to measure the system inter‐
rupt latency and thus calibrate to account for it.  Using usually
requires a gadget box to convert from TTL to RS‐232  signal  lev‐
els.   The gadget box and PPS are described in more detail in the
HTML documentation for in currently supports this option  in  and
Allows  scripts  with the setuid bit set to execute as the effec‐
tive user rather than the real user, just  like  binary  executa‐
bles.   Using  this  option  will also enable Allows execution of
scripts with the execute bit set, but not the read bit, by  open‐
ing the file and passing the file descriptor to the shell, rather
than the filename.  Execute only (non‐readable) scripts will have
set  to  What this option allows as far as security is concerned,
is the ability to safely ensure that the correct script is run by
the interpreter, as it is passed as an already  open  file.   The
kernel  (and  typically  the  hardware battery backed‐up clock on
those machines that have one) keeps time  in  (Universal  Coordi‐
nated  Time, once known as or Greenwich Mean Time) and not in the
time of the local time zone.  The option is used  on  some  ports
(such  as the i386) to tell the kernel that the hardware clock is
offset from by the specified number of minutes.   This  is  typi‐
cally used when a machine boots several operating systems and one
of  them  wants  the hardware clock to run in the local time zone
and not in e.g.  means the hardware clock is set  to  US  Eastern
Time  (300  minutes behind and not (Note: is used to initialize a
kernel variable named which is the source actually used to deter‐
mine the  clock  offset,  and  which  may  be  accessed  via  the
kern.rtc_offset sysctl variable.  See and for details.  Since the
kernel clock is initialized from the hardware clock very early in
the  boot  process,  it is not possible to meaningfully change in
system initialization scripts.  Changing this value currently may
only be done at kernel compile time or by patching the kernel and
rebooting).  Unfortunately, in  many  cases  where  the  hardware
clock  is kept in local time, it is adjusted for Daylight Savings
Time; this means that attempting to use to let coexist with  such
an  operating  system,  like  Windows, would necessitate changing
twice a year.  As such, this solution  is  imperfect.   Sets  the
soft resource limit, which specifies the maximum number of simul‐
taneous processes a user is permitted to run, for process 0; this
value  is inherited by its child processes.  It defaults to which
is currently defined to be 160.  Setting to a value less than  is
not  permitted, as this would result in a violation of the seman‐
tics of Sets the soft resource limit, which specifies the maximum
number of open file descriptors for each process; this  value  is
inherited  by  its child processes.  It defaults to which is cur‐
rently defined to be 128.  Sets the default value of  the  sysctl
variable, which indicates the maximum number of files that may be
open  in  the system.  Sets the default value of the sysctl vari‐
able, otherwise it is set to See and for details.  Enables  clip‐
ping  within the raster‐console output system.  only available on
architectures that use for  console  output.   Removes  optimized
character  writing  code  from  the raster‐console output system.
only available on architectures that use for console output.  Em‐
beds the kernel config file used to define the kernel in the ker‐
nel binary itself.  The embedded data also includes any files di‐
rectly included by the config file itself, e.g.  or The  embedded
config file can be extracted from the resulting kernel with or by
the  following  command:  strings netbsd | sed ‐n ’s/ˆ_CFG_//p’ |
unvis Similar to the above option, but includes just  the  actual
config  file,  not  any  included files.  Use slower, but smaller
socketpair(2)‐based  pipe  implementation  instead   of   default
faster,  but  bigger one.  Primarily useful for installation ker‐
nels.  Compiles in the in‐kernel  device  configuration  manager.
See  for  details.  Used with the described below to choose which
types of events are displayed.  Show system  call  entry  points.
Show system call exit points.  Show all system call requests, in‐
cluding unimplemented calls.  Show the arguments provided.  Store
a  restricted  form  of the system call debug in a kernel history
instead of printing it to the console.  This option  relies  upon
The  default  value  is  Useful for debugging system call issues,
usually in early single user bringup.  By default, writes entries
to the system console for most system call events.  Can  be  con‐
figured  with  the  option to to use the facility instead.  Count
the number of times each system call number is called.  The  val‐
ues  can be read through the sysctl interface and displayed using
not yet available on all architectures.   Count  the  time  spent
(using  in  each system call.  Using this option will also enable
Force use of even if reports false.  Useful for systems where the
cycle counter doesn’t  run  at  a  constant  rate  (e.g.  Soekris
boxes).   A  supplement to XSERVER that adds support for entering
while in X11.  Support for Required for  and  Number  of  storage
slots per file for Default is 4.  Enables and (on most ports) in‐
creases the size of In general, is used to indicate that a system
should  act as a router, and is not invoked directly.  (Note that
has no impact on protocols other than IP).  option also  compiles
IPv4 and IPv6 fast forwarding code into the kernel.  If is 1 this
enables  IP routing behavior.  If is 0 (the default), it disables
it.  The option sets this to 1 automatically.  With  this  option
enabled, the machine will forward IP datagrams destined for other
machines between its interfaces.  Note that even without this op‐
tion,  the kernel will still forward some packets (such as source
routed packets) — removing and is insufficient to stop all  rout‐
ing through a bastion host on a firewall — source routing is con‐
trolled  independently.  Note that IP forwarding may be turned on
and off independently of the setting of the  option  through  the
use  of  the sysctl variable.  If is 1, IP forwarding is on.  See
and for details.  Tells the kernel to maintain  per‐address  sta‐
tistics  on bytes sent and received over (currently) Internet and
AppleTalk addresses.  The option is not  recommended  as  it  de‐
grades  system stability.  Increases the allowed size of the net‐
work interface packet queues.  The default queue size is 50 pack‐
ets, and you do not normally need to increase it.  Includes  sup‐
port for source‐address selection policies.  See Includes support
for  IP multicast routers.  You certainly want with this.  Multi‐
cast routing is controlled by the daemon.  See  also  option  In‐
cludes  support for Protocol Independent Multicast (PIM) routing.
You need and with this.  Software using this can be found e.g. in
Includes support for the TCP/IP protocol stack.  You almost  cer‐
tainly  want  this.   See  for details.  Includes support for the
IPv6 protocol stack.  See for details.  Unlike enables  multicast
routing  code  as well.  This option requires at this moment, but
it  should  not.   The  option  sets   the   default   value   of
net.inet6.icmp6.nd6_debug  to 1, for debugging IPv6 neighbor dis‐
covery protocol handling.  See for details.  Includes support for
the IPsec protocol, using the implementation derived from relying
on to carry out cryptographic operations.  See for details.   En‐
ables  debugging  code in IPsec stack.  See for details.  The op‐
tion includes support for IPsec Network Address  Translator  tra‐
versal (NAT‐T), as described in RFCs 3947 and 3948.  This feature
might  be patent‐encumbered in some countries.  Enabled ALTQ (Al‐
ternate Queueing).  For simple rate‐limiting, use to set  up  the
interface  transmission rate.  To use queueing disciplines, their
appropriate kernel options should also be defined (documented be‐
low).  Queueing disciplines are managed by See for details.   In‐
clude  support  for ALTQ‐implemented HFSC (Hierarchical Fair Ser‐
vice Curve) module.  HFSC supports both link‐sharing and  guaran‐
teed  real‐time services.  HFSC employs a service curve based QoS
model, and its unique feature is an ability to decouple delay and
bandwidth allocation.  Requires to use the  RED  queueing  disci‐
pline  on  HFSC classes, or to use the RIO queueing discipline on
HFSC classes.  This option assumes Include support  for  ALTQ‐im‐
plemented  PRIQ  (Priority  Queueing).   PRIQ implements a simple
priority‐based queueing discipline.  A higher priority  class  is
always served first.  Requires to use the RED queueing discipline
on  HFSC  classes,  or to use the RIO queueing discipline on HFSC
classes.  This option assumes  Include  support  for  ALTQ‐imple‐
mented  WFQ (Weighted Fair Queueing).  WFQ implements a weighted‐
round robin scheduler for a set of queues.  A weight can  be  as‐
signed  to  each queue to give a different proportion of the link
capacity.  A hash function is used to map a flow to one of a  set
of  queues.   This option assumes Include support for ALTQ‐imple‐
mented FIFO queueing.  FIFOQ is a simple  drop‐tail  FIFO  (First
In,  First Out) queueing discipline.  This option assumes Include
support for ALTQ‐implemented RIO (RED with In/Out).  The original
RIO has 2 sets of RED parameters; one for in‐profile packets  and
the other for out‐of‐profile packets.  At the ingress of the net‐
work, profile meters tag packets as IN or OUT based on contracted
profiles  for  customers.  Inside the network, IN packets receive
preferential treatment by the RIO dropper.  ALTQ/RIO has  3  drop
precedence levels defined for the Assured Forwarding PHB of Diff‐
Serv  (RFC  2597).  This option assumes Include support for ALTQ‐
implemented Blue  buffer  management.   Blue  is  another  active
buffer management mechanism.  This option assumes Include support
for  ALTQ‐implemented Flowvalve.  Flowvalve is a simple implemen‐
tation of a RED penalty box that identifies and  punishes  misbe‐
having  flows.   This option requires and assumes Include support
for ALTQ‐implemented CDNR (diffserv traffic  conditioner)  packet
marking/manipulation.  Traffic conditioners are components to me‐
ter,  mark, or drop incoming packets according to some rules.  As
opposed to queueing disciplines, traffic conditioners handle  in‐
coming  packets  at an input interface.  This option assumes Dis‐
ables use of processor cycle counter to  measure  time  in  ALTQ.
This  option  should  be defined for a non‐Pentium i386 CPU which
does not have TSC, SMP (per‐CPU counters are  not  in  sync),  or
power management which affects processor cycle counter.  This op‐
tion assumes Include support for IPsec in IPv4 ALTQ.  This option
assumes  Include  support for ALTQ‐implemented JoBS (Joint Buffer
Management and Scheduling).  This option assumes Include  support
for  an  undocumented ALTQ feature that is used to map an IP flow
to an ATM VC (Virtual Circuit).  This option assumes Include sup‐
port for ALTQ‐implemented local queues.  Its practical use is un‐
defined.  Assumes Sets default value for  net.inet.ip.subnetsare‐
local  variable,  which  controls  whether non‐directly‐connected
subnets of connected networks are considered "local" for purposes
of choosing the MSS for a TCP connection.  This is mostly present
for historic reasons and completely irrelevant if you enable Path
MTU discovery.  Sets default value for net.inet.ip.hostzerobroad‐
cast variable, which controls whether the zeroth host address  of
each  connected  subnet  is  also considered a broadcast address.
Default value is "1", for compatibility with old systems; if this
is set to zero on all hosts on a subnet, you should  be  able  to
fit an extra host per subnet on the ".0" address.  This option is
the  base‐2 logarithm of the size of mbuf clusters.  The network‐
ing stack keeps network packets in a linked list,  or  chain,  of
kernel  buffer  objects called mbufs.  The system provides larger
mbuf clusters as an optimization for large  packets,  instead  of
using  long  chains for large packets.  The mbuf cluster size, or
must be a power of two, and is computed  as  two  raised  to  the
power  On systems with Ethernet network adapters, is often set to
11, giving 2048‐byte  mbuf  clusters,  large  enough  to  hold  a
1500‐byte  Ethernet frame in a single cluster.  Systems with net‐
work interfaces supporting larger frame sizes like ATM, FDDI,  or
HIPPI  may perform better with set to 12 or 13, giving mbuf clus‐
ter sizes of 4096 and 8192 bytes, respectively.  Include  support
for  the AppleTalk protocol stack.  The kernel provides provision
for the (DDP), providing SOCK_DGRAM support and  AppleTalk  rout‐
ing.   This  stack is used by the package, which adds support for
AppleTalk server services via user  libraries  and  applications.
Include  support  for  the Bluetooth protocol stack.  See for de‐
tails.  Normally, only root can bind a socket descriptor to a so‐
called TCP port, that is, a port  number  in  the  range  0‐1023.
This option eliminates those checks from the kernel.  This can be
useful  if  there is a desire to allow daemons without privileges
to bind those ports, e.g., on firewalls.  The security  tradeoffs
in doing this are subtle.  This option should only be used by ex‐
perts.  Record the last TCP packets with SO_DEBUG set, and decode
to  the  console  if is set.  Number of packets to record for De‐
faults to 100.  These options set the  max  TCP  window  size  to
other  sizes  than  the default.  The TCP window sizes can be al‐
tered via as well.  This option sets the initial TCP window  size
for  non‐local  connections,  which is used when the transmission
starts.  The default size is 1, but if  the  machine  should  act
more  aggressively,  the  initial  size  can be set to some other
value.  The initial TCP window size can be set via as well.   En‐
able MD5 TCP signatures (RFC 2385) to protect BGP sessions.  This
option,  in  conjunction with enables logging of IP packets using
IP‐Filter.  This option enables the IP‐Filter functionality to be
enabled.  This option enables older IP‐Filter binaries  to  work.
This  option sets the default policy of IP‐Filter.  If it is set,
IP‐Filter will block packets by default.  This  option  can  help
track  down  mbuf leaks.  When enabled, mbufs are tagged with the
devices and protocols using them.   This  can  significantly  de‐
crease network performance, particularly on MP systems.  This ad‐
ditional information can be viewed with Not all devices or proto‐
cols  support this option.  Disallows the creation or deletion of
nodes from the sysctl tree, as well as the assigning of  descrip‐
tions  to nodes that lack them, by any process.  These operations
are still available to  kernel  sub‐systems,  including  loadable
kernel  modules.   Prevents  processes  from  adding nodes to the
sysctl tree that make  existing  kernel  memory  areas  writable.
Sections  of  kernel  memory can still be read and new nodes that
own  their  own  data  may  still  be   writable.    Causes   the
SYSCTL_SETUP  routines to print a brief message when they are in‐
voked.  This is merely meant as an aid in determining  the  order
in which sections of the tree are created.  Prints a message each
time the function that adds nodes to the tree, is called.  Causes
the  kernel  to  include  short,  human readable descriptions for
nodes in the sysctl tree.  The descriptions can be retrieved pro‐
grammatically (see or by the sysctl binary itself  (see  The  de‐
scriptions  are meant to give an indication of the purpose and/or
effects of a given node’s value, not  replace  the  documentation
for  the  given subsystem as a whole.  Includes support for style
message queues.  See Includes support for style semaphores.   See
Sets the number of style semaphore identifiers.  The GENERIC con‐
fig file for your port will have the default.  Sets the number of
style semaphores in the system.  The GENERIC config file for your
port  will have the default.  Sets the maximum number of undo en‐
tries per process for style semaphores.  The GENERIC config  file
for  your  port  will  have the default.  Sets the number of undo
structures in the system for style semaphores.  The GENERIC  con‐
fig  file  for your port will have the default.  Includes support
for style shared memory.  See Sets the maximum  number  of  style
shared  memory  pages that are available through the system call.
Default value is 1024 on most ports.  See for the  default.   The
number  of  mbuf clusters the kernel supports.  Mbuf clusters are
MCLBYTES in size (usually 2k).  The default value  is  calculated
from the amount of physical memory.  Architectures without direct
mapping  also  limit it based on the kmem_map size, which is used
as backing store.  Some archs limit the value with See for  those
archs.   This value can be accessed via the kern.mbuf.nmbclusters
sysctl variable.  Increase this value if you get  messages.   The
upper  limit of NMBCLUSTERS.  Size of kernel VM map in PAGE_SIZE‐
sized chunks (the VM page size; this value may be read  from  the
variable  ).  This VM map is used to map the kernel malloc arena.
The kernel attempts to auto‐size this map based on the amount  of
physical  memory in the system.  Platform‐specific code may place
bounds on this computed size, which may be viewed with the  vari‐
able See for the default upper and lower bounds.  The related op‐
tions and allow the bounds to be overridden in the kernel config‐
uration  file.   These options are provided in the event the com‐
puted value is insufficient resulting in an panic.  Sets the  max
size in bytes that a socket buffer is allowed to occupy.  The de‐
fault  is 256k, but sometimes it needs to be increased, for exam‐
ple when using large TCP windows.  This option can be changed via
as well.  Sets the maximum size of kernel virtual memory that the
socket buffers are allowed to use.  The default is 16MB,  but  in
situations  where  for  example  large  TCP windows are used this
value must also be increased.  This option can be changed via  as
well.   Size  of the buffer cache as a percentage of total avail‐
able RAM.  Ignored if BUFPAGES is also specified.  Sets the  num‐
ber  of  buffer headers available, i.e., the number of open files
that may have a buffer cache entry.  Each buffer header  requires
MAXBSIZE  (machine  dependent, but usually 65536) bytes.  The de‐
fault value is machine dependent, but is  usually  equal  to  the
value  of BUFPAGES.  These options set the number of pages avail‐
able for the buffer cache.  Their default value is a machine  de‐
pendent  value,  often  calculated as between 5% and 10% of total
available RAM.  Sets the maximum size limit of  a  process’  text
segment.   See  for  the port‐specific default.  Sets the default
size limit of a process’ data segment, the value that will be re‐
turned as the soft limit for (as returned by See  for  the  port‐
specific default.  Sets the maximum size limit of a process’ data
segment,  the  value  that will be returned as the hard limit for
(as returned by See for the port‐specific default.  Sets the  de‐
fault size limit of a process’ stack segment, the value that will
be  returned  as  the  soft limit for (as returned by See for the
port‐specific default.  Sets the maximum size limit of a process’
stack segment, the value that will be returned as the hard  limit
for  (as returned by See for the port‐specific default.  Defaults
to one.  If set to zero, the kernel will not dump to the dump de‐
vice when it panics, though dumps can still be  forced  via  with
the command.  Note that this sets the value of the variable which
may  be changed at run time — see for details.  Enable paging de‐
vice/file support.  This option is on by default.  Store swap  in
plaintext,  not encrypted, which may expose secrets if the under‐
lying nonvolatile medium is disclosed.  This option is off by de‐
fault; it is available only for extremely slow machines where the
performance impact of swapping early at boot outweighs the  secu‐
rity  risks.   Swap encryption can still be turned on dynamically
with the knob.  Use CLOCK‐Pro, an alternative page  replace  pol‐
icy.  Initializes the kernel security level with -1 instead of 0.
This means that the system always starts in secure level -1 mode,
even  when  running multiuser, unless the securelevel variable is
set to value > -1 in In this case the kernel security level  will
be  raised  to  that  value  when the script is run during system
startup.  See the manual page for for details on the implications
of this.  The kernel secure level may manipulated  by  the  supe‐
ruser by altering the variable (the secure level may only be low‐
ered by a call from process ID 1, i.e., See also and Enables sup‐
port  for  SHA256 hashes in Veriexec.  Enables support for SHA384
hashes in Veriexec.  Enables support for SHA512  hashes  in  Ver‐
iexec.   Enables PaX MPROTECT, restrictions from the PaX project.
The is the default value for the knob, see  If  0,  PaX  MPROTECT
will  be  enabled  only if explicitly set on programs using If 1,
PaX MPROTECT will be enabled for all programs.  Programs  can  be
exempted using See for more details.  Enables PaX Segvguard.  Re‐
quires The is the default value for the knob, see If 0, PaX Segv‐
guard will be enabled only if explicitly set on programs using If
1,  PaX  Segvguard will be enabled to all programs, and exemption
can be done using See for more details.  Enables PaX  ASLR.   The
is the default value for the knob, see If 0, PaX ASLR will be en‐
abled  only  if  explicitly  set on programs using If 1, PaX ASLR
will be enabled to all programs, and exemption can be done  using
See  for  more details.  Sets the initial value of the flag which
controls whether user programs can map virtual  address  0.   The
flag  can  be  changed at runtime by Enables Kernel Address Sani‐
tizer.  not available on all architectures.  Enables Kernel ASLR.
This randomizes the location of the kernel image in memory.   not
available  on all architectures.  Enables Separate Virtual Space.
On architectures that are designed to function with a shared  ad‐
dress  space, this option explicitly isolates the kernel and user
spaces.  not available on all architectures.  When the bootloader
(which passes AmigaOS ROM information) claims we have a 68060 CPU
without FPU, go look into the  Processor  Configuration  Register
(PCR)  to  find  out.   You  need  this with Amiga ROMs up to (at
least) V40.xxx (OS3.1), when you  boot  via  the  bootblocks  and
don’t  have  a  DraCo.  The IOBlix boards come with two different
serial master clocks: older ones  use  24  MHz,  newer  ones  use
22.1184  MHz.   The  driver normally assumes the latter.  If your
board uses 24 MHz, you can recompile  your  kernel  with  options
IOBZCLOCK=24000000  or patch the kernel variable iobzclock to the
same value.  If there, limit the part of the  first  memory  bank
used  by  to value megabytes.  Default is unlimited.  Add special
support for Phase5 mixed 68k+PPC boards.   Currently,  this  only
affects  rebooting  from  and is only needed on 68040+PPC, not on
68060+PPC; without this, affected machines will  hang  after  has
shut down and will only restart after a keyboard reset or a power
cycle.   Include support for AHDI (native Atari) disklabels.  In‐
clude support for labels.  If you don’t set this option, it  will
be set automatically.  will not work without it.  Include support
for  the 5380‐SCSI configuration as found on the Falcon.  If set,
the kernel will relocate itself to  TT‐RAM,  if  possible.   This
will  give you a slightly faster system.  that on some TT030 sys‐
tems, the system will frequently dump with MMU‐faults  with  this
option  enabled.  Allow the modem1‐port to act as the system‐con‐
sole.  A carrier should be active on modem1 during system boot to
active  the  console  functionality.   Include  support  for  the
5380‐SCSI  configuration as found on the TT030 and Hades.  Speci‐
fies the time (in millisecond) to wait before  doing  a  hardware
reset  in  the  last  phase  of  a reboot.  This gives the user a
chance to see error messages from the shutdown  operations  (like
NFS  unmounts,  buffer  cache flush, etc ...).  Setting this to 0
will disable the delay.  Default is 2 seconds.  Include i386‐spe‐
cific system calls for modifying the local descriptor table, used
by Windows emulators.  Enable PAE  (Physical  Address  Extension)
mode.   PAE  permits  up  to 36 bits physical addressing (64GB of
physical memory), and turns physical addresses to 64  bits  enti‐
ties  in  the  memory management subsystem.  Userland virtual ad‐
dress space remains at 32 bits (4GB).  PAE mode  is  required  to
enable  the  NX/XD  (No‐eXecute/eXecute  Disable)  bit for pages,
which allows marking certain ones as not being  executable.   Any
attempt to execute code from such a page will raise an exception.
Overrides  the  base  memory  size passed in from the boot block.
(Value given in kilobytes.)  Use this option  only  if  the  boot
block  reports  the size incorrectly.  (Note that some BIOSes put
the extended BIOS data area at the top of base memory, and there‐
fore report a smaller base memory size to prevent programs  over‐
writing it.  This is correct behavior, and you should not use the
option to access this memory).  Enable GCC‐specific Spectre vari‐
ant  2 mitigations.  For 32‐bit kernels this means these options:
‐mindirect‐branch=thunk  ‐mindirect‐branch‐register  For   64‐bit
kernels  this means these options: ‐mindirect‐branch=thunk‐inline
‐mindirect‐branch‐register Overrides  the  extended  memory  size
passed  in  from the boot block.  (Value given in kilobytes.  Ex‐
tended memory does not include the first megabyte.)  Use this op‐
tion only if the boot block reports the size incorrectly.   Rele‐
vant  only  to the Cyrix 486DLC CPU.  This option is used to turn
on the cache in hold‐flush mode.  It is not turned on by  default
because  it  is known to have problems in certain motherboard im‐
plementations.  Relevant only to the Cyrix 486DLC CPU.  This  op‐
tion  is used to turn on the cache in write‐back mode.  It is not
turned on by default because it is known to have problems in cer‐
tain motherboard implementations.  In order for  this  option  to
take  effect,  option must also be specified.  Enable support for
initializing the PCI bus using information from  the  BIOS.   See
for  details.   Include support for accessing MTRR registers from
user‐space.  See Make the system speaker emit several beeps  when
it is completely safe to power down the computer after a command.
Requires  support.   Number  of times to beep the speaker when is
enabled.  Defaults to 3.  The tone frequency used when option, in
hertz.  Defaults to 1500.  The duration of each beep when is  en‐
abled,  in  milliseconds.   Defaults  to  250.   Makes the kernel
Multiboot‐compliant, allowing it to be booted  through  a  Multi‐
boot‐compliant  boot manager such as GRUB.  See for more informa‐
tion.  Display a splash screen during boot.  Options specific  to
busses.   Control the section of IO bus space used for PCMCIA bus
space mapping.  Ideally the  probed  defaults  are  satisfactory,
however  in  practice  that  is not always the case.  See for de‐
tails.  Controls the allowable interrupts that may  be  used  for
PCMCIA  devices.  This mask is a logical‐or of power‐of‐2s of al‐
lowable interrupts:
 0  0x0001    4  0x0010    8  0x0100    12  0x1000
 1  0x0002    5  0x0020    9  0x0200    13  0x2000
 2  0x0004    6  0x0040   10  0x0400    14  0x4000
 3  0x0008    7  0x0080   11  0x0800    15  0x8000 Perform a self
test of the keyboard controller before attaching it as a console.
This might be necessary on machines where we boot on  cold  iron,
and  pckbc  refuses  to  talk until we request a self test.  Cur‐
rently only the netwinder port uses it.  If this  option  is  set
the PS/2 keyboard will not be used as the console if it cannot be
found  during boot.  This allows other keyboards, like USB, to be
the console keyboard.  Sets the default keyboard layout, see  In‐
clude  support for MC68881/MC68882 emulator.  Include support for
68040 floating point.  Include support for  a  specific  CPU,  at
least  one  (the one you are using) should be specified.  Include
software support for 68060.  This provides emulation of  unimple‐
mented integer instructions as well as emulation of unimplemented
floating  point  instructions and data types and software support
for floating point traps.  Limit the amount of memory seen by the
kernel to bytes.  Specify the size  of  the  page  table  as  PTE
groups.  Normally, one PTEG is allocated per physical page frame.
Enable  simple event debugging of the logging of the device.  En‐
able blinking of LED.  Blink rate is full cycle every  N  seconds
for  N < then current load average.  See Count how many times the
sw SCSI device has left 3, 2,  1  and  0  in  the  sw_3_leftover,
sw_2_leftover,  sw_1_leftover, and sw_0_leftover variables acces‐
sible from See Adds debugging messages calls when  user‐requested
alignment  fault handling happens.  Adds debugging messages calls
for emulated floating  point  and  alignment  fixing  operations.
Adds  debugging  functions  callable  from  The debug_pagetables,
test_region and print_fe_map functions  print  information  about
page  tables  for  the  SUN4M platforms only.  Adds extra info to
Make getcontext and setcontext include floating point  registers.
Adds debugging messages to the device.  Use the entire screen for
the  console.   Use the Fixed font on the console, instead of the
normal font.  Support sun4 class machines.  Support  sun4c  class
machines.  Support sun4m class machines.  Enable support for sun4
3‐level MMU machines.  Enable SPARC V9 assembler in Enable simple
event debugging of the logging of the device.  Enable blinking of
LED.   Blink rate is full cycle every N seconds for N < then cur‐
rent load average.  See  Include  support  for  extended  memory,
e.g.,  TS‐6BE16  and  060turbo  on‐board.   Include  support  for
Jupiter‐X MPU accelerator Use the built‐in  serial  port  as  the
system‐console.   Speed  is  specified  in bps, defaults to 9600.
Set the kernel message attribute for ITE.  Value, an integer,  is
a  logical  or of the following values: color inversed underlined
bolded Disable support for MSI/MSIX in the kernel.  See  for  de‐
tails  of  MSI/MSIX  support Disables support in the kernel.  The
man page first appeared in
