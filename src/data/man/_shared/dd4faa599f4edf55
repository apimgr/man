examines  a  set  of  file descriptors to see if some of them are
ready for I/O or if certain events have occurred  on  them.   The
argument  is  a  pointer to an array of pollfd structures, as de‐
fined in (shown below).  The argument specifies the size  of  the
array.  struct pollfd {
    int    fd;       /* file descriptor */
    short  events;   /* events to look for */
    short   revents;   /* events returned */ }; The fields of are
as follows: File descriptor to poll.  Events to poll  for.   (See
below.)   Events  which may occur or have occurred.  (See below.)
The event bitmasks in and have the following bits: An exceptional
condition has occurred on the device or  socket.   This  flag  is
output  only,  and  ignored if present in the input bitmask.  The
device or socket has been  disconnected.   This  flag  is  output
only,  and  ignored  if  present in the input bitmask.  Note that
POLLHUP and POLLOUT are mutually exclusive and  should  never  be
present  in  the  bitmask at the same time.  Data other than high
priority data may be read without blocking.  This  is  equivalent
to ( POLLRDNORM | POLLRDBAND ).  The file descriptor is not open.
This  flag  is  output  only, and ignored if present in the input
bitmask.  Normal data may be written without blocking.   This  is
equivalent to POLLWRNORM.  High priority data may be read without
blocking.   Priority  data  may be read without blocking.  Normal
data may be read without blocking.  Priority data may be  written
without  blocking.   Normal data may be written without blocking.
The distinction between normal, priority, and high‐priority  data
is  specific  to particular file types or devices.  If is greater
than zero, it specifies a maximum interval (in  milliseconds)  to
wait  for  any file descriptor to become ready.  If is zero, then
will return without blocking. If the value of  is  ‐1,  the  poll
blocks  indefinitely.  returns the number of descriptors that are
ready for I/O, or ‐1 if an error occurred.  If the time limit ex‐
pires, returns 0.  If returns with an error, including one due to
an interrupted call, the array will be unmodified and the  global
variable  will be set to indicate the error.  will fail if: Allo‐
cation of internal data structures fails.  A  subsequent  request
may  succeed.   points  outside  the  process’s allocated address
space.  A signal is delivered before the time limit  expires  and
before  any  of  the  selected  events  occurs.   The argument is
greater than OPEN_MAX or the argument is less than ‐1.  The  sys‐
tem  call  currently does not support devices.  The function call
appeared in
