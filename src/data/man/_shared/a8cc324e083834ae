_s_c_h_e_d___s_e_t_s_c_h_e_d_u_l_e_r(2)         System Calls Manual        _s_c_h_e_d___s_e_t_s_c_h_e_d_u_l_e_r(2)

NNAAMMEE
       sched_setscheduler,  sched_getscheduler  -  set and get scheduling pol‐
       icy/parameters

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<sscchheedd..hh>>

       iinntt sscchheedd__sseettsscchheedduulleerr((ppiidd__tt _p_i_d,, iinntt _p_o_l_i_c_y,,
                              ccoonnsstt ssttrruucctt sscchheedd__ppaarraamm **_p_a_r_a_m));;
       iinntt sscchheedd__ggeettsscchheedduulleerr((ppiidd__tt _p_i_d));;

DDEESSCCRRIIPPTTIIOONN
       The sscchheedd__sseettsscchheedduulleerr() system call sets both  the  scheduling  policy
       and  parameters  for  the  thread whose ID is specified in _p_i_d.  If _p_i_d
       equals zero, the scheduling policy and parameters of the calling thread
       will be set.

       The scheduling parameters are specified in the _p_a_r_a_m argument, which is
       a pointer to a structure of the following form:

           struct sched_param {
               ...
               int sched_priority;
               ...
           };

       In the current implementation, the structure contains only  one  field,
       _s_c_h_e_d___p_r_i_o_r_i_t_y.   The  interpretation  of _p_a_r_a_m depends on the selected
       policy.

       Currently, Linux supports the following "normal" (i.e.,  non‐real‐time)
       scheduling policies as values that may be specified in _p_o_l_i_c_y:

       SSCCHHEEDD__OOTTHHEERR   the standard round‐robin time‐sharing policy;

       SSCCHHEEDD__BBAATTCCHH   for "batch" style execution of processes; and

       SSCCHHEEDD__IIDDLLEE    for running _v_e_r_y low priority background jobs.

       For each of the above policies, _p_a_r_a_m_-_>_s_c_h_e_d___p_r_i_o_r_i_t_y must be 0.

       Various "real‐time" policies are also supported, for special time‐crit‐
       ical  applications  that  need  precise  control  over the way in which
       runnable threads are selected for execution.  For the  rules  governing
       when  a  process  may  use these policies, see sscchheedd(7).  The real‐time
       policies that may be specified in _p_o_l_i_c_y are:

       SSCCHHEEDD__FFIIFFOO    a first‐in, first‐out policy; and

       SSCCHHEEDD__RRRR      a round‐robin policy.

       For each of  the  above  policies,  _p_a_r_a_m_-_>_s_c_h_e_d___p_r_i_o_r_i_t_y  specifies  a
       scheduling  priority for the thread.  This is a number in the range re‐
       turned  by  calling  sscchheedd__ggeett__pprriioorriittyy__mmiinn(2)   and   sscchheedd__ggeett__pprriioorr‐‐
       iittyy__mmaaxx(2) with the specified _p_o_l_i_c_y.  On Linux, these system calls re‐
       turn, respectively, 1 and 99.

       Since  Linux 2.6.32, the SSCCHHEEDD__RREESSEETT__OONN__FFOORRKK flag can be ORed in _p_o_l_i_c_y
       when calling sscchheedd__sseettsscchheedduulleerr().  As a result of including this flag,
       children created by ffoorrkk(2) do not inherit privileged scheduling  poli‐
       cies.  See sscchheedd(7) for details.

       sscchheedd__ggeettsscchheedduulleerr()  returns  the  current  scheduling  policy  of the
       thread identified by _p_i_d.  If _p_i_d equals zero, the policy of the  call‐
       ing thread will be retrieved.

RREETTUURRNN VVAALLUUEE
       On   success,   sscchheedd__sseettsscchheedduulleerr()   returns   zero.    On   success,
       sscchheedd__ggeettsscchheedduulleerr() returns the policy for the thread  (a  nonnegative
       integer).  On error, both calls return -1, and _e_r_r_n_o is set to indicate
       the error.

EERRRROORRSS
       EEIINNVVAALL Invalid arguments: _p_i_d is negative or _p_a_r_a_m is NULL.

       EEIINNVVAALL (sscchheedd__sseettsscchheedduulleerr()) _p_o_l_i_c_y is not one of the recognized poli‐
              cies.

       EEIINNVVAALL (sscchheedd__sseettsscchheedduulleerr())  _p_a_r_a_m does not make sense for the speci‐
              fied _p_o_l_i_c_y.

       EEPPEERRMM  The calling thread does not have appropriate privileges.

       EESSRRCCHH  The thread whose ID is _p_i_d could not be found.

VVEERRSSIIOONNSS
       POSIX.1 does not detail the permissions that an unprivileged thread re‐
       quires in order to call sscchheedd__sseettsscchheedduulleerr(), and details  vary  across
       systems.   For example, the Solaris 7 manual page says that the real or
       effective user ID of the caller must match the real user ID or the save
       set‐user‐ID of the target.

       The scheduling policy and parameters are in fact per‐thread  attributes
       on Linux.  The value returned from a call to ggeettttiidd(2) can be passed in
       the  argument  _p_i_d.  Specifying _p_i_d as 0 will operate on the attributes
       of the calling thread, and passing the value returned from  a  call  to
       ggeettppiidd(2)  will  operate  on  the  attributes of the main thread of the
       thread group.  (If you are  using  the  POSIX  threads  API,  then  use
       pptthhrreeaadd__sseettsscchheeddppaarraamm(3),         pptthhrreeaadd__ggeettsscchheeddppaarraamm(3),         and
       pptthhrreeaadd__sseettsscchheeddpprriioo(3), instead of the sscchheedd__**(2) system calls.)

SSTTAANNDDAARRDDSS
       POSIX.1‐2024 (but see BUGS below).

       SSCCHHEEDD__BBAATTCCHH and SSCCHHEEDD__IIDDLLEE are Linux‐specific.

HHIISSTTOORRYY
       POSIX.1‐2001.

NNOOTTEESS
       Further details of the semantics of  all  of  the  above  "normal"  and
       "real‐time"  scheduling  policies  can  be found in the sscchheedd(7) manual
       page.  That page also describes an additional  policy,  SSCCHHEEDD__DDEEAADDLLIINNEE,
       which is settable only via sscchheedd__sseettaattttrr(2).

       POSIX  systems  on  which sscchheedd__sseettsscchheedduulleerr() and sscchheedd__ggeettsscchheedduulleerr()
       are available define __PPOOSSIIXX__PPRRIIOORRIITTYY__SSCCHHEEDDUULLIINNGG in _<_u_n_i_s_t_d_._h_>.

BBUUGGSS
       POSIX.1 says that on success, sscchheedd__sseettsscchheedduulleerr()  should  return  the
       previous  scheduling  policy.  Linux sscchheedd__sseettsscchheedduulleerr() does not con‐
       form to this requirement, since it always returns 0 on success.

SSEEEE AALLSSOO
       cchhrrtt(1), nniiccee(2), sscchheedd__ggeett__pprriioorriittyy__mmaaxx(2), sscchheedd__ggeett__pprriioorriittyy__mmiinn(2),
       sscchheedd__ggeettaaffffiinniittyy(2), sscchheedd__ggeettaattttrr(2), sscchheedd__ggeettppaarraamm(2),
       sscchheedd__rrrr__ggeett__iinntteerrvvaall(2), sscchheedd__sseettaaffffiinniittyy(2), sscchheedd__sseettaattttrr(2),
       sscchheedd__sseettppaarraamm(2), sscchheedd__yyiieelldd(2), sseettpprriioorriittyy(2), ccaappaabbiilliittiieess(7),
       ccppuusseett(7), sscchheedd(7)

Linux man‐pages (unreleased)      2025‐10‐29             _s_c_h_e_d___s_e_t_s_c_h_e_d_u_l_e_r(2)
