The  program  is the last stage of the boot process.  It normally
begins multi‐user operation.  is executed  automatically  by  the
kernel,  after the kernel has initialised all devices and mounted
the root file system.  The kernel may try multiple possible paths
for including and The following table describes the state machine
used by Single user shell.  If the kernel  is  booted  in  single
user mode (see then the kernel will pass the option to to prevent
the  system from going multi‐user and to instead execute a single
user shell without starting the normal daemons.  If the kernel is
in a secure mode, will downgrade it to  securelevel  0  (insecure
mode).  The system is then quiescent for maintenance work and may
later  be  made to go to state 2 (multi‐user) by exiting the sin‐
gle‐user shell (with ˆD).  Multi‐user boot  (default  operation).
Executes  (see If this was the first state entered (as opposed to
entering here after state 1), then will be invoked with its first
argument being If exits with a non‐zero (error) exit  code,  com‐
mence  single  user operation by giving the super‐user a shell on
the console by going to state 1 (single user).   Otherwise,  pro‐
ceed  to state 3.  If value of the sysctl node is not equal to at
this point, the process will be run inside a indicated by  sysctl
with  the same error handling as above.  If the administrator has
not set the security level to -1  to  indicate  that  the  kernel
should  not  run multiuser in secure mode, and the script has not
set a higher level of security than level 1, then  will  put  the
kernel  into  securelevel  mode 1.  See and for more information.
Set up ttys as specified in See below for more  information.   On
completion,  continue  to  state 4.  If we did chroot in state 2,
each process will be run in the same path as in 2 (that  is,  the
value  of sysctl is not re‐read).  Multi‐user operation.  Depend‐
ing upon the signal received, change state appropriately;  on  go
to  state  7; on go to state 5; on go to state 6.  Clean‐up mode;
re‐read killing off the controlling processes on lines  that  are
now  and  starting  processes that are newly On completion, go to
state 4.  mode; no new sessions.  Signals as per state 4.   Shut‐
down mode.  Send to all controlling processes, reap the processes
for  30 seconds, and then go to state 1 (single user); warning if
not all the processes died.  If the entry in the file  is  marked
then  will  require that the superuser password be entered before
the system will start a single‐user shell.  The password check is
skipped if the is marked as It should be noted that while has the
ability to start multi‐user operation inside a  environment,  the
process itself will always run in the This also implies that sin‐
gle‐user  mode is always started in the original root, giving the
possibility to create multi‐user sessions in different  root  di‐
rectories over time.  The sysctl node is fabricated by at startup
and  re‐created  any time it is found to be missing.  Type of the
node is string capable of holding full pathname, and is only  ac‐
cessible  by  the  superuser (unless explicitly destroyed and re‐
created with different specification).  In multi‐user  operation,
maintains  processes  for  the  terminal  ports found in the file
reads this file, and executes the command  found  in  the  second
field.   This command is usually it opens and initializes the tty
line and executes the program.  The program, when  a  valid  user
logs  in,  executes a shell for that user.  When this shell dies,
either because the user logged out or an abnormal termination oc‐
curred (a signal), the program wakes up, deletes  the  user  from
the  and files of current users and records the logout in the and
files.  The cycle is then restarted by executing a  new  for  the
line.   Line  status  (on, off, secure, getty, or window informa‐
tion) may be changed in the file without a reboot by sending  the
signal  to with the command This is referenced in the table above
as state 5.  On receipt of this signal, re‐reads the file.   When
a  line  is  turned  off in will send a signal to the controlling
process for the session associated with the line.  For any  lines
that  were previously turned off in the file and are now on, exe‐
cutes a new to enable a new login.  If the getty or window  field
for  a line is changed, the change takes effect at the end of the
current login session (e.g., the next time starts  a  process  on
the  line).   If a line is commented out or deleted from will not
do anything at all to that line.  However, it will complain  that
the  relationship  between  lines  in the file and records in the
file is out of sync, so this practice is not  recommended.   will
terminate  multi‐user  operations  and resume single‐user mode if
sent a terminate signal, for example, If there are processes out‐
standing that are deadlocked (because  of  hardware  or  software
failure),  will  not  wait  for them all to die (which might take
forever), but will time out after 30 seconds and print a  warning
message.   will cease creating new and allow the system to slowly
die away, if it is sent a terminal stop  signal,  i.e.,  A  later
hangup  will  resume  full  multi‐user operations, or a terminate
will start a single user shell.  This hook is  used  by  and  The
role  of  is  so critical that if it dies, the system will reboot
itself automatically.  If, at bootstrap time, the process  cannot
be  located,  or exits during its initialisation, the system will
panic with the message If does not exist, will cd to and run will
use or to create a memory file system mounted over that  contains
the  standard  devices  considered  necessary to boot the system.
System console device.  Terminal ports found in Record of current
users on the system.  Record of all logins and logouts.  The ter‐
minal initialization information file.  System startup  commands.
A process being started to service a line is exiting quickly each
time  it  is started.  This is often caused by a ringing or noisy
terminal line.  A process is hung and could not  be  killed  when
the  system  was shutting down.  This condition is usually caused
by a process that is stuck in a device driver because of  a  per‐
sistent device error condition.  An command appeared in
