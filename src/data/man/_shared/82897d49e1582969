MAC_PREPARE(3)           BSD Library Functions Manual           MAC_PREPARE(3)

NNAAMMEE
     mmaacc__pprreeppaarree, mmaacc__pprreeppaarree__ttyyppee, mmaacc__pprreeppaarree__ffiillee__llaabbeell,
     mmaacc__pprreeppaarree__iiffnneett__llaabbeell, mmaacc__pprreeppaarree__pprroocceessss__llaabbeell — allocate appropriate
     storage for _m_a_c___t

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//mmaacc..hh>>

     _i_n_t
     mmaacc__pprreeppaarree(_m_a_c___t _*_m_a_c, _c_o_n_s_t _c_h_a_r _*_e_l_e_m_e_n_t_s);

     _i_n_t
     mmaacc__pprreeppaarree__ttyyppee(_m_a_c___t _*_m_a_c, _c_o_n_s_t _c_h_a_r _*_n_a_m_e);

     _i_n_t
     mmaacc__pprreeppaarree__ffiillee__llaabbeell(_m_a_c___t _*_m_a_c);

     _i_n_t
     mmaacc__pprreeppaarree__iiffnneett__llaabbeell(_m_a_c___t _*_m_a_c);

     _i_n_t
     mmaacc__pprreeppaarree__pprroocceessss__llaabbeell(_m_a_c___t _*_m_a_c);

DDEESSCCRRIIPPTTIIOONN
     The mmaacc__pprreeppaarree family of functions allocates the appropriate amount of
     storage and initializes _*_m_a_c for use by mac_get(3).  When the resulting
     label is passed into the mac_get(3) functions, the kernel will attempt to
     fill in the label elements specified when the label was prepared.  Ele‐
     ments are specified in a nul-terminated string, using commas to delimit
     fields.  Element names may be prefixed with the ‘?’ character to indicate
     that a failure by the kernel to retrieve that element should not be con‐
     sidered fatal.

     The mmaacc__pprreeppaarree() function accepts a list of policy names as a parameter,
     and allocates the storage to fit those label elements accordingly.  The
     remaining functions in the family make use of system defaults defined in
     mac.conf(5) instead of an explicit _e_l_e_m_e_n_t_s argument, deriving the de‐
     fault from the specified object type.

     mmaacc__pprreeppaarree__ttyyppee() allocates the storage to fit an object label of the
     type specified by the _n_a_m_e argument.  The mmaacc__pprreeppaarree__ffiillee__llaabbeell(),
     mmaacc__pprreeppaarree__iiffnneett__llaabbeell(), and mmaacc__pprreeppaarree__pprroocceessss__llaabbeell() functions are
     equivalent to invocations of mmaacc__pprreeppaarree__ttyyppee() with arguments of "file",
     "ifnet", and "process" respectively.

RREETTUURRNN VVAALLUUEESS
     Upon successful completion, the value 0 is returned; otherwise the
     value -1 is returned and the global variable _e_r_r_n_o is set to indicate the
     error.

SSEEEE AALLSSOO
     mac(3), mac_free(3), mac_get(3), mac_is_present(3), mac_set(3), mac(4),
     mac.conf(5), maclabel(7)

SSTTAANNDDAARRDDSS
     POSIX.1e is described in IEEE POSIX.1e draft 17.

HHIISSTTOORRYY
     Support for Mandatory Access Control was introduced in FreeBSD 5.0 as
     part of the TrustedBSD Project.  Support for generic object types first
     appeared in FreeBSD 5.2.

BSD                           September 21, 2023                           BSD
