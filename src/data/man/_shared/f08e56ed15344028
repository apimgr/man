curs_initscr(3X)                 Library calls                curs_initscr(3X)



NNAAMMEE
       iinniittssccrr,  nneewwtteerrmm,  eennddwwiinn, iisseennddwwiinn, sseett__tteerrmm, ddeellssccrreeeenn - initialize,
       manipulate, or tear down _c_u_r_s_e_s terminal interface

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ccuurrsseess..hh>>

       WWIINNDDOOWW **iinniittssccrr((vvooiidd));;
       iinntt eennddwwiinn((vvooiidd));;

       bbooooll iisseennddwwiinn((vvooiidd));;

       SSCCRREEEENN **nneewwtteerrmm((ccoonnsstt cchhaarr **_t_y_p_e,, FFIILLEE **_o_u_t_f,, FFIILLEE **_i_n_f));;
       SSCCRREEEENN **sseett__tteerrmm((SSCCRREEEENN **_n_e_w));;
       vvooiidd ddeellssccrreeeenn((SSCCRREEEENN** _s_p));;

DDEESSCCRRIIPPTTIIOONN
   iinniittssccrr
       iinniittssccrr is normally the first ccuurrsseess routine to call when  initializing
       a  program.   A few special routines sometimes need to be called before
       it; these are ssllkk__iinniitt(3X), ffiilltteerr, rriippoofffflliinnee, uussee__eennvv.  For multiple-
       terminal applications, nneewwtteerrmm may be called before iinniittssccrr.

       The  initscr  code  determines  the  terminal  type and initializes all
       ccuurrsseess data structures.  iinniittssccrr also causes  the  first  call  to  rree‐‐
       ffrreesshh(3X)  to clear the screen.  If errors occur, iinniittssccrr writes an ap‐
       propriate error message to  standard  error  and  exits;  otherwise,  a
       pointer is returned to ssttddssccrr.

   nneewwtteerrmm
       A program that outputs to more than one terminal should use the nneewwtteerrmm
       routine for each terminal instead of iinniittssccrr.  A program that needs  to
       inspect capabilities, so it can continue to run in a line-oriented mode
       if the terminal cannot support a screen-oriented  program,  would  also
       use nneewwtteerrmm.

       The  routine  nneewwtteerrmm  should be called once for each terminal.  It re‐
       turns a variable of type _S_C_R_E_E_N _* which should be saved as a  reference
       to that terminal.  nneewwtteerrmm's arguments are

       •   the _t_y_p_e of the terminal to be used in place of $$TTEERRMM,

       •   an output stream connected to the terminal, and

       •   an input stream connected to the terminal

       If the _t_y_p_e parameter is NNUULLLL, $$TTEERRMM will be used.

       The  file  descriptor  of the output stream is passed to sseettuupptteerrmm(3X),
       which returns a pointer to  a  _T_E_R_M_I_N_A_L  structure.   nneewwtteerrmm's  return
       value holds a pointer to the _T_E_R_M_I_N_A_L structure.

   eennddwwiinn
       The  program  must also call eennddwwiinn for each terminal being used before
       exiting from ccuurrsseess.  If nneewwtteerrmm is called more than once for the  same
       terminal, the first terminal referred to must be the last one for which
       eennddwwiinn is called.

       A program should always call eennddwwiinn before  exiting  or  escaping  from
       ccuurrsseess mode temporarily.  This routine

       •   resets colors to correspond with the default color pair 0,

       •   moves the cursor to the lower left-hand corner of the screen,

       •   clears  the  remainder of the line so that it uses the default col‐
           ors,

       •   sets the cursor to normal visibility (see ccuurrss__sseett(3X)),

       •   stops cursor-addressing mode using the _e_x_i_t___c_a___m_o_d_e terminal  capa‐
           bility,

       •   restores tty modes (see rreesseett__sshheellll__mmooddee(3X)).

       Calling rreeffrreesshh(3X) or ddoouuppddaattee(3X) after a temporary escape causes the
       program to resume visual mode.

   iisseennddwwiinn
       The iisseennddwwiinn routine returns TTRRUUEE if eennddwwiinn has been called without any
       subsequent calls to wwrreeffrreesshh, and FFAALLSSEE otherwise.

   sseett__tteerrmm
       The  sseett__tteerrmm  routine  is  used to switch between different terminals.
       The screen reference _n_e_w becomes the new current terminal.  The  previ‐
       ous  terminal  is  returned  by  the routine.  This is the only routine
       which manipulates _S_C_R_E_E_N pointers; all other routines affect  only  the
       current terminal.

   ddeellssccrreeeenn
       The  ddeellssccrreeeenn  routine  frees  storage associated with the _S_C_R_E_E_N data
       structure.  The eennddwwiinn routine does not do this, so ddeellssccrreeeenn should be
       called after eennddwwiinn if a particular _S_C_R_E_E_N is no longer needed.

RREETTUURRNN VVAALLUUEE
       eennddwwiinn returns the integer EERRRR upon failure and OOKK upon successful com‐
       pletion.

       Routines that return pointers always return NNUULLLL on error.

       X/Open defines no error conditions.  In this implementation

       •   eennddwwiinn returns an error if

           •   the terminal was not initialized, or

           •   eennddwwiinn is called more than once without updating the screen, or

           •   rreesseett__sshheellll__mmooddee(3X) returns an error.

       •   nneewwtteerrmm returns an error if it cannot allocate the data  structures
           for  the  screen,  or  for the top-level windows within the screen,
           i.e., ccuurrssccrr, nneewwssccrr, or ssttddssccrr.

       •   sseett__tteerrmm returns no error.

PPOORRTTAABBIILLIITTYY
       These functions were described in X/Open Curses, Issue 4.  As of  2015,
       the current document is X/Open Curses, Issue 7.

   DDiiffffeerreenncceess
       X/Open  Curses  specifies  that  portable  applications  must  not call
       iinniittssccrr more than once:

       •   The portable way to use iinniittssccrr is once only, using rreeffrreesshh(3X)  to
           restore the screen after eennddwwiinn.

       •   This implementation allows using iinniittssccrr after eennddwwiinn.

       Old versions of curses, e.g., BSD 4.4, would return a null pointer from
       iinniittssccrr when an error is detected, rather than exiting.  It is safe but
       redundant to check the return value of iinniittssccrr in X/Open Curses.

       Calling  eennddwwiinn  does not dispose of the memory allocated in iinniittssccrr or
       nneewwtteerrmm.  Deleting a _S_C_R_E_E_N provides a way to do this:

       •   X/Open Curses does not say what happens to _W_I_N_D_O_Ws  when  ddeellssccrreeeenn
           “frees  storage associated with the _S_C_R_E_E_N” nor does the SVr4 docu‐
           mentation help, adding that it should be called after eennddwwiinn  if  a
           _S_C_R_E_E_N is no longer needed.

       •   However,  _W_I_N_D_O_Ws are implicitly associated with a _S_C_R_E_E_N.  so that
           it is reasonable to expect ddeellssccrreeeenn to deal with these.

       •   SVr4 curses deletes  the  standard  _W_I_N_D_O_W  structures  ssttddssccrr  and
           ccuurrssccrr  as  well  as a work area nneewwssccrr.  SVr4 curses ignores other
           windows.

       •   Since version 4.0 (1996), _n_c_u_r_s_e_s has maintained a list of all win‐
           dows  for  each screen, using that information to delete those win‐
           dows when ddeellssccrreeeenn is called.

       •   NetBSD copied this feature of _n_c_u_r_s_e_s in  2001.   PDCurses  follows
           the SVr4 model, deleting only the standard _W_I_N_D_O_W structures.

   HHiigghh--lleevveell vveerrssuuss LLooww--lleevveell
       Different  implementations  may  disagree  regarding  the level of some
       functions.  For example, _S_C_R_E_E_N (returned by nneewwtteerrmm) and _T_E_R_M_I_N_A_L (re‐
       turned  by  sseettuupptteerrmm(3X)) hold file descriptors for the output stream.
       If an application switches screens using sseett__tteerrmm, or  switches  termi‐
       nals  using sseett__ccuurrtteerrmm(3X), applications which use the output file de‐
       scriptor can have different behavior depending on which structure holds
       the corresponding descriptor.

       For example

       •   NetBSD's  bbaauuddrraattee(3X)  function  uses  the descriptor in _T_E_R_M_I_N_A_L.
           _n_c_u_r_s_e_s and SVr4 use the descriptor in _S_C_R_E_E_N.

       •   NetBSD and _n_c_u_r_s_e_s use the descriptor in _T_E_R_M_I_N_A_L for terminal  I/O
           modes,  e.g.,  ddeeff__sshheellll__mmooddee(3X),  ddeeff__pprroogg__mmooddee(3X).  SVr4 curses
           uses the descriptor in _S_C_R_E_E_N.

   UUnnsseett _T_E_R_M VVaarriiaabbllee
       If the _T_E_R_M variable is missing or empty, iinniittssccrr uses the  value  “un‐
       known”, which normally corresponds to a terminal entry with the _g_e_n_e_r_i_c
       (_g_n) capability.  Generic entries are  detected  by  sseettuupptteerrmm(3X)  and
       cannot  be  used  for full-screen operation.  Other implementations may
       handle a missing/empty _T_E_R_M variable differently.

   SSiiggnnaall HHaannddlleerrss
       Quoting from X/Open Curses Issue 7, section 3.1.1:

            Curses implementations may provide for  special  handling  of  the
            SIGINT,  SIGQUIT,  and  SIGTSTP  signals  if  their disposition is
            SIG_DFL at the time _i_n_i_t_s_c_r is called...

            Any special handling for these signals may remain  in  effect  for
            the  life of the process or until the process changes the disposi‐
            tion of the signal.

            None of the Curses functions are required to be safe with  respect
            to signals...

       This  implementation establishes signal handlers during initialization,
       e.g., iinniittssccrr or nneewwtteerrmm.  Applications which must handle these signals
       should  set  up  the  corresponding handlers _a_f_t_e_r initializing the li‐
       brary:

       SSIIGGIINNTT
            The handler _a_t_t_e_m_p_t_s to clean up the screen on exit.  Although  it
            _u_s_u_a_l_l_y works as expected, there are limitations:

            •   Walking  the  _S_C_R_E_E_N list is unsafe, since all list management
                is done without any signal blocking.

            •   On systems which have RREEEENNTTRRAANNTT turned on, sseett__tteerrmm uses func‐
                tions which could deadlock or misbehave in other ways.

            •   eennddwwiinn  calls  other  functions, many of which use _s_t_d_i_o(3) or
                other library functions which are clearly unsafe.

       SSIIGGTTEERRMM
            This uses the same handler as SSIIGGIINNTT, with the  same  limitations.
            It  is  not  mentioned  in X/Open Curses, but is more suitable for
            this purpose than SSIIGGQQUUIITT (which is used in debugging).

       SSIIGGTTSSTTPP
            This handles the _s_t_o_p signal, used in job control.  When  resuming
            the  process,  this  implementation  discards  pending  input with
            fflluusshhiinnpp(3X), and repaints the screen assuming that  it  has  been
            completely altered.  It also updates the saved terminal modes with
            ddeeff__sshheellll__mmooddee(3X).

       SSIIGGWWIINNCCHH
            This handles the window-size changes which  were  ignored  in  the
            standardization  efforts.   The handler sets a (signal-safe) vari‐
            able which is later tested in wwggeettcchh(3X).  If kkeeyyppaadd has been  en‐
            abled  for the corresponding window, wwggeettcchh returns the key symbol
            KKEEYY__RREESSIIZZEE.  At the same time, wwggeettcchh calls rreessiizzeetteerrmm  to  adjust
            the  standard  screen  ssttddssccrr, and update other data such as LLIINNEESS
            and CCOOLLSS.

SSEEEE AALLSSOO
       ccuurrsseess(3X),    ccuurrss__kkeerrnneell(3X),     ccuurrss__rreeffrreesshh(3X),     ccuurrss__ssllkk(3X),
       ccuurrss__tteerrmmiinnffoo(3X), ccuurrss__uuttiill(3X), ccuurrss__vvaarriiaabblleess(3X)



ncurses 6.5                       2024-04-20                  curs_initscr(3X)
