The  function was introduced by as a slightly stronger version of
The and arguments are all identical to the analogous arguments of
The argument in points to a rather than the (modifiable) used  by
as  in  a null pointer may be passed to indicate that should wait
indefinitely.  Finally, specifies a  signal  mask  which  is  set
while  waiting for input.  When returns, the original signal mask
is restored.  See for a more detailed discussion of the semantics
of this interface, and for macros used  to  manipulate  the  data
type.   The  function  returns the same values and under the same
conditions as The function may fail for any of the reasons  docu‐
mented  for  and (if a signal mask is provided) The function con‐
forms to The function first appeared in The manual page was writ‐
ten by
