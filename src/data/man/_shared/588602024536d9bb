The  utility  displays the target of a symbolic link.  If a given
argument is not a symbolic link and the option is not  specified,
will  print  nothing to standard output about that and eventually
exit with an error status.  If the option is specified, the  out‐
put  is  canonicalized by following every symlink in every compo‐
nent of the given path recursively.  will resolve  both  absolute
and  relative  paths, and, if possible, return the absolute path‐
name corresponding to In this case, the argument does not need to
be a symbolic link.  The options are as follows: Canonicalize the
pathname of as described above.  Do not force a newline to appear
after the output for each Suppress failure messages if  calls  to
fail.  This is the default for This is an alternative to Turn off
quiet  mode.  will display errors about for which fails.  This is
the inverse of and The utility appeared along with  within  which
it is integrated, in The utility was written by The original com‐
bined man page was written by
