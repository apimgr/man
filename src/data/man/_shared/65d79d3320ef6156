attribute  on  a  The  function sets the on the attributes object
referenced by The function retrieves the on the attributes object
referenced by The argument is the set of signals to  be  returned
to  default  behaviour in the new process on creation if the flag
is set in the It’s default value must be set via a call to or the
behaviour is undefined.  On success, these functions return 0; on
failure they return an error number from  Additionally,  if  suc‐
cessful,  will modify the contents of the parameter with the cur‐
rent attribute value.  These functions may  fail  if:  The  value
specified by is invalid.  The value of is invalid.  The and func‐
tion calls appeared in
