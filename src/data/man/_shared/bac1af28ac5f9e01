The daemon handles Bluetooth Personal Area Networking services in
the  system.   It  can  operate in client mode as a Personal Area
Networking User or in server mode as Network Access  Point  Group
ad‐hoc  Network  or PANU host.  connects to the system via a vir‐
tual Ethernet device and  forwards  Ethernet  packets  to  remote
Bluetooth  devices using the Bluetooth Network Encapsulation Pro‐
tocol The PANU client is the device that uses either the  NAP  or
GN  service,  or  can talk directly to a PANU host in a crossover
cable fashion.  A GN host forwards Ethernet packets  to  each  of
the  connected PAN users as needed but does not provide access to
any additional networks.  The NAP service provides  some  of  the
features of an Ethernet bridge, with the NAP host forwarding Eth‐
ernet packets between each of the connected PAN users, and a dif‐
ferent network media.  Note, the only differences between NAP and
GN services as implemented by are in the SDP service record.  The
bridging of packets by the NAP must be configured separately with
The  options  are  as  follows: In client mode, address of remote
server.  May be given as BDADDR or name, in which case  will  at‐
tempt to resolve the address via the call.  In server mode, spec‐
ify  to the control socket.  The default path is Restrict connec‐
tions to the local May be given as BDADDR or name, in which  case
will  attempt  to resolve the address via the call.  will set the
interface physical address to the BDADDR of the Bluetooth  radio.
uses  the  driver to create a new network interface for use.  Use
this option to select a specific device interface which must  al‐
ready be created.  In server mode, limit the number of simultane‐
ous  connections.  The default limit is 7 for NAP and GN servers,
and 1 for a PANU server.  Set L2CAP connection link  mode.   Sup‐
ported  modes  are: require devices to be paired.  auth, plus en‐
able encryption.  encryption, plus change of link  key.   Use  an
alternative L2CAP Protocol/Service Multiplexer for server mode or
client  mode The default PSM for BNEP is 15 Name of to provide or
connect to, the following services are recognised:  Group  ad‐hoc
Network.   Network  Access Point.  Personal Area Networking User.
As per except that will  not  use  SDP  services  for  connection
setup.   When  providing  networking  services, the Bluetooth PAN
profile says that the property of the bluetooth controller  SHALL
include  Networking capability See for details.  After has set up
the client or server connection and opened the interface, it will
create a pid file and detach.  Will create an  encrypted  connec‐
tion to the NAP on and link that to the interface.  Will create a
Group  Network  requiring authentication to join and register the
GN service with the local SDP server.  The and specifications are
available at There is no way to supply alternative values for the
SDP record.  There is no way to set net type or multicast address
filters.  does not do any address routing except to directly con‐
nected unicast addresses.  All other packets are  multicast.   As
uses the BDADDR of the Bluetooth radio as the physical address of
the  tap,  only one instance can be run per radio.  can only pro‐
vide a single service.
