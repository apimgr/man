The  utility  opens  a pipe to and feeds an input stream into it,
while displaying a progress bar to standard output.  If no  file‐
name  is  specified,  reads from standard input.  Where feasible,
the input to determine the length, so a time estimate can be cal‐
culated.  If no length is specified or  determined,  simply  dis‐
plays  a count of the data and the data rate.  The options are as
follows: Read in buffers of the specified size (default 64k).  An
optional suffix (per may be given.  Display progress to  standard
error  instead  of  standard output.  Read from the specified in‐
stead of standard input.  Use the specified length for  the  time
estimate,  rather than attempting to the input.  An optional suf‐
fix (per may be given.  Print the given text before (left of) the
progress bar.  Filter the input through If is  specified,  calcu‐
late the length using The command will extract the displaying the
progress bar as time passes:
  0%  |                                |      0        0.00 KiB/s
‐‐:‐‐ ETA
 40% |********                       |   273  KiB   271.95  KiB/s
00:01 ETA
 81%  |***********************         |    553 KiB  274.61 KiB/s
00:00  ETA  100%  |*******************************|     680   KiB
264.59  KiB/s     00:00  ETA  If  it  is preferred to monitor the
progress of the decompression process (unlikely), then  could  be
used.   The  command  will  copy the 120 GiB disk to displaying a
progress bar during the operation.  first appeared in The dynamic
progress bar display code is  part  of  was  written  by  dynamic
progress bar was written by Luke Mewburn.  Since the progress bar
is displayed asynchronously, it may be difficult to read some er‐
ror  messages,  both  those  produced by the pipeline, as well as
those produced by itself.
