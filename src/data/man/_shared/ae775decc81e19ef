provides a shell command interface to the system log module.  Op‐
tions:  Log  to  console  as  a fallback if the message cannot be
passed to the message is written to Log this  in  the  structured
data  (SD)  field.  has to be passed as one argument and will re‐
quire careful quoting when used from the shell.  Log  the  speci‐
fied  file.   Log  the process id of the logger process with each
line The MSGID used for the message.  Open log  file  immediately
Enter  the message with the specified priority.  The priority may
be specified numerically or as a pair.   For  example,  logs  the
message(s) as level in the facility.  The default is Log the mes‐
sage to standard error, as well as the system log Mark every line
in  the  log  with the specified Write the message to log; if not
specified, and the  flag  is  not  provided,  standard  input  is
logged.  logger System rebooted

logger  -p local0.notice -t HOSTIDM -f /dev/idmc The utility con‐
forms to
