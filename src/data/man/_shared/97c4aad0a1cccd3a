Cloning directories with these functions is strongly discouraged.
Use  to clone directories instead.  The function causes the named
file to be cloned to the named file The cloned  file  shares  its
data  blocks with the file but has its own copy of attributes and
extended attributes which are identical to  those  of  the  named
file  with  the exceptions listed below: ownership information is
set as it would be if was created by or or if  the  current  user
does  not  have  privileges  to change ownership. If the optional
flag CLONE_NOOWNERCOPY is passed, the  ownership  information  is
the  same  as if the the current user does not have privileges to
change ownership.  setuid and setgid bits are turned off  in  the
mode  bits  for regular files.  inherit target directory’s access
control lists. If the optional flag CLONE_ACL is passed, it  will
additionally  copy  the  ACLs from the source file and also apply
the inherited ACLs from the target directory.  Subsequent  writes
to either the original or cloned file are private to the file be‐
ing  modified (copy‐on‐write).  The named file must not exist for
the call to be successful.  Since  the  clonefile()  system  call
might  not  allocate  new storage for data blocks, it is possible
for a subsequent overwrite of an existing data  block  to  return
ENOSPC.   If names a directory, the directory hierarchy is cloned
as if each item was cloned individually.  However, the use of  to
clone directory hierarchies is strongly discouraged.  Use instead
for copying directories.  The function is equivalent to except in
the  case  where either or specifies a relative path. If src is a
relative path, the file to be cloned is located relative  to  the
directory associated with the file descriptor instead of the cur‐
rent  working  directory. If is a relative path, the same happens
only relative to the directory associated with If is  passed  the
special  value  in  either the or parameters, the current working
directory is used in the determination of the file  for  the  re‐
spective  path parameter.  The function is similar to except that
the source is identified by file descriptor rather  than  a  path
(as  in  or  ).   The parameter specifies the options that can be
passed. Options are specified in the argument by or’ing the  fol‐
lowing values: Don’t follow the src file if it is a symbolic link
(applicable only if the source is not a directory).  The symbolic
link  is itself cloned if names a symbolic link.  Don’t copy own‐
ership information from the source when run called with superuser
privileges.  The symbolic link is itself cloned if names  a  sym‐
bolic  link.   Copy ACLs from the source file.  do not follow any
symbolic links encountered during pathname resolution.  An  error
is returned if a symlink is encountered before the last component
of  either or If and does not reside in the hierarchy beneath the
starting directory, an error is returned.  The and functions  are
expected  to  be  atomic i.e. the system call will result all new
objects being created successfully or no new objects will be cre‐
ated. POSIX conforming applications cannot  use  Upon  successful
completion,  returns  0. Otherwise, a value of ‐1 is returned and
errno is set to indicate the error.  Not all  volumes  support  A
volume can be tested for support by using to get the volume capa‐
bilities  attribute  ATTR_VOL_CAPABILITIES,  and then testing the
VOL_CAP_INT_CLONE flag.  The function will fail if: Read  permis‐
sions  are  denied  on the source or write permissions are on the
destination parent.  The underlying filesystem does  not  support
this  call.   The  named  file  exists.   and are not on the same
filesystem.  The value of the parameter is invalid.  There is  no
free  space remaining on the file system containing the file.  An
I/O error occurred while reading from or writing to the file sys‐
tem.  The calling process does not have  appropriate  privileges.
is  the  root of the Filesystem.  A loop exists in symbolic links
encountered during in resolution of the or  path  arguments.   If
CLONE_NOFOLLOW_ANY  option was passed and a symbolic link was en‐
countered in translating the pathname of either or arguments.  If
CLONE_RESOLVE_BENEATH was passed and either or does not reside in
the directory hierarchy beneath the starting directory.  The  re‐
quested  operation requires writing in a directory on a read‐only
file system.  The length of a component of a pathname  is  longer
than  {NAME_MAX}.   A component of path or the path does not name
an existing file or path is an empty string.  A component of path
prefix of either or names an  existing file that  is   neither  a
directory  nor  a symbolic link to a directory, or the path argu‐
ment contains at least one non <slash> character  and  ends  with
one  or  more  trailing  <slash> characters and the last pathname
component names an existing file that is neither a directory  nor
a  symbolic  link to a directory.  A component of either pathname
refers to a directory that requires materialization and  the  I/O
policy of the current thread or process disallows dataless direc‐
tory  materialization  The pathname refers to a file that must be
materialized before being cloned and the I/O policy of  the  cur‐
rent  thread  or  process disallows file materialization In addi‐
tion, the or functions may fail with the following errors: The or
argument does not specify an absolute path and the or argument is
neither nor a valid file descriptor open for searching.   The  or
argument is not an absolute path and or is neither nor a file de‐
scriptor associated with a directory.  The and function calls ap‐
peared in OS X version 10.12
