To enable history editing: To disable entering upon kernel panic:
To enable teeing all output to the kernel msgbuf: To specify com‐
mands  which  will  be  executed  on  each entry to In this case,
"trace" and then "show registers" will be executed automatically.
To enable extended online help: is the  in‐kernel  debugger.   It
may  be  entered  at any time via a special key sequence, and op‐
tionally may be invoked when the kernel panics.  Unless is set to
0, will be activated whenever the kernel would  otherwise  panic.
may  also  be  activated from the console.  In general, sending a
break on a serial console will activate There are  also  key  se‐
quences  for  each  port  that  will  activate from the keyboard:
<Ctrl>‐<Alt>‐<Esc> on  PC  style  keyboards.   <Ctrl>‐<Alt>‐<Esc>
<Break>  on  serial  console.  <LAlt>‐<LAmiga>‐<F10> <Alt>‐<Left‐
Shift>‐<F9> <Ctrl>‐<Alt>‐<Esc> on PC style keyboards.  <Break> on
serial console.  Some models: +++++ (five plus signs)  on  serial
console.   <Shift>‐<Reset>  <Ctrl>‐<Alt>‐<Esc> <Ctrl>‐<Alt>‐<Esc>
<Ctrl>‐<Alt>‐<Esc>  <Ctrl>‐<Alt>‐<Esc>  on  PC  style  keyboards.
+++++ (five plus signs) on PDC console <Break> on serial console.
<Ctrl>‐<Alt>‐<Esc> <Break> on serial console.  <Command>‐<Power>,
or the Interrupt switch.  Some models: <Command>‐<Option>‐<Power>
Abort switch on CPU card.  <Do> on LK‐201 rcons console.  <Break>
on  serial  console.   <Break>  on  serial  console.   <L1>‐A, or
<Stop>‐A on a Sun keyboard.  <Break> on serial console.   <L1>‐A,
or  <Stop>‐A  on  a  Sun  keyboard.   <Break>  on serial console.
<L1>‐A, or <Stop>‐A on a Sun keyboard.  <Break>  on  serial  con‐
sole.   <Esc>‐<Shift>‐D  on  serial console.  Interrupt switch on
the body.  <Ctrl>‐<Alt>‐<Esc> on PC style keyboards.  +++++ (five
plus signs) on serial console.  +++++ (five plus signs) on serial
console.  <Ctrl>‐<Alt>‐<Esc> The key sequence to activate can  be
changed  by modifying with If the console is not dedicated to the
sequence should not be easily typed by  accident.   In  addition,
may  be  explicitly activated by the debugging code in the kernel
if is configured.  Commands can be automatically run when is  en‐
tered  by using or by setting with Multiple commands can be sepa‐
rated by a semi‐colon.  The general command syntax is:  The  cur‐
rent  memory location being edited is referred to as and the next
location is They are displayed as hexadecimal numbers.   Commands
that  examine  and/or  modify memory update to the address of the
last line examined or the last location modified, and set to  the
next  location  to be examined or modified.  Other commands don’t
change and set to be the same as A blank line repeats the  previ‐
ous  command from the address with the previous and no modifiers.
Specifying sets to the address.  If is omitted, is used.  A miss‐
ing is taken to be 1 for  printing  commands,  and  infinity  for
stack  traces.  The syntax: repeats the previous command, just as
a blank line does, but with the specified has a functionality; if
a number of lines in a command’s output exceeds  the  number  de‐
fined  in  the  variable, then displays and waits for a response,
which may be one of: one more line.  one more  page.   abort  the
current  command,  and return to the command input mode.  You can
set variable to zero to disable this feature.  If history editing
is enabled (by defining the kernel option), then a history of the
last commands is kept.  The history can be manipulated  with  the
following key sequences: retrieve previous command in history (if
any).   retrieve  next command in history (if any).  supports the
following commands: A synonym for Set a breakpoint at If is  sup‐
plied, continues times before stopping at the breakpoint.  If the
breakpoint  is set, a breakpoint number is printed with This num‐
ber can be used to the breakpoint, or to add  conditions  to  it.
If is specified, set a breakpoint at a user‐space address.  With‐
out  is  considered  to be in the kernel‐space, and an address in
the wrong space will be rejected, and an error  message  will  be
emitted.   This  modifier  may only be used if it is supported by
machine dependent routines.  Warning: if a user text is  shadowed
by  a  normal user‐space debugger, user‐space breakpoints may not
work correctly.  Setting a breakpoint at the low‐level code paths
may also cause strange behavior.  A synonym for A synonym  for  A
synonym  for  Call the function specified by with the argument(s)
listed in parentheses.  Parentheses may be omitted if  the  func‐
tion  takes  no  arguments.  The number of arguments is currently
limited to 10.  Continue execution until a breakpoint  or  watch‐
point.   If  is  specified,  count  instructions while executing.
Some machines (e.g., pmax) also count loads and stores.  Warning:
when counting, the debugger is really  silently  single‐stepping.
This  means  that  single‐stepping on low‐level may cause strange
behavior.  Delete a breakpoint.  The  target  breakpoint  may  be
specified  by  as  per or by the breakpoint number returned by if
it’s prefixed with Prints the  contents  of  the  kernel  message
buffer.  The optional argument will limit printing to at most the
last  bytes of the message buffer.  Delete the watchpoint at that
was previously set with command.  Display the  address  locations
according to the format in Multiple modifier formats display mul‐
tiple  locations.  If isn’t specified, the modifier from the last
use of is used.  The valid format  characters  for  are:  examine
bytes  (8  bits).   examine  half‐words (16 bits).  examine words
(legacy 32 bits).  examine quad‐words (64  bits).   examine  long
words  (implementation  dependent) print the location being exam‐
ined.  print the location with a line number if  possible.   dis‐
play  in  unsigned  hex.   display in signed hex.  display in un‐
signed octal.  display in signed decimal.   display  in  unsigned
decimal.   display  in current radix, signed.  display low 8 bits
as a character.  Non‐printing characters as displayed as an octal
escape code (e.g., display the NUL terminated string at the loca‐
tion.  Non‐printing characters are displayed  as  octal  escapes.
display  in unsigned hex with a character dump at the end of each
line.  The location is displayed as hex at the beginning of  each
line.  display as a pointer and it’s symbol if possible.  display
as a machine instruction.  display as a machine instruction, with
possible  alternative formats depending upon the machine: use Mo‐
torola syntax don’t assume that each external label is  a  proce‐
dure  entry  mask  Send  a signal to the process specified by the
Note that is interpreted using the current radix (see command for
details).  If isn’t specified, the SIGTERM  signal  is  sent.   A
synonym for Stop at the matching return instruction.  If is spec‐
ified,  print  the call nesting depth and the cumulative instruc‐
tion count at each call or return.  Otherwise,  only  print  when
the  matching  return  is  hit.  Print addresses according to the
modifier character, as per Valid modifiers are: and (as per If no
modifier is specified, the most recent  one  specified  is  used.
may  be  a  string,  and is printed For example: print/x "eax = "
$eax "\necx = " $ecx "\n" will produce: eax = xxxxxx ecx = yyyyyy
A synonym for Reboot, using the optionally supplied boot which is
a bitmask supporting the same values as for Some of the more use‐
ful flags: Note: Limitations of the command line  interface  pre‐
clude specification of a boot string.  Search memory from for The
unit  size  is  specified with a modifier character, as per Valid
modifiers are: and If no modifier is specified,  is  used.   This
command might fail in interesting ways if it doesn’t find This is
because doesn’t always recover from touching bad memory.  The op‐
tional  limits the search.  Set the named variable or register to
the value of Valid variable names are described in Display infor‐
mation about callouts in the system.  See for more information on
callouts.  Display details information about  all  active  locks.
If  is  specified,  stack  traces  of LWPs holding locks are also
printed.  This command is only useful if  a  kernel  is  compiled
with Display all mount points.  If is specified, the complete vn‐
ode  list is printed.  Display basic information about all physi‐
cal pages managed by the VM system.  For more  detailed  informa‐
tion about a single page, use Display all pool information.  Mod‐
ifiers  are  the  same as Display all process information.  Valid
modifiers: show process information in a style format.   Informa‐
tion  printed  includes:  process  ID, parent process ID, process
group, UID, process status, process flags, number of  LWPs,  com‐
mand  name,  and process wait channel message.  show each process
ID, command name, kernel virtual addresses of each process’  proc
structure, u‐area, and vmspace structure.  The vmspace address is
also  the  address  of  the process’ vm_map structure, and can be
used in the command.  show each LWP ID, process ID, command name,
system call emulation, priority, wait channel  message  and  wait
channel address.  LWPs currently running on a CPU are marked with
the  ’>’ sign.  show each LWP ID, process ID, process status, CPU
ID the LWP runs on, process flags, kernel virtual address of  LWP
structure,  LWP  name  and  wait channel message.  LWPs currently
running on a CPU are marked with the ’>’ sign.  This is  the  de‐
fault.  Display all lwps that are currently waiting in turnstiles
to  acquire locks, which locks they are waiting for, and who cur‐
rently holds them.  Valid modifiers: show a stack  trace  of  the
lwp that currently holds each lock Dump the entire routing table.
This  command  is  available  only on systems which support inet.
Display all breakpoints.  Print the struct buf at The does  noth‐
ing  at this time.  Print all the non‐zero event counters.  Valid
modifiers: event counters with a count of  zero  are  printed  as
well.   interrupted  counters  will  be displayed.  misc counters
will be displayed.  trap counters will be displayed.  If none  of
or  are  specified, all are shown.  You can combine any of these.
For example, the modifier will select  both  interrupt  and  trap
events,  including  those that are non‐zero.  Display information
about the vnodes of the files that  are  currently  open  by  the
process associated with the proc structure at This address can be
found using the command.  If the kernel is compiled with then de‐
tails about the locking of the underlying uvm object will also be
displayed.   Display  information about a lock at This command is
only useful if a kernel  is  compiled  with  Display  information
about  lock  statistics.  This command is only useful if a kernel
is compiled with Print the vm_map at If is  specified,  the  com‐
plete  map is printed.  Print the mount structure at If is speci‐
fied, the complete vnode list is printed.  Print the mbuf  struc‐
ture at Valid modifiers: The mbufs in the chain are NOT followed.
The  data is dumped.  Decode the mbuf chain as a packet.  It cur‐
rently supports Ethernet, PPP,  PPPoE,  ARP,  IPv4,  ICMP,  IPv6,
ICMP6,  TCP and UDP.  Dump the namecache list associated with vn‐
ode at Print the vm_object at If is specified, the  complete  ob‐
ject  is printed.  Print the vm_page at If is specified, the com‐
plete page is printed.  Print the current "panic" string.   Print
the  pool at Valid modifiers: Print the cachelist and its statis‐
tics for this pool.  Print the log entries for this pool.   Print
the  pagelist  for  this pool.  Print a short (one line) list per
pool, showing the wait channel, pool  address,  allocation  size,
alignment,  allocated pages, allocated items, consumed items, al‐
location  requests,  allocation  frees,  pages  allocated,  pages
freed,  and  currently idle pages, respectively.  Skip pools with
zero allocations.  Show information about a process and its LWPs.
LWPs currently running on a CPU are marked  with  the  ’>’  sign.
The  argument  passed is the kernel virtual address of LWP struc‐
ture.  The argument passed is a PID.  Note  that  is  interpreted
using  the  current radix (see command for details).  This is the
default.  Display the register set.   If  is  specified,  display
user  registers instead of kernel registers or the currently save
one.  Warning: support for is machine  dependent.   If  not  sup‐
ported, incorrect information will be displayed.  Print the state
of  the  scheduler’s  run queues.  For each run queue that has an
LWP, the run queue index and the list of LWPs will be shown.   If
the  run queue has LWPs, but the sched_whichqs bit is not set for
that queue, the queue index will be prefixed with a  Print  usage
of  system’s  socket  buffers.   By default, empty sockets aren’t
printed.  Print all processes which use the socket.   Print  mbuf
chain in the socket buffer.  By default, a process which uses the
socket  is  printed  (only  one  socket).   If  is specified, the
process isn’t printed.  Verbose mode.  If is specified, all sock‐
ets are printed.  Print a selection of UVM counters  and  statis‐
tics.  Dumps all the kernel histories if no address is specified,
or the history at the address.  If is specified, display informa‐
tion about the named history or all histories, instead of history
entries.   If  is  specified,  only the last entries will be dis‐
played.  Currently the handling is only  performed  if  a  single
history is requested.  This command is available only if a kernel
is  compiled  with  one  or more of the kernel history options or
Print the vmem at Display all vmems.  Print the vnode  at  If  is
specified,  the complete vnode is printed.  Print the vnode which
has its lock at If is specified, the complete vnode  is  printed.
Display  all  watchpoints.  Search the symbol tables for all sym‐
bols of which is a substring, and display them.  If is specified,
a character is displayed immediately after each symbol name indi‐
cating the type of symbol.  Object symbols display function  sym‐
bols  display section symbols display and file symbols display To
sift for a string beginning with a number, escape the first char‐
acter with a backslash as: sifting \386 Single‐step times.  If is
specified, print each instruction at each step.  Otherwise,  only
print  the  last  instruction.  Warning: depending on the machine
type, it may not be possible to  single‐step  through  some  low‐
level  code paths or user‐space code.  On machines with software‐
emulated single‐stepping (e.g., pmax), stepping through code exe‐
cuted by interrupt handlers will probably  do  the  wrong  thing.
Sync the disks, force a crash dump, and then reboot.  Stack trace
from  If  is specified, trace user‐space, otherwise trace kernel‐
space.  is the number of frames to be traced.  If is omitted, all
frames are printed.  If is specified, the trace  is  printed  and
also  stored  in  the kernel message buffer.  Warning: user‐space
stack trace is valid only if the machine dependent code  supports
it.   Stack  trace by (process, on rather than by stack frame ad‐
dress.  Note that is interpreted using the current radix,  whilst
displays  pids  in  decimal; prefix with to force it to be inter‐
preted as decimal (see section for radix).  If is specified,  the
trace  is  printed  and also stored in the kernel message buffer.
Warning: trace by pid is valid only if the machine dependent code
supports it.  Stack trace by light weight process  (LWP)  address
rather  than  by stack frame address.  If is specified, the trace
is printed and also stored in the kernel message  buffer.   Warn‐
ing:  trace by LWP address is valid only if the machine dependent
code supports it.  Stop at the next call or  return  instruction.
If  is specified, print the call nesting depth and the cumulative
instruction count at each call or return.  Otherwise, only  print
when  the matching return is hit.  Set a watchpoint for a region.
Execution stops when an attempt to modify the region occurs.  de‐
faults to 4.  If you specify a wrong space address,  the  request
is  rejected  with  an error message.  Warning: attempts to watch
wired kernel memory may cause an unrecoverable error in some sys‐
tems such as i386.  Watchpoints on user addresses work the  best.
Describe  what an address is.  Write the at succeeding locations.
The unit size is specified with  a  modifier  character,  as  per
Valid  modifiers  are:  and If no modifier is specified, is used.
Specifying the modifiers in upper case, will prevent from reading
the memory location first, which is useful for avoiding side  ef‐
fects  when  writing to I/O memory regions.  Warning: since there
is no delimiter between strange things may occur.  It’s  best  to
enclose  each in parentheses.  A synonym for The "glue" code that
hooks into the kernel for any given port  can  also  add  machine
specific  commands  to the command parser.  All of these commands
are preceded by the command word to indicate that they  are  part
of the machine‐specific command set (e.g.  Some of these commands
are:  Set or clear a hardware breakpoint.  Switch to another CPU.
Print CPU information about the ‘‘struct cpuinfo’’.  Given a trap
frame address, print out the trap frame.  Print  lwp  information
about the ‘‘struct lwp’’.  Print PTE information.  Reset the sys‐
tem.   Print  system  registers.   Set or clear a hardware watch‐
point.  Pass the address to be watched, or watchpoint  number  to
clear  the  watchpoint.   Optional modifiers are for read access,
for write access (default: trap on read or write access),  for  8
bit  width,  for  16  bit, for 32 bit or, for 64 bit (default: 32
bit).  Switch to another CPU.  Switch to another  CPU.   Given  a
trap  frame address, print out the trap frame.  Reset the system.
Without an address the default trap frame is printed.  Otherwise,
the trap frame address can be given, or,  when  the  modifier  is
used,  an LWP address.  Switch to another CPU.  Without a vector,
information about all 256 vectors is shown.  Otherwise, the given
vector is shown.   Dump  CP0  (coprocessor  0)  register  values.
Switch  to  another  CPU.  Print the physical address for a given
kernel virtual address.  Send an NMI to a  different  CPU.   This
DDB command is currently only implemented for Cavium Octeon CPUs.
Reset  the system.  Not implemented for many CPUs and/or systems.
Print out the Translation Lookaside Buffer (TLB).  Use the  modi‐
fier  to  show only valid TLB entries.  Set a hardware watchpoint
on an address or a TLB ASID.  Pass the address to be watched.  If
no address is specified, show a list of active watchpoints.   The
modifiers  are  i for trap on an instruction fetch, for trap on a
read, for trap on a write, for a mask on the  address  to  match,
for trap on a TLB ASID match.  The and modifiers require an extra
argument  for  the  mask and ASID respectively.  Clear a hardware
watchpoint.  If an address is specified, clear  watchpoints  that
match that address.  If no address is specified, clear all watch‐
points.   Print  process  MMU  context information.  Print PA‐>VA
mapping information.  Reset the system.  Display the contents  of
the  trapframe.   Display  instruction translation storage buffer
information.  Set the DCR register.  Must  be  between  0x00  and
0x3ff.  Display user memory.  Use the modifier to get instruction
decoding.   Print BAT registers and translations.  Print MMU reg‐
isters.  Print TLB entries.  Print cache entries.   Print  switch
frame  and trap frames.  Print kernel stack usage.  Only works in
kernels compiled with the option.  Switch to another CPU.   Enter
the  Sun  PROM  monitor.   Display some information about the LWP
pointed to, or curlwp.  Display  information  about  the  listed.
Display  the  pointer  to  the  for this physical address.  Print
process context information.  Switch to another CPU.  Print  data
translation  look‐aside buffer context information.  Display data
translation  storage  buffer  information.   Display  information
about the listed mapping in the kernel pmap.  Use the modifier to
get  a  full  listing.   Extract the physical address for a given
virtual address from the kernel pmap.  Dump the FPU state.  Print
instruction translation look‐aside  buffer  context  information.
Display instruction translation storage buffer information.  Dis‐
play  a struct lwp Display information about the listed.  Attempt
to change process context.  Display the pointer to the  for  this
physical  address.   Display  physical memory.  Display the pmap.
Use the modifier to get a fuller listing.  Display some  informa‐
tion  about  the  process  pointed to, or curproc.  Enter the OFW
PROM.  Display the pointed to.  Reset the machine and enter  prom
(do a Software Initiated Reset).  Dump the window stack.  Use the
modifier  to  get  userland information.  Display full trap frame
state.  This is most useful for inclusion with bug reports.  Dis‐
play trap state.  Display or set trap trace information.  Use the
and modifiers to get reversed and full information, respectively.
Set or clear a physical or virtual hardware watchpoint.  Pass the
address to be watched, or (or omit  the  address)  to  clear  the
watchpoint.   Optional  modifiers  are  for physical address, for
trap on read access (default: trap on write access only),  for  8
bit  width, for 16 bit, for 32 bit or for 64 bit.  Print register
window information.  Argument is a stack frame number (0  is  top
of stack, which is used when no index is given).  Drop into moni‐
tor  via abort (allows continue).  Exit to Sun PROM monitor as in
Reboot the machine as in Given an  address,  print  the  address,
segment map, page map, and Page Table Entry (PTE).  Switch to an‐
other  CPU.   accesses  registers and variables as Register names
are as per the command.  Some variables are  suffixed  with  num‐
bers, and may have a modifier following a colon immediately after
the  variable  name.   For example, register variables may have a
modifier to indicate user register (e.g., Built‐in variables cur‐
rently supported are: If non‐zero (the default), causes  a  stack
trace  to  be printed when is entered on panic.  If non‐zero (the
default), the kernel allows to enter from the console  (by  break
signal or special key sequence).  If the kernel configuration op‐
tion  is  used, will be initialized to off.  The number of lines.
This is used by the feature.  When this variable is set  to  zero
the  feature  is  disabled.   Addresses  are printed as unless is
greater than The width of the displayed line.  wraps the  current
line  by  printing  new  line  when column is reached.  When this
variable is set to zero doesn’t perform any wrapping.  If greater
than zero (the default is 1), will be  invoked  when  the  kernel
panics.  If the kernel configuration option is used, will be ini‐
tialized to off, causing a stack trace to be printed and the sys‐
tem  to be rebooted instead of being entered.  Setting to -1 sup‐
presses the stack trace before reboot.  Input and  output  radix.
Tab  stop  width.  If explicitly set to non zero (zero is the de‐
fault) all output will not only be displayed on screen  but  also
be fed to the msgbuf.  The default of the variable can be set us‐
ing  the  kernel configuration option which will initialize to be
1.  This option is especially handy for poor souls who don’t have
a serial console but want to recall output from a crash  investi‐
gation.  This option is more generic than the /l command modifier
possible for selected commands as discussed above to log the out‐
put.   Mixing  both /l and this setting can give double loggings.
Number of stack frames to display  on  panic.   Useful  to  avoid
scrolling  away  the  interesting frames on a glass tty.  Default
value is (all frames), useful value around All built‐in variables
are accessible via Almost all expression operators in C are  sup‐
ported,  except and unary Special rules in are: name of a symbol.
It is translated to the address (or value) of  it.   and  can  be
used  in the identifier.  If supported by an object format depen‐
dent routine, and can be accepted as a symbol.  The symbol may be
prefixed with (e.g., to specify other than kernel symbols.   num‐
ber.   Radix  is determined by the first two characters: ‐ hex, ‐
octal, ‐ decimal, otherwise follow current radix.  address of the
start of the last line examined.  Unlike or this is only  changed
by the or commands.  last address explicitly specified.  register
name  or  variable.  It is translated to the value of it.  It may
be followed by a and modifiers as described above.  a binary  op‐
erator which rounds up the left hand side to the next multiple of
right  hand side.  expression indirection.  It may be followed by
a and modifiers as described  above.   The  kernel  debugger  was
written  as  part  of the MACH project at Carnegie‐Mellon Univer‐
sity.
