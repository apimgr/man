ZPOOL-SCRUB(8)            BSD System Manager's Manual           ZPOOL-SCRUB(8)

NNAAMMEE
     zzppooooll--ssccrruubb — begin or resume scrub of ZFS storage pools

SSYYNNOOPPSSIISS
     zzppooooll ssccrruubb [--ss|--pp] [--ww] [--ee] _p_o_o_lâ¦

DDEESSCCRRIIPPTTIIOONN
     Begins a scrub or resumes a paused scrub.  The scrub examines all data in
     the specified pools to verify that it checksums correctly.  For repli‐
     cated (mirror, raidz, or draid) devices, ZFS automatically repairs any
     damage discovered during the scrub.  The zzppooooll ssttaattuuss command reports the
     progress of the scrub and summarizes the results of the scrub upon com‐
     pletion.

     Scrubbing and resilvering are very similar operations.  The difference is
     that resilvering only examines data that ZFS knows to be out of date (for
     example, when attaching a new device to a mirror or replacing an existing
     device), whereas scrubbing examines all data to discover silent errors
     due to hardware faults or disk failure.

     When scrubbing a pool with encrypted filesystems the keys do not need to
     be loaded.  However, if the keys are not loaded and an unrepairable
     checksum error is detected the file name cannot be included in the zzppooooll
     ssttaattuuss --vv verbose error report.

     Because scrubbing and resilvering are I/O-intensive operations, ZFS only
     allows one at a time.

     A scrub is split into two parts: metadata scanning and block scrubbing.
     The metadata scanning sorts blocks into large sequential ranges which can
     then be read much more efficiently from disk when issuing the scrub I/O.

     If a scrub is paused, the zzppooooll ssccrruubb resumes it.  If a resilver is in
     progress, ZFS does not allow a scrub to be started until the resilver
     completes.

     Note that, due to changes in pool data on a live system, it is possible
     for scrubs to progress slightly beyond 100% completion.  During this pe‐
     riod, no completion time estimate will be provided.

OOPPTTIIOONNSS
     --ss  Stop scrubbing.

     --pp  Pause scrubbing.  Scrub pause state and progress are periodically
         synced to disk.  If the system is restarted or pool is exported dur‐
         ing a paused scrub, even after import, scrub will remain paused until
         it is resumed.  Once resumed the scrub will pick up from the place
         where it was last checkpointed to disk.  To resume a paused scrub is‐
         sue zzppooooll ssccrruubb or zzppooooll ssccrruubb --ee again.

     --ww  Wait until scrub has completed before returning.

     --ee  Only scrub files with known data errors as reported by zzppooooll ssttaattuuss
         --vv.  The pool must have been scrubbed at least once with the
         hheeaadd__eerrrrlloogg feature enabled to use this option.  Error scrubbing can‐
         not be run simultaneously with regular scrubbing or resilvering, nor
         can it be run when a regular scrub is paused.

EEXXAAMMPPLLEESS
   EExxaammppllee 11
     Status of pool with ongoing scrub:

     # zzppooooll ssttaattuuss
       ...
       scan: scrub in progress since Sun Jul 25 16:07:49 2021
             403M / 405M scanned at 100M/s, 68.4M / 405M issued at 10.0M/s
             0B repaired, 16.91% done, 00:00:04 to go
       ...

     Where metadata which references 403M of file data has been scanned at
     100M/s, and 68.4M of that file data has been scrubbed sequentially at
     10.0M/s.

PPEERRIIOODDIICC SSCCRRUUBB
     On machines using systemd, scrub timers can be enabled on per-pool basis.
     wweeeekkllyy and mmoonntthhllyy timer units are provided.

             ssyysstteemmccttll eennaabbllee zzffss--ssccrruubb--_w_e_e_k_l_y@@_r_p_o_o_l..ttiimmeerr ----nnooww

             ssyysstteemmccttll eennaabbllee zzffss--ssccrruubb--_m_o_n_t_h_l_y@@_o_t_h_e_r_p_o_o_l..ttiimmeerr ----nnooww

SSEEEE AALLSSOO
     systemd.timer(55),, zpool-iostat(88),, zpool-resilver(88),, zpool-status(88)

BSD                              June 22, 2023                             BSD
