takes a list of names and looks for the files which would be exe‐
cuted  had  these names been given as commands.  Each argument is
searched for along the user’s If the flag is given, will continue
to search the until all instances of a program  file  are  found.
The  command  appeared  in  This  implementation  does not expand
aliases, and is shell agnostic.  This is really a feature.
