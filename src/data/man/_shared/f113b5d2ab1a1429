compresses  using  threads to make use of multiple processors and
cores.  The input is broken up into 128 KB chunks with each  com‐
pressed  in  parallel.  The individual check value for each chunk
is also calculated in parallel.  The compressed data  is  written
in  order to the output, and a combined check value is calculated
from the individual check values.   The  compressed  data  format
generated  is in the gzip, zlib, or single‐entry zip format using
the deflate compression method.  The compression produces partial
raw deflate streams which are  concatenated  by  a  single  write
thread and wrapped with the appropriate header and trailer, where
the  trailer contains the combined check value.  Each partial raw
deflate stream is terminated by an empty stored block (using  the
option  of  in  order  to  end  that partial bit stream at a byte
boundary.  That allows the partial  streams  to  be  concatenated
simply  as  sequences  of  bytes.  This adds a very small four to
five byte overhead to the output for each input chunk.   The  de‐
fault  input  block size is 128K, but can be changed with the op‐
tion.  The number of compress threads is set by  default  to  the
number  of  online processors, which can be changed using the op‐
tion.  Specifying avoids the use of threads entirely.  The  input
blocks,  while compressed independently, have the last 32K of the
previous block loaded as a preset dictionary to preserve the com‐
pression effectiveness of deflating in a single thread.  This can
be turned off using the or option, so that the blocks can be  de‐
compressed independently for partial error recovery or for random
access.   Decompression can’t be parallelized, at least not with‐
out specially prepared deflate streams for that  purpose.   As  a
result, uses a single thread (the main thread) for decompression,
but  will  create  three  other threads for reading, writing, and
check calculation, which can speed up  decompression  under  some
circumstances.  Parallel decompression can be turned off by spec‐
ifying  one  process or Compressed files can be restored to their
original form using or Regulate the speed  of  compression  using
the  specified  digit  where or indicates the fastest compression
method (less compression) and or indicates the  slowest  compres‐
sion  method (best compression).  is no compression.  gives a few
percent better compression at a severe cost  in  execution  time.
The  default is Set compression block size to K (default 128KiB).
Write all processed output to stdout (won’t delete).   Decompress
the  compressed input.  If compressing at level 11, do iterations
first, before block split (default is  last).   Force  overwrite,
compress  .gz, links, and to terminal.  Display a help screen and
quit.  If compressing at level 11, number of iterations for opti‐
mization (default 15).  Compress blocks independently for  damage
recovery.  Compress to PKWare zip (.zip) single entry format.  Do
not  delete  original file after processing.  Display the license
and quit.  List the contents of the compressed  input.   If  com‐
pressing  at  level  11,  maximum number of split blocks (default
15).  Store/restore file name and mod time  in/from  header.   Do
not store or restore file name in/from header.  If compressing at
level  11,  do  not  split  into smaller blocks (default is block
splitting).  Allow up to n processes (default is  the  number  of
online  processors)  Print no messages, even on error.  Input‐de‐
termined block locations for rsync.  Process the contents of  all
subdirectories.  Use suffix instead of (for compression).  Do not
store  or restore mod time in/from header.  Test the integrity of
the compressed input.  Show the version of  pigz.   Provide  more
verbose  output.   Compress to zlib instead of gzip format.  This
software is provided ’as‐is’, without any express or implied war‐
ranty.  In no event will the author be held liable for  any  dam‐
ages  arising from the use of this software.  Copyright (C) 2007,
2008, 2009, 2010
