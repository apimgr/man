_s_d(4)                      Kernel Interfaces Manual                      _s_d(4)

NNAAMMEE
       sd - driver for SCSI disk drives

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<lliinnuuxx//hhddrreegg..hh>>        /* for HDIO_GETGEO */
       ##iinncclluuddee <<lliinnuuxx//ffss..hh>>           /* for BLKGETSIZE and BLKRRPART */

CCOONNFFIIGGUURRAATTIIOONN
       The block device name has the following form: ssdd_l_p_, where _l is a letter
       denoting  the  physical drive, and _p is a number denoting the partition
       on that physical drive.  Often, the partition number, _p, will  be  left
       off when the device corresponds to the whole drive.

       SCSI  disks  have a major device number of 8, and a minor device number
       of the form (16 * _d_r_i_v_e___n_u_m_b_e_r) + _p_a_r_t_i_t_i_o_n___n_u_m_b_e_r, where  _d_r_i_v_e___n_u_m_b_e_r
       is  the  number of the physical drive in order of detection, and _p_a_r_t_i_‐
       _t_i_o_n___n_u_m_b_e_r is as follows:

       •  partition 0 is the whole drive

       •  partitions 1–4 are the DOS "primary" partitions

       •  partitions 5–8 are the DOS "extended" (or "logical") partitions

       For example, _/_d_e_v_/_s_d_a will have major 8, minor 0, and will refer to all
       of the first SCSI drive in the system; and _/_d_e_v_/_s_d_b_3 will have major 8,
       minor 19, and will refer to the third DOS "primary"  partition  on  the
       second SCSI drive in the system.

       At  this  time,  only block devices are provided.  Raw devices have not
       yet been implemented.

DDEESSCCRRIIPPTTIIOONN
       The following _i_o_c_t_ls are provided:

       HHDDIIOO__GGEETTGGEEOO
              Returns the BIOS disk parameters in the following structure:

           struct hd_geometry {
               unsigned char  heads;
               unsigned char  sectors;
               unsigned short cylinders;
               unsigned long  start;
           };

              A pointer to this structure is passed as the iiooccttll(2) parameter.

              The information returned in the parameter is the  disk  geometry
              of  the  drive  _a_s  _u_n_d_e_r_s_t_o_o_d _b_y _D_O_S_!  This geometry is _n_o_t the
              physical geometry of the drive.  It is  used  when  constructing
              the  drive’s  partition table, however, and is needed for conve‐
              nient operation of ffddiisskk(1), eeffddiisskk(1),  and  lliilloo(1).   If  the
              geometry information is not available, zero will be returned for
              all of the parameters.

       BBLLKKGGEETTSSIIZZEE
              Returns  the  device  size  in  sectors.  The iiooccttll(2) parameter
              should be a pointer to a _l_o_n_g.

       BBLLKKRRRRPPAARRTT
              Forces a reread of the SCSI disk partition tables.  No parameter
              is needed.

              The  SCSI  iiooccttll(2)  operations  are  also  supported.   If  the
              iiooccttll(2)  parameter  is  required, and it is NULL, then iiooccttll(2)
              fails with the error EEIINNVVAALL.

FFIILLEESS
       _/_d_e_v_/_s_d_[_a_-_h_]
              the whole device

       _/_d_e_v_/_s_d_[_a_-_h_]_[_0_-_8_]
              individual block partitions

Linux man‐pages (unreleased)      2025‐09‐21                             _s_d(4)
