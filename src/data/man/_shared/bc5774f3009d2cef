The  utility  reads the given and breaks it up into files of 1000
lines each.  If is a single dash or absent, reads from the  stan‐
dard  input.  itself is not altered.  The options are as follows:
Use letters to form the suffix of the file name.  Create  smaller
files bytes in length.  If is appended to the number, the file is
split  into  kilobyte  pieces.  If is appended to the number, the
file is split into megabyte pieces.  Create smaller  files  lines
in  length.   Split file into smaller files.  If additional argu‐
ments are specified, the first is used as the name of  the  input
file  which  is  to be split.  If a second additional argument is
specified, it is used as a prefix for the names of the files into
which the file is split.  In this case, each file into which  the
file  is split is named by the prefix followed by a lexically or‐
dered suffix using characters in the range If is  not  specified,
two letters are used as the suffix.  If the argument is not spec‐
ified,  is  used.   The utility conforms to A command appeared in
The option was introduced in Before that, if was  not  specified,
would  vary the first letter of the filename to increase the num‐
ber of possible output files.  The option makes this unnecessary.
