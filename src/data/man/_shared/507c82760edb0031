services  Maintenance  Operations  Protocol  Load requests on the
Ethernet connected to or all interfaces if option is given.  In a
load request received by a filename can be given by  the  client.
This  is  the  normal  case  for terminal servers.  If a filename
isn’t in the client load request must know what  image  to  load.
Upon  receiving a request, checks if the requested file exists in
(unless the option is given, see below) the filename is  normally
uppercase  and  with an extension of If the filename isn’t given,
the ethernet address of the target is used as filename, e.g.  and
it might be a soft link to another file.  supports two  kinds  of
files.  The first type that is check is if the file is in format.
If not, a couple of Digital’s formats are checked.  In normal op‐
eration,  forks  a  copy  of  itself  and runs in the background.
Anomalies and errors are reported via Listen on all the Ethernets
attached to the system.  If is  omitted,  an  interface  must  be
specified.   Run  in  debug  mode, with all the output to stdout.
The process will run in the foreground.  Run in  the  foreground.
Change  the  directory to look for files in from to automatically
appends an upper case to the filename  provided  by  the  client.
The typical client sends the requested file name in upper case.
