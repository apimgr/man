DWARF_SET_RELOC_APPLI... BSD Library Functions Manual DWARF_SET_RELOC_APPLI...

NNAAMMEE
     ddwwaarrff__sseett__rreelloocc__aapppplliiccaattiioonn — set a library-wide relocation flag

LLIIBBRRAARRYY
     DWARF Access Library (libdwarf, -ldwarf)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<lliibbddwwaarrff..hh>>

     _i_n_t
     ddwwaarrff__sseett__rreelloocc__aapppplliiccaattiioonn(_i_n_t _a_p_p_l_y);

DDEESSCCRRIIPPTTIIOONN
     Function ddwwaarrff__sseett__rreelloocc__aapppplliiccaattiioonn() allows applications to specify how
     relocation information is to be handled by the DWARF(3) library.

     If the argument _a_p_p_l_y holds a non-zero value, the library will process
     all the relevant “.rel” and “.rela” relocation sections and will apply
     the relocation records found to their corresponding DWARF sections.

     If the argument _a_p_p_l_y is zero, the library will not attempt to apply any
     relocations.

     The default behaviour of the library is to process relocation records.

NNOOTTEESS
     Function ddwwaarrff__sseett__rreelloocc__aapppplliiccaattiioonn() should be called before initialis‐
     ing a dwarf debugging context, i.e, it should be called by the applica‐
     tion before calling either of the functions dwarf_init(3) or
     dwarf_elf_init(3).

RREETTUURRNN VVAALLUUEESS
     Function ddwwaarrff__sseett__rreelloocc__aapppplliiccaattiioonn() returns the previous value of the
     library-wide relocation application flag.

EERRRROORRSS
     Function ddwwaarrff__sseett__rreelloocc__aapppplliiccaattiioonn() does not return an error.

SSEEEE AALLSSOO
     dwarf(3), dwarf_elf_init(3), dwarf_init(3)

BSD                            February 11, 2015                           BSD
