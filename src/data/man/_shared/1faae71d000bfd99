tells which server supplies services to a client, or which is the
master  for  a  map.   If invoked without arguments, it gives the
server for the local machine.  If is specified, that  machine  is
queried to find out which server it is using.  The options are as
follows:  Specify  a  domain other than the default domain.  When
used in conjunction with force to query directly,  without  using
the  local  copy  of Specify a host other than localhost to query
for information.  Find the master server for the named map.   can
be a map name or nickname.  If is omitted, will produce a list of
available maps.  Use TCP protocol instead of UDP.  Inhibit trans‐
lation  of  map nicknames to their corresponding map names.  Dis‐
play the map nickname table, found in A built‐in default transla‐
tion table is used if does not exist (see  for  details).   exits
with  a non‐zero exit code if is used, and there was a problem in
determining the map’s master.
