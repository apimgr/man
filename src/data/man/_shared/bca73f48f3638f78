SMTP(1)                   BSD General Commands Manual                  SMTP(1)

NNAAMMEE
     ssmmttpp — Simple Mail Transfer Protocol client

SSYYNNOOPPSSIISS
     ssmmttpp [--CChhnnvv] [--aa _a_u_t_h_f_i_l_e] [--FF _f_r_o_m] [--HH _h_e_l_o] [--ss _s_e_r_v_e_r] [--TT _p_a_r_a_m_s]
          [_r_e_c_i_p_i_e_n_t _._._.]

DDEESSCCRRIIPPTTIIOONN
     The ssmmttpp utility is a Simple Mail Transfer Protocol (SMTP) client which
     can be used to run an SMTP transaction against an SMTP server.

     By default, ssmmttpp reads the mail content from the standard input, estab‐
     lishes an SMTP session, and runs an SMTP transaction for all the speci‐
     fied recipients.  The content is sent unaltered as mail data.

     The options are as follows:

     --aa _a_u_t_h_f_i_l_e
             Perform a login before sending the message.  The username and
             password are read from _a_u_t_h_f_i_l_e and need to be on the first and
             second line respectively.  This option requires a TLS or STARTTLS
             _s_e_r_v_e_r.

     --CC      Do not require server certificate to be valid.  This flag is dep‐
             recated.  Use “--TT nnoovveerriiffyy” instead.

     --FF _f_r_o_m
             Set the return-path (MAIL FROM) for the SMTP transaction.  De‐
             fault to the current username.

     --HH _h_e_l_o
             Define the hostname to advertise (HELO) when establishing the
             SMTP session.

     --hh      Display usage.

     --nn      Do not actually execute a transaction, just try to establish an
             SMTP session and quit.  When this option is given, no message is
             read from the standard input.

     --ss _s_e_r_v_e_r
             Specify the server to connect to and connection parameters.  The
             format is [_p_r_o_t_o://[_u_s_e_r:_p_a_s_s@]]_h_o_s_t[:_p_o_r_t].  The following pro‐
             tocols are available:

             smtp        Normal SMTP session with opportunistic STARTTLS.
             smtp+tls    Normal SMTP session with mandatory STARTTLS.
             smtp+notls  Plain text SMTP session without TLS.
             lmtp        LMTP session with opportunistic STARTTLS.
             lmtp+tls    LMTP session with mandatory STARTTLS.
             lmtp+notls  Plain text LMTP session without TLS.
             smtps       SMTP session with forced TLS on connection.

             Defaults to “smtp://localhost:25”.

     --TT _p_a_r_a_m_s
             Set specific parameters for TLS sessions.  The _p_a_r_a_m_s string is a
             comma or space separated list of options.  The available options
             are:

             ccaaffiillee=_f_i_l_e_n_a_m_e
                     Use _f_i_l_e_n_a_m_e as root certificates file instead of the
                     system default.

             cciipphheerrss=_v_a_l_u_e
                     Specify the allowed ciphers.  Refer to
                     tls_config_set_ciphers(3) for _v_a_l_u_e.

             nnoossnnii   Disable Server Name Indication (SNI).

             nnoovveerriiffyy
                     Do not require server certificate to be valid.

             pprroottooccoollss=_v_a_l_u_e
                     Specify the protocols to use.  Refer to
                     tls_config_parse_protocols(3) for _v_a_l_u_e.

             sseerrvveerrnnaammee=_v_a_l_u_e
                     Use _v_a_l_u_e for Server Name Indication (SNI).  Defaults to
                     the specified server hostname.

     --vv      Be more verbose.  This option can be specified multiple times.

SSEEEE AALLSSOO
     smtpd(8)

HHIISSTTOORRYY
     The ssmmttpp program first appeared in OpenBSD 6.4.

BSD                              May 23, 2021                              BSD
