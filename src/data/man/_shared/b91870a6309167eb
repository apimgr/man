The  utility sorts text files by lines.  Comparisons are based on
one or more sort keys extracted from each line of input, and  are
performed  lexicographically.  By default, if keys are not given,
regards each input line as a single field.  The following options
are available: Identical to without the  error  messages  in  the
case  of  unsorted  input.   Check  that the single input file is
sorted.  If the file is not sorted, produces the appropriate  er‐
ror  messages  and exits with code 1; otherwise, returns 0.  pro‐
duces no output.  See also Ignored for compatibility with earlier
versions of Merge only; the input files are assumed  to  be  pre‐
sorted.  The argument given is the name of an file to be used in‐
stead  of  the standard output.  This file can be the same as one
of the input files.  Don’t use stable sort.  Default  is  to  use
stable  sort.   Use stable sort, keeps records with equal keys in
their original order.  This is the default.  Provided for compat‐
ibility with other implementations only.  Use  as  the  directory
for  temporary  files.  The default is the value specified in the
environment variable if is not defined.  Unique: suppress all but
one in each set of lines having equal keys.  If used with the op‐
tion, check that there are no lines  with  duplicate  keys.   The
following  options,  which  should  be  given before any options,
override the default ordering rules.  When ordering  options  ap‐
pear  independent  of,  and before, key field specifications, the
requested field ordering rules are applied globally to  all  sort
keys.   When attached to a specific key (see the ordering options
override all global ordering options for that  key.   Only  blank
space and alphanumeric characters are used in making comparisons.
Considers  all  lowercase  characters that have uppercase equiva‐
lents to be the same for purposes of comparison.  Ignore all non‐
printable characters.  Sort by the string length  of  the  field,
not  by  the field itself.  An initial numeric string, consisting
of optional blank space, optional plus or minus sign, and zero or
more digits (including decimal point)  is  sorted  by  arithmetic
value.   (The  option no longer implies the option.)  Reverse the
sense of comparisons.  The treatment of field separators  can  be
altered using these options: Ignores leading blank space when de‐
termining  the  start and end of a restricted sort key.  A option
specified before the first option applies  globally  to  all  op‐
tions.   Otherwise,  the  option can be attached independently to
each argument of the option (see below).  Note  that  the  option
has  no  effect  unless key fields are specified.  Designates the
starting position, and optional ending position, of a key  field.
The  option  replaces  the obsolescent options and is used as the
record separator character.  This should be used with discretion;
usually produces undesirable results.  If char is  not  a  single
character, then it specifies the value of the desired record sep‐
arator as an integer specified in any of the normal NNN, 0ooo, or
0xXXX  ways, or as an octal value preceded by \.  Caution: do not
attempt to specify Ctl‐A as which will not do what  was  intended
at all!  The default record separator is newline.  is used as the
field  separator  character.  The initial is not considered to be
part of a field when determining key offsets (see  below).   Each
occurrence  of  is  significant  (for  example, delimits an empty
field).  If is not specified, the default field  separator  is  a
sequence  of blank‐space characters, and consecutive blank spaces
do delimit an empty field; further, the initial blank space  con‐
sidered  part  of a field when determining key offsets.  The fol‐
lowing operands are available: The  pathname  of  a  file  to  be
sorted,  merged, or checked.  If no operands are specified, or if
a operand is the standard input is used.  A field is defined as a
minimal sequence of characters followed by a field separator or a
newline character.  By default, the first blank space  of  a  se‐
quence  of  blank  spaces acts as the field separator.  All blank
spaces in a sequence of blank spaces are considered  as  part  of
the next field; for example, all blank spaces at the beginning of
a  line are considered to be part of the first field.  Fields are
specified by the argument.  A missing argument  defaults  to  the
end  of  a line.  The arguments and have the form and can be fol‐
lowed by one or more of the letters and which correspond  to  the
options  discussed above.  A position specified by is interpreted
as the character in the field.  A missing in means indicating the
first character of the field; if the  option  is  in  effect,  is
counted  from  the first non‐blank character in the field; refers
to the first non‐blank character in the field.  A position speci‐
fied by is interpreted as the character (including separators) of
the field.  A missing indicates the last character of the  field;
=  0 designates the end of a line.  Thus the option is synonymous
with the obsolescent option when is omitted, is  synonymous  with
The  obsolescent  option is still supported, except for which has
no equivalent.  compares records by comparing the key fields  se‐
lected  by arguments, from first given to last, until discovering
a difference.  If there are no arguments,  the  whole  record  is
treated  as  a single key.  After exhausting the arguments, if no
difference has been found, then the result depends upon  the  and
option  settings.  With the records are considered identical, and
one is suppressed.  Otherwise with set (default) the records  are
left  in  their  original  order,  or with (posix mode) the whole
record is considered as a tie breaker.  If the following environ‐
ment variable exists, it is used by uses the contents of the  en‐
vironment variable as the path in which to store temporary files.
Default temporary files.  Temporary file which is used for output
if  already exists.  Once sorting is finished, this file replaces
(via and Sort exits with one of the following values: Normal  be‐
havior.  On disorder (or non‐uniqueness) with the (or option.  An
error  occurred.   A  command appeared in This implementation ap‐
peared in and is used since Posix requires the locale’s thousands
separator be ignored in numbers.  It may be faster to  sort  very
large  files  in pieces and then explicitly merge them.  This has
no limits on input line length (other than imposed  by  available
memory)  or  any  restrictions on bytes allowed within lines.  To
protect data calls and and thus fails on  protected  directories.
Input  files  should  be  text  files.   If file doesn’t end with
record  separator  (which  is  typically  newline),  the  utility
silently  supplies  one.   The  current  uses lexicographic radix
sorting, which requires that sort keys be kept in memory (as  op‐
posed  to  previous versions which used quick and merge sorts and
did not.)  Thus performance depends highly on efficient choice of
sort keys, and the option and the argument of the  option  should
be  used  whenever possible.  Similarly, is equivalent to and may
take twice as long.
