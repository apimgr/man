The  file(s) are copied (or linked if the option is specified) to
the target file or directory.  If the destination is a directory,
then the is copied into with its original filename.  If the  tar‐
get file already exists, it is either renamed to if the option is
given  or  overwritten  if permissions allow; an alternate backup
suffix may be specified via the option’s argument.   Run  on  the
target  after  installation  and  stripping but before ownership,
permissions or timestamps are set and before renaming occurs.  is
invoked via the shell, allowing a single argument be to specified
to which the shell can then tokenize.  Use as the  backup  suffix
if  is  given.  If contains a ’%’ sign, a numbered backup will be
performed, and the %‐pattern will be expanded using given an  in‐
teger counter as the backup number.  The counter used starts from
0,  and  the first available name resulting from the expansion is
used.  Backup any existing files before overwriting them  by  re‐
naming  them  to  for specifying a different backup suffix.  Copy
the file.  This is the default behavior; the flag  is  maintained
for  backwards  compatibility only.  Specify the (top of the file
hierarchy) that the items are installed in to.  If is in  use,  a
leading  string  of will be removed from the file names logged to
the This option does not affect where the actual  files  are  in‐
stalled.   Create  directories.   Missing  parent directories are
created as required.  Specify the target’s file flags.  (See  for
a  list  of possible flags and their meanings.)  Specify a group.
When copying, calculate the digest of the files with to store  in
the  Supported  digests:  No hash.  This is the default.  The MD5
cryptographic message digest.  The RMD‐160 cryptographic  message
digest.   The  SHA‐1  cryptographic message digest.  The 256‐bits
cryptographic message digest of the file.  The  384‐bits  crypto‐
graphic  message  digest of the file.  The 512‐bits cryptographic
message digest of the file.  Instead of copying the file  make  a
link  to  the  source.  The type of the link is determined by the
argument.  Valid are: (absolute), (relative), (hard), (symbolic),
(mixed).  Absolute and relative have  effect  only  for  symbolic
links.  Mixed links are hard links for files on the same filesys‐
tem, symbolic otherwise.  Write the metadata associated with each
item  installed  to  in  an specification line.  The metadata in‐
cludes: the file name and file type, and depending upon other op‐
tions, the owner, group, file flags, modification time, and tags.
Specify an alternative mode.  The default mode is set to rwxr‐xr‐
x (0755).  The specified mode may be either an octal or  symbolic
value;  see  for  a description of possible mode values.  Use the
user database text file and group database text file from  rather
than  using  the  results from the system’s and (and related) li‐
brary calls.  Specify an owner.  Preserve the source files access
and modification times.  Install to a temporary file and then re‐
name the file to its final destination name.  This  can  be  used
for  precious files, to avoid truncation of the original when er‐
ror conditions (filesystem full etc.) occur.   passes  as  option
arguments  to  When is used, is invoked via the shell, allowing a
single argument be to specified to which the shell can then  tok‐
enize.  Normally, invokes directly.  This flag implies exec’s the
command  to strip binaries so that install can be portable over a
large number of systems and binary  types.   If  the  environment
variable  is set, it is used as the program.  Specify the tags to
write out for the file when using Indicate that install  is  run‐
ning  unprivileged,  and  that  it  should  not try to change the
owner, the group, or the file flags of the destination.  The  in‐
formation  that  would  have  been updated can be stored in a log
file with By default, preserves all file flags, with  the  excep‐
tion  of  the  ‘‘nodump’’  flag.  The utility attempts to prevent
copying a file onto itself.  Installing creates  an  empty  file.
The  program  used to strip installed binaries when the option is
used.  If unspecified, is used.  The utility appeared in
