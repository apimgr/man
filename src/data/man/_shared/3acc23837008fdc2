asks  a server process for the order number and which host is the
master server for The options are as follows:  Use  TCP  protocol
instead  of  UDP  for  request if the option is present.  Ask the
server process running on for information about If is not  speci‐
fied, the server polled is the default server returned by Use the
domain instead of the default domain as returned by Theo de Raadt
and John Brezak
