and are filters that invoke or respectively to compare compressed
files.  Any that are specified are passed to or If only is speci‐
fied,  it is compared against a file with the same name, but with
the extension removed.  When both or are specified,  either  file
may  be  compressed.   Extensions  handled by z, Z, gz, taz, tgz.
Extensions handled by bz, bz2, tbz, tbz2.  Extensions handled  by
lzma, xz, tlz, txz.  Directory in which to place temporary files.
If  unset,  is  used.   Temporary file for Temporary file for and
rely solely on the file extension to determine  what  is,  or  is
not, a compressed file.  Consequently, the following are not sup‐
ported  as  arguments: directories device special files filenames
indicating the standard input
