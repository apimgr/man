MQ_CLOSE(2)                 BSD System Calls Manual                MQ_CLOSE(2)

NNAAMMEE
     mmqq__cclloossee — close a message queue (REALTIME)

LLIIBBRRAARRYY
     POSIX Real-time Library (librt, -lrt)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<mmqquueeuuee..hh>>

     _i_n_t
     mmqq__cclloossee(_m_q_d___t _m_q_d_e_s);

DDEESSCCRRIIPPTTIIOONN
     The mmqq__cclloossee() system call removes the association between the message
     queue descriptor, _m_q_d_e_s, and its message queue.  The results of using
     this message queue descriptor after successful return from this
     mmqq__cclloossee(), and until the return of this message queue descriptor from a
     subsequent mmqq__ooppeenn(), are undefined.

     If the process has successfully attached a notification request to the
     message queue via this _m_q_d_e_s, this attachment will be removed, and the
     message queue is available for another process to attach for notifica‐
     tion.

RREETTUURRNN VVAALLUUEESS
     Upon successful completion, the value 0 is returned; otherwise the
     value -1 is returned and the global variable _e_r_r_n_o is set to indicate the
     error.

EERRRROORRSS
     The mmqq__cclloossee() system call will fail if:

     [EBADF]            The _m_q_d_e_s argument is not a valid message queue de‐
                        scriptor.

SSEEEE AALLSSOO
     mq_open(2), mq_unlink(2)

SSTTAANNDDAARRDDSS
     The mmqq__cclloossee() system call conforms to IEEE Std 1003.1-2004 (“POSIX.1”).

HHIISSTTOORRYY
     Support for POSIX message queues first appeared in FreeBSD 7.0.

CCOOPPYYRRIIGGHHTT
     Portions of this text are reprinted and reproduced in electronic form
     from IEEE Std 1003.1, 2004 Edition, Standard for Information Technology
     -- Portable Operating System Interface (POSIX), The Open Group Base Spec‐
     ifications Issue 6, Copyright (C) 2001-2004 by the Institute of Electri‐
     cal and Electronics Engineers, Inc and The Open Group.  In the event of
     any discrepancy between this version and the original IEEE and The Open
     Group Standard, the original IEEE and The Open Group Standard is the ref‐
     eree document.  The original Standard can be obtained online at
     http://www.opengroup.org/unix/online.html.

BSD                            November 29, 2005                           BSD
