will  take a raw disk device that covers the whole disk or a file
and will find all possible FFSv[12]/LFS  partitions,  independent
of  block sizes on it.  It will show the file system type (FFSv1,
FFSv2, or LFS), size, and offset.  Also it has an option to  show
the  values  with  a  disklabel‐alike output.  The options are as
follows: Report every superblock found with its  sector  address,
rather  than trying to report the partition boundaries.  This op‐
tion can be useful to find the other superblocks in  a  partition
if  the first superblock has become corrupted.  It is most useful
if refers to the raw device for the partition,  rather  than  the
entire  disk.  Where to stop searching for file systems.  The ar‐
gument specifies the last sector that will be searched.   Default
is  the  last sector of Path to a file containing possible parti‐
tions inside of it.  Print out a string looking much like the in‐
put to disklabel.  With a little massaging, this output can  usu‐
ally  be used by Where to start searching for file systems.  This
makes it easier to skip  swap  partitions  or  other  large  non‐
UFS/FFS partitions.  The argument specifies the first sector that
will  be  searched.   Default  is  the first sector of Be verbose
about what is doing, and what has been found.  The argument spec‐
ifies which device should scan for file systems.  is just another
name for the same program, both behave in exactly the  same  way.
The  program  first appeared in and then in Support for searching
in files was added in was written for by and It was ported to  by
who  added support for LFS/FFSv2, partitions with fragsize/block‐
size greater than 2048/16384 for FFSv1, searching on files,  etc.
Currently  won’t  find partitions with fragsize/blocksize greater
than 8192/65536.
