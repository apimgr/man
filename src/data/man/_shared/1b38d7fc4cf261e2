The  modules  provide  support for devices.  The module group in‐
cludes tuners, demodulators,  and  analog  video  decoders.   The
usual hardware structure consists of a host controller (connected
for instance via or that provides the data moving facilities (for
the  analog  or  digital video streams) and an bus over which the
other chips can be configured.   For  example,  a  typical  setup
would  look  like  this:  Initialize transport stream port on the
host controller.  Configure the demodulator that the  information
from  an intermediate frequency that is provided to it by the as‐
sociated tuner.  This step typically includes setting the desired
quadrature  amplitude  modulation  and  bandwidth,  among   other
things.   Configure  the  tuner.  Typically this step includes at
least setting the frequency.  Start TS transfer (using  transfers
or  high  speed  transfers).  The supported demodulators include:
Auvitek AU8522  ATSC/QAM64/QAM256  demodulator  Conexant  CX24227
ATSC/QAM64/QAM256  demodulator  LG LGDT3303 ATSC/QAM64/QAM256 de‐
modulator MicroTune MT2131 ATSC  demodulator  NxtWave  Communica‐
tions  NXT2004  ATSC  demodulator  Zarlink ZL10353 (Intel CE623x)
COFDM/DVB‐T demodulator The supported tuners  are:  Generic  PLL‐
based tuners XCeive XC3028 analog and digital tuner Xceive XC5000
analog  and  digital  tuner  needed  by or (provided by needed by
needed by provided by the The modules first appeared  in  Authors
include and
