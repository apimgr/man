changes  the  current  thread’s effective, real, and saved userid
and groupid to the requested userid and groupid ( and  ,  respec‐
tively)  and  clears all other groupids.  can be the current real
userid, KAUTH_UID_NONE, or, if  the  caller  is  privileged,  any
userid.   can  be  the  current real groupid or, if the caller is
priviledged, any single groupid.  Setting to KAUTH_UID_NONE means
to "revert to the per process credential".  Temporarily restoring
root privileges for a non‐privileged process is only possible  on
a  per‐process basis and not a per‐thread basis.  is not intended
as a privilege escalation mechanism.  Do not use  in  a  security
sensitive situation.  Upon successful completion, a value of 0 is
returned.   Otherwise,  ‐1 is returned and the global variable is
set to indicate the error.  fails if one or more of the following
are true: The calling process does not have the  correct  creden‐
tials  to set the override identity (i.e. The current credentials
do not imply "super‐user").  If is  set  to  KAUTH_UID_NONE,  the
current thread must already be assuming another identity in order
to  revert  back.   The current thread cannot already be assuming
another identity.
