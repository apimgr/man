The  program  displays  and modifies the Internet‐to‐Ethernet ad‐
dress translation tables used by the address resolution  protocol
With  no  flags,  the  program displays the current entry for The
host may be specified by name or by number,  using  Internet  dot
notation.  The state of the arp entry is shown as a single letter
preceded  by  the  time the state is applicable for: Delay Incom‐
plete Nostate Probe Reachable Stale  Unreachable  Waitdelete  Un‐
known  state  (should never happen).  Available options: The pro‐
gram displays all of  the  current  entries.   A  super‐user  may
delete  an  entry for the host called with the flag.  If the key‐
word is specified, only the published entry for this host will be
deleted.  If used with instead of a it will delete  all  arp  en‐
tries.  Causes the file to be read and multiple entries to be set
in  the  tables.   Entries in the file should be of the form with
argument meanings as described below.  Show network addresses  as
numbers  (normally  attempts  to display addresses symbolically).
Create an entry for the host called with the Ethernet address The
Ethernet address is given as six hex bytes separated  by  colons.
The  entry will be permanent unless the word is given in the com‐
mand.  If the word is given, the entry will be "published"; i.e.,
this system will act as an server,  responding  to  requests  for
even though the host address is not its own.  If the word is also
given, the published entry will be a entry.  In this case the can
be given as in which case the interfaces on this host will be ex‐
amined,  and  if  one of them is found to occupy the same subnet,
its Ethernet address will be used.  Display  verbose  information
when adding or deleting entries.  The command appeared in
