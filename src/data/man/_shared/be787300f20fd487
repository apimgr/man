BEINSTALL.SH(8)           BSD System Manager's Manual          BEINSTALL.SH(8)

NNAAMMEE
     bbeeiinnssttaallll..sshh — install a boot environment using the current FreeBSD
     source tree

SSYYNNOOPPSSIISS
     bbeeiinnssttaallll..sshh [_o_p_t_i_o_n_s _._._.]

DDEESSCCRRIIPPTTIIOONN
     bbeeiinnssttaallll..sshh installs a boot environment using the current FreeBSD source
     tree.  bbeeiinnssttaallll..sshh also automatically performs _/_e_t_c updates (using ei‐
     ther etcupdate(8) or mergemaster(8)) and package updates using
     pkg-upgrade(8) automatically in the new boot environment sandbox.

     Upon successful completion, the system will be ready to boot into the new
     boot environment.  Upon failure, the target boot environment will be de‐
     stroyed.  In all cases, the running system is left untouched and a reboot
     into a partially updated system (due to install or hardware failure) can‐
     not happen.  Additionally, the full installation process requires only
     one reboot as it is performed in a new boot environment.

     bbeeiinnssttaallll..sshh requires a fully built world and kernel.  It also requires
     pkg(8), which is not present in the base system and has to be installed
     manually.

     The _o_p_t_i_o_n_s provided to bbeeiinnssttaallll..sshh are world and kernel flags like
     KERNCONF as described in build(7).

EENNVVIIRROONNMMEENNTT
     User modifiable variables.  Set these in the environment if desired:

     BE_UTILITY (default: “bectl”)
             Utility to manage ZFS boot environments.  This can be either
             bectl(8) from the base system or beadm(1) from ports (sysu‐
             tils/beadm).

     CONFIG_UPDATER (default: “etcupdate”)
             Config updater: etcupdate(8) and mergemaster(8) are supported.
             Set to an empty string to skip.

     ETCUPDATE_FLAGS (default: “-F”)
             Flags for etcupdate(8) if used.

     MERGEMASTER_FLAGS (default: “-iFU”)
             Flags for mergemaster(8) if used.

     NO_PKG_UPGRADE (default: “”)
             If not empty, “pkg upgrade” will be skipped.

FFIILLEESS
     ssrrcc//tools/build/beinstall.sh
             Place where bbeeiinnssttaallll..sshh lives in the src tree.

SSEEEE AALLSSOO
     build(7), development(7), bectl(8), etcupdate(8), mergemaster(8), pkg(8)

HHIISSTTOORRYY
     bbeeiinnssttaallll..sshh is inspired by and similar in function to Solaris/illumos-
     style upgrades.

     The bbeeiinnssttaallll..sshh manual page first appeared in FreeBSD 12.0.

AAUUTTHHOORRSS
     The bbeeiinnssttaallll..sshh script was implemented by Will Andrews
     <_w_i_l_l_@_F_r_e_e_B_S_D_._o_r_g>.  This manual page was written by
     Mateusz Piotrowski <_0_m_p_@_F_r_e_e_B_S_D_._o_r_g>.

BSD                            October 30, 2020                            BSD
