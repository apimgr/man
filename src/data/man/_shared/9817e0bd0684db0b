The  command  invokes  file system‐specific programs to check the
special devices listed in the file or in  the  command  line  for
consistency.   It is normally used in the script during automatic
reboot.  If no file systems are specified, and mode is enabled  (
option) reads the table to determine which file systems to check,
in what order.  Only partitions in fstab that are mounted ‘‘rw,’’
‘‘rq’’  or ‘‘ro’’ and that have non‐zero pass number are checked.
File systems with pass number 1 (normally just the root file sys‐
tem) are checked one at a time.  When pass 1 completes,  all  re‐
maining  file  systems  are checked, running one process per disk
drive.  By default, file systems which are already mounted  read‐
write  are not checked.  The disk drive containing each file sys‐
tem is inferred from the longest prefix of the device  name  that
ends  in  a digit; the remaining characters are assumed to be the
partition designator.  The  options  are  as  follows:  Debugging
mode.   Just  print  the  commands without executing them.  Force
checking of file systems, even when they are  marked  clean  (for
file  systems  that support this), or when they are mounted read‐
write.  Limit the number of parallel checks to the number  speci‐
fied  in  the  following  argument.  By default, the limit is the
number of disks, running one process  per  disk.   If  a  smaller
limit  is given, the disks are checked round‐robin, one file sys‐
tem at a time.  Causes to assume no as the answer to all operator
questions, except "CONTINUE?".  Display a progress meter for each
file system check.  This option also disables parallel  checking.
Note  that  progress  meters are not supported by all file system
types.  Enter preen mode.  In preen mode,  will  check  all  file
systems  listed  in according to their pass number, and will make
minor repairs without human intervention.   Quiet  mode,  do  not
output  any messages for clean file systems.  List of comma sepa‐
rated file system specific options for the specified file  system
type,  in  the same format as Invoke only for the comma separated
list of file system types.  If the list starts with  then  invoke
for  the  file  system  types that are not specified in the list.
Print the commands before executing them.  Exclude the file  sys‐
tem  which has a the same as in Used only in mode.  Causes to as‐
sume yes as the answer to all operator  questions.   file  system
table  exits  with  on success.  Any major problems will cause to
exit with the following non‐zero codes, so as to alert any invok‐
ing program or script that human intervention is required.  Usage
problem.  Unresolved errors while checking the file system.   Re‐
running  on the file system(s) is required.  The root file system
was changed in the process of checking, and  updating  the  mount
was unsuccessful.  A reboot (without sync) is required.  The file
system  check has failed, and a subsequent check is required that
will require human intervention.  exited because of the result of
a signal (usually or from the terminal).  An utility appeared  in
It  was reimplemented as a file system independent wrapper in The
original file system specific utility became at this point.
