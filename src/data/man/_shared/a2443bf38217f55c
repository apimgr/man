cpu_machdep(9)           BSD Kernel Developer's Manual          cpu_machdep(9)

NNAAMMEE
     ccppuu__mmaacchhddeepp, ccppuu__ccooppyy__tthhrreeaadd, ccppuu__eexxeecc__vvmmssppaaccee__rreeuussee, ccppuu__eexxiitt,
     ccppuu__ffeettcchh__ssyyssccaallll__aarrggss, ccppuu__ffoorrkk, ccppuu__ffoorrkk__kktthhrreeaadd__hhaannddlleerr, ccppuu__iiddllee,
     ccppuu__iiddllee__wwaakkeeuupp, ccppuu__pprrooccccttll, ccppuu__sseett__ssyyssccaallll__rreettvvaall, ccppuu__sseett__uuppccaallll,
     ccppuu__sseett__uusseerr__ttllss, ccppuu__sswwiittcchh, ccppuu__ssyynncc__ccoorree, ccppuu__tthhrreeaadd__aalllloocc,
     ccppuu__tthhrreeaadd__cclleeaann, ccppuu__tthhrreeaadd__eexxiitt, ccppuu__tthhrreeaadd__ffrreeee, ccppuu__tthhrrooww — machine-
     dependent interfaces to handle CPU and thread state

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//pprroocc..hh>>
     ##iinncclluuddee <<ssyyss//ppttrraaccee..hh>>

     _v_o_i_d
     ccppuu__ccooppyy__tthhrreeaadd(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d, _s_t_r_u_c_t _t_h_r_e_a_d _*_t_d_0);

     _b_o_o_l
     ccppuu__eexxeecc__vvmmssppaaccee__rreeuussee(_s_t_r_u_c_t _p_r_o_c _*_p, _s_t_r_u_c_t _v_m___m_a_p _*_m_a_p);

     _v_o_i_d
     ccppuu__eexxiitt(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d);

     _i_n_t
     ccppuu__ffeettcchh__ssyyssccaallll__aarrggss(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d);

     _v_o_i_d
     ccppuu__ffoorrkk(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d_1, _s_t_r_u_c_t _p_r_o_c _*_p_2, _s_t_r_u_c_t _t_h_r_e_a_d _*_t_d_2,
         _i_n_t _f_l_a_g_s);

     _v_o_i_d
     ccppuu__ffoorrkk__kktthhrreeaadd__hhaannddlleerr(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d, _v_o_i_d _(_*_f_u_n_c_)_(_v_o_i_d _*_),
         _v_o_i_d _*_a_r_g);

     _v_o_i_d
     ccppuu__iiddllee(_i_n_t _b_u_s_y);

     _i_n_t
     ccppuu__iiddllee__wwaakkeeuupp(_i_n_t _c_p_u);

     _i_n_t
     ccppuu__pprrooccccttll(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d, _i_n_t _i_d_t_y_p_e, _i_d___t _i_d, _i_n_t _c_o_m, _v_o_i_d _*_d_a_t_a);

     _i_n_t
     ccppuu__ppttrraaccee(_s_t_r_u_c_t _t_h_r_e_a_d _*___t_d, _i_n_t _r_e_q, _v_o_i_d _*_a_d_d_r, _i_n_t _d_a_t_a);

     _v_o_i_d
     ccppuu__sseett__ssyyssccaallll__rreettvvaall(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d, _i_n_t _e_r_r_o_r);

     _i_n_t
     ccppuu__sseett__uuppccaallll(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d, _v_o_i_d _(_*_e_n_t_r_y_)_(_v_o_i_d _*_), _v_o_i_d _*_a_r_g,
         _s_t_a_c_k___t _*_s_t_a_c_k);

     _i_n_t
     ccppuu__sseett__uusseerr__ttllss(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d, _v_o_i_d _*_t_l_s___b_a_s_e);

     _v_o_i_d
     ccppuu__sswwiittcchh(_s_t_r_u_c_t _t_h_r_e_a_d _*_o_l_d, _s_t_r_u_c_t _t_h_r_e_a_d _*_n_e_w, _s_t_r_u_c_t _m_t_x _*_m_t_x);

     _v_o_i_d
     ccppuu__ssyynncc__ccoorree(_v_o_i_d);

     _v_o_i_d
     ccppuu__tthhrreeaadd__aalllloocc(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d);

     _v_o_i_d
     ccppuu__tthhrreeaadd__cclleeaann(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d);

     _v_o_i_d
     ccppuu__tthhrreeaadd__eexxiitt(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d);

     _v_o_i_d
     ccppuu__tthhrreeaadd__ffrreeee(_s_t_r_u_c_t _t_h_r_e_a_d _*_t_d);

     _v_o_i_d
     ccppuu__tthhrrooww(_s_t_r_u_c_t _t_h_r_e_a_d _*_o_l_d, _s_t_r_u_c_t _t_h_r_e_a_d _*_n_e_w);

DDEESSCCRRIIPPTTIIOONN
     These functions provide architecture-specific implementations of machine-
     independent abstractions.

     ccppuu__eexxeecc__vvmmssppaaccee__rreeuussee() returns true if eexxeecc__nneeww__vvmmssppaaccee() can reuse an
     existing _s_t_r_u_c_t _v_m_s_p_a_c_e (_m_a_p) for the process _p during execve(2).  This
     is only invoked if _m_a_p is not shared with any other consumers.  If this
     returns false, eexxeecc__nneeww__vvmmssppaaccee() will create a new _s_t_r_u_c_t _v_m_s_p_a_c_e.

     ccppuu__eexxiitt() releases machine-dependent resources other than the address
     space for the process containing _t_d during process exit.

     ccppuu__ffoorrkk() copies and updates machine-dependent state (for example, the
     pcb and user registers) from the forking thread _t_d_1 in an existing
     process to the new thread _t_d_2 in the new process _p_2.  This function must
     set up the new thread's kernel stack and pcb so that _t_d_2 calls
     ffoorrkk__eexxiitt() when it begins execution passing a pointer to ffoorrkk__rreettuurrnn()
     as the _c_a_l_l_o_u_t argument and _t_d_2 as the _a_r_g argument.

     ccppuu__ffoorrkk__kktthhrreeaadd__hhaannddlleerr() adjusts a new thread's initial pcb and/or ker‐
     nel stack to pass _f_u_n_c and _a_r_g as the _c_a_l_l_o_u_t and _a_r_g arguments to
     ffoorrkk__eexxiitt().  This must be called before a new thread is scheduled to run
     and is used to set the “main” function for kernel threads.

     ccppuu__ccooppyy__tthhrreeaadd() copies machine-dependent state (for example, the pcb
     and user registers) from _t_d to _t_d_0 when creating a new thread in the same
     process.  This function must set up the new thread's kernel stack and pcb
     so that _t_d_0 calls ffoorrkk__eexxiitt() when it begins execution passing a pointer
     to ffoorrkk__rreettuurrnn() as the _c_a_l_l_o_u_t argument and _t_d_0 as the _a_r_g argument.

     ccppuu__sseett__uuppccaallll() updates a new thread's initial user register state to
     call _e_n_t_r_y with _a_r_g as the sole argument using the user stack described
     in _s_t_a_c_k.

     ccppuu__sseett__uusseerr__ttllss() sets a new thread's initial user thread pointer regis‐
     ter to reference the user TLS base pointer _t_l_s___b_a_s_e.

     ccppuu__ffeettcchh__ssyyssccaallll__aarrggss() fetches the current system call arguments for
     the native FreeBSD ABI from the current thread's user register state
     and/or user stack.  The arguments are saved in the _t_d___s_a member of _t_d.

     ccppuu__sseett__ssyyssccaallll__rreettvvaall() updates the user register state for _t_d to store
     system call error and return values.  If _e_r_r_o_r is 0, indicate success and
     return the two values in _t_d___r_e_t_v_a_l.  If _e_r_r_o_r is ERESTART, adjust the
     user PC to re-invoke the current system call after returning to user
     mode.  If _e_r_r_o_r is EJUSTRETURN, leave the current user register state un‐
     changed.  For any other value of _e_r_r_o_r, indicate error and return _e_r_r_o_r
     as the error code.

     ccppuu__iiddllee() waits for the next interrupt to occur on the current CPU.  If
     an architecture supports low power idling, this function should place the
     CPU into a low power state while waiting.  _b_u_s_y is a hint from the sched‐
     uler.  If _b_u_s_y is non-zero, the scheduler expects a short sleep, so the
     CPU should prefer low-latency over maximum power savings.  If _b_u_s_y is
     zero, the CPU should maximumize power savings including deferring unnec‐
     essary clock interrupts via ccppuu__iiddlleecclloocckk().

     ccppuu__iiddllee__wwaakkeeuupp() awakens the idle CPU with the ID _c_p_u from a low-power
     state.

     ccppuu__pprrooccccttll() handles any machine-dependent procctl(2) requests.

     ccppuu__ppttrraaccee() handles any machine-dependent ptrace(2) requests.

     ccppuu__sswwiittcchh() switches the current CPU between threads by swapping regis‐
     ter state.  This function saves the current CPU register state in the pcb
     of _o_l_d and loads register values from the pcb of _n_e_w before returning.
     While the pcb generally contains caller-save kernel register state, it
     can also contain user registers that are not saved in the trapframe.

     After saving the current CPU register state of _o_l_d, ccppuu__sswwiittcchh() stores
     _m_t_x in the _t_d___l_o_c_k member of _o_l_d transferring ownership of the old
     thread.  No data belonging to _o_l_d can be accessed after that store.
     Specifically, the old thread's kernel stack must not be accessed after
     this point.

     When SCHED_ULE is being used, this function must wait (via spinning) for
     the _t_d___l_o_c_k member of _n_e_w to change to a value not equal to _&_b_l_o_c_k_e_d___l_o_c_k
     before loading register values from _n_e_w or accessing its kernel stack.

     From the caller's perspective, ccppuu__sswwiittcchh() returns when _o_l_d is resched‐
     uled in the future, possibly on a different CPU.  However, the implemen‐
     tation of ccppuu__sswwiittcchh() returns immediately on the same CPU into the pre‐
     viously-saved context of _n_e_w.

     ccppuu__tthhrrooww() is similar to ccppuu__sswwiittcchh() but does not save any state for
     _o_l_d or write to the old thread's _t_d___l_o_c_k member.

     ccppuu__ssyynncc__ccoorree() ensures that all possible speculation and out-of-order
     execution is serialized on the current CPU.  Note that this is called
     from an IPI handler so only has to handle additional serialization beyond
     that provided by handling an IPI.

   TThhrreeaadd OObbjjeecctt LLiiffeeccyyccllee
     These functions support the management of machine-dependent thread state
     in conjunction with a thread object's lifecycle.

     The general model is that a thread object is allocated each time a new
     kernel thread is created either by system calls like fork(2) or
     thr_new(2) or when kernel-only threads are created via kproc_create(9),
     kproc_kthread_add(9), or kthread_add(9).  When a kernel thread exits, the
     thread object is freed.  However, there is one special case to support an
     optimization where each free process object caches a thread object.  When
     a process exits, the last thread object is not freed but remains attached
     to the process.  When the process object is later reused for a new
     process in fork(2), the kernel recycles that last thread object and uses
     it as the initial thread in the new process.  When a thread is recycled,
     some of the steps in the thread allocation and free cycle are skipped as
     an optimization.

     ccppuu__tthhrreeaadd__aalllloocc() initializes machine-dependent fields in _t_d after allo‐
     cating a new kernel stack.  This function typically sets the _t_d___p_c_b and
     initial _t_d___f_r_a_m_e pointers.  ccppuu__tthhrreeaadd__aalllloocc() is called both when allo‐
     cating a new thread object and when a recycled thread allocates a new
     kernel stack.  Note that this function is _n_o_t called if a recycled thread
     reuses its existing kernel stack.

     ccppuu__tthhrreeaadd__cclleeaann() releases any machine-dependent resources for the last
     thread in a process during wait(2).  The thread is a candidate for recy‐
     cling so should be reset to run as a new thread in case it is recycled by
     a future fork(2).

     ccppuu__tthhrreeaadd__eexxiitt() cleans any machine-dependent state in _t_d while it is
     exiting.  This is called by the exiting thread so cannot free state
     needed during in-kernel execution.

     ccppuu__tthhrreeaadd__ffrreeee() releases any machine-dependent state in _t_d when it is
     being freed.  This is called for any thread that was not the last thread
     in a process once it has finished execution.

SSEEEE AALLSSOO
     fork(2), procctl(2), ptrace(2), thr_new(2), wait(2), kproc_create(9),
     kproc_kthread_add(9), kthread_add(9), mi_switch(9)

AAUUTTHHOORRSS
     This manual page was developed by SRI International, the University of
     Cambridge Computer Laboratory (Department of Computer Science and Tech‐
     nology), and Capabilities Limited under contract (FA8750-24-C-B047)
     (“DEC”).

BSD                             January 3, 2025                            BSD
