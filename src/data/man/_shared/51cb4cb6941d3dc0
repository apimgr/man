attempts  to  write  of  data to the object referenced by the de‐
scriptor from the buffer pointed to by performs the same  action,
but  gathers  the  output  data from the buffers specified by the
members of the array: iov[0], iov[1],  ...,  iov[iovcnt‐1].   and
perform  the  same functions, but write to the specified position
in the file without modifying the  file  pointer.   For  and  the
structure is defined as: struct iovec {         char   *iov_base;
/*  Base  address. */         size_t iov_len;    /* Length. */ };
Each entry specifies the base address and length of  an  area  in
memory  from which data should be written.  and will always write
a complete area before proceeding to the next.  On objects  capa‐
ble of seeking, the starts at a position given by the pointer as‐
sociated  with see Upon return from the pointer is incremented by
the number of bytes which were written.  Objects that are not ca‐
pable of seeking always write from  the  current  position.   The
value of the pointer associated with such an object is undefined.
If the real user is not the super‐user, then clears the set‐user‐
id  bit  on a file.  This prevents penetration of system security
by a user who a writable set‐user‐id file  owned  by  the  super‐
user.   When  using non‐blocking I/O on objects, such as sockets,
that are subject to flow control, and may write fewer bytes  than
requested;  the  return value must be noted, and the remainder of
the operation should be retried when possible.  and will fail  if
the  parameter  exceeds  and they do not attempt a partial write.
Upon successful completion the number of bytes which were written
is returned.  Otherwise, a ‐1 is returned and the global variable
is set to indicate the error.  The and system calls will fail and
the file pointer will remain unchanged if: The  user’s  quota  of
disk  blocks on the file system containing the file is exhausted.
Part of or data to be written to  the  file  points  outside  the
process’s  allocated  address space.  The pointer associated with
is negative.  The and system calls will fail and the file pointer
will remain unchanged if: The file  is  marked  for  non‐blocking
I/O,  and  no  data could be written immediately.  is not a valid
file descriptor open for writing.  A  write  is  attempted  on  a
socket that is not connected.  An attempt is made to write a file
that  exceeds  the  process’s file size limit or the maximum file
size.  The file is a regular file, is greater  than  0,  and  the
starting  position is greater than or equal to the offset maximum
established in the open file description associated with A signal
interrupts the write before it could  be  completed.   The  value
provided  for  exceeds  An I/O error occurs while reading from or
writing to the file system.  A write is attempted on a socket and
the local network interface used  to  reach  the  destination  is
down.   A write is attempted on a socket and no route to the net‐
work is present.  There is no free space remaining  on  the  file
system  containing  the file.  A request is made of a nonexistent
device, or the request is outside the capabilities of the device.
An attempt is made to write to a pipe that is not open for  read‐
ing  by  any process.  An attempt is made to write to a socket of
type that is not connected to a peer socket.  The file is a  file
that  requires  materialization and the I/O policy of the current
thread or process disallows dataless file materialization The and
calls may also return the following errors: See EWOULDBLOCK,  be‐
low.   The file descriptor is for a socket, is marked O_NONBLOCK,
and write would block.  The exact error code depends on the  pro‐
tocol,  but  EWOULDBLOCK is more common.  In addition, may return
one of the following errors: The destination is no longer  avail‐
able  when  writing  to  a domain datagram socket on which or had
been used to set a destination address.  The mbuf pool  has  been
completely exhausted when writing to a socket.  The and calls may
also  return the following errors: is less than or equal to 0, or
greater than One of the values in the array is negative.  The sum
of the values in the array overflows a 32‐bit integer.   The  and
calls  may  also  return the following errors: The specified file
offset is invalid.  The file  descriptor  is  associated  with  a
pipe,  socket,  or  FIFO.  These include files are needed for all
three functions.  The function call is expected to conform to The
and functions are expected to conform  to  is  nonstandard.   The
function  call  appeared in The function call appeared in A func‐
tion call appeared in
