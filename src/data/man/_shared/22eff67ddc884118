is the utility in that transfers maps to the local host.  The op‐
tions  are  as follows: Don’t send a "Clear current map" to local
process. Useful if isn’t running locally to  avoid  timeout  mes‐
sage.   This option is only used by This is to open communication
with on another host.  Don’t use default domain, use  the  speci‐
fied domain.  Force map transfer, even if the master’s version is
older than the local copy.  Get map from host instead of the maps
master  host.   Specify a source domain other than the target do‐
main.
