manipulate  and  databases  allows manipulation of and databases.
has three modes of operation to perform upon Displays  the  given
and  keys described in If no keys and no is specified, the entire
database is displayed.  This is the default  mode  of  operation.
Enabled with Deletes the given and keys described in Enabled with
Writes  the  given  and  and keys and values described in (in the
latter case, entries are separated by There are two mandatory ar‐
guments: is the database type; either or and is the database file
to manipulate.  Options valid for all modes are: Set the  endian‐
ness of the database.  may be one of: Big endian Host endian Lit‐
tle  endian  Defaults  to (host endian).  Contains a list of keys
(for read and delete), or separated keys and values  (for  write)
to  be used as arguments to the given mode.  If is is used.  Keys
are converted to lower case before manipulation.  Include the NUL
byte at the end of the key or value.  By convention, each key and
value in the underlying database is terminated with a  NUL  byte,
although  it  is  possible to have keys or values that are not so
terminated.  Without the option, adds a  NUL  terminator  to  any
keys  and  values  specified  on the command line or in the input
file, before searching or modifying the underlying database,  and
removes  the last byte (which is usually a NUL terminator) before
printing any keys and values retrieved from the underlying  data‐
base.   With  the  option,  does  not add or remove a terminating
byte.  The and options may be useful to encode the NUL terminator
in a readable form.  Quiet operation.  In read mode, missing keys
are considered to be an error.  In delete and  write  modes,  the
result  of  various  operations  is  suppressed.  Control how the
items specified by the option are encoded and option are decoded.
The option‐argument is a string  specifying  flags.   The  string
consists  of  one or more characters: Overrides and Overrides and
Overrides and only supports See  corresponding  options  for  the
meaning  of  these  characters, and for more detail on the flags.
Specify items to decode.   The  option‐argument  is  a  character
specifying  if the key the value or both should be decoded.  Read
mode specific options are: Display  duplicate  entries  in  data‐
bases.   Display  key.   Field  separator  string between key and
value.  Defaults to a single tab Specify items  to  encode.   The
option‐argument is a character specifying if the key the value or
both should be encoded.  Display value.  When encoding items with
option  also encode characters in per (If neither of or is given,
both options are enabled.)  Write mode specific options are: Cre‐
ate new database, and truncate existing databases.  Allow  dupli‐
cate entries in databases.  (Requires to be useful.)  Input field
separator string between key and value used when parsing Defaults
to  a  single  space Octal mode of created database.  Defaults to
Set the page size of the table to bytes.  If set to  a  database‐
specific  default  is  determined, based on the block‐size of the
underlying file‐system.  Defaults to Overwrite existing  entries.
If  not  specified, writing to an existing entry raises an error.
The command appeared in
