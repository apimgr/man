edits  the password file after setting the appropriate locks, and
does any necessary processing after  the  password  file  is  un‐
locked.   If  the  password file is already locked for editing by
another user, will ask you to try again later. The default editor
for is performs a number of consistency checks  on  the  password
entries,  and  will  not allow a password file with a entry to be
installed.  If  rejects  the  new  password  file,  the  user  is
prompted  to re‐enter the edit session.  Once the information has
been verified, uses to update the user database.  This is run  in
the  background, and, at very large sites could take several min‐
utes.  Until this update is completed, the password file  is  un‐
available for other updates and the new information is not avail‐
able  to  programs.   The options are as follows: Change the root
directory of the password file from to If a session is killed  it
may  leave  which will cause future executions to fail with until
it is removed.  If the following environment variable  exists  it
will  be  used  by The editor specified by the string will be in‐
voked instead of the default editor The  current  password  file.
Temporary copy of the password file used while editing.  The com‐
mand appeared in
