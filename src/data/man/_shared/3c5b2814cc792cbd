
MAKEFS(8)                 BSD System Manager's Manual                MAKEFS(8)

NNAAMMEE
     mmaakkeeffss — create a file system image from a directory tree or a mtree man‐
     ifest

SSYYNNOOPPSSIISS
     mmaakkeeffss [--DDxxZZ] [--BB _e_n_d_i_a_n] [--bb _f_r_e_e_-_b_l_o_c_k_s] [--dd _d_e_b_u_g_-_m_a_s_k]
            [--FF _m_t_r_e_e_-_s_p_e_c_f_i_l_e] [--ff _f_r_e_e_-_f_i_l_e_s] [--MM _m_i_n_i_m_u_m_-_s_i_z_e]
            [--mm _m_a_x_i_m_u_m_-_s_i_z_e] [--NN _u_s_e_r_d_b_-_d_i_r] [--OO _o_f_f_s_e_t] [--oo _f_s_-_o_p_t_i_o_n_s]
            [--RR _r_o_u_n_d_u_p_-_s_i_z_e] [--SS _s_e_c_t_o_r_-_s_i_z_e] [--ss _i_m_a_g_e_-_s_i_z_e] [--TT _t_i_m_e_s_t_a_m_p]
            [--tt _f_s_-_t_y_p_e] _i_m_a_g_e_-_f_i_l_e _d_i_r_e_c_t_o_r_y | _m_a_n_i_f_e_s_t [_e_x_t_r_a_-_d_i_r_e_c_t_o_r_y _._._.]

DDEESSCCRRIIPPTTIIOONN
     The utility mmaakkeeffss creates a file system image into _i_m_a_g_e_-_f_i_l_e from the
     directory tree _d_i_r_e_c_t_o_r_y or from the mtree manifest _m_a_n_i_f_e_s_t.  If any op‐
     tional directory trees are passed in the _e_x_t_r_a_-_d_i_r_e_c_t_o_r_y arguments, then
     the directory tree of each argument will be merged into the _d_i_r_e_c_t_o_r_y or
     _m_a_n_i_f_e_s_t first before creating _i_m_a_g_e_-_f_i_l_e.  No special devices or privi‐
     leges are required to perform this task.

     The options are as follows:

     --BB _e_n_d_i_a_n
           Set the byte order of the image to _e_n_d_i_a_n.  Valid byte orders are
           ‘4321’, ‘big’, or ‘be’ for big endian, and ‘1234’, ‘little’, or
           ‘le’ for little endian.  Some file systems may have a fixed byte
           order; in those cases this argument will be ignored.

     --bb _f_r_e_e_-_b_l_o_c_k_s
           Ensure that a minimum of _f_r_e_e_-_b_l_o_c_k_s free blocks exist in the im‐
           age.  An optional ‘%’ suffix may be provided to indicate that
           _f_r_e_e_-_b_l_o_c_k_s indicates a percentage of the calculated image size.

     --DD    Treat duplicate paths in an mtree manifest as warnings not error.
           If this flag is specified more than once, warnings about duplicate
           paths are not printed at all.

     --dd _d_e_b_u_g_-_m_a_s_k
           Enable various levels of debugging, depending upon which bits are
           set in _d_e_b_u_g_-_m_a_s_k.  This option is intended for source debugging.
           _d_e_b_u_g_-_m_a_s_k is a bit map defined in the header file _m_a_k_e_f_s_._h.  See
           the source for usage, and look for defines starting with _D_E_B_U_G__.

     --FF _m_t_r_e_e_-_s_p_e_c_f_i_l_e
           _T_h_i_s _i_s _a_l_m_o_s_t _c_e_r_t_a_i_n_l_y _n_o_t _t_h_e _o_p_t_i_o_n _y_o_u _a_r_e _l_o_o_k_i_n_g _f_o_r_. To
           create an image from a list of files in an mtree format manifest,
           specify it as the last argument on the command line, not as a the
           argument to --FF.

           Use _m_t_r_e_e_-_s_p_e_c_f_i_l_e as an mtree(8) ‘specfile’ specification.  This
           option has no effect when the image is created from a mtree mani‐
           fest rather than a directory.

           If a specfile entry exists in the underlying file system, its per‐
           missions and modification time will be used unless specifically
           overridden by the specfile.  An error will be raised if the type of
           entry in the specfile conflicts with that of an existing entry.

           In the opposite case (where a specfile entry does not have an entry
           in the underlying file system) the following occurs: If the spec‐
           file entry is marked ooppttiioonnaall, the specfile entry is ignored.  Oth‐
           erwise, the entry will be created in the image, and it is necessary
           to specify at least the following parameters in the specfile: ttyyppee,
           mmooddee, ggnnaammee, or ggiidd, and uunnaammee or uuiidd, and lliinnkk (in the case of
           symbolic links).  If ttiimmee is not provided, the current time will be
           used.  If ffllaaggss is not provided, the current file flags will be
           used.  Missing regular file entries will be created as zero-length
           files.

     --ff _f_r_e_e_-_f_i_l_e_s
           Ensure that a minimum of _f_r_e_e_-_f_i_l_e_s free files (inodes) exist in
           the image.  An optional ‘%’ suffix may be provided to indicate that
           _f_r_e_e_-_f_i_l_e_s indicates a percentage of the calculated image size.

     --MM _m_i_n_i_m_u_m_-_s_i_z_e
           Set the minimum size of the file system image to _m_i_n_i_m_u_m_-_s_i_z_e.

     --mm _m_a_x_i_m_u_m_-_s_i_z_e
           Set the maximum size of the file system image to _m_a_x_i_m_u_m_-_s_i_z_e.  An
           error will be raised if the target file system needs to be larger
           than this to accommodate the provided directory tree.

     --NN _u_s_e_r_d_b_-_d_i_r
           Use the user database text file _m_a_s_t_e_r_._p_a_s_s_w_d and group database
           text file _g_r_o_u_p from _u_s_e_r_d_b_-_d_i_r, rather than using the results from
           the system's getpwnam(3) and getgrnam(3) (and related) library
           calls.

     --OO _o_f_f_s_e_t
           Instead of creating the filesystem at the beginning of the file,
           start at offset.  Valid only for ffffss and mmssddooss.

     --oo _f_s_-_o_p_t_i_o_n_s
           Set file system specific options.  _f_s_-_o_p_t_i_o_n_s is a comma separated
           list of options.  Valid file system specific options are detailed
           below.

     --pp    Deprecated.  See the --ZZ flag.

     --RR _r_o_u_n_d_u_p_-_s_i_z_e
           Round the image up to _r_o_u_n_d_u_p_-_s_i_z_e.  _r_o_u_n_d_u_p_-_s_i_z_e should be a mul‐
           tiple of the file system block size.  This option only applies to
           the ffffss file system type.

     --SS _s_e_c_t_o_r_-_s_i_z_e
           Set the file system sector size to _s_e_c_t_o_r_-_s_i_z_e.  Defaults to 512.

     --ss _i_m_a_g_e_-_s_i_z_e
           Set the size of the file system image to _i_m_a_g_e_-_s_i_z_e.  This is
           equivalent to setting both the minimum (--MM) and the maximum (--mm)
           sizes to the same value.  For ffffss and mmssddooss the _i_m_a_g_e_-_s_i_z_e does not
           include the _o_f_f_s_e_t.  _o_f_f_s_e_t is not included in that size.

     --TT _t_i_m_e_s_t_a_m_p
           Specify a timestamp to be set for all filesystem files and directo‐
           ries created so that repeatable builds are possible.  The _t_i_m_e_s_t_a_m_p
           can be a _p_a_t_h_n_a_m_e, where the timestamps are derived from that file,
           or an integer value interpreted as the number of seconds from the
           Epoch.  Note that timestamps specified in an mtree(5) spec file,
           override the default timestamp.

     --tt _f_s_-_t_y_p_e
           Create an _f_s_-_t_y_p_e file system image.  The following file system
           types are supported:

                 ffffss     BSD fast file system (default).

                 ccdd99666600  ISO 9660 file system.

                 mmssddooss   FAT12, FAT16, or FAT32 file system.

                 zzffss     ZFS pool containing one or more file systems.

     --xx    Exclude file system nodes not explicitly listed in the specfile.

     --ZZ    Create a sparse file for ffffss.  This is useful for virtual machine
           images.

     Where sizes are specified, a decimal number of bytes is expected.  Two or
     more numbers may be separated by an “x” to indicate a product.  Each num‐
     ber may have one of the following optional suffixes:
           b    Block; multiply by 512
           k    Kibi; multiply by 1024 (1 KiB)
           m    Mebi; multiply by 1048576 (1 MiB)
           g    Gibi; multiply by 1073741824 (1 GiB)
           t    Tebi; multiply by 1099511627776 (1 TiB)
           w    Word; multiply by the number of bytes in an integer

   FFFFSS--ssppeecciiffiicc ooppttiioonnss
     ffffss images have ffs-specific optional parameters that may be provided.
     Each of the options consists of a keyword, an equal sign (‘=’), and a
     value.  The following keywords are supported:

           aavvggffiilleessiizzee   Expected average file size.
           aavvggffppddiirr      Expected number of files per directory.
           bbssiizzee         Block size.
           ddeennssiittyy       Bytes per inode. If unset, will allocate the minimum
                         number of inodes to represent the filesystem if no
                         free space has been requested (free blocks or minimum
                         size set); otherwise the larger of the newfs defaults
                         or what is required by the free inode parameters if
                         set.
           ffssiizzee         Fragment size.
           llaabbeell         Label name of the image.
           mmaaxxbbppgg        Maximum blocks per file in a cylinder group.
           mmiinnffrreeee       Minimum % free.
           ooppttiimmiizzaattiioonn  Optimization preference; one of ‘space’ or ‘time’.
           eexxtteenntt        Maximum extent size.
           mmaaxxbbppccgg       Maximum total number of blocks in a cylinder group.
           vveerrssiioonn       UFS version.  1 for FFS (default), 2 for UFS2.
           ssooffttuuppddaatteess   0 for disable (default), 1 for enable

   CCDD99666600--ssppeecciiffiicc ooppttiioonnss
     ccdd99666600 images have ISO9660-specific optional parameters that may be pro‐
     vided.  The arguments consist of a keyword and, optionally, an equal sign
     (‘=’), and a value.  The following keywords are supported:

           aallllooww--ddeeeepp--ttrreeeess      Allow the directory structure to exceed the
                                 maximum specified in the spec.
           aallllooww--iilllleeggaall--cchhaarrss   Allow illegal characters in filenames.  This
                                 option is not implemented.
           aallllooww--lloowweerrccaassee       Allow lowercase characters in filenames.
                                 This option is not implemented.
           aallllooww--mmaaxx--nnaammee        Allow 37 instead of 33 characters for file‐
                                 names by omitting the version id.
           aallllooww--mmuullttiiddoott        Allow multiple dots in a filename.
           aapppplliiccaattiioonniidd         Application ID of the image.
           aarrcchhiimmeeddeess            Use the ‘ARCHIMEDES’ extension to encode RISC
                                 OS metadata.
           bboooottiimmaaggeeddiirr          Boot image directory.  This option is not im‐
                                 plemented.
           cchhrrpp--bboooott             Write an MBR partition table to the image to
                                 allow older CHRP hardware to boot.
           bboooott--llooaadd--sseeggmmeenntt     Set load segment for the boot image.
           bboooottiimmaaggee             Filename of a boot image in the format
                                 “sysid;filename”, where “sysid” is one of
                                 ‘efi’, ‘i386’, ‘mac68k’, ‘macppc’, or
                                 ‘powerpc’.
           ggeenneerriicc--bboooottiimmaaggee     Load a generic boot image into the first 32K
                                 of the cd9660 image.
           hhaarrdd--ddiisskk--bboooott        Boot image is a hard disk image.
           iissoolleevveell              An integer representing the ISO 9660 inter‐
                                 change level where “level” is either ‘1’ or
                                 ‘2’.  “level” ‘3’ is not implemented.
           kkeeeepp--bbaadd--iimmaaggeess       Do not discard images whose write was aborted
                                 due to an error.  For debugging purposes.
           llaabbeell                 Label name of the image.
           nnoo--bboooott               Boot image is not bootable.
           nnoo--eemmuull--bboooott          Boot image is a “no emulation” ElTorito im‐
                                 age.
           nnoo--ttrraaiilliinngg--ppaaddddiinngg   Do not pad the image (apparently Linux needs
                                 the padding).
           oommiitt--ttrraaiilliinngg--ppeerriioodd  Omit trailing periods in filenames.
           ppllaattffoorrmmiidd            Set platform ID of section header entry of
                                 the boot image.
           pprreeppaarreerr              Preparer ID of the image.
           ppuubblliisshheerr             Publisher ID of the image.
           rroocckkrriiddggee             Use RockRidge extensions (for longer file‐
                                 names, etc.).
           vveerrbboossee               Turns on verbose output.
           vvoolluummeeiidd              Volume set identifier of the image.

   mmssddooss--ssppeecciiffiicc ooppttiioonnss
     mmssddooss images have MS-DOS-specific optional parameters that may be pro‐
     vided.  The arguments consist of a keyword, an equal sign (‘=’), and a
     value.  The following keywords are supported (see newfs_msdos(8) for more
     details):

           bbaacckkuupp__sseeccttoorr         Location of the backup boot sector.
           bblloocckk__ssiizzee            Block size.
           bboooottssttrraapp             Bootstrap file.
           bbyytteess__ppeerr__sseeccttoorr      Bytes per sector.
           ccrreeaattee__ssiizzee           Create file size.
           ddiirreeccttoorryy__eennttrriieess     Directory entries.
           ddrriivvee__hheeaaddss           Drive heads.
           ffaatt__ttyyppee              FAT type (12, 16, or 32).
           ffllooppppyy                Preset drive parameters for standard format
                                 floppy disks (160, 180, 320, 360, 640, 720,
                                 1200, 1232, 1440, or 2880).
           hhiiddddeenn__sseeccttoorrss        Hidden sectors.
           iinnffoo__sseeccttoorr           Location of the info sector.
           mmeeddiiaa__ddeessccrriippttoorr      Media descriptor.
           nnuumm__FFAATT               Number of FATs.
           OOEEMM__ssttrriinngg            OEM string.
           ooffffsseett                Offset in device.  This option will be ig‐
                                 nored if --OO is set to a positive number.
           rreesseerrvveedd__sseeccttoorrss      Reserved sectors.
           sseeccttoorrss__ppeerr__cclluusstteerr   Sectors per cluster.
           sseeccttoorrss__ppeerr__ffaatt       Sectors per FAT.
           sseeccttoorrss__ppeerr__ttrraacckk     Sectors per track.
           ssiizzee                  File System size.
           vvoolluummee__iidd             Volume ID.
           vvoolluummee__llaabbeell          Volume Label.

   zzffss--ssppeecciiffiicc ooppttiioonnss
     The image created by mmaakkeeffss contains a ZFS pool with a single vdev of
     type ‘disk’.  The root dataset is always created implicitly and contains
     the entire input directory tree unless additional datasets are specified
     using the options described below.

     To keep images reproducible, the pool GUID and other random identifiers
     will always be the same across runs of mmaakkeeffss.  This means that when a
     pool is first imported, its GUID should be reset using zpool-reguid(8) to
     avoid conflicting with other pools also generated by mmaakkeeffss; otherwise,
     it will not be possible to import other pools.  This can be configured
     from _/_e_t_c_/_r_c_._c_o_n_f using the _z_p_o_o_l___r_e_g_u_i_d variable.

     The arguments consist of a keyword, an equal sign (‘=’), and a value.
     The following keywords are supported:

           ashift                The base-2 logarithm of the minimum block
                                 size.  Typical values are 9 (512B blocks) and
                                 12 (4KB blocks).  The default value is 12.
           bootfs                The name of the bootable dataset for the
                                 pool.  Specifying this option causes the
                                 ‘bootfs’ property to be set in the created
                                 pool.
           mssize                The size of metaslabs in the created pool.
                                 By default, mmaakkeeffss allocates large (up to
                                 512MB) metaslabs with the expectation that
                                 the image will be auto-expanded upon first
                                 use.  This option allows the default heuris‐
                                 tic to be overridden.
           verify-txgs           Prompt OpenZFS to verify pool metadata during
                                 import.  This is disabled by default as it
                                 may significantly increase import times.
           poolname              The name of the ZFS pool.  This option must
                                 be specified.
           rootpath              An implicit path prefix added to dataset
                                 mountpoints.  By default it is _/_<_p_o_o_l_n_a_m_e_>.
                                 For creating bootable pools, the _r_o_o_t_p_a_t_h
                                 should be set to _/.  At least one dataset
                                 must have a mountpoint equal to _r_o_o_t_p_a_t_h.
           fs                    Create an additional dataset.  This option
                                 may be specified multiple times.  The argu‐
                                 ment value must be of the form
                                 _<_d_a_t_a_s_e_t_>_[_;_<_p_r_o_p_1_=_v_1_>_[_;_<_p_r_o_p_2_=_v_2_>_[_;_._._._]_]_],
                                 where _d_a_t_a_s_e_t is the name of the dataset and
                                 must belong to the pool's namespace.  For ex‐
                                 ample, with a pool name of ‘test’ all dataset
                                 names must be prefixed by ‘test/’.  A dataset
                                 must exist at each level of the pool's name‐
                                 space.  For example, to create
                                 ‘test/foo/bar’, ‘test/foo’ must be created as
                                 well.

                                 The dataset mountpoints determine how the
                                 datasets are populated with files from the
                                 staged directory tree.  Conceptually, all
                                 datasets are mounted before any are populated
                                 with files.  The root of the staged directory
                                 tree is mapped to _r_o_o_t_p_a_t_h.

                                 Dataset properties, as described in
                                 zfsprops(7), may be specified following the
                                 dataset name.  The following properties may
                                 be set for a dataset:

                                       atime
                                       canmount
                                       exec
                                       mountpoint
                                       setuid

SSEEEE AALLSSOO
     mtree(5), rc.conf(5), zfsconcepts(7), zfsprops(7), zpoolprops(7),
     mtree(8), newfs(8), zpool-reguid(8)

HHIISSTTOORRYY
     The mmaakkeeffss utility appeared in NetBSD 1.6.  It was ported to FreeBSD and
     first appeared in FreeBSD 8.0.

AAUUTTHHOORRSS
     Luke Mewburn ⟨_l_u_k_e_m_@_N_e_t_B_S_D_._o_r_g⟩ (original program),
     Daniel Watt,
     Walter Deignan,
     Ryan Gabrys,
     Alan Perez-Rathke,
     Ram Vedam (cd9660 support),
     Christos Zoulas (msdos support),
     Mark Johnston (zfs support).

BSD                            January 19, 2024                            BSD
