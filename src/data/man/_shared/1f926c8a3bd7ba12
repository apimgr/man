The system call checks the accessibility of the file named by the
argument  for  the  access permissions indicated by the argument.
The value of is either the bitwise‐inclusive  OR  of  the  access
permissions  to be checked for read permission, for write permis‐
sion, and for execute/search permission), or the  existence  test
For  additional  information,  see the section of The system call
uses the real user ID in place of the effective user ID, the real
group ID in place of the effective group ID, and the rest of  the
group  access  list.   The system call is equivalent to except in
the case where specifies a relative path.  In this case the  file
whose  accessibility  is  to be determined is located relative to
the directory associated with the file descriptor instead of  the
current working directory.  If is passed the special value in the
parameter, the current working directory is used and the behavior
is  identical  to  a call to Values for are constructed by a bit‐
wise‐inclusive OR of flags from the following  list,  defined  in
The  checks  for  accessibility are performed using the effective
user and group IDs instead of the real user and group ID  as  re‐
quired  in  a call to If names a symbolic link, the status of the
symbolic link is returned.  If names a symbolic link, the  status
of  the  symbolic  link is returned and if the path has any other
symbolic links, an error is returned.  If does not reside in  the
hierarchy  beneath  the starting directory, an error is returned.
Even if a process has appropriate privileges and  indicates  suc‐
cess  for  the file may not actually have execute permission bits
set.  Likewise for and or will fail if: The value of the argument
is invalid.  A component of the path prefix is not  a  directory.
A  component of a pathname exceeded characters, or an entire path
name exceeded characters.  The named file does  not  exist.   Too
many symbolic links were encountered in translating the pathname.
AT_SYMLINK_NOFOLLOW_ANY  was  passed  and a symbolic link was en‐
countered in translating the pathname.  Write access is requested
for a file on a read‐only file system.  Write access is requested
for a pure procedure (shared text) file presently being executed.
Permission bits of the file mode do not permit the requested  ac‐
cess,  or  search permission is denied on a component of the path
prefix.  The argument points outside the process’s allocated  ad‐
dress space.  An I/O error occurred while reading from or writing
to the file system.  Also, the system call may fail if: The argu‐
ment  does  not specify an absolute path and the argument is nei‐
ther nor a valid file descriptor.  The value of the  argument  is
not  valid.   The argument is not an absolute path and is neither
nor a  file  descriptor  associated  with  a  directory.   AT_RE‐
SOLVE_BENEATH was passed and does not reside in the directory hi‐
erarchy  beneath  the starting directory.  The system call is ex‐
pected to conform to The system call is expected  to  conform  to
POSIX.1‐2008 .  The function appeared in The result of should not
be  used to make an actual access control decision, since its re‐
sponse, even if correct at the moment it is formed, may  be  out‐
dated  at the time you act on it.  results should only be used to
pre‐flight, such as when configuring user interface  elements  or
for  optimization  purposes.   The actual access control decision
should be made by attempting to execute the relevant system  call
while  holding  the applicable credentials, and properly handling
any resulting errors; and this must be done even though may  have
predicted  success.   Additionally,  set‐user‐ID and set‐group‐ID
applications should restore the effective user or group  ID,  and
perform  actions  directly  rather  than  use  to simulate access
checks for the real user or group ID.
