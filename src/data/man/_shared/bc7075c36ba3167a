BUILD-DEBUG-INFO(1)       BSD General Commands Manual      BUILD-DEBUG-INFO(1)

NNAAMMEE
     bbuuiilldd--ddeebbuugg--iinnffoo — generate packing-lists and Makefiles for debug pack‐
     ages

SSYYNNOOPPSSIISS
     PORTSDIR=_p_a_t_h
     bbuuiilldd--ddeebbuugg--iinnffoo [--mmnnqqvvxx] --PP _p_k_g_d_i_r ---- _p_k_g___c_r_e_a_t_e___a_r_g_s _._._.

DDEESSCCRRIIPPTTIIOONN
     bbuuiilldd--ddeebbuugg--iinnffoo is a helper script for generating debug package informa‐
     tion in bsd.port.mk(5), with similar calling conventions to
     update-plist(1).

     Along with options and environment variables, bbuuiilldd--ddeebbuugg--iinnffoo uses the
     exact same options and arguments that would be passed to pkg_create(1).
     Since pkg_create(1) normally takes one single actual _p_k_g_n_a_m_e, there is no
     ambiguity in MULTI_PACKAGES situations.

     The first set of arguments correspond to the ‘default package’.

     The options are as follows:

     --PP _p_k_g_d_i_r
             directory used  for generating output (mandatory).

     --qq      Quiet mode.  Do not display status progress messages.  Note that
             this is not the opposite of verbose mode.

     --vv      Verbose mode.  Explain about directories stripped from dependen‐
             cies.  Note that this is not the opposite of quiet mode.

     bbuuiilldd--ddeebbuugg--iinnffoo will read each packing-list _l_i_s_t and generate a corre‐
     sponding _d_e_b_u_g_-_l_i_s_t under _p_k_g_d_i_r.

     The full list of Makefile rules needed to generate those files and direc‐
     tories will be written as _p_k_g_d_i_r_/_M_a_k_e_f_i_l_e.

SSEEEE AALLSSOO
     pkg_add(1), pkg_create(1), update-plist(1), bsd.port.mk(5)

BSD                              March 1, 2021                             BSD
