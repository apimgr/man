_d_l_s_y_m(3)                   Library Functions Manual                   _d_l_s_y_m(3)

NNAAMMEE
       dlsym,  dlvsym  - obtain address of a symbol in a shared object or exe‐
       cutable

LLIIBBRRAARRYY
       Dynamic linking library (_l_i_b_d_l, _-_l_d_l)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ddllffccnn..hh>>

       vvooiidd **ddllssyymm((vvooiidd **rreessttrriicctt _h_a_n_d_l_e,, ccoonnsstt cchhaarr **rreessttrriicctt _s_y_m_b_o_l));;

       ##ddeeffiinnee __GGNNUU__SSOOUURRCCEE
       ##iinncclluuddee <<ddllffccnn..hh>>

       vvooiidd **ddllvvssyymm((vvooiidd **rreessttrriicctt _h_a_n_d_l_e,, ccoonnsstt cchhaarr **rreessttrriicctt _s_y_m_b_o_l,,
                    ccoonnsstt cchhaarr **rreessttrriicctt _v_e_r_s_i_o_n));;

DDEESSCCRRIIPPTTIIOONN
       The function ddllssyymm() takes a "handle" of a dynamic loaded shared object
       returned by ddllooppeenn(3) along with a null‐terminated symbol name, and re‐
       turns the address where that symbol is loaded into memory.  If the sym‐
       bol is not found, in the specified object or any of the shared  objects
       that  were  automatically  loaded  by  ddllooppeenn(3)  when  that object was
       loaded, ddllssyymm() returns NULL.  (The  search  performed  by  ddllssyymm()  is
       breadth first through the dependency tree of these shared objects.)

       In  unusual cases (see NOTES) the value of the symbol could actually be
       NULL.  Therefore, a NULL return from ddllssyymm() need not indicate  an  er‐
       ror.  The correct way to distinguish an error from a symbol whose value
       is  NULL  is to call ddlleerrrroorr(3) to clear any old error conditions, then
       call ddllssyymm(), and then call ddlleerrrroorr(3) again, saving its  return  value
       into a variable, and check whether this saved value is not NULL.

       There are two special pseudo‐handles that may be specified in _h_a_n_d_l_e:

       RRTTLLDD__DDEEFFAAUULLTT
              Find  the  first  occurrence of the desired symbol using the de‐
              fault shared object  search  order.   The  search  will  include
              global  symbols  in the executable and its dependencies, as well
              as symbols in shared objects that were dynamically  loaded  with
              the RRTTLLDD__GGLLOOBBAALL flag.

       RRTTLLDD__NNEEXXTT
              Find the next occurrence of the desired symbol in the search or‐
              der  after  the  current  object.   This allows one to provide a
              wrapper around a function in another shared object, so that, for
              example, the definition of a function in a preloaded shared  ob‐
              ject (see LLDD__PPRREELLOOAADD in lldd..ssoo(8)) can find and invoke the "real"
              function  provided in another shared object (or for that matter,
              the "next" definition of the function in cases where  there  are
              multiple layers of preloading).

       The  __GGNNUU__SSOOUURRCCEE  feature test macro must be defined in order to obtain
       the definitions of RRTTLLDD__DDEEFFAAUULLTT and RRTTLLDD__NNEEXXTT from _<_d_l_f_c_n_._h_>.

       The function ddllvvssyymm() does the same as  ddllssyymm()  but  takes  a  version
       string as an additional argument.

RREETTUURRNN VVAALLUUEE
       On  success, these functions return the address associated with _s_y_m_b_o_l.
       On failure, they return NULL; the error can be  diagnosed  using  ddlleerr‐‐
       rroorr(3).

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       ddllssyymm(), ddllvvssyymm() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       ddllssyymm()
              POSIX.1‐2008.

       ddllvvssyymm()
              GNU.

HHIISSTTOORRYY
       ddllssyymm()
              glibc 2.0.  POSIX.1‐2001.

       ddllvvssyymm()
              glibc 2.1.

NNOOTTEESS
       There are several scenarios when the address  of  a  global  symbol  is
       NULL.   For  example,  a  symbol  can  be placed at zero address by the
       linker, via a linker  script  or  with  _-_-_d_e_f_s_y_m  command‐line  option.
       Undefined weak symbols also have NULL value.  Finally, the symbol value
       may  be the result of a GNU indirect function (IFUNC) resolver function
       that returns NULL as the resolved value.  In the latter  case,  ddllssyymm()
       also returns NULL without error.  However, in the former two cases, the
       behavior  of  GNU dynamic linker is inconsistent: relocation processing
       succeeds and the symbol can be observed to have NULL value, but ddllssyymm()
       fails and ddlleerrrroorr() indicates a lookup error.

   HHiissttoorryy
       The ddllssyymm() function is part of the dlopen  API,  derived  from  SunOS.
       That system does not have ddllvvssyymm().

EEXXAAMMPPLLEESS
       See ddllooppeenn(3).

SSEEEE AALLSSOO
       ddll__iitteerraattee__pphhddrr(3),   ddllaaddddrr(3),   ddlleerrrroorr(3),   ddlliinnffoo(3),  ddllooppeenn(3),
       lldd..ssoo(8)

Linux man‐pages (unreleased)      2025‐05‐17                          _d_l_s_y_m(3)
