RPC.BOOTPARAMD(8)         BSD System Manager's Manual        RPC.BOOTPARAMD(8)

NNAAMMEE
     rrppcc..bboooottppaarraammdd — boot parameter server

SSYYNNOOPPSSIISS
     rrppcc..bboooottppaarraammdd [--ddss] [--ff _f_i_l_e] [--rr _r_o_u_t_e_r]

DDEESSCCRRIIPPTTIIOONN
     rrppcc..bboooottppaarraammdd is a server process that provides information to diskless
     clients necessary for booting.  It consults the file _/_e_t_c_/_b_o_o_t_p_a_r_a_m_s.  It
     should normally be started from _/_e_t_c_/_r_c.

     This version will allow the use of aliases on the hostname in the
     _/_e_t_c_/_b_o_o_t_p_a_r_a_m_s file.  The hostname returned in response to the booting
     client's whoami request will be the name that appears in the config file,
     not the canonical name.  In this way you can keep the answer short enough
     so that machines that cannot handle long hostnames won't fail during
     boot.

     While parsing, if a line containing just “+” is found, and the YP subsys‐
     tem is active, the YP map _b_o_o_t_p_a_r_a_m_s will be searched immediately.

     The options are as follows:

     --dd      Display the debugging information.  The daemon does not fork in
             this case.

     --ff _f_i_l_e
             Specify the _f_i_l_e to read boot parameters from.  Defaults to
             _/_e_t_c_/_b_o_o_t_p_a_r_a_m_s.

     --rr _r_o_u_t_e_r
             Set the default _r_o_u_t_e_r (a hostname or IP address).  This defaults
             to the machine running the server.

     --ss      Log the debugging information with syslog.

FFIILLEESS
     /etc/bootparams  default configuration file

SSEEEE AALLSSOO
     bootparams(5), diskless(8)

AAUUTTHHOORRSS
     Originally written by Klas Heggemann <_k_l_a_s_@_n_a_d_a_._k_t_h_._s_e>.

BBUUGGSS
     You may find the syslog loggings too verbose.

     It's not clear if the non-canonical hack mentioned above is a good idea.

WWAARRNNIINNGG
     If rrppcc..bboooottppaarraammdd is run on a system which is also running YP, your YP
     domainname will be made public information.

BSD                           September 27, 2022                           BSD
