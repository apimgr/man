makes a typescript of everything printed on your terminal.  It is
useful  for students who need a hardcopy record of an interactive
session as proof of an assignment, as the typescript file can  be
printed  out  later with If the argument is given, saves all dia‐
logue in If no file name is given, the typescript is saved in the
file Option: Append the output to or  retaining  the  prior  con‐
tents.  Run the named instead of the shell.  Useful for capturing
the  output of a program that behaves differently when associated
with a tty.  When playing back a session  with  the  flag,  don’t
sleep  between  records  when playing back a timestamped session.
Exit with the shell‐style exit status of the shell or instead  of
always  exiting  successfully.   Flush  output  after each write.
This is useful for watching the script output in real time.  Play
back a session recorded with the flag in real  time.   Be  quiet,
and  don’t output started and ended lines.  Record a session with
input, output, and timestamping.  The script ends when the forked
shell exits (a to exit the Bourne shell and or (if  is  not  set)
for  the  C‐shell,  Certain  interactive commands, such as create
garbage in the typescript file.  works best with commands that do
not manipulate the screen, the results are  meant  to  emulate  a
hardcopy terminal.  The following environment variable is used by
If  the  variable exists, the shell forked by will be that shell.
If is not set, the Bourne shell is  assumed.   (Most  shells  set
this  variable automatically).  (for the mechanism).  The command
appeared in places in the log file, including linefeeds and back‐
spaces.  This is not what the naive user expects.
