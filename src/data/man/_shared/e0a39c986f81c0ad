APM(4)                 BSD/i386 Kernel Interfaces Manual                APM(4)

NNAAMMEE
     aappmm — APM BIOS interface

SSYYNNOOPPSSIISS
     ddeevviiccee aappmm

DDEEPPRREECCAATTIIOONN NNOOTTIICCEE
     This driver is scheduled for removal prior to the release of
     FreeBSD 13.0.

DDEESSCCRRIIPPTTIIOONN
     aappmm is an interface to the Intel / Microsoft APM (Advanced Power Manage‐
     ment) BIOS on laptop PCs.

     aappmm provides the following power management functions.

           1.   When the system wakes up from suspended mode, aappmm adjusts the
                system clock to RTC.

           2.   When the system wakes up from suspended mode, aappmm passes a
                message to syslogd(8) comprising of system wakeup time and
                elapsed time during suspended mode.

           3.   aappmm slows CPU clock when there are no system activities
                (runnable processes, interrupts, etc.).  This function is
                available only on systems whose APM supports CPU idling.

           4.   aappmm exports an application interface as a character device.
                Applications can control APM, or retrieve APM status informa‐
                tion via this interface.  aappmm exports the following inter‐
                faces.  These symbols are defined in <_m_a_c_h_i_n_e_/_a_p_m___b_i_o_s_._h>.

                      AAPPMMIIOO__SSUUSSPPEENNDD
                            Suspend system.

                      AAPPMMIIOO__GGEETT
                            Get power management information.

                      AAPPMMIIOO__EENNAABBLLEE

                      AAPPMMIIOO__DDIISSAABBLLEE
                            Enable / Disable power management.

                      AAPPMMIIOO__HHAALLTTCCPPUU

                      AAPPMMIIOO__NNOOTTHHAALLTTCCPPUU
                            Control execution of HLT in the kernel context
                            switch routine.

                      AAPPMMIIOO__GGEETTPPWWSSTTAATTUUSS
                            Get per battery information.

                            Some APM implementations execute the HLT (Halt CPU
                            until an interrupt occurs) instruction in the
                            “_I_d_l_e _C_P_U” call, while others do not.  Thus en‐
                            abling this may result in redundant HLT executions
                            because “_I_d_l_e _C_P_U” is called from the kernel con‐
                            text switch routine that inherently executes HLT.
                            This may reduce peak system performance.

                            Also the system hangs up if HLT instruction is
                            disabled in the kernel context switch routine, and
                            if the APM implementation of the machine does not
                            execute HLT in “_I_d_l_e _C_P_U”.  On some implementa‐
                            tions that do not support CPU clock slowdown, APM
                            might not execute HLT.  aappmm disables
                            AAPPMMIIOO__NNOOTTHHAALLTTCCPPUU operation on such machines.

                            The current version of aappmm does not call “_I_d_l_e
                            _C_P_U” from the kernel context switch routine if
                            clock slowdown is not supported, and it executes
                            HLT instruction by default.  Therefore, there is
                            no need to use these two operations in most cases.

                These interfaces are used by apm(8).

           5.   aappmm polls APM events and handles the following events.

                NNaammee                 AAccttiioonn           DDeessccrriippttiioonn
                PMEV_STANDBYREQ      suspend system   standby request
                PMEV_SUSPENDREQ      suspend system   suspend request
                PMEV_USERSUSPENDREQ  suspend system   user suspend request
                PMEV_CRITSUSPEND     suspend system   critical suspend request
                PMEV_NORMRESUME      resume system    normal resume
                PMEV_CRITRESUME      resume system    critical resume
                PMEV_STANDBYRESUME   resume system    standby resume
                PMEV_BATTERYLOW      notify message   battery low
                PMEV_UPDATETIME      adjust clock     update time

SSEEEE AALLSSOO
     apm(8), zzz(8)

AAUUTTHHOORRSS
     Tatsumi Hosokawa <_h_o_s_o_k_a_w_a_@_j_p_._F_r_e_e_B_S_D_._o_r_g>

BBUUGGSS
     WARNING!  Many, if not most, of the implementations of APM-bios in lap‐
     tops today are buggy.  You may be putting your LCD-display and batteries
     at a risk by using this interface.  (The reason this is not a problem for
     MS-Windows is that they use the real-mode interface.)  If you see any
     weird behavior from your system with this code in use, unplug the power
     and batteries ASAP, if not immediately, and disable this code.

     We are very interested in getting this code working, so please send your
     observations of any anomalous behavior to us.

     When aappmm is active, calling the BIOS setup routine by using hot-keys, may
     cause serious trouble when resuming the system.  BIOS setup programs
     should be called during bootstrap, or from DOS.

     Some APM implementations cannot handle events such as pushing the power
     button or closing the cover.  On such implementations, the system _m_u_s_t be
     suspended _o_n_l_y by using apm(8) or zzz(8).

     Disk spin-down, LCD backlight control, and power on demand have not been
     supported on the current version.

BSD                            November 1, 1994                            BSD
