is  a  user  shell  for  authenticating  gateways.  It is used to
change rules when a user authenticates and starts a session  with
and  to  undo these changes when the user’s session exits.  It is
designed for changing filter and translation rules for  an  indi‐
vidual  source  IP  address as long as a user maintains an active
session.  Typical use would be for a gateway  that  authenticates
users before allowing them Internet use, or a gateway that allows
different users into different places.  logs the successful start
and  end of a session to This, combined with properly set up fil‐
ter rules and secure switches, can be used to  ensure  users  are
held  accountable  for their network traffic.  can add filter and
translation rules using the syntax described in requires that the
system be enabled before use.  can also maintain the list  of  IP
address  of  connected users in the "authpf_users" is meant to be
used with users who can connect via only.  On startup,  retrieves
the  client’s  connecting IP address via the environment variable
and, after performing additional access checks, reads a  template
file  to  determine what filter and translation rules (if any) to
add.  On session exit the same rules that were added  at  startup
are removed.  Each process stores its rules in a separate ruleset
inside  a shared by all processes.  By default, the name "authpf"
is used, and the ruleset names equal the username and PID of  the
processes  as  "username(pid)".   The  following rules need to be
added to the main ruleset in order to  cause  evaluation  of  any
rules:  nat‐anchor  "authpf/*" rdr‐anchor "authpf/*" binat‐anchor
"authpf/*" anchor "authpf/*" The "/*" at the end  of  the  anchor
name  is required for to process the rulesets attached to the an‐
chor by Filter and translation rules for use the same format  de‐
scribed in The only difference is that these rules may (and prob‐
ably  should)  use  the macro which is assigned the connecting IP
address whenever is run.  Additionally, the macro is assigned the
user name.  Filter and translation rules are  stored  in  a  file
called  This  file will first be searched for in and then in Only
one of these files will be used if both  are  present.   Per‐user
rules from the directory are intended to be used when non‐default
rules are needed on an individual user basis.  It is important to
ensure  that  a  user can not write or change these configuration
files.  The file must exist in one of the above locations for  to
run.   Options are controlled by the file.  If the file is empty,
defaults are used for all configuration options.  The  file  con‐
sists  of pairs of the form one per line.  Currently, the allowed
values are as follows: Use the specified  name  instead  of  "au‐
thpf".   Use  the  specified  name instead of "authpf_users".  On
successful invocation, displays a message telling the user he  or
she  has  been  authenticated.   It will additionally display the
contents of the file if the file exists and is  readable.   There
exist  two  methods  for  providing additional granularity to the
control offered by ‐ it is possible to set the gateway to explic‐
itly allow users who have authenticated to  and  deny  access  to
only  a  few troublesome individuals.  This is done by creating a
file with the banned user’s login name as  the  filename  in  The
contents  of  this  file will be displayed to a banned user, thus
providing a method for informing the user  that  they  have  been
banned,  and  where they can go and how to get there if they want
to have their service restored.  This is the  default  behaviour.
It is also possible to configure to only allow specific users ac‐
cess.   This  is done by listing their login names, one per line,
in If "*" is found on a line, then all usernames  match.   If  is
unable  to  verify  the  user’s permission to use the gateway, it
will print a brief message and die.  It should be  noted  that  a
ban takes precedence over an allow.  On failure, messages will be
logged  to  for  the system administrator.  The user does not see
these, but will be told the system is unavailable due to  techni‐
cal  difficulties.   The  contents  of the file will also be dis‐
played if the file exists and is readable.  maintains the changed
filter rules as long as the user maintains an active session.  It
is important to remember however, that the existence of this ses‐
sion means the user is authenticated.  Because of this, it is im‐
portant to configure to ensure the security of the  session,  and
to ensure that the network through which users connect is secure.
should  be  configured to use the and parameters to ensure that a
ssh session is terminated quickly if it becomes unresponsive,  or
if  arp  or address spoofing is used to hijack the session.  Note
that TCP keepalives are not sufficient for this, since  they  are
not  secure.   Also  note  that the various SSH tunnelling mecha‐
nisms, such as and should be disabled for users to  prevent  them
from  circumventing  restrictions  imposed  by  the packet filter
ruleset.  will remove state table entries that were created  dur‐
ing  a  user’s session.  This ensures that there will be no unau‐
thenticated traffic allowed to pass after the controlling session
has been closed.  is designed for gateway  machines  which  typi‐
cally  do  not  have regular (non‐administrative) users using the
machine.  An administrator must remember that can be used to mod‐
ify the filter rules through the environment in which it is  run,
and  as  such  could be used to modify the filter rules (based on
the contents of the configuration files) by  regular  users.   In
the  case  where a machine has regular users using it, as well as
users with as their shell, the regular users should be  prevented
from  running  by  using  the or facilities.  modifies the packet
filter and address translation rules,  and  because  of  this  it
needs  to  be  configured  carefully.  will not run and will exit
silently if the file does not exist.  After considering  the  ef‐
fect  may have on the main packet filter rules, the system admin‐
istrator may enable by creating an appropriate file.  - To illus‐
trate the user‐specific access control mechanisms,  let  us  con‐
sider a typical user named bob.  Normally, as long as bob can au‐
thenticate  himself, the program will load the appropriate rules.
Enter the directory.  If bob has somehow fallen from grace in the
eyes of the powers‐that‐be, they can prohibit him from using  the
gateway  by  creating  the file containing a message about why he
has been banned from using the network.  Once bob has done  suit‐
able  penance,  his  access may be restored by moving or removing
the file Now consider a workgroup containing  alice,  bob,  carol
and  dave.  They have a wireless network which they would like to
protect from unauthorized use.  To accomplish this,  they  create
the  file  which  lists  their  login ids, one per line.  At this
point, even if eve could authenticate to she would not be allowed
to use the gateway.  Adding and  removing  users  from  the  work
group  is  a  simple  matter  of  maintaining  a  list of allowed
userids.  If bob once again manages to annoy the  powers‐that‐be,
they  can ban him from using the gateway by creating the familiar
file.  Though bob is listed in the allow file,  he  is  prevented
from using this gateway due to the existence of a ban file.  - It
is  often desirable to interface with a distributed password sys‐
tem rather than forcing the sysadmins to keep a large  number  of
local  password  files  in sync.  The mechanism in can be used to
fork the right shell.  To make that happen, should  have  entries
that look something like this: shell‐default:shell=/bin/csh

default:\         ...          :shell=/usr/sbin/authpf

daemon:\          ...           :shell=/bin/csh:\         :tc=de‐
fault:

staff:\          ...           :shell=/bin/csh:\          :tc=de‐
fault: Using a default password file, all users will get as their
shell  except  for root who will get - As stated earlier, must be
properly configured to detect and  defeat  network  attacks.   To
that  end,  the  following  options should be added to Protocol 2
ClientAliveInterval 15 ClientAliveCountMax 3  This  ensures  that
unresponsive  or spoofed sessions are terminated within a minute,
since a hijacker should not be able to spoof ssh  keepalive  mes‐
sages.   -  Once authenticated, the user is shown the contents of
This message may be a screen‐full of the appropriate use  policy,
the  contents  of  or  something as simple as the following: This
means you will be held accountable by  the  powers  that  be  for
traffic  originating  from your machine, so please play nice.  To
tell the user where to go when the system is broken,  could  con‐
tain  something like this: Sorry, there appears to be some system
problem. To report this problem so we can fix  it,  please  phone
1‐900‐314‐1597  or send an email to remove@bulkmailerz.net.  - In
areas where this gateway is used to protect a wireless network (a
hub with several hundred ports), the default rule set as well  as
the  per‐user  rules should probably allow very few things beyond
encrypted protocols like or On a securely switched network,  with
plug‐in jacks for visitors who are given authentication accounts,
you  might  want to allow out everything.  In this context, a se‐
cure switch is one that tries to prevent address  table  overflow
attacks.   Example # by default we allow internal clients to talk
to us using # ssh and use us as a dns server.  internal_if="fxp1"
gateway_addr="10.0.1.1"  nat‐anchor  "authpf/*"  rdr‐anchor  "au‐
thpf/*" binat‐anchor "authpf/*" block in on $internal_if from any
to any pass in quick on $internal_if proto tcp from any to $gate‐
way_addr \
      port = ssh pass in quick on $internal_if proto udp from any
to $gateway_addr \
      port  =  domain  anchor  "authpf/*" - This example makes no
real restrictions; it turns the IP address on  and  off,  logging
TCP connections.  external_if = "xl0" internal_if = "fxp0"

pass  in log quick on $internal_if proto tcp from $user_ip to any
pass in quick on $internal_if from $user_ip to any - This example
could be used for an insecure network (such as a public  wireless
network)  where  we might need to be a bit more restrictive.  in‐
ternal_if="fxp1" ipsec_gw="10.2.3.4"

# rdr ftp for proxying by ftp‐proxy(8) rdr on $internal_if  proto
tcp from $user_ip to any port 21 \
      ‐ 127.0.0.1 port 8021

# allow out ftp, ssh, www and https only, and allow user to nego‐
tiate  #  ipsec with the ipsec server.  pass in log quick on $in‐
ternal_if proto tcp from $user_ip to any \
      port { 21, 22, 80, 443 }  pass  in  quick  on  $internal_if
proto tcp from $user_ip to any \
      port  {  21,  22,  80,  443  } pass in quick proto udp from
$user_ip to $ipsec_gw port = isakmp pass in quick proto esp  from
$user_ip to $ipsec_gw - The following shows how to deal with NAT,
using  tags:  ext_if  =  "fxp1" ext_addr = 129.128.11.10 int_if =
"fxp0" # nat and tag connections...  nat on $ext_if from $user_ip
to any tag $user_ip ‐ $ext_addr pass in  quick  on  $int_if  from
$user_ip  to  any  pass  out log quick on $ext_if tagged $user_ip
With the above rules added by outbound connections  corresponding
to each users NAT’ed connections will be logged as in the example
below, where the user may be identified from the ruleset name.  #
tcpdump  ‐n  ‐e  ‐ttt  ‐i  pflog0  Oct  31  19:42:30.296553  rule
0.bbeck(20267).1/0(match):    pass     out     on     fxp1:     \
129.128.11.10.60539         >         198.137.240.92.22:        S
2131494121:2131494121(0) win \  16384  <mss  1460,nop,nop,sackOK>
(DF)  -  Simple  settings can be implemented without an anchor by
just using the "authpf_users" For example,  the  following  lines
will  give  SMTP  and  IMAP access to logged in users: table <au‐
thpf_users> persist pass  in  on  $ext_if  proto  tcp  from  <au‐
thpf_users> \
        to port { smtp imap } It is also possible to use the "au‐
thpf_users" in combination with anchors.  For example, processing
can  be  sped up by looking up the anchor only for packets coming
from logged in users: table <authpf_users>  persist  anchor  "au‐
thpf/*"  from  <authpf_users>  rdr‐anchor  "authpf/*"  from  <au‐
thpf_users> The program first appeared  in  Configuration  issues
are tricky.  The authenticating connection may be secured, but if
the network is not secured the user may expose insecure protocols
to  attackers  on  the same network, or enable other attackers on
the network to pretend to be the user by spoofing  their  IP  ad‐
dress.   is not designed to prevent users from denying service to
other users.
