Pamrubber User Manual(1)    General Commands Manual   Pamrubber User Manual(1)



NNAAMMEE
       pamrubber  - a rubber-sheeting utility that stretches an image based on
       control points


SSYYNNOOPPSSIISS
       ppaammrruubbbbeerr {--ttrrii || --qquuaadd} [--lliinneeaarr] [--ffrraammee] [--rraannddoommsseeeedd==_N]  _c_p_1_x  _c_p_1_y
       [_c_p_2_x  _c_p_2_y  [_c_p_3_x  _c_p_3_y  [_c_p_4_x _c_p_4_y]]] _c_p_1_x _c_p_1_y [_c_p_2_x _c_p_2_y [_c_p_3_x _c_p_3_y
       [_c_p_4_x _c_p_4_y]]] [_f_i_l_e_n_a_m_e]

       Minimum unique abbreviation of options is acceptable.  You may use dou‐
       ble hyphens instead of single hyphen to denote options.


DDEESSCCRRIIPPTTIIOONN
       This program is part of NNeettppbbmm(1).

       The  ppaammrruubbbbeerr  utility  converts a pam image into a new image with the
       contents moved around.  The  transformation  is  often  called  "rubber
       sheeting":  you  identify  control  points (CP) on the source image and
       specify new positions for those points in  the  new  image.   ppaammrruubbbbeerr
       moves  all  the pixels around, stretching and compressing as necessary,
       as if the original image were on a sheet of rubber and  you  pulled  on
       the sheet to make the control points move to their new locations.

       The new image has the same dimensions and format as the original.

       The transformation can happen in two very different ways, called "quad"
       and "tri." With the former, you must specify four control  points  (for
       both  source  and target).  These are the corners of two quadrilaterals
       that will act as the coordinate system for both source and  target  im‐
       ages.   Consider  them  as non-orthogonal (0,0), (0,1), (1,0) and (1,1)
       points.  This transformation comes  close  to  the  one  ppaammppeerrssppeeccttiivvee
       does, however that program does other corrections as well.

       When  you  specify less than four control points, the program adds con‐
       trol points in the following way.  With three control points, ppaammrruubbbbeerr
       chooses  the fourth one such that the four points form a parallelogram.
       With two points, ppaammrruubbbbeerr considers them the  opposite  corners  of  a
       rectangle.   When  you specify only one control point, ppaammrruubbbbeerr uses a
       rectangle from the top left corner of the image to the  single  control
       point.

       In "tri" mode, ppaammrruubbbbeerr conceptually cuts up the source and target im‐
       age into triangles.  It Transforms within each  corresponding  pair  of
       triangles in a stretching fashion.  It's like pulling on the three cor‐
       ners of the triangle.  In this mode, each pixel  in  the  source  image
       gets mapped to a position in the target image. No pixels are lost.

       When,  in  "tri"  mode,  you specify only a single control point in the
       source and target image, ppaammrruubbbbeerr creates  four  triangles  from  this
       point  to  the  four corners of the image. With two points, the program
       creates six triangles from the two endpoints of the line connecting the
       two  points,  again  to  the  four  corners of the image. Three control
       points is in a way the core of this utility in "tri" mode.  Between the
       three  edges  of  the central triangle and the four edges of the image,
       ppaammrruubbbbeerr constructs another seven triangles.  Four control points  de‐
       fine two central connected triangles.  In total this results in cutting
       the source and target image up into ten triangles.

       In this case clearly a picture says more than a thousand words.   There
       is a graphical illustration of these various modes at
        www.schaik.com/netpbm/rubber  ⟨http://www.schaik.com/netpbm/rubber⟩  .
       An example of how to use this type of rubber sheeting in cartography is
       in the article
        Visualizing       the       Landscape      of      Old-Time      Tokyo
       ⟨http://www.isprs.org/proceedings/XXXVI/5-W1/papers/21.pdf⟩ .



PPAARRAAMMEETTEERRSS
       The parameters are control points (_c_p) in pairs of _x and _y.  The source
       and target image must have the same number of control points.  The min‐
       imum number of values specified here is 4 for a single control point in
       the source and target image.  The maximum is 16 for four control points
       in each image.

       _f_i_l_e_n_a_m_e is the name of the input file. If you don't specify this, ppaamm‐‐
       rruubbbbeerr reads the image from Standard Input.



OOPPTTIIOONNSS
       In  addition  to  the options common to all programs based on libnetpbm
       (most notably --qquuiieett, see
        Common Options ⟨index.html#commonoptions⟩ ), ppaammrruubbbbeerr recognizes  the
       following command line options:




       --ttrrii | --qquuaadd

              This selects the type of rubber sheeting done.  You must specify
              exactly one of these options.



       --lliinneeaarr

              This determines whether ppaammrruubbbbeerr uses nearest neighbor interpo‐
              lation or bilinear interpolation of four source pixels.




       --ffrraammee

              This  option  causes  ppaammrruubbbbeerr to overlay the target image with
              the edges of the quadrilaterals, respectively the triangles used
              for the rubber sheeting.  To get the same overlay for the source
              image, use a ppaammrruubbbbeerr  transformation  with  identical  control
              points for source and target.



       --rraannddoommsseeeedd==_N

              ppaammrruubbbbeerr  randomizes  some of its output.  So that you can pro‐
              duce repeatable results, you can choose the seed of  the  random
              number  generator  with  this option.  If you use the same input
              image and the same random number generator seed, you should  al‐
              ways  get the exact same output.  By default, ppaammrruubbbbeerr uses the
              time of day as the seed, so you get  slightly  different  output
              when you run the program twice on the same input.

              Before  Netpbm 10.61 (December 2012), this was called --rraannddsseeeedd,
              and that still works.






SSEEEE AALLSSOO
       ppaamm(1) and ppaammppeerrssppeeccttiivvee(1)


HHIISSTTOORRYY
       ppaammrruubbbbeerr was new in Netpbm 10.54 (March 2011).



AAUUTTHHOORRSS
       _W_i_l_l_e_m _v_a_n _S_c_h_a_i_k wrote this program in February 2011  and  contributed
       it to Netpbm.

DDOOCCUUMMEENNTT SSOOUURRCCEE
       This  manual  page was generated by the Netpbm tool 'makeman' from HTML
       source.  The master documentation is at

              hhttttpp::////nneettppbbmm..ssoouurrcceeffoorrggee..nneett//ddoocc//ppaammrruubbbbeerr..hhttmmll

netpbm documentation             February 2011        Pamrubber User Manual(1)
