is  a utility used to manually manipulate the network routing ta‐
bles.  Except for setting up the default route,  it  is  normally
not  needed,  as a system routing table management daemon such as
should tend to this task.  can be used to modify nearly  any  as‐
pect  of  the routing policy, except packet forwarding, which can
be manipulated through the command.  The utility supports a  lim‐
ited  number of general options, but a rich command language, en‐
abling the user to specify any arbitrary request  that  could  be
delivered via the programmatic interface discussed in Turn on de‐
bugging Remove all routes (as per If used in conjunction with the
or  commands,  removes  the routes before performing the command.
Don’t show link layer entries in  routing  table.   Bypasses  at‐
tempts  to print host and network names symbolically when report‐
ing actions.  (The process of translating between symbolic  names
and  numerical  equivalents  can be quite time consuming, and may
require correct operation of the network; thus it may be  expedi‐
ent  to forgo this, especially when attempting to repair network‐
ing operations).  Suppress all output from commands that  manipu‐
late  the routing table.  Print a space when a flag is missing so
that flags are vertically aligned instead of printing  the  flags
that are set as a contiguous string.  (short) Suppresses all out‐
put  from  a  command  except for the actual gateway that will be
used.  How the gateway is printed depends on the  type  of  route
being  looked  up.   Show  tags in the route display.  Test only,
don’t perform any actions.  (verbose) Print  additional  details.
The  utility  provides several commands: Add a route.  Remove all
routes.  Remove all routes including the default gateway.  Delete
a specific route.  Change aspects of a route (such as  its  gate‐
way).  Lookup and display the route for a destination.  Print out
the  route table similar to "netstat -r" (see Continuously report
any changes to  the  routing  information  base,  routing  lookup
misses,  or suspected network partitionings.  The monitor command
has the syntax If is specified,  exits  after  receiving  routing
messages.   The  flush  command  has the syntax If the command is
specified, will ‘‘flush’’ the routing tables of all  gateway  en‐
tries.   When  the  address  family is specified by any of the or
modifiers, only routes having destinations with addresses in  the
delineated  family  will be manipulated.  The other commands have
the following syntax: where is the destination host  or  network,
and  is  the  next‐hop  intermediary  via which packets should be
routed.  Routes to a particular host may  be  distinguished  from
those to a network by interpreting the Internet address specified
as  the  argument.  The optional modifiers and force the destina‐
tion to be interpreted as a  network  or  a  host,  respectively.
Otherwise,  if  the  has a ‘‘local address part’’ of or if the is
the symbolic name of a network, then the route is assumed  to  be
to  a network; otherwise, it is presumed to be a route to a host.
Optionally, the can also be specified in the format.   For  exam‐
ple, is interpreted as is interpreted as is interpreted as and is
interpreted as The keyword can be used as the to set up a default
route  to  a  smart  If no other routes match, this default route
will be used as a last resort.  If the  destination  is  directly
reachable  via  an  interface requiring no intermediary system to
act as a gateway, the modifier should be specified;  the  gateway
given is the address of this host on the common network, indicat‐
ing the interface to be used for transmission.  The optional mod‐
ifiers  and  specify that all subsequent addresses are in the ad‐
dress family, or are specified as  link‐level  addresses  in  the
form  described  in  and the names must be numeric specifications
rather than symbolic names.  The optional modifier  specifies  an
address  associated  with  the route.  How the address is used is
specific to the address family of the destination and the  inter‐
face  used  to forward the packet.  Currently route tags are con‐
sumed only by the stack; therefore assumes  that  the  subsequent
addresses are in the address family.  See for examples of setting
routes  involving  MPLS.   The  optional qualifier is intended to
achieve the effect of an redirect with the netmask option, or  to
manually  add  subnet routes with netmasks different from that of
the implied network interface (as would otherwise be communicated
using the OSPF or ISIS routing protocols).  One specifies an  ad‐
ditional  ensuing  address parameter (to be interpreted as a net‐
work mask).  The implicit network mask generated in the case  can
be  overridden by making sure this option follows the destination
parameter.  is also available for similar purpose,  in  IPv4  and
IPv6  case.   Routes have associated flags which influence opera‐
tion of the protocols when sending to destinations matched by the
routes.  These flags are displayed using the following ID charac‐
ters in the routing display and may be set (or sometimes cleared)
by indicating the following corresponding modifiers: The optional
modifiers and provide initial values to quantities maintained  in
the  routing  entry  by transport level protocols, such as TCP or
TP4.  These may be individually locked  by  preceding  each  such
modifier  to  be  locked by the meta‐modifier, or one can specify
that all ensuing metrics may be locked by the meta‐modifier.   In
a or command where the destination and gateway are not sufficient
to  specify  the  route the or modifiers may be used to determine
the interface or interface address.  All symbolic names specified
for a or are looked up first as a host name using If this  lookup
fails,  is  then used to interpret the name as that of a network.
uses a routing socket and the new message types and As such, only
the super‐user may modify the routing tables.  This includes  the
use  of  the command to look up a route that is incomplete.  This
sets the default route to 192.168.0.1:  This  shows  all  routes,
without  DNS  resolution (this is useful if the DNS is not avail‐
able): To install a static route through 10.200.0.1 to reach  the
network  192.168.1.0/28,  use  this: The specified route is being
added to the tables.  The values printed are from the routing ta‐
ble entry supplied in the call.  If the gateway address used  was
not the primary address of the gateway (the first one returned by
the  gateway  address  is printed numerically as well as symboli‐
cally.  As above, but when deleting an entry.  When  the  command
is  specified, each routing table entry deleted is indicated with
a message of this form.  An attempt to add a route failed because
the gateway listed was not on a directly‐connected network.   The
next‐hop gateway must be given.  A delete operation was attempted
for  an  entry which wasn’t present in the tables.  An add opera‐
tion was attempted, but the system was low on resources  and  was
unable to allocate memory to create the new entry.  The attempted
operation  is  privileged.   Only root may modify the routing ta‐
bles.  These privileges are enforced by the kernel.  The  command
appeared  in  IPv6 support was added by WIDE/KAME project.  Since
and were obsolete and and were deprecated.  and appeared  in  The
first  paragraph  may  have slightly exaggerated abilities.  Some
uses of the or modifiers with the add  command  will  incorrectly
fail  with  a  message if there is no default route.  See case in
for details.
