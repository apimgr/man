is used to create, destroy and configure userspace instances.  is
experimental.   The  following  commands are supported: Create an
interface.  The interface will appear as to the rest of the  sys‐
tem, and will be served by a rump server in whose context the in‐
terface  appears as Destroy an interface and stop the rump server
behind it.  Run in the context of the  interface’s  rump  server.
For  example,          #  wg‐userspace 0 ifconfig wg0 10.0.1.0/24
will set the interface’s IP address.  Run in the context  of  the
interface’s  rump  server.  For example,         # wg‐userspace 0
wgconfig wg0 set listen‐port 1234 will set the  interface’s  lis‐
tening  port.  Run an arbitrary command in the context of the in‐
terface’s rump server, using The command first  appeared  in  The
command is written by
