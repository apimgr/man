The  file  contains the default configuration used by and to find
manual pages and information about manual pages (e.g., the  data‐
base).   Manual  pages are located by searching an ordered set of
directories called the for a file that matches the  name  of  the
requested  page.  Each directory in the search path usually has a
set of subdirectories in it (though this is not required).   When
subdirectories  are  used,  there are normally two subdirectories
for each section of the manual.  One subdirectory  contains  for‐
matted copies of that section’s manual pages that can be directly
displayed  to  a  terminal,  while the other section subdirectory
contains unformatted copies of the pages (see and Formatted  man‐
ual  pages  are  normally named with a trailing suffix.  The file
contains comment and configuration lines.   Comment  lines  start
with  the  character.   Blank  lines  are also treated as comment
lines.  Configuration lines consist of  a  configuration  keyword
followed  by a configuration string.  There are two types of con‐
figuration keywords: control keywords and section keywords.  Con‐
trol keywords must start with the character.  The following  con‐
trol  keywords  are currently defined: Identifies the set of suf‐
fixes used for manual pages that must be  formatted  for  display
and  the command that should be used to format them.  Manual file
names, regardless of their format, are expected to end in a  pat‐
tern,  i.e. a followed by some suffix.  The first field of a line
contains a man page suffix specification.  The suffix  specifica‐
tion  may  contain the normal shell globbing characters including
csh‐style curly braces The rest of the line is  a  shell  command
line whose standard output is a formatted manual page that can be
directly  displayed to the user.  There should be exactly one oc‐
currence of the string in the shell command line, and it will  be
replaced  by the name of the file which is being formatted.  Used
by to determine how to crunch formatted  pages  which  originally
were  compressed man pages.  The first field lists a suffix which
indicates what kind of compression was used to compress  the  man
page.  The rest of the line must be a shell command line, used to
compress the formatted pages.  There should be exactly one occur‐
rence of the string in the shell command line, and it will be re‐
placed  by the name of the output file.  Contains the system‐wide
default man path used to search for man pages.  Defines the  full
pathname (not just a directory path) for a database to be used by
the  and  commands.   The  pathname  may contain the normal shell
globbing characters, including csh‐style curly braces; to  escape
a  shell globbing character, precede it with a backslash Contains
the list (in search order) of section subdirectories  which  will
be searched in any man path directory named with a trailing slash
character.   This list is also used, even if there is no trailing
slash character, when a path is specified to the utility  by  the
user,  by the environment variable, or by the and options.  iden‐
tifies the set of suffixes used for formatted man pages (the suf‐
fix is normally used here).  Formatted man pages can be  directly
displayed  to the user.  Each suffix may contain the normal shell
globbing characters including csh‐style curly braces Contains the
version of the configuration file.  Defines additional  paths  to
be  searched for the particular hardware name whose literal value
is taken from For example, on an amd64, the  control  keyword  is
used,  where  is supplied by default as an alternate machine path
to also search.  Section configuration lines in consist of a sec‐
tion keyword naming the section and a configuration  string  that
defines  the  directory  or  subdirectory path that the section’s
manual pages are located in.  The path  may  contain  the  normal
shell  globbing  characters, including csh‐style curly braces; to
escape a shell globbing character, precede it  with  a  backslash
Section  keywords  must  not start with the character.  A section
path may contain either a list of absolute directories or a  list
of relative directories (but not both).  Relative directory paths
are  treated as a list of subdirectories that are appended to the
current man path directory being searched.  Section configuration
lines with absolute directory paths (starting with completely re‐
place the current man search path directory with  their  content.
Section  configuration lines with absolute directory paths ending
with a trailing slash character are expected to contain subdirec‐
tories of manual pages, (see the keyword above).   The  subdirec‐
tory list is not applied to absolute section directories if there
is  no  trailing slash.  In addition to the above rules, the com‐
mand also always checks in each directory that it searches for  a
subdirectory  with the same name as the current machine type.  If
a machine‐specific subdirectory is found, it is also searched be‐
fore the parent directory is.  This allows the manual to  contain
machine‐specific  man  pages.  Note that the machine subdirectory
does not need to be specified in the file.   Multiple  specifica‐
tions for all types of configuration lines are cumulative and the
entries  are  used  in the order listed in the file; multiple en‐
tries may be listed per line, as well.  Standard manual  configu‐
ration  file.   Given  the  following file: _version        BSD.2
_subdir         cat[123]                       _suffix         .0
_build          .[1‐9]  nroff ‐man %s _build          .tbl    tbl
%s      |      nroff      ‐man      _i386           x86      _de‐
fault        /usr/share/man/
sect3           /usr/share/man/{old/,}cat3 By default,  the  com‐
mand  will  search for and in the directories and If on a machine
of type the subdirectory in each directory would be  searched  as
well, before the directory was searched.  If was found first, the
command would be run to build a man page for display to the user.
The  command  would search the directories and in that order, for
the mktemp manual page.  If a subdirectory with the same name  as
the  current  machine  type  existed  in any of them, it would be
searched as well, before each of them were searched.
