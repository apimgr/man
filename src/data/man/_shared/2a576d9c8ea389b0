MAC_IFOFF(4)             BSD Kernel Interfaces Manual             MAC_IFOFF(4)

NNAAMMEE
     mmaacc__iiffooffff — interface silencing policy

SSYYNNOOPPSSIISS
     To compile the interface silencing policy into your kernel, place the
     following lines in your kernel configuration file:

           ooppttiioonnss MMAACC
           ooppttiioonnss MMAACC__IIFFOOFFFF

     Alternately, to load the interface silencing policy module at boot time,
     place the following line in your kernel configuration file:

           ooppttiioonnss MMAACC

     and in loader.conf(5):

           mac_ifoff_load="YES"

DDEESSCCRRIIPPTTIIOONN
     The mmaacc__iiffooffff interface silencing module allows administrators to enable
     and disable incoming and outgoing data flow on system network interfaces
     via the sysctl(8) interface.

     To disable network traffic over the loopback (lo(4)) interface, set the
     sysctl(8) OID _s_e_c_u_r_i_t_y_._m_a_c_._i_f_o_f_f_._l_o___e_n_a_b_l_e_d to 0 (default 1).

     To enable network traffic over other interfaces, set the sysctl(8) OID
     _s_e_c_u_r_i_t_y_._m_a_c_._i_f_o_f_f_._o_t_h_e_r___e_n_a_b_l_e_d to 1 (default 0).

     To allow BPF traffic to be received, even while other traffic is dis‐
     abled, set the sysctl(8) OID _s_e_c_u_r_i_t_y_._m_a_c_._i_f_o_f_f_._b_p_f_r_e_c_v___e_n_a_b_l_e_d to 1 (de‐
     fault 0).

   LLaabbeell FFoorrmmaatt
     No labels are defined.

SSEEEE AALLSSOO
     mac(4), mac_bsdextended(4), mac_lomac(4), mac_mls(4), mac_none(4),
     mac_partition(4), mac_portacl(4), mac_seeotheruids(4), mac_test(4),
     mac(9)

HHIISSTTOORRYY
     The mmaacc__iiffooffff policy module first appeared in FreeBSD 5.0 and was devel‐
     oped by the TrustedBSD Project.

AAUUTTHHOORRSS
     This software was contributed to the FreeBSD Project by Network Asso‐
     ciates Labs, the Security Research Division of Network Associates Inc.
     under DARPA/SPAWAR contract N66001-01-C-8035 (“CBOSS”), as part of the
     DARPA CHATS research program.

BBUUGGSS
     While the MAC Framework design is intended to support the containment of
     the root user, not all attack channels are currently protected by entry
     point checks.  As such, MAC Framework policies should not be relied on,
     in isolation, to protect against a malicious privileged user.

BSD                              July 25, 2015                             BSD
