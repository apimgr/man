The  command  is  used  to extract and upgrade packages that have
been previously created with the command.  Packages are  prepared
collections of pre‐built binaries, documentation, configurations,
installation  instructions  and/or  other files.  can recursively
install other packages that the current package depends on or re‐
quires from both local disk and via FTP or HTTP.  Since the  com‐
mand  may  execute scripts or programs contained within a package
file, your system may be susceptible to or other  subtle  attacks
from  miscreants who create dangerous package files.  You are ad‐
vised to verify the competence and identity of those who  provide
installable package files.  For extra protection, use the digital
signatures provided where possible (see the or, failing that, use
to extract the package file, and inspect its contents and scripts
to  ensure  it  poses  no danger to your system’s integrity.  Pay
particular attention to any or files, and inspect  the  file  for
(check for setuid), and directives, and/or use the command to ex‐
amine the package file.  The following command line arguments are
supported:  The  named packages are installed.  will first try to
use as full URL or path name without any wildcard processing.  If
that fails, will try to match packages using wildcard processing.
If that fails as well and does not contain any /, the entries  of
the  variable  are  searched using the wildcard processing rules.
Mark package as installed automatically, as dependency of another
package.  You can use to mark packages this way  after  installa‐
tion,  and to remove the mark.  If you a package without specify‐
ing after it had already been automatically installed,  the  mark
is removed.  Read the configuration file from instead of the sys‐
tem  default.  Force updating even if the dependencies of depend‐
ing packages are not satisfied by the new package.  This is  used
by  "make  replace",  after which one would typically replace the
depending packages.  Force installation to proceed even  if  pre‐
requisite packages are not installed or the install script fails.
Although  will still try to find and auto‐install missing prereq‐
uisite packages, a failure to find one will not be  fatal.   This
flag  also overrides the fatal error when the operating system or
architecture the package was built on differ  from  that  of  the
host.   Display  help and exit.  If an installation script exists
for a given package, do not execute it.  Override  the  value  of
the  configuration option with the value Override the machine ar‐
chitecture returned by uname with Don’t actually install a  pack‐
age, just report the steps that would be taken if it was.  Prefix
all  file  and  directory names with For packages without install
scripts this has the same behavior as using Override  the  prefix
stored  in  the  package with Do not record the installation of a
package.  This implies This means that you  cannot  deinstall  it
later,  so  only  use this option if you know what you are doing!
Replace an already installed version from a package.  Implies  If
the  package  that’s being installed is already installed, an up‐
date is performed.  Installed dependent packages are updated  re‐
cursively, if they are too old to fulfill the dependencies of the
to‐be‐installed  version.  See below for a more detailed descrip‐
tion of the process.  Print version number  and  exit.   Turn  on
verbose output.  One or more arguments may be specified, each be‐
ing  either  a  file containing the package (these usually ending
with the suffix) or a URL pointing at a file available on an  ftp
or  web  site.   Thus  you  may extract files directly from their
anonymous      ftp      or       WWW       locations       (e.g.,
http://cdn.NetBSD.org/pub/pkgsrc/pack‐
ages/NetBSD/x86_64/9.0/All/bash‐5.0.18.tgz   or  http://www.exam‐
ple.org/packages/screen‐4.0.tbz).  Note:  For ftp  transfers,  if
you wish to use passive mode ftp in such transfers, set the vari‐
able  FTP_PASSIVE_MODE to some value in your environment.  Other‐
wise, the more standard ACTIVE mode may be used.  If consistently
fails to fetch a package from a site known to work, it may be be‐
cause you have a firewall that demands the usage of passive  mode
ftp.   extracts each package’s meta data (including the to memory
and then runs through the following sequence to fully extract the
contents of the package: A check is  made  to  determine  if  the
package  or  another  version  of  it  is already recorded as in‐
stalled.  If it is, installation is terminated if the or  options
are  not  given.   If  the  same  version is installed and is not
given, it is marked as manually installed and process stops.   If
the  option is given, it’s assumed the package should be replaced
by the new version instead.  Before doing so, all  packages  that
depend  on  the  pkg being upgraded are checked if they also work
with the new version.  If that test is not successful, the depen‐
dent packages are updated first.  The replacing is then  prepared
by  moving  an existing file aside (if it exists), and running on
the installed package.  Installation  then  proceeds  as  if  the
package was not installed, and restores the file afterwards.  The
package build information is extracted from the file and compared
against  the result of If the operating system or architecture of
the package  differ  from  that  of  the  host,  installation  is
aborted.   This behavior is overridable with the flag.  The pack‐
age build information from is then checked for (or If the package
was built with ABI dependency recommendations ignored, a  warning
will be issued.  A check is made to determine if the package con‐
flicts  (from  directives,  see  with  an already recorded as in‐
stalled package or if an installed  package  conflicts  with  the
package.  If it is, installation is terminated.  The file list of
the  package is compared to the file lists of the installed pack‐
ages.  If there is any overlap, the installation  is  terminated.
All  package dependencies (from directives, see are read from the
packing list.  If any of these required  packages  are  not  cur‐
rently  installed,  an attempt is made to find and install it; if
the missing package cannot be found or installed,  the  installa‐
tion is terminated.  If the package contains an script, it is ex‐
ecuted  with the following arguments: The name of the package be‐
ing installed.  Keyword denoting that the script  is  to  perform
any  actions  needed  before  the  package  is installed.  If the
script exits with a non‐zero status  code,  the  installation  is
terminated.   The  files  from the file list are extracted to the
chosen prefix.  If an script exists for the package, it  is  exe‐
cuted with the following arguments: The name of the package being
installed.   Keyword  denoting  that the script is to perform any
actions needed after the package has been installed.   After  in‐
stallation  is  complete, a copy of the packing list, script, de‐
scription, and display files are copied into for subsequent  pos‐
sible  use  by Any package dependencies are recorded in the other
packages’ file.  Finally, if we were  upgrading  a  package,  any
file  that  was  moved  aside before upgrading was started is now
moved back into place.  The script is called with the environment
variable set to the installation prefix (see the  option  above).
This allows a package author to write a script that reliably per‐
forms  some  action  on  the  directory  where the package is in‐
stalled, even if the user might change it with the  flag  to  The
scripts  are also called with the environment variable set to the
location of the meta‐data files, and with the  environment  vari‐
able set to the location of the package reference counts database
directory.  If the flag was given to will be set to Additionally,
and are prefixed with See for options, that can also be specified
using  the environment.  In all cases, will try to install binary
packages listed in dependencies list.  You can  grab  a  compiled
binary  package  from  remote  location  by  specifying  a URL: #
pkg_add                    http://cdn.NetBSD.org/pub/pkgsrc/pack‐
ages/NetBSD/x86_64/9.0/All/firefox‐84.0.tgz The base URL can also
be   provided   by   the   configuration   variable,   #   export
PKG_PATH=http://cdn.NetBSD.org/pub/pkgsrc/pack‐
ages/NetBSD/x86_64/9.0/All # pkg_add firefox You can also specify
a  compiled  binary  package  by  filesystem  path:   #   pkg_add
/usr/pkgsrc/packages/All/tcsh‐6.14.00.tgz If you omit the version
number,  will  install  the latest version available.  With emits
more  messages  to  terminal:  #  pkg_add  ‐v   /usr/pkgsrc/pack‐
ages/All/unzip  Initial  work  and  ongoing development.  refine‐
ments.  wildcard dependency processing,  pkgdb,  upgrading,  etc.
HTTP  support.  Rewrote most of the code base to work without ex‐
ternal commands.  Package upgrading needs a lot more work  to  be
really universal.  Sure to be others.
