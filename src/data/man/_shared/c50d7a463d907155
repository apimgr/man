WATCH(8)                  BSD System Manager's Manual                 WATCH(8)

NNAAMMEE
     wwaattcchh — snoop on another tty line

SSYYNNOOPPSSIISS
     wwaattcchh [--cciinnoottWW] [--ff _s_n_p_d_e_v] [_t_t_y]

DDEESSCCRRIIPPTTIIOONN
     The wwaattcchh utility allows the user to examine all data coming through a
     specified tty using the snp(4) device.  If the snp(4) device is not
     available, wwaattcchh will attempt to load the module (ssnnpp).  The wwaattcchh util‐
     ity writes to standard output.

     The options are as follows:

     --cc      Reconnect on close.  If the tty observed by wwaattcchh is closed, au‐
             tomatically reattach to the same tty.  If this option is not
             specified, wwaattcchh will request a new tty if running in interactive
             mode or exit if running without a controlling tty.

     --ff _s_n_p_d_e_v
             If this option is specified, wwaattcchh will use _s_n_p_d_e_v as the snp(4)
             device.  Without this option, wwaattcchh will attempt to find the next
             available snp(4) device.

     --ii      Force interactive mode.  Interactive mode is a default if wwaattcchh
             is started from a tty.  If output is redirected to a file, inter‐
             active mode can still be requested by specifying this option.

     --nn      Disable the ability to switch the watched tty interactively.
             This disables both change requests made with <control-X> as well
             as automatic prompting when the current tty is closed or over‐
             flows.  In all cases where a prompt would be displayed, wwaattcchh
             will exit.  The reconnect flags are unaffected by this option.
             When this flag is used, <control-X> is passed through to the ter‐
             minal.

     --oo      Reconnect on overflow.  The behavior of wwaattcchh if the observed tty
             overflows is similar to the behavior if the observed tty is
             closed.  For more info see snp(4).

     --tt      Print the date and time when observation of a given tty is
             started.

     --WW      Allow write access to observed tty.

     _t_t_y     Tty may be specified as a tty-style device, such as a pseudo tty
             device, a virtual console, or a serial line, etc.  Names may be
             preceded by _/_d_e_v_/.

OOPPEERRAATTIIOONN
     While running in interactive mode, all user input is discarded except
     for:

     <<ccoonnttrrooll--GG>>
           Exit wwaattcchh.
     <<ccoonnttrrooll--WW>>
           Clear screen.
     <<ccoonnttrrooll--XX>>
           Change attached tty, unless this feature is disabled, in which case
           control-X is passed to the terminal as with other control charac‐
           ters.

SSEEEE AALLSSOO
     pty(4), snp(4), kldload(8)

HHIISSTTOORRYY
     The wwaattcchh utility first appeared in FreeBSD 2.1.

AAUUTTHHOORRSS
     Ugen J.S. Antsilevich <_u_g_e_n_@_N_e_t_V_i_s_i_o_n_._n_e_t_._i_l>

BBUUGGSS
     No terminal emulation is performed.  All user output is reproduced as-is.

BSD                            November 24, 2001                           BSD
