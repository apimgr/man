SKEY(1)                   BSD General Commands Manual                  SKEY(1)

NNAAMMEE
     sskkeeyy, oottpp--mmdd55, oottpp--rrmmdd116600, oottpp--sshhaa11 — respond to an OTP challenge

SSYYNNOOPPSSIISS
     sskkeeyy [--xx] [--mmdd55 | --rrmmdd116600 | --sshhaa11] [--nn _c_o_u_n_t] [--pp _p_a_s_s_p_h_r_a_s_e] ⟨_s_e_q_u_e_n_c_e_#⟩
          [/] _k_e_y

DDEESSCCRRIIPPTTIIOONN
     SS//KKeeyy is a procedure for using one-time passwords to authenticate access
     to computer systems.  It uses 64 bits of information transformed by the
     MD5, RIPEMD-160, or SHA1 algorithms.  The user supplies the 64 bits in
     the form of 6 English words that are generated by a secure computer.
     This implementation of SS//KKeeyy is RFC 2289 compliant.

     Before using sskkeeyy, the system needs to be initialized using skeyinit(1);
     this will establish a secret passphrase.  After that, one-time passwords
     can be generated using sskkeeyy, which will prompt for the secret passphrase.
     After a one-time password has been used to log in, it can no longer be
     used.

     When sskkeeyy is invoked as oottpp--mmeetthhoodd, sskkeeyy will use _m_e_t_h_o_d as the hash
     function where _m_e_t_h_o_d is currently one of md5, rmd160, or sha1.

     If you misspell your secret passphrase while running sskkeeyy, you will get a
     list of one-time passwords that will not work, and no indication of the
     problem.

     Password sequence numbers count backwards.  You can enter the passwords
     using small letters, even though sskkeeyy prints them capitalized.

     The options are as follows:

     --mmdd55 | --rrmmdd116600 | --sshhaa11
             Selects the hash algorithm: MD5, RMD-160 (160-bit Ripe Message
             Digest), or SHA1 (NIST Secure Hash Algorithm Revision 1).

     --nn _c_o_u_n_t
             Prints out _c_o_u_n_t one-time passwords.  The default is to print
             one.

     --pp _p_a_s_s_p_h_r_a_s_e
             Uses _p_a_s_s_p_h_r_a_s_e as the secret passphrase.  Use of this option is
             discouraged as your secret passphrase could be visible in a
             process listing.

     --xx      Causes output to be in hexadecimal instead of ASCII.

EEXXAAMMPPLLEESS
           $ skey 99 th91334
           Enter secret passphrase: <your secret passphrase is entered here>
           OMEN US HORN OMIT BACK AHOY
           $

SSEEEE AALLSSOO
     login(1), skeyaudit(1), skeyinfo(1), skeyinit(1), skey(5), skeyprune(8)

SSTTAANNDDAARRDDSS
     N. Haller, C. Metz, P. Nesser, and M. Straw, _A _O_n_e_-_T_i_m_e _P_a_s_s_w_o_r_d _S_y_s_t_e_m,
     RFC 2289, February 1998.

TTRRAADDEEMMAARRKKSS AANNDD PPAATTEENNTTSS
     S/Key is a Trademark of Bellcore.

AAUUTTHHOORRSS
     Phil Karn
     Neil M. Haller
     John S. Walden
     Scott Chasin

BSD                             March 31, 2022                             BSD
