PTHREAD_RWLOCK_TIMEDR... BSD Library Functions Manual PTHREAD_RWLOCK_TIMEDR...

NNAAMMEE
     pptthhrreeaadd__rrwwlloocckk__ttiimmeeddrrddlloocckk — acquire a read-write lock for reading or
     give up after a specified period

LLIIBBRRAARRYY
     POSIX Threads Library (libpthread, -lpthread)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<pptthhrreeaadd..hh>>

     _i_n_t
     pptthhrreeaadd__rrwwlloocckk__ttiimmeeddrrddlloocckk(_p_t_h_r_e_a_d___r_w_l_o_c_k___t _*_r_e_s_t_r_i_c_t _r_w_l_o_c_k,
         _c_o_n_s_t _s_t_r_u_c_t _t_i_m_e_s_p_e_c _*_r_e_s_t_r_i_c_t _a_b_s___t_i_m_e_o_u_t);

DDEESSCCRRIIPPTTIIOONN
     This function acquires a read lock on the read-write lock _r_w_l_o_c_k.  How‐
     ever, if the lock cannot be acquired without waiting for another thread
     to unlock the lock, this wait shall be terminated when _a_b_s___t_i_m_e_o_u_t ex‐
     pires.

     A thread may hold multiple concurrent read locks.  The
     pthread_rwlock_unlock(3) function must be called once for each lock ac‐
     quired.

     If the thread should be interrupted by a signal, the
     pptthhrreeaadd__rrwwlloocckk__ttiimmeeddrrddlloocckk() function will be automatically restarted af‐
     ter the thread returns from the signal handler.

     The calling thread may deadlock if at the time the call is made it holds
     a write lock on _r_w_l_o_c_k.  The results are undefined if this function is
     called with an uninitialized read-write lock.

IIMMPPLLEEMMEENNTTAATTIIOONN NNOOTTEESS
     To prevent writer starvation, writers are favored over readers.

RREETTUURRNN VVAALLUUEESS
     If successful, the pptthhrreeaadd__rrwwlloocckk__ttiimmeeddrrddlloocckk() function will return
     zero.  Otherwise, an error number will be returned to indicate the error.

     This function shall not return an error code of EINTR.

EERRRROORRSS
     The pptthhrreeaadd__rrwwlloocckk__ttiimmeeddrrddlloocckk() function will fail if:

     [ETIMEDOUT]        The lock could not be acquired before the specified
                        timeout expired.

     The pptthhrreeaadd__rrwwlloocckk__ttiimmeeddrrddlloocckk() function may fail if:

     [EAGAIN]           The read lock could not be acquired because the maxi‐
                        mum number of read locks for _r_w_l_o_c_k would be exceeded.

     [EDEADLK]          The calling thread already holds a write lock on
                        _r_w_l_o_c_k.

     [EINVAL]           The value specified by _r_w_l_o_c_k does not refer to an
                        initialized read-write lock object, or the _a_b_s___t_i_m_e_o_u_t
                        nanosecond value is less than zero or greater than or
                        equal to 1 billion.

SSEEEE AALLSSOO
     pthread_rwlock_init(3), pthread_rwlock_timedwrlock(3),
     pthread_rwlock_unlock(3)

SSTTAANNDDAARRDDSS
     The pptthhrreeaadd__rrwwlloocckk__ttiimmeeddrrddlloocckk() function is expected to conform to
     ISO/IEC 9945-1:1996 (“POSIX.1”).

HHIISSTTOORRYY
     The pptthhrreeaadd__rrwwlloocckk__ttiimmeeddrrddlloocckk() function first appeared in FreeBSD 5.2.

BSD                             August 17, 2018                            BSD
