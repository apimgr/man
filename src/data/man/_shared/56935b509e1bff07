removes  a  directory  file  whose name is given by The directory
must not have any entries other than and A 0 is returned  if  the
remove  succeeds; otherwise a ‐1 is returned and an error code is
stored in the global location The named file is  removed  unless:
Search  permission  is denied for a component of the path prefix.
Write permission is denied on the directory containing  the  link
to  be  removed.   The directory to be removed is the mount point
for a mounted file system.  points outside  the  process’s  allo‐
cated  address space.  An I/O error occurs while deleting the di‐
rectory entry or deallocating the inode.  Too many symbolic links
are encountered in translating the pathname.  This is taken to be
indicative of a looping symbolic link.  A component of a pathname
(possibly expanded by a symbolic link) exceeds characters, or  an
entire  path  name exceeded characters.  The named directory does
not exist.  A component of the path  is  not  a  directory.   The
named  directory contains files other than and in it.  The direc‐
tory containing the directory to be removed is marked sticky, and
neither the containing directory nor the directory to be  removed
are  owned  by  the effective user ID.  The directory entry to be
removed resides on a read‐only file system.   The  function  call
appeared in
