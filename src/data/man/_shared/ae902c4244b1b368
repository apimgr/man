LGE(4)                   BSD Kernel Interfaces Manual                   LGE(4)

NNAAMMEE
     llggee — Level 1 LXT1001 NetCellerator PCI Gigabit Ethernet adapter driver

SSYYNNOOPPSSIISS
     To compile this driver into the kernel, place the following lines in your
     kernel configuration file:

           ddeevviiccee mmiiiibbuuss
           ddeevviiccee llggee

     Alternatively, to load the driver as a module at boot time, place the
     following line in loader.conf(5):

           if_lge_load="YES"

DDEESSCCRRIIPPTTIIOONN
     The llggee driver provides support for various NICs based on the Level 1
     LXT1001 NetCellerator Gigabit Ethernet controller chip.

     The LXT1001 supports fiber PHYs and also a GMII port for use with
     10/100/1000 copper PHYs, however there are currently no NICs on the mar‐
     ket that use this feature.

     The LXT1001 supports TCP/IP checksum offload for receive and VLAN-based
     filtering as well as a 64-bit multicast hash filter.  It also supports
     jumbo frames, which can be configured via the interface MTU setting.  Se‐
     lecting an MTU larger than 1500 bytes with the ifconfig(8) utility con‐
     figures the adapter to receive and transmit jumbo frames.  Using jumbo
     frames can greatly improve performance for certain tasks, such as file
     transfers and data streaming.

     The llggee driver supports the following media types:

     aauuttoosseelleecctt  Enable autoselection of the media type and options.  The user
                 can manually override the autoselected mode by adding media
                 options to rc.conf(5).

     11000000bbaasseeSSXX  Set 1000baseSX operation over fiber optic cable.  Both
                 ffuullll--dduupplleexx and hhaallff--dduupplleexx modes are supported.

     The llggee driver supports the following media options:

     ffuullll--dduupplleexx  Force full duplex operation.

     hhaallff--dduupplleexx  Force half duplex operation.

     For more information on configuring this device, see ifconfig(8).

HHAARRDDWWAARREE
     Adapters supported by the llggee driver include:

     ••   SMC TigerCard 1000 (SMC9462SX)
     ••   D-Link DGE-500SX

DDIIAAGGNNOOSSTTIICCSS
     llggee%%dd:: ccoouullddnn''tt mmaapp mmeemmoorryy  A fatal initialization error has occurred.

     llggee%%dd:: ccoouullddnn''tt mmaapp ppoorrttss  A fatal initialization error has occurred.

     llggee%%dd:: ccoouullddnn''tt mmaapp iinntteerrrruupptt  A fatal initialization error has occurred.

     llggee%%dd:: nnoo mmeemmoorryy ffoorr ssooffttcc ssttrruucctt!!  The driver failed to allocate memory
     for per-device instance information during initialization.

     llggee%%dd:: ffaaiilleedd ttoo eennaabbllee mmeemmoorryy mmaappppiinngg!!  The driver failed to initialize
     PCI shared memory mapping.  This might happen if the card is not in a
     bus-master slot.

     llggee%%dd:: nnoo mmeemmoorryy ffoorr jjuummbboo bbuuffffeerrss!!  The driver failed to allocate memory
     for jumbo frames during initialization.

     llggee%%dd:: wwaattcchhddoogg ttiimmeeoouutt  The device has stopped responding to the net‐
     work, or there is a problem with the network connection (cable).

SSEEEE AALLSSOO
     arp(4), miibus(4), netintro(4), ng_ether(4), ifconfig(8)

     _L_e_v_e_l _1 _L_X_T_1_0_0_1 _P_r_o_g_r_a_m_m_i_n_g _M_a_n_u_a_l,
     https://www.FreeBSD.org/~wpaul/Level1/LXT1001SRM.pdf.

HHIISSTTOORRYY
     The llggee device driver first appeared in FreeBSD 4.4.

AAUUTTHHOORRSS
     The llggee driver was written by Bill Paul <_w_i_l_l_i_a_m_._p_a_u_l_@_w_i_n_d_r_i_v_e_r_._c_o_m>.

BSD                              July 16, 2005                             BSD
