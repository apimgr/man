The  file defines parameters regarding to the workstation console
(wscons).  The file consists of lines starting  with  a  keyword,
and one or more arguments.  Empty lines and lines starting with a
hash  are ignored.  This configuration file is used by the script
which parses and runs and/or as configured.  See for  details  on
enabling  the  rc.d script.  The following keywords and arguments
are recognized: Used to load a font via gives a  font  name  that
can  be  used  later,  can be used to specify the width of a font
character in pixel, is the same, just for  the  font  characters’
height.  is used to declare the font’s encoding, see the descrip‐
tion  on  option for more detail.  gives the absolute path to the
font file.  See for more information.  Add and configure  virtual
console number using a screen type of (e.g. 80x25) and a terminal
emulation  (e.g.  vt100).  See for further parameter description.
Attach and configure keyboard using If is or the first free  key‐
board  will be used.  See for more information.  Set the keyboard
map to the given language code using The map must be supported by
the keyboard driver in use and must be compiled into the  kernel.
See the keyboard driver’s manpage (e.g., for details.  Parses the
contents of which contains a keyboard map per line, and calls for
each  line.   See for details.  Used to attach and configure key‐
board/mouse multiplexors, using See for  more  information.   Set
arbitrary  wscons  variable to value for specified control device
Can be used for direct modification of variables, when  no  other
keywords  are suitable.  See for more information.  Command argu‐
ments can be specified as which makes default  values  come  into
effect as described in the documentation of the utilities.
