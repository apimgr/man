The  function  creates  a  new  process from the executable file,
called the specified by which is an absolute or relative path  to
the file.  The function is identical to the function if the spec‐
ified  contains  a  slash  character; otherwise, the parameter is
used to construct a pathname, with its path prefix being obtained
by a search of the path specified in the environment  by  the  If
this  variable isn’t specified, the default path is set according
to the definition in which is set to This pathname either  refers
to  an  executable  object  file, or a file of data for an inter‐
preter; for more details.  The argument is a pointer to  a  pid_t
variable  to  receive  the pid of the spawned process; if this is
NULL, then the pid of the spawned process is  not  returned.   If
this  pointer  is  non‐NULL,  then  on successful completion, the
variable will be modified to  contain  the  pid  of  the  spawned
process.   The  value is undefined in the case of a failure.  The
argument is either NULL, or it is a pointer to a file actions ob‐
ject that was initialized by a call to  and  represents  zero  or
more  file actions.  File descriptors open in the calling process
image remain open in the new process image, except for those  for
which the close‐on‐exec flag is set (see and Descriptors that re‐
main open are unaffected by unless their behaviour is modified by
particular  spawn  flags or a file action; see and for additional
information.  The argument is either NULL, or it is a pointer  to
an attributes object that was initialized by a call to and repre‐
sents  a set of spawn attributes to apply.  If NULL, then the de‐
fault attributes are applied;  otherwise,  these  attributes  can
control  various  aspects of the spawned process, and are applied
prior to the spawned process beginning execution;  see  for  more
information.   The argument is a pointer to a null‐terminated ar‐
ray of character pointers to null‐terminated  character  strings.
These strings construct the argument list to be made available to
the  new  process.   At  least  must be present in the array, and
should contain the file name of the program being  spawned,  e.g.
the last component of the or argument.  The argument is a pointer
to  a  null‐terminated array of character pointers to null‐termi‐
nated strings.  A pointer to this array is normally stored in the
global variable These strings pass information to the new process
that is not directly an argument to the command (see Signals  set
to be ignored in the calling process are set to be ignored in the
new  process,  unless the behaviour is modified by user specified
Signals which are set to be caught in the calling  process  image
are set to default action in the new process image.  Blocked sig‐
nals  remain  blocked regardless of changes to the signal action,
unless the mask is overridden by user specified The signal  stack
is reset to be undefined (see for more information).  By default,
the  effective  user ID and group ID will be the same as those of
the calling process image; however, this  may  be  overridden  to
force  them  to  be  the  real user ID and group ID of the parent
process by user specified (see for  more  information).   If  the
set‐user‐ID  mode  bit  of the new process image file is set (see
the effective user ID of the new process  image  is  set  to  the
owner ID of the new process image file.  If the set‐group‐ID mode
bit  of the new process image file is set, the effective group ID
of the new process image is set  to  the  group  ID  of  the  new
process image file.  (The effective group ID is the first element
of  the group list.)  The real user ID, real group ID and supple‐
mentary group IDs of the new process image remain the same as the
calling process image.  After any  set‐user‐ID  and  set‐group‐ID
processing,  the  effective user ID is recorded as the saved set‐
user‐ID, and the effective group ID is recorded as the saved set‐
group‐ID.  These values may be used in changing the effective IDs
later (see The new process also inherits the following attributes
from the calling process: When a program is executed as a  result
of  a  or  call, it is entered as follows: where is the number of
elements in (the ‘‘arg count’’) and points to the array of  char‐
acter  pointers  to the arguments themselves.  If the argument is
NULL, no pid is returned to the calling process; if  it  is  non‐
NULL,  then  and  functions  return  the  process ID of the child
process into the pid_t variable pointed to by  the  argument  and
return  a  0  on success.  If an error occurs, they return a non‐
zero error code as  the  function  return  value,  and  no  child
process  is  created.   The and functions will fail and return to
the calling process if: The value specified  by  or  is  invalid.
The  number of bytes in the new process’s argument list is larger
than the system‐imposed limit.  This limit is  specified  by  the
MIB  variable  Search permission is denied for a component of the
path prefix.  The new process file is not an ordinary file.   The
new process file mode denies execute permission.  The new process
file  is  on  a filesystem mounted with execution disabled in The
new process file is not as long as indicated by the  size  values
in its header.  or point to an illegal address.  An I/O error oc‐
curred  while  reading  from  the file system.  Too many symbolic
links were encountered in  translating  the  pathname.   This  is
taken  to  be indicative of a looping symbolic link.  A component
of a pathname exceeded characters, or an  entire  path  name  ex‐
ceeded characters.  The new process file does not exist.  The new
process  file  has  the appropriate access permission, but has an
unrecognized  format  (e.g.,  an  invalid  magic  number  in  its
header).   The  new  process requires more virtual memory than is
allowed by the imposed maximum A component of the path prefix  is
not  a  directory.   The  new  process  file  is a pure procedure
(shared text) file that is currently open for writing or  reading
by  some  process.  The new process file has no architectures ap‐
propriate for the current system.  Bad file descriptor for one or
more Additionally, they may fail for any of the reasons listed in
If a program is to a non‐super‐user, but  is  executed  when  the
real  is  ‘‘root’’,  then the program has some of the powers of a
super‐user as well.  The and function calls appeared in
