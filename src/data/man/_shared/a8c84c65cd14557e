attempts  to translate a given into the locale‐specific string as
determined by the environment.  If  a  translation  file  is  not
found,  or a translation for the given is not found, the original
string will be echoed.  If the is not provided  as  an  argument,
the  environment  variable will be consulted instead.  By default
will look for message catalogs in If the environment variable  is
defined,  then that will override the default location.  The fol‐
lowing flags are available: Interpret backslash escape  sequences
prior to translation.  Print the usage information If is used, do
not add the trailing newline.  If is not provided, this flag will
do  nothing.   Act similarly to All arguments will be translated,
and a newline will be appended to the output string.  The  domain
to  use  when looking up The location of message catalogs to use,
overriding $ LANG=es gettext grep ’writing output’ escribiendo el
resultado
