LOGSAVE(8)                  System Manager's Manual                 LOGSAVE(8)



NNAAMMEE
       logsave - save the output of a command in a logfile

SSYYNNOOPPSSIISS
       llooggssaavvee [ --aassvv ] _l_o_g_f_i_l_e _c_m_d___p_r_o_g _[ _._._. _]

DDEESSCCRRIIPPTTIIOONN
       The  llooggssaavvee  program  will  execute  _c_m_d___p_r_o_g with the specified argu‐
       ment(s), and save a copy of its output to _l_o_g_f_i_l_e.  If  the  containing
       directory  for _l_o_g_f_i_l_e does not exist, llooggssaavvee will accumulate the out‐
       put in memory until it can be written out.  A copy of the  output  will
       also be written to standard output.

       If  _c_m_d___p_r_o_g is a single hyphen ('-'), then instead of executing a pro‐
       gram, llooggssaavvee will take its input from standard input and  save  it  in
       _l_o_g_f_i_l_e

       llooggssaavvee  is  useful for saving the output of initial boot scripts until
       the /var partition  is  mounted,  so  the  output  can  be  written  to
       /var/log.

OOPPTTIIOONNSS
       --aa     This option will cause the output to be appended to _l_o_g_f_i_l_e, in‐
              stead of replacing its current contents.

       --ss     This option will cause llooggssaavvee to skip writing to the  log  file
              text  which is bracketed with a control-A (ASCII 001 or Start of
              Header) and control-B (ASCII 002 or Start of Text).  This allows
              progress  bar  information to be visible to the user on the con‐
              sole, while not being written to the log file.

       --vv     This option will make llooggssaavvee to be more verbose in  its  output
              to the user.

AAUUTTHHOORR
       Theodore Ts'o (tytso@mit.edu)

SSEEEE AALLSSOO
       ffsscckk(8)



E2fsprogs version 1.47.0         February 2023                      LOGSAVE(8)
