GETENT(1)                 BSD General Commands Manual                GETENT(1)

NNAAMMEE
     ggeetteenntt — get entries from administrative database

SSYYNNOOPPSSIISS
     ggeetteenntt _d_a_t_a_b_a_s_e [_k_e_y _._._.]

DDEESSCCRRIIPPTTIIOONN
     The ggeetteenntt program retrieves and displays entries from the administrative
     database specified by _d_a_t_a_b_a_s_e.  The order in which entries are looked up
     is defined by the specific database (for example, the lookup order for
     the “hosts” database is determined by resolv.conf(5)).  The display for‐
     mat for a given _d_a_t_a_b_a_s_e is as per the “traditional” file format for that
     database.

     _d_a_t_a_b_a_s_e may be one of:

           DDaattaabbaassee    DDiissppllaayy ffoorrmmaatt
           ethers      address name
           group       group:passwd:gid:[member[,member]...]
           hosts       address name [alias ...]
           passwd      user:passwd:uid:gid:gecos:home_dir:shell
           protocols   name protocol [alias ...]
           rpc         name number [alias ...]
           services    name port/protocol [alias ...]
           shells      /path/to/shell

     If one or more _k_e_y arguments are provided, they will be looked up in
     _d_a_t_a_b_a_s_e using the appropriate function.  For example, ppaasssswwdd supports a
     numeric UID or user name; hhoossttss supports an IPv4 address, IPv6 address,
     or host name; and sseerrvviicceess supports a service name, service name/protocol
     name, numeric port, or numeric port/protocol name.

     If no _k_e_y is provided and _d_a_t_a_b_a_s_e supports enumeration, all entries for
     _d_a_t_a_b_a_s_e will be retrieved using the appropriate enumeration function and
     printed.

EEXXIITT SSTTAATTUUSS
     The ggeetteenntt utility exits with one of the following values:

           0       Success.
           1       There was an error in the command syntax.
           2       One of the specified key names was not found in _d_a_t_a_b_a_s_e.
           3       There is no support for enumeration on _d_a_t_a_b_a_s_e.

SSEEEE AALLSSOO
     ethers(5), group(5), hosts(5), passwd(5), protocols(5), rpc(5),
     services(5), shells(5)

HHIISSTTOORRYY
     A ggeetteenntt command first appeared in OpenBSD 3.9.  It was based on the com‐
     mand of the same name in Solaris and Linux.

BSD                            October 24, 2014                            BSD
