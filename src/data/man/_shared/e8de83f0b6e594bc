ACCF_DATA(9)             BSD Kernel Developer's Manual            ACCF_DATA(9)

NNAAMMEE
     aaccccff__ddaattaa — buffer incoming connections until data arrives

SSYYNNOOPPSSIISS
     ooppttiioonnss IINNEETT
     ooppttiioonnss AACCCCEEPPTT__FFIILLTTEERR__DDAATTAA
     kkllddllooaadd aaccccff__ddaattaa

DDEESSCCRRIIPPTTIIOONN
     This is a filter to be placed on a socket that will be using aacccceepptt() to
     receive incoming connections.

     It prevents the application from receiving the connected descriptor via
     aacccceepptt() until data arrives on the connection.

     The _A_C_C_E_P_T___F_I_L_T_E_R___D_A_T_A kernel option is also a module that can be enabled
     at runtime via kldload(8) if the INET option has been compiled into the
     kernel.

EEXXAAMMPPLLEESS
     Assuming ACCEPT_FILTER_DATA has been included in the kernel config file
     or the aaccccff__ddaattaa module has been loaded, this will enable the data accept
     filter on the socket _s_o_k.

             struct accept_filter_arg afa;

             bzero(&afa, sizeof(afa));
             strcpy(afa.af_name, "dataready");
             setsockopt(sok, SOL_SOCKET, SO_ACCEPTFILTER, &afa, sizeof(afa));

SSEEEE AALLSSOO
     setsockopt(2), accept_filter(9), accf_dns(9), accf_http(9)

HHIISSTTOORRYY
     The accept filter mechanism and the accf_data filter were introduced in
     FreeBSD 4.0.

AAUUTTHHOORRSS
     This manual page and the filter were written by Alfred Perlstein.

BSD                            November 15, 2000                           BSD
