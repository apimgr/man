MSDOSFS(5)                  BSD File Formats Manual                 MSDOSFS(5)

NNAAMMEE
     mmssddoossffss — MS-DOS (FAT) file system

SSYYNNOOPPSSIISS
     ooppttiioonnss MMSSDDOOSSFFSS

DDEESSCCRRIIPPTTIIOONN
     The mmssddoossffss driver will permit the FreeBSD kernel to read and write MS-
     DOS based file systems.

     The most common usage follows:

           mount -t msdosfs /dev/ada0sN /mnt

     where _N is the partition number and _/_m_n_t is a mount point.  Some users
     tend to create a _/_d_o_s directory for mmssddoossffss mount points.  This helps to
     keep better track of the file system, and make it more easily accessible.

     It is possible to define an entry in _/_e_t_c_/_f_s_t_a_b that looks similar to:

     /dev/ada0sN             /dos    msdosfs         rw      0       0

     This will mount an MS-DOS based partition at the _/_d_o_s mount point during
     system boot.  Using _/_m_n_t as a permanent mount point is not advised as its
     intention has always been to be a temporary mount point for floppy and
     ZIP disks.  See hier(7) for more information on FreeBSD directory layout.

EEXXAAMMPPLLEESS
     Determine which FAT file system version (e.g, FAT16, FAT32) is a parti‐
     tion formatted with:

           file -s /dev/da0s1

     gpart(8) may also be used to extract this information.

SSEEEE AALLSSOO
     mount(2), unmount(2), fsck_msdosfs(8), mount(8), mount_msdosfs(8),
     newfs_msdos(8), umount(8)

AAUUTTHHOORRSS
     This manual page was written by Tom Rhodes <_t_r_h_o_d_e_s_@_F_r_e_e_B_S_D_._o_r_g>.

BSD                           September 27, 2018                           BSD
