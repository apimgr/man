DEV_CLONE(9)             BSD Kernel Developer's Manual            DEV_CLONE(9)

NNAAMMEE
     ddeevv__cclloonnee, ddrraaiinn__ddeevv__cclloonnee__eevveennttss — eventhandler for name-based device
     cloning in devfs

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ppaarraamm..hh>>
     ##iinncclluuddee <<ssyyss//ccoonnff..hh>>

     _v_o_i_d
     cclloonnee__hhaannddlleerr(_v_o_i_d _*_a_r_g, _s_t_r_u_c_t _u_c_r_e_d _*_c_r, _c_h_a_r _*_n_a_m_e, _i_n_t _n_a_m_e_l_e_n,
         _s_t_r_u_c_t _c_d_e_v _*_*_d_e_v);

     EVENTHANDLER_REGISTER(dev_clone, clone_handler, arg, priority);

     _v_o_i_d
     ddrraaiinn__ddeevv__cclloonnee__eevveennttss();

DDEESSCCRRIIPPTTIIOONN
     A device driver may register a listener that will be notified each time a
     name lookup on the devfs(5) mount point fails to find the vnode.  A lis‐
     tener shall be registered for the _d_e_v___c_l_o_n_e event.  When called, it is
     supplied with the first argument _a_r_g that was specified at handler regis‐
     tration time, appropriate credentials _c_r, and a name _n_a_m_e of length
     _n_a_m_e_l_e_n that we look for.  If the handler decides that the name is appro‐
     priate and wants to create the device that will be associated with the
     name, it should return it to devfs in the _d_e_v argument.

     The ddrraaiinn__ddeevv__cclloonnee__eevveennttss() function is a barrier.  It is guaranteed
     that all calls to eventhandlers for ddeevv__cclloonnee that were started before
     ddrraaiinn__ddeevv__cclloonnee__eevveennttss() call, are finished before it returns control.

SSEEEE AALLSSOO
     devfs(5), namei(9)

BSD                             January 3, 2009                            BSD
