The  functions  are designed to copy contiguous data from one ad‐
dress to another.  All but copy data from user‐space  to  kernel‐
space  or  vice‐versa.   The routines provide the following func‐
tionality: Copies bytes of data from the  user‐space  address  to
the  kernel‐space address Copies a NUL‐terminated string, at most
bytes long, from user‐space address to kernel‐space  address  The
number  of  bytes actually copied, including the terminating NUL,
is returned in Copies bytes of data from the kernel‐space address
to the user‐space address Copies a NUL‐terminated string, at most
bytes long, from kernel‐space address to kernel‐space address The
number of bytes actually copied, including the  terminating  NUL,
is returned in The functions return 0 on success or the following
error on failure: If a bad address is encountered.  When this er‐
ror is returned, the contents of the destination buffer ( for and
for  are  undefined.   For  and the contents of the parameter are
also undefined on a return of EFAULT.  In addition to EFAULT, and
on failure will return: When the string is longer than bytes.  On
this error return, the destination buffer is not null‐terminated,
but the parameter is maintained.
