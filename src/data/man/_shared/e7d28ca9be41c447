MOUNT_SMBFS(8)            BSD System Manager's Manual           MOUNT_SMBFS(8)

NNAAMMEE
     mmoouunntt__ssmmbbffss — mount a server message block (SMB1/CIFS) file system

SSYYNNOOPPSSIISS
     mmoouunntt__ssmmbbffss [--EE _c_s_1::_c_s_2] [--II _h_o_s_t] [--LL _l_o_c_a_l_e] [--MM _c_r_i_g_h_t_s::_s_r_i_g_h_t_s] [--NN]
                 [--OO _c_o_w_n_e_r::_c_g_r_o_u_p//_s_o_w_n_e_r::_s_g_r_o_u_p] [--RR _r_e_t_r_y_c_o_u_n_t] [--TT _t_i_m_e_o_u_t]
                 [--UU _u_s_e_r_n_a_m_e] [--WW _w_o_r_k_g_r_o_u_p] [--cc _c_a_s_e] [--dd _m_o_d_e] [--ff _m_o_d_e]
                 [--gg _g_i_d] [--nn _o_p_t] [--uu _u_i_d]
                 ////_u_s_e_r@@_s_e_r_v_e_r[::_p_o_r_t_1[::_p_o_r_t_2]]//_s_h_a_r_e _n_o_d_e

DDEESSCCRRIIPPTTIIOONN
     The mmoouunntt__ssmmbbffss command mounts a share from a remote server using
     SMB/CIFS protocol.

     The options are as follows:

     --EE _c_s_1::_c_s_2
             Specifies local (_c_s_1) and server's (_c_s_2) character sets.

     --II _h_o_s_t
             Do not use NetBIOS name resolver and connect directly to _h_o_s_t,
             which can be either a valid DNS name or an IP address.

     --LL _l_o_c_a_l_e
             Use _l_o_c_a_l_e for lower/upper case conversion routines.  Set the lo‐
             cale for case conversion.  By default, mmoouunntt__ssmmbbffss tries to use
             an environment variable LC_* to determine it.

     --MM _c_r_i_g_h_t_s::_s_r_i_g_h_t_s
             Assign access rights to the newly created connection.

     --NN      Do not ask for a password.  At run time, mmoouunntt__ssmmbbffss reads the
             _~_/_._n_s_m_b_r_c file for additional configuration parameters and a
             password.  If no password is found, mmoouunntt__ssmmbbffss prompts for it.

     --OO _c_o_w_n_e_r::_c_g_r_o_u_p//_s_o_w_n_e_r::_s_g_r_o_u_p
             Assign owner/group attributes to the newly created connection.

     --RR _r_e_t_r_y_c_o_u_n_t
             How many retries should be done before the SMB requester decides
             to drop the connection.  Default is 4.

     --TT _t_i_m_e_o_u_t
             Timeout in seconds for each request.  Default is 15.

     --UU _u_s_e_r_n_a_m_e
             Username to authenticate with.

     --WW _w_o_r_k_g_r_o_u_p
             This option specifies the workgroup to be used in the authentica‐
             tion request.

     --cc _c_a_s_e
             Set a _c_a_s_e option which affects name representation.  _c_a_s_e can be
             one of the following:

             _V_a_l_u_e  _M_e_a_n_i_n_g

             ll      All existing file names are converted to lower case.
                    Newly created file gets a lower case.

             uu      All existing file names are converted to upper case.
                    Newly created file gets an upper case.

     --ff _m_o_d_e, --dd _m_o_d_e
             Specify permissions that should be assigned to files and directo‐
             ries.  The values must be specified as octal numbers.  Default
             value for the file mode is taken from mount point, default value
             for the directory mode adds execute permission where the file
             mode gives read permission.

             Note that these permissions can differ from the rights granted by
             SMB server.

     --uu _u_i_d, --gg _g_i_d
             User ID and group ID assigned to files.  The default are owner
             and group IDs from the directory where the volume is mounted.

     ////_u_s_e_r@@_s_e_r_v_e_r[::_p_o_r_t_1[::_p_o_r_t_2]]//_s_h_a_r_e
             The mmoouunntt__ssmmbbffss command will use _s_e_r_v_e_r as the NetBIOS name of
             remote computer, _u_s_e_r as the remote user name and _s_h_a_r_e as the
             resource name on a remote server.  Optional _p_o_r_t_1 and _p_o_r_t_2 argu‐
             ments can be used to override default values of port numbers used
             by communication protocols.  For SMB over NetBIOS default value
             for _p_o_r_t_1 are 139, and _p_o_r_t_2 are 137.

     _n_o_d_e    Path to mount point.

FFIILLEESS
     ~/.nsmbrc       Keeps user-specific static parameters for connections and
                     other information.  See
                     _/_u_s_r_/_s_h_a_r_e_/_e_x_a_m_p_l_e_s_/_s_m_b_f_s_/_d_o_t_._n_s_m_b_r_c for details.
     /etc/nsmb.conf  Keeps system-wide static parameters for connections and
                     other information.

EEXXAAMMPPLLEESS
     The following example illustrates how to connect to SMB server “SAMBA” as
     user “GUEST”, and mount shares “PUBLIC” and “TMP”:

           mount_smbfs -I samba.mydomain.com //guest@samba/public /smb/public
           mount_smbfs -I 192.168.20.3 -E koi8-r:cp866 //guest@samba/tmp /smb/tmp

     It is also possible to use fstab(5) for smbfs mounts (the example below
     does not prompt for a password):

           //guest@samba/public    /smb/public     smbfs  rw,noauto,-N 0   0

SSEEEE AALLSSOO
     smbutil(1), nsmb.conf(5)

     Other resources:
     --   Chapter dedicated to Samba configuration in the FreeBSD Handbook:
         hhttttppss::////wwwwww..ffrreeeebbssdd..oorrgg//ddoocc//hhaannddbbooookk//nneettwwoorrkk--ssaammbbaa..hhttmmll

SSTTAANNDDAARRDDSS
     mmoouunntt__ssmmbbffss offers support for SMB/CIFS/SMB1.  It does not support newer
     versions of the protocol like SMB2 and SMB3.  SMB2 and SMB3 are supported
     by software available in the ports(7) collection.

     The list of supported SMB servers includes:
     --   Samba
     --   Windows 95/98/ME/2000/NT4.0 (SPs 4, 5, 6)
     --   IBM LanManager
     --   NetApp

HHIISSTTOORRYY
     SMB/CIFS protocol and SMB/CIFS file system implementation first appeared
     in FreeBSD 4.5.

AAUUTTHHOORRSS
     Boris Popov <bp@butya.kz>, ⟨bp@FreeBSD.org⟩

BBUUGGSS
     Please report bugs to the author.

BSD                            November 1, 2018                            BSD
