The  function  forces  a write of dirty (modified) buffers in the
block buffer cache out to disk. The kernel keeps this information
in core to reduce the number of disk I/O  transfers  required  by
the  system.   As information in the cache is lost after a system
crash a call is issued frequently  by  the  user  process  (about
every 30 seconds).  The function may be used to synchronize indi‐
vidual file descriptor attributes.  may return before the buffers
are completely flushed.  A function call appeared in
