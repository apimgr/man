RLOG(1)                   BSD General Commands Manual                  RLOG(1)

NNAAMMEE
     rrlloogg — display information about RCS files

SSYYNNOOPPSSIISS
     rrlloogg [--bbhhLLNNRRttVV] [--dd_d_a_t_e_s] [--EE_e_n_d_s_e_p] [--ll[_l_o_c_k_e_r_s]] [--rr[_r_e_v_s]] [--SS_r_e_v_s_e_p]
          [--ss_s_t_a_t_e_s] [--ww[_l_o_g_i_n_s]] [--xx_s_u_f_f_i_x_e_s] [--zz_t_z] _f_i_l_e _._._.

DDEESSCCRRIIPPTTIIOONN
     The rrlloogg program displays information about RCS files.

     A file's complete RCS history can be displayed (the default if no options
     are specified) or a subset of its history log can be requested, depending
     on which options are specified.  RCS keywords are displayed using the
     ident(1) utility.

     The following options are supported:

     --bb      Print information about revisions of the default branch only.

     --dd_d_a_t_e_s
             Specify revisions with dates matching the specification.  The
             specification might be as follows:

             date1<date2 or date2>date1
                     Select all revisions between _d_a_t_e_1 and _d_a_t_e_2.

             <date or date>
                     Select all revisions before _d_a_t_e.

             >date or date<
                     Select all revisions after _d_a_t_e.

             date    Select the latest revision before or equal to _d_a_t_e.

             The ‘>’ and ‘<’ characters can be followed by the ‘=’ character
             to imply an inclusive specification.  Several specifications can
             be used by separating them with the ‘;’ character.

             See also the --zz option, below.

     --EE_e_n_d_s_e_p
             Print _e_n_d_s_e_p at the end of each RCS file, instead of the default
             string of 77 equal signs.

     --hh      Print the RCS header, describing a file's branch, lock details,
             symbolic names, etc.

     --LL      Ignore RCS files with no locks set.

     --ll[_l_o_c_k_e_r_s]
             Print information about locked revisions only.  If a comma-sepa‐
             rated list of login names is specified, ignore all locks other
             than those held in the list.

     --NN      Do not print symbolic names.

     --RR      Print name of RCS file only.

     --rr[_r_e_v_s]
             Specify revision(s) to list:

             rev1,rev2,...
                     A list of revisions is specified by separating names or
                     numbers of revisions by the ‘,’ character.

             rev1:rev2
                     List all revisions between _r_e_v_1 and _r_e_v_2 (they must be on
                     the same branch).

             :rev    List all revisions since the beginning of the branch un‐
                     til _r_e_v included.

             rev:    List all revisions of the branch beginning with _r_e_v.

             branch  List all revisions of a branch.

             branch.
                     List the latest revision of the branch _b_r_a_n_c_h.

             branch1:branch2
                     List all revisions of branches between _b_r_a_n_c_h_1 and
                     _b_r_a_n_c_h_2.

             Without argument, the --rr option means the latest revision of the
             default branch.

     --SS_r_e_v_s_e_p
             Print _r_e_v_s_e_p at the end of each RCS revision, instead of the de‐
             fault string of 28 dash signs.

     --ss_s_t_a_t_e_s
             Print information about revisions whose state matches one of the
             specified _s_t_a_t_e_s.  Multiple states may be specified as a comma-
             separated list.

     --tt      Print header and description only.

     --VV      Print RCS's version number.

     --ww[_l_o_g_i_n_s]
             Print information about revisions checked in by users specified
             in a comma-separated list.  If _l_o_g_i_n_s is omitted, the user's lo‐
             gin is assumed.

     --xx_s_u_f_f_i_x_e_s
             Specifies the suffixes for RCS files.  Suffixes should be sepa‐
             rated by the ‘/’ character.

     --zz_t_z    Specify the date output format.  The _t_z argument should be a nu‐
             meric UTC offset (e.g. +02:45 would specify an offset of 2 hours
             45 minutes).  ‘LT’ may instead be used to specify local time.  If
             no argument is given, a default format is used.  This option is
             also used to set the default time zone for dates used in the --dd
             option.

EENNVVIIRROONNMMEENNTT
     RCSINIT  If set, this variable should contain a list of space-delimited
              options that are prepended to the argument list.

EEXXIITT SSTTAATTUUSS
     The rrlloogg utility exits 0 on success, and >0 if an error occurs.

EEXXAAMMPPLLEESS
     Print complete information about files:

           $ rlog RCS/*

     Print the names of RCS files with locks set:

           $ rlog -L -R RCS/*

SSEEEE AALLSSOO
     ci(1), co(1), ident(1), rcs(1), rcsclean(1), rcsdiff(1), rcsmerge(1)

SSTTAANNDDAARRDDSS
     The flags [--qqTT] have no effect and are provided for compatibility only.

BSD                             August 31, 2016                            BSD
