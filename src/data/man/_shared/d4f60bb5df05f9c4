The utility simply executes the given allowing to mount and based
file  systems via or the file.  This command is usually only exe‐
cuted by as users can directly  execute  file  servers  to  mount
them.  The parameter is either the path to the file server or the
file name of it.  In the latter case the is searched in the vari‐
able.   The  parameter,  when specified, will be passed to the as
its first non‐option argument, with being the  second  one.   The
options  are  as  follows: Options are specified with a flag fol‐
lowed by a comma separated string of options.  They are passed to
the without any modifications except for which is typically  used
by  When there is in the list, discards all the other options and
prints the kernel arguments for a file system already mounted  at
Mount  at  Or  in dmesgfs  /dmesg  puffs  rw These are equivalent
to: Mount for at with  option  Or  in  /usr/bin/rot13fs#/home/foo
/mnt/rot13   puffs   rdonly  These are equivalent to: The utility
first appeared in
