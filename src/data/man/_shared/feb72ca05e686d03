BOOT_CONFIG(8)            BSD System Manager's Manual           BOOT_CONFIG(8)

NNAAMMEE
     bboooott__ccoonnffiigg — how to change kernel configuration at boot

DDEESSCCRRIIPPTTIIOONN
     BBOOOOTT__CCOONNFFIIGG is a kernel option that makes it possible to change the con‐
     figuration at boot time.  The same interface is available from userland,
     using the --ee argument to config(8).

     The boot time configuration is invoked by passing the --cc option, either
     from the firmware prompt or, depending on the architecture, when the
     bootstrap loader prompts for a kernel to boot:

     >> OpenBSD/i386 BOOT 3.26
     boot> bboooott --cc
     booting hd0a:/bsd: ...
     ...
     real mem  = 1073086464 (1023MB)
     avail mem = 1043189760 (994MB)
     User Kernel Config
     UKC>

     Changes made can be saved for the next reboot, by using config(8).  How‐
     ever, those changes would not persist across system upgrades and would
     prevent kernel relinking.  To ensure these changes are carried over to
     upgraded kernels, they can be saved to the bsd.re-config(5) configuration
     file.

CCOOMMMMAANNDDSS
     aadddd _d_e_v              Add a device through copying another.

     bbaassee 88 | 1100 | 1166     Change the base of numbers displayed and entered.

     cchhaannggee _d_e_v_n_o | _d_e_v   Modify one or more devices.

     ddiissaabbllee _d_e_v_n_o | _d_e_v  Disable one or more devices.

     eennaabbllee _d_e_v_n_o | _d_e_v   Enable one or more devices.

     eexxiitt                 Continue boot.

     ffiinndd _d_e_v_n_o | _d_e_v     Find one or more devices.

     hheellpp                 Give a short summary of all commands and their argu‐
                          ments.

     lliisstt                 Show all known devices, a screen at a time.

     lliinneess [_c_o_u_n_t]        Set the number of rows per page.

     qquuiitt                 Continue boot.

     sshhooww [_a_t_t_r [_v_a_l]]    Show all devices for which attribute _a_t_t_r has the
                          value _v_a_l.

     vveerrbboossee              Toggle the autoconfig verbose variable.

EEXXAAMMPPLLEESS
     The Ethernet card is not detected at boot because the kernel configura‐
     tion does not match the physical hardware configuration, e.g. wrong IRQ
     in OpenBSD/i386.  The Ethernet card is supposed to use the ne(4) driver.

     UKC> ffiinndd nnee
     24 ne0 at isa0 port 0x240 size 0 iomem 0xd8000 iosiz 0 irq 9 drq -1 drq2 -1 flags 0x0
     25 ne1 at isa0 port 0x300 size 0 iomem -1 iosiz 0 irq 10 drq -1 drq2 -1 flags 0x0
     26 ne* at isapnp0 port -1 size 0 iomem -1 iosiz 0 irq -1 drq -1 flags 0x0
     27 ne* at pci* dev -1 function -1 flags 0x0
     28 ne* at pcmcia* function -1 irq -1 flags 0x0
     UKC>

     ne1 seems to match the configuration except it uses IRQ 5 instead of IRQ
     10.  So the irq on ne1 should be changed via the _c_h_a_n_g_e command.  The de‐
     vice can be specified by either name or number.

     UKC> cchhaannggee nnee11
     25 ne1 at isa0 port 0x300 size 0 iomem -1 iosiz 0 irq 10 drq -1 drq2 -1 flags 0x0
     change (y/n) ? yy
     port [0x300] ?
     size [0] ?
     iomem [-1] ?
     iosiz [0] ?
     irq [10] ? 55
     drq [-1] ?
     drq2 [-1] ?
     flags [0] ?
     25 ne1 changed
     25 ne1 at isa0 port 0x300 size 0 iomem -1 iosiz 0 irq 5 drq -1 drq2 -1 flags 0x0
     UKC>

     The show command is useful for finding which devices have a certain at‐
     tribute.  It can also be used to find those devices with a particular
     value for an attribute.

     UKC> sshhooww sslloott
       2 ahc* at eisa0 slot -1 flags 0x0
      10 uha* at eisa0 slot -1 flags 0x0
      12 ep0 at eisa0 slot -1 flags 0x0
      17 ep* at eisa0 slot -1 flags 0x0
     102 ahb* at eisa0 slot -1 flags 0x0
     103 fea* at eisa0 slot -1 flags 0x0
     UKC> sshhooww ppoorrtt 00xx330000
      25 ne1 at isa0 port 0x300 size 0 iomem -1 iosiz 0 irq 10 drq -1 drq2 -1 flags 0x0
      72 we1 at isa0 port 0x300 size 0 iomem 0xcc000 iosiz 0 irq 10 drq -1 drq2 -1 flags 0x0
      75 el0 at isa0 port 0x300 size 0 iomem -1 iosiz 0 irq 9 drq -1 drq2 -1 flags 0x0
      77 ie1 at isa0 port 0x300 size 0 iomem -1 iosiz 0 irq 10 drq -1 drq2 -1 flags 0x0
     UKC>

     It is possible to add new devices, but only devices that were linked into
     the kernel.  If a new device is added, following devices will be renum‐
     bered.

     UKC> ffiinndd eepp
      11 ep0 at isa0 port -1 size 0 iomem -1 iosiz 0 irq -1 drq -1 drq2 -1 flags 0x0
      12 ep0 at eisa0 slot -1 flags 0x0
      13 ep0 at pci* dev -1 function -1 flags 0x0
      14 ep* at isapnp0 port -1 size 0 iomem -1 iosiz 0 irq -1 drq -1 flags 0x0
      15 ep* at isa0 port -1 size 0 iomem -1 iosiz 0 irq -1 drq -1 drq2 -1 flags 0x0
      16 ep* at eisa0 slot -1 flags 0x0
      17 ep* at pci* dev -1 function -1 flags 0x0
      18 ep* at pcmcia* dev -1 irq -1 flags 0x0
     UKC> aadddd eepp11
     Clone Device (DevNo, 'q' or '?') ? 1133
     Insert before Device (DevNo, 'q' or '?') 1144
      14 ep1 at pci* dev -1 function -1
     UKC> cchhaannggee 1144
      14 ep1 at pci* dev -1 function -1
     change (y/n) ? yy
     dev [-1] ? 1144
     function [-1] ?
     flags [0] ? 1188
      14 ep1 changed
      14 ep1 at pci* dev 14 function -1 flags 0x12
     UKC>

     When configuration is completed, booting can proceed by issuing the qquuiitt
     or eexxiitt commands.

     UKC> qquuiitt
     Continuing...
     mainbus0 (root)

SSEEEE AALLSSOO
     bsd.re-config(5), config(8)

AAUUTTHHOORRSS
     Mats O Jansson <_m_o_j_@_s_t_a_c_k_e_n_._k_t_h_._s_e>

BSD                               May 5, 2025                              BSD
