This  manual page describes features specific to the file server.
Please see for a full description of the available  command  line
options.   The  utility can be used to mount ext2fs file systems.
It uses and to facilitate running the file system as a server  in
userspace.   As  opposed  to does not use file system code within
the kernel and therefore does not require kernel  support  except
Apart  from  a  minor speed penalty there is no downside with re‐
spect to in‐kernel code.  does not require using for mounts  from
regular files and the file path can be passed directly as the pa‐
rameter.   In fact, the use of is discouraged, since it is unable
to properly deal with images on sparse files.  In case the  image
contains multiple partitions, the desired partition must be indi‐
cated  by  appending the token to the path.  The letter specifies
the partition as obtained via For  example,  to  mount  partition
from  image  use It is recommended that untrusted file system im‐
ages be mounted with instead of Corrupt file system  images  com‐
monly  cause the file system to crash the entire kernel, but with
only the userspace server process will dump core.  To use via the
flags and should be given.  Similarly, is used instead of  if  is
added to the options field of The utility first appeared in
