The interface acts as a link between a rump kernel and a host in‐
terface.   Interface  number <n> always corresponds with the host
tap interface tap<n>.  All data sent by is written into  and  all
data read from is passed as Ethernet input to the rump kernel.  A
interface can be created and destroyed in the normal fashion with
The  host’s interface can be further bridged with hardware inter‐
faces to provide full Internet access to a rump kernel.
