GRAID3(8)                 BSD System Manager's Manual                GRAID3(8)

NNAAMMEE
     ggrraaiidd33 — control utility for RAID3 devices

SSYYNNOOPPSSIISS
     ggrraaiidd33 llaabbeell [--FFhhnnrrvvww] [--ss _b_l_o_c_k_s_i_z_e] _n_a_m_e _p_r_o_v _p_r_o_v _p_r_o_v _._._.
     ggrraaiidd33 cclleeaarr [--vv] _p_r_o_v _._._.
     ggrraaiidd33 ccoonnffiigguurree [--aaddffFFhhnnrrRRvvwwWW] _n_a_m_e
     ggrraaiidd33 rreebbuuiilldd [--vv] _n_a_m_e _p_r_o_v
     ggrraaiidd33 iinnsseerrtt [--hhvv] [--nn _n_u_m_b_e_r] _n_a_m_e _p_r_o_v
     ggrraaiidd33 rreemmoovvee [--vv] --nn _n_u_m_b_e_r _n_a_m_e
     ggrraaiidd33 ssttoopp [--ffvv] _n_a_m_e _._._.
     ggrraaiidd33 lliisstt
     ggrraaiidd33 ssttaattuuss
     ggrraaiidd33 llooaadd
     ggrraaiidd33 uunnllooaadd

DDEESSCCRRIIPPTTIIOONN
     The ggrraaiidd33 utility is used for RAID3 array configuration.  After a device
     is created, all components are detected and configured automatically.
     All operations such as failure detection, stale component detection, re‐
     build of stale components, etc. are also done automatically.  The ggrraaiidd33
     utility uses on-disk metadata (the provider's last sector) to store all
     needed information.

     The first argument to ggrraaiidd33 indicates an action to be performed:

     llaabbeell      Create a RAID3 device.  The last given component will contain
                parity data, whilst the others will all contain regular data.
                The number of components must be equal to 3, 5, 9, 17, etc.
                (2^n + 1).

                Additional options include:

                --FF  Do not synchronize after a power failure or system crash.
                    Assumes device is in consistent state.

                --hh  Hardcode providers' names in metadata.

                --nn  Turn off autosynchronization of stale components.

                --rr  Use parity component for reading in round-robin fashion.
                    Without this option the parity component is not used at
                    all for reading operations when the device is in a com‐
                    plete state.  With this option specified random I/O read
                    operations are even 40% faster, but sequential reads are
                    slower.  One cannot use this option if the --ww option is
                    also specified.

                --ss  Manually specify array block size. Block size will be set
                    equal to least common multiple of all component's sector
                    sizes and specified value.  Note that array sector size
                    calculated as multiple of block size and number of regular
                    data components. Big values may decrease performance and
                    compatibility, as all I/O requests have to be multiple of
                    sector size.

                --ww  Use verify reading feature.  When reading from a device in
                    a complete state, also read data from the parity component
                    and verify the data by comparing XORed regular data with
                    parity data.  If verification fails, an EIO error is re‐
                    turned and the value of the
                    _k_e_r_n_._g_e_o_m_._r_a_i_d_3_._s_t_a_t_._p_a_r_i_t_y___m_i_s_m_a_t_c_h sysctl is increased.
                    One cannot use this option if the --rr option is also speci‐
                    fied.

     cclleeaarr      Clear metadata on the given providers.

     ccoonnffiigguurree  Configure the given device.

                Additional options include:

                --aa  Turn on autosynchronization of stale components.

                --dd  Do not hardcode providers' names in metadata.

                --ff  Synchronize device after a power failure or system crash.

                --FF  Do not synchronize after a power failure or system crash.
                    Assumes device is in consistent state.

                --hh  Hardcode providers' names in metadata.

                --nn  Turn off autosynchronization of stale components.

                --rr  Turn on round-robin reading.

                --RR  Turn off round-robin reading.

                --ww  Turn on verify reading.

                --WW  Turn off verify reading.

     rreebbuuiilldd    Rebuild the given component forcibly.  If autosynchronization
                was not turned off for the given device, this command should
                be unnecessary.

     iinnsseerrtt     Add the given component to the existing array, if one of the
                components was removed previously with the rreemmoovvee command or
                if one component is missing and will not be connected again.
                If no number is given, new component will be added instead of
                first missed component.

                Additional options include:

                --hh  Hardcode providers' names in metadata.

     rreemmoovvee     Remove the given component from the given array and clear
                metadata on it.

     ssttoopp       Stop the given arrays.

                Additional options include:

                --ff  Stop the given array even if it is opened.

     lliisstt       See geom(8).

     ssttaattuuss     See geom(8).

     llooaadd       See geom(8).

     uunnllooaadd     See geom(8).

     Additional options include:

     --vv  Be more verbose.

EEXXIITT SSTTAATTUUSS
     Exit status is 0 on success, and 1 if the command fails.

EEXXAAMMPPLLEESS
     Use 3 disks to setup a RAID3 array (with the round-robin reading fea‐
     ture).  Create a file system, mount it, then unmount it and stop device:

           graid3 label -v -r data da0 da1 da2
           newfs /dev/raid3/data
           mount /dev/raid3/data /mnt
           ...
           umount /mnt
           graid3 stop data
           graid3 unload

     Create a RAID3 array, but do not use the automatic synchronization fea‐
     ture.  Rebuild parity component:

           graid3 label -n data da0 da1 da2
           graid3 rebuild data da2

     Replace one data disk with a brand new one:

           graid3 remove -n 0 data
           graid3 insert -n 0 data da5

SSEEEE AALLSSOO
     geom(4), geom(8), gvinum(8), mount(8), newfs(8), umount(8)

HHIISSTTOORRYY
     The ggrraaiidd33 utility appeared in FreeBSD 5.3.

AAUUTTHHOORRSS
     Pawel Jakub Dawidek <_p_j_d_@_F_r_e_e_B_S_D_._o_r_g>

BBUUGGSS
     There should be a section with an implementation description.

     Documentation for sysctls _k_e_r_n_._g_e_o_m_._r_a_i_d_3_._* is missing.

BSD                            January 15, 2012                            BSD
