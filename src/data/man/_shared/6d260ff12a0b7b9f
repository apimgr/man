is  used  to  assign a tty line to a network interface which uses
asynchronous serial lines.  Currently the command is used to  at‐
tach  interfaces.   These interfaces have to be created using the
The resulting point‐to‐point link is not  a  broadcast  interface
and normally has a netmask of 255.255.255.255.  subcommand before
the  command.   The  network source and destination addresses and
other interface  parameters  are  configured  via  The  following
operands  are  supported by Turn on DTR/CTS flow control.  By de‐
fault, no flow control is done.  Turn on  RTS/CTS  flow  control.
By  default,  no  flow control is done.  Turn on the CLOCAL flag,
making it possible to run SLIP on a cable without  modem  control
signals (e.g. DTR, DSR, DCD).  Maintain modem control signals af‐
ter  closing the line.  Specifically, this disables HUPCL.  Don’t
detach from invoking tty.  Specifies the speed of the connection.
If not specified, the default of 9600  is  used.   Specifies  the
line  discipline  to use for the tty.  Supported line disciplines
are (creates a instance).  If this option is not  specified,  the
default  is  This  option is retained for backwards compatibility
with existing scripts.  Specifies the name  of  the  tty  device.
should  be a string of the form or Only the super‐user may attach
a network interface.  To detach the interface, use after  killing
off  the process.  is the name that is shown by Lock, initialize,
and attach a line (on  both  sides):  cu  ‐t  ‐F  [hard|soft]  ‐l
/dev/dty00  ifconfig sl0 create slattach ‐l ‐t slip /dev/dty00 On
the local side:  ifconfig  sl0  inet  10.0.0.2  10.0.0.1  netmask
255.255.255.255  On  the  remote side: ifconfig sl0 inet 10.0.0.1
10.0.0.2 netmask 255.255.255.255  Messages  indicating  that  the
specified  interface  is not configured or created, the requested
address is unknown, or that the user is not privileged but  tried
to  alter  an interface’s configuration.  The command appeared in
There is no way to specify the interface name (sl%d etc.)  to  be
attached  by the command.  There is no way to see which interface
is assigned to the specified tty  by  the  command,  either.   It
would  be  better  if  the network interfaces were created by the
command rather than by using the subcommand before the command.
