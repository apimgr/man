_r_m_d_i_r(2)                      System Calls Manual                     _r_m_d_i_r(2)

NNAAMMEE
       rmdir - delete a directory

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<uunniissttdd..hh>>

       iinntt rrmmddiirr((ccoonnsstt cchhaarr **_p_a_t_h));;

DDEESSCCRRIIPPTTIIOONN
       rrmmddiirr() deletes a directory, which must be empty.

RREETTUURRNN VVAALLUUEE
       On  success,  zero is returned.  On error, -1 is returned, and _e_r_r_n_o is
       set to indicate the error.

EERRRROORRSS
       EEAACCCCEESS Write access to the directory containing _p_a_t_h was  not  allowed,
              or one of the directories in the path prefix of _p_a_t_h did not al‐
              low search permission.  (See also ppaatthh__rreessoolluuttiioonn(7).)

       EEBBUUSSYY  _p_a_t_h is currently in use by the system or some process that pre‐
              vents  its removal.  On Linux, this means _p_a_t_h is currently used
              as a mount point  or  is  the  root  directory  of  the  calling
              process.

       EEFFAAUULLTT _p_a_t_h points outside your accessible address space.

       EEIINNVVAALL _p_a_t_h has _.  as last component.

       EELLOOOOPP  Too many symbolic links were encountered in resolving _p_a_t_h.

       EENNAAMMEETTOOOOLLOONNGG
              _p_a_t_h was too long.

       EENNOOEENNTT A  directory  component  in _p_a_t_h does not exist or is a dangling
              symbolic link.

       EENNOOMMEEMM Insufficient kernel memory was available.

       EENNOOTTDDIIRR
              _p_a_t_h, or a component used as a directory in  _p_a_t_h,  is  not,  in
              fact, a directory.

       EENNOOTTEEMMPPTTYY
              _p_a_t_h  contains entries other than _.  and _._.; or, _p_a_t_h has _._.  as
              its final component.  POSIX.1 also allows EEEEXXIISSTT for this condi‐
              tion.

       EEPPEERRMM  The directory containing _p_a_t_h has the sticky bit  (SS__IISSVVTTXX)  set
              and  the  process’s  effective user ID is neither the user ID of
              the file to be deleted nor that of the directory containing  it,
              and  the  process  is  not  privileged (Linux: does not have the
              CCAAPP__FFOOWWNNEERR capability).

       EEPPEERRMM  The filesystem containing _p_a_t_h does not support the  removal  of
              directories.

       EERROOFFSS  _p_a_t_h refers to a directory on a read‐only filesystem.

SSTTAANNDDAARRDDSS
       POSIX.1‐2024.

HHIISSTTOORRYY
       POSIX.1‐2001, SVr4, 4.3BSD.

BBUUGGSS
       Infelicities  in  the  protocol underlying NFS can cause the unexpected
       disappearance of directories which are still being used.

SSEEEE AALLSSOO
       rrmm(1), rrmmddiirr(1), cchhddiirr(2), cchhmmoodd(2),  mmkkddiirr(2),  rreennaammee(2),  uunnlliinnkk(2),
       uunnlliinnkkaatt(2)

Linux man‐pages (unreleased)      2025‐10‐29                          _r_m_d_i_r(2)
