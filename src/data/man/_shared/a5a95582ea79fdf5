The script is run, by default, every night on a system.  The file
specifies  which  of  the  standard  services are performed.  The
variables described below can be set to or in the file.  Most de‐
fault to but not all.  Check the file if you are in doubt.  (Note
that you should never edit directly, as it is often replaced dur‐
ing system upgrades.)  This runs over the entire  local  filesys‐
tem,  looking for core files.  This runs with the argument.  This
runs the script.  This ages accounting files in  This  runs  with
the  argument.   This  uses the and to give disk status, and also
reports failed components.  In check_disks, show remote file sys‐
tems, which are not reported on by default.  This runs This  runs
with  the  argument,  and  also  checks the database, and runs if
there are hosts in By default, outputs a  summarized  version  of
the report.  If a full version of the output run with the options
is  desired, set this variable.  This runs with the option.  This
runs with This runs the  script  looking  for  possible  security
problems  with  the  system.  Runs the program to check the S/Key
database and informs users of S/Keys that are  about  to  expire.
If  the  file exists, runs the utility to update the database for
use by Refreshes the local database of  package  vulnerabilities.
See  the  settings  in  for details on the actual package checks.
The variables described below can be set  to  modify  the  tests:
Lists filesystem types to ignore during the phase.  Prefixing the
type  with  a  inverts  the match.  For example, will ignore type
filesystems and filesystems that are not Lists  paths  to  ignore
during  the phase.  For example, will not descend into any direc‐
tories under the hierarchy.  This, on a file  server,  allows  to
skip  user data while still scanning system files.  Extra options
to be passed to if is enabled.  If set, the report  generated  by
the  phase  will always be sent, even if it is empty.  Please set
in instead.  If defined, points to the location of  the  packages
database.  Defaults to daily maintenance script daily maintenance
configuration  default  settings,  overridden by local site addi‐
tions to The file appeared in
