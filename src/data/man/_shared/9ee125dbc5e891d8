_T_A_I_L_Q(3)                   Library Functions Manual                   _T_A_I_L_Q(3)

NNAAMMEE
       TAILQ_CONCAT,  TAILQ_EMPTY,  TAILQ_ENTRY,  TAILQ_FIRST,  TAILQ_FOREACH,
       TAILQ_FOREACH_REVERSE, TAILQ_HEAD, TAILQ_HEAD_INITIALIZER,  TAILQ_INIT,
       TAILQ_INSERT_AFTER,  TAILQ_INSERT_BEFORE,  TAILQ_INSERT_HEAD, TAILQ_IN‐
       SERT_TAIL, TAILQ_LAST, TAILQ_NEXT, TAILQ_PREV, TAILQ_REMOVE - implemen‐
       tation of a doubly linked tail queue

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssyyss//qquueeuuee..hh>>

       TTAAIILLQQ__EENNTTRRYY((TTYYPPEE));;

       TTAAIILLQQ__HHEEAADD((HHEEAADDNNAAMMEE,, TTYYPPEE));;
       TTAAIILLQQ__HHEEAADD TTAAIILLQQ__HHEEAADD__IINNIITTIIAALLIIZZEERR((TTAAIILLQQ__HHEEAADD _h_e_a_d));;
       vvooiidd TTAAIILLQQ__IINNIITT((TTAAIILLQQ__HHEEAADD **_h_e_a_d));;

       iinntt TTAAIILLQQ__EEMMPPTTYY((TTAAIILLQQ__HHEEAADD **_h_e_a_d));;

       vvooiidd TTAAIILLQQ__IINNSSEERRTT__HHEEAADD((TTAAIILLQQ__HHEEAADD **_h_e_a_d,,
                                ssttrruucctt TTYYPPEE **_e_l_m,, TTAAIILLQQ__EENNTTRRYY _N_A_M_E));;
       vvooiidd TTAAIILLQQ__IINNSSEERRTT__TTAAIILL((TTAAIILLQQ__HHEEAADD **_h_e_a_d,,
                                ssttrruucctt TTYYPPEE **_e_l_m,, TTAAIILLQQ__EENNTTRRYY _N_A_M_E));;
       vvooiidd TTAAIILLQQ__IINNSSEERRTT__BBEEFFOORREE((ssttrruucctt TTYYPPEE **_l_i_s_t_e_l_m,,
                                ssttrruucctt TTYYPPEE **_e_l_m,, TTAAIILLQQ__EENNTTRRYY _N_A_M_E));;
       vvooiidd TTAAIILLQQ__IINNSSEERRTT__AAFFTTEERR((TTAAIILLQQ__HHEEAADD **_h_e_a_d,, ssttrruucctt TTYYPPEE **_l_i_s_t_e_l_m,,
                                ssttrruucctt TTYYPPEE **_e_l_m,, TTAAIILLQQ__EENNTTRRYY _N_A_M_E));;

       ssttrruucctt TTYYPPEE **TTAAIILLQQ__FFIIRRSSTT((TTAAIILLQQ__HHEEAADD **_h_e_a_d));;
       ssttrruucctt TTYYPPEE **TTAAIILLQQ__LLAASSTT((TTAAIILLQQ__HHEEAADD **_h_e_a_d,, HHEEAADDNNAAMMEE));;
       ssttrruucctt TTYYPPEE **TTAAIILLQQ__PPRREEVV((ssttrruucctt TTYYPPEE **_e_l_m,, HHEEAADDNNAAMMEE,, TTAAIILLQQ__EENNTTRRYY _N_A_M_E));;
       ssttrruucctt TTYYPPEE **TTAAIILLQQ__NNEEXXTT((ssttrruucctt TTYYPPEE **_e_l_m,, TTAAIILLQQ__EENNTTRRYY _N_A_M_E));;

       TTAAIILLQQ__FFOORREEAACCHH((ssttrruucctt TTYYPPEE **_v_a_r,, TTAAIILLQQ__HHEEAADD **_h_e_a_d,,
                                TTAAIILLQQ__EENNTTRRYY _N_A_M_E));;
       TTAAIILLQQ__FFOORREEAACCHH__RREEVVEERRSSEE((ssttrruucctt TTYYPPEE **_v_a_r,, TTAAIILLQQ__HHEEAADD **_h_e_a_d,, HHEEAADDNNAAMMEE,,
                                TTAAIILLQQ__EENNTTRRYY _N_A_M_E));;

       vvooiidd TTAAIILLQQ__RREEMMOOVVEE((TTAAIILLQQ__HHEEAADD **_h_e_a_d,, ssttrruucctt TTYYPPEE **_e_l_m,,
                                TTAAIILLQQ__EENNTTRRYY _N_A_M_E));;

       vvooiidd TTAAIILLQQ__CCOONNCCAATT((TTAAIILLQQ__HHEEAADD **_h_e_a_d_1,, TTAAIILLQQ__HHEEAADD **_h_e_a_d_2,,
                                TTAAIILLQQ__EENNTTRRYY _N_A_M_E));;

DDEESSCCRRIIPPTTIIOONN
       These macros define and operate on doubly linked tail queues.

       In the macro definitions, _T_Y_P_E is the name of a user defined structure,
       that must contain a field of type _T_A_I_L_Q___E_N_T_R_Y, named _N_A_M_E.   The  argu‐
       ment  _H_E_A_D_N_A_M_E is the name of a user defined structure that must be de‐
       clared using the macro TTAAIILLQQ__HHEEAADD().

   CCrreeaattiioonn
       A tail queue is headed by  a  structure  defined  by  the  TTAAIILLQQ__HHEEAADD()
       macro.   This  structure  contains a pair of pointers, one to the first
       element in the queue and the other to the last element  in  the  queue.
       The  elements are doubly linked so that an arbitrary element can be re‐
       moved without traversing the queue.  New elements can be added  to  the
       queue  after  an  existing  element, before an existing element, at the
       head of the queue, or at the end of the queue.  A _T_A_I_L_Q___H_E_A_D  structure
       is declared as follows:

           TAILQ_HEAD(HEADNAME, TYPE) head;

       where  _s_t_r_u_c_t  _H_E_A_D_N_A_M_E is the structure to be defined, and _s_t_r_u_c_t _T_Y_P_E
       is the type of the elements to be linked into the queue.  A pointer  to
       the head of the queue can later be declared as:

           struct HEADNAME *headp;

       (The names _h_e_a_d and _h_e_a_d_p are user selectable.)

       TTAAIILLQQ__EENNTTRRYY()  declares  a  structure that connects the elements in the
       queue.

       TTAAIILLQQ__HHEEAADD__IINNIITTIIAALLIIZZEERR() evaluates to  an  initializer  for  the  queue
       _h_e_a_d.

       TTAAIILLQQ__IINNIITT() initializes the queue referenced by

       TTAAIILLQQ__EEMMPPTTYY()  evaluates  to  true  if there are no items on the queue.
       _h_e_a_d.

   IInnsseerrttiioonn
       TTAAIILLQQ__IINNSSEERRTT__HHEEAADD() inserts the new element _e_l_m  at  the  head  of  the
       queue.

       TTAAIILLQQ__IINNSSEERRTT__TTAAIILL()  inserts  the  new  element  _e_l_m  at the end of the
       queue.

       TTAAIILLQQ__IINNSSEERRTT__BBEEFFOORREE() inserts the new element _e_l_m  before  the  element
       _l_i_s_t_e_l_m.

       TTAAIILLQQ__IINNSSEERRTT__AAFFTTEERR() inserts the new element _e_l_m after the element _l_i_s_‐
       _t_e_l_m.

   TTrraavveerrssaall
       TTAAIILLQQ__FFIIRRSSTT() returns the first item on the queue, or NULL if the queue
       is empty.

       TTAAIILLQQ__LLAASSTT() returns the last item on the queue.  If the queue is empty
       the return value is NULL.

       TTAAIILLQQ__PPRREEVV()  returns  the  previous item on the queue, or NULL if this
       item is the first.

       TTAAIILLQQ__NNEEXXTT() returns the next item on the queue, or NULL if  this  item
       is the last.

       TTAAIILLQQ__FFOORREEAACCHH()  traverses  the queue referenced by _h_e_a_d in the forward
       direction, assigning each element in turn to _v_a_r.  _v_a_r is set  to  NULL
       if the loop completes normally, or if there were no elements.

       TTAAIILLQQ__FFOORREEAACCHH__RREEVVEERRSSEE()  traverses  the queue referenced by _h_e_a_d in the
       reverse direction, assigning each element in turn to _v_a_r.

   RReemmoovvaall
       TTAAIILLQQ__RREEMMOOVVEE() removes the element _e_l_m from the queue.

   OOtthheerr ffeeaattuurreess
       TTAAIILLQQ__CCOONNCCAATT() concatenates the queue headed by _h_e_a_d_2 onto the  end  of
       the one headed by _h_e_a_d_1 removing all entries from the former.

RREETTUURRNN VVAALLUUEE
       TTAAIILLQQ__EEMMPPTTYY()  returns  nonzero  if the queue is empty, and zero if the
       queue contains at least one entry.

       TTAAIILLQQ__FFIIRRSSTT(), TTAAIILLQQ__LLAASSTT(), TTAAIILLQQ__PPRREEVV(), and  TTAAIILLQQ__NNEEXXTT()  return  a
       pointer  to  the first, last, previous, or next _T_Y_P_E structure, respec‐
       tively.

       TTAAIILLQQ__HHEEAADD__IINNIITTIIAALLIIZZEERR() returns an initializer that can be assigned to
       the queue _h_e_a_d.

SSTTAANNDDAARRDDSS
       BSD.

HHIISSTTOORRYY
       4.4BSD.

CCAAVVEEAATTSS
       TTAAIILLQQ__FFOORREEAACCHH() and TTAAIILLQQ__FFOORREEAACCHH__RREEVVEERRSSEE() don’t allow _v_a_r to  be  re‐
       moved  or freed within the loop, as it would interfere with the traver‐
       sal.  TTAAIILLQQ__FFOORREEAACCHH__SSAAFFEE() and TTAAIILLQQ__FFOORREEAACCHH__RREEVVEERRSSEE__SSAAFFEE(), which  are
       present  on  the BSDs but are not present in glibc, fix this limitation
       by allowing _v_a_r to safely be removed  from  the  list  and  freed  from
       within the loop without interfering with the traversal.

EEXXAAMMPPLLEESS
       #include <stddef.h>
       #include <stdio.h>
       #include <stdlib.h>
       #include <sys/queue.h>

       struct entry {
           int data;
           TAILQ_ENTRY(entry) entries;             /* Tail queue */
       };

       TAILQ_HEAD(tailhead, entry);

       int
       main(void)
       {
           struct entry *n1, *n2, *n3, *np;
           struct tailhead head;                   /* Tail queue head */
           int i;

           TAILQ_INIT(&head);                      /* Initialize the queue */

           n1 = malloc(sizeof(struct entry));      /* Insert at the head */
           TAILQ_INSERT_HEAD(&head, n1, entries);

           n1 = malloc(sizeof(struct entry));      /* Insert at the tail */
           TAILQ_INSERT_TAIL(&head, n1, entries);

           n2 = malloc(sizeof(struct entry));      /* Insert after */
           TAILQ_INSERT_AFTER(&head, n1, n2, entries);

           n3 = malloc(sizeof(struct entry));      /* Insert before */
           TAILQ_INSERT_BEFORE(n2, n3, entries);

           TAILQ_REMOVE(&head, n2, entries);       /* Deletion */
           free(n2);
                                                   /* Forward traversal */
           i = 0;
           TAILQ_FOREACH(np, &head, entries)
               np->data = i++;
                                                   /* Reverse traversal */
           TAILQ_FOREACH_REVERSE(np, &head, tailhead, entries)
               printf("%i\n", np->data);
                                                   /* TailQ deletion */
           n1 = TAILQ_FIRST(&head);
           while (n1 != NULL) {
               n2 = TAILQ_NEXT(n1, entries);
               free(n1);
               n1 = n2;
           }
           TAILQ_INIT(&head);

           exit(EXIT_SUCCESS);
       }

SSEEEE AALLSSOO
       iinnssqquuee(3), qquueeuuee(7)

Linux man‐pages (unreleased)      2025‐05‐17                          _T_A_I_L_Q(3)
