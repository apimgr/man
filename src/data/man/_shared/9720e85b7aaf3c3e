VOP_BMAP(9)              BSD Kernel Developer's Manual             VOP_BMAP(9)

NNAAMMEE
     VVOOPP__BBMMAAPP — Logical to physical block number conversion

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ppaarraamm..hh>>
     ##iinncclluuddee <<ssyyss//vvnnooddee..hh>>

     _i_n_t
     VVOOPP__BBMMAAPP(_s_t_r_u_c_t _v_n_o_d_e _*_v_p, _d_a_d_d_r___t _b_n, _s_t_r_u_c_t _b_u_f_o_b_j _*_*_b_o_p, _d_a_d_d_r___t _*_b_n_p,
         _i_n_t _*_r_u_n_p, _i_n_t _*_r_u_n_b);

DDEESSCCRRIIPPTTIIOONN
     This vnode call is used to lookup the physical block number of the file
     system's underlying device where a given logical block of a file is
     stored.  Its arguments are:

     _v_p    The vnode of the file.

     _b_n    Logical block number within the file identified by _v_p.

     _b_o_p   Return storage for the buffer object associated with the file sys‐
           tem's underlying device.

     _b_n_p   Return storage for the physical block number.

     _r_u_n_p  Return storage for the number of succeeding logical blocks that may
           be efficiently read at the same time as the requested block.  This
           will usually be the number of logical blocks whose physical blocks
           are contiguously allocated.  However a file system is free to de‐
           fine "efficient" as it sees fit.

     _r_u_n_b  Like _r_u_n_p but for preceding rather than succeeding blocks.

     Any of the return arguments may be NULL to indicate that the caller does
     not care about that information.

LLOOCCKKSS
     The vnode will be locked on entry and should remain locked on return.

RREETTUURRNN VVAALLUUEESS
     Zero is returned on success, otherwise an error code is returned.

SSEEEE AALLSSOO
     vnode(9)

HHIISSTTOORRYY
     A bbmmaapp() function first appeared in 4.2BSD.

AAUUTTHHOORRSS
     This manual page was written by Alan Somers.

BSD                              June 19, 2019                             BSD
