and  are  shell scripts which reboot or halt the system, and when
next started, the system will skip the normal  the  file  systems
checks.   This  is done by creating a file then invoking the pro‐
gram.  The system startup script, looks for  this  file  and,  if
present, skips the normal invocation of The command appeared in
