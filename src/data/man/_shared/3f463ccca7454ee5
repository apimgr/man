PWCACHE(3)               BSD Library Functions Manual               PWCACHE(3)

NNAAMMEE
     ppwwccaacchhee, uusseerr__ffrroomm__uuiidd, ggrroouupp__ffrroomm__ggiidd — cache password and group entries

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ppwwdd..hh>>

     _c_o_n_s_t _c_h_a_r _*
     uusseerr__ffrroomm__uuiidd(_u_i_d___t _u_i_d, _i_n_t _n_o_u_s_e_r);

     _i_n_t
     uuiidd__ffrroomm__uusseerr(_c_o_n_s_t _c_h_a_r _*_n_a_m_e, _u_i_d___t _*_u_i_d);

     _i_n_t
     ppwwccaacchhee__uusseerrddbb(_i_n_t _(_*_s_e_t_p_a_s_s_e_n_t_)_(_i_n_t_), _v_o_i_d _(_*_e_n_d_p_w_e_n_t_)_(_v_o_i_d_),
         _s_t_r_u_c_t _p_a_s_s_w_d _* _(_*_g_e_t_p_w_n_a_m_)_(_c_o_n_s_t _c_h_a_r _*_),
         _s_t_r_u_c_t _p_a_s_s_w_d _* _(_*_g_e_t_p_w_u_i_d_)_(_u_i_d___t_));

     ##iinncclluuddee <<ggrrpp..hh>>

     _c_o_n_s_t _c_h_a_r _*
     ggrroouupp__ffrroomm__ggiidd(_g_i_d___t _g_i_d, _i_n_t _n_o_g_r_o_u_p);

     _i_n_t
     ggiidd__ffrroomm__ggrroouupp(_c_o_n_s_t _c_h_a_r _*_n_a_m_e, _g_i_d___t _*_g_i_d);

     _i_n_t
     ppwwccaacchhee__ggrroouuppddbb(_i_n_t _(_*_s_e_t_g_r_o_u_p_e_n_t_)_(_i_n_t_), _v_o_i_d _(_*_e_n_d_g_r_e_n_t_)_(_v_o_i_d_),
         _s_t_r_u_c_t _g_r_o_u_p _* _(_*_g_e_t_g_r_n_a_m_)_(_c_o_n_s_t _c_h_a_r _*_),
         _s_t_r_u_c_t _g_r_o_u_p _* _(_*_g_e_t_g_r_g_i_d_)_(_g_i_d___t_));

DDEESSCCRRIIPPTTIIOONN
     The uusseerr__ffrroomm__uuiidd() function returns the user name associated with the
     argument _u_i_d.  The user name is cached so that multiple calls with the
     same _u_i_d do not require additional calls to getpwuid(3).  If there is no
     user associated with the _u_i_d, a pointer is returned to a string represen‐
     tation of the _u_i_d, unless the argument _n_o_u_s_e_r is non-zero, in which case
     a NULL pointer is returned.

     The ggrroouupp__ffrroomm__ggiidd() function returns the group name associated with the
     argument _g_i_d.  The group name is cached so that multiple calls with the
     same _g_i_d do not require additional calls to getgrgid(3).  If there is no
     group associated with the _g_i_d, a pointer is returned to a string repre‐
     sentation of the _g_i_d, unless the argument _n_o_g_r_o_u_p is non-zero, in which
     case a NULL pointer is returned.

     The uuiidd__ffrroomm__uusseerr() function returns the uid associated with the argument
     _n_a_m_e.  The uid is cached so that multiple calls with the same _n_a_m_e do not
     require additional calls to getpwnam(3).  If there is no uid associated
     with the _n_a_m_e, the uuiidd__ffrroomm__uusseerr() function returns -1; otherwise it
     stores the uid at the location pointed to by _u_i_d and returns 0.

     The ggiidd__ffrroomm__ggrroouupp() function returns the gid associated with the argu‐
     ment _n_a_m_e.  The gid is cached so that multiple calls with the same _n_a_m_e
     do not require additional calls to getgrnam(3).  If there is no gid asso‐
     ciated with the _n_a_m_e, the ggiidd__ffrroomm__ggrroouupp() function returns -1; otherwise
     it stores the gid at the location pointed to by _g_i_d and returns 0.

     The ppwwccaacchhee__uusseerrddbb() function changes the user database access routines
     which uusseerr__ffrroomm__uuiidd() and uuiidd__ffrroomm__uusseerr() call to search for users.  The
     caches are flushed and the existing eennddppwweenntt() method is called before
     switching to the new routines.  _g_e_t_p_w_n_a_m and _g_e_t_p_w_u_i_d must be provided,
     and _s_e_t_p_a_s_s_e_n_t and _e_n_d_p_w_e_n_t may be NULL pointers.

     The ppwwccaacchhee__ggrroouuppddbb() function changes the group database access routines
     which ggrroouupp__ffrroomm__ggiidd() and ggiidd__ffrroomm__ggrroouupp() call to search for groups.
     The caches are flushed and the existing eennddggrreenntt() method is called be‐
     fore switching to the new routines.  _g_e_t_g_r_n_a_m and _g_e_t_g_r_g_i_d must be pro‐
     vided, and _s_e_t_g_r_o_u_p_e_n_t and _e_n_d_g_r_e_n_t may be NULL pointers.

EERRRROORRSS
     If insufficient memory is available, uusseerr__ffrroomm__uuiidd() and ggrroouupp__ffrroomm__ggiidd()
     may return NULL pointers.  _e_r_r_n_o is set to ENOMEM.

SSEEEE AALLSSOO
     getgrgid(3), getgrnam(3), getpwnam(3), getpwuid(3)

HHIISSTTOORRYY
     The uusseerr__ffrroomm__uuiidd() and ggrroouupp__ffrroomm__ggiidd() functions first appeared in
     4.4BSD.

     The uuiidd__ffrroomm__uusseerr() and ggiidd__ffrroomm__ggrroouupp() functions first appeared in
     NetBSD 1.4.

     The ppwwccaacchhee__uusseerrddbb() and ppwwccaacchhee__ggrroouuppddbb() functions first appeared in
     NetBSD 1.6 and FreeBSD 10.0.

BSD                               May 5, 2020                              BSD
