The utility loads a kernel module specified by the parameter into
the  running  system.  Modules are loaded from the default system
module areas unless the parameter contains a path separator char‐
acter The options to are as follows: Pass the  module  a  boolean
property  with the name may be either or When used in conjunction
with delete from the specified.  Force the module to  be  loaded.
When  a module is loaded, the kernel checks if the module is com‐
patible with the running kernel and will refuse to  load  modules
that are potentially incompatible.  This option disables compati‐
bility checks.  an incompatible module can cause system instabil‐
ity,  including data loss or corruption.  This option is also re‐
quired for re‐enabling a builtin module that was  disabled  using
Pass  the  module  an integer property with the name and integral
value When used in conjunction with merge new options with an ex‐
isting property list contained in This option  tells  the  kernel
not  to load an associated property list.  Output a property list
suitable for loading along with a module.  When  using  this  op‐
tion,  you  do not need to specify a module.  Use and to read and
modify an existing property list.  Pass the module a string prop‐
erty with the name and string value The utility exits with a sta‐
tus of 0 on success and with a nonzero status if an error occurs.
A utility appeared in The command was designed to be  similar  in
functionality to the corresponding command in was switched to the
kernel  object  linker module framework for derived from the same
framework in The  original  implementation  was  written  by  The
switch to the kernel object linker module framework was by
