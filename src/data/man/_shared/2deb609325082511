G_BIO(9)                 BSD Kernel Developer's Manual                G_BIO(9)

NNAAMMEE
     gg__nneeww__bbiioo, gg__cclloonnee__bbiioo, gg__ddeessttrrooyy__bbiioo, gg__ffoorrmmaatt__bbiioo, gg__pprriinntt__bbiioo,
     gg__rreesseett__bbiioo — GEOM bio controlling functions

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//bbiioo..hh>>
     ##iinncclluuddee <<ggeeoomm//ggeeoomm..hh>>

     _s_t_r_u_c_t _b_i_o _*
     gg__nneeww__bbiioo(_v_o_i_d);

     _s_t_r_u_c_t _b_i_o _*
     gg__aalllloocc__bbiioo(_v_o_i_d);

     _s_t_r_u_c_t _b_i_o _*
     gg__cclloonnee__bbiioo(_s_t_r_u_c_t _b_i_o _*_b_p);

     _s_t_r_u_c_t _b_i_o _*
     gg__dduupplliiccaattee__bbiioo(_s_t_r_u_c_t _b_i_o _*_b_p);

     _v_o_i_d
     gg__ddeessttrrooyy__bbiioo(_s_t_r_u_c_t _b_i_o _*_b_p);

     _v_o_i_d
     gg__ffoorrmmaatt__bbiioo(_s_t_r_u_c_t _s_b_u_f _*_s_b, _c_o_n_s_t _s_t_r_u_c_t _b_i_o _*_b_p);

     _v_o_i_d
     gg__pprriinntt__bbiioo(_s_t_r_u_c_t _s_b_u_f _*_s_b, _c_o_n_s_t _c_h_a_r _*_p_r_e_f_i_x, _c_o_n_s_t _s_t_r_u_c_t _b_i_o _*_b_p,
         _c_o_n_s_t _c_h_a_r _*_f_m_t_s_u_f_f_i_x, _._._.);

     _v_o_i_d
     gg__rreesseett__bbiioo(_s_t_r_u_c_t _b_i_o _*_b_p);

DDEESSCCRRIIPPTTIIOONN
     A _s_t_r_u_c_t _b_i_o is used by GEOM to describe I/O requests, its most important
     fields are described below:

     _b_i_o___c_m_d        I/O request command.  There are five I/O requests avail‐
                    able in GEOM:

                    BIO_READ     A read request.

                    BIO_WRITE    A write request.

                    BIO_DELETE   Indicates that a certain range of data is no
                                 longer used and that it can be erased or
                                 freed as the underlying technology supports.
                                 Technologies like flash adaptation layers can
                                 arrange to erase the relevant blocks before
                                 they will become reassigned and cryptographic
                                 devices may want to fill random bits into the
                                 range to reduce the amount of data available
                                 for attack.

                    BIO_GETATTR  Inspect and manipulate out-of-band attributes
                                 on a particular provider or path.  Attributes
                                 are named by ascii strings and are stored in
                                 the _b_i_o___a_t_t_r_i_b_u_t_e field.

                    BIO_FLUSH    Tells underlying providers to flush their
                                 write caches.

     _b_i_o___f_l_a_g_s      Available flags:

                    BIO_ERROR  Request failed (error value is stored in
                               _b_i_o___e_r_r_o_r field).

                    BIO_DONE   Request finished.

     _b_i_o___c_f_l_a_g_s     Private use by the consumer.

     _b_i_o___p_f_l_a_g_s     Private use by the provider.

     _b_i_o___o_f_f_s_e_t     Offset into provider.

     _b_i_o___d_a_t_a       Pointer to data buffer.

     _b_i_o___e_r_r_o_r      Error value when BIO_ERROR is set.

     _b_i_o___d_o_n_e       Pointer to function which will be called when the request
                    is finished.

     _b_i_o___d_r_i_v_e_r_1    Private use by the provider.

     _b_i_o___d_r_i_v_e_r_2    Private use by the provider.

     _b_i_o___c_a_l_l_e_r_1    Private use by the consumer.

     _b_i_o___c_a_l_l_e_r_2    Private use by the consumer.

     _b_i_o___a_t_t_r_i_b_u_t_e  Attribute string for BIO_GETATTR request.

     _b_i_o___f_r_o_m       Consumer to use for request (attached to provider stored
                    in _b_i_o___t_o field) (typically read-only for a class).

     _b_i_o___t_o         Destination provider (typically read-only for a class).

     _b_i_o___l_e_n_g_t_h     Request length in bytes.

     _b_i_o___c_o_m_p_l_e_t_e_d  Number of bytes completed, but they may not be completed
                    from the front of the request.

     _b_i_o___c_h_i_l_d_r_e_n   Number of _b_i_o clones (typically read-only for a class).

     _b_i_o___i_n_b_e_d      Number of finished _b_i_o clones.

     _b_i_o___p_a_r_e_n_t     Pointer to parent _b_i_o.

     The gg__nneeww__bbiioo() function allocates a new, empty _b_i_o structure.

     gg__aalllloocc__bbiioo() - same as gg__nneeww__bbiioo(), but always succeeds (allocates bio
     with the M_WAITOK malloc flag).

     The gg__cclloonnee__bbiioo() function allocates a new _b_i_o structure and copies the
     following fields from the _b_i_o given as an argument to clone: _b_i_o___c_m_d,
     _b_i_o___l_e_n_g_t_h, _b_i_o___o_f_f_s_e_t, _b_i_o___d_a_t_a, _b_i_o___a_t_t_r_i_b_u_t_e.  The field _b_i_o___p_a_r_e_n_t in
     the clone points to the passed _b_i_o and the field _b_i_o___c_h_i_l_d_r_e_n in the
     passed _b_i_o is incremented.

     This function should be used for every request which enters through the
     provider of a particular geom and needs to be scheduled down.  Proper or‐
     der is:

     1.   Clone the received _s_t_r_u_c_t _b_i_o.
     2.   Modify the clone.
     3.   Schedule the clone on its own consumer.

     gg__dduupplliiccaattee__bbiioo() - same as gg__cclloonnee__bbiioo(), but always succeeds (allocates
     bio with the M_WAITOK malloc flag).

     The gg__ddeessttrrooyy__bbiioo() function deallocates and destroys the given _b_i_o
     structure.

     The gg__ffoorrmmaatt__bbiioo() function prints information about the given _b_i_o struc‐
     ture into the provided _s_b_u_f.

     The gg__pprriinntt__bbiioo() function is a convenience wrapper around gg__ffoorrmmaatt__bbiioo()
     that can be used for debugging purposes.  It prints a provided _p_r_e_f_i_x
     string, followed by the formatted _b_i_o, followed by a _f_m_t_s_u_f_f_i_x in the
     style of printf(9).  Any of the prefix or suffix strings may be the empty
     string.  gg__pprriinntt__bbiioo() always prints a newline character at the end of
     the line.

     The gg__rreesseett__bbiioo() function resets the given _b_i_o structure back to its
     initial state.  gg__rreesseett__bbiioo() preserves internal data structures, while
     setting all user visible fields to their initial values.  When reusing a
     _b_i_o obtained from gg__nneeww__bbiioo(), gg__aalllloocc__bbiioo(), gg__cclloonnee__bbiioo(), or
     gg__dduupplliiccaattee__bbiioo() for multiple transactions, gg__rreesseett__bbiioo() must be called
     between the transactions in lieu of bbzzeerroo().  While not strictly required
     for a _b_i_o structure created by other means, gg__rreesseett__bbiioo() should be used
     to initialize it and between transactions.

RREETTUURRNN VVAALLUUEESS
     The gg__nneeww__bbiioo() and gg__cclloonnee__bbiioo() functions return a pointer to the allo‐
     cated _b_i_o, or NULL if an error occurred.

EEXXAAMMPPLLEESS
     Implementation of “NULL-transformation”, meaning that an I/O request is
     cloned and scheduled down without any modifications.  Let us assume that
     field _e_x___c_o_n_s_u_m_e_r in structure _e_x_a_m_p_l_e___s_o_f_t_c contains a consumer attached
     to the provider we want to operate on.

           void
           example_start(struct bio *bp)
           {
                   struct example_softc *sc;
                   struct bio *cbp;

                   g_print_bio("Request received: ", bp, "");

                   sc = bp->bio_to->geom->softc;
                   if (sc == NULL) {
                           g_io_deliver(bp, ENXIO);
                           return;
                   }

                   /* Let's clone our bio request. */
                   cbp = g_clone_bio(bp);
                   if (cbp == NULL) {
                           g_io_deliver(bp, ENOMEM);
                           return;
                   }
                   cbp->bio_done = g_std_done;     /* Standard 'done' function. */

                   /* Ok, schedule it down. */
                   /*
                    * The consumer can be obtained from
                    * LIST_FIRST(&bp->bio_to->geom->consumer) as well,
                    * if there is only one in our geom.
                    */
                   g_io_request(cbp, sc->ex_consumer);
           }

SSEEEE AALLSSOO
     geom(4), DECLARE_GEOM_CLASS(9), g_access(9), g_attach(9), g_consumer(9),
     g_data(9), g_event(9), g_geom(9), g_provider(9), g_provider_by_name(9),
     g_wither_geom(9)

AAUUTTHHOORRSS
     This manual page was written by Pawel Jakub Dawidek <_p_j_d_@_F_r_e_e_B_S_D_._o_r_g>.

BSD                             August 7, 2019                             BSD
