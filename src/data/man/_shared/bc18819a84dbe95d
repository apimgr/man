PCAP_IS_SWAPPED(3)         Library Functions Manual         PCAP_IS_SWAPPED(3)



NNAAMMEE
       pcap_is_swapped - find out whether a savefile has the native byte order

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ppccaapp//ppccaapp..hh>>

       iinntt ppccaapp__iiss__sswwaappppeedd((ppccaapp__tt **pp));;

DDEESSCCRRIIPPTTIIOONN
       ppccaapp__iiss__sswwaappppeedd()  returns  true (11) if _p refers to a ``savefile'' that
       uses a different byte order than the current system.  For a  live  cap‐
       ture, it always returns false (00).

       It  must  not  be called on a pcap descriptor created by ppccaapp__ccrreeaattee(3)
       that has not yet been activated by ppccaapp__aaccttiivvaattee(3).

RREETTUURRNN VVAALLUUEE
       ppccaapp__iiss__sswwaappppeedd() returns true (11) or false (00) on success and PPCCAAPP__EERR‐‐
       RROORR__NNOOTT__AACCTTIIVVAATTEEDD  if  called on a capture handle that has been created
       but not activated.

SSEEEE AALLSSOO
       ppccaapp(3)



                                 7 April 2014               PCAP_IS_SWAPPED(3)
