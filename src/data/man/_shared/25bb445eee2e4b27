The  file is used as initial configuration for the packet filter.
This file is loaded before  the  network  is  configured  by  the
script  Its  purpose  is to protect the machine from possible at‐
tacks between the network configuration and the  loading  of  the
final ruleset.  The syntax of this file is described in Note that
at  the  stage  the  configuration  is loaded, the network inter‐
face(s) do not have an IP address yet, so you use rules that  de‐
rive  addresses  from  an interface (for example: Default initial
ruleset file.  Override of  the  default  initial  ruleset  file.
When  using  NFS (e.g. diskless situations), you’ll also need the
following rules in addition to the default rules to unblock  NFS:
scrub  in all no‐df pass in proto udp from any port { 111, 2049 }
to any pass out proto udp from any to any port { 111, 2049 }
