WPA_CLI(8)                BSD System Manager's Manual               WPA_CLI(8)

NNAAMMEE
     wwppaa__ccllii — console utility for WiFi authentication with wpa_supplicant

SSYYNNOOPPSSIISS
     wwppaa__ccllii [--pp _p_a_t_h___t_o___c_t_r_l___s_o_c_k_e_t_s] [--ii _i_f_n_a_m_e] [--hhvvBB] [--aa _a_c_t_i_o_n___f_i_l_e]
             [--PP _p_i_d___f_i_l_e] [--gg _g_l_o_b_a_l___c_t_r_l] [--GG _p_i_n_g___i_n_t_e_r_v_a_l] _c_o_m_m_a_n_d _._._.

DDEESSCCRRIIPPTTIIOONN
     The wwppaa__ccllii utility is a text-based frontend program for interacting with
     wpa_supplicant(8).  It is used to query current status, change configura‐
     tion, trigger events, and request interactive user input.

     The wwppaa__ccllii utility can show the current authentication status, selected
     security mode, dot11 and dot1x MIBs, etc.  In addition, wwppaa__ccllii can con‐
     figure EAPOL state machine parameters and trigger events such as reasso‐
     ciation and IEEE 802.1X logoff/logon.

     The wwppaa__ccllii utility provides an interface to supply authentication infor‐
     mation such as username and password when it is not provided in the
     wpa_supplicant.conf(5) configuration file.  This can be used, for exam‐
     ple, to implement one-time passwords or generic token card authentication
     where the authentication is based on a challenge-response that uses an
     external device for generating the response.

     The wwppaa__ccllii utility supports two modes: interactive and command line.
     Both modes share the same command set and the main difference is in in‐
     teractive mode providing access to unsolicited messages (event messages,
     username/password requests).

     Interactive mode is started when wwppaa__ccllii is executed without any parame‐
     ters on the command line.  Commands are then entered from the controlling
     terminal in response to the wwppaa__ccllii prompt.  In command line mode, the
     same commands are entered as command line arguments.

     The control interface of wpa_supplicant(8) can be configured to allow
     non-root user access by using the _c_t_r_l___i_n_t_e_r_f_a_c_e___g_r_o_u_p parameter in the
     wpa_supplicant.conf(5) configuration file.  This makes it possible to run
     wwppaa__ccllii with a normal user account.

AAUUTTHHEENNTTIICCAATTIIOONN PPAARRAAMMEETTEERRSS
     When wpa_supplicant(8) needs authentication parameters, such as username
     and password, that are not present in the configuration file, it sends a
     request message to all attached frontend programs, e.g., wwppaa__ccllii in in‐
     teractive mode.  The wwppaa__ccllii utility shows these requests with a
     “CTRL-REQ-⟨_t_y_p_e⟩-⟨_i_d⟩:⟨_t_e_x_t⟩” prefix, where ⟨_t_y_p_e⟩ is IDENTITY, PASSWORD,
     or OTP (One-Time Password), ⟨_i_d⟩ is a unique identifier for the current
     network, ⟨_t_e_x_t⟩ is a description of the request.  In the case of an OTP
     (One-Time Password) request, it includes the challenge from the authenti‐
     cation server.

     A user must supply wpa_supplicant(8) the needed parameters in response to
     these requests.

     For example,

           CTRL-REQ-PASSWORD-1:Password needed for SSID foobar
           > password 1 mysecretpassword

           Example request for generic token card challenge-response:

           CTRL-REQ-OTP-2:Challenge 1235663 needed for SSID foobar
           > otp 2 9876

OOPPTTIIOONNSS
     These options are available:

     --pp _p_a_t_h
             Control sockets path.  This should match the ccttrrll__iinntteerrffaaccee in
             wpa_supplicant.conf(5).  The default path is
             _/_v_a_r_/_r_u_n_/_w_p_a___s_u_p_p_l_i_c_a_n_t.

     --ii _i_f_n_a_m_e
             Interface to be configured.  By default, the first interface
             found in the socket path is used.

     --hh      Show help.

     --vv      Show version information.

     --BB      Run the daemon in the background.

     --aa _a_c_t_i_o_n___f_i_l_e
             Run in daemon mode, executing the action file based on events
             from wpa_supplicant(8).

     --PP _p_i_d___f_i_l_e
             PID file location.

     --gg _g_l_o_b_a_l___c_t_r_l
             Use a global control interface to wpa_supplicant(8) rather than
             the default Unix domain sockets.

     --GG _p_i_n_g___i_n_t_e_r_v_a_l
             Wait “ping_interval” seconds before sending each ping to
             wpa_supplicant(8).  See the ppiinngg command.

     command
             See available commands in the next section.

CCOOMMMMAANNDDSS
     These commands can be supplied on the command line or at a prompt when
     operating interactively.

     ssttaattuuss  Report the current WPA/EAPOL/EAP status for the current inter‐
             face.

     iiffnnaammee  Show the current interface name.  The default interface is the
             first interface found in the socket path.

     ppiinngg    Ping the wpa_supplicant(8) utility.  This command can be used to
             test the status of the wpa_supplicant(8) daemon.

     mmiibb     Report MIB variables (dot1x, dot11) for the current interface.

     hheellpp    Show usage help.

     iinntteerrffaaccee [_i_f_n_a_m_e]
             Show available interfaces and/or set the current interface when
             multiple interfaces are available.

     lleevveell _d_e_b_u_g___l_e_v_e_l
             Change the debugging level in wpa_supplicant(8).  Larger numbers
             generate more messages.

     lliicceennssee
             Display the full license for wwppaa__ccllii.

     llooggooffff  Send the IEEE 802.1X EAPOL state machine into the “logoff” state.

     llooggoonn   Send the IEEE 802.1X EAPOL state machine into the “logon” state.

     sseett [_s_e_t_t_i_n_g_s]
             Set variables.  When no arguments are supplied, the known vari‐
             ables and their settings are displayed.

     ppmmkkssaa   Show the contents of the PMKSA cache.

     rreeaassssoocciiaattee
             Force a reassociation to the current access point.

     rreeccoonnffiigguurree
             Force wpa_supplicant(8) to re-read its configuration file.

     pprreeaauutthheennttiiccaattee _B_S_S_I_D
             Force preauthentication of the specified _B_S_S_I_D.

     iiddeennttiittyy _n_e_t_w_o_r_k___i_d _i_d_e_n_t_i_t_y
             Configure an identity for an SSID.

     ppaasssswwoorrdd _n_e_t_w_o_r_k___i_d _p_a_s_s_w_o_r_d
             Configure a password for an SSID.

     nneeww__ppaasssswwoorrdd _n_e_t_w_o_r_k___i_d _p_a_s_s_w_o_r_d
             Change the password for an SSID.

     PPIINN _n_e_t_w_o_r_k___i_d _p_i_n
             Configure a PIN for an SSID.

     ppaasssspphhrraassee _n_e_t_w_o_r_k___i_d _p_a_s_s_p_h_r_a_s_e
             Configure a private key passphrase for an SSID.

     bbssssiidd _n_e_t_w_o_r_k___i_d _b_s_s_i_d
             Set a preferred BSSID for an SSID

     bbllaacckklliisstt [_b_s_s_i_d | _c_l_e_a_r]
             Add a BSSID to the blacklist.  When invoked without any extra ar‐
             guments, display the blacklist.  Specifying _c_l_e_a_r causes wwppaa__ccllii
             to clear the blacklist.

     lliisstt__nneettwwoorrkkss
             List configured networks.

     sseelleecctt__nneettwwoorrkk _n_e_t_w_o_r_k___i_d
             Select a network and disable others.

     eennaabbllee__nneettwwoorrkk _n_e_t_w_o_r_k___i_d
             Enable a network.

     ddiissaabbllee__nneettwwoorrkk _n_e_t_w_o_r_k___i_d
             Disable a network.

     aadddd__nneettwwoorrkk
             Add a network.

     rreemmoovvee__nneettwwoorrkk _n_e_t_w_o_r_k___i_d
             Remove a network.

     sseett__nneettwwoorrkk [_n_e_t_w_o_r_k___i_d _v_a_r_i_a_b_l_e _v_a_l_u_e]
             Set network variables.  Shows a list of variables when run with‐
             out arguments.

     ggeett__nneettwwoorrkk _n_e_t_w_o_r_k___i_d _v_a_r_i_a_b_l_e
             Get network variables.

     ddiissccoonnnneecctt
             Disconnect and wait for reassociate/reconnect command before con‐
             necting.

     rreeccoonnnneecctt
             Similar to rreeaassssoocciiaattee, but only takes effect if already discon‐
             nected.

     ssccaann    Request new BSS scan.

     ssccaann__rreessuullttss
             Get the latest BSS scan results.  This command can be invoked af‐
             ter running a BSS scan with ssccaann.

     bbssss [_i_d_x | _b_s_s_i_d]
             Get a detailed BSS scan result for the network identified by
             “bssid” or “idx”.

     oottpp _n_e_t_w_o_r_k___i_d _p_a_s_s_w_o_r_d
             Configure a one-time password for an SSID.

     tteerrmmiinnaattee
             Force wpa_supplicant(8) to terminate.

     iinntteerrffaaccee__aadddd _i_f_n_a_m_e [_c_o_n_f_n_a_m_e _d_r_i_v_e_r _c_t_r_l___i_n_t_e_r_f_a_c_e _d_r_i_v_e_r___p_a_r_a_m
             _b_r_i_d_g_e___n_a_m_e]
             Add a new interface with the given parameters.

     iinntteerrffaaccee__rreemmoovvee _i_f_n_a_m_e
             Remove the interface.

     iinntteerrffaaccee__lliisstt
             List available interfaces.

     qquuiitt    Exit wwppaa__ccllii.

SSEEEE AALLSSOO
     wpa_supplicant.conf(5), wpa_supplicant(8)

HHIISSTTOORRYY
     The wwppaa__ccllii utility first appeared in FreeBSD 6.0.

AAUUTTHHOORRSS
     The wwppaa__ccllii utility was written by Jouni Malinen <_j_@_w_1_._f_i>.  This manual
     page is derived from the _R_E_A_D_M_E and _w_p_a___c_l_i_._c files included in the
     wwppaa__ssuupppplliiccaanntt distribution.

BSD                              June 21, 2024                             BSD
