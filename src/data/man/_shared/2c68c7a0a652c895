For  a.out‐format  core files: For ELF‐format core files: A small
number of signals which cause abnormal termination of  a  process
also  cause a record of the process’s in‐core state to be written
to disk for later examination by one of the  available  debuggers
(see  This  memory  image  is written to a file named from a per‐
process template; provided the terminated process had write  per‐
mission,  and  provided  the  abnormality  did not cause a system
crash.  (In this event, the decision to save the core file is ar‐
bitrary, see The file  is  named  from  a  per‐process  template,
mapped  to the sysctl variable (where <pid> has to be replaced by
the pid in decimal format of the process).  This template is  ei‐
ther  an  absolute or relative path name, in which format charac‐
ters can be used, preceded by the percent character The following
characters  are  recognized  as  format  and   substituted:   The
process’s  name The PID of the process (in decimal) The process’s
creation date (a la in decimal) The login name, as returned by By
default, the per‐process template string points  to  the  default
core name template, which is mapped to the sysctl variable Chang‐
ing  this  value  on a live system will change the core name tem‐
plate for all processes which didn’t have a per‐process  template
set.   The default value of the default core name template is and
can be changed at compile‐time with the kernel configuration  op‐
tion (see The per‐process template string is inherited on process
creation, but is reset to point to the default core name template
on execution of a set‐id binary.  The maximum size of a core file
is  limited by Files which would be larger than the limit are not
created.  ELF‐format core files are described by a  standard  ELF
exec  header  and  a series of ELF program headers.  Each program
header describes a range of the  virtual  address  space  of  the
process.  In addition, ELF core files include an ELF note section
which  provides  additional  information  about the process.  The
first note in the note section has a note name of and a note type
of and  contains  the  following  structure:  struct  netbsd_elf‐
core_procinfo {
    uint32_t cpi_version;      /* netbsd_elfcore_procinfo version
*/
    uint32_t cpi_cpisize;      /* sizeof(netbsd_elfcore_procinfo)
*/
    uint32_t cpi_signo;        /* killing signal */
    uint32_t cpi_sigcode;      /* signal code */
    uint32_t cpi_sigpend[4];   /* pending signals */
    uint32_t cpi_sigmask[4];   /* blocked signals */
    uint32_t cpi_sigignore[4]; /* blocked signals */
    uint32_t cpi_sigcatch[4];  /* blocked signals */
    int32_t  cpi_pid;          /* process ID */
    int32_t  cpi_ppid;         /* parent process ID */
    int32_t  cpi_pgrp;         /* process group ID */
    int32_t  cpi_sid;          /* session ID */
    uint32_t cpi_ruid;         /* real user ID */
    uint32_t cpi_euid;         /* effective user ID */
    uint32_t cpi_svuid;        /* saved user ID */
    uint32_t cpi_rgid;         /* real group ID */
    uint32_t cpi_egid;         /* effective group ID */
    uint32_t cpi_svgid;        /* saved group ID */
    uint32_t cpi_nlwps;        /* number of LWPs */
    int8_t   cpi_name[32];     /* copy of p‐>p_comm */
    int32_t  cpi_siglwp;       /* LWP target of killing signal */
};  The  fields of are as follows: The version of this structure.
The current version is defined by the constant.  The size of this
structure.  Signal that caused the process to dump core.  Signal‐
specific code, if any, corresponding to A mask of signals pending
delivery to the process.  This may be examined by copying it to a
The set of signals currently blocked by the process.  This may be
examined by copying it to a The set of  signals  currently  being
ignored  by the process.  This may be examined by copying it to a
The set of  signals  with  registers  signals  handlers  for  the
process.   This  may be examined by copying it to a Process ID of
the process.  Process ID of the parent process.  Process group ID
of the process.  Session ID of the process.  Real user ID of  the
process.  Effective user ID of the process.  Saved user ID of the
process.   Real  group  ID of the process.  Effective group ID of
the process.  Saved group ID of the process.  Number  of  kernel‐
visible  execution contexts (LWPs) of the process.  Process name,
copied from the p_comm field of LWP  target  of  killing  signal.
The second note with name is a note type of and contains an array
of AuxInfo structures.  The note section also contains additional
notes  for  each  kernel‐visible execution context of the process
(LWP).  These notes have names of the form where is the LWP ID of
the execution context, for example: These notes contain  register
and  other  per‐execution  context  data in the same format as is
used by the system call.  The note types correspond  to  the  re‐
quest  numbers  that  return  the same data.  For example, a note
with a note type of PT_GETREGS would contain a with the  register
contents of the execution context.  For a complete list of avail‐
able request types for a given architecture, refer to that archi‐
tecture’s  header  file.  The core file consists of a core header
followed by a number of segments.  Each segment is preceded by  a
core  segment  header.   Both  the  core  header and core segment
header are defined in The core header, specifies the  lengths  of
the  core  header  itself  and each of the following core segment
headers to allow for any  machine  dependent  alignment  require‐
ments.  struct core {
    uint32_t c_midmag;         /* magic, id, flags */
    uint16_t  c_hdrsize;         /*  Size of this header (machdep
algn) */
    uint16_t c_seghdrsize;     /* Size of a segment header */
    uint32_t c_nseg;           /* # of core segments */
    char      c_name[MAXCOMLEN+1];      /* Copy of p‐>p_comm */
    uint32_t c_signo;          /* Killing signal */
    u_long    c_ucode;          /* Signal code */
    u_long    c_cpusize;        /* Size of machine dependent seg‐
ment */
    u_long    c_tsize;          /* Size of traditional text  seg‐
ment */
    u_long     c_dsize;          /* Size of traditional data seg‐
ment */
    u_long    c_ssize;          /* Size of traditional stack seg‐
ment */ }; The fields of are as follows: Core  file  machine  ID,
magic  value,  and flags.  These values may be extracted with the
and macros.  The machine ID values are listed in For a valid core
file, the magic value in the header must be No flags are  defined
for  the  core  header.   Size of this data structure.  Size of a
segment header.  Number of  segments  that  follow  this  header.
Process  name, copied from the p_comm field of Signal that caused
the process to dump core.  Code associated with the signal.  Size
of the segment containing CPU‐specific information.  This segment
will have the flag set.  Size of the segment containing the  pro‐
gram  text.   Size of the segment containing the program’s tradi‐
tional data area.  Size of the segment containing  the  program’s
traditional  stack  area.   This  segment will have the flag set.
The header is followed by segments, each  of  which  is  preceded
with a segment header, struct coreseg {
   uint32_t c_midmag;  /* magic, id, flags */
   u_long    c_addr;    /* Virtual address of segment */
   u_long     c_size;    /* Size of this segment */ }; The fields
of are as follows: Core segment magic  value  and  flags.   These
values  may be extracted with the and macros.  The magic value in
the segment header must be Exactly one of the flags  or  will  be
set to indicate the segment type.  Virtual address of the segment
in the program image.  Meaningless if the segment type is Size of
the  segment,  not including this header.  A file format appeared
in The a.out core file format was introduced in The ELF core file
format was introduced in In releases previous to ELF program  im‐
ages  produced a.out‐format core files.  The member of the struc‐
ture first appeared in However it retained the  procinfo  version
1,  stored  in was added in There is no standard location or name
for the CPU‐dependent data structure stored in the segment.
