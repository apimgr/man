is  a  tool  that lets the administrator update the configuration
and startup files in (and some other directories like and without
having to manually check and modify every file.  The  administra‐
tor  should  run this script after performing an operating system
update (e.g. after running in or after extracting new binary dis‐
tribution files)  to  update  to  the  latest  configuration  and
startup  files.  compares the new configuration files against the
currently installed files.  The user is given the option  of  in‐
stalling, merging or deleting each modified or missing file.  The
user  can  also  view  the differences between the files.  By de‐
fault, it shows the differences in the unified diff format.   The
default format can be toggled to show the differences in unified,
context,  or  side by side formats or an user‐defined command may
be used to view differences.  (And if is installed, it  can  also
show  differences  on a word by word basis.)  also detects if the
user installs certain special files  and  performs  corresponding
tasks  like  remaking  device nodes or rebuilding a database from
the file.  Finally, runs to check the results.  needs a clean set
of new configuration files to compare the existing files against.
These files, called the in this manual, may be derived  from  ei‐
ther  a  source or binary distribution of If the user is updating
from sources (which is the default mode),  will  first  create  a
copy of the reference files by running in installing the files to
a  so‐called  (See  usage of the and options later in this manual
page.)  Although this is the default mode, it is not  recommended
(see  the  section).  Instead of using sources, it is recommended
that the user should extract one or more binary distribution sets
in a special location and use those as the reference  files  (see
usage of the option later in this manual page), or specify one or
more  binary  distribution sets directly (see usage of the option
later in this manual page).  The following options are available:
can automatically update files which have not been  modified  lo‐
cally.   The  flag  instructs  to  store MD5 checksums in and use
these checksums to determine if there have been any local modifi‐
cations.  Use instead of as the top of the file system  hierarchy
to be updated.  For example, will be used instead of Display help
to stdout, and exit.  Automatically skip files with unchanged RCS
IDs.   This  has the effect of leaving alone files that have been
altered locally but which have not been changed in the  reference
files.   Since  this  works  using RCS IDs, files without RCS IDs
will not be skipped even if only modified locally.  This flag may
be used together with the flag described above.  The pager to use
when displaying files.  By default this is but it can be  changed
either  with  this option or by defining the variable.  The loca‐
tion of the reference files, or the source files used  to  create
the reference files.  This may be specified in one of three ways:
The  top  level directory of the source tree.  By default this is
but it can be changed either with this option  or  the  variable.
The  reference files will be created by running in the directory.
Note that should refer to the top of the source  directory  tree;
earlier  versions of expected to refer to the subdirectory within
the source tree.  A directory in which reference files have  been
extracted  from  a binary distribution of The files that are dis‐
tributed in the or set file must be present.  The files that  are
distributed in the or set file are optional.  The reference files
from  the  specified  directory  will be copied to the directory.
The location of a set file (or such as or from a binary distribu‐
tion of Each set file is a compressed archive  containing  refer‐
ence  files,  which will be extracted to the directory.  Multiple
options may be used to specify multiple set files.   The  or  set
file  must be specified.  The or set file is optional.  Specifies
the location of the directory.  This directory will be used for a
temporary copy of the reference files created by running  in  the
source  directory  specified by or a temporary copy of the refer‐
ence files extracted from the binary sets specified by or a  tem‐
porary  copy  of the reference files from the directory specified
by By default this is but can be changed either with this  option
or  the  environment  variable.  Makes verbose about its actions.
Sets screen width used during interactive merge.  By default this
is the number of columns reports but it  can  be  changed  either
with this option or by defining the variable.  This is useful for
users  with  wider  shell  windows.   Display help to stdout, and
exit.  Sets a default value for See above.  The location  of  the
sources  files.   See  above.   The  pager to use when displaying
files.  See above.  The  screen  width  used  during  interactive
merge.   See  above.   A list of files that should ignore.  Files
listed in this variable will never be considered for updating  by
The  environment  variables  can also be defined in the following
configuration files.  The user’s personal configuration file set‐
tings  override   the   global   settings.    /etc/etcupdate.conf
˜/.etcupdaterc  You  have just upgraded your host from 3.0 to 4.0
and now it’s time to update the configuration files as well.   To
update  the configuration files from the sources (if you have the
directory): The default location of the source files is but  this
may  be  overridden with the command line argument: To update the
configuration files from binary distribution  sets  do  something
like  this: or like this: You have modified only few files in the
directory so you would like install most of the  updates  without
being  asked.   To automatically update the unmodified configura‐
tion files: To get a better idea what’s going on, use  the  flag:
The  command  appeared in In the option was added, the option was
converted to and the option was changed to refer to  the  top  of
the  source  directory  tree rather than to the subdirectory.  In
the ability to specify multiple colon‐separated files with a sin‐
gle option was deprecated, and options  deprecated  in  were  re‐
moved.   In the ability to specify multiple colon‐separated files
with a single option was removed (multiple options must  be  used
instead),  and  the  option was added.  The script was written by
and improved by several other users.  The idea  for  this  script
(including  code  fragments,  variable names etc.)  came from the
mergemaster (by Douglas Barton).  Unlike  the  mergemaster,  this
does  not use CVS version tags by default to compare if the files
need to be updated.  Files are compared with as this is more  re‐
liable  and the only way if the version numbers are the same even
though the files are different.  If a source directory is  speci‐
fied  via  the  option  (or if the directory is used by default),
then will run in the subdirectory of the source directory, but it
will not use the same options or environment variables that would
be used during a full build of the operating  system.   For  this
reason,  use  of the option is not recommended, and use of the or
options is recommended.
