The function initializes the object referenced by to an empty set
of  spawn  attributes for subsequent use in a call to or A object
is an opaque type that is used to collect set of spawn attributes
specified by calls to or such that once the  spawn  operation  is
complete,  the child process will have a different set of attrib‐
utes than the parent.  The order of these operations is  unspeci‐
fied  and  may change; programs should not depend on their order‐
ing.  The function destroys the object referenced  by  which  was
previously  intialized by returning any resources obtained at the
time of initialization to the system for subsequent reuse.  A may
be reinitialized after having been destroyed, but be reused after
destruction, unless it has been reinitialized.  On success, these
functions return 0; on failure they return an error  number  from
The  and functions will fail and return an error if: Insufficient
resources to initialize the object.  The being destroyed  is  in‐
valid.  The and function calls appeared in
