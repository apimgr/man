is a server process that provides information to diskless clients
necessary for booting. It consults the file It should normally be
started  from  This  version will allow the use of aliases on the
hostname in the file. The hostname returned in  response  to  the
booting  client’s whoami request will be the name that appears in
the config file, not the canonical name.  In  this  way  you  can
keep  the answer short enough so that machines that cannot handle
long hostnames won’t fail during boot.  While parsing, if a  line
containing  just  ‘‘+’’ is found, and the YP subsystem is active,
the YP map will be searched immediately.  Display  the  debugging
information.  The daemon does not fork in this case.  Specify the
interface to become the default router.  picks the first IPv4 ad‐
dress it finds on the system by default.  With  you  can  control
which  interface to be used to obtain the default router address.
overrides Log the debugging  information  with  Set  the  default
router  (a hostname or IP‐address).  This defaults to the machine
running the server.  Specify the file to use  as  boot  parameter
file  instead of default configuration file Originally written by
You may find the syslog messages too verbose.  It’s not clear  if
the non‐canonical hack mentioned above is a good idea.
