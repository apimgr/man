PCAP_GET_REQUIRED_SELECT_TIMLEiObUrTa(r3y)FunctionsPCMAaPn_uGaElT_REQUIRED_SELECT_TIMEOUT(3)



NNAAMMEE
       pcap_get_required_select_timeout  - get a timeout to be used when doing
       select() for a live capture

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ppccaapp//ppccaapp..hh>>

       ccoonnsstt ssttrruucctt ttiimmeevvaall **ppccaapp__ggeett__rreeqquuiirreedd__sseelleecctt__ttiimmeeoouutt((ppccaapp__tt **pp));;

DDEESSCCRRIIPPTTIIOONN
       ppccaapp__ggeett__rreeqquuiirreedd__sseelleecctt__ttiimmeeoouutt() returns, on UNIX,  a  pointer  to  a
       ssttrruucctt  ttiimmeevvaall  containing  a  value  that must be used as the minimum
       timeout in sseelleecctt(2), ppoollll(2), eeppoollll__wwaaiitt(2), and kkeevveenntt(2)  calls,  or
       NNUULLLL  if there is no such timeout.  If a non-NNUULLLL value is returned, it
       must be used regardless of whether ppccaapp__ggeett__sseelleeccttaabbllee__ffdd(3) returns --11
       for  any  descriptor on which those calls are being done.  ppccaapp__ggeett__rree‐‐
       qquuiirreedd__sseelleecctt__ttiimmeeoouutt() should be called for all ppccaapp__tts before a  call
       to  sseelleecctt(),  ppoollll(), eeppoollll__wwaaiitt(), or kkeevveenntt(), and any timeouts used
       for those calls should be updated as appropriate given the new value of
       the timeout.

       For  kkeevveenntt(), one EEVVFFIILLTT__TTIIMMEERR filter per selectable descriptor can be
       used, rather than using the timeout argument to kkeevveenntt();  if  the  EEVV‐‐
       FFIILLTT__TTIIMMEERR  event  for  a  particular  selectable descriptor signals an
       event, ppccaapp__ddiissppaattcchh(3) should be called for the corresponding ppccaapp__tt.

       On Linux systems with ttiimmeerrffdd__ccrreeaattee(2), one timer  object  created  by
       ttiimmeerrffdd__ccrreeaattee() per selectable descriptor can be used, rather than us‐
       ing the timeout argument to eeppoollll__wwaaiitt(); if the  timer  object  for  a
       particular  selectable  descriptor  signals  an event, ppccaapp__ddiissppaattcchh(3)
       should be called for the corresponding ppccaapp__tt.

       Otherwise, a timeout value no larger than the smallest of all  timeouts
       returned  by  ppccaapp__ggeett__rreeqquuiirreedd__sseelleecctt__ttiimmeeoouutt() for devices from which
       packets will be captured and any other timeouts to be used in the  call
       should be used as the timeout for the call, and, when the call returns,
       ppccaapp__ddiissppaattcchh(3) should be called for all ppccaapp__tts for which a  non-NNUULLLL
       timeout  was  returned,  regardless of whether it's indicated as having
       anything to read from it or not.

       All devices with a non-NNUULLLL timeout must be put  in  non-blocking  mode
       with ppccaapp__sseettnnoonnbblloocckk(3).

       Note that a device on which a read can be done without blocking may, on
       some platforms, not have any packets to read if the packet buffer time‐
       out has expired.  A call to ppccaapp__ddiissppaattcchh() or ppccaapp__nneexxtt__eexx(3) will re‐
       turn 00 in this case, but will not block.

       ppccaapp__ggeett__rreeqquuiirreedd__sseelleecctt__ttiimmeeoouutt() is not available on Windows.

RREETTUURRNN VVAALLUUEE
       A pointer to a ssttrruucctt ttiimmeevvaall is returned if the timeout  is  required;
       otherwise NNUULLLL is returned.

BBAACCKKWWAARRDD CCOOMMPPAATTIIBBIILLIITTYY
       This  function  became available in libpcap release 1.9.0.  In previous
       releases, sseelleecctt(), ppoollll(), eeppoollll__wwaaiitt(), and  kkeevveenntt()  could  not  be
       used  for  devices  that don't provide a selectable file descriptor (in
       other words, on any capture source  for  that  ppccaapp__ggeett__sseelleeccttaabbllee__ffdd()
       returns --11).

       In  libpcap release 1.10.0 and later, the timeout value can change from
       call to call, so ppccaapp__ggeett__rreeqquuiirreedd__sseelleecctt__ttiimmeeoouutt() must be called  be‐
       fore  each call to sseelleecctt(), ppoollll(), eeppoollll__wwaaiitt(), or kkeevveenntt(), and the
       new value must be used to calculate timeouts for the call.   Code  that
       does  that  will  also  work with libpcap 1.9.x releases, so code using
       ppccaapp__ggeett__rreeqquuiirreedd__sseelleecctt__ttiimmeeoouutt() should be changed  to  call  it  for
       each  call  to  sseelleecctt(), ppoollll(), eeppoollll__wwaaiitt(), or kkeevveenntt() even if the
       code must also work with libpcap 1.9.x.

SSEEEE AALLSSOO
       ppccaapp(3), ppccaapp__ggeett__sseelleeccttaabbllee__ffdd(3), sseelleecctt(2), ppoollll(2),  eeppoollll__wwaaiitt(2),
       kkqquueeuuee(2)



                                29 January 2P0C2A0P_GET_REQUIRED_SELECT_TIMEOUT(3)
