LOCATE.MKLOCATEDB(8)      BSD System Manager's Manual     LOCATE.MKLOCATEDB(8)

NNAAMMEE
     llooccaattee..mmkkllooccaatteeddbb — create locate database

SSYYNNOOPPSSIISS
     llooccaattee..mmkkllooccaatteeddbb [--pprreessoorrtt]

DDEESSCCRRIIPPTTIIOONN
     llooccaattee..mmkkllooccaatteeddbb reads a list of filenames (one per line) from standard
     input and creates a database usable by locate(1) on standard output.  It
     is typically run by locate.updatedb(8), but it can be used independently
     to generate a database from any list of paths.

     The options are as follows:

     --pprreessoorrtt  Assume the input list is already sorted.

SSEEEE AALLSSOO
     locate(1), locate.updatedb(8)

BSD                              July 30, 2021                             BSD
