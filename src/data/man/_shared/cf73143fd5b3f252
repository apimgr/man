The  and  functions  fill a buffer with the current PC and return
addresses of a thread’s kernel and user call stack, respectively.
This is only possible when  frame  pointers  are  pushed  to  the
stack,  alongside  the  return addresses.  disables this behavior
with the flag, so it will prevent these functions  from  working.
Furthermore,  leaf  functions and inlined function calls can also
prevent backtracing from reporting the source‐level function con‐
trol flow.  operates on user call stacks, while captures the cur‐
rent kernel call stack.  writes a more compact representation  of
the  return  addresses  to  a  buffer, which can be unpacked with
Calling on a kernel thread is undefined.  Up to  instruction  ad‐
dresses or bytes for are written to the buffer at These functions
also  accept and arguments, described in and respectively.  takes
a to control which packing scheme to use.  records the kernel  PC
and  call stack of the current thread.  records the kernel PC and
call stack of the current thread in a buffer in a compact  repre‐
sentation.   See  for  a  description  of  the supported formats.
records the user PC and call stack of the current  thread,  which
must  be associated with a user space task.  The functions return
the number of PC and return address elements or bytes for written
to the provided buffer.  If there is space, the buffer is  termi‐
nated  with a NULL entry except for The argument will be set with
information about the provided call stack.  will set to an  error
of  the routine if an error occurred during call stack traversal.
The functions accept a control argument to alter their  behavior,
with  the  following fields: These flags control the backtracer’s
behavior: For only, record the PC and return addresses of the in‐
terrupted call stack.  Start backtracing from the provided  frame
address.   Capture  the backtrace of the provided thread pointer.
This must be either the current thread or a different thread that
is suspended and unable to run in  user  space.   For  only,  the
function  to  use  instead of to copy data from the thread’s user
space virtual address space into  the  kernel.   Additional  data
that’s  passed  to the custom copy routine to act as private con‐
text.  The functions  report  additional  information  through  a
flags  out‐parameter, with the following options: The PC and call
stack return addresses are 64‐bit quantities.  The backtrace  has
been  truncated  and does not terminate with the base frame.  The
variant uses an out‐parameter structure to return additional con‐
text: The flags, described above.  Any  error  encountered  while
copying  data.   For  Swift  continuations the location where the
continuation hint was found and where it logically branches  from
the standard call stack.  The frame address of the Swift continu‐
ation  to  pass  in  to a subsequent call to to follow the corre‐
sponding async stack.  In the case of a truncated  backtrace  due
to  lack  of  space in the destination buffer, the next frame ad‐
dress to resume the backtrace operation.  uintptr_t bt[8]  =  {};
enum  backtrace_info  bti  =  BTI_NONE;  unsigned int len = back‐
trace(bt, sizeof(bt) / sizeof(bt[0]), NULL, &bti); for  (unsigned
int  i  =  0;  i  < len; i++)  {         printf("%d: 0x%lx\n", i,
bt[i]); } if (bti & BTI_TRUNCATED) {          printf("[...  TRUN‐
CATED ...]\n"); } and
