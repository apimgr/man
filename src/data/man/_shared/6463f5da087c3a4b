(EGA  display  on  ISA)  (VGA  display on ISA or PCI) (generic PC
(ISA) display) (DEC TGA display, alpha  only)  (PCI  framebuffer,
bebox  only)  (Open Firmware framebuffer, macppc only) (NeXT dis‐
play) (VAXstation small monochrome display) The driver is an  ab‐
straction layer for display devices within the framework.  It at‐
taches  to  the hardware specific display device driver and makes
it available as a text terminal or graphics interface.  A display
device can have the ability to display characters on it  (without
the  help of an X server), either directly by hardware or through
software putting pixel data into the display memory.   Such  dis‐
plays  are  called  the  driver will connect a terminal emulation
module and provide a tty‐like software interface.   In  contrary,
non‐emulating  displays can only be used by special programs like
X servers.  The locator in the configuration line refers  to  the
device’s  use as the output part of the operating system console.
A device specification containing a positive value here will only
match if the device is in use as the system console.   (The  con‐
sole device selection in early system startup is not influenced.)
This  way,  the console device can be connected to a known wsdis‐
play device instance.  (Naturally, only display devices  are  us‐
able  as  console.)  The locator in the configuration line refers
to the that will be used to get keyboard events.  If this locator
is ‐1 no mux will be used.  The logical unit  of  an  independent
contents  displayed  on  a  display  (sometimes referred to as is
called a here.  If the underlying device driver supports it, mul‐
tiple screens can be used on one display.  (As of  this  writing,
only  the  and  the  VAX  display  drivers provide this ability.)
Screens have different minor device numbers and separate tty  in‐
stances.   One  screen possesses the this means it is visible and
its tty device will get the keyboard input.  (In some cases -  if
no  screen is set up or if a screen was just deleted - it is pos‐
sible that no focus  is  present  at  all.)   The  focus  can  be
switched by either special keyboard input (typically on Sun hard‐
ware, on ADB keyboards) or an ioctl command issued by a user pro‐
gram.  Screens are created and deleted through the control device
(preferably  using the utility).  Alternatively, the compile‐time
option will  also  create  (at  autoconfiguration  time)  initial
screens  of  the  display driver’s default type with the system’s
default terminal emulator.   The  following  kernel  options  are
available  to configure the behavior of the driver: Sets the bor‐
der color at boot time.  Possible values are defined in  Defaults
to  Enables  the and ioctls, which allow the customization of the
border color from userland (after boot).   See  Enables  the  and
ioctls,  which  allow the customization of the console output and
kernel messages from userland (after boot).  See Sets the  number
of  virtual  screens  to allocate at boot time.  Useful for small
root filesystems  where  the  utility  is  not  wanted.   Enables
scrolling  support.   The  key  combinations  are and by default.
Please note that this function may not work under the system con‐
sole and is available depending on the framebuffer you are using.
The following calls are provided by  the  driver  or  by  devices
which  use  it.  Their definitions are found in Retrieve the type
of the display.  The list of types is in Retrieve extended infor‐
mation about a framebuffer display, including  the  framebuffer’s
pixel  packing  layout.   The  returned  structure is as follows:
struct   wsdisplayio_fbinfo   {           uint64_t    fbi_fbsize;
        uint64_t    fbi_fboffset;           uint32_t   fbi_width;
        uint32_t   fbi_height;            uint32_t    fbi_stride;
        uint32_t  fbi_bitsperpixel;          uint32_t  fbi_pixel‐
type;           union   _fbi_subtype   {                   struct
_fbi_rgbmasks   {                           uint32_t  red_offset;
                        uint32_t                        red_size;
                        uint32_t                    green_offset;
                        uint32_t                      green_size;
                        uint32_t                     blue_offset;
                        uint32_t                       blue_size;
                        uint32_t                    alpha_offset;
                        uint32_t  alpha_size;                   }
fbi_rgbmasks;                      struct     _fbi_cmapinfo     {
                        uint32_t cmap_entries;                  }
fbi_cmapinfo;          } fbi_subtype;         uint32_t fbi_flags;
}; For a "true colour" display, the field contains and the  field
contains the pixel packing layout.  For a colour indexed display,
the field contains and the field contains the number of color map
entries.  Retrieve basic information about a framebuffer display.
The  returned  structure is as follows: struct wsdisplay_fbinfo {
        u_int   height;                            u_int   width;
        u_int   depth;         u_int   cmsize; }; The and members
are  counted  in pixels.  The member indicates the number of bits
per pixel, and indicates the number of color map entries accessi‐
ble through and This call is likely to be  unavailable  on  text‐
only  displays.  Retrieve the current color map from the display.
This call needs the following structure set up beforehand: struct
wsdisplay_cmap  {          u_int   index;          u_int   count;
        u_char  *red;                             u_char  *green;
        u_char  *blue; }; The and members specify  the  range  of
color map entries to retrieve.  The and members should each point
to an array of On return, these will be filled in with the appro‐
priate  entries from the color map.  On all displays that support
this call, values range from 0 for minimum intensity to  255  for
maximum  intensity,  even  if the display does not use eight bits
internally to represent intensity.  Change  the  display’s  color
map.  The argument structure is the same as for but and are taken
as pointers to the values to use to set the color map.  This call
is not available on displays with fixed color maps.  Get the cur‐
rent  state  of the display’s video output.  Possible values are:
The display is blanked.  The display is enabled.  Set  the  state
of  the  display’s  video output.  See above for possible values.
Retrieve the current position of the hardware  cursor.   The  re‐
turned structure is as follows: struct wsdisplay_curpos {
        u_int x, y; }; The and members count the number of pixels
right  and  down,  respectively,  from the top‐left corner of the
display to the hot spot of the cursor.  This call is  not  avail‐
able on displays without a hardware cursor.  Set the current cur‐
sor position.  The argument structure, and its semantics, are the
same  as  for  This  call  is not available on displays without a
hardware cursor.  Retrieve the maximum size of  cursor  supported
by  the display.  The and members of the returned structure indi‐
cate the maximum number of pixel rows and columns,  respectively,
in a hardware cursor on this display.  This call is not available
on  displays  without a hardware cursor.  Retrieve some or all of
the hardware cursor’s attributes.  The argument structure  is  as
follows:   struct   wsdisplay_cursor   {           u_int   which;
        u_int   enable;          struct   wsdisplay_curpos   pos;
        struct   wsdisplay_curpos   hot;           struct  wsdis‐
play_cmap    cmap;            struct    wsdisplay_curpos    size;
        u_char  *image;         u_char *mask; }; The member indi‐
cates which of the values the  application  requires  to  be  re‐
turned.  It should contain the logical OR of the following flags:
Get  which  indicates  whether  the cursor is currently displayed
(non‐zero) or not (zero).  Get which indicates the current  posi‐
tion  of  the  cursor on the display, as would be returned by Get
which indicates the location of the within the cursor.   This  is
the  point on the cursor whose position on the display is treated
as being the position of the cursor by other calls.  Its location
is counted in pixels from the top‐right  corner  of  the  cursor.
Get  which  indicates  the current cursor color map.  Unlike in a
call to here need not have its  and  members  initialized.   They
will be set to 0 and 2 respectively by the call.  This means that
and  must each point to at least enough space to hold two Get and
These are, respectively, the dimensions of the cursor in  pixels,
the  bitmap  of set pixels in the cursor and the bitmap of opaque
pixels in the cursor.  The format in which these bitmaps are  re‐
turned,  and  hence  the amount of space that must be provided by
the application, are device‐dependent.  Get  all  of  the  above.
The device may elect to return information that was not requested
by  the  user,  so those elements of which are pointers should be
initialized to if not otherwise used.  This call is not available
on displays without a hardware cursor.  Set some or  all  of  the
hardware cursor’s attributes.  The argument structure is the same
as for The member specifies which attributes of the cursor are to
be  changed.   It  should contain the logical OR of the following
flags: If is zero, hide the cursor.  Otherwise, display it.   Set
the  cursor’s  position  on the display to the same as Set the of
the cursor, as defined above, to Set some or all  of  the  cursor
color  map  based on The and elements of indicate which color map
entries to set, and the entries themselves come from and Set  the
cursor  shape  from  and See above for their meanings.  Do all of
the above.  This call is not  available  on  displays  without  a
hardware  cursor.  Get the current mode of the display.  Possible
results include: The display is in emulating  (text)  mode.   The
display  is  in mapped (graphics) mode.  The display is in mapped
(frame buffer) mode.  Set the current mode of the  display.   For
possible  arguments,  see  Get the number of bytes per row, which
may be the same as the number  of  pixels.   Get  the  attributes
(colors and flags) used to print console messages, including sep‐
arate  fields for default output and kernel output.  The returned
structure is as follows: struct wsdisplay_msgattrs {          int
default_attrs,  default_bg, default_fg;         int kernel_attrs,
kernel_bg, kernel_fg; }; The and variables are a  combination  of
bits,  and specify the attributes used to draw messages.  The and
variables specify the colors used to print  messages,  being  for
the  background  and  for the foreground; their values are one of
all the macros available.  Set the attributes (colors and  flags)
used to print console messages, including separate fields for de‐
fault  output  and  kernel output.  The argument structure is the
same as for Retrieve the color of the screen border.  This number
corresponds to an ANSI standard color.   Set  the  color  of  the
screen border, if applicable.  This number corresponds to an ANSI
standard  color.   Not  all drivers support this feature.  Gets a
single character from the screen, specified by its position.  The
structure used is as follows: struct wsdisplay_char {         int
row, col;         uint16_t  letter;          uint8_t  background,
foreground;         char flags; }; The and parameters are used as
input;  the  rest  of the structure is filled by the ioctl and is
returned to you.  is the ASCII code of the letter  found  at  the
specified  position,  and  are its colors and is a combination of
and/or Puts a character on the screen.   The  structure  has  the
same  meaning  as  described  in  although  all of its fields are
treated as input.  Toggle the splash screen.  This call  is  only
available with the kernel option.  Retrieve EDID data from a dri‐
ver.     struct    wsdisplayio_edid_info    {            uint32_t
buffer_size;         uint32_t data_size;         void *edid_data;
}; The caller is responsible for allocating a buffer of at  least
128  bytes  (the minimum size of an EDID block) and set data_size
to its size.  If the EDID block is bigger the call will fail with
and the driver will set data_size to the  required  buffer  size.
Otherwise  the EDID block will be written into the buffer pointed
at by edid_data and data_size will be set to the number of  bytes
written.   Set the wscons_event protocol version.  The default is
0 for binary compatibility.  The latest version is always  avail‐
able as and is currently 1.  All new code should use a call simi‐
lar  to the below to ensure the correct version is returned.  int
ver   =   WSDISPLAYIO_EVENT_VERSION;   if    (ioctl(fd,    WSDIS‐
PLAYIO_SETVERSION, &ver) == ‐1)
    err(EXIT_FAILURE,  "cannot  set  version");  Terminal devices
(per screen).  Control device.  Status  device.   The  code  cur‐
rently  limits  the  number  of screens on one display to 8.  The
terms and are not cleanly distinguished in the code and in manual
pages.  display devices are not tested.
