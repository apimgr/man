_t_i_m_e_r___c_r_e_a_t_e(2)               System Calls Manual              _t_i_m_e_r___c_r_e_a_t_e(2)

NNAAMMEE
       timer_create - create a POSIX per‐process timer

LLIIBBRRAARRYY
       Real‐time library (_l_i_b_r_t, _-_l_r_t)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssiiggnnaall..hh>>           /* Definition of SSIIGGEEVV__** constants */
       ##iinncclluuddee <<ttiimmee..hh>>

       iinntt ttiimmeerr__ccrreeaattee((cclloocckkiidd__tt _c_l_o_c_k_i_d,,
                        ssttrruucctt ssiiggeevveenntt **__NNuullllaabbllee rreessttrriicctt _s_e_v_p,,
                        ttiimmeerr__tt **rreessttrriicctt _t_i_m_e_r_i_d));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       ttiimmeerr__ccrreeaattee():
           _POSIX_C_SOURCE >= 199309L

DDEESSCCRRIIPPTTIIOONN
       ttiimmeerr__ccrreeaattee() creates a new per‐process interval timer.  The ID of the
       new  timer  is returned in the buffer pointed to by _t_i_m_e_r_i_d, which must
       be a non‐null pointer.  This ID is unique within the process, until the
       timer is deleted.  The new timer is initially disarmed.

       The _c_l_o_c_k_i_d argument specifies the clock that the  new  timer  uses  to
       measure time.  It can be specified as one of the following values:

       CCLLOOCCKK__RREEAALLTTIIMMEE
              A settable system‐wide real‐time clock.

       CCLLOOCCKK__MMOONNOOTTOONNIICC
              A  nonsettable monotonically increasing clock that measures time
              from some unspecified point in the past that does not change af‐
              ter system startup.

       CCLLOOCCKK__PPRROOCCEESSSS__CCPPUUTTIIMMEE__IIDD (since Linux 2.6.12)
              A clock that measures (user and system)  CPU  time  consumed  by
              (all of the threads in) the calling process.

       CCLLOOCCKK__TTHHRREEAADD__CCPPUUTTIIMMEE__IIDD (since Linux 2.6.12)
              A clock that measures (user and system) CPU time consumed by the
              calling thread.

       CCLLOOCCKK__BBOOOOTTTTIIMMEE (Since Linux 2.6.39)
              Like  CCLLOOCCKK__MMOONNOOTTOONNIICC, this is a monotonically increasing clock.
              However, whereas the CCLLOOCCKK__MMOONNOOTTOONNIICC clock does not measure  the
              time  while a system is suspended, the CCLLOOCCKK__BBOOOOTTTTIIMMEE clock does
              include the time during which the system is suspended.  This  is
              useful   for   applications   that  need  to  be  suspend‐aware.
              CCLLOOCCKK__RREEAALLTTIIMMEE is not suitable for such applications, since that
              clock is affected by discontinuous changes to the system clock.

       CCLLOOCCKK__RREEAALLTTIIMMEE__AALLAARRMM (since Linux 3.0)
              This clock is like CCLLOOCCKK__RREEAALLTTIIMMEE, but will wake the  system  if
              it  is suspended.  The caller must have the CCAAPP__WWAAKKEE__AALLAARRMM capa‐
              bility in order to set a timer against this clock.

       CCLLOOCCKK__BBOOOOTTTTIIMMEE__AALLAARRMM (since Linux 3.0)
              This clock is like CCLLOOCCKK__BBOOOOTTTTIIMMEE, but will wake the  system  if
              it  is suspended.  The caller must have the CCAAPP__WWAAKKEE__AALLAARRMM capa‐
              bility in order to set a timer against this clock.

       CCLLOOCCKK__TTAAII (since Linux 3.10)
              A system‐wide clock derived from wall‐clock  time  but  counting
              leap seconds.

       See cclloocckk__ggeettrreess(2) for some further details on the above clocks.

       As  well  as  the above values, _c_l_o_c_k_i_d can be specified as the _c_l_o_c_k_i_d
       returned  by  a  call  to  cclloocckk__ggeettccppuucclloocckkiidd(3)  or   pptthhrreeaadd__ggeettccppuu‐‐
       cclloocckkiidd(3).

       The _s_e_v_p argument points to a _s_i_g_e_v_e_n_t structure that specifies how the
       caller  should  be notified when the timer expires.  For the definition
       and general details of this structure, see ssiiggeevveenntt(3type).

       The _s_e_v_p_._s_i_g_e_v___n_o_t_i_f_y field can have the following values:

       SSIIGGEEVV__NNOONNEE
              Don’t asynchronously notify when the timer expires.  Progress of
              the timer can be monitored using ttiimmeerr__ggeettttiimmee(2).

       SSIIGGEEVV__SSIIGGNNAALL
              Upon timer expiration, generate the signal _s_i_g_e_v___s_i_g_n_o  for  the
              process.   See ssiiggeevveenntt(3type) for general details.  The _s_i___c_o_d_e
              field of the _s_i_g_i_n_f_o___t structure will be set  to  SSII__TTIIMMEERR.   At
              any  point  in time, at most one signal is queued to the process
              for a given timer; see ttiimmeerr__ggeettoovveerrrruunn(2) for more details.

       SSIIGGEEVV__TTHHRREEAADD
              Upon timer expiration, invoke  _s_i_g_e_v___n_o_t_i_f_y___f_u_n_c_t_i_o_n  as  if  it
              were  the  start  function of a new thread.  See ssiiggeevveenntt(3type)
              for details.

       SSIIGGEEVV__TTHHRREEAADD__IIDD (Linux‐specific)
              As for SSIIGGEEVV__SSIIGGNNAALL, but the signal is targeted  at  the  thread
              whose  ID  is  given  in _s_i_g_e_v___n_o_t_i_f_y___t_h_r_e_a_d___i_d, which must be a
              thread in  the  same  process  as  the  caller.   The  _s_i_g_e_v___n_o_‐
              _t_i_f_y___t_h_r_e_a_d___i_d  field specifies a kernel thread ID, that is, the
              value returned by cclloonnee(2) or ggeettttiidd(2).  This flag is  intended
              only for use by threading libraries.

       Specifying  _s_e_v_p  as  NULL  is  equivalent to specifying a pointer to a
       _s_i_g_e_v_e_n_t structure in which _s_i_g_e_v___n_o_t_i_f_y is  SSIIGGEEVV__SSIIGGNNAALL,  _s_i_g_e_v___s_i_g_n_o
       is SSIIGGAALLRRMM, and _s_i_g_e_v___v_a_l_u_e_._s_i_v_a_l___i_n_t is the timer ID.

RREETTUURRNN VVAALLUUEE
       On  success,  ttiimmeerr__ccrreeaattee()  returns 0, and the ID of the new timer is
       placed in _*_t_i_m_e_r_i_d.  On failure, -1 is returned, and _e_r_r_n_o  is  set  to
       indicate the error.

EERRRROORRSS
       EEAAGGAAIINN Temporary error during kernel allocation of timer structures.

       EEIINNVVAALL Clock  ID,  _s_i_g_e_v___n_o_t_i_f_y, _s_i_g_e_v___s_i_g_n_o, or _s_i_g_e_v___n_o_t_i_f_y___t_h_r_e_a_d___i_d
              is invalid.

       EENNOOMMEEMM Could not allocate memory.

       EENNOOTTSSUUPP
              The kernel does  not  support  creating  a  timer  against  this
              _c_l_o_c_k_i_d.

       EEPPEERRMM  _c_l_o_c_k_i_d was CCLLOOCCKK__RREEAALLTTIIMMEE__AALLAARRMM or CCLLOOCCKK__BBOOOOTTTTIIMMEE__AALLAARRMM but the
              caller did not have the CCAAPP__WWAAKKEE__AALLAARRMM capability.

VVEERRSSIIOONNSS
   CC lliibbrraarryy//kkeerrnneell ddiiffffeerreenncceess
       Part  of  the  implementation  of  the  POSIX timers API is provided by
       glibc.  In particular:

       •  Much of the functionality for  SSIIGGEEVV__TTHHRREEAADD  is  implemented  within
          glibc,  rather  than the kernel.  (This is necessarily so, since the
          thread involved in handling the notification is  one  that  must  be
          managed  by  the  C library POSIX threads implementation.)  Although
          the notification delivered to the process is via  a  thread,  inter‐
          nally   the   NPTL  implementation  uses  a  _s_i_g_e_v___n_o_t_i_f_y  value  of
          SSIIGGEEVV__TTHHRREEAADD__IIDD along with a real‐time signal that  is  reserved  by
          the implementation (see nnppttll(7)).

       •  The  implementation of the default case where _e_v_p is NULL is handled
          inside glibc, which invokes the underlying system call with a  suit‐
          ably populated _s_i_g_e_v_e_n_t structure.

       •  The timer IDs presented at user level are maintained by glibc, which
          maps these IDs to the timer IDs employed by the kernel.

SSTTAANNDDAARRDDSS
       POSIX.1‐2024.

HHIISSTTOORRYY
       Linux 2.6.  POSIX.1‐2001.

       Prior to Linux 2.6, glibc provided an incomplete user‐space implementa‐
       tion (CCLLOOCCKK__RREEAALLTTIIMMEE timers only) using POSIX threads, and before glibc
       2.17,  the  implementation falls back to this technique on systems run‐
       ning kernels older than Linux 2.6.

NNOOTTEESS
       A program may create multiple interval timers using ttiimmeerr__ccrreeaattee().

       Timers are not inherited by the child of a ffoorrkk(2),  and  are  disarmed
       and deleted during an eexxeeccvvee(2).

       The kernel preallocates a "queued real‐time signal" for each timer cre‐
       ated  using ttiimmeerr__ccrreeaattee().  Consequently, the number of timers is lim‐
       ited by the RRLLIIMMIITT__SSIIGGPPEENNDDIINNGG resource limit (see sseettrrlliimmiitt(2)).

       The timers created by ttiimmeerr__ccrreeaattee() are commonly known as "POSIX  (in‐
       terval) timers".  The POSIX timers API consists of the following inter‐
       faces:

       ttiimmeerr__ccrreeaattee()
              Create a timer.

       ttiimmeerr__sseettttiimmee(2)
              Arm (start) or disarm (stop) a timer.

       ttiimmeerr__ggeettttiimmee(2)
              Fetch  the  time remaining until the next expiration of a timer,
              along with the interval setting of the timer.

       ttiimmeerr__ggeettoovveerrrruunn(2)
              Return the overrun count for the last timer expiration.

       ttiimmeerr__ddeelleettee(2)
              Disarm and delete a timer.

       Since Linux 3.10, the _/_p_r_o_c_/pid_/_t_i_m_e_r_s file can be  used  to  list  the
       POSIX timers for the process with PID _p_i_d.  See pprroocc(5) for further in‐
       formation.

       Since  Linux  4.10,  support  for POSIX timers is a configurable option
       that is enabled by default.  Kernel support can  be  disabled  via  the
       CCOONNFFIIGG__PPOOSSIIXX__TTIIMMEERRSS option.

EEXXAAMMPPLLEESS
       The program below takes two arguments: a sleep period in seconds, and a
       timer  frequency in nanoseconds.  The program establishes a handler for
       the signal it uses for the timer, blocks that signal, creates and  arms
       a timer that expires with the given frequency, sleeps for the specified
       number  of  seconds, and then unblocks the timer signal.  Assuming that
       the timer expired at least once while the  program  slept,  the  signal
       handler  will  be  invoked,  and  the handler displays some information
       about the timer notification.  The program terminates after one invoca‐
       tion of the signal handler.

       In the following example run, the program sleeps for  1  second,  after
       creating  a timer that has a frequency of 100 nanoseconds.  By the time
       the signal is unblocked and delivered, there have been around ten  mil‐
       lion overruns.

           $ ..//aa..oouutt 11 110000;
           Establishing handler for signal 34
           Blocking signal 34
           timer ID is 0x804c008
           Sleeping for 1 seconds
           Unblocking signal 34
           Caught signal 34
               sival_ptr = 0xbfb174f4;     *sival_ptr = 0x804c008
               overrun count = 10004886

   PPrrooggrraamm ssoouurrccee

       #include <err.h>
       #include <signal.h>
       #include <stdint.h>
       #include <stdio.h>
       #include <stdlib.h>
       #include <time.h>
       #include <unistd.h>

       #define CLOCKID CLOCK_REALTIME
       #define SIG SIGRTMIN

       static void
       print_siginfo(siginfo_t *si)
       {
           int      or;
           timer_t  *tidp;

           tidp = si->si_value.sival_ptr;

           printf("    sival_ptr = %p; ", si->si_value.sival_ptr);
           printf("    *sival_ptr = %#jx\n", (uintmax_t) *tidp);

           or = timer_getoverrun(*tidp);
           if (or == -1)
               err(EXIT_FAILURE, "timer_getoverrun");

           printf("    overrun count = %d\n", or);
       }

       static void
       handler(int sig, siginfo_t *si, void *uc)
       {
           /* Note: calling printf() from a signal handler is not safe
              (and should not be done in production programs), since
              printf() is not async-signal-safe; see signal-safety(7).
              Nevertheless, we use printf() here as a simple way of
              showing that the handler was called.  */

           printf("Caught signal %d\n", sig);
           print_siginfo(si);
           signal(sig, SIG_IGN);
       }

       int
       main(int argc, char *argv[])
       {
           timer_t            timerid;
           sigset_t           mask;
           long long          freq_nanosecs;
           struct sigevent    sev;
           struct sigaction   sa;
           struct itimerspec  its;

           if (argc != 3) {
               fprintf(stderr, "Usage: %s <sleep-secs> <freq-nanosecs>\n",
                       argv[0]);
               exit(EXIT_FAILURE);
           }

           /* Establish handler for timer signal.  */

           printf("Establishing handler for signal %d\n", SIG);
           sa.sa_flags = SA_SIGINFO;
           sa.sa_sigaction = handler;
           sigemptyset(&sa.sa_mask);
           if (sigaction(SIG, &sa, NULL) == -1)
               err(EXIT_FAILURE, "sigaction");

           /* Block timer signal temporarily.  */

           printf("Blocking signal %d\n", SIG);
           sigemptyset(&mask);
           sigaddset(&mask, SIG);
           if (sigprocmask(SIG_SETMASK, &mask, NULL) == -1)
               err(EXIT_FAILURE, "sigprocmask");

           /* Create the timer.  */

           sev.sigev_notify = SIGEV_SIGNAL;
           sev.sigev_signo = SIG;
           sev.sigev_value.sival_ptr = &timerid;
           if (timer_create(CLOCKID, &sev, &timerid) == -1)
               err(EXIT_FAILURE, "timer_create");

           printf("timer ID is %#jx\n", (uintmax_t) timerid);

           /* Start the timer.  */

           freq_nanosecs = atoll(argv[2]);
           its.it_value.tv_sec = freq_nanosecs / 1000000000;
           its.it_value.tv_nsec = freq_nanosecs % 1000000000;
           its.it_interval.tv_sec = its.it_value.tv_sec;
           its.it_interval.tv_nsec = its.it_value.tv_nsec;

           if (timer_settime(timerid, 0, &its, NULL) == -1)
                err(EXIT_FAILURE, "timer_settime");

           /* Sleep for a while; meanwhile, the timer may expire
              multiple times.  */

           printf("Sleeping for %d seconds\n", atoi(argv[1]));
           sleep(atoi(argv[1]));

           /* Unlock the timer signal, so that timer notification
              can be delivered.  */

           printf("Unblocking signal %d\n", SIG);
           if (sigprocmask(SIG_UNBLOCK, &mask, NULL) == -1)
               err(EXIT_FAILURE, "sigprocmask");

           exit(EXIT_SUCCESS);
       }

SSEEEE AALLSSOO
       cclloocckk__ggeettttiimmee(2), sseettiittiimmeerr(2), ttiimmeerr__ddeelleettee(2), ttiimmeerr__ggeettoovveerrrruunn(2),
       ttiimmeerr__sseettttiimmee(2), ttiimmeerrffdd__ccrreeaattee(2), cclloocckk__ggeettccppuucclloocckkiidd(3),
       pptthhrreeaadd__ggeettccppuucclloocckkiidd(3), pptthhrreeaaddss(7), ssiiggeevveenntt(3type), ssiiggnnaall(7),
       ttiimmee(7)

Linux man‐pages (unreleased)      2025‐10‐29                   _t_i_m_e_r___c_r_e_a_t_e(2)
