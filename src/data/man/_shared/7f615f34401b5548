The  command  attaches the 7th Edition(V7) File System on the de‐
vice on to the file system tree at point Both and  are  converted
to  absolute paths before use.  This command is normally executed
by at boot time.  The options are as follows: Specify  the  meta‐
data byte order of the file system.  Valid byte orders are and If
no  byte  order  is specified, the file system is mounted in host
byte order.  Options are specified with  a  flag  followed  by  a
comma separated string of options.  See the man page for possible
options and their meanings.  A utility appeared in was written by
The v7fs support is still experimental.
