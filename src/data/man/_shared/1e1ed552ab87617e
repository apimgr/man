OPENPAM_GET_FEATURE(3)   BSD Library Functions Manual   OPENPAM_GET_FEATURE(3)

NNAAMMEE
     ooppeennppaamm__ggeett__ffeeaattuurree — query the state of an optional feature

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ttyyppeess..hh>>
     ##iinncclluuddee <<sseeccuurriittyy//ppaamm__aappppll..hh>>
     ##iinncclluuddee <<sseeccuurriittyy//ooppeennppaamm..hh>>

     _i_n_t
     ooppeennppaamm__ggeett__ffeeaattuurree(_i_n_t _f_e_a_t_u_r_e, _i_n_t _*_o_n_o_f_f);

DDEESSCCRRIIPPTTIIOONN
     TThhiiss ffuunnccttiioonn iiss eexxppeerriimmeennttaall aanndd mmaayy bbee mmooddiiffiieedd oorr rreemmoovveedd iinn aa ffuuttuurree
     rreelleeaassee wwiitthhoouutt pprriioorr wwaarrnniinngg..

     The ooppeennppaamm__ggeett__ffeeaattuurree() function stores the current state of the speci‐
     fied feature in the variable pointed to by its _o_n_o_f_f argument.

     The following features are recognized:

     OPENPAM_RESTRICT_SERVICE_NAME
                         Disallow path separators in service names.  This fea‐
                         ture is enabled by default.  Disabling it allows the
                         application to specify the path to the desired policy
                         file directly.

     OPENPAM_VERIFY_POLICY_FILE
                         Verify the ownership and permissions of the policy
                         file and the path leading up to it.  This feature is
                         enabled by default.

     OPENPAM_RESTRICT_MODULE_NAME
                         Disallow path separators in module names.  This fea‐
                         ture is disabled by default.  Enabling it prevents
                         the use of modules in non-standard locations.

     OPENPAM_VERIFY_MODULE_FILE
                         Verify the ownership and permissions of each loadable
                         module and the path leading up to it.  This feature
                         is enabled by default.

RREETTUURRNN VVAALLUUEESS
     The ooppeennppaamm__ggeett__ffeeaattuurree() function returns one of the following values:

     [PAM_SUCCESS]       Success.

     [PAM_BAD_FEATURE]   Unrecognized or restricted feature.

SSEEEE AALLSSOO
     openpam_set_feature(3), pam(3), pam_strerror(3)

SSTTAANNDDAARRDDSS
     The ooppeennppaamm__ggeett__ffeeaattuurree() function is an OpenPAM extension.

AAUUTTHHOORRSS
     The ooppeennppaamm__ggeett__ffeeaattuurree() function and this manual page were developed by
     Dag-Erling Smørgrav <_d_e_s_@_d_e_s_._n_o>.

BSD                              June 27, 2023                             BSD
