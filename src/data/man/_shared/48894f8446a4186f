ELF_CNTL(3)              BSD Library Functions Manual              ELF_CNTL(3)

NNAAMMEE
     eellff__ccnnttll — control an elf file descriptor

LLIIBBRRAARRYY
     ELF Access Library (libelf, -lelf)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<lliibbeellff..hh>>

     _i_n_t
     eellff__ccnnttll(_E_l_f _*_e_l_f, _E_l_f___C_m_d _c_m_d);

DDEESSCCRRIIPPTTIIOONN
     Function eellff__ccnnttll() controls the ELF library's subsequent use of the file
     descriptor used to create ELF descriptor _e_l_f.

     Argument _c_m_d informs the library of the action to be taken:

     ELF_C_FDDONE  This value instructs the ELF library not to perform any
                   further I/O on the file descriptor associated with argument
                   _e_l_f.  For ELF descriptors opened with mode _E_L_F___C___W_R_I_T_E or
                   _E_L_F___C___R_D_W_R subsequent eellff__uuppddaattee() operations on the de‐
                   scriptor will fail.

     ELF_C_FDREAD  This value instructs the ELF library to read in all neces‐
                   sary data associated with ELF descriptor _e_l_f into memory so
                   that the underlying file descriptor can be safely closed
                   with command ELF_C_FDDONE.

     Argument _e_l_f must be an ELF descriptor associated with a file system ob‐
     ject (e.g., an ar(1) archive, an ELF file, or other data file).

IIMMPPLLEEMMEENNTTAATTIIOONN NNOOTTEESS
     Due to use of mmap(2) internally, this function is a no-op for ELF ob‐
     jects opened in ELF_C_READ mode.

RREETTUURRNN VVAALLUUEESS
     Function eellff__ccnnttll() returns 0 on success, or -1 if an error was detected.

EERRRROORRSS
     [ELF_E_ARCHIVE]   Argument _e_l_f is a descriptor for an archive member.

     [ELF_E_ARGUMENT]  Argument _e_l_f was NULL.

     [ELF_E_ARGUMENT]  Argument _c_m_d was not recognized.

     [ELF_E_MODE]      An ELF_C_FDREAD operation was requested on an ELF de‐
                       scriptor opened for writing.

SSEEEE AALLSSOO
     elf(3), elf_begin(3), elf_end(3), elf_next(3), elf_update(3), gelf(3)

BSD                             August 9, 2006                             BSD
