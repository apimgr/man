_B_P_F_‐_H_E_L_P_E_R_S(7)         Miscellaneous Information Manual         _B_P_F_‐_H_E_L_P_E_R_S(7)

NNAAMMEE
       BPF‐HELPERS - list of eBPF helper functions

DDEESSCCRRIIPPTTIIOONN
       The  extended  Berkeley Packet Filter (eBPF) subsystem consists in pro‐
       grams written in a pseudo-assembly language, then attached  to  one  of
       the  several  kernel hooks and run in reaction of specific events. This
       framework differs from the older, "classic" BPF (or "cBPF") in  several
       aspects,  one  of  them being the ability to call special functions (or
       "helpers") from within a program.  These functions are restricted to  a
       white-list of helpers defined in the kernel.

       These helpers are used by eBPF programs to interact with the system, or
       with  the context in which they work. For instance, they can be used to
       print debugging messages, to get the time since the system was  booted,
       to  interact  with  eBPF  maps, or to manipulate network packets. Since
       there are several eBPF program types, and that they do not run  in  the
       same  context,  each  program  type  can  only  call  a subset of those
       helpers.

       Due to eBPF conventions, a helper can not have  more  than  five  argu‐
       ments.

       Internally,  eBPF programs call directly into the compiled helper func‐
       tions without requiring any foreign-function interface.  As  a  result,
       calling helpers introduces no overhead, thus offering excellent perfor‐
       mance.

       This  document is an attempt to list and document the helpers available
       to eBPF developers. They are sorted by chronological order (the  oldest
       helpers in the kernel at the top).

HHEELLPPEERRSS
       vvooiidd **bbppff__mmaapp__llooookkuupp__eelleemm((ssttrruucctt bbppff__mmaapp **_m_a_p,, ccoonnsstt vvooiidd **_k_e_y))

              DDeessccrriippttiioonn
                     Perform a lookup in _m_a_p for an entry associated to _k_e_y.

              RReettuurrnn Map  value  associated  to  _k_e_y,  or NNUULLLL if no entry was
                     found.

       lloonngg bbppff__mmaapp__uuppddaattee__eelleemm((ssttrruucctt bbppff__mmaapp **_m_a_p,, ccoonnsstt vvooiidd **_k_e_y,, ccoonnsstt
       vvooiidd **_v_a_l_u_e,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Add or update the value of the entry associated to _k_e_y in
                     _m_a_p with _v_a_l_u_e. _f_l_a_g_s is one of:

                     BBPPFF__NNOOEEXXIISSTT
                            The entry for _k_e_y must not exist in the map.

                     BBPPFF__EEXXIISSTT
                            The entry for _k_e_y must already exist in the map.

                     BBPPFF__AANNYY
                            No condition on the existence  of  the  entry  for
                            _k_e_y.

                     Flag  value  BBPPFF__NNOOEEXXIISSTT cannot be used for maps of types
                     BBPPFF__MMAAPP__TTYYPPEE__AARRRRAAYY or BBPPFF__MMAAPP__TTYYPPEE__PPEERRCCPPUU__AARRRRAAYY  (all el‐
                     ements always exist), the helper would return an error.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__mmaapp__ddeelleettee__eelleemm((ssttrruucctt bbppff__mmaapp **_m_a_p,, ccoonnsstt vvooiidd **_k_e_y))

              DDeessccrriippttiioonn
                     Delete entry with _k_e_y from _m_a_p.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__pprroobbee__rreeaadd((vvooiidd **_d_s_t,, uu3322 _s_i_z_e,, ccoonnsstt vvooiidd **_u_n_s_a_f_e___p_t_r))

              DDeessccrriippttiioonn
                     For tracing programs, safely attempt to read  _s_i_z_e  bytes
                     from  kernel  space address _u_n_s_a_f_e___p_t_r and store the data
                     in _d_s_t.

                     Generally,       use       bbppff__pprroobbee__rreeaadd__uusseerr()       or
                     bbppff__pprroobbee__rreeaadd__kkeerrnneell() instead.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       uu6644 bbppff__kkttiimmee__ggeett__nnss((vvooiidd))

              DDeessccrriippttiioonn
                     Return  the  time  elapsed since system boot, in nanosec‐
                     onds.  Does not include time the  system  was  suspended.
                     See: cclloocckk__ggeettttiimmee(CCLLOOCCKK__MMOONNOOTTOONNIICC)

              RReettuurrnn Current _k_t_i_m_e.

       lloonngg bbppff__ttrraaccee__pprriinnttkk((ccoonnsstt cchhaarr **_f_m_t,, uu3322 _f_m_t___s_i_z_e,, ......))

              DDeessccrriippttiioonn
                     This  helper is a "printk()-like" facility for debugging.
                     It prints a  message  defined  by  format  _f_m_t  (of  size
                     _f_m_t___s_i_z_e) to file _/_s_y_s_/_k_e_r_n_e_l_/_t_r_a_c_i_n_g_/_t_r_a_c_e from TraceFS,
                     if  available. It can take up to three additional uu6644 ar‐
                     guments (as an eBPF helpers, the total  number  of  argu‐
                     ments is limited to five).

                     Each  time the helper is called, it appends a line to the
                     trace.   Lines  are  discarded  while   _/_s_y_s_/_k_e_r_n_e_l_/_t_r_a_c_‐
                     _i_n_g_/_t_r_a_c_e  is open, use _/_s_y_s_/_k_e_r_n_e_l_/_t_r_a_c_i_n_g_/_t_r_a_c_e___p_i_p_e to
                     avoid this.  The format of the trace is customizable, and
                     the exact output one will get depends on the options  set
                     in _/_s_y_s_/_k_e_r_n_e_l_/_t_r_a_c_i_n_g_/_t_r_a_c_e___o_p_t_i_o_n_s (see also the _R_E_A_D_M_E
                     file  under  the same directory). However, it usually de‐
                     faults to something like:

                        telnet-470   [001] .N.. 419421.045894: 0x00000001: <formatted msg>

                     In the above:

                        • tteellnneett is the name of the current task.

                        • 447700 is the PID of the current task.

                        • 000011 is the CPU number on which the task is running.

                        • In ..NN...., each character refers to a set  of  options
                          (whether   irqs  are  enabled,  scheduling  options,
                          whether hard/softirqs are  running,  level  of  pre‐
                          empt_disabled    respectively).    NN    means   that
                          TTIIFF__NNEEEEDD__RREESSCCHHEEDD and PPRREEEEMMPPTT__NNEEEEDD__RREESSCCHHEEDD are set.

                        • 441199442211..004455889944 is a timestamp.

                        • 00xx0000000000000011 is a fake value used by BPF for  the  in‐
                          struction pointer register.

                        • <<ffoorrmmaatttteedd mmssgg>> is the message formatted with _f_m_t.

                     The  conversion  specifiers supported by _f_m_t are similar,
                     but more limited than for printk(). They are %%dd, %%ii,  %%uu,
                     %%xx,  %%lldd,  %%llii, %%lluu, %%llxx, %%lllldd, %%llllii, %%lllluu, %%llllxx, %%pp, %%ss.
                     No modifier (size of field, padding with zeroes, etc.) is
                     available, and the helper will return --EEIINNVVAALL (but  print
                     nothing) if it encounters an unknown specifier.

                     Also,  note  that  bbppff__ttrraaccee__pprriinnttkk() is slow, and should
                     only be used for debugging purposes. For this  reason,  a
                     notice  block (spanning several lines) is printed to ker‐
                     nel logs and states that the helper should  not  be  used
                     "for  production  use" the first time this helper is used
                     (or more precisely, when ttrraaccee__pprriinnttkk() buffers are allo‐
                     cated). For passing values to  user  space,  perf  events
                     should be preferred.

              RReettuurrnn The  number of bytes written to the buffer, or a negative
                     error in case of failure.

       uu3322 bbppff__ggeett__pprraannddoomm__uu3322((vvooiidd))

              DDeessccrriippttiioonn
                     Get a pseudo-random number.

                     From a security point of view, this helper uses  its  own
                     pseudo-random internal state, and cannot be used to infer
                     the  seed  of  other random functions in the kernel. How‐
                     ever, it is essential to note that the generator used  by
                     the helper is not cryptographically secure.

              RReettuurrnn A random 32-bit unsigned value.

       uu3322 bbppff__ggeett__ssmmpp__pprroocceessssoorr__iidd((vvooiidd))

              DDeessccrriippttiioonn
                     Get  the  SMP  (symmetric  multiprocessing) processor id.
                     Note that all programs run with migration disabled, which
                     means that the SMP processor id is stable during all  the
                     execution of the program.

              RReettuurrnn The SMP id of the processor running the program.

       lloonngg bbppff__sskkbb__ssttoorree__bbyytteess((ssttrruucctt sskk__bbuuffff **_s_k_b,, uu3322 _o_f_f_s_e_t,, ccoonnsstt vvooiidd
       **_f_r_o_m,, uu3322 _l_e_n,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Store _l_e_n bytes from address _f_r_o_m into the packet associ‐
                     ated  to  _s_k_b,  at _o_f_f_s_e_t. The _f_l_a_g_s are a combination of
                     the following values:

                     BBPPFF__FF__RREECCOOMMPPUUTTEE__CCSSUUMM
                            Automatically update _s_k_b-->>ccssuumm after  storing  the
                            bytes.

                     BBPPFF__FF__IINNVVAALLIIDDAATTEE__HHAASSHH
                            Set _s_k_b-->>hhaasshh, _s_k_b-->>sswwhhaasshh and _s_k_b-->>ll44hhaasshh to 0.

                     A call to this helper is susceptible to change the under‐
                     lying  packet buffer. Therefore, at load time, all checks
                     on pointers previously done by the verifier  are  invali‐
                     dated  and must be performed again, if the helper is used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__ll33__ccssuumm__rreeppllaaccee((ssttrruucctt sskk__bbuuffff **_s_k_b,, uu3322 _o_f_f_s_e_t,, uu6644 _f_r_o_m,, uu6644
       _t_o,, uu6644 _s_i_z_e))

              DDeessccrriippttiioonn
                     Recompute the layer 3 (e.g. IP) checksum for  the  packet
                     associated  to  _s_k_b.  Computation  is incremental, so the
                     helper must know the former value  of  the  header  field
                     that  was  modified  (_f_r_o_m),  the new value of this field
                     (_t_o), and the number of bytes (2 or 4)  for  this  field,
                     stored  in  _s_i_z_e.  Alternatively, it is possible to store
                     the difference between the previous and the new values of
                     the header field in _t_o, by setting _f_r_o_m and  _s_i_z_e  to  0.
                     For both methods, _o_f_f_s_e_t indicates the location of the IP
                     checksum within the packet.

                     This  helper  works  in combination with bbppff__ccssuumm__ddiiffff(),
                     which does not update the checksum in-place,  but  offers
                     more  flexibility and can handle sizes larger than 2 or 4
                     for the checksum to update.

                     A call to this helper is susceptible to change the under‐
                     lying packet buffer. Therefore, at load time, all  checks
                     on  pointers  previously done by the verifier are invali‐
                     dated and must be performed again, if the helper is  used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__ll44__ccssuumm__rreeppllaaccee((ssttrruucctt sskk__bbuuffff **_s_k_b,, uu3322 _o_f_f_s_e_t,, uu6644 _f_r_o_m,, uu6644
       _t_o,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Recompute  the  layer  4 (e.g. TCP, UDP or ICMP) checksum
                     for the packet associated to _s_k_b. Computation  is  incre‐
                     mental,  so  the helper must know the former value of the
                     header field that was modified (_f_r_o_m), the new  value  of
                     this  field  (_t_o),  and  the number of bytes (2 or 4) for
                     this field, stored on the lowest four bits of _f_l_a_g_s.  Al‐
                     ternatively,  it  is possible to store the difference be‐
                     tween the previous and the new values of the header field
                     in _t_o, by setting _f_r_o_m and the four lowest bits of  _f_l_a_g_s
                     to  0. For both methods, _o_f_f_s_e_t indicates the location of
                     the IP checksum within the packet.  In  addition  to  the
                     size of the field, _f_l_a_g_s can be added (bitwise OR) actual
                     flags. With BBPPFF__FF__MMAARRKK__MMAANNGGLLEEDD__00, a null checksum is left
                     untouched  (unless  BBPPFF__FF__MMAARRKK__EENNFFOORRCCEE is added as well),
                     and for updates resulting in a null checksum the value is
                     set to CCSSUUMM__MMAANNGGLLEEDD__00 instead. Flag BBPPFF__FF__PPSSEEUUDDOO__HHDDRR  in‐
                     dicates  that  the  modified  header field is part of the
                     pseudo-header.  Flag BBPPFF__FF__IIPPVV66 should be  set  for  IPv6
                     packets.

                     This  helper  works  in combination with bbppff__ccssuumm__ddiiffff(),
                     which does not update the checksum in-place,  but  offers
                     more  flexibility and can handle sizes larger than 2 or 4
                     for the checksum to update.

                     A call to this helper is susceptible to change the under‐
                     lying packet buffer. Therefore, at load time, all  checks
                     on  pointers  previously done by the verifier are invali‐
                     dated and must be performed again, if the helper is  used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__ttaaiill__ccaallll((vvooiidd **_c_t_x,, ssttrruucctt bbppff__mmaapp **_p_r_o_g___a_r_r_a_y___m_a_p,, uu3322 _i_n_‐
       _d_e_x))

              DDeessccrriippttiioonn
                     This  special helper is used to trigger a "tail call", or
                     in other words, to jump into another  eBPF  program.  The
                     same stack frame is used (but values on stack and in reg‐
                     isters  for the caller are not accessible to the callee).
                     This mechanism allows for program  chaining,  either  for
                     raising  the  maximum  number  of available eBPF instruc‐
                     tions,  or  to  execute  given  programs  in  conditional
                     blocks.  For security reasons, there is an upper limit to
                     the number of successive tail  calls  that  can  be  per‐
                     formed.

                     Upon  call  of  this helper, the program attempts to jump
                     into a program referenced  at  index  _i_n_d_e_x  in  _p_r_o_g___a_r_‐
                     _r_a_y___m_a_p,  a  special map of type BBPPFF__MMAAPP__TTYYPPEE__PPRROOGG__AARRRRAAYY,
                     and passes _c_t_x, a pointer to the context.

                     If the call succeeds, the  kernel  immediately  runs  the
                     first instruction of the new program. This is not a func‐
                     tion  call, and it never returns to the previous program.
                     If the call fails, then the helper has no effect, and the
                     caller continues to run its  subsequent  instructions.  A
                     call  can  fail  if  the destination program for the jump
                     does not exist (i.e. _i_n_d_e_x is superior to the  number  of
                     entries  in  _p_r_o_g___a_r_r_a_y___m_a_p), or if the maximum number of
                     tail calls has been reached for this chain  of  programs.
                     This  limit  is  defined  in  the  kernel  by  the  macro
                     MMAAXX__TTAAIILL__CCAALLLL__CCNNTT (not accessible to user  space),  which
                     is currently set to 33.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__cclloonnee__rreeddiirreecctt((ssttrruucctt sskk__bbuuffff **_s_k_b,, uu3322 _i_f_i_n_d_e_x,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Clone  and  redirect  the packet associated to _s_k_b to an‐
                     other net device  of  index  _i_f_i_n_d_e_x.  Both  ingress  and
                     egress  interfaces  can  be  used  for  redirection.  The
                     BBPPFF__FF__IINNGGRREESSSS value in _f_l_a_g_s is used to make the distinc‐
                     tion (ingress path is selected if the  flag  is  present,
                     egress  path otherwise).  This is the only flag supported
                     for now.

                     In comparison with bbppff__rreeddiirreecctt() helper, bbppff__cclloonnee__rreeddii‐‐
                     rreecctt() has the associated cost of duplicating the  packet
                     buffer, but this can be executed out of the eBPF program.
                     Conversely,  bbppff__rreeddiirreecctt()  is more efficient, but it is
                     handled through an action code where the redirection hap‐
                     pens only after the eBPF program has returned.

                     A call to this helper is susceptible to change the under‐
                     lying packet buffer. Therefore, at load time, all  checks
                     on  pointers  previously done by the verifier are invali‐
                     dated and must be performed again, if the helper is  used
                     in combination with direct packet access.

              RReettuurrnn 0  on  success,  or  a negative error in case of failure.
                     Positive error indicates a potential drop  or  congestion
                     in the target device. The particular positive error codes
                     are not defined.

       uu6644 bbppff__ggeett__ccuurrrreenntt__ppiidd__ttggiidd((vvooiidd))

              DDeessccrriippttiioonn
                     Get the current pid and tgid.

              RReettuurrnn A 64-bit integer containing the current tgid and pid, and
                     created   as   such:  _c_u_r_r_e_n_t___t_a_s_k-->>ttggiidd  <<<<  3322  ||  _c_u_r_‐
                     _r_e_n_t___t_a_s_k-->>ppiidd.

       uu6644 bbppff__ggeett__ccuurrrreenntt__uuiidd__ggiidd((vvooiidd))

              DDeessccrriippttiioonn
                     Get the current uid and gid.

              RReettuurrnn A 64-bit integer containing the current GID and UID,  and
                     created as such: _c_u_r_r_e_n_t___g_i_d <<<< 3322 || _c_u_r_r_e_n_t___u_i_d.

       lloonngg bbppff__ggeett__ccuurrrreenntt__ccoommmm((vvooiidd **_b_u_f,, uu3322 _s_i_z_e___o_f___b_u_f))

              DDeessccrriippttiioonn
                     Copy  the  ccoommmm attribute of the current task into _b_u_f of
                     _s_i_z_e___o_f___b_u_f. The ccoommmm attribute contains the name of  the
                     executable (excluding the path) for the current task. The
                     _s_i_z_e___o_f___b_u_f  must  be  strictly positive. On success, the
                     helper makes sure that  the  _b_u_f  is  NUL-terminated.  On
                     failure, it is filled with zeroes.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       uu3322 bbppff__ggeett__ccggrroouupp__ccllaassssiidd((ssttrruucctt sskk__bbuuffff **_s_k_b))

              DDeessccrriippttiioonn
                     Retrieve  the  classid for the current task, i.e. for the
                     net_cls cgroup to which _s_k_b belongs.

                     This helper can be used on TC egress  path,  but  not  on
                     ingress.

                     The  net_cls  cgroup provides an interface to tag network
                     packets based on a user-provided identifier for all traf‐
                     fic coming  from  the  tasks  belonging  to  the  related
                     cgroup. See also the related kernel documentation, avail‐
                     able  from  the  Linux  sources in file _D_o_c_u_m_e_n_t_a_t_i_o_n_/_a_d_‐
                     _m_i_n_-_g_u_i_d_e_/_c_g_r_o_u_p_-_v_1_/_n_e_t___c_l_s_._r_s_t.

                     The Linux kernel has two versions for cgroups: there  are
                     cgroups  v1  and cgroups v2. Both are available to users,
                     who can use a mixture of them, but note that the  net_cls
                     cgroup  is for cgroup v1 only. This makes it incompatible
                     with  BPF  programs  run   on   cgroups,   which   is   a
                     cgroup-v2-only  feature  (a socket can only hold data for
                     one version of cgroups at a time).

                     This helper is only available is the kernel was  compiled
                     with  the  CCOONNFFIIGG__CCGGRROOUUPP__NNEETT__CCLLAASSSSIIDD configuration option
                     set to "yy" or to "mm".

              RReettuurrnn The classid, or 0 for the default unconfigured classid.

       lloonngg bbppff__sskkbb__vvllaann__ppuusshh((ssttrruucctt sskk__bbuuffff **_s_k_b,, ____bbee1166 _v_l_a_n___p_r_o_t_o,, uu1166
       _v_l_a_n___t_c_i))

              DDeessccrriippttiioonn
                     Push a _v_l_a_n___t_c_i (VLAN tag control information) of  proto‐
                     col  _v_l_a_n___p_r_o_t_o to the packet associated to _s_k_b, then up‐
                     date the checksum. Note that if _v_l_a_n___p_r_o_t_o  is  different
                     from EETTHH__PP__88002211QQ and EETTHH__PP__88002211AADD, it is considered to be
                     EETTHH__PP__88002211QQ.

                     A call to this helper is susceptible to change the under‐
                     lying  packet buffer. Therefore, at load time, all checks
                     on pointers previously done by the verifier  are  invali‐
                     dated  and must be performed again, if the helper is used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__sskkbb__vvllaann__ppoopp((ssttrruucctt sskk__bbuuffff **_s_k_b))

              DDeessccrriippttiioonn
                     Pop a VLAN header from the packet associated to _s_k_b.

                     A call to this helper is susceptible to change the under‐
                     lying packet buffer. Therefore, at load time, all  checks
                     on  pointers  previously done by the verifier are invali‐
                     dated and must be performed again, if the helper is  used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__sskkbb__ggeett__ttuunnnneell__kkeeyy((ssttrruucctt sskk__bbuuffff **_s_k_b,, ssttrruucctt bbppff__ttuunnnneell__kkeeyy
       **_k_e_y,, uu3322 _s_i_z_e,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Get  tunnel  metadata. This helper takes a pointer _k_e_y to
                     an empty ssttrruucctt bbppff__ttuunnnneell__kkeeyy  of  ssiizzee,  that  will  be
                     filled  with tunnel metadata for the packet associated to
                     _s_k_b.  The _f_l_a_g_s can be set to  BBPPFF__FF__TTUUNNIINNFFOO__IIPPVV66,  which
                     indicates  that  the tunnel is based on IPv6 protocol in‐
                     stead of IPv4.

                     The ssttrruucctt bbppff__ttuunnnneell__kkeeyy is an object  that  generalizes
                     the principal parameters used by various tunneling proto‐
                     cols  into  a  single struct. This way, it can be used to
                     easily make a decision based on the contents of  the  en‐
                     capsulation  header, "summarized" in this struct. In par‐
                     ticular, it holds the IP address of the remote end  (IPv4
                     or  IPv6,  depending  on the case) in _k_e_y-->>rreemmoottee__iippvv44 or
                     _k_e_y-->>rreemmoottee__iippvv66. Also, this struct exposes the _k_e_y-->>ttuunn‐‐
                     nneell__iidd, which is generally mapped to a VNI (Virtual  Net‐
                     work  Identifier),  making  it programmable together with
                     the bbppff__sskkbb__sseett__ttuunnnneell__kkeeyy() helper.

                     Let's imagine that the following code is part of  a  pro‐
                     gram  attached to the TC ingress interface, on one end of
                     a GRE tunnel, and is supposed to filter out all  messages
                     coming  from  remote  ends  with  IPv4 address other than
                     10.0.0.1:

                        int ret;
                        struct bpf_tunnel_key key = {};

                        ret = bpf_skb_get_tunnel_key(skb, &key, sizeof(key), 0);
                        if (ret < 0)
                                return TC_ACT_SHOT;     // drop packet

                        if (key.remote_ipv4 != 0x0a000001)
                                return TC_ACT_SHOT;     // drop packet

                        return TC_ACT_OK;               // accept packet

                     This interface can also be used  with  all  encapsulation
                     devices  that can operate in "collect metadata" mode: in‐
                     stead of having one network device per specific  configu‐
                     ration,  the "collect metadata" mode only requires a sin‐
                     gle device where the configuration can be extracted  from
                     this helper.

                     This  can  be  used together with various tunnels such as
                     VXLan, Geneve, GRE or IP in IP (IPIP).

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__sskkbb__sseett__ttuunnnneell__kkeeyy((ssttrruucctt sskk__bbuuffff **_s_k_b,, ssttrruucctt bbppff__ttuunnnneell__kkeeyy
       **_k_e_y,, uu3322 _s_i_z_e,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Populate tunnel metadata for packet  associated  to  _s_k_b_.
                     The  tunnel  metadata  is  set to the contents of _k_e_y, of
                     _s_i_z_e. The _f_l_a_g_s can be set to a combination of  the  fol‐
                     lowing values:

                     BBPPFF__FF__TTUUNNIINNFFOO__IIPPVV66
                            Indicate that the tunnel is based on IPv6 protocol
                            instead of IPv4.

                     BBPPFF__FF__ZZEERROO__CCSSUUMM__TTXX
                            For  IPv4  packets,  add a flag to tunnel metadata
                            indicating that  checksum  computation  should  be
                            skipped and checksum set to zeroes.

                     BBPPFF__FF__DDOONNTT__FFRRAAGGMMEENNTT
                            Add  a flag to tunnel metadata indicating that the
                            packet should not be fragmented.

                     BBPPFF__FF__SSEEQQ__NNUUMMBBEERR
                            Add a flag to tunnel metadata  indicating  that  a
                            sequence  number  should be added to tunnel header
                            before sending the packet. This flag was added for
                            GRE encapsulation, but might be  used  with  other
                            protocols as well in the future.

                     BBPPFF__FF__NNOO__TTUUNNNNEELL__KKEEYY
                            Add  a  flag to tunnel metadata indicating that no
                            tunnel key should be set in the  resulting  tunnel
                            header.

                     Here is a typical usage on the transmit path:

                        struct bpf_tunnel_key key;
                             populate key ...
                        bpf_skb_set_tunnel_key(skb, &key, sizeof(key), 0);
                        bpf_clone_redirect(skb, vxlan_dev_ifindex, 0);

                     See  also the description of the bbppff__sskkbb__ggeett__ttuunnnneell__kkeeyy()
                     helper for additional information.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       uu6644 bbppff__ppeerrff__eevveenntt__rreeaadd((ssttrruucctt bbppff__mmaapp **_m_a_p,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Read the value of a perf event counter. This  helper  re‐
                     lies  on a _m_a_p of type BBPPFF__MMAAPP__TTYYPPEE__PPEERRFF__EEVVEENNTT__AARRRRAAYY. The
                     nature of the perf event counter is selected when _m_a_p  is
                     updated  with  perf event file descriptors. The _m_a_p is an
                     array whose size is the number  of  available  CPUs,  and
                     each cell contains a value relative to one CPU. The value
                     to  retrieve is indicated by _f_l_a_g_s, that contains the in‐
                     dex of the CPU to look up, masked with  BBPPFF__FF__IINNDDEEXX__MMAASSKK.
                     Alternatively,  _f_l_a_g_s  can be set to BBPPFF__FF__CCUURRRREENNTT__CCPPUU to
                     indicate that the value for the current CPU should be re‐
                     trieved.

                     Note that before Linux 4.13, only hardware perf event can
                     be retrieved.

                     Also,    be    aware    that     the     newer     helper
                     bbppff__ppeerrff__eevveenntt__rreeaadd__vvaalluuee()     is    recommended    over
                     bbppff__ppeerrff__eevveenntt__rreeaadd() in general. The latter has some ABI
                     quirks where error and counter value are used as a return
                     code (which is wrong to do  since  ranges  may  overlap).
                     This  issue  is  fixed  with bbppff__ppeerrff__eevveenntt__rreeaadd__vvaalluuee(),
                     which at the same time provides more  features  over  the
                     bbppff__ppeerrff__eevveenntt__rreeaadd()  interface. Please refer to the de‐
                     scription of bbppff__ppeerrff__eevveenntt__rreeaadd__vvaalluuee() for details.

              RReettuurrnn The value of the perf event counter read from the map, or
                     a negative error code in case of failure.

       lloonngg bbppff__rreeddiirreecctt((uu3322 _i_f_i_n_d_e_x,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Redirect the  packet  to  another  net  device  of  index
                     _i_f_i_n_d_e_x.     This   helper   is   somewhat   similar   to
                     bbppff__cclloonnee__rreeddiirreecctt(),  except  that  the  packet  is  not
                     cloned, which provides increased performance.

                     Except for XDP, both ingress and egress interfaces can be
                     used for redirection. The BBPPFF__FF__IINNGGRREESSSS value in _f_l_a_g_s is
                     used to make the distinction (ingress path is selected if
                     the  flag  is present, egress path otherwise). Currently,
                     XDP only supports redirection to  the  egress  interface,
                     and accepts no flag at all.

                     The  same  effect  can  also  be  attained  with the more
                     generic bbppff__rreeddiirreecctt__mmaapp(), which uses a BPF map to store
                     the redirect target instead of providing it  directly  to
                     the helper.

              RReettuurrnn For  XDP,  the  helper returns XXDDPP__RREEDDIIRREECCTT on success or
                     XXDDPP__AABBOORRTTEEDD on error. For other program types, the values
                     are TTCC__AACCTT__RREEDDIIRREECCTT on success or TTCC__AACCTT__SSHHOOTT on error.

       uu3322 bbppff__ggeett__rroouuttee__rreeaallmm((ssttrruucctt sskk__bbuuffff **_s_k_b))

              DDeessccrriippttiioonn
                     Retrieve the realm or the  route,  that  is  to  say  the
                     ttccllaassssiidd  field of the destination for the _s_k_b. The iden‐
                     tifier retrieved is a user-provided tag, similar  to  the
                     one  used  with  the  net_cls cgroup (see description for
                     bbppff__ggeett__ccggrroouupp__ccllaassssiidd() helper), but here  this  tag  is
                     held by a route (a destination entry), not by a task.

                     Retrieving  this  identifier  works  with  the  clsact TC
                     egress hook (see also  ttcc--bbppff((88))),  or  alternatively  on
                     conventional  classful  egress  qdiscs,  but  not  on  TC
                     ingress path. In case of clsact TC egress hook, this  has
                     the advantage that, internally, the destination entry has
                     not been dropped yet in the transmit path. Therefore, the
                     destination  entry  does not need to be artificially held
                     via nneettiiff__kkeeeepp__ddsstt() for a classful qdisc until  the  _s_k_b
                     is freed.

                     This  helper is available only if the kernel was compiled
                     with CCOONNFFIIGG__IIPP__RROOUUTTEE__CCLLAASSSSIIDD configuration option.

              RReettuurrnn The realm of the route for the packet associated to  _s_k_b,
                     or 0 if none was found.

       lloonngg bbppff__ppeerrff__eevveenntt__oouuttppuutt((vvooiidd **_c_t_x,, ssttrruucctt bbppff__mmaapp **_m_a_p,, uu6644 _f_l_a_g_s,,
       vvooiidd **_d_a_t_a,, uu6644 _s_i_z_e))

              DDeessccrriippttiioonn
                     Write raw _d_a_t_a blob into a special BPF perf event held by
                     _m_a_p  of  type  BBPPFF__MMAAPP__TTYYPPEE__PPEERRFF__EEVVEENNTT__AARRRRAAYY.  This  perf
                     event must have the following attributes: PPEERRFF__SSAAMMPPLLEE__RRAAWW
                     as   ssaammppllee__ttyyppee,   PPEERRFF__TTYYPPEE__SSOOFFTTWWAARREE   as   ttyyppee,   and
                     PPEERRFF__CCOOUUNNTT__SSWW__BBPPFF__OOUUTTPPUUTT as ccoonnffiigg.

                     The _f_l_a_g_s are used to indicate the index in _m_a_p for which
                     the value must be put, masked with BBPPFF__FF__IINNDDEEXX__MMAASSKK.  Al‐
                     ternatively, _f_l_a_g_s can be set to BBPPFF__FF__CCUURRRREENNTT__CCPPUU to in‐
                     dicate  that  the index of the current CPU core should be
                     used.

                     The value to write, of _s_i_z_e, is passed through eBPF stack
                     and pointed by _d_a_t_a.

                     The context of the program _c_t_x needs also  be  passed  to
                     the helper.

                     On user space, a program willing to read the values needs
                     to  call  ppeerrff__eevveenntt__ooppeenn() on the perf event (either for
                     one or for all CPUs) and to  store  the  file  descriptor
                     into  the  _m_a_p. This must be done before the eBPF program
                     can send data into it. An example is  available  in  file
                     _s_a_m_p_l_e_s_/_b_p_f_/_t_r_a_c_e___o_u_t_p_u_t___u_s_e_r_._c   in   the  Linux  kernel
                     source tree (the eBPF  program  counterpart  is  in  _s_a_m_‐
                     _p_l_e_s_/_b_p_f_/_t_r_a_c_e___o_u_t_p_u_t_._b_p_f_._c).

                     bbppff__ppeerrff__eevveenntt__oouuttppuutt()  achieves better performance than
                     bbppff__ttrraaccee__pprriinnttkk() for sharing data with user space,  and
                     is much better suitable for streaming data from eBPF pro‐
                     grams.

                     Note  that  this  helper is not restricted to tracing use
                     cases and can be used with programs attached to TC or XDP
                     as well, where it allows for passing data to  user  space
                     listeners. Data can be:

                     • Only custom structs,

                     • Only the packet payload, or

                     • A combination of both.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__sskkbb__llooaadd__bbyytteess((ccoonnsstt vvooiidd **_s_k_b,, uu3322 _o_f_f_s_e_t,, vvooiidd **_t_o,, uu3322 _l_e_n))

              DDeessccrriippttiioonn
                     This helper was provided as an easy way to load data from
                     a  packet.  It  can be used to load _l_e_n bytes from _o_f_f_s_e_t
                     from the  packet  associated  to  _s_k_b,  into  the  buffer
                     pointed by _t_o.

                     Since Linux 4.7, usage of this helper has mostly been re‐
                     placed by "direct packet access", enabling packet data to
                     be  manipulated with _s_k_b-->>ddaattaa and _s_k_b-->>ddaattaa__eenndd pointing
                     respectively to the first byte of packet data and to  the
                     byte  after the last byte of packet data. However, it re‐
                     mains useful if one wishes to read  large  quantities  of
                     data at once from a packet into the eBPF stack.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__ggeett__ssttaacckkiidd((vvooiidd **_c_t_x,, ssttrruucctt bbppff__mmaapp **_m_a_p,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Walk  a  user  or  a  kernel  stack and return its id. To
                     achieve this, the helper needs _c_t_x, which is a pointer to
                     the context on which the tracing program is executed, and
                     a pointer to a _m_a_p of type BBPPFF__MMAAPP__TTYYPPEE__SSTTAACCKK__TTRRAACCEE.

                     The last argument,  _f_l_a_g_s,  holds  the  number  of  stack
                     frames   to   skip   (from   0   to   255),  masked  with
                     BBPPFF__FF__SSKKIIPP__FFIIEELLDD__MMAASSKK. The next bits can be used to set a
                     combination of the following flags:

                     BBPPFF__FF__UUSSEERR__SSTTAACCKK
                            Collect a user space stack  instead  of  a  kernel
                            stack.

                     BBPPFF__FF__FFAASSTT__SSTTAACCKK__CCMMPP
                            Compare stacks by hash only.

                     BBPPFF__FF__RREEUUSSEE__SSTTAACCKKIIDD
                            If   two  different  stacks  hash  into  the  same
                            _s_t_a_c_k_i_d, discard the old one.

                     The stack id retrieved is a 32 bit  long  integer  handle
                     which  can be further combined with other data (including
                     other stack ids) and used as a key into maps. This can be
                     useful for generating a variety of graphs (such as  flame
                     graphs or off-cpu graphs).

                     For  walking  a stack, this helper is an improvement over
                     bbppff__pprroobbee__rreeaadd(), which can be used with  unrolled  loops
                     but  is not efficient and consumes a lot of eBPF instruc‐
                     tions.  Instead,  bbppff__ggeett__ssttaacckkiidd()  can  collect  up  to
                     PPEERRFF__MMAAXX__SSTTAACCKK__DDEEPPTTHH  both  kernel  and user frames. Note
                     that this limit can be controlled with  the  ssyyssccttll  pro‐
                     gram,  and  that it should be manually increased in order
                     to profile long user stacks (such as stacks for Java pro‐
                     grams). To do so, use:

                        # sysctl kernel.perf_event_max_stack=<new value>

              RReettuurrnn The positive or null stack id on success, or  a  negative
                     error in case of failure.

       ss6644 bbppff__ccssuumm__ddiiffff((____bbee3322 **_f_r_o_m,, uu3322 _f_r_o_m___s_i_z_e,, ____bbee3322 **_t_o,, uu3322 _t_o___s_i_z_e,,
       ____wwssuumm _s_e_e_d))

              DDeessccrriippttiioonn
                     Compute  a  checksum  difference,  from  the  raw  buffer
                     pointed by _f_r_o_m, of length _f_r_o_m___s_i_z_e (that must be a mul‐
                     tiple of 4), towards the raw buffer  pointed  by  _t_o,  of
                     size _t_o___s_i_z_e (same remark). An optional _s_e_e_d can be added
                     to  the  value  (this  can be cascaded, the seed may come
                     from a previous call to the helper).

                     This is flexible enough to be used in several ways:

                     • With _f_r_o_m___s_i_z_e == 0, _t_o___s_i_z_e > 0 and _s_e_e_d set to check‐
                       sum, it can be used when pushing new data.

                     • With _f_r_o_m___s_i_z_e > 0, _t_o___s_i_z_e == 0 and _s_e_e_d set to check‐
                       sum, it can be used when removing data from a packet.

                     • With _f_r_o_m___s_i_z_e > 0, _t_o___s_i_z_e > 0 and _s_e_e_d set to  0,  it
                       can  be used to compute a diff. Note that _f_r_o_m___s_i_z_e and
                       _t_o___s_i_z_e do not need to be equal.

                     This   helper   can   be   used   in   combination   with
                     bbppff__ll33__ccssuumm__rreeppllaaccee() and bbppff__ll44__ccssuumm__rreeppllaaccee(), to which
                     one   can   feed   in   the   difference   computed  with
                     bbppff__ccssuumm__ddiiffff().

              RReettuurrnn The checksum result, or a negative error code in case  of
                     failure.

       lloonngg bbppff__sskkbb__ggeett__ttuunnnneell__oopptt((ssttrruucctt sskk__bbuuffff **_s_k_b,, vvooiidd **_o_p_t,, uu3322 _s_i_z_e))

              DDeessccrriippttiioonn
                     Retrieve  tunnel  options metadata for the packet associ‐
                     ated to _s_k_b, and store the raw tunnel option data to  the
                     buffer _o_p_t of _s_i_z_e.

                     This  helper  can be used with encapsulation devices that
                     can operate in "collect metadata" mode (please  refer  to
                     the  related  note in the description of bbppff__sskkbb__ggeett__ttuunn‐‐
                     nneell__kkeeyy() for more details). A particular  example  where
                     this can be used is in combination with the Geneve encap‐
                     sulation  protocol,  where  it  allows  for pushing (with
                     bbppff__sskkbb__ggeett__ttuunnnneell__oopptt() helper) and retrieving arbitrary
                     TLVs (Type-Length-Value headers) from the  eBPF  program.
                     This allows for full customization of these headers.

              RReettuurrnn The size of the option data retrieved.

       lloonngg bbppff__sskkbb__sseett__ttuunnnneell__oopptt((ssttrruucctt sskk__bbuuffff **_s_k_b,, vvooiidd **_o_p_t,, uu3322 _s_i_z_e))

              DDeessccrriippttiioonn
                     Set  tunnel options metadata for the packet associated to
                     _s_k_b to the option data contained in the raw buffer _o_p_t of
                     _s_i_z_e.

                     See also the description of the  bbppff__sskkbb__ggeett__ttuunnnneell__oopptt()
                     helper for additional information.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__sskkbb__cchhaannggee__pprroottoo((ssttrruucctt sskk__bbuuffff **_s_k_b,, ____bbee1166 _p_r_o_t_o,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Change  the  protocol of the _s_k_b to _p_r_o_t_o. Currently sup‐
                     ported are transition from IPv4 to IPv6, and from IPv6 to
                     IPv4. The helper takes care of  the  groundwork  for  the
                     transition,  including  resizing  the  socket buffer. The
                     eBPF program is expected to fill the new headers, if any,
                     via sskkbb__ssttoorree__bbyytteess() and to recompute the checksums with
                     bbppff__ll33__ccssuumm__rreeppllaaccee() and bbppff__ll44__ccssuumm__rreeppllaaccee(). The main
                     case for this helper is to perform NAT64  operations  out
                     of an eBPF program.

                     Internally, the GSO type is marked as dodgy so that head‐
                     ers  are  checked  and  segments  are recalculated by the
                     GSO/GRO engine.  The size for GSO target  is  adapted  as
                     well.

                     All  values  for _f_l_a_g_s are reserved for future usage, and
                     must be left at zero.

                     A call to this helper is susceptible to change the under‐
                     lying packet buffer. Therefore, at load time, all  checks
                     on  pointers  previously done by the verifier are invali‐
                     dated and must be performed again, if the helper is  used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__sskkbb__cchhaannggee__ttyyppee((ssttrruucctt sskk__bbuuffff **_s_k_b,, uu3322 _t_y_p_e))

              DDeessccrriippttiioonn
                     Change  the packet type for the packet associated to _s_k_b.
                     This comes down to setting _s_k_b-->>ppkktt__ttyyppee to _t_y_p_e,  except
                     the  eBPF  program  does  not  have  a  write  access  to
                     _s_k_b-->>ppkktt__ttyyppee beside this helper. Using a helper here al‐
                     lows for graceful handling of errors.

                     The major  use  case  is  to  change  incoming  _s_k_b_*_s  _t_o
                     _*_*_P_A_C_K_E_T___H_O_S_T_* in a programmatic way instead of having to
                     recirculate  via  rreeddiirreecctt(..., BBPPFF__FF__IINNGGRREESSSS), for exam‐
                     ple.

                     Note that _t_y_p_e only allows certain values. At this  time,
                     they are:

                     PPAACCKKEETT__HHOOSSTT
                            Packet is for us.

                     PPAACCKKEETT__BBRROOAADDCCAASSTT
                            Send packet to all.

                     PPAACCKKEETT__MMUULLTTIICCAASSTT
                            Send packet to group.

                     PPAACCKKEETT__OOTTHHEERRHHOOSSTT
                            Send packet to someone else.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__sskkbb__uunnddeerr__ccggrroouupp((ssttrruucctt sskk__bbuuffff **_s_k_b,, ssttrruucctt bbppff__mmaapp **_m_a_p,, uu3322
       _i_n_d_e_x))

              DDeessccrriippttiioonn
                     Check  whether _s_k_b is a descendant of the cgroup2 held by
                     _m_a_p of type BBPPFF__MMAAPP__TTYYPPEE__CCGGRROOUUPP__AARRRRAAYY, at _i_n_d_e_x.

              RReettuurrnn The return value depends on the result of the  test,  and
                     can be:

                     • 0, if the _s_k_b failed the cgroup2 descendant test.

                     • 1, if the _s_k_b succeeded the cgroup2 descendant test.

                     • A negative error code, if an error occurred.

       uu3322 bbppff__ggeett__hhaasshh__rreeccaallcc((ssttrruucctt sskk__bbuuffff **_s_k_b))

              DDeessccrriippttiioonn
                     Retrieve  the hash of the packet, _s_k_b-->>hhaasshh. If it is not
                     set, in particular if the hash was cleared  due  to  man‐
                     gling,  recompute  this  hash. Later accesses to the hash
                     can be done directly with _s_k_b-->>hhaasshh.

                     Calling bbppff__sseett__hhaasshh__iinnvvaalliidd(), changing a packet  proto‐
                     type     with    bbppff__sskkbb__cchhaannggee__pprroottoo(),    or    calling
                     bbppff__sskkbb__ssttoorree__bbyytteess() with the BBPPFF__FF__IINNVVAALLIIDDAATTEE__HHAASSHH  are
                     actions  susceptible  to  clear the hash and to trigger a
                     new computation for the  next  call  to  bbppff__ggeett__hhaasshh__rree‐‐
                     ccaallcc().

              RReettuurrnn The 32-bit hash.

       uu6644 bbppff__ggeett__ccuurrrreenntt__ttaasskk((vvooiidd))

              DDeessccrriippttiioonn
                     Get the current task.

              RReettuurrnn A pointer to the current task struct.

       lloonngg bbppff__pprroobbee__wwrriittee__uusseerr((vvooiidd **_d_s_t,, ccoonnsstt vvooiidd **_s_r_c,, uu3322 _l_e_n))

              DDeessccrriippttiioonn
                     Attempt  in a safe way to write _l_e_n bytes from the buffer
                     _s_r_c to _d_s_t in memory. It only works for threads that  are
                     in  user  context, and _d_s_t must be a valid user space ad‐
                     dress.

                     This helper should not be used to implement any  kind  of
                     security mechanism because of TOC-TOU attacks, but rather
                     to  debug, divert, and manipulate execution of semi-coop‐
                     erative processes.

                     Keep in mind that this feature is meant for  experiments,
                     and it has a risk of crashing the system and running pro‐
                     grams.  Therefore, when an eBPF program using this helper
                     is  attached, a warning including PID and process name is
                     printed to kernel logs.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__ccuurrrreenntt__ttaasskk__uunnddeerr__ccggrroouupp((ssttrruucctt bbppff__mmaapp **_m_a_p,, uu3322 _i_n_d_e_x))

              DDeessccrriippttiioonn
                     Check whether the probe is being run is the context of  a
                     given  subset  of  the  cgroup2 hierarchy. The cgroup2 to
                     test is held by _m_a_p of type BBPPFF__MMAAPP__TTYYPPEE__CCGGRROOUUPP__AARRRRAAYY, at
                     _i_n_d_e_x.

              RReettuurrnn The return value depends on the result of the  test,  and
                     can be:

                     • 1, if current task belongs to the cgroup2.

                     • 0, if current task does not belong to the cgroup2.

                     • A negative error code, if an error occurred.

       lloonngg bbppff__sskkbb__cchhaannggee__ttaaiill((ssttrruucctt sskk__bbuuffff **_s_k_b,, uu3322 _l_e_n,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Resize (trim or grow) the packet associated to _s_k_b to the
                     new  _l_e_n.  The  _f_l_a_g_s  are reserved for future usage, and
                     must be left at zero.

                     The basic idea is that the  helper  performs  the  needed
                     work to change the size of the packet, then the eBPF pro‐
                     gram    rewrites    the    rest    via    helpers    like
                     bbppff__sskkbb__ssttoorree__bbyytteess(),             bbppff__ll33__ccssuumm__rreeppllaaccee(),
                     bbppff__ll33__ccssuumm__rreeppllaaccee()  and  others. This helper is a slow
                     path utility intended for replies with control  messages.
                     And  because it is targeted for slow path, the helper it‐
                     self can afford to be slow: it implicitly linearizes, un‐
                     clones and drops offloads from the _s_k_b.

                     A call to this helper is susceptible to change the under‐
                     lying packet buffer. Therefore, at load time, all  checks
                     on  pointers  previously done by the verifier are invali‐
                     dated and must be performed again, if the helper is  used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__sskkbb__ppuullll__ddaattaa((ssttrruucctt sskk__bbuuffff **_s_k_b,, uu3322 _l_e_n))

              DDeessccrriippttiioonn
                     Pull in non-linear data in case the _s_k_b is non-linear and
                     not  all  of _l_e_n are part of the linear section. Make _l_e_n
                     bytes from _s_k_b readable and writable. If a zero value  is
                     passed  for _l_e_n, then all bytes in the linear part of _s_k_b
                     will be made readable and writable.

                     This helper is only needed for reading and  writing  with
                     direct packet access.

                     For  direct packet access, testing that offsets to access
                     are within packet boundaries (test on  _s_k_b-->>ddaattaa__eenndd)  is
                     susceptible to fail if offsets are invalid, or if the re‐
                     quested  data is in non-linear parts of the _s_k_b. On fail‐
                     ure the program can just bail out, or in the  case  of  a
                     non-linear  buffer,  use a helper to make the data avail‐
                     able. The bbppff__sskkbb__llooaadd__bbyytteess() helper is a first solution
                     to  access  the  data.  Another  one  consists  in  using
                     bbppff__sskkbb__ppuullll__ddaattaa  to  pull in once the non-linear parts,
                     then retesting and eventually access the data.

                     At the same time, this also makes sure  the  _s_k_b  is  un‐
                     cloned,  which is a necessary condition for direct write.
                     As this needs to be an invariant for the write part only,
                     the verifier detects writes and adds a prologue  that  is
                     calling  bbppff__sskkbb__ppuullll__ddaattaa(())  to  effectively unclone the
                     _s_k_b from the very beginning in case it is indeed cloned.

                     A call to this helper is susceptible to change the under‐
                     lying packet buffer. Therefore, at load time, all  checks
                     on  pointers  previously done by the verifier are invali‐
                     dated and must be performed again, if the helper is  used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       ss6644 bbppff__ccssuumm__uuppddaattee((ssttrruucctt sskk__bbuuffff **_s_k_b,, ____wwssuumm _c_s_u_m))

              DDeessccrriippttiioonn
                     Add  the  checksum _c_s_u_m into _s_k_b-->>ccssuumm in case the driver
                     has supplied a checksum for the entire packet  into  that
                     field. Return an error otherwise. This helper is intended
                     to  be  used in combination with bbppff__ccssuumm__ddiiffff(), in par‐
                     ticular when the checksum needs to be updated after  data
                     has  been  written  into the packet through direct packet
                     access.

              RReettuurrnn The checksum on success, or a negative error code in case
                     of failure.

       vvooiidd bbppff__sseett__hhaasshh__iinnvvaalliidd((ssttrruucctt sskk__bbuuffff **_s_k_b))

              DDeessccrriippttiioonn
                     Invalidate the current _s_k_b-->>hhaasshh. It can  be  used  after
                     mangling  on headers through direct packet access, in or‐
                     der to indicate that the hash is outdated and to  trigger
                     a  recalculation the next time the kernel tries to access
                     this hash or when  the  bbppff__ggeett__hhaasshh__rreeccaallcc()  helper  is
                     called.

              RReettuurrnn void.

       lloonngg bbppff__ggeett__nnuummaa__nnooddee__iidd((vvooiidd))

              DDeessccrriippttiioonn
                     Return  the  id of the current NUMA node. The primary use
                     case for this helper is the selection of sockets for  the
                     local  NUMA node, when the program is attached to sockets
                     using  the  SSOO__AATTTTAACCHH__RREEUUSSEEPPOORRTT__EEBBPPFF  option  (see   also
                     ssoocckkeett((77))),  but  the  helper  is also available to other
                     eBPF  program  types,  similarly  to  bbppff__ggeett__ssmmpp__pprroocceess‐‐
                     ssoorr__iidd().

              RReettuurrnn The id of current NUMA node.

       lloonngg bbppff__sskkbb__cchhaannggee__hheeaadd((ssttrruucctt sskk__bbuuffff **_s_k_b,, uu3322 _l_e_n,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Grows  headroom  of  packet associated to _s_k_b and adjusts
                     the offset of the  MAC  header  accordingly,  adding  _l_e_n
                     bytes  of space. It automatically extends and reallocates
                     memory as required.

                     This helper can be used on a layer 3 _s_k_b to  push  a  MAC
                     header for redirection into a layer 2 device.

                     All  values  for _f_l_a_g_s are reserved for future usage, and
                     must be left at zero.

                     A call to this helper is susceptible to change the under‐
                     lying packet buffer. Therefore, at load time, all  checks
                     on  pointers  previously done by the verifier are invali‐
                     dated and must be performed again, if the helper is  used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__xxddpp__aaddjjuusstt__hheeaadd((ssttrruucctt xxddpp__bbuuffff **_x_d_p___m_d,, iinntt _d_e_l_t_a))

              DDeessccrriippttiioonn
                     Adjust  (move)  _x_d_p___m_d-->>ddaattaa by _d_e_l_t_a bytes. Note that it
                     is possible to use  a  negative  value  for  _d_e_l_t_a.  This
                     helper  can  be used to prepare the packet for pushing or
                     popping headers.

                     A call to this helper is susceptible to change the under‐
                     lying packet buffer. Therefore, at load time, all  checks
                     on  pointers  previously done by the verifier are invali‐
                     dated and must be performed again, if the helper is  used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__pprroobbee__rreeaadd__ssttrr((vvooiidd **_d_s_t,, uu3322 _s_i_z_e,, ccoonnsstt vvooiidd **_u_n_s_a_f_e___p_t_r))

              DDeessccrriippttiioonn
                     Copy  a  NUL  terminated string from an unsafe kernel ad‐
                     dress _u_n_s_a_f_e___p_t_r to _d_s_t. See  bbppff__pprroobbee__rreeaadd__kkeerrnneell__ssttrr()
                     for more details.

                     Generally,      use      bbppff__pprroobbee__rreeaadd__uusseerr__ssttrr()     or
                     bbppff__pprroobbee__rreeaadd__kkeerrnneell__ssttrr() instead.

              RReettuurrnn On success, the strictly positive length of  the  string,
                     including  the  trailing NUL character. On error, a nega‐
                     tive value.

       uu6644 bbppff__ggeett__ssoocckkeett__ccooookkiiee((ssttrruucctt sskk__bbuuffff **_s_k_b))

              DDeessccrriippttiioonn
                     If the ssttrruucctt sskk__bbuuffff pointed by _s_k_b has a known  socket,
                     retrieve  the  cookie  (generated  by the kernel) of this
                     socket.  If no cookie has been set yet,  generate  a  new
                     cookie.  Once generated, the socket cookie remains stable
                     for the life of the socket. This helper can be useful for
                     monitoring per socket networking traffic statistics as it
                     provides a global socket identifier that can  be  assumed
                     unique.

              RReettuurrnn A  8-byte  long  unique  number  on  success, or 0 if the
                     socket field is missing inside _s_k_b.

       uu6644 bbppff__ggeett__ssoocckkeett__ccooookkiiee((ssttrruucctt bbppff__ssoocckk__aaddddrr **_c_t_x))

              DDeessccrriippttiioonn
                     Equivalent to bpf_get_socket_cookie() helper that accepts
                     _s_k_b, but gets socket from ssttrruucctt bbppff__ssoocckk__aaddddrr context.

              RReettuurrnn A 8-byte long unique number.

       uu6644 bbppff__ggeett__ssoocckkeett__ccooookkiiee((ssttrruucctt bbppff__ssoocckk__ooppss **_c_t_x))

              DDeessccrriippttiioonn
                     Equivalent to bbppff__ggeett__ssoocckkeett__ccooookkiiee() helper that accepts
                     _s_k_b, but gets socket from ssttrruucctt bbppff__ssoocckk__ooppss context.

              RReettuurrnn A 8-byte long unique number.

       uu6644 bbppff__ggeett__ssoocckkeett__ccooookkiiee((ssttrruucctt ssoocckk **_s_k))

              DDeessccrriippttiioonn
                     Equivalent to bbppff__ggeett__ssoocckkeett__ccooookkiiee() helper that accepts
                     _s_k, but gets socket from a BTF ssttrruucctt ssoocckk.  This  helper
                     also works for sleepable programs.

              RReettuurrnn A 8-byte long unique number or 0 if _s_k is NULL.

       uu3322 bbppff__ggeett__ssoocckkeett__uuiidd((ssttrruucctt sskk__bbuuffff **_s_k_b))

              DDeessccrriippttiioonn
                     Get the owner UID of the socked associated to _s_k_b.

              RReettuurrnn The  owner  UID  of  the socket associated to _s_k_b. If the
                     socket is NNUULLLL, or if it is not a full socket (i.e. if it
                     is a time-wait or a request socket instead),  oovveerrfflloowwuuiidd
                     value  is  returned  (note that oovveerrfflloowwuuiidd might also be
                     the actual UID value for the socket).

       lloonngg bbppff__sseett__hhaasshh((ssttrruucctt sskk__bbuuffff **_s_k_b,, uu3322 _h_a_s_h))

              DDeessccrriippttiioonn
                     Set the full hash for _s_k_b (set the  field  _s_k_b-->>hhaasshh)  to
                     value _h_a_s_h.

              RReettuurrnn 0

       lloonngg bbppff__sseettssoocckkoopptt((vvooiidd **_b_p_f___s_o_c_k_e_t,, iinntt _l_e_v_e_l,, iinntt _o_p_t_n_a_m_e,, vvooiidd
       **_o_p_t_v_a_l,, iinntt _o_p_t_l_e_n))

              DDeessccrriippttiioonn
                     Emulate  a  call to sseettssoocckkoopptt(()) on the socket associated
                     to _b_p_f___s_o_c_k_e_t, which must be a full socket. The _l_e_v_e_l  at
                     which  the option resides and the name _o_p_t_n_a_m_e of the op‐
                     tion must be specified, see sseettssoocckkoopptt((22)) for more infor‐
                     mation.  The option value of length _o_p_t_l_e_n is pointed  by
                     _o_p_t_v_a_l.

                     _b_p_f___s_o_c_k_e_t should be one of the following:

                     • ssttrruucctt bbppff__ssoocckk__ooppss for BBPPFF__PPRROOGG__TTYYPPEE__SSOOCCKK__OOPPSS.

                     • ssttrruucctt   bbppff__ssoocckk__aaddddrr   for  BBPPFF__CCGGRROOUUPP__IINNEETT44__CCOONNNNEECCTT,
                       BBPPFF__CCGGRROOUUPP__IINNEETT66__CCOONNNNEECCTT and BBPPFF__CCGGRROOUUPP__UUNNIIXX__CCOONNNNEECCTT.

                     This helper actually implements a subset of sseettssoocckkoopptt(()).
                     It supports the following _l_e_v_e_ls:

                     • SSOOLL__SSOOCCKKEETT,  which  supports  the  following  _o_p_t_n_a_m_es:
                       SSOO__RRCCVVBBUUFF,  SSOO__SSNNDDBBUUFF, SSOO__MMAAXX__PPAACCIINNGG__RRAATTEE, SSOO__PPRRIIOORRIITTYY,
                       SSOO__RRCCVVLLOOWWAATT,  SSOO__MMAARRKK,  SSOO__BBIINNDDTTOODDEEVVIICCEE,  SSOO__KKEEEEPPAALLIIVVEE,
                       SSOO__RREEUUSSEEAADDDDRR,  SSOO__RREEUUSSEEPPOORRTT, SSOO__BBIINNDDTTOOIIFFIINNDDEEXX, SSOO__TTXXRREE‐‐
                       HHAASSHH.

                     • IIPPPPRROOTTOO__TTCCPP, which  supports  the  following  _o_p_t_n_a_m_es:
                       TTCCPP__CCOONNGGEESSTTIIOONN,    TTCCPP__BBPPFF__IIWW,   TTCCPP__BBPPFF__SSNNDDCCWWNNDD__CCLLAAMMPP,
                       TTCCPP__SSAAVVEE__SSYYNN, TTCCPP__KKEEEEPPIIDDLLEE, TTCCPP__KKEEEEPPIINNTTVVLL, TTCCPP__KKEEEEPPCCNNTT,
                       TTCCPP__SSYYNNCCNNTT,    TTCCPP__UUSSEERR__TTIIMMEEOOUUTT,     TTCCPP__NNOOTTSSEENNTT__LLOOWWAATT,
                       TTCCPP__NNOODDEELLAAYY,        TTCCPP__MMAAXXSSEEGG,       TTCCPP__WWIINNDDOOWW__CCLLAAMMPP,
                       TTCCPP__TTHHIINN__LLIINNEEAARR__TTIIMMEEOOUUTTSS,           TTCCPP__BBPPFF__DDEELLAACCKK__MMAAXX,
                       TTCCPP__BBPPFF__RRTTOO__MMIINN, TTCCPP__BBPPFF__SSOOCCKK__OOPPSS__CCBB__FFLLAAGGSS.

                     • IIPPPPRROOTTOO__IIPP, which supports _o_p_t_n_a_m_e IIPP__TTOOSS.

                     • IIPPPPRROOTTOO__IIPPVV66,  which  supports  the following _o_p_t_n_a_m_es:
                       IIPPVV66__TTCCLLAASSSS, IIPPVV66__AAUUTTOOFFLLOOWWLLAABBEELL.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__sskkbb__aaddjjuusstt__rroooomm((ssttrruucctt sskk__bbuuffff **_s_k_b,, ss3322 _l_e_n___d_i_f_f,, uu3322 _m_o_d_e,,
       uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Grow or shrink the room for data in the packet associated
                     to _s_k_b by _l_e_n___d_i_f_f, and according to the selected _m_o_d_e.

                     By default, the helper will reset any offloaded  checksum
                     indicator  of  the  skb  to  CHECKSUM_NONE.  This  can be
                     avoided by the following flag:

                     • BBPPFF__FF__AADDJJ__RROOOOMM__NNOO__CCSSUUMM__RREESSEETT: Do  not  reset  offloaded
                       checksum data of the skb to CHECKSUM_NONE.

                     There are two supported modes at this time:

                     • BBPPFF__AADDJJ__RROOOOMM__MMAACC:  Adjust  room  at the mac layer (room
                       space is added or removed between the layer 2 and layer
                       3 headers).

                     • BBPPFF__AADDJJ__RROOOOMM__NNEETT: Adjust  room  at  the  network  layer
                       (room space is added or removed between the layer 3 and
                       layer 4 headers).

                     The following flags are supported at this time:

                     • BBPPFF__FF__AADDJJ__RROOOOMM__FFIIXXEEDD__GGSSOO:  Do not adjust gso_size.  Ad‐
                       justing mss in this way is not allowed for datagrams.

                     • BBPPFF__FF__AADDJJ__RROOOOMM__EENNCCAAPP__LL33__IIPPVV44,        BBPPFF__FF__AADDJJ__RROOOOMM__EENN‐‐
                       CCAAPP__LL33__IIPPVV66: Any new space is reserved to hold a tunnel
                       header.  Configure skb offsets and other fields accord‐
                       ingly.

                     • BBPPFF__FF__AADDJJ__RROOOOMM__EENNCCAAPP__LL44__GGRREE,         BBPPFF__FF__AADDJJ__RROOOOMM__EENN‐‐
                       CCAAPP__LL44__UUDDPP: Use with ENCAP_L3 flags to further  specify
                       the tunnel type.

                     • BBPPFF__FF__AADDJJ__RROOOOMM__EENNCCAAPP__LL22(_l_e_n):   Use   with  ENCAP_L3/L4
                       flags to further specify the tunnel type;  _l_e_n  is  the
                       length of the inner MAC header.

                     • BBPPFF__FF__AADDJJ__RROOOOMM__EENNCCAAPP__LL22__EETTHH:          Use          with
                       BPF_F_ADJ_ROOM_ENCAP_L2 flag to further specify the  L2
                       type as Ethernet.

                     • BBPPFF__FF__AADDJJ__RROOOOMM__DDEECCAAPP__LL33__IIPPVV44,        BBPPFF__FF__AADDJJ__RROOOOMM__DDEE‐‐
                       CCAAPP__LL33__IIPPVV66: Indicate the new IP header  version  after
                       decapsulating  the outer IP header. Used when the inner
                       and outer IP versions are different.

                     A call to this helper is susceptible to change the under‐
                     lying packet buffer. Therefore, at load time, all  checks
                     on  pointers  previously done by the verifier are invali‐
                     dated and must be performed again, if the helper is  used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__rreeddiirreecctt__mmaapp((ssttrruucctt bbppff__mmaapp **_m_a_p,, uu6644 _k_e_y,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Redirect  the packet to the endpoint referenced by _m_a_p at
                     index _k_e_y. Depending on its type, this  _m_a_p  can  contain
                     references to net devices (for forwarding packets through
                     other  ports),  or to CPUs (for redirecting XDP frames to
                     another CPU; but this is only implemented for native  XDP
                     (with driver support) as of this writing).

                     The  lower  two bits of _f_l_a_g_s are used as the return code
                     if the map lookup fails. This is so that the return value
                     can be one of the XDP program return codes up to  XXDDPP__TTXX,
                     as  chosen by the caller. The higher bits of _f_l_a_g_s can be
                     set to BPF_F_BROADCAST or  BPF_F_EXCLUDE_INGRESS  as  de‐
                     fined below.

                     With  BPF_F_BROADCAST  the  packet will be broadcasted to
                     all the interfaces in the map, with BPF_F_EXCLUDE_INGRESS
                     the ingress interface will be excluded when do broadcast‐
                     ing.

                     See also bbppff__rreeddiirreecctt(), which only supports  redirecting
                     to an ifindex, but doesn't require a map to do so.

              RReettuurrnn XXDDPP__RREEDDIIRREECCTT  on  success,  or the value of the two lower
                     bits of the _f_l_a_g_s argument on error.

       lloonngg bbppff__sskk__rreeddiirreecctt__mmaapp((ssttrruucctt sskk__bbuuffff **_s_k_b,, ssttrruucctt bbppff__mmaapp **_m_a_p,, uu3322
       _k_e_y,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Redirect the packet to the socket referenced by  _m_a_p  (of
                     type BBPPFF__MMAAPP__TTYYPPEE__SSOOCCKKMMAAPP) at index _k_e_y. Both ingress and
                     egress  interfaces  can  be  used  for  redirection.  The
                     BBPPFF__FF__IINNGGRREESSSS value in _f_l_a_g_s is used to make the distinc‐
                     tion (ingress path is selected if the  flag  is  present,
                     egress  path  otherwise). This is the only flag supported
                     for now.

              RReettuurrnn SSKK__PPAASSSS on success, or SSKK__DDRROOPP on error.

       lloonngg bbppff__ssoocckk__mmaapp__uuppddaattee((ssttrruucctt bbppff__ssoocckk__ooppss **_s_k_o_p_s,, ssttrruucctt bbppff__mmaapp
       **_m_a_p,, vvooiidd **_k_e_y,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Add an entry to, or update a _m_a_p referencing sockets. The
                     _s_k_o_p_s is used as a new value for the entry associated  to
                     _k_e_y. _f_l_a_g_s is one of:

                     BBPPFF__NNOOEEXXIISSTT
                            The entry for _k_e_y must not exist in the map.

                     BBPPFF__EEXXIISSTT
                            The entry for _k_e_y must already exist in the map.

                     BBPPFF__AANNYY
                            No  condition  on  the  existence of the entry for
                            _k_e_y.

                     If the _m_a_p has eBPF programs (parser and verdict),  those
                     will  be  inherited  by  the  socket  being added. If the
                     socket is already attached to eBPF programs, this results
                     in an error.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__xxddpp__aaddjjuusstt__mmeettaa((ssttrruucctt xxddpp__bbuuffff **_x_d_p___m_d,, iinntt _d_e_l_t_a))

              DDeessccrriippttiioonn
                     Adjust the address pointed by _x_d_p___m_d-->>ddaattaa__mmeettaa by  _d_e_l_t_a
                     (which can be positive or negative). Note that this oper‐
                     ation modifies the address stored in _x_d_p___m_d-->>ddaattaa, so the
                     latter  must  be  loaded  only  after the helper has been
                     called.

                     The use of _x_d_p___m_d-->>ddaattaa__mmeettaa is optional and programs are
                     not required to use it. The rationale is  that  when  the
                     packet  is processed with XDP (e.g. as DoS filter), it is
                     possible to push further meta data along with  it  before
                     passing  to  the stack, and to give the guarantee that an
                     ingress eBPF program attached as a TC classifier  on  the
                     same device can pick this up for further post-processing.
                     Since  TC  works with socket buffers, it remains possible
                     to set from XDP the mmaarrkk or pprriioorriittyy pointers,  or  other
                     pointers  for  the  socket  buffer.   Having this scratch
                     space generic and programmable allows for more  flexibil‐
                     ity  as the user is free to store whatever meta data they
                     need.

                     A call to this helper is susceptible to change the under‐
                     lying packet buffer. Therefore, at load time, all  checks
                     on  pointers  previously done by the verifier are invali‐
                     dated and must be performed again, if the helper is  used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__ppeerrff__eevveenntt__rreeaadd__vvaalluuee((ssttrruucctt bbppff__mmaapp **_m_a_p,, uu6644 _f_l_a_g_s,, ssttrruucctt
       bbppff__ppeerrff__eevveenntt__vvaalluuee **_b_u_f,, uu3322 _b_u_f___s_i_z_e))

              DDeessccrriippttiioonn
                     Read the value of a perf event counter, and store it into
                     _b_u_f of size _b_u_f___s_i_z_e. This helper relies on a _m_a_p of type
                     BBPPFF__MMAAPP__TTYYPPEE__PPEERRFF__EEVVEENNTT__AARRRRAAYY.  The  nature  of  the perf
                     event counter is selected when _m_a_p is updated  with  perf
                     event file descriptors. The _m_a_p is an array whose size is
                     the  number  of  available CPUs, and each cell contains a
                     value relative to one CPU. The value to retrieve is indi‐
                     cated by _f_l_a_g_s, that contains the index  of  the  CPU  to
                     look  up,  masked  with  BBPPFF__FF__IINNDDEEXX__MMAASSKK. Alternatively,
                     _f_l_a_g_s can be set to BBPPFF__FF__CCUURRRREENNTT__CCPPUU  to  indicate  that
                     the value for the current CPU should be retrieved.

                     This    helper    behaves    in    a    way    close   to
                     bbppff__ppeerrff__eevveenntt__rreeaadd() helper, save that instead  of  just
                     returning the value observed, it fills the _b_u_f structure.
                     This  allows for additional data to be retrieved: in par‐
                     ticular, the enabled and running times  (in  _b_u_f-->>eennaabblleedd
                     and  _b_u_f-->>rruunnnniinngg,  respectively) are copied. In general,
                     bbppff__ppeerrff__eevveenntt__rreeaadd__vvaalluuee()    is    recommended     over
                     bbppff__ppeerrff__eevveenntt__rreeaadd(), which has some ABI issues and pro‐
                     vides fewer functionalities.

                     These  values are interesting, because hardware PMU (Per‐
                     formance Monitoring Unit) counters are limited resources.
                     When there are more PMU based  perf  events  opened  than
                     available counters, kernel will multiplex these events so
                     each  event  gets certain percentage (but not all) of the
                     PMU time. In case that multiplexing happens,  the  number
                     of  samples  or  counter  value will not reflect the case
                     compared to when no multiplexing occurs. This makes  com‐
                     parison between different runs difficult.  Typically, the
                     counter  value  should  be normalized before comparing to
                     other experiments. The usual  normalization  is  done  as
                     follows.

                        normalized_counter = counter * t_enabled / t_running

                     Where  t_enabled is the time enabled for event and t_run‐
                     ning is the time running for event since last  normaliza‐
                     tion. The enabled and running times are accumulated since
                     the  perf  event  open. To achieve scaling factor between
                     two invocations of an eBPF program, users can use CPU  id
                     as  the key (which is typical for perf array usage model)
                     to remember the previous value and do the calculation in‐
                     side the eBPF program.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__ppeerrff__pprroogg__rreeaadd__vvaalluuee((ssttrruucctt bbppff__ppeerrff__eevveenntt__ddaattaa **_c_t_x,, ssttrruucctt
       bbppff__ppeerrff__eevveenntt__vvaalluuee **_b_u_f,, uu3322 _b_u_f___s_i_z_e))

              DDeessccrriippttiioonn
                     For an eBPF program attached to a  perf  event,  retrieve
                     the  value  of  the  event  counter associated to _c_t_x and
                     store it in the structure pointed  by  _b_u_f  and  of  size
                     _b_u_f___s_i_z_e.  Enabled  and  running times are also stored in
                     the    structure    (see    description     of     helper
                     bbppff__ppeerrff__eevveenntt__rreeaadd__vvaalluuee() for more details).

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__ggeettssoocckkoopptt((vvooiidd **_b_p_f___s_o_c_k_e_t,, iinntt _l_e_v_e_l,, iinntt _o_p_t_n_a_m_e,, vvooiidd
       **_o_p_t_v_a_l,, iinntt _o_p_t_l_e_n))

              DDeessccrriippttiioonn
                     Emulate  a  call to ggeettssoocckkoopptt(()) on the socket associated
                     to _b_p_f___s_o_c_k_e_t, which must be a full socket. The _l_e_v_e_l  at
                     which  the option resides and the name _o_p_t_n_a_m_e of the op‐
                     tion must be specified, see ggeettssoocckkoopptt((22)) for more infor‐
                     mation.  The retrieved value is stored in  the  structure
                     pointed by _o_p_v_a_l and of length _o_p_t_l_e_n.

                     _b_p_f___s_o_c_k_e_t should be one of the following:

                     • ssttrruucctt bbppff__ssoocckk__ooppss for BBPPFF__PPRROOGG__TTYYPPEE__SSOOCCKK__OOPPSS.

                     • ssttrruucctt   bbppff__ssoocckk__aaddddrr   for  BBPPFF__CCGGRROOUUPP__IINNEETT44__CCOONNNNEECCTT,
                       BBPPFF__CCGGRROOUUPP__IINNEETT66__CCOONNNNEECCTT and BBPPFF__CCGGRROOUUPP__UUNNIIXX__CCOONNNNEECCTT.

                     This helper actually implements a subset of ggeettssoocckkoopptt(()).
                     It supports the same set of _o_p_t_n_a_m_es that is supported by
                     the  bbppff__sseettssoocckkoopptt()   helper.    The   exceptions   are
                     TTCCPP__BBPPFF__**  is  bbppff__sseettssoocckkoopptt() only and TTCCPP__SSAAVVEEDD__SSYYNN is
                     bbppff__ggeettssoocckkoopptt() only.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__oovveerrrriiddee__rreettuurrnn((ssttrruucctt pptt__rreeggss **_r_e_g_s,, uu6644 _r_c))

              DDeessccrriippttiioonn
                     Used for error injection, this  helper  uses  kprobes  to
                     override  the return value of the probed function, and to
                     set it to _r_c.  The first argument is the context _r_e_g_s  on
                     which the kprobe works.

                     This  helper works by setting the PC (program counter) to
                     an override function which is run in place of the  origi‐
                     nal  probed  function.  This means the probed function is
                     not run at all. The  replacement  function  just  returns
                     with the required value.

                     This  helper  has security implications, and thus is sub‐
                     ject to restrictions. It is only available if the  kernel
                     was compiled with the CCOONNFFIIGG__BBPPFF__KKPPRROOBBEE__OOVVEERRRRIIDDEE configu‐
                     ration  option,  and  in this case it only works on func‐
                     tions tagged with  AALLLLOOWW__EERRRROORR__IINNJJEECCTTIIOONN  in  the  kernel
                     code.

              RReettuurrnn 0

       lloonngg bbppff__ssoocckk__ooppss__ccbb__ffllaaggss__sseett((ssttrruucctt bbppff__ssoocckk__ooppss **_b_p_f___s_o_c_k,, iinntt
       _a_r_g_v_a_l))

              DDeessccrriippttiioonn
                     Attempt  to  set  the  value of the bbppff__ssoocckk__ooppss__ccbb__ffllaaggss
                     field for the full TCP socket associated to  _b_p_f___s_o_c_k___o_p_s
                     to _a_r_g_v_a_l.

                     The  primary  use  of this field is to determine if there
                     should   be   calls   to   eBPF    programs    of    type
                     BBPPFF__PPRROOGG__TTYYPPEE__SSOOCCKK__OOPPSS at various points in the TCP code.
                     A program of the same type can change its value, per con‐
                     nection  and  as necessary, when the connection is estab‐
                     lished. This field is directly  accessible  for  reading,
                     but  this helper must be used for updates in order to re‐
                     turn an error if an eBPF program tries to set a  callback
                     that is not supported in the current kernel.

                     _a_r_g_v_a_l is a flag array which can combine these flags:

                     • BBPPFF__SSOOCCKK__OOPPSS__RRTTOO__CCBB__FFLLAAGG (retransmission time out)

                     • BBPPFF__SSOOCCKK__OOPPSS__RREETTRRAANNSS__CCBB__FFLLAAGG (retransmission)

                     • BBPPFF__SSOOCCKK__OOPPSS__SSTTAATTEE__CCBB__FFLLAAGG (TCP state change)

                     • BBPPFF__SSOOCCKK__OOPPSS__RRTTTT__CCBB__FFLLAAGG (every RTT)

                     Therefore,  this function can be used to clear a callback
                     flag by setting the appropriate bit to zero. e.g. to dis‐
                     able the RTO callback:

                     bbppff__ssoocckk__ooppss__ccbb__ffllaaggss__sseett((bbppff__ssoocckk,,
                            bbppff__ssoocckk-->>bbppff__ssoocckk__ooppss__ccbb__ffllaaggss                  &&
                            ˜˜BBPPFF__SSOOCCKK__OOPPSS__RRTTOO__CCBB__FFLLAAGG))

                     Here  are some examples of where one could call such eBPF
                     program:

                     • When RTO fires.

                     • When a packet is retransmitted.

                     • When the connection terminates.

                     • When a packet is sent.

                     • When a packet is received.

              RReettuurrnn Code --EEIINNVVAALL if the socket is not a full TCP socket; oth‐
                     erwise, a positive number containing the bits that  could
                     not be set is returned (which comes down to 0 if all bits
                     were set as required).

       lloonngg bbppff__mmssgg__rreeddiirreecctt__mmaapp((ssttrruucctt sskk__mmssgg__bbuuffff **_m_s_g,, ssttrruucctt bbppff__mmaapp **_m_a_p,,
       uu3322 _k_e_y,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     This  helper is used in programs implementing policies at
                     the socket level. If the message _m_s_g is allowed  to  pass
                     (i.e. if the verdict eBPF program returns SSKK__PPAASSSS), redi‐
                     rect  it  to  the  socket  referenced  by  _m_a_p  (of  type
                     BBPPFF__MMAAPP__TTYYPPEE__SSOOCCKKMMAAPP) at  index  _k_e_y.  Both  ingress  and
                     egress  interfaces  can  be  used  for  redirection.  The
                     BBPPFF__FF__IINNGGRREESSSS value in _f_l_a_g_s is used to make the distinc‐
                     tion (ingress path is selected if the  flag  is  present,
                     egress  path  otherwise). This is the only flag supported
                     for now.

              RReettuurrnn SSKK__PPAASSSS on success, or SSKK__DDRROOPP on error.

       lloonngg bbppff__mmssgg__aappppllyy__bbyytteess((ssttrruucctt sskk__mmssgg__bbuuffff **_m_s_g,, uu3322 _b_y_t_e_s))

              DDeessccrriippttiioonn
                     For socket policies, apply the verdict of the  eBPF  pro‐
                     gram to the next _b_y_t_e_s (number of bytes) of message _m_s_g.

                     For  example,  this  helper  can be used in the following
                     cases:

                     • A single sseennddmmssgg() or sseennddffiillee() system  call  contains
                       multiple logical messages that the eBPF program is sup‐
                       posed to read and for which it should apply a verdict.

                     • An eBPF program only cares to read the first _b_y_t_e_s of a
                       _m_s_g.  If  the message has a large payload, then setting
                       up and calling the  eBPF  program  repeatedly  for  all
                       bytes,  even though the verdict is already known, would
                       create unnecessary overhead.

                     When called from within an eBPF program, the helper  sets
                     a  counter  internal  to  the BPF infrastructure, that is
                     used to apply the last verdict  to  the  next  _b_y_t_e_s.  If
                     _b_y_t_e_s  is  smaller  than the current data being processed
                     from a sseennddmmssgg() or sseennddffiillee()  system  call,  the  first
                     _b_y_t_e_s  will  be  sent and the eBPF program will be re-run
                     with the pointer for start of data pointing to byte  num‐
                     ber  _b_y_t_e_s  ++ 11. If _b_y_t_e_s is larger than the current data
                     being processed, then the eBPF verdict will be applied to
                     multiple sseennddmmssgg() or sseennddffiillee() calls  until  _b_y_t_e_s  are
                     consumed.

                     Note  that  if  a socket closes with the internal counter
                     holding a non-zero value, this is not a  problem  because
                     data is not being buffered for _b_y_t_e_s and is sent as it is
                     received.

              RReettuurrnn 0

       lloonngg bbppff__mmssgg__ccoorrkk__bbyytteess((ssttrruucctt sskk__mmssgg__bbuuffff **_m_s_g,, uu3322 _b_y_t_e_s))

              DDeessccrriippttiioonn
                     For socket policies, prevent the execution of the verdict
                     eBPF  program  for  message _m_s_g until _b_y_t_e_s (byte number)
                     have been accumulated.

                     This can be used when one  needs  a  specific  number  of
                     bytes  before a verdict can be assigned, even if the data
                     spans multiple sseennddmmssgg() or sseennddffiillee() calls. The extreme
                     case would be a user calling  sseennddmmssgg()  repeatedly  with
                     1-byte  long message segments. Obviously, this is bad for
                     performance, but it is still valid. If the  eBPF  program
                     needs  _b_y_t_e_s  bytes to validate a header, this helper can
                     be used to prevent the eBPF program to  be  called  again
                     until _b_y_t_e_s have been accumulated.

              RReettuurrnn 0

       lloonngg bbppff__mmssgg__ppuullll__ddaattaa((ssttrruucctt sskk__mmssgg__bbuuffff **_m_s_g,, uu3322 _s_t_a_r_t,, uu3322 _e_n_d,, uu6644
       _f_l_a_g_s))

              DDeessccrriippttiioonn
                     For  socket  policies,  pull in non-linear data from user
                     space  for   _m_s_g   and   set   pointers   _m_s_g-->>ddaattaa   and
                     _m_s_g-->>ddaattaa__eenndd  to  _s_t_a_r_t  and _e_n_d bytes offsets into _m_s_g,
                     respectively.

                     If a program of type BBPPFF__PPRROOGG__TTYYPPEE__SSKK__MMSSGG is run on a _m_s_g
                     it can only parse data that the (ddaattaa, ddaattaa__eenndd) pointers
                     have already consumed. For sseennddmmssgg() hooks this is likely
                     the first scatterlist element. But for calls  relying  on
                     the  sseennddppaaggee  handler (e.g. sseennddffiillee()) this will be the
                     range (00, 00) because the data is shared with  user  space
                     and  by  default  the objective is to avoid allowing user
                     space to modify data while (or after) eBPF verdict is be‐
                     ing decided. This helper can be used to pull in data  and
                     to  set  the  start and end pointer to given values. Data
                     will be copied if necessary (i.e. if data was not  linear
                     and  if  start  and end pointers do not point to the same
                     chunk).

                     A call to this helper is susceptible to change the under‐
                     lying packet buffer. Therefore, at load time, all  checks
                     on  pointers  previously done by the verifier are invali‐
                     dated and must be performed again, if the helper is  used
                     in combination with direct packet access.

                     All  values  for _f_l_a_g_s are reserved for future usage, and
                     must be left at zero.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__bbiinndd((ssttrruucctt bbppff__ssoocckk__aaddddrr **_c_t_x,, ssttrruucctt ssoocckkaaddddrr **_a_d_d_r,, iinntt
       _a_d_d_r___l_e_n))

              DDeessccrriippttiioonn
                     Bind the socket associated to _c_t_x to the address  pointed
                     by  _a_d_d_r, of length _a_d_d_r___l_e_n. This allows for making out‐
                     going connection from the desired IP address,  which  can
                     be  useful for example when all processes inside a cgroup
                     should use one single IP address on a host that has  mul‐
                     tiple IP configured.

                     This helper works for IPv4 and IPv6, TCP and UDP sockets.
                     The   domain   (_a_d_d_r-->>ssaa__ffaammiillyy)   must  be  AAFF__IINNEETT  (or
                     AAFF__IINNEETT66). It's advised to pass zero  port  (ssiinn__ppoorrtt  or
                     ssiinn66__ppoorrtt)  which  triggers  IP_BIND_ADDRESS_NO_PORT-like
                     behavior and lets the kernel efficiently pick up  an  un‐
                     used  port as long as 4-tuple is unique. Passing non-zero
                     port might lead to degraded performance.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__xxddpp__aaddjjuusstt__ttaaiill((ssttrruucctt xxddpp__bbuuffff **_x_d_p___m_d,, iinntt _d_e_l_t_a))

              DDeessccrriippttiioonn
                     Adjust (move) _x_d_p___m_d-->>ddaattaa__eenndd by _d_e_l_t_a bytes. It is pos‐
                     sible to both shrink and grow the  packet  tail.   Shrink
                     done via _d_e_l_t_a being a negative integer.

                     A call to this helper is susceptible to change the under‐
                     lying  packet buffer. Therefore, at load time, all checks
                     on pointers previously done by the verifier  are  invali‐
                     dated  and must be performed again, if the helper is used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__sskkbb__ggeett__xxffrrmm__ssttaattee((ssttrruucctt sskk__bbuuffff **_s_k_b,, uu3322 _i_n_d_e_x,, ssttrruucctt
       bbppff__xxffrrmm__ssttaattee **_x_f_r_m___s_t_a_t_e,, uu3322 _s_i_z_e,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Retrieve the XFRM state (IP transform framework, see also
                     iipp--xxffrrmm((88))) at _i_n_d_e_x in XFRM "security path" for _s_k_b.

                     The   retrieved   value   is   stored   in   the   ssttrruucctt
                     bbppff__xxffrrmm__ssttaattee pointed by _x_f_r_m___s_t_a_t_e and of length _s_i_z_e.

                     All  values  for _f_l_a_g_s are reserved for future usage, and
                     must be left at zero.

                     This helper is available only if the kernel was  compiled
                     with CCOONNFFIIGG__XXFFRRMM configuration option.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__ggeett__ssttaacckk((vvooiidd **_c_t_x,, vvooiidd **_b_u_f,, uu3322 _s_i_z_e,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Return  a  user or a kernel stack in bpf program provided
                     buffer.  To achieve this, the helper needs _c_t_x, which  is
                     a  pointer to the context on which the tracing program is
                     executed.  To store the stacktrace, the bpf program  pro‐
                     vides _b_u_f with a nonnegative _s_i_z_e.

                     The  last  argument,  _f_l_a_g_s,  holds  the  number of stack
                     frames  to  skip   (from   0   to   255),   masked   with
                     BBPPFF__FF__SSKKIIPP__FFIIEELLDD__MMAASSKK.  The  next bits can be used to set
                     the following flags:

                     BBPPFF__FF__UUSSEERR__SSTTAACCKK
                            Collect a user space stack  instead  of  a  kernel
                            stack.

                     BBPPFF__FF__UUSSEERR__BBUUIILLDD__IIDD
                            Collect (build_id, file_offset) instead of ips for
                            user stack, only valid if BBPPFF__FF__UUSSEERR__SSTTAACCKK is also
                            specified.

                            _f_i_l_e___o_f_f_s_e_t is an offset relative to the beginning
                            of  the  executable  or shared object file backing
                            the vma which the _i_p falls in. It is _n_o_t an offset
                            relative to that object's  base  address.  Accord‐
                            ingly,  it  must  be adjusted by adding (sh_addr -
                            sh_offset), where sh_{addr,offset}  correspond  to
                            the  executable  section containing _f_i_l_e___o_f_f_s_e_t in
                            the object, for comparisons to  symbols'  st_value
                            to be valid.

                     bbppff__ggeett__ssttaacckk()  can  collect  up to PPEERRFF__MMAAXX__SSTTAACCKK__DDEEPPTTHH
                     both kernel and user frames, subject to sufficient  large
                     buffer  size. Note that this limit can be controlled with
                     the ssyyssccttll program, and that it should  be  manually  in‐
                     creased  in  order  to  profile long user stacks (such as
                     stacks for Java programs). To do so, use:

                        # sysctl kernel.perf_event_max_stack=<new value>

              RReettuurrnn The non-negative copied _b_u_f length equal to or less  than
                     _s_i_z_e on success, or a negative error in case of failure.

       lloonngg bbppff__sskkbb__llooaadd__bbyytteess__rreellaattiivvee((ccoonnsstt vvooiidd **_s_k_b,, uu3322 _o_f_f_s_e_t,, vvooiidd **_t_o,,
       uu3322 _l_e_n,, uu3322 _s_t_a_r_t___h_e_a_d_e_r))

              DDeessccrriippttiioonn
                     This helper is similar to bbppff__sskkbb__llooaadd__bbyytteess() in that it
                     provides  an  easy way to load _l_e_n bytes from _o_f_f_s_e_t from
                     the packet associated to _s_k_b, into the buffer pointed  by
                     _t_o.  The  difference  to  bbppff__sskkbb__llooaadd__bbyytteess()  is that a
                     fifth argument _s_t_a_r_t___h_e_a_d_e_r exists in order to  select  a
                     base offset to start from. _s_t_a_r_t___h_e_a_d_e_r can be one of:

                     BBPPFF__HHDDRR__SSTTAARRTT__MMAACC
                            Base offset to load data from is _s_k_b's mac header.

                     BBPPFF__HHDDRR__SSTTAARRTT__NNEETT
                            Base  offset  to  load  data from is _s_k_b's network
                            header.

                     In general,  "direct  packet  access"  is  the  preferred
                     method  to access packet data, however, this helper is in
                     particular useful in socket filters where _s_k_b-->>ddaattaa  does
                     not always point to the start of the mac header and where
                     "direct packet access" is not available.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__ffiibb__llooookkuupp((vvooiidd **_c_t_x,, ssttrruucctt bbppff__ffiibb__llooookkuupp **_p_a_r_a_m_s,, iinntt _p_l_e_n,,
       uu3322 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Do  FIB  lookup  in  kernel  tables  using  parameters in
                     _p_a_r_a_m_s.  If lookup is successful and result shows  packet
                     is  to be forwarded, the neighbor tables are searched for
                     the nexthop.  If successful (ie., FIB lookup  shows  for‐
                     warding  and nexthop is resolved), the nexthop address is
                     returned in ipv4_dst or ipv6_dst based on family, smac is
                     set to mac address of egress device, dmac is set to  nex‐
                     thop  mac  address, rt_metric is set to metric from route
                     (IPv4/IPv6 only), and ifindex is set to the device  index
                     of the nexthop from the FIB lookup.

                     _p_l_e_n argument is the size of the passed in struct.  _f_l_a_g_s
                     argument  can be a combination of one or more of the fol‐
                     lowing values:

                     BBPPFF__FFIIBB__LLOOOOKKUUPP__DDIIRREECCTT
                            Do a direct table lookup vs full lookup using  FIB
                            rules.

                     BBPPFF__FFIIBB__LLOOOOKKUUPP__TTBBIIDD
                            Used  with BPF_FIB_LOOKUP_DIRECT.  Use the routing
                            table ID  present  in  _p_a_r_a_m_s->tbid  for  the  fib
                            lookup.

                     BBPPFF__FFIIBB__LLOOOOKKUUPP__OOUUTTPPUUTT
                            Perform lookup from an egress perspective (default
                            is ingress).

                     BBPPFF__FFIIBB__LLOOOOKKUUPP__SSKKIIPP__NNEEIIGGHH
                            Skip  the neighbour table lookup. _p_a_r_a_m_s->dmac and
                            _p_a_r_a_m_s->smac will not be set as output.  A  common
                            use case is to call bbppff__rreeddiirreecctt__nneeiigghh() after do‐
                            ing bbppff__ffiibb__llooookkuupp().

                     BBPPFF__FFIIBB__LLOOOOKKUUPP__SSRRCC
                            Derive    and    set    source    IP    addr    in
                            _p_a_r_a_m_s->ipv{4,6}_src for the nexthop. If  the  src
                            addr          cannot          be          derived,
                            BBPPFF__FFIIBB__LLKKUUPP__RREETT__NNOO__SSRRCC__AADDDDRR is returned. In  this
                            case,  _p_a_r_a_m_s->dmac  and  _p_a_r_a_m_s->smac are not set
                            either.

                     BBPPFF__FFIIBB__LLOOOOKKUUPP__MMAARRKK
                            Use the mark present in _p_a_r_a_m_s->mark for  the  fib
                            lookup.   This  option  should  not  be  used with
                            BPF_FIB_LOOKUP_DIRECT, as it only has meaning  for
                            full lookups.

                     _c_t_x  is  either  ssttrruucctt xxddpp__mmdd for XDP programs or ssttrruucctt
                     sskk__bbuuffff tc cls_act programs.

              RReettuurrnn

                     • < 0 if any input argument is invalid

                     • 0 on success (packet is forwarded, nexthop neighbor ex‐
                       ists)

                     • > 0 one of BBPPFF__FFIIBB__LLKKUUPP__RREETT__ codes explaining  why  the
                       packet is not forwarded or needs assist from full stack

                     If  lookup  fails with BPF_FIB_LKUP_RET_FRAG_NEEDED, then
                     the MTU was exceeded and output  params->mtu_result  con‐
                     tains the MTU.

       lloonngg bbppff__ssoocckk__hhaasshh__uuppddaattee((ssttrruucctt bbppff__ssoocckk__ooppss **_s_k_o_p_s,, ssttrruucctt bbppff__mmaapp
       **_m_a_p,, vvooiidd **_k_e_y,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Add  an  entry  to,  or update a sockhash _m_a_p referencing
                     sockets.  The _s_k_o_p_s is used as a new value for the  entry
                     associated to _k_e_y. _f_l_a_g_s is one of:

                     BBPPFF__NNOOEEXXIISSTT
                            The entry for _k_e_y must not exist in the map.

                     BBPPFF__EEXXIISSTT
                            The entry for _k_e_y must already exist in the map.

                     BBPPFF__AANNYY
                            No  condition  on  the  existence of the entry for
                            _k_e_y.

                     If the _m_a_p has eBPF programs (parser and verdict),  those
                     will  be  inherited  by  the  socket  being added. If the
                     socket is already attached to eBPF programs, this results
                     in an error.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__mmssgg__rreeddiirreecctt__hhaasshh((ssttrruucctt sskk__mmssgg__bbuuffff **_m_s_g,, ssttrruucctt bbppff__mmaapp
       **_m_a_p,, vvooiidd **_k_e_y,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     This helper is used in programs implementing policies  at
                     the  socket  level. If the message _m_s_g is allowed to pass
                     (i.e. if the verdict eBPF program returns SSKK__PPAASSSS), redi‐
                     rect  it  to  the  socket  referenced  by  _m_a_p  (of  type
                     BBPPFF__MMAAPP__TTYYPPEE__SSOOCCKKHHAASSHH)  using  hash _k_e_y. Both ingress and
                     egress  interfaces  can  be  used  for  redirection.  The
                     BBPPFF__FF__IINNGGRREESSSS value in _f_l_a_g_s is used to make the distinc‐
                     tion  (ingress  path  is selected if the flag is present,
                     egress path otherwise). This is the only  flag  supported
                     for now.

              RReettuurrnn SSKK__PPAASSSS on success, or SSKK__DDRROOPP on error.

       lloonngg bbppff__sskk__rreeddiirreecctt__hhaasshh((ssttrruucctt sskk__bbuuffff **_s_k_b,, ssttrruucctt bbppff__mmaapp **_m_a_p,,
       vvooiidd **_k_e_y,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     This  helper is used in programs implementing policies at
                     the skb socket level. If the sk_buff _s_k_b  is  allowed  to
                     pass (i.e.  if the verdict eBPF program returns SSKK__PPAASSSS),
                     redirect  it  to  the  socket  referenced by _m_a_p (of type
                     BBPPFF__MMAAPP__TTYYPPEE__SSOOCCKKHHAASSHH) using hash _k_e_y. Both  ingress  and
                     egress  interfaces  can  be  used  for  redirection.  The
                     BBPPFF__FF__IINNGGRREESSSS value in _f_l_a_g_s is used to make the distinc‐
                     tion (ingress path is selected if the  flag  is  present,
                     egress  otherwise).  This  is the only flag supported for
                     now.

              RReettuurrnn SSKK__PPAASSSS on success, or SSKK__DDRROOPP on error.

       lloonngg bbppff__llwwtt__ppuusshh__eennccaapp((ssttrruucctt sskk__bbuuffff **_s_k_b,, uu3322 _t_y_p_e,, vvooiidd **_h_d_r,, uu3322
       _l_e_n))

              DDeessccrriippttiioonn
                     Encapsulate the packet associated to _s_k_b within a Layer 3
                     protocol header. This header is provided in the buffer at
                     address _h_d_r, with _l_e_n its size in bytes.  _t_y_p_e  indicates
                     the protocol of the header and can be one of:

                     BBPPFF__LLWWTT__EENNCCAAPP__SSEEGG66
                            IPv6  encapsulation  with  Segment  Routing Header
                            (ssttrruucctt iippvv66__ssrr__hhddrr). _h_d_r only contains  the  SRH,
                            the IPv6 header is computed by the kernel.

                     BBPPFF__LLWWTT__EENNCCAAPP__SSEEGG66__IINNLLIINNEE
                            Only  works if _s_k_b contains an IPv6 packet. Insert
                            a Segment Routing Header (ssttrruucctt iippvv66__ssrr__hhddrr)  in‐
                            side the IPv6 header.

                     BBPPFF__LLWWTT__EENNCCAAPP__IIPP
                            IP  encapsulation  (GRE/GUE/IPIP/etc).  The  outer
                            header must be IPv4 or IPv6, followed by  zero  or
                            more  additional  headers, up to LLWWTT__BBPPFF__MMAAXX__HHEEAADD‐‐
                            RROOOOMM total bytes in all prepended headers.  Please
                            note that if sskkbb__iiss__ggssoo(_s_k_b) is true, no more than
                            two  headers  can  be  prepended,  and  the  inner
                            header,  if  present,  should  be  either  GRE  or
                            UDP/GUE.

                     BBPPFF__LLWWTT__EENNCCAAPP__SSEEGG66*  types  can be called by BPF programs
                     of type BBPPFF__PPRROOGG__TTYYPPEE__LLWWTT__IINN; BBPPFF__LLWWTT__EENNCCAAPP__IIPP  type  can
                     be  called  by bpf programs of types BBPPFF__PPRROOGG__TTYYPPEE__LLWWTT__IINN
                     and BBPPFF__PPRROOGG__TTYYPPEE__LLWWTT__XXMMIITT.

                     A call to this helper is susceptible to change the under‐
                     lying packet buffer. Therefore, at load time, all  checks
                     on  pointers  previously done by the verifier are invali‐
                     dated and must be performed again, if the helper is  used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__llwwtt__sseegg66__ssttoorree__bbyytteess((ssttrruucctt sskk__bbuuffff **_s_k_b,, uu3322 _o_f_f_s_e_t,, ccoonnsstt
       vvooiidd **_f_r_o_m,, uu3322 _l_e_n))

              DDeessccrriippttiioonn
                     Store _l_e_n bytes from address _f_r_o_m into the packet associ‐
                     ated  to _s_k_b, at _o_f_f_s_e_t. Only the flags, tag and TLVs in‐
                     side the outermost IPv6 Segment  Routing  Header  can  be
                     modified through this helper.

                     A call to this helper is susceptible to change the under‐
                     lying  packet buffer. Therefore, at load time, all checks
                     on pointers previously done by the verifier  are  invali‐
                     dated  and must be performed again, if the helper is used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__llwwtt__sseegg66__aaddjjuusstt__ssrrhh((ssttrruucctt sskk__bbuuffff **_s_k_b,, uu3322 _o_f_f_s_e_t,, ss3322
       _d_e_l_t_a))

              DDeessccrriippttiioonn
                     Adjust the size allocated to TLVs in the  outermost  IPv6
                     Segment Routing Header contained in the packet associated
                     to  _s_k_b,  at position _o_f_f_s_e_t by _d_e_l_t_a bytes. Only offsets
                     after the segments are accepted. _d_e_l_t_a  can  be  as  well
                     positive (growing) as negative (shrinking).

                     A call to this helper is susceptible to change the under‐
                     lying  packet buffer. Therefore, at load time, all checks
                     on pointers previously done by the verifier  are  invali‐
                     dated  and must be performed again, if the helper is used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__llwwtt__sseegg66__aaccttiioonn((ssttrruucctt sskk__bbuuffff **_s_k_b,, uu3322 _a_c_t_i_o_n,, vvooiidd **_p_a_r_a_m,,
       uu3322 _p_a_r_a_m___l_e_n))

              DDeessccrriippttiioonn
                     Apply an IPv6 Segment Routing action of  type  _a_c_t_i_o_n  to
                     the packet associated to _s_k_b. Each action takes a parame‐
                     ter  contained  at address _p_a_r_a_m, and of length _p_a_r_a_m___l_e_n
                     bytes.  _a_c_t_i_o_n can be one of:

                     SSEEGG66__LLOOCCAALL__AACCTTIIOONN__EENNDD__XX
                            End.X action: Endpoint with Layer-3 cross-connect.
                            Type of _p_a_r_a_m: ssttrruucctt iinn66__aaddddrr.

                     SSEEGG66__LLOOCCAALL__AACCTTIIOONN__EENNDD__TT
                            End.T action: Endpoint with  specific  IPv6  table
                            lookup.  Type of _p_a_r_a_m: iinntt.

                     SSEEGG66__LLOOCCAALL__AACCTTIIOONN__EENNDD__BB66
                            End.B6  action:  Endpoint bound to an SRv6 policy.
                            Type of _p_a_r_a_m: ssttrruucctt iippvv66__ssrr__hhddrr.

                     SSEEGG66__LLOOCCAALL__AACCTTIIOONN__EENNDD__BB66__EENNCCAAPP
                            End.B6.Encap action: Endpoint bound to an SRv6 en‐
                            capsulation  policy.   Type   of   _p_a_r_a_m:   ssttrruucctt
                            iippvv66__ssrr__hhddrr.

                     A call to this helper is susceptible to change the under‐
                     lying  packet buffer. Therefore, at load time, all checks
                     on pointers previously done by the verifier  are  invali‐
                     dated  and must be performed again, if the helper is used
                     in combination with direct packet access.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__rrcc__rreeppeeaatt((vvooiidd **_c_t_x))

              DDeessccrriippttiioonn
                     This helper is used in programs implementing IR decoding,
                     to report a successfully decoded repeat key message. This
                     delays the generation of a key up  event  for  previously
                     generated key down event.

                     Some  IR protocols like NEC have a special IR message for
                     repeating last button, for when a button is held down.

                     The _c_t_x should point to the lirc sample  as  passed  into
                     the program.

                     This  helper is only available is the kernel was compiled
                     with the CCOONNFFIIGG__BBPPFF__LLIIRRCC__MMOODDEE22 configuration  option  set
                     to "yy".

              RReettuurrnn 0

       lloonngg bbppff__rrcc__kkeeyyddoowwnn((vvooiidd **_c_t_x,, uu3322 _p_r_o_t_o_c_o_l,, uu6644 _s_c_a_n_c_o_d_e,, uu3322 _t_o_g_g_l_e))

              DDeessccrriippttiioonn
                     This helper is used in programs implementing IR decoding,
                     to report a successfully decoded key press with _s_c_a_n_c_o_d_e,
                     _t_o_g_g_l_e  value in the given _p_r_o_t_o_c_o_l. The scancode will be
                     translated to a keycode using the rc keymap, and reported
                     as an input key down event. After a period a key up event
                     is generated. This period can be extended by calling  ei‐
                     ther  bbppff__rrcc__kkeeyyddoowwnn()  again  with  the  same values, or
                     calling bbppff__rrcc__rreeppeeaatt().

                     Some protocols include a toggle bit, in case  the  button
                     was  released and pressed again between consecutive scan‐
                     codes.

                     The _c_t_x should point to the lirc sample  as  passed  into
                     the program.

                     The  _p_r_o_t_o_c_o_l  is  the  decoded protocol number (see eennuumm
                     rrcc__pprroottoo for some predefined values).

                     This helper is only available is the kernel was  compiled
                     with  the  CCOONNFFIIGG__BBPPFF__LLIIRRCC__MMOODDEE22 configuration option set
                     to "yy".

              RReettuurrnn 0

       uu6644 bbppff__sskkbb__ccggrroouupp__iidd((ssttrruucctt sskk__bbuuffff **_s_k_b))

              DDeessccrriippttiioonn
                     Return the cgroup v2 id of the socket associated with the
                     _s_k_b.  This is roughly similar to the bbppff__ggeett__ccggrroouupp__ccllaass‐‐
                     ssiidd() helper for cgroup v1 by providing a tag resp. iden‐
                     tifier that can be matched on or  used  for  map  lookups
                     e.g.  to  implement  policy.  The cgroup v2 id of a given
                     path in the hierarchy is exposed in  user  space  through
                     the f_handle API in order to get to the same 64-bit id.

                     This  helper  can  be  used on TC egress path, but not on
                     ingress, and is available only if the kernel was compiled
                     with the CCOONNFFIIGG__SSOOCCKK__CCGGRROOUUPP__DDAATTAA configuration option.

              RReettuurrnn The id is returned or 0 in case the id could not  be  re‐
                     trieved.

       uu6644 bbppff__ggeett__ccuurrrreenntt__ccggrroouupp__iidd((vvooiidd))

              DDeessccrriippttiioonn
                     Get  the  current  cgroup  id  based on the cgroup within
                     which the current task is running.

              RReettuurrnn A 64-bit integer containing the current cgroup  id  based
                     on the cgroup within which the current task is running.

       vvooiidd **bbppff__ggeett__llooccaall__ssttoorraaggee((vvooiidd **_m_a_p,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Get  the pointer to the local storage area.  The type and
                     the size of the local storage is defined by the _m_a_p argu‐
                     ment.  The _f_l_a_g_s meaning is specific for each  map  type,
                     and has to be 0 for cgroup local storage.

                     Depending  on  the BPF program type, a local storage area
                     can be shared between multiple instances of the BPF  pro‐
                     gram, running simultaneously.

                     A  user should care about the synchronization by himself.
                     For example, by using the BBPPFF__AATTOOMMIICC instructions to  al‐
                     ter the shared data.

              RReettuurrnn A pointer to the local storage area.

       lloonngg bbppff__sskk__sseelleecctt__rreeuusseeppoorrtt((ssttrruucctt sskk__rreeuusseeppoorrtt__mmdd **_r_e_u_s_e,, ssttrruucctt
       bbppff__mmaapp **_m_a_p,, vvooiidd **_k_e_y,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Select  a  SSOO__RREEUUSSEEPPOORRTT socket from a BBPPFF__MMAAPP__TTYYPPEE__RREEUUSSEE‐‐
                     PPOORRTT__SSOOCCKKAARRRRAAYY _m_a_p.  It checks  the  selected  socket  is
                     matching the incoming request in the socket buffer.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       uu6644 bbppff__sskkbb__aanncceessttoorr__ccggrroouupp__iidd((ssttrruucctt sskk__bbuuffff **_s_k_b,, iinntt _a_n_c_e_s_t_o_r___l_e_v_e_l))

              DDeessccrriippttiioonn
                     Return id of cgroup v2 that is ancestor of cgroup associ‐
                     ated with the _s_k_b at the _a_n_c_e_s_t_o_r___l_e_v_e_l.  The root cgroup
                     is  at _a_n_c_e_s_t_o_r___l_e_v_e_l zero and each step down the hierar‐
                     chy increments the level. If _a_n_c_e_s_t_o_r___l_e_v_e_l ==  level  of
                     cgroup  associated  with  _s_k_b,  then return value will be
                     same as that of bbppff__sskkbb__ccggrroouupp__iidd().

                     The helper is  useful  to  implement  policies  based  on
                     cgroups that are upper in hierarchy than immediate cgroup
                     associated with _s_k_b.

                     The format of returned id and helper limitations are same
                     as in bbppff__sskkbb__ccggrroouupp__iidd().

              RReettuurrnn The  id  is returned or 0 in case the id could not be re‐
                     trieved.

       ssttrruucctt bbppff__ssoocckk **bbppff__sskk__llooookkuupp__ttccpp((vvooiidd **_c_t_x,, ssttrruucctt bbppff__ssoocckk__ttuuppllee
       **_t_u_p_l_e,, uu3322 _t_u_p_l_e___s_i_z_e,, uu6644 _n_e_t_n_s,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Look for TCP socket matching _t_u_p_l_e, optionally in a child
                     network  namespace  _n_e_t_n_s.  The  return  value  must   be
                     checked, and if non-NNUULLLL, released via bbppff__sskk__rreelleeaassee().

                     The  _c_t_x should point to the context of the program, such
                     as the skb or socket (depending on the hook in use). This
                     is used to determine the base network namespace  for  the
                     lookup.

                     _t_u_p_l_e___s_i_z_e must be one of:

                     ssiizzeeooff((_t_u_p_l_e-->>iippvv44))
                            Look for an IPv4 socket.

                     ssiizzeeooff((_t_u_p_l_e-->>iippvv66))
                            Look for an IPv6 socket.

                     If  the  _n_e_t_n_s  is a negative signed 32-bit integer, then
                     the socket lookup table in the netns associated with  the
                     _c_t_x  will be used. For the TC hooks, this is the netns of
                     the device in the skb. For  socket  hooks,  this  is  the
                     netns of the socket.  If _n_e_t_n_s is any other signed 32-bit
                     value greater than or equal to zero then it specifies the
                     ID of the netns relative to the netns associated with the
                     _c_t_x. _n_e_t_n_s values beyond the range of 32-bit integers are
                     reserved for future use.

                     All  values  for _f_l_a_g_s are reserved for future usage, and
                     must be left at zero.

                     This helper is available only if the kernel was  compiled
                     with CCOONNFFIIGG__NNEETT configuration option.

              RReettuurrnn Pointer  to  ssttrruucctt bbppff__ssoocckk, or NNUULLLL in case of failure.
                     For sockets with reuseport option,  the  ssttrruucctt  bbppff__ssoocckk
                     result  is  from _r_e_u_s_e-->>ssoocckkss[] using the hash of the tu‐
                     ple.

       ssttrruucctt bbppff__ssoocckk **bbppff__sskk__llooookkuupp__uuddpp((vvooiidd **_c_t_x,, ssttrruucctt bbppff__ssoocckk__ttuuppllee
       **_t_u_p_l_e,, uu3322 _t_u_p_l_e___s_i_z_e,, uu6644 _n_e_t_n_s,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Look for UDP socket matching _t_u_p_l_e, optionally in a child
                     network  namespace  _n_e_t_n_s.  The  return  value  must   be
                     checked, and if non-NNUULLLL, released via bbppff__sskk__rreelleeaassee().

                     The  _c_t_x should point to the context of the program, such
                     as the skb or socket (depending on the hook in use). This
                     is used to determine the base network namespace  for  the
                     lookup.

                     _t_u_p_l_e___s_i_z_e must be one of:

                     ssiizzeeooff((_t_u_p_l_e-->>iippvv44))
                            Look for an IPv4 socket.

                     ssiizzeeooff((_t_u_p_l_e-->>iippvv66))
                            Look for an IPv6 socket.

                     If  the  _n_e_t_n_s  is a negative signed 32-bit integer, then
                     the socket lookup table in the netns associated with  the
                     _c_t_x  will be used. For the TC hooks, this is the netns of
                     the device in the skb. For  socket  hooks,  this  is  the
                     netns of the socket.  If _n_e_t_n_s is any other signed 32-bit
                     value greater than or equal to zero then it specifies the
                     ID of the netns relative to the netns associated with the
                     _c_t_x. _n_e_t_n_s values beyond the range of 32-bit integers are
                     reserved for future use.

                     All  values  for _f_l_a_g_s are reserved for future usage, and
                     must be left at zero.

                     This helper is available only if the kernel was  compiled
                     with CCOONNFFIIGG__NNEETT configuration option.

              RReettuurrnn Pointer  to  ssttrruucctt bbppff__ssoocckk, or NNUULLLL in case of failure.
                     For sockets with reuseport option,  the  ssttrruucctt  bbppff__ssoocckk
                     result  is  from _r_e_u_s_e-->>ssoocckkss[] using the hash of the tu‐
                     ple.

       lloonngg bbppff__sskk__rreelleeaassee((vvooiidd **_s_o_c_k))

              DDeessccrriippttiioonn
                     Release the reference  held  by  _s_o_c_k.  _s_o_c_k  must  be  a
                     non-NNUULLLL     pointer     that     was    returned    from
                     bbppff__sskk__llooookkuupp__xxxxxx().

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__mmaapp__ppuusshh__eelleemm((ssttrruucctt bbppff__mmaapp **_m_a_p,, ccoonnsstt vvooiidd **_v_a_l_u_e,, uu6644
       _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Push an element _v_a_l_u_e in _m_a_p. _f_l_a_g_s is one of:

                     BBPPFF__EEXXIISSTT
                            If the queue/stack is full, the oldest element  is
                            removed to make room for this.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__mmaapp__ppoopp__eelleemm((ssttrruucctt bbppff__mmaapp **_m_a_p,, vvooiidd **_v_a_l_u_e))

              DDeessccrriippttiioonn
                     Pop an element from _m_a_p.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__mmaapp__ppeeeekk__eelleemm((ssttrruucctt bbppff__mmaapp **_m_a_p,, vvooiidd **_v_a_l_u_e))

              DDeessccrriippttiioonn
                     Get an element from _m_a_p without removing it.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__mmssgg__ppuusshh__ddaattaa((ssttrruucctt sskk__mmssgg__bbuuffff **_m_s_g,, uu3322 _s_t_a_r_t,, uu3322 _l_e_n,, uu6644
       _f_l_a_g_s))

              DDeessccrriippttiioonn
                     For  socket policies, insert _l_e_n bytes into _m_s_g at offset
                     _s_t_a_r_t.

                     If a program of type BBPPFF__PPRROOGG__TTYYPPEE__SSKK__MMSSGG is run on a _m_s_g
                     it may want to insert metadata or options into  the  _m_s_g.
                     This can later be read and used by any of the lower layer
                     BPF hooks.

                     This  helper  may fail if under memory pressure (a malloc
                     fails) in these cases BPF programs will get an  appropri‐
                     ate error and BPF programs will need to handle them.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__mmssgg__ppoopp__ddaattaa((ssttrruucctt sskk__mmssgg__bbuuffff **_m_s_g,, uu3322 _s_t_a_r_t,, uu3322 _l_e_n,, uu6644
       _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Will  remove _l_e_n bytes from a _m_s_g starting at byte _s_t_a_r_t.
                     This may result in EENNOOMMEEMM errors under certain situations
                     if an allocation and copy are required due to a full ring
                     buffer.  However, the helper will try to avoid doing  the
                     allocation  if  possible. Other errors can occur if input
                     parameters are invalid either due to _s_t_a_r_t byte not being
                     valid part of _m_s_g  payload  and/or  _p_o_p  value  being  to
                     large.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__rrcc__ppooiinntteerr__rreell((vvooiidd **_c_t_x,, ss3322 _r_e_l___x,, ss3322 _r_e_l___y))

              DDeessccrriippttiioonn
                     This helper is used in programs implementing IR decoding,
                     to report a successfully decoded pointer movement.

                     The  _c_t_x  should  point to the lirc sample as passed into
                     the program.

                     This helper is only available is the kernel was  compiled
                     with  the  CCOONNFFIIGG__BBPPFF__LLIIRRCC__MMOODDEE22 configuration option set
                     to "yy".

              RReettuurrnn 0

       lloonngg bbppff__ssppiinn__lloocckk((ssttrruucctt bbppff__ssppiinn__lloocckk **_l_o_c_k))

              DDeessccrriippttiioonn
                     Acquire a spinlock represented by the pointer _l_o_c_k, which
                     is stored as part of a value of a map.  Taking  the  lock
                     allows  to  safely  update the rest of the fields in that
                     value. The spinlock can (and must) later be released with
                     a call to bbppff__ssppiinn__uunnlloocckk(_l_o_c_k).

                     Spinlocks in BPF programs come with a number of  restric‐
                     tions and constraints:

                     • bbppff__ssppiinn__lloocckk  objects  are only allowed inside maps of
                       types BBPPFF__MMAAPP__TTYYPPEE__HHAASSHH  and  BBPPFF__MMAAPP__TTYYPPEE__AARRRRAAYY  (this
                       list could be extended in the future).

                     • BTF description of the map is mandatory.

                     • The BPF program can take ONE lock at a time, since tak‐
                       ing two or more could cause dead locks.

                     • Only  one  ssttrruucctt bbppff__ssppiinn__lloocckk is allowed per map ele‐
                       ment.

                     • When the lock is taken, calls (either  BPF  to  BPF  or
                       helpers) are not allowed.

                     • The  BBPPFF__LLDD__AABBSS and BBPPFF__LLDD__IINNDD instructions are not al‐
                       lowed inside a spinlock-ed region.

                     • The BPF program MUST call bbppff__ssppiinn__uunnlloocckk() to  release
                       the lock, on all execution paths, before it returns.

                     • The  BPF  program  can access ssttrruucctt bbppff__ssppiinn__lloocckk only
                       via the bbppff__ssppiinn__lloocckk() and bbppff__ssppiinn__uunnlloocckk()  helpers.
                       Loading  or  storing data into the ssttrruucctt bbppff__ssppiinn__lloocckk
                       _l_o_c_k;; field of a map is not allowed.

                     • To use the bbppff__ssppiinn__lloocckk() helper, the BTF  description
                       of  the  map  value  must  be  a struct and have ssttrruucctt
                       bbppff__ssppiinn__lloocckk _a_n_y_n_a_m_e;; field at the top level.   Nested
                       lock inside another struct is not allowed.

                     • The ssttrruucctt bbppff__ssppiinn__lloocckk _l_o_c_k field in a map value must
                       be aligned on a multiple of 4 bytes in that value.

                     • Syscall  with command BBPPFF__MMAAPP__LLOOOOKKUUPP__EELLEEMM does not copy
                       the bbppff__ssppiinn__lloocckk field to user space.

                     • Syscall with  command  BBPPFF__MMAAPP__UUPPDDAATTEE__EELLEEMM,  or  update
                       from  a  BPF  program,  do not update the bbppff__ssppiinn__lloocckk
                       field.

                     • bbppff__ssppiinn__lloocckk cannot be on the stack or inside  a  net‐
                       working packet (it can only be inside of a map values).

                     • bbppff__ssppiinn__lloocckk is available to root only.

                     • Tracing  programs and socket filter programs cannot use
                       bbppff__ssppiinn__lloocckk() due to insufficient  preemption  checks
                       (but this may change in the future).

                     • bbppff__ssppiinn__lloocckk   is   not   allowed  in  inner  maps  of
                       map-in-map.

              RReettuurrnn 0

       lloonngg bbppff__ssppiinn__uunnlloocckk((ssttrruucctt bbppff__ssppiinn__lloocckk **_l_o_c_k))

              DDeessccrriippttiioonn
                     Release  the  _l_o_c_k  previously  locked  by  a   call   to
                     bbppff__ssppiinn__lloocckk(_l_o_c_k).

              RReettuurrnn 0

       ssttrruucctt bbppff__ssoocckk **bbppff__sskk__ffuullllssoocckk((ssttrruucctt bbppff__ssoocckk **_s_k))

              DDeessccrriippttiioonn
                     This  helper gets a ssttrruucctt bbppff__ssoocckk pointer such that all
                     the fields in this bbppff__ssoocckk can be accessed.

              RReettuurrnn A ssttrruucctt bbppff__ssoocckk pointer on success, or NNUULLLL in case  of
                     failure.

       ssttrruucctt bbppff__ttccpp__ssoocckk **bbppff__ttccpp__ssoocckk((ssttrruucctt bbppff__ssoocckk **_s_k))

              DDeessccrriippttiioonn
                     This  helper  gets  a  ssttrruucctt bbppff__ttccpp__ssoocckk pointer from a
                     ssttrruucctt bbppff__ssoocckk pointer.

              RReettuurrnn A ssttrruucctt bbppff__ttccpp__ssoocckk pointer on success, or NNUULLLL in case
                     of failure.

       lloonngg bbppff__sskkbb__eeccnn__sseett__ccee((ssttrruucctt sskk__bbuuffff **_s_k_b))

              DDeessccrriippttiioonn
                     Set ECN (Explicit Congestion Notification)  field  of  IP
                     header to CCEE (Congestion Encountered) if current value is
                     EECCTT (ECN Capable Transport). Otherwise, do nothing. Works
                     with IPv6 and IPv4.

              RReettuurrnn 1  if  the  CCEE  flag is set (either by the current helper
                     call or because it was already present), 0 if it  is  not
                     set.

       ssttrruucctt bbppff__ssoocckk **bbppff__ggeett__lliisstteenneerr__ssoocckk((ssttrruucctt bbppff__ssoocckk **_s_k))

              DDeessccrriippttiioonn
                     Return  a  ssttrruucctt  bbppff__ssoocckk  pointer in TTCCPP__LLIISSTTEENN state.
                     bbppff__sskk__rreelleeaassee() is unnecessary and not allowed.

              RReettuurrnn A ssttrruucctt bbppff__ssoocckk pointer on success, or NNUULLLL in case  of
                     failure.

       ssttrruucctt bbppff__ssoocckk **bbppff__sskkcc__llooookkuupp__ttccpp((vvooiidd **_c_t_x,, ssttrruucctt bbppff__ssoocckk__ttuuppllee
       **_t_u_p_l_e,, uu3322 _t_u_p_l_e___s_i_z_e,, uu6644 _n_e_t_n_s,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Look for TCP socket matching _t_u_p_l_e, optionally in a child
                     network   namespace  _n_e_t_n_s.  The  return  value  must  be
                     checked, and if non-NNUULLLL, released via bbppff__sskk__rreelleeaassee().

                     This function is identical to bbppff__sskk__llooookkuupp__ttccpp(), except
                     that it also returns timewait  or  request  sockets.  Use
                     bbppff__sskk__ffuullllssoocckk()  or  bbppff__ttccpp__ssoocckk()  to access the full
                     structure.

                     This helper is available only if the kernel was  compiled
                     with CCOONNFFIIGG__NNEETT configuration option.

              RReettuurrnn Pointer  to  ssttrruucctt bbppff__ssoocckk, or NNUULLLL in case of failure.
                     For sockets with reuseport option,  the  ssttrruucctt  bbppff__ssoocckk
                     result  is  from _r_e_u_s_e-->>ssoocckkss[] using the hash of the tu‐
                     ple.

       lloonngg bbppff__ttccpp__cchheecckk__ssyynnccooookkiiee((vvooiidd **_s_k,, vvooiidd **_i_p_h,, uu3322 _i_p_h___l_e_n,, ssttrruucctt
       ttccpphhddrr **_t_h,, uu3322 _t_h___l_e_n))

              DDeessccrriippttiioonn
                     Check whether _i_p_h and _t_h contain a valid SYN  cookie  ACK
                     for the listening socket in _s_k.

                     _i_p_h points to the start of the IPv4 or IPv6 header, while
                     _i_p_h___l_e_n  contains  ssiizzeeooff(ssttrruucctt  iipphhddrr) or ssiizzeeooff(ssttrruucctt
                     iippvv66hhddrr).

                     _t_h points to the start of the TCP  header,  while  _t_h___l_e_n
                     contains   the   length  of  the  TCP  header  (at  least
                     ssiizzeeooff(ssttrruucctt ttccpphhddrr)).

              RReettuurrnn 0 if _i_p_h and _t_h are a valid SYN cookie ACK, or a negative
                     error otherwise.

       lloonngg bbppff__ssyyssccttll__ggeett__nnaammee((ssttrruucctt bbppff__ssyyssccttll **_c_t_x,, cchhaarr **_b_u_f,, ssiizzee__tt
       _b_u_f___l_e_n,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Get name of sysctl in /proc/sys/ and copy  it  into  pro‐
                     vided by program buffer _b_u_f of size _b_u_f___l_e_n.

                     The   buffer   is  always  NUL  terminated,  unless  it's
                     zero-sized.

                     If _f_l_a_g_s is zero, full name (e.g. "net/ipv4/tcp_mem")  is
                     copied. Use BBPPFF__FF__SSYYSSCCTTLL__BBAASSEE__NNAAMMEE flag to copy base name
                     only (e.g. "tcp_mem").

              RReettuurrnn Number  of  character  copied (not including the trailing
                     NUL).

                     --EE22BBIIGG if the buffer wasn't big enough (_b_u_f will  contain
                     truncated name in this case).

       lloonngg bbppff__ssyyssccttll__ggeett__ccuurrrreenntt__vvaalluuee((ssttrruucctt bbppff__ssyyssccttll **_c_t_x,, cchhaarr **_b_u_f,,
       ssiizzee__tt _b_u_f___l_e_n))

              DDeessccrriippttiioonn
                     Get  current  value  of  sysctl  as  it  is  presented in
                     /proc/sys (incl. newline, etc), and copy it as  a  string
                     into provided by program buffer _b_u_f of size _b_u_f___l_e_n.

                     The  whole  value is copied, no matter what file position
                     user space issued e.g. sys_read at.

                     The  buffer  is  always  NUL  terminated,   unless   it's
                     zero-sized.

              RReettuurrnn Number  of  character  copied (not including the trailing
                     NUL).

                     --EE22BBIIGG if the buffer wasn't big enough (_b_u_f will  contain
                     truncated name in this case).

                     --EEIINNVVAALL  if  current  value was unavailable, e.g. because
                     sysctl is uninitialized and read returns -EIO for it.

       lloonngg bbppff__ssyyssccttll__ggeett__nneeww__vvaalluuee((ssttrruucctt bbppff__ssyyssccttll **_c_t_x,, cchhaarr **_b_u_f,, ssiizzee__tt
       _b_u_f___l_e_n))

              DDeessccrriippttiioonn
                     Get new value being written by user space to sysctl  (be‐
                     fore  the  actual  write happens) and copy it as a string
                     into provided by program buffer _b_u_f of size _b_u_f___l_e_n.

                     User space may write new value at file position > 0.

                     The  buffer  is  always  NUL  terminated,   unless   it's
                     zero-sized.

              RReettuurrnn Number  of  character  copied (not including the trailing
                     NUL).

                     --EE22BBIIGG if the buffer wasn't big enough (_b_u_f will  contain
                     truncated name in this case).

                     --EEIINNVVAALL if sysctl is being read.

       lloonngg bbppff__ssyyssccttll__sseett__nneeww__vvaalluuee((ssttrruucctt bbppff__ssyyssccttll **_c_t_x,, ccoonnsstt cchhaarr **_b_u_f,,
       ssiizzee__tt _b_u_f___l_e_n))

              DDeessccrriippttiioonn
                     Override  new value being written by user space to sysctl
                     with value provided by program  in  buffer  _b_u_f  of  size
                     _b_u_f___l_e_n.

                     _b_u_f  should  contain a string in same form as provided by
                     user space on sysctl write.

                     User space may write new value at file position >  0.  To
                     override  the  whole sysctl value file position should be
                     set to zero.

              RReettuurrnn 0 on success.

                     --EE22BBIIGG if the _b_u_f___l_e_n is too big.

                     --EEIINNVVAALL if sysctl is being read.

       lloonngg bbppff__ssttrrttooll((ccoonnsstt cchhaarr **_b_u_f,, ssiizzee__tt _b_u_f___l_e_n,, uu6644 _f_l_a_g_s,, lloonngg **_r_e_s))

              DDeessccrriippttiioonn
                     Convert the initial part of the string from buffer _b_u_f of
                     size _b_u_f___l_e_n to a long integer  according  to  the  given
                     base and save the result in _r_e_s.

                     The  string  may  begin with an arbitrary amount of white
                     space (as determined by iissssppaaccee(3)) followed by a  single
                     optional '--' sign.

                     Five  least  significant bits of _f_l_a_g_s encode base, other
                     bits are currently unused.

                     Base must be either 8, 10, 16 or 0 to detect it automati‐
                     cally similar to user space ssttrrttooll(3).

              RReettuurrnn Number of characters consumed on success. Must  be  posi‐
                     tive but no more than _b_u_f___l_e_n.

                     --EEIINNVVAALL if no valid digits were found or unsupported base
                     was provided.

                     --EERRAANNGGEE if resulting value was out of range.

       lloonngg bbppff__ssttrrttoouull((ccoonnsstt cchhaarr **_b_u_f,, ssiizzee__tt _b_u_f___l_e_n,, uu6644 _f_l_a_g_s,, uunnssiiggnneedd
       lloonngg **_r_e_s))

              DDeessccrriippttiioonn
                     Convert the initial part of the string from buffer _b_u_f of
                     size _b_u_f___l_e_n to an unsigned long integer according to the
                     given base and save the result in _r_e_s.

                     The  string  may  begin with an arbitrary amount of white
                     space (as determined by iissssppaaccee(3)).

                     Five least significant bits of _f_l_a_g_s encode  base,  other
                     bits are currently unused.

                     Base must be either 8, 10, 16 or 0 to detect it automati‐
                     cally similar to user space ssttrrttoouull(3).

              RReettuurrnn Number  of  characters consumed on success. Must be posi‐
                     tive but no more than _b_u_f___l_e_n.

                     --EEIINNVVAALL if no valid digits were found or unsupported base
                     was provided.

                     --EERRAANNGGEE if resulting value was out of range.

       vvooiidd **bbppff__sskk__ssttoorraaggee__ggeett((ssttrruucctt bbppff__mmaapp **_m_a_p,, vvooiidd **_s_k,, vvooiidd **_v_a_l_u_e,,
       uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Get a bpf-local-storage from a _s_k.

                     Logically, it could be thought of getting the value  from
                     a  _m_a_p  with  _s_k as the kkeeyy.  From this perspective,  the
                     usage is not much different from bbppff__mmaapp__llooookkuupp__eelleemm(_m_a_p,
                     &&_s_k) except this helper enforces the key must be  a  full
                     socket  and  the  map  must  be a BBPPFF__MMAAPP__TTYYPPEE__SSKK__SSTTOORRAAGGEE
                     also.

                     Underneath, the value is stored locally at _s_k instead  of
                     the  _m_a_p.   The  _m_a_p  is  used  as  the bpf-local-storage
                     "type". The bpf-local-storage "type" (i.e.  the  _m_a_p)  is
                     searched against all bpf-local-storages residing at _s_k.

                     _s_k  is  a kernel ssttrruucctt ssoocckk pointer for LSM program.  _s_k
                     is a ssttrruucctt bbppff__ssoocckk pointer for other program types.

                     An optional _f_l_a_g_s  (BBPPFF__SSKK__SSTTOORRAAGGEE__GGEETT__FF__CCRREEAATTEE)  can  be
                     used such that a new bpf-local-storage will be created if
                     one  does  not  exist.   _v_a_l_u_e  can be used together with
                     BBPPFF__SSKK__SSTTOORRAAGGEE__GGEETT__FF__CCRREEAATTEE to specify the initial  value
                     of  a  bpf-local-storage.   If  _v_a_l_u_e  is  NNUULLLL,  the new
                     bpf-local-storage will be zero initialized.

              RReettuurrnn A bpf-local-storage pointer is returned on success.

                     NNUULLLL if not found or there was an error in adding  a  new
                     bpf-local-storage.

       lloonngg bbppff__sskk__ssttoorraaggee__ddeelleettee((ssttrruucctt bbppff__mmaapp **_m_a_p,, vvooiidd **_s_k))

              DDeessccrriippttiioonn
                     Delete a bpf-local-storage from a _s_k.

              RReettuurrnn 0 on success.

                     --EENNOOEENNTT  if the bpf-local-storage cannot be found.  --EEIINN‐‐
                     VVAALL if sk is not a fullsock (e.g. a request_sock).

       lloonngg bbppff__sseenndd__ssiiggnnaall((uu3322 _s_i_g))

              DDeessccrriippttiioonn
                     Send signal _s_i_g to the process of the current task.   The
                     signal may be delivered to any of this process's threads.

              RReettuurrnn 0 on success or successfully queued.

                     --EEBBUUSSYY if work queue under nmi is full.

                     --EEIINNVVAALL if _s_i_g is invalid.

                     --EEPPEERRMM if no permission to send the _s_i_g.

                     --EEAAGGAAIINN if bpf program can try again.

       ss6644 bbppff__ttccpp__ggeenn__ssyynnccooookkiiee((vvooiidd **_s_k,, vvooiidd **_i_p_h,, uu3322 _i_p_h___l_e_n,, ssttrruucctt
       ttccpphhddrr **_t_h,, uu3322 _t_h___l_e_n))

              DDeessccrriippttiioonn
                     Try to issue a SYN cookie for the packet with correspond‐
                     ing  IP/TCP  headers, _i_p_h and _t_h, on the listening socket
                     in _s_k.

                     _i_p_h points to the start of the IPv4 or IPv6 header, while
                     _i_p_h___l_e_n contains ssiizzeeooff(ssttrruucctt  iipphhddrr)  or  ssiizzeeooff(ssttrruucctt
                     iippvv66hhddrr).

                     _t_h  points  to  the start of the TCP header, while _t_h___l_e_n
                     contains the length of the TCP header  with  options  (at
                     least ssiizzeeooff(ssttrruucctt ttccpphhddrr)).

              RReettuurrnn On  success,  lower 32 bits hold the generated SYN cookie
                     in followed by 16 bits which hold the MSS value for  that
                     cookie, and the top 16 bits are unused.

                     On failure, the returned value is one of the following:

                     --EEIINNVVAALL SYN cookie cannot be issued due to error

                     --EENNOOEENNTT SYN cookie should not be issued (no SYN flood)

                     --EEOOPPNNOOTTSSUUPPPP  kernel  configuration  does  not  enable SYN
                     cookies

                     --EEPPRROOTTOONNOOSSUUPPPPOORRTT IP packet version is not 4 or 6

       lloonngg bbppff__sskkbb__oouuttppuutt((vvooiidd **_c_t_x,, ssttrruucctt bbppff__mmaapp **_m_a_p,, uu6644 _f_l_a_g_s,, vvooiidd
       **_d_a_t_a,, uu6644 _s_i_z_e))

              DDeessccrriippttiioonn
                     Write raw _d_a_t_a blob into a special BPF perf event held by
                     _m_a_p  of  type  BBPPFF__MMAAPP__TTYYPPEE__PPEERRFF__EEVVEENNTT__AARRRRAAYY.  This  perf
                     event must have the following attributes: PPEERRFF__SSAAMMPPLLEE__RRAAWW
                     as   ssaammppllee__ttyyppee,   PPEERRFF__TTYYPPEE__SSOOFFTTWWAARREE   as   ttyyppee,   and
                     PPEERRFF__CCOOUUNNTT__SSWW__BBPPFF__OOUUTTPPUUTT as ccoonnffiigg.

                     The _f_l_a_g_s are used to indicate the index in _m_a_p for which
                     the value must be put, masked with BBPPFF__FF__IINNDDEEXX__MMAASSKK.  Al‐
                     ternatively, _f_l_a_g_s can be set to BBPPFF__FF__CCUURRRREENNTT__CCPPUU to in‐
                     dicate that the index of the current CPU core  should  be
                     used.

                     The value to write, of _s_i_z_e, is passed through eBPF stack
                     and pointed by _d_a_t_a.

                     _c_t_x is a pointer to in-kernel struct sk_buff.

                     This helper is similar to bbppff__ppeerrff__eevveenntt__oouuttppuutt() but re‐
                     stricted to raw_tracepoint bpf programs.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__pprroobbee__rreeaadd__uusseerr((vvooiidd **_d_s_t,, uu3322 _s_i_z_e,, ccoonnsstt vvooiidd **_u_n_s_a_f_e___p_t_r))

              DDeessccrriippttiioonn
                     Safely attempt to read _s_i_z_e bytes from user space address
                     _u_n_s_a_f_e___p_t_r and store the data in _d_s_t.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__pprroobbee__rreeaadd__kkeerrnneell((vvooiidd **_d_s_t,, uu3322 _s_i_z_e,, ccoonnsstt vvooiidd **_u_n_s_a_f_e___p_t_r))

              DDeessccrriippttiioonn
                     Safely  attempt  to read _s_i_z_e bytes from kernel space ad‐
                     dress _u_n_s_a_f_e___p_t_r and store the data in _d_s_t.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__pprroobbee__rreeaadd__uusseerr__ssttrr((vvooiidd **_d_s_t,, uu3322 _s_i_z_e,, ccoonnsstt vvooiidd **_u_n_‐
       _s_a_f_e___p_t_r))

              DDeessccrriippttiioonn
                     Copy a NUL terminated string from an unsafe user  address
                     _u_n_s_a_f_e___p_t_r  to _d_s_t. The _s_i_z_e should include the terminat‐
                     ing NUL byte. In case the string length is  smaller  than
                     _s_i_z_e, the target is not padded with further NUL bytes. If
                     the  string length is larger than _s_i_z_e, just _s_i_z_e-1 bytes
                     are copied and the last byte is set to NUL.

                     On success, returns the number of bytes that  were  writ‐
                     ten,  including  the terminal NUL. This makes this helper
                     useful in tracing programs for reading strings, and  more
                     importantly to get its length at runtime. See the follow‐
                     ing snippet:

                        SEC("kprobe/sys_open")
                        void bpf_sys_open(struct pt_regs *ctx)
                        {
                                char buf[PATHLEN]; // PATHLEN is defined to 256
                                int res = bpf_probe_read_user_str(buf, sizeof(buf),
                                                                  ctx->di);

                                // Consume buf, for example push it to
                                // userspace via bpf_perf_event_output(); we
                                // can use res (the string length) as event
                                // size, after checking its boundaries.
                        }

                     In  comparison,  using  bbppff__pprroobbee__rreeaadd__uusseerr() helper here
                     instead to read the string would require to estimate  the
                     length at compile time, and would often result in copying
                     more memory than necessary.

                     Another  useful  use  case  is  when  parsing  individual
                     process arguments  or  individual  environment  variables
                     navigating      _c_u_r_r_e_n_t-->>mmmm-->>aarrgg__ssttaarrtt      and      _c_u_r_‐
                     _r_e_n_t-->>mmmm-->>eennvv__ssttaarrtt: using this  helper  and  the  return
                     value, one can quickly iterate at the right offset of the
                     memory area.

              RReettuurrnn On  success,  the  strictly positive length of the output
                     string, including the trailing NUL character. On error, a
                     negative value.

       lloonngg bbppff__pprroobbee__rreeaadd__kkeerrnneell__ssttrr((vvooiidd **_d_s_t,, uu3322 _s_i_z_e,, ccoonnsstt vvooiidd **_u_n_‐
       _s_a_f_e___p_t_r))

              DDeessccrriippttiioonn
                     Copy a NUL terminated string from an  unsafe  kernel  ad‐
                     dress   _u_n_s_a_f_e___p_t_r   to   _d_s_t.  Same  semantics  as  with
                     bbppff__pprroobbee__rreeaadd__uusseerr__ssttrr() apply.

              RReettuurrnn On success, the strictly positive length of  the  string,
                     including  the  trailing NUL character. On error, a nega‐
                     tive value.

       lloonngg bbppff__ttccpp__sseenndd__aacckk((vvooiidd **_t_p,, uu3322 _r_c_v___n_x_t))

              DDeessccrriippttiioonn
                     Send out a tcp-ack. _t_p is the in-kernel struct  ttccpp__ssoocckk.
                     _r_c_v___n_x_t is the ack_seq to be sent out.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__sseenndd__ssiiggnnaall__tthhrreeaadd((uu3322 _s_i_g))

              DDeessccrriippttiioonn
                     Send  signal  _s_i_g to the thread corresponding to the cur‐
                     rent task.

              RReettuurrnn 0 on success or successfully queued.

                     --EEBBUUSSYY if work queue under nmi is full.

                     --EEIINNVVAALL if _s_i_g is invalid.

                     --EEPPEERRMM if no permission to send the _s_i_g.

                     --EEAAGGAAIINN if bpf program can try again.

       uu6644 bbppff__jjiiffffiieess6644((vvooiidd))

              DDeessccrriippttiioonn
                     Obtain the 64bit jiffies

              RReettuurrnn The 64 bit jiffies

       lloonngg bbppff__rreeaadd__bbrraanncchh__rreeccoorrddss((ssttrruucctt bbppff__ppeerrff__eevveenntt__ddaattaa **_c_t_x,, vvooiidd
       **_b_u_f,, uu3322 _s_i_z_e,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     For an eBPF program attached to a  perf  event,  retrieve
                     the  branch records (ssttrruucctt ppeerrff__bbrraanncchh__eennttrryy) associated
                     to _c_t_x and store it in the buffer pointed by  _b_u_f  up  to
                     size _s_i_z_e bytes.

              RReettuurrnn On  success,  number of bytes written to _b_u_f. On error, a
                     negative value.

                     The _f_l_a_g_s can be set to BBPPFF__FF__GGEETT__BBRRAANNCCHH__RREECCOORRDDSS__SSIIZZEE  to
                     instead  return the number of bytes required to store all
                     the branch entries. If this flag is set, _b_u_f may be NULL.

                     --EEIINNVVAALL if arguments invalid or ssiizzee not  a  multiple  of
                     ssiizzeeooff(ssttrruucctt ppeerrff__bbrraanncchh__eennttrryy).

                     --EENNOOEENNTT if architecture does not support branch records.

       lloonngg bbppff__ggeett__nnss__ccuurrrreenntt__ppiidd__ttggiidd((uu6644 _d_e_v,, uu6644 _i_n_o,, ssttrruucctt
       bbppff__ppiiddnnss__iinnffoo **_n_s_d_a_t_a,, uu3322 _s_i_z_e))

              DDeessccrriippttiioonn
                     Returns  0  on  success,  values for _p_i_d and _t_g_i_d as seen
                     from the current _n_a_m_e_s_p_a_c_e will be returned in _n_s_d_a_t_a.

              RReettuurrnn 0 on success, or one of the following in case of failure:

                     --EEIINNVVAALL if dev and inum supplied don't  match  dev_t  and
                     inode number with nsfs of current task, or if dev conver‐
                     sion to dev_t lost high bits.

                     --EENNOOEENNTT if pidns does not exists for the current task.

       lloonngg bbppff__xxddpp__oouuttppuutt((vvooiidd **_c_t_x,, ssttrruucctt bbppff__mmaapp **_m_a_p,, uu6644 _f_l_a_g_s,, vvooiidd
       **_d_a_t_a,, uu6644 _s_i_z_e))

              DDeessccrriippttiioonn
                     Write raw _d_a_t_a blob into a special BPF perf event held by
                     _m_a_p  of  type  BBPPFF__MMAAPP__TTYYPPEE__PPEERRFF__EEVVEENNTT__AARRRRAAYY.  This  perf
                     event must have the following attributes: PPEERRFF__SSAAMMPPLLEE__RRAAWW
                     as   ssaammppllee__ttyyppee,   PPEERRFF__TTYYPPEE__SSOOFFTTWWAARREE   as   ttyyppee,   and
                     PPEERRFF__CCOOUUNNTT__SSWW__BBPPFF__OOUUTTPPUUTT as ccoonnffiigg.

                     The _f_l_a_g_s are used to indicate the index in _m_a_p for which
                     the value must be put, masked with BBPPFF__FF__IINNDDEEXX__MMAASSKK.  Al‐
                     ternatively, _f_l_a_g_s can be set to BBPPFF__FF__CCUURRRREENNTT__CCPPUU to in‐
                     dicate  that  the index of the current CPU core should be
                     used.

                     The value to write, of _s_i_z_e, is passed through eBPF stack
                     and pointed by _d_a_t_a.

                     _c_t_x is a pointer to in-kernel struct xdp_buff.

                     This helper is similar to bbppff__ppeerrff__eevveennttoouuttppuutt() but  re‐
                     stricted to raw_tracepoint bpf programs.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       uu6644 bbppff__ggeett__nneettnnss__ccooookkiiee((vvooiidd **_c_t_x))

              DDeessccrriippttiioonn
                     Retrieve the cookie (generated by the kernel) of the net‐
                     work namespace the input _c_t_x is associated with. The net‐
                     work namespace cookie remains stable for its lifetime and
                     provides  a global identifier that can be assumed unique.
                     If _c_t_x is NULL, then the helper returns  the  cookie  for
                     the  initial network namespace. The cookie itself is very
                     similar to that of  bbppff__ggeett__ssoocckkeett__ccooookkiiee()  helper,  but
                     for network namespaces instead of sockets.

              RReettuurrnn A 8-byte long opaque number.

       uu6644 bbppff__ggeett__ccuurrrreenntt__aanncceessttoorr__ccggrroouupp__iidd((iinntt _a_n_c_e_s_t_o_r___l_e_v_e_l))

              DDeessccrriippttiioonn
                     Return id of cgroup v2 that is ancestor of the cgroup as‐
                     sociated with the current task at the _a_n_c_e_s_t_o_r___l_e_v_e_l. The
                     root  cgroup is at _a_n_c_e_s_t_o_r___l_e_v_e_l zero and each step down
                     the hierarchy increments the level. If _a_n_c_e_s_t_o_r___l_e_v_e_l  ==
                     level  of  cgroup  associated with the current task, then
                     return value will be the same  as  that  of  bbppff__ggeett__ccuurr‐‐
                     rreenntt__ccggrroouupp__iidd().

                     The  helper  is  useful  to  implement  policies based on
                     cgroups that are upper in hierarchy than immediate cgroup
                     associated with the current task.

                     The format of returned id and helper limitations are same
                     as in bbppff__ggeett__ccuurrrreenntt__ccggrroouupp__iidd().

              RReettuurrnn The id is returned or 0 in case the id could not  be  re‐
                     trieved.

       lloonngg bbppff__sskk__aassssiiggnn((ssttrruucctt sskk__bbuuffff **_s_k_b,, vvooiidd **_s_k,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Helper  is overloaded depending on BPF program type. This
                     description  applies   to   BBPPFF__PPRROOGG__TTYYPPEE__SSCCHHEEDD__CCLLSS   and
                     BBPPFF__PPRROOGG__TTYYPPEE__SSCCHHEEDD__AACCTT programs.

                     Assign  the _s_k to the _s_k_b. When combined with appropriate
                     routing configuration to receive the packet  towards  the
                     socket,  will  cause _s_k_b to be delivered to the specified
                     socket.  Subsequent redirection  of  _s_k_b  via   bbppff__rreeddii‐‐
                     rreecctt(),  bbppff__cclloonnee__rreeddiirreecctt() or other methods outside of
                     BPF may interfere with successful delivery to the socket.

                     This operation is only valid from TC ingress path.

                     The _f_l_a_g_s argument must be zero.

              RReettuurrnn 0 on success, or a negative error in case of failure:

                     --EEIINNVVAALL if specified _f_l_a_g_s are not supported.

                     --EENNOOEENNTT if the socket is unavailable for assignment.

                     --EENNEETTUUNNRREEAACCHH if the socket is unreachable (wrong netns).

                     --EEOOPPNNOOTTSSUUPPPP if the operation is not supported, for  exam‐
                     ple a call from outside of TC ingress.

       lloonngg bbppff__sskk__aassssiiggnn((ssttrruucctt bbppff__sskk__llooookkuupp **_c_t_x,, ssttrruucctt bbppff__ssoocckk **_s_k,, uu6644
       _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Helper  is overloaded depending on BPF program type. This
                     description applies to BBPPFF__PPRROOGG__TTYYPPEE__SSKK__LLOOOOKKUUPP programs.

                     Select the _s_k as a result of a socket lookup.

                     For the operation to succeed passed socket must  be  com‐
                     patible  with  the packet description provided by the _c_t_x
                     object.

                     L4 protocol (IIPPPPRROOTTOO__TTCCPP or IIPPPPRROOTTOO__UUDDPP) must be an exact
                     match. While IP family (AAFF__IINNEETT or AAFF__IINNEETT66) must be com‐
                     patible, that is IPv6 sockets that are not v6-only can be
                     selected for IPv4 packets.

                     Only TCP listeners and UDP unconnected sockets can be se‐
                     lected. _s_k can also be NULL to reset any previous  selec‐
                     tion.

                     _f_l_a_g_s argument can combination of following values:

                     • BBPPFF__SSKK__LLOOOOKKUUPP__FF__RREEPPLLAACCEE to override the previous socket
                       selection,  potentially  done by a BPF program that ran
                       before us.

                     • BBPPFF__SSKK__LLOOOOKKUUPP__FF__NNOO__RREEUUSSEEPPOORRTT  to  skip   load-balancing
                       within reuseport group for the socket being selected.

                     On success _c_t_x_-_>_s_k will point to the selected socket.

              RReettuurrnn 0 on success, or a negative errno in case of failure.

                     • --EEAAFFNNOOSSUUPPPPOORRTT if socket family (_s_k_-_>_f_a_m_i_l_y) is not com‐
                       patible with packet family (_c_t_x_-_>_f_a_m_i_l_y).

                     • --EEEEXXIISSTT  if  socket  has  been already selected, poten‐
                       tially by another program, and  BBPPFF__SSKK__LLOOOOKKUUPP__FF__RREEPPLLAACCEE
                       flag was not specified.

                     • --EEIINNVVAALL if unsupported flags were specified.

                     • --EEPPRROOTTOOTTYYPPEE   if   socket  L4  protocol  (_s_k_-_>_p_r_o_t_o_c_o_l)
                       doesn't match packet protocol (_c_t_x_-_>_p_r_o_t_o_c_o_l).

                     • --EESSOOCCKKTTNNOOSSUUPPPPOORRTT if socket is not in allowed state (TCP
                       listening or UDP unconnected).

       uu6644 bbppff__kkttiimmee__ggeett__bboooott__nnss((vvooiidd))

              DDeessccrriippttiioonn
                     Return the time elapsed since system  boot,  in  nanosec‐
                     onds.   Does  include  the time the system was suspended.
                     See: cclloocckk__ggeettttiimmee(CCLLOOCCKK__BBOOOOTTTTIIMMEE)

              RReettuurrnn Current _k_t_i_m_e.

       lloonngg bbppff__sseeqq__pprriinnttff((ssttrruucctt sseeqq__ffiillee **_m,, ccoonnsstt cchhaarr **_f_m_t,, uu3322 _f_m_t___s_i_z_e,,
       ccoonnsstt vvooiidd **_d_a_t_a,, uu3322 _d_a_t_a___l_e_n))

              DDeessccrriippttiioonn
                     bbppff__sseeqq__pprriinnttff() uses seq_file sseeqq__pprriinnttff() to print  out
                     the  format  string.   The _m represents the seq_file. The
                     _f_m_t and _f_m_t___s_i_z_e are for the format  string  itself.  The
                     _d_a_t_a  and  _d_a_t_a___l_e_n are format string arguments. The _d_a_t_a
                     are a uu6644 array and corresponding  format  string  values
                     are  stored  in the array. For strings and pointers where
                     pointees are accessed, only the pointer values are stored
                     in the _d_a_t_a array.  The _d_a_t_a___l_e_n is the size of  _d_a_t_a  in
                     bytes - must be a multiple of 8.

                     Formats  %%ss, %%pp{{ii,,II}}{{44,,66}} requires to read kernel memory.
                     Reading kernel memory may fail due to either invalid  ad‐
                     dress  or  valid  address  but  requiring  a major memory
                     fault. If reading kernel memory fails, the string for  %%ss
                     will   be  an  empty  string,  and  the  ip  address  for
                     %%pp{{ii,,II}}{{44,,66}} will be 0. Not returning error to  bpf  pro‐
                     gram  is consistent with what bbppff__ttrraaccee__pprriinnttkk() does for
                     now.

              RReettuurrnn 0 on success, or a negative error in case of failure:

                     --EEBBUUSSYY if per-CPU memory copy buffer  is  busy,  can  try
                     again by returning 1 from bpf program.

                     --EEIINNVVAALL  if  arguments  are  invalid,  or  if  _f_m_t is in‐
                     valid/unsupported.

                     --EE22BBIIGG if _f_m_t contains too many format specifiers.

                     --EEOOVVEERRFFLLOOWW if an overflow happened: The same object  will
                     be tried again.

       lloonngg bbppff__sseeqq__wwrriittee((ssttrruucctt sseeqq__ffiillee **_m,, ccoonnsstt vvooiidd **_d_a_t_a,, uu3322 _l_e_n))

              DDeessccrriippttiioonn
                     bbppff__sseeqq__wwrriittee()  uses  seq_file  sseeqq__wwrriittee() to write the
                     data.  The _m represents the seq_file. The  _d_a_t_a  and  _l_e_n
                     represent the data to write in bytes.

              RReettuurrnn 0 on success, or a negative error in case of failure:

                     --EEOOVVEERRFFLLOOWW  if an overflow happened: The same object will
                     be tried again.

       uu6644 bbppff__sskk__ccggrroouupp__iidd((vvooiidd **_s_k))

              DDeessccrriippttiioonn
                     Return the cgroup v2 id of the socket _s_k.

                     _s_k must be a non-NNUULLLL pointer to a socket, e.g.  one  re‐
                     turned  from bbppff__sskk__llooookkuupp__xxxxxx(), bbppff__sskk__ffuullllssoocckk(), etc.
                     The   format   of   returned   id   is   same    as    in
                     bbppff__sskkbb__ccggrroouupp__iidd().

                     This  helper is available only if the kernel was compiled
                     with the CCOONNFFIIGG__SSOOCCKK__CCGGRROOUUPP__DDAATTAA configuration option.

              RReettuurrnn The id is returned or 0 in case the id could not  be  re‐
                     trieved.

       uu6644 bbppff__sskk__aanncceessttoorr__ccggrroouupp__iidd((vvooiidd **_s_k,, iinntt _a_n_c_e_s_t_o_r___l_e_v_e_l))

              DDeessccrriippttiioonn
                     Return id of cgroup v2 that is ancestor of cgroup associ‐
                     ated  with the _s_k at the _a_n_c_e_s_t_o_r___l_e_v_e_l.  The root cgroup
                     is at _a_n_c_e_s_t_o_r___l_e_v_e_l zero and each step down the  hierar‐
                     chy  increments  the level. If _a_n_c_e_s_t_o_r___l_e_v_e_l == level of
                     cgroup associated with _s_k, then return value will be same
                     as that of bbppff__sskk__ccggrroouupp__iidd().

                     The helper is  useful  to  implement  policies  based  on
                     cgroups that are upper in hierarchy than immediate cgroup
                     associated with _s_k.

                     The format of returned id and helper limitations are same
                     as in bbppff__sskk__ccggrroouupp__iidd().

              RReettuurrnn The  id  is returned or 0 in case the id could not be re‐
                     trieved.

       lloonngg bbppff__rriinnggbbuuff__oouuttppuutt((vvooiidd **_r_i_n_g_b_u_f,, vvooiidd **_d_a_t_a,, uu6644 _s_i_z_e,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Copy _s_i_z_e bytes from _d_a_t_a into a ring buffer _r_i_n_g_b_u_f.  If
                     BBPPFF__RRBB__NNOO__WWAAKKEEUUPP is specified in _f_l_a_g_s,  no  notification
                     of new data availability is sent.  If BBPPFF__RRBB__FFOORRCCEE__WWAAKKEEUUPP
                     is  specified  in  _f_l_a_g_s, notification of new data avail‐
                     ability is sent unconditionally.  If 00  is  specified  in
                     _f_l_a_g_s,  an adaptive notification of new data availability
                     is sent.

                     An adaptive notification is a notification sent  whenever
                     the  user-space  process  has  caught up and consumed all
                     available payloads. In case  the  user-space  process  is
                     still processing a previous payload, then no notification
                     is  needed as it will process the newly added payload au‐
                     tomatically.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       vvooiidd **bbppff__rriinnggbbuuff__rreesseerrvvee((vvooiidd **_r_i_n_g_b_u_f,, uu6644 _s_i_z_e,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Reserve _s_i_z_e bytes of payload in a ring  buffer  _r_i_n_g_b_u_f.
                     _f_l_a_g_s must be 0.

              RReettuurrnn Valid  pointer with _s_i_z_e bytes of memory available; NULL,
                     otherwise.

       vvooiidd bbppff__rriinnggbbuuff__ssuubbmmiitt((vvooiidd **_d_a_t_a,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Submit reserved ring buffer sample, pointed to  by  _d_a_t_a.
                     If  BBPPFF__RRBB__NNOO__WWAAKKEEUUPP  is specified in _f_l_a_g_s, no notifica‐
                     tion   of   new   data   availability   is   sent.     If
                     BBPPFF__RRBB__FFOORRCCEE__WWAAKKEEUUPP  is  specified in _f_l_a_g_s, notification
                     of new data availability is sent unconditionally.   If  00
                     is  specified  in  _f_l_a_g_s, an adaptive notification of new
                     data availability is sent.

                     See 'bpf_ringbuf_output()' for the definition of adaptive
                     notification.

              RReettuurrnn Nothing. Always succeeds.

       vvooiidd bbppff__rriinnggbbuuff__ddiissccaarrdd((vvooiidd **_d_a_t_a,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Discard reserved ring buffer sample, pointed to by  _d_a_t_a.
                     If  BBPPFF__RRBB__NNOO__WWAAKKEEUUPP  is specified in _f_l_a_g_s, no notifica‐
                     tion   of   new   data   availability   is   sent.     If
                     BBPPFF__RRBB__FFOORRCCEE__WWAAKKEEUUPP  is  specified in _f_l_a_g_s, notification
                     of new data availability is sent unconditionally.   If  00
                     is  specified  in  _f_l_a_g_s, an adaptive notification of new
                     data availability is sent.

                     See 'bpf_ringbuf_output()' for the definition of adaptive
                     notification.

              RReettuurrnn Nothing. Always succeeds.

       uu6644 bbppff__rriinnggbbuuff__qquueerryy((vvooiidd **_r_i_n_g_b_u_f,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Query various characteristics of  provided  ring  buffer.
                     What exactly is queries is determined by _f_l_a_g_s:

                     • BBPPFF__RRBB__AAVVAAIILL__DDAATTAA: Amount of data not yet consumed.

                     • BBPPFF__RRBB__RRIINNGG__SSIIZZEE: The size of ring buffer.

                     • BBPPFF__RRBB__CCOONNSS__PPOOSS: Consumer position (can wrap around).

                     • BBPPFF__RRBB__PPRROODD__PPOOSS:   Producer(s)   position   (can   wrap
                       around).

                     Data returned is just a momentary snapshot of actual val‐
                     ues and could be inaccurate, so this facility  should  be
                     used  to  power heuristics and for reporting, not to make
                     100% correct calculation.

              RReettuurrnn Requested value, or 0, if _f_l_a_g_s are not recognized.

       lloonngg bbppff__ccssuumm__lleevveell((ssttrruucctt sskk__bbuuffff **_s_k_b,, uu6644 _l_e_v_e_l))

              DDeessccrriippttiioonn
                     Change the skbs checksum level by one layer up  or  down,
                     or  reset  it entirely to none in order to have the stack
                     perform checksum validation. The level is  applicable  to
                     the  following  protocols: TCP, UDP, GRE, SCTP, FCOE. For
                     example, a decap of | ETH | IP | UDP | GUE | IP |  TCP  |
                     into  |  ETH  |  IP | TCP | through bbppff__sskkbb__aaddjjuusstt__rroooomm()
                     helper with passing in BBPPFF__FF__AADDJJ__RROOOOMM__NNOO__CCSSUUMM__RREESSEETT  flag
                     would   require   one   call   to  bbppff__ccssuumm__lleevveell()  with
                     BBPPFF__CCSSUUMM__LLEEVVEELL__DDEECC since the UDP header is removed. Simi‐
                     larly, an encap of the latter into the  former  could  be
                     accompanied  by  a  helper  call to bbppff__ccssuumm__lleevveell() with
                     BBPPFF__CCSSUUMM__LLEEVVEELL__IINNCC if the skb is  still  intended  to  be
                     processed  in  higher layers of the stack instead of just
                     egressing at tc.

                     There are three supported level settings at this time:

                     • BBPPFF__CCSSUUMM__LLEEVVEELL__IINNCC: Increases skb->csum_level for  skbs
                       with CHECKSUM_UNNECESSARY.

                     • BBPPFF__CCSSUUMM__LLEEVVEELL__DDEECC:  Decreases skb->csum_level for skbs
                       with CHECKSUM_UNNECESSARY.

                     • BBPPFF__CCSSUUMM__LLEEVVEELL__RREESSEETT: Resets skb->csum_level to  0  and
                       sets  CHECKSUM_NONE to force checksum validation by the
                       stack.

                     • BBPPFF__CCSSUUMM__LLEEVVEELL__QQUUEERRYY:  No-op,   returns   the   current
                       skb->csum_level.

              RReettuurrnn 0  on success, or a negative error in case of failure. In
                     the   case   of   BBPPFF__CCSSUUMM__LLEEVVEELL__QQUUEERRYY,    the    current
                     skb->csum_level  is returned or the error code -EACCES in
                     case the skb is not subject to CHECKSUM_UNNECESSARY.

       ssttrruucctt ttccpp66__ssoocckk **bbppff__sskkcc__ttoo__ttccpp66__ssoocckk((vvooiidd **_s_k))

              DDeessccrriippttiioonn
                     Dynamically cast a _s_k pointer to a _t_c_p_6___s_o_c_k pointer.

              RReettuurrnn _s_k if casting is valid, or NNUULLLL otherwise.

       ssttrruucctt ttccpp__ssoocckk **bbppff__sskkcc__ttoo__ttccpp__ssoocckk((vvooiidd **_s_k))

              DDeessccrriippttiioonn
                     Dynamically cast a _s_k pointer to a _t_c_p___s_o_c_k pointer.

              RReettuurrnn _s_k if casting is valid, or NNUULLLL otherwise.

       ssttrruucctt ttccpp__ttiimmeewwaaiitt__ssoocckk **bbppff__sskkcc__ttoo__ttccpp__ttiimmeewwaaiitt__ssoocckk((vvooiidd **_s_k))

              DDeessccrriippttiioonn
                     Dynamically cast a  _s_k  pointer  to  a  _t_c_p___t_i_m_e_w_a_i_t___s_o_c_k
                     pointer.

              RReettuurrnn _s_k if casting is valid, or NNUULLLL otherwise.

       ssttrruucctt ttccpp__rreeqquueesstt__ssoocckk **bbppff__sskkcc__ttoo__ttccpp__rreeqquueesstt__ssoocckk((vvooiidd **_s_k))

              DDeessccrriippttiioonn
                     Dynamically  cast  a  _s_k  pointer  to  a _t_c_p___r_e_q_u_e_s_t___s_o_c_k
                     pointer.

              RReettuurrnn _s_k if casting is valid, or NNUULLLL otherwise.

       ssttrruucctt uuddpp66__ssoocckk **bbppff__sskkcc__ttoo__uuddpp66__ssoocckk((vvooiidd **_s_k))

              DDeessccrriippttiioonn
                     Dynamically cast a _s_k pointer to a _u_d_p_6___s_o_c_k pointer.

              RReettuurrnn _s_k if casting is valid, or NNUULLLL otherwise.

       lloonngg bbppff__ggeett__ttaasskk__ssttaacckk((ssttrruucctt ttaasskk__ssttrruucctt **_t_a_s_k,, vvooiidd **_b_u_f,, uu3322 _s_i_z_e,,
       uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Return a user or a kernel stack in bpf  program  provided
                     buffer.   Note:  the user stack will only be populated if
                     the _t_a_s_k is the current task; all other tasks will return
                     -EOPNOTSUPP.  To achieve this,  the  helper  needs  _t_a_s_k,
                     which  is a valid pointer to ssttrruucctt ttaasskk__ssttrruucctt. To store
                     the stacktrace, the bpf program provides _b_u_f with a  non‐
                     negative _s_i_z_e.

                     The  last  argument,  _f_l_a_g_s,  holds  the  number of stack
                     frames  to  skip   (from   0   to   255),   masked   with
                     BBPPFF__FF__SSKKIIPP__FFIIEELLDD__MMAASSKK.  The  next bits can be used to set
                     the following flags:

                     BBPPFF__FF__UUSSEERR__SSTTAACCKK
                            Collect a user space stack  instead  of  a  kernel
                            stack.  The _t_a_s_k must be the current task.

                     BBPPFF__FF__UUSSEERR__BBUUIILLDD__IIDD
                            Collect  buildid+offset  instead  of  ips for user
                            stack, only  valid  if  BBPPFF__FF__UUSSEERR__SSTTAACCKK  is  also
                            specified.

                     bbppff__ggeett__ttaasskk__ssttaacckk()      can      collect      up     to
                     PPEERRFF__MMAAXX__SSTTAACCKK__DDEEPPTTHH both kernel and user frames, subject
                     to sufficient large buffer size. Note that this limit can
                     be controlled with the ssyyssccttll program, and that it should
                     be manually increased  in  order  to  profile  long  user
                     stacks (such as stacks for Java programs). To do so, use:

                        # sysctl kernel.perf_event_max_stack=<new value>

              RReettuurrnn The  non-negative copied _b_u_f length equal to or less than
                     _s_i_z_e on success, or a negative error in case of failure.

       lloonngg bbppff__llooaadd__hhddrr__oopptt((ssttrruucctt bbppff__ssoocckk__ooppss **_s_k_o_p_s,, vvooiidd **_s_e_a_r_c_h_b_y___r_e_s,,
       uu3322 _l_e_n,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Load header option.  Support  reading  a  particular  TCP
                     header option for bpf program (BBPPFF__PPRROOGG__TTYYPPEE__SSOOCCKK__OOPPSS).

                     If  _f_l_a_g_s  is  0,  it  will  search  the  option from the
                     _s_k_o_p_s-->>sskkbb__ddaattaa.  The comment in ssttrruucctt bbppff__ssoocckk__ooppss  has
                     details   on   what  skb_data  contains  under  different
                     _s_k_o_p_s-->>oopp.

                     The first byte of the  _s_e_a_r_c_h_b_y___r_e_s  specifies  the  kind
                     that it wants to search.

                     If  the  searching kind is an experimental kind (i.e. 253
                     or 254 according to RFC6994).  It also needs  to  specify
                     the  "magic" which is either 2 bytes or 4 bytes.  It then
                     also needs to specify the size of the magic by using  the
                     2nd  byte  which  is "kind-length" of a TCP header option
                     and the "kind-length" also includes  the  first  2  bytes
                     "kind"  and  "kind-length"  itself as a normal TCP header
                     option also does.

                     For example, to search experimental kind 254 with 2  byte
                     magic  0xeB9F, the searchby_res should be [ 254, 4, 0xeB,
                     0x9F, 0, 0, .... 0 ].

                     To search for the standard window scale option  (3),  the
                     _s_e_a_r_c_h_b_y___r_e_s  should  be  [  3,  0,  0,  .... 0 ].  Note,
                     kind-length must be 0 for regular option.

                     Searching for No-Op (0) and  End-of-Option-List  (1)  are
                     not supported.

                     _l_e_n must be at least 2 bytes which is the minimal size of
                     a header option.

                     Supported flags:

                     • BBPPFF__LLOOAADD__HHDDRR__OOPPTT__TTCCPP__SSYYNN  to  search from the saved_syn
                       packet or the just-received syn packet.

              RReettuurrnn >  0  when  found,  the  header  option  is   copied   to
                     _s_e_a_r_c_h_b_y___r_e_s.   The  return  value  is  the  total length
                     copied. On failure, a negative error code is returned:

                     --EEIINNVVAALL if a parameter is invalid.

                     --EENNOOMMSSGG if the option is not found.

                     --EENNOOEENNTT   if   no   syn   packet   is   available    when
                     BBPPFF__LLOOAADD__HHDDRR__OOPPTT__TTCCPP__SSYYNN is used.

                     --EENNOOSSPPCC if there is not enough space.  Only _l_e_n number of
                     bytes are copied.

                     --EEFFAAUULLTT  on  failure  to  parse the header options in the
                     packet.

                     --EEPPEERRMM if the helper cannot be  used  under  the  current
                     _s_k_o_p_s-->>oopp.

       lloonngg bbppff__ssttoorree__hhddrr__oopptt((ssttrruucctt bbppff__ssoocckk__ooppss **_s_k_o_p_s,, ccoonnsstt vvooiidd **_f_r_o_m,,
       uu3322 _l_e_n,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Store header option.  The data will be copied from buffer
                     _f_r_o_m with length _l_e_n to the TCP header.

                     The  buffer  _f_r_o_m  should  have the whole option that in‐
                     cludes the kind, kind-length, and the actual option data.
                     The  _l_e_n  must  be  at  least  kind-length   long.    The
                     kind-length does not have to be 4 byte aligned.  The ker‐
                     nel will take care of the padding and setting the 4 bytes
                     aligned value to th->doff.

                     This helper will check for duplicated option by searching
                     the same option in the outgoing skb.

                     This     helper     can    only    be    called    during
                     BBPPFF__SSOOCCKK__OOPPSS__WWRRIITTEE__HHDDRR__OOPPTT__CCBB.

              RReettuurrnn 0 on success, or negative error in case of failure:

                     --EEIINNVVAALL If param is invalid.

                     --EENNOOSSPPCC if there is  not  enough  space  in  the  header.
                     Nothing has been written

                     --EEEEXXIISSTT if the option already exists.

                     --EEFFAAUULLTT on failure to parse the existing header options.

                     --EEPPEERRMM  if  the  helper  cannot be used under the current
                     _s_k_o_p_s-->>oopp.

       lloonngg bbppff__rreesseerrvvee__hhddrr__oopptt((ssttrruucctt bbppff__ssoocckk__ooppss **_s_k_o_p_s,, uu3322 _l_e_n,, uu6644
       _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Reserve _l_e_n bytes for the bpf header option.   The  space
                     will    be   used   by   bbppff__ssttoorree__hhddrr__oopptt()   later   in
                     BBPPFF__SSOOCCKK__OOPPSS__WWRRIITTEE__HHDDRR__OOPPTT__CCBB.

                     If bbppff__rreesseerrvvee__hhddrr__oopptt() is called  multiple  times,  the
                     total number of bytes will be reserved.

                     This     helper     can    only    be    called    during
                     BBPPFF__SSOOCCKK__OOPPSS__HHDDRR__OOPPTT__LLEENN__CCBB.

              RReettuurrnn 0 on success, or negative error in case of failure:

                     --EEIINNVVAALL if a parameter is invalid.

                     --EENNOOSSPPCC if there is not enough space in the header.

                     --EEPPEERRMM if the helper cannot be  used  under  the  current
                     _s_k_o_p_s-->>oopp.

       vvooiidd **bbppff__iinnooddee__ssttoorraaggee__ggeett((ssttrruucctt bbppff__mmaapp **_m_a_p,, vvooiidd **_i_n_o_d_e,, vvooiidd
       **_v_a_l_u_e,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Get a bpf_local_storage from an _i_n_o_d_e.

                     Logically,  it  could  be thought of as getting the value
                     from a _m_a_p with _i_n_o_d_e as the kkeeyy.  From this perspective,
                     the    usage    is    not     much     different     from
                     bbppff__mmaapp__llooookkuupp__eelleemm(_m_a_p,  &&_i_n_o_d_e)  except this helper en‐
                     forces the key must be an inode and the map must also  be
                     a BBPPFF__MMAAPP__TTYYPPEE__IINNOODDEE__SSTTOORRAAGGEE.

                     Underneath,  the value is stored locally at _i_n_o_d_e instead
                     of the _m_a_p.  The _m_a_p is  used  as  the  bpf-local-storage
                     "type".  The  bpf-local-storage  "type" (i.e. the _m_a_p) is
                     searched against all bpf_local_storage residing at _i_n_o_d_e.

                     An optional _f_l_a_g_s (BBPPFF__LLOOCCAALL__SSTTOORRAAGGEE__GGEETT__FF__CCRREEAATTEE) can be
                     used such that a new bpf_local_storage will be created if
                     one does not exist.  _v_a_l_u_e  can  be  used  together  with
                     BBPPFF__LLOOCCAALL__SSTTOORRAAGGEE__GGEETT__FF__CCRREEAATTEE  to  specify  the  initial
                     value of a bpf_local_storage.  If _v_a_l_u_e is NNUULLLL, the  new
                     bpf_local_storage will be zero initialized.

              RReettuurrnn A bpf_local_storage pointer is returned on success.

                     NNUULLLL  if  not found or there was an error in adding a new
                     bpf_local_storage.

       iinntt bbppff__iinnooddee__ssttoorraaggee__ddeelleettee((ssttrruucctt bbppff__mmaapp **_m_a_p,, vvooiidd **_i_n_o_d_e))

              DDeessccrriippttiioonn
                     Delete a bpf_local_storage from an _i_n_o_d_e.

              RReettuurrnn 0 on success.

                     --EENNOOEENNTT if the bpf_local_storage cannot be found.

       lloonngg bbppff__dd__ppaatthh((ssttrruucctt ppaatthh **_p_a_t_h,, cchhaarr **_b_u_f,, uu3322 _s_z))

              DDeessccrriippttiioonn
                     Return full path for  given  ssttrruucctt  ppaatthh  object,  which
                     needs  to  be the kernel BTF _p_a_t_h object. The path is re‐
                     turned in the provided buffer _b_u_f of size _s_z and is  zero
                     terminated.

              RReettuurrnn On  success,  the strictly positive length of the string,
                     including the trailing NUL character. On error,  a  nega‐
                     tive value.

       lloonngg bbppff__ccooppyy__ffrroomm__uusseerr((vvooiidd **_d_s_t,, uu3322 _s_i_z_e,, ccoonnsstt vvooiidd **_u_s_e_r___p_t_r))

              DDeessccrriippttiioonn
                     Read  _s_i_z_e  bytes  from  user  space address _u_s_e_r___p_t_r and
                     store  the  data  in  _d_s_t.   This   is   a   wrapper   of
                     ccooppyy__ffrroomm__uusseerr().

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__ssnnpprriinnttff__bbttff((cchhaarr **_s_t_r,, uu3322 _s_t_r___s_i_z_e,, ssttrruucctt bbttff__ppttrr **_p_t_r,, uu3322
       _b_t_f___p_t_r___s_i_z_e,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Use  BTF  to store a string representation of _p_t_r->ptr in
                     _s_t_r, using _p_t_r->type_id.  This value should  specify  the
                     type      that      _p_t_r->ptr      points     to.     LLVM
                     __builtin_btf_type_id(type, 1) can be used to look up vm‐
                     linux BTF type ids. Traversing the data  structure  using
                     BTF,  the  type  information and values are stored in the
                     first _s_t_r___s_i_z_e - 1  bytes  of  _s_t_r.   Safe  copy  of  the
                     pointer  data is carried out to avoid kernel crashes dur‐
                     ing operation.  Smaller types can use string space on the
                     stack; larger programs can use  map  data  to  store  the
                     string representation.

                     The  string can be subsequently shared with userspace via
                     bpf_perf_event_output()  or   ring   buffer   interfaces.
                     bpf_trace_printk()  is  to  be  avoided  as it places too
                     small a limit on string size to be useful.

                     _f_l_a_g_s is a combination of

                     BBTTFF__FF__CCOOMMPPAACCTT
                            no formatting around type information

                     BBTTFF__FF__NNOONNAAMMEE
                            no struct/union member names/types

                     BBTTFF__FF__PPTTRR__RRAAWW
                            show raw (unobfuscated) pointer values; equivalent
                            to printk specifier %px.

                     BBTTFF__FF__ZZEERROO
                            show zero-valued struct/union  members;  they  are
                            not displayed by default

              RReettuurrnn The number of bytes that were written (or would have been
                     written  if  output  had  to  be  truncated due to string
                     size), or a negative error in cases of failure.

       lloonngg bbppff__sseeqq__pprriinnttff__bbttff((ssttrruucctt sseeqq__ffiillee **_m,, ssttrruucctt bbttff__ppttrr **_p_t_r,, uu3322
       _p_t_r___s_i_z_e,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Use BTF to write to seq_write a string representation  of
                     _p_t_r->ptr,  using  _p_t_r->type_id as per bpf_snprintf_btf().
                     _f_l_a_g_s are identical to those used for bpf_snprintf_btf.

              RReettuurrnn 0 on success or a negative error in case of failure.

       uu6644 bbppff__sskkbb__ccggrroouupp__ccllaassssiidd((ssttrruucctt sskk__bbuuffff **_s_k_b))

              DDeessccrriippttiioonn
                     See bbppff__ggeett__ccggrroouupp__ccllaassssiidd() for  the  main  description.
                     This helper differs from bbppff__ggeett__ccggrroouupp__ccllaassssiidd() in that
                     the  cgroup  v1  net_cls class is retrieved only from the
                     _s_k_b's associated socket instead of the current process.

              RReettuurrnn The id is returned or 0 in case the id could not  be  re‐
                     trieved.

       lloonngg bbppff__rreeddiirreecctt__nneeiigghh((uu3322 _i_f_i_n_d_e_x,, ssttrruucctt bbppff__rreeddiirr__nneeiigghh **_p_a_r_a_m_s,,
       iinntt _p_l_e_n,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Redirect  the  packet  to  another  net  device  of index
                     _i_f_i_n_d_e_x and fill in L2 addresses from neighboring subsys‐
                     tem. This helper is somewhat similar  to  bbppff__rreeddiirreecctt(),
                     except  that  it populates L2 addresses as well, meaning,
                     internally, the helper relies on the neighbor lookup  for
                     the L2 address of the nexthop.

                     The  helper  will perform a FIB lookup based on the skb's
                     networking header to get the address of the next hop, un‐
                     less this is supplied by the caller in the  _p_a_r_a_m_s  argu‐
                     ment.  The  _p_l_e_n argument indicates the len of _p_a_r_a_m_s and
                     should be set to 0 if _p_a_r_a_m_s is NULL.

                     The _f_l_a_g_s argument is reserved and must be 0. The  helper
                     is currently only supported for tc BPF program types, and
                     enabled for IPv4 and IPv6 protocols.

              RReettuurrnn The   helper   returns   TTCC__AACCTT__RREEDDIIRREECCTT  on  success  or
                     TTCC__AACCTT__SSHHOOTT on error.

       vvooiidd **bbppff__ppeerr__ccppuu__ppttrr((ccoonnsstt vvooiidd **_p_e_r_c_p_u___p_t_r,, uu3322 _c_p_u))

              DDeessccrriippttiioonn
                     Take a pointer to a percpu ksym, _p_e_r_c_p_u___p_t_r, and return a
                     pointer to the percpu kernel variable on _c_p_u. A  ksym  is
                     an  extern  variable  decorated  with '__ksym'. For ksym,
                     there is a global var (either static or  global)  defined
                     of the same name in the kernel. The ksym is percpu if the
                     global var is percpu.  The returned pointer points to the
                     global percpu var on _c_p_u.

                     bpf_per_cpu_ptr()  has the same semantic as per_cpu_ptr()
                     in the kernel, except that bpf_per_cpu_ptr()  may  return
                     NULL.  This happens if _c_p_u is larger than nr_cpu_ids. The
                     caller  of  bpf_per_cpu_ptr()  must  check  the  returned
                     value.

              RReettuurrnn A  pointer pointing to the kernel percpu variable on _c_p_u,
                     or NULL, if _c_p_u is invalid.

       vvooiidd **bbppff__tthhiiss__ccppuu__ppttrr((ccoonnsstt vvooiidd **_p_e_r_c_p_u___p_t_r))

              DDeessccrriippttiioonn
                     Take a pointer to a percpu ksym, _p_e_r_c_p_u___p_t_r, and return a
                     pointer to the percpu kernel variable on  this  cpu.  See
                     the description of 'ksym' in bbppff__ppeerr__ccppuu__ppttrr().

                     bpf_this_cpu_ptr()    has    the    same    semantic   as
                     this_cpu_ptr()   in   the    kernel.    Different    from
                     bbppff__ppeerr__ccppuu__ppttrr(), it would never return NULL.

              RReettuurrnn A  pointer pointing to the kernel percpu variable on this
                     cpu.

       lloonngg bbppff__rreeddiirreecctt__ppeeeerr((uu3322 _i_f_i_n_d_e_x,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Redirect the  packet  to  another  net  device  of  index
                     _i_f_i_n_d_e_x.   This  helper  is somewhat similar to bbppff__rreeddii‐‐
                     rreecctt(),  except  that  the  redirection  happens  to  the
                     _i_f_i_n_d_e_x'  peer  device  and  the netns switch takes place
                     from ingress to ingress without going through  the  CPU's
                     backlog queue.

                     _s_k_b-->>mmaarrkk  and  _s_k_b-->>ttssttaammpp  are  not  cleared during the
                     netns switch.

                     The _f_l_a_g_s argument is reserved and must be 0. The  helper
                     is  currently  only supported for tc BPF program types at
                     the ingress hook and for veth and  netkit  target  device
                     types. The peer device must reside in a different network
                     namespace.

              RReettuurrnn The   helper   returns   TTCC__AACCTT__RREEDDIIRREECCTT  on  success  or
                     TTCC__AACCTT__SSHHOOTT on error.

       vvooiidd **bbppff__ttaasskk__ssttoorraaggee__ggeett((ssttrruucctt bbppff__mmaapp **_m_a_p,, ssttrruucctt ttaasskk__ssttrruucctt
       **_t_a_s_k,, vvooiidd **_v_a_l_u_e,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Get a bpf_local_storage from the _t_a_s_k.

                     Logically, it could be thought of as  getting  the  value
                     from  a _m_a_p with _t_a_s_k as the kkeeyy.  From this perspective,
                     the    usage    is    not     much     different     from
                     bbppff__mmaapp__llooookkuupp__eelleemm(_m_a_p,  &&_t_a_s_k)  except  this helper en‐
                     forces the key must be a task_struct  and  the  map  must
                     also be a BBPPFF__MMAAPP__TTYYPPEE__TTAASSKK__SSTTOORRAAGGEE.

                     Underneath,  the  value is stored locally at _t_a_s_k instead
                     of the _m_a_p.  The _m_a_p is  used  as  the  bpf-local-storage
                     "type".  The  bpf-local-storage  "type" (i.e. the _m_a_p) is
                     searched against all bpf_local_storage residing at _t_a_s_k.

                     An optional _f_l_a_g_s (BBPPFF__LLOOCCAALL__SSTTOORRAAGGEE__GGEETT__FF__CCRREEAATTEE) can be
                     used such that a new bpf_local_storage will be created if
                     one does not exist.  _v_a_l_u_e  can  be  used  together  with
                     BBPPFF__LLOOCCAALL__SSTTOORRAAGGEE__GGEETT__FF__CCRREEAATTEE  to  specify  the  initial
                     value of a bpf_local_storage.  If _v_a_l_u_e is NNUULLLL, the  new
                     bpf_local_storage will be zero initialized.

              RReettuurrnn A bpf_local_storage pointer is returned on success.

                     NNUULLLL  if  not found or there was an error in adding a new
                     bpf_local_storage.

       lloonngg bbppff__ttaasskk__ssttoorraaggee__ddeelleettee((ssttrruucctt bbppff__mmaapp **_m_a_p,, ssttrruucctt ttaasskk__ssttrruucctt
       **_t_a_s_k))

              DDeessccrriippttiioonn
                     Delete a bpf_local_storage from a _t_a_s_k.

              RReettuurrnn 0 on success.

                     --EENNOOEENNTT if the bpf_local_storage cannot be found.

       ssttrruucctt ttaasskk__ssttrruucctt **bbppff__ggeett__ccuurrrreenntt__ttaasskk__bbttff((vvooiidd))

              DDeessccrriippttiioonn
                     Return a BTF pointer to the "current" task.  This pointer
                     can  also   be   used   in   helpers   that   accept   an
                     _A_R_G___P_T_R___T_O___B_T_F___I_D of type _t_a_s_k___s_t_r_u_c_t.

              RReettuurrnn Pointer to the current task.

       lloonngg bbppff__bbpprrmm__ooppttss__sseett((ssttrruucctt lliinnuuxx__bbiinnpprrmm **_b_p_r_m,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Set or clear certain options on _b_p_r_m:

                     BBPPFF__FF__BBPPRRMM__SSEECCUURREEEEXXEECC  Set  the secureexec bit which sets
                     the AATT__SSEECCUURREE auxv for glibc. The bit is cleared  if  the
                     flag is not specified.

              RReettuurrnn --EEIINNVVAALL if invalid _f_l_a_g_s are passed, zero otherwise.

       uu6644 bbppff__kkttiimmee__ggeett__ccooaarrssee__nnss((vvooiidd))

              DDeessccrriippttiioonn
                     Return a coarse-grained version of the time elapsed since
                     system  boot,  in  nanoseconds. Does not include time the
                     system was suspended.

                     See: cclloocckk__ggeettttiimmee(CCLLOOCCKK__MMOONNOOTTOONNIICC__CCOOAARRSSEE)

              RReettuurrnn Current _k_t_i_m_e.

       lloonngg bbppff__iimmaa__iinnooddee__hhaasshh((ssttrruucctt iinnooddee **_i_n_o_d_e,, vvooiidd **_d_s_t,, uu3322 _s_i_z_e))

              DDeessccrriippttiioonn
                     Returns the stored IMA hash of the _i_n_o_d_e (if it's  avail‐
                     able).   If  the hash is larger than _s_i_z_e, then only _s_i_z_e
                     bytes will be copied to _d_s_t

              RReettuurrnn The hhaasshh__aallggoo is returned on success, --EEOOPPNNOOTTSSUUPPPP if  IMA
                     is disabled or --EEIINNVVAALL if invalid arguments are passed.

       ssttrruucctt ssoocckkeett **bbppff__ssoocckk__ffrroomm__ffiillee((ssttrruucctt ffiillee **_f_i_l_e))

              DDeessccrriippttiioonn
                     If  the given file represents a socket, returns the asso‐
                     ciated socket.

              RReettuurrnn A pointer to a struct socket on success or  NULL  if  the
                     file is not a socket.

       lloonngg bbppff__cchheecckk__mmttuu((vvooiidd **_c_t_x,, uu3322 _i_f_i_n_d_e_x,, uu3322 **_m_t_u___l_e_n,, ss3322 _l_e_n___d_i_f_f,,
       uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Check  packet  size  against  exceeding MTU of net device
                     (based on _i_f_i_n_d_e_x).  This helper will likely be  used  in
                     combination  with  helpers  that adjust/change the packet
                     size.

                     The argument _l_e_n___d_i_f_f can be used  for  querying  with  a
                     planned  size  change.  This allows to check MTU prior to
                     changing packet ctx. Providing a _l_e_n___d_i_f_f adjustment that
                     is larger than the actual packet size (resulting in nega‐
                     tive packet size) will in principle not exceed  the  MTU,
                     which  is  why it is not considered a failure.  Other BPF
                     helpers  are  needed  for  performing  the  planned  size
                     change; therefore the responsibility for catching a nega‐
                     tive packet size belongs in those helpers.

                     Specifying  _i_f_i_n_d_e_x zero means the MTU check is performed
                     against the current net device.   This  is  practical  if
                     this isn't used prior to redirect.

                     On  input  _m_t_u___l_e_n must be a valid pointer, else verifier
                     will reject BPF program.  If the value  _m_t_u___l_e_n  is  ini‐
                     tialized  to  zero then the ctx packet size is use.  When
                     value _m_t_u___l_e_n is provided as input this  specify  the  L3
                     length  that  the MTU check is done against. Remember XDP
                     and TC length operate at L2, but this value is L3 as this
                     correlate to MTU and IP-header tot_len values  which  are
                     L3 (similar behavior as bpf_fib_lookup).

                     The Linux kernel route table can configure MTUs on a more
                     specific  per  route level, which is not provided by this
                     helper.    For   route   level   MTU   checks   use   the
                     bbppff__ffiibb__llooookkuupp() helper.

                     _c_t_x  is  either  ssttrruucctt xxddpp__mmdd for XDP programs or ssttrruucctt
                     sskk__bbuuffff for tc cls_act programs.

                     The _f_l_a_g_s argument can be a combination of one or more of
                     the following values:

                     BBPPFF__MMTTUU__CCHHKK__SSEEGGSS
                            This flag will only works for _c_t_x ssttrruucctt  sskk__bbuuffff.
                            If  packet  context  contains extra packet segment
                            buffers (often knows as GSO skb), then  MTU  check
                            is  harder  to  check  at  this  point, because in
                            transmit path it is possible for the skb packet to
                            get re-segmented (depending  on  net  device  fea‐
                            tures).   This  could still be a MTU violation, so
                            this flag enables  performing  MTU  check  against
                            segments,  with  a different violation return code
                            to tell it apart. Check cannot use len_diff.

                     On return _m_t_u___l_e_n pointer contains the MTU value  of  the
                     net  device.   Remember  the net device configured MTU is
                     the L3 size, which is returned here and XDP and TC length
                     operate at L2.  Helper take this into  account  for  you,
                     but remember when using MTU value in your BPF-code.

              RReettuurrnn

                     • 0  on  success,  and  populate  MTU  value  in  _m_t_u___l_e_n
                       pointer.

                     • < 0 if any input argument is invalid (_m_t_u___l_e_n  not  up‐
                       dated)

                     MTU  violations return positive values, but also populate
                     MTU value in _m_t_u___l_e_n pointer, as this can be  needed  for
                     implementing PMTU handing:

                     • BBPPFF__MMTTUU__CCHHKK__RREETT__FFRRAAGG__NNEEEEDDEEDD

                     • BBPPFF__MMTTUU__CCHHKK__RREETT__SSEEGGSS__TTOOOOBBIIGG

       lloonngg bbppff__ffoorr__eeaacchh__mmaapp__eelleemm((ssttrruucctt bbppff__mmaapp **_m_a_p,, vvooiidd **_c_a_l_l_b_a_c_k___f_n,, vvooiidd
       **_c_a_l_l_b_a_c_k___c_t_x,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     For  each  element in mmaapp, call ccaallllbbaacckk__ffnn function with
                     mmaapp, ccaallllbbaacckk__ccttxx and other map-specific parameters.  The
                     ccaallllbbaacckk__ffnn should be a static  function  and  the  ccaallll‐‐
                     bbaacckk__ccttxx  should be a pointer to the stack.  The ffllaaggss is
                     used to control certain  aspects  of  the  helper.   Cur‐
                     rently, the ffllaaggss must be 0.

                     The following are a list of supported map types and their
                     respective expected callback signatures:

                     BPF_MAP_TYPE_HASH,              BPF_MAP_TYPE_PERCPU_HASH,
                     BPF_MAP_TYPE_LRU_HASH,      BPF_MAP_TYPE_LRU_PERCPU_HASH,
                     BPF_MAP_TYPE_ARRAY, BPF_MAP_TYPE_PERCPU_ARRAY

                     long (*callback_fn)(struct bpf_map *map, const void *key,
                     void *value, void *ctx);

                     For  per_cpu  maps, the map_value is the value on the cpu
                     where the bpf_prog is running.

                     If ccaallllbbaacckk__ffnn return 0, the helper will continue to  the
                     next  element. If return value is 1, the helper will skip
                     the rest of elements and return. Other return values  are
                     not used now.

              RReettuurrnn The number of traversed map elements for success, --EEIINNVVAALL
                     for invalid ffllaaggss.

       lloonngg bbppff__ssnnpprriinnttff((cchhaarr **_s_t_r,, uu3322 _s_t_r___s_i_z_e,, ccoonnsstt cchhaarr **_f_m_t,, uu6644 **_d_a_t_a,,
       uu3322 _d_a_t_a___l_e_n))

              DDeessccrriippttiioonn
                     Outputs  a  string  into  the ssttrr buffer of size ssttrr__ssiizzee
                     based on a  format  string  stored  in  a  read-only  map
                     pointed by ffmmtt.

                     Each  format specifier in ffmmtt corresponds to one u64 ele‐
                     ment in the ddaattaa array. For strings  and  pointers  where
                     pointees are accessed, only the pointer values are stored
                     in  the  _d_a_t_a  array. The _d_a_t_a___l_e_n is the size of _d_a_t_a in
                     bytes - must be a multiple of 8.

                     Formats %%ss and %%pp{{ii,,II}}{{44,,66}} require to read  kernel  mem‐
                     ory. Reading kernel memory may fail due to either invalid
                     address  or  valid  address  but requiring a major memory
                     fault. If reading kernel memory fails, the string for  %%ss
                     will   be  an  empty  string,  and  the  ip  address  for
                     %%pp{{ii,,II}}{{44,,66}} will be 0.  Not returning error to bpf  pro‐
                     gram  is consistent with what bbppff__ttrraaccee__pprriinnttkk() does for
                     now.

              RReettuurrnn The strictly positive length of the formatted string, in‐
                     cluding the trailing zero character. If the return  value
                     is  greater  than  ssttrr__ssiizzee,  ssttrr  contains  a  truncated
                     string, guaranteed  to  be  zero-terminated  except  when
                     ssttrr__ssiizzee is 0.

                     Or --EEBBUUSSYY if the per-CPU memory copy buffer is busy.

       lloonngg bbppff__ssyyss__bbppff((uu3322 _c_m_d,, vvooiidd **_a_t_t_r,, uu3322 _a_t_t_r___s_i_z_e))

              DDeessccrriippttiioonn
                     Execute bpf syscall with given arguments.

              RReettuurrnn A syscall result.

       lloonngg bbppff__bbttff__ffiinndd__bbyy__nnaammee__kkiinndd((cchhaarr **_n_a_m_e,, iinntt _n_a_m_e___s_z,, uu3322 _k_i_n_d,, iinntt
       _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Find  BTF type with given name and kind in vmlinux BTF or
                     in module's BTFs.

              RReettuurrnn Returns btf_id and btf_obj_fd in lower and upper 32 bits.

       lloonngg bbppff__ssyyss__cclloossee((uu3322 _f_d))

              DDeessccrriippttiioonn
                     Execute close syscall for given FD.

              RReettuurrnn A syscall result.

       lloonngg bbppff__ttiimmeerr__iinniitt((ssttrruucctt bbppff__ttiimmeerr **_t_i_m_e_r,, ssttrruucctt bbppff__mmaapp **_m_a_p,, uu6644
       _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Initialize the timer.  First  4  bits  of  _f_l_a_g_s  specify
                     clockid.      Only    CLOCK_MONOTONIC,    CLOCK_REALTIME,
                     CLOCK_BOOTTIME are allowed.  All other bits of _f_l_a_g_s  are
                     reserved.   The verifier will reject the program if _t_i_m_e_r
                     is not from the same _m_a_p.

              RReettuurrnn 0 on success.  --EEBBUUSSYY if _t_i_m_e_r  is  already  initialized.
                     --EEIINNVVAALL  if invalid _f_l_a_g_s are passed.  --EEPPEERRMM if _t_i_m_e_r is
                     in a map that doesn't have any user references.  The user
                     space should either hold a file descriptor to a map  with
                     timers  or pin such map in bpffs. When map is unpinned or
                     file descriptor is closed all timers in the map  will  be
                     cancelled and freed.

       lloonngg bbppff__ttiimmeerr__sseett__ccaallllbbaacckk((ssttrruucctt bbppff__ttiimmeerr **_t_i_m_e_r,, vvooiidd **_c_a_l_l_b_a_c_k___f_n))

              DDeessccrriippttiioonn
                     Configure the timer to call _c_a_l_l_b_a_c_k___f_n static function.

              RReettuurrnn 0  on success.  --EEIINNVVAALL if _t_i_m_e_r was not initialized with
                     bpf_timer_init() earlier.  --EEPPEERRMM if _t_i_m_e_r is  in  a  map
                     that  doesn't  have  any user references.  The user space
                     should either hold a file descriptor to a map with timers
                     or pin such map in bpffs. When map is  unpinned  or  file
                     descriptor  is  closed all timers in the map will be can‐
                     celled and freed.

       lloonngg bbppff__ttiimmeerr__ssttaarrtt((ssttrruucctt bbppff__ttiimmeerr **_t_i_m_e_r,, uu6644 _n_s_e_c_s,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Set timer expiration N nanoseconds from the current time.
                     The configured callback will be invoked in soft irq  con‐
                     text  on  some  cpu  and  will  not repeat unless another
                     bpf_timer_start() is made.  In such case the next invoca‐
                     tion can  migrate  to  a  different  cpu.   Since  struct
                     bpf_timer  is a field inside map element the map owns the
                     timer. The bpf_timer_set_callback() will increment refcnt
                     of BPF program to make sure that callback_fn  code  stays
                     valid.   When  user space reference to a map reaches zero
                     all timers in a map are cancelled and corresponding  pro‐
                     gram's refcnts are decremented. This is done to make sure
                     that  Ctrl-C  of  a user process doesn't leave any timers
                     running. If map is pinned in bpffs  the  callback_fn  can
                     re-arm itself indefinitely.  bpf_map_update/delete_elem()
                     helpers  and  user space sys_bpf commands cancel and free
                     the timer in the given map element.  The map can  contain
                     timers that invoke callback_fn-s from different programs.
                     The same callback_fn can serve different timers from dif‐
                     ferent  maps  if  key/value  layout  matches across maps.
                     Every bpf_timer_set_callback() can have  different  call‐
                     back_fn.

                     _f_l_a_g_s can be one of:

                     BBPPFF__FF__TTIIMMEERR__AABBSS
                            Start  the  timer in absolute expire value instead
                            of the default relative one.

                     BBPPFF__FF__TTIIMMEERR__CCPPUU__PPIINN
                            Timer will be pinned to the CPU of the caller.

              RReettuurrnn 0 on success.  --EEIINNVVAALL if _t_i_m_e_r was not initialized  with
                     bpf_timer_init() earlier or invalid _f_l_a_g_s are passed.

       lloonngg bbppff__ttiimmeerr__ccaanncceell((ssttrruucctt bbppff__ttiimmeerr **_t_i_m_e_r))

              DDeessccrriippttiioonn
                     Cancel the timer and wait for callback_fn to finish if it
                     was running.

              RReettuurrnn 0  if  the  timer was not active.  1 if the timer was ac‐
                     tive.   --EEIINNVVAALL  if  _t_i_m_e_r  was  not   initialized   with
                     bpf_timer_init()  earlier.  --EEDDEEAADDLLKK if callback_fn tried
                     to call bpf_timer_cancel() on its own timer  which  would
                     have led to a deadlock otherwise.

       uu6644 bbppff__ggeett__ffuunncc__iipp((vvooiidd **_c_t_x))

              DDeessccrriippttiioonn
                     Get  address  of  the  traced  function  (for tracing and
                     kprobe programs).

                     When called for kprobe program attached as uprobe it  re‐
                     turns probe address for both entry and return uprobe.

              RReettuurrnn Address of the traced function for kprobe.  0 for kprobes
                     placed  within  the function (not at the entry).  Address
                     of the probe for uprobe and return uprobe.

       uu6644 bbppff__ggeett__aattttaacchh__ccooookkiiee((vvooiidd **_c_t_x))

              DDeessccrriippttiioonn
                     Get bpf_cookie value  provided  (optionally)  during  the
                     program  attachment. It might be different for each indi‐
                     vidual attachment, even if  BPF  program  itself  is  the
                     same.   Expects  BPF program context _c_t_x as a first argu‐
                     ment.

                     SSuuppppoorrtteedd ffoorr tthhee ffoolllloowwiinngg pprrooggrraamm ttyyppeess::

                            • kprobe/uprobe;

                            • tracepoint;

                            • perf_event.

              RReettuurrnn Value specified by user at BPF  link  creation/attachment
                     time or 0, if it was not specified.

       lloonngg bbppff__ttaasskk__pptt__rreeggss((ssttrruucctt ttaasskk__ssttrruucctt **_t_a_s_k))

              DDeessccrriippttiioonn
                     Get the struct pt_regs associated with ttaasskk.

              RReettuurrnn A pointer to struct pt_regs.

       lloonngg bbppff__ggeett__bbrraanncchh__ssnnaappsshhoott((vvooiidd **_e_n_t_r_i_e_s,, uu3322 _s_i_z_e,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Get  branch  trace  from hardware engines like Intel LBR.
                     The hardware engine is stopped shortly after  the  helper
                     is  called. Therefore, the user need to filter branch en‐
                     tries based on the actual use  case.  To  capture  branch
                     trace  before  the  trigger point of the BPF program, the
                     helper should be called at the beginning of the BPF  pro‐
                     gram.

                     The  data is stored as struct perf_branch_entry into out‐
                     put buffer _e_n_t_r_i_e_s. _s_i_z_e is the size of _e_n_t_r_i_e_s in bytes.
                     _f_l_a_g_s is reserved for now and must be zero.

              RReettuurrnn On success, number of bytes written to _b_u_f. On  error,  a
                     negative value.

                     --EEIINNVVAALL if _f_l_a_g_s is not zero.

                     --EENNOOEENNTT if architecture does not support branch records.

       lloonngg bbppff__ttrraaccee__vvpprriinnttkk((ccoonnsstt cchhaarr **_f_m_t,, uu3322 _f_m_t___s_i_z_e,, ccoonnsstt vvooiidd **_d_a_t_a,,
       uu3322 _d_a_t_a___l_e_n))

              DDeessccrriippttiioonn
                     Behaves  like bbppff__ttrraaccee__pprriinnttkk() helper, but takes an ar‐
                     ray of u64 to format and can handle more format args as a
                     result.

                     Arguments are to be used as in bbppff__sseeqq__pprriinnttff() helper.

              RReettuurrnn The number of bytes written to the buffer, or a  negative
                     error in case of failure.

       ssttrruucctt uunniixx__ssoocckk **bbppff__sskkcc__ttoo__uunniixx__ssoocckk((vvooiidd **_s_k))

              DDeessccrriippttiioonn
                     Dynamically cast a _s_k pointer to a _u_n_i_x___s_o_c_k pointer.

              RReettuurrnn _s_k if casting is valid, or NNUULLLL otherwise.

       lloonngg bbppff__kkaallllssyymmss__llooookkuupp__nnaammee((ccoonnsstt cchhaarr **_n_a_m_e,, iinntt _n_a_m_e___s_z,, iinntt _f_l_a_g_s,,
       uu6644 **_r_e_s))

              DDeessccrriippttiioonn
                     Get  the address of a kernel symbol, returned in _r_e_s. _r_e_s
                     is set to 0 if the symbol is not found.

              RReettuurrnn On success, zero. On error, a negative value.

                     --EEIINNVVAALL if _f_l_a_g_s is not zero.

                     --EEIINNVVAALL if string _n_a_m_e is not the same size as _n_a_m_e___s_z.

                     --EENNOOEENNTT if symbol is not found.

                     --EEPPEERRMM if caller does not have permission to obtain  ker‐
                     nel address.

       lloonngg bbppff__ffiinndd__vvmmaa((ssttrruucctt ttaasskk__ssttrruucctt **_t_a_s_k,, uu6644 _a_d_d_r,, vvooiidd **_c_a_l_l_‐
       _b_a_c_k___f_n,, vvooiidd **_c_a_l_l_b_a_c_k___c_t_x,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Find  vma  of  _t_a_s_k  that contains _a_d_d_r, call _c_a_l_l_b_a_c_k___f_n
                     function with _t_a_s_k, _v_m_a,  and  _c_a_l_l_b_a_c_k___c_t_x.   The  _c_a_l_l_‐
                     _b_a_c_k___f_n  should be a static function and the _c_a_l_l_b_a_c_k___c_t_x
                     should be a pointer to the stack.  The _f_l_a_g_s is  used  to
                     control  certain  aspects  of the helper.  Currently, the
                     _f_l_a_g_s must be 0.

                     The expected callback signature is

                     long  (*callback_fn)(struct  task_struct  *task,   struct
                     vm_area_struct *vma, void *callback_ctx);

              RReettuurrnn 0  on  success.   --EENNOOEENNTT  if _t_a_s_k_-_>_m_m is NULL, or no vma
                     contains _a_d_d_r.  --EEBBUUSSYY if failed to try  lock  mmap_lock.
                     --EEIINNVVAALL for invalid ffllaaggss.

       lloonngg bbppff__lloooopp((uu3322 _n_r___l_o_o_p_s,, vvooiidd **_c_a_l_l_b_a_c_k___f_n,, vvooiidd **_c_a_l_l_b_a_c_k___c_t_x,, uu6644
       _f_l_a_g_s))

              DDeessccrriippttiioonn
                     For nnrr__llooooppss, call ccaallllbbaacckk__ffnn function with ccaallllbbaacckk__ccttxx
                     as  the  context  parameter.  The ccaallllbbaacckk__ffnn should be a
                     static function and the ccaallllbbaacckk__ccttxx should be a  pointer
                     to  the  stack.  The ffllaaggss is used to control certain as‐
                     pects of the helper.  Currently, the  ffllaaggss  must  be  0.
                     Currently,  nr_loops  is  limited to 1 << 23 (˜8 million)
                     loops.

                     long (*callback_fn)(u64 index, void *ctx);

                     where iinnddeexx is the current index in the loop.  The  index
                     is zero-indexed.

                     If ccaallllbbaacckk__ffnn returns 0, the helper will continue to the
                     next loop. If return value is 1, the helper will skip the
                     rest of the loops and return. Other return values are not
                     used now, and will be rejected by the verifier.

              RReettuurrnn The number of loops performed, --EEIINNVVAALL for invalid ffllaaggss,
                     --EE22BBIIGG if nnrr__llooooppss exceeds the maximum number of loops.

       lloonngg bbppff__ssttrrnnccmmpp((ccoonnsstt cchhaarr **_s_1,, uu3322 _s_1___s_z,, ccoonnsstt cchhaarr **_s_2))

              DDeessccrriippttiioonn
                     Do  strncmp()  between  ss11  and ss22. ss11 doesn't need to be
                     null-terminated and ss11__sszz is the maximum storage size  of
                     ss11. ss22 must be a read-only string.

              RReettuurrnn An  integer  less than, equal to, or greater than zero if
                     the first ss11__sszz bytes of ss11 is found to be less than,  to
                     match, or be greater than ss22.

       lloonngg bbppff__ggeett__ffuunncc__aarrgg((vvooiidd **_c_t_x,, uu3322 _n,, uu6644 **_v_a_l_u_e))

              DDeessccrriippttiioonn
                     Get  nn-th  argument  register  (zero based) of the traced
                     function (for tracing programs) returned in vvaalluuee.

              RReettuurrnn 0 on success.  --EEIINNVVAALL if n >= argument register count of
                     traced function.

       lloonngg bbppff__ggeett__ffuunncc__rreett((vvooiidd **_c_t_x,, uu6644 **_v_a_l_u_e))

              DDeessccrriippttiioonn
                     Get return value of the traced function (for tracing pro‐
                     grams) in vvaalluuee.

              RReettuurrnn 0 on success.  --EEOOPPNNOOTTSSUUPPPP  for  tracing  programs  other
                     than BPF_TRACE_FEXIT or BPF_MODIFY_RETURN.

       lloonngg bbppff__ggeett__ffuunncc__aarrgg__ccnntt((vvooiidd **_c_t_x))

              DDeessccrriippttiioonn
                     Get number of registers of the traced function (for trac‐
                     ing  programs)  where  function  arguments  are stored in
                     these registers.

              RReettuurrnn The number of argument registers of the traced function.

       iinntt bbppff__ggeett__rreettvvaall((vvooiidd))

              DDeessccrriippttiioonn
                     Get the BPF program's return value that will be  returned
                     to the upper layers.

                     This helper is currently supported by cgroup programs and
                     only by the hooks where BPF program's return value is re‐
                     turned to the userspace via errno.

              RReettuurrnn The BPF program's return value.

       iinntt bbppff__sseett__rreettvvaall((iinntt _r_e_t_v_a_l))

              DDeessccrriippttiioonn
                     Set  the BPF program's return value that will be returned
                     to the upper layers.

                     This helper is currently supported by cgroup programs and
                     only by the hooks where BPF program's return value is re‐
                     turned to the userspace via errno.

                     Note that there is the following corner  case  where  the
                     program  exports  an error via bpf_set_retval but signals
                     success via 'return 1':
                        bpf_set_retval(-EPERM); return 1;

                     In this case, the BPF program's  return  value  will  use
                     helper's    -EPERM.    This    still   holds   true   for
                     cgroup/bind{4,6} which supports extra 'return 3'  success
                     case.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       uu6644 bbppff__xxddpp__ggeett__bbuuffff__lleenn((ssttrruucctt xxddpp__bbuuffff **_x_d_p___m_d))

              DDeessccrriippttiioonn
                     Get  the total size of a given xdp buff (linear and paged
                     area)

              RReettuurrnn The total size of a given xdp buffer.

       lloonngg bbppff__xxddpp__llooaadd__bbyytteess((ssttrruucctt xxddpp__bbuuffff **_x_d_p___m_d,, uu3322 _o_f_f_s_e_t,, vvooiidd **_b_u_f,,
       uu3322 _l_e_n))

              DDeessccrriippttiioonn
                     This helper is provided as an easy way to load data  from
                     a  xdp buffer. It can be used to load _l_e_n bytes from _o_f_f_‐
                     _s_e_t from the frame associated to _x_d_p___m_d, into the  buffer
                     pointed by _b_u_f.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__xxddpp__ssttoorree__bbyytteess((ssttrruucctt xxddpp__bbuuffff **_x_d_p___m_d,, uu3322 _o_f_f_s_e_t,, vvooiidd
       **_b_u_f,, uu3322 _l_e_n))

              DDeessccrriippttiioonn
                     Store _l_e_n bytes from buffer _b_u_f into the frame associated
                     to _x_d_p___m_d, at _o_f_f_s_e_t.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       lloonngg bbppff__ccooppyy__ffrroomm__uusseerr__ttaasskk((vvooiidd **_d_s_t,, uu3322 _s_i_z_e,, ccoonnsstt vvooiidd **_u_s_e_r___p_t_r,,
       ssttrruucctt ttaasskk__ssttrruucctt **_t_s_k,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Read _s_i_z_e bytes from user space address _u_s_e_r___p_t_r in _t_s_k's
                     address  space,  and stores the data in _d_s_t. _f_l_a_g_s is not
                     used yet and is provided for future  extensibility.  This
                     helper can only be used by sleepable programs.

              RReettuurrnn 0  on success, or a negative error in case of failure. On
                     error _d_s_t buffer is zeroed out.

       lloonngg bbppff__sskkbb__sseett__ttssttaammpp((ssttrruucctt sskk__bbuuffff **_s_k_b,, uu6644 _t_s_t_a_m_p,, uu3322
       _t_s_t_a_m_p___t_y_p_e))

              DDeessccrriippttiioonn
                     Change the __sk_buff->tstamp_type to _t_s_t_a_m_p___t_y_p_e and  set
                     _t_s_t_a_m_p to the __sk_buff->tstamp together.

                     If there is no need to change the __sk_buff->tstamp_type,
                     the   tstamp   value   can   be   directly   written   to
                     __sk_buff->tstamp instead.

                     BPF_SKB_TSTAMP_DELIVERY_MONO is the only tstamp that will
                     be kept during bpf_redirect_*().  A non zero _t_s_t_a_m_p  must
                     be    used    with    the    BPF_SKB_TSTAMP_DELIVERY_MONO
                     _t_s_t_a_m_p___t_y_p_e.

                     A BPF_SKB_TSTAMP_UNSPEC _t_s_t_a_m_p___t_y_p_e can only be used with
                     a zero _t_s_t_a_m_p.

                     Only IPv4 and IPv6 skb->protocol are supported.

                     This function is most useful when it needs to set a  mono
                     delivery  time  to  __sk_buff->tstamp  and then bpf_redi‐
                     rect_*() to the egress of an iface.  For example,  chang‐
                     ing  the  (rcv) timestamp in __sk_buff->tstamp at ingress
                     to a mono delivery  time  and  then  bpf_redirect_*()  to
                     <sch_fq@phy-dev> .

              RReettuurrnn 0  on success.  --EEIINNVVAALL for invalid input --EEOOPPNNOOTTSSUUPPPP for
                     unsupported protocol

       lloonngg bbppff__iimmaa__ffiillee__hhaasshh((ssttrruucctt ffiillee **_f_i_l_e,, vvooiidd **_d_s_t,, uu3322 _s_i_z_e))

              DDeessccrriippttiioonn
                     Returns a calculated IMA hash of the _f_i_l_e.  If  the  hash
                     is  larger than _s_i_z_e, then only _s_i_z_e bytes will be copied
                     to _d_s_t

              RReettuurrnn The hhaasshh__aallggoo is returned on success, --EEOOPPNNOOTTSSUUPPPP if  the
                     hash  calculation  failed or --EEIINNVVAALL if invalid arguments
                     are passed.

       vvooiidd **bbppff__kkppttrr__xxcchhgg((vvooiidd **_d_s_t,, vvooiidd **_p_t_r))

              DDeessccrriippttiioonn
                     Exchange kptr at pointer _d_s_t with _p_t_r, and return the old
                     value.  _d_s_t can be map value or local kptr.  _p_t_r  can  be
                     NULL,  otherwise  it  must  be a referenced pointer which
                     will be released when this helper is called.

              RReettuurrnn The old value of kptr (which can be NULL).  The  returned
                     pointer  if  not  NULL,  is a reference which must be re‐
                     leased using its corresponding release function, or moved
                     into a BPF map before program exit.

       vvooiidd **bbppff__mmaapp__llooookkuupp__ppeerrccppuu__eelleemm((ssttrruucctt bbppff__mmaapp **_m_a_p,, ccoonnsstt vvooiidd **_k_e_y,,
       uu3322 _c_p_u))

              DDeessccrriippttiioonn
                     Perform a lookup in _p_e_r_c_p_u _m_a_p for an entry associated to
                     _k_e_y on _c_p_u.

              RReettuurrnn Map value associated to _k_e_y on _c_p_u, or NNUULLLL if  no  entry
                     was found or _c_p_u is invalid.

       ssttrruucctt mmppttccpp__ssoocckk **bbppff__sskkcc__ttoo__mmppttccpp__ssoocckk((vvooiidd **_s_k))

              DDeessccrriippttiioonn
                     Dynamically cast a _s_k pointer to a _m_p_t_c_p___s_o_c_k pointer.

              RReettuurrnn _s_k if casting is valid, or NNUULLLL otherwise.

       lloonngg bbppff__ddyynnppttrr__ffrroomm__mmeemm((vvooiidd **_d_a_t_a,, uu3322 _s_i_z_e,, uu6644 _f_l_a_g_s,, ssttrruucctt
       bbppff__ddyynnppttrr **_p_t_r))

              DDeessccrriippttiioonn
                     Get a dynptr to local memory _d_a_t_a.

                     _d_a_t_a must be a ptr to a map value.  The maximum _s_i_z_e sup‐
                     ported is DYNPTR_MAX_SIZE.  _f_l_a_g_s is currently unused.

              RReettuurrnn 0 on success, -E2BIG if the size exceeds DYNPTR_MAX_SIZE,
                     -EINVAL if flags is not 0.

       lloonngg bbppff__rriinnggbbuuff__rreesseerrvvee__ddyynnppttrr((vvooiidd **_r_i_n_g_b_u_f,, uu3322 _s_i_z_e,, uu6644 _f_l_a_g_s,,
       ssttrruucctt bbppff__ddyynnppttrr **_p_t_r))

              DDeessccrriippttiioonn
                     Reserve  _s_i_z_e  bytes  of payload in a ring buffer _r_i_n_g_b_u_f
                     through the dynptr interface. _f_l_a_g_s must be 0.

                     Please  note  that   a   corresponding   bpf_ringbuf_sub‐
                     mit_dynptr  or  bpf_ringbuf_discard_dynptr must be called
                     on _p_t_r, even if the reservation fails. This  is  enforced
                     by the verifier.

              RReettuurrnn 0 on success, or a negative error in case of failure.

       vvooiidd bbppff__rriinnggbbuuff__ssuubbmmiitt__ddyynnppttrr((ssttrruucctt bbppff__ddyynnppttrr **_p_t_r,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Submit  reserved  ring buffer sample, pointed to by _d_a_t_a,
                     through the dynptr interface. This  is  a  no-op  if  the
                     dynptr is invalid/null.

                     For  more  information  on  _f_l_a_g_s,  please see 'bpf_ring‐
                     buf_submit'.

              RReettuurrnn Nothing. Always succeeds.

       vvooiidd bbppff__rriinnggbbuuff__ddiissccaarrdd__ddyynnppttrr((ssttrruucctt bbppff__ddyynnppttrr **_p_t_r,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Discard reserved ring buffer sample  through  the  dynptr
                     interface. This is a no-op if the dynptr is invalid/null.

                     For  more  information  on  _f_l_a_g_s,  please see 'bpf_ring‐
                     buf_discard'.

              RReettuurrnn Nothing. Always succeeds.

       lloonngg bbppff__ddyynnppttrr__rreeaadd((vvooiidd **_d_s_t,, uu3322 _l_e_n,, ccoonnsstt ssttrruucctt bbppff__ddyynnppttrr **_s_r_c,,
       uu3322 _o_f_f_s_e_t,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Read _l_e_n bytes from _s_r_c into _d_s_t,  starting  from  _o_f_f_s_e_t
                     into _s_r_c.  _f_l_a_g_s is currently unused.

              RReettuurrnn 0  on  success, -E2BIG if _o_f_f_s_e_t + _l_e_n exceeds the length
                     of _s_r_c's data, -EINVAL if _s_r_c is an invalid dynptr or  if
                     _f_l_a_g_s is not 0.

       lloonngg bbppff__ddyynnppttrr__wwrriittee((ccoonnsstt ssttrruucctt bbppff__ddyynnppttrr **_d_s_t,, uu3322 _o_f_f_s_e_t,, vvooiidd
       **_s_r_c,, uu3322 _l_e_n,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Write  _l_e_n  bytes from _s_r_c into _d_s_t, starting from _o_f_f_s_e_t
                     into _d_s_t.

                     _f_l_a_g_s must be 0 except for skb-type dynptrs.

                     FFoorr sskkbb--ttyyppee ddyynnppttrrss::

                            • All data slices of the dynptr are  automatically
                              invalidated  after  bbppff__ddyynnppttrr__wwrriittee().  This is
                              because writing may pull the skb and change  the
                              underlying packet buffer.

                            • For  _f_l_a_g_s,  please  see  the  flags accepted by
                              bbppff__sskkbb__ssttoorree__bbyytteess().

              RReettuurrnn 0 on success, -E2BIG if _o_f_f_s_e_t + _l_e_n exceeds  the  length
                     of  _d_s_t's data, -EINVAL if _d_s_t is an invalid dynptr or if
                     _d_s_t is a read-only dynptr or if _f_l_a_g_s is not correct. For
                     skb-type dynptrs, other errors correspond to  errors  re‐
                     turned by bbppff__sskkbb__ssttoorree__bbyytteess().

       vvooiidd **bbppff__ddyynnppttrr__ddaattaa((ccoonnsstt ssttrruucctt bbppff__ddyynnppttrr **_p_t_r,, uu3322 _o_f_f_s_e_t,, uu3322
       _l_e_n))

              DDeessccrriippttiioonn
                     Get a pointer to the underlying dynptr data.

                     _l_e_n  must  be a statically known value. The returned data
                     slice is invalidated whenever the dynptr is invalidated.

                     skb and xdp type dynptrs  may  not  use  bpf_dynptr_data.
                     They    should    instead    use   bpf_dynptr_slice   and
                     bpf_dynptr_slice_rdwr.

              RReettuurrnn Pointer to the underlying dynptr data, NULL if the dynptr
                     is read-only, if the dynptr is invalid, or if the  offset
                     and length is out of bounds.

       ss6644 bbppff__ttccpp__rraaww__ggeenn__ssyynnccooookkiiee__iippvv44((ssttrruucctt iipphhddrr **_i_p_h,, ssttrruucctt ttccpphhddrr
       **_t_h,, uu3322 _t_h___l_e_n))

              DDeessccrriippttiioonn
                     Try to issue a SYN cookie for the packet with correspond‐
                     ing  IPv4/TCP headers, _i_p_h and _t_h, without depending on a
                     listening socket.

                     _i_p_h points to the IPv4 header.

                     _t_h points to the start of the TCP  header,  while  _t_h___l_e_n
                     contains   the   length  of  the  TCP  header  (at  least
                     ssiizzeeooff(ssttrruucctt ttccpphhddrr)).

              RReettuurrnn On success, lower 32 bits hold the generated  SYN  cookie
                     in  followed by 16 bits which hold the MSS value for that
                     cookie, and the top 16 bits are unused.

                     On failure, the returned value is one of the following:

                     --EEIINNVVAALL if _t_h___l_e_n is invalid.

       ss6644 bbppff__ttccpp__rraaww__ggeenn__ssyynnccooookkiiee__iippvv66((ssttrruucctt iippvv66hhddrr **_i_p_h,, ssttrruucctt ttccpphhddrr
       **_t_h,, uu3322 _t_h___l_e_n))

              DDeessccrriippttiioonn
                     Try to issue a SYN cookie for the packet with correspond‐
                     ing IPv6/TCP headers, _i_p_h and _t_h, without depending on  a
                     listening socket.

                     _i_p_h points to the IPv6 header.

                     _t_h  points  to  the start of the TCP header, while _t_h___l_e_n
                     contains  the  length  of  the  TCP  header   (at   least
                     ssiizzeeooff(ssttrruucctt ttccpphhddrr)).

              RReettuurrnn On  success,  lower 32 bits hold the generated SYN cookie
                     in followed by 16 bits which hold the MSS value for  that
                     cookie, and the top 16 bits are unused.

                     On failure, the returned value is one of the following:

                     --EEIINNVVAALL if _t_h___l_e_n is invalid.

                     --EEPPRROOTTOONNOOSSUUPPPPOORRTT if CONFIG_IPV6 is not builtin.

       lloonngg bbppff__ttccpp__rraaww__cchheecckk__ssyynnccooookkiiee__iippvv44((ssttrruucctt iipphhddrr **_i_p_h,, ssttrruucctt ttccpphhddrr
       **_t_h))

              DDeessccrriippttiioonn
                     Check  whether  _i_p_h and _t_h contain a valid SYN cookie ACK
                     without depending on a listening socket.

                     _i_p_h points to the IPv4 header.

                     _t_h points to the TCP header.

              RReettuurrnn 0 if _i_p_h and _t_h are a valid SYN cookie ACK.

                     On failure, the returned value is one of the following:

                     --EEAACCCCEESS if the SYN cookie is not valid.

       lloonngg bbppff__ttccpp__rraaww__cchheecckk__ssyynnccooookkiiee__iippvv66((ssttrruucctt iippvv66hhddrr **_i_p_h,, ssttrruucctt
       ttccpphhddrr **_t_h))

              DDeessccrriippttiioonn
                     Check whether _i_p_h and _t_h contain a valid SYN  cookie  ACK
                     without depending on a listening socket.

                     _i_p_h points to the IPv6 header.

                     _t_h points to the TCP header.

              RReettuurrnn 0 if _i_p_h and _t_h are a valid SYN cookie ACK.

                     On failure, the returned value is one of the following:

                     --EEAACCCCEESS if the SYN cookie is not valid.

                     --EEPPRROOTTOONNOOSSUUPPPPOORRTT if CONFIG_IPV6 is not builtin.

       uu6644 bbppff__kkttiimmee__ggeett__ttaaii__nnss((vvooiidd))

              DDeessccrriippttiioonn
                     A  nonsettable  system-wide clock derived from wall-clock
                     time but ignoring leap seconds.  This clock does not  ex‐
                     perience  discontinuities  and  backwards jumps caused by
                     NTP inserting leap seconds as CLOCK_REALTIME does.

                     See: cclloocckk__ggeettttiimmee(CCLLOOCCKK__TTAAII)

              RReettuurrnn Current _k_t_i_m_e.

       lloonngg bbppff__uusseerr__rriinnggbbuuff__ddrraaiinn((ssttrruucctt bbppff__mmaapp **_m_a_p,, vvooiidd **_c_a_l_l_b_a_c_k___f_n,,
       vvooiidd **_c_t_x,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Drain samples from the specified user  ring  buffer,  and
                     invoke the provided callback for each such sample:

                     long (*callback_fn)(const struct bpf_dynptr *dynptr, void
                     *ctx);

                     If ccaallllbbaacckk__ffnn returns 0, the helper will continue to try
                     and   drain   the   next  sample,  up  to  a  maximum  of
                     BPF_MAX_USER_RINGBUF_SAMPLES samples. If the return value
                     is 1, the helper will skip the rest of  the  samples  and
                     return. Other return values are not used now, and will be
                     rejected by the verifier.

              RReettuurrnn The number of drained samples if no error was encountered
                     while  draining  samples, or 0 if no samples were present
                     in  the  ring  buffer.  If  a  user-space  producer   was
                     epoll-waiting  on  this  map, and at least one sample was
                     drained, they will receive an event notification  notify‐
                     ing  them  of  available space in the ring buffer. If the
                     BPF_RB_NO_WAKEUP flag is  passed  to  this  function,  no
                     wakeup    notification    will    be    sent.    If   the
                     BPF_RB_FORCE_WAKEUP flag is passed, a wakeup notification
                     will be sent even if no sample was drained.

                     On failure, the returned value is one of the following:

                     --EEBBUUSSYY if the ring buffer is contended, and another call‐
                     ing context was concurrently draining the ring buffer.

                     --EEIINNVVAALL if user-space is not properly tracking  the  ring
                     buffer  due to the producer position not being aligned to
                     8 bytes, a sample not being aligned to 8  bytes,  or  the
                     producer position not matching the advertised length of a
                     sample.

                     --EE22BBIIGG  if user-space has tried to publish a sample which
                     is larger than the size of the ring buffer, or which can‐
                     not fit within a struct bpf_dynptr.

       vvooiidd **bbppff__ccggrrpp__ssttoorraaggee__ggeett((ssttrruucctt bbppff__mmaapp **_m_a_p,, ssttrruucctt ccggrroouupp **_c_g_r_o_u_p,,
       vvooiidd **_v_a_l_u_e,, uu6644 _f_l_a_g_s))

              DDeessccrriippttiioonn
                     Get a bpf_local_storage from the _c_g_r_o_u_p.

                     Logically, it could be thought of as  getting  the  value
                     from  a  _m_a_p  with _c_g_r_o_u_p as the kkeeyy.  From this perspec‐
                     tive,    the   usage   is   not   much   different   from
                     bbppff__mmaapp__llooookkuupp__eelleemm(_m_a_p,  &&_c_g_r_o_u_p) except this helper en‐
                     forces the key must be a cgroup struct and the  map  must
                     also be a BBPPFF__MMAAPP__TTYYPPEE__CCGGRRPP__SSTTOORRAAGGEE.

                     In  reality, the local-storage value is embedded directly
                     inside of the _c_g_r_o_u_p object itself, rather than being lo‐
                     cated in the BBPPFF__MMAAPP__TTYYPPEE__CCGGRRPP__SSTTOORRAAGGEE map. When the  lo‐
                     cal-storage value is queried for some _m_a_p on a _c_g_r_o_u_p ob‐
                     ject,  the kernel will perform an O(n) iteration over all
                     of the live local-storage values for that  _c_g_r_o_u_p  object
                     until the local-storage value for the _m_a_p is found.

                     An optional _f_l_a_g_s (BBPPFF__LLOOCCAALL__SSTTOORRAAGGEE__GGEETT__FF__CCRREEAATTEE) can be
                     used such that a new bpf_local_storage will be created if
                     one  does  not  exist.   _v_a_l_u_e  can be used together with
                     BBPPFF__LLOOCCAALL__SSTTOORRAAGGEE__GGEETT__FF__CCRREEAATTEE  to  specify  the  initial
                     value  of a bpf_local_storage.  If _v_a_l_u_e is NNUULLLL, the new
                     bpf_local_storage will be zero initialized.

              RReettuurrnn A bpf_local_storage pointer is returned on success.

                     NNUULLLL if not found or there was an error in adding  a  new
                     bpf_local_storage.

       lloonngg bbppff__ccggrrpp__ssttoorraaggee__ddeelleettee((ssttrruucctt bbppff__mmaapp **_m_a_p,, ssttrruucctt ccggrroouupp
       **_c_g_r_o_u_p))

              DDeessccrriippttiioonn
                     Delete a bpf_local_storage from a _c_g_r_o_u_p.

              RReettuurrnn 0 on success.

                     --EENNOOEENNTT if the bpf_local_storage cannot be found.

EEXXAAMMPPLLEESS
       Example  usage  for most of the eBPF helpers listed in this manual page
       are available within the Linux kernel sources, at the  following  loca‐
       tions:

       • _s_a_m_p_l_e_s_/_b_p_f_/

       • _t_o_o_l_s_/_t_e_s_t_i_n_g_/_s_e_l_f_t_e_s_t_s_/_b_p_f_/

LLIICCEENNSSEE
       eBPF  programs  can  have  an associated license, passed along with the
       bytecode instructions to the kernel when the programs are  loaded.  The
       format  for  that string is identical to the one in use for kernel mod‐
       ules (Dual licenses, such as "Dual BSD/GPL", may be used). Some  helper
       functions  are only accessible to programs that are compatible with the
       GNU General Public License (GNU GPL).

       In order to use such helpers, the eBPF program must be loaded with  the
       correct  license string passed (via aattttrr) to the bbppff() system call, and
       this generally translates into the C source code of  the  program  con‐
       taining a line similar to the following:

          char ____license[] __attribute__((section("license"), used)) = "GPL";

IIMMPPLLEEMMEENNTTAATTIIOONN
       This  manual  page  is  an  effort to document the existing eBPF helper
       functions.  But as of this writing, the BPF sub-system is  under  heavy
       development.  New  eBPF  program or map types are added, along with new
       helper functions. Some helpers are occasionally made available for  ad‐
       ditional  program  types.  So in spite of the efforts of the community,
       this page might not be up-to-date. If you want  to  check  by  yourself
       what  helper  functions exist in your kernel, or what types of programs
       they can support, here are some files among the kernel  tree  that  you
       may be interested in:

       • _i_n_c_l_u_d_e_/_u_a_p_i_/_l_i_n_u_x_/_b_p_f_._h is the main BPF header. It contains the full
         list  of  all helper functions, as well as many other BPF definitions
         including most of  the  flags,  structs  or  constants  used  by  the
         helpers.

       • _n_e_t_/_c_o_r_e_/_f_i_l_t_e_r_._c  contains  the  definition  of most network-related
         helper functions, and the list of program types from which  they  can
         be used.

       • _k_e_r_n_e_l_/_t_r_a_c_e_/_b_p_f___t_r_a_c_e_._c  is  the  equivalent  for  most tracing pro‐
         gram-related helpers.

       • _k_e_r_n_e_l_/_b_p_f_/_v_e_r_i_f_i_e_r_._c contains the functions used to check that valid
         types of eBPF maps are used with a given helper function.

       • _k_e_r_n_e_l_/_b_p_f_/  directory  contains  other  files  in  which  additional
         helpers are defined (for cgroups, sockmaps, etc.).

       • The  bpftool  utility can be used to probe the availability of helper
         functions on the system (as well as supported program and map  types,
         and  a  number  of  other  parameters). To do so, run bbppffttooooll ffeeaattuurree
         pprroobbee (see bbppffttooooll--ffeeaattuurree(8) for details). Add the uunnpprriivviilleeggeedd key‐
         word to list features available to unprivileged users.

       Compatibility between helper functions and program types can  generally
       be  found in the files where helper functions are defined. Look for the
       ssttrruucctt bbppff__ffuunncc__pprroottoo objects and for functions returning  them:  these
       functions contain a list of helpers that a given program type can call.
       Note  that  the  ddeeffaauulltt::  label  of the sswwiittcchh ...... ccaassee used to filter
       helpers can call other functions, themselves allowing access  to  addi‐
       tional helpers. The requirement for GPL license is also in those ssttrruucctt
       bbppff__ffuunncc__pprroottoo.

       Compatibility  between  helper  functions and map types can be found in
       the cchheecckk__mmaapp__ffuunncc__ccoommppaattiibbiilliittyy() function  in  file  _k_e_r_n_e_l_/_b_p_f_/_v_e_r_i_‐
       _f_i_e_r_._c.

       Helper functions that invalidate the checks on ddaattaa and ddaattaa__eenndd point‐
       ers     for    network    processing    are    listed    in    function
       bbppff__hheellppeerr__cchhaannggeess__ppkktt__ddaattaa() in file _n_e_t_/_c_o_r_e_/_f_i_l_t_e_r_._c.

SSEEEE AALLSSOO
       bbppff(2), bbppffttooooll(8), ccggrroouuppss(7), iipp(8), ppeerrff__eevveenntt__ooppeenn(2),  sseennddmmssgg(2),
       ssoocckkeett(7), ttcc--bbppff(8)

Linux v6.17                       2025‐06‐26                    _B_P_F_‐_H_E_L_P_E_R_S(7)
