attribute  on  a  The  function sets the on the attributes object
referenced by The function retrieves the on the attributes object
referenced by The argument is the initial signal mask to  be  set
for  the  new  process on creation if the flag is set in the It’s
default value must be set via a call to or the behaviour is unde‐
fined.  On success, these functions return 0; on failure they re‐
turn an error number from Additionally, if successful, will  mod‐
ify  the  contents  of  the  parameter with the current attribute
value.  These functions may fail if: The value  specified  by  is
invalid.   The  value  of is invalid.  The and function calls ap‐
peared in
