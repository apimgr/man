The  driver  provides  a read‐only interface to the snapshot of a
currently mounted file system.  Reading from a device  gives  the
view  of  the file system when the snapshot was taken.  It can be
configured via The command codes below are defined in The (third)
argument to should be a pointer to the type  indicated.   Config‐
ures   a  device.   struct  fss_set  {          char  *fss_mount;
        char    *fss_bstore;             blksize_t     fss_csize;
        int  fss_flags;  }; The struct element is the mount point
of the file system.  The struct element is either a regular  file
or  a  raw  disk device where data overwritten on the file system
will be saved.  The struct element is the preferred size of  this
data.   The struct element is the initial set of flags.  Gets the
status   of   a   device.    struct   fss_get   {            char
fsg_mount[MNAMELEN];             struct     timeval     fsg_time;
        blksize_t  fsg_csize;          blkcnt_t   fsg_mount_size;
        blkcnt_t  fsg_bs_size; }; The struct element is the mount
point of the file system.  The struct element is  the  time  this
snapshot  was  taken.   The struct element is the current size of
data clusters.  The struct element is the number of  clusters  of
the  file  system.   The struct element is the number of clusters
written to the backing store.  Unconfigures a device.   Sets  the
flags of a device.  Possible flags are: Unconfigure the device on
the  last  close.   Unlink  the backing file before the device is
created.  Gets the flags of a device.  For each  active  snapshot
device  there  is a kernel thread that handles the backing store.
This thread is named where is the device minor number.   The  de‐
vice appeared in
