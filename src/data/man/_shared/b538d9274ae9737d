connects  to  the  specified  and uses the to enter text as if it
were typed in.  This is limited to the superuser.  will send  the
provided  if  any; otherwise it sends the standard input.  The is
interpreted using The command first appeared in The  command  was
written by
