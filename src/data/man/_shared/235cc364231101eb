The  utility is designed to provide a way for users to access and
control the IEEE1394 subsystem.  Without options, will  output  a
list  of devices that are/were connected to the bus.  The follow‐
ing options are available: Set  the  register  on  all  supported
nodes.   Show the configuration ROM on the node.  Hex dump of the
configuration ROM.  Force specified to be the root  node  on  the
next  bus reset by sending a PHY config packet.  Valid values are
0 ‐ 63.  Broadcast new by phy_config  packet.   By  default  this
value is 63 on all nodes.  Valid values are 0 ‐ 63.  Set the reg‐
ister on all supported nodes.  Load hex dump file of the configu‐
ration  ROM  and parse it.  Explicitly specify either or mode for
the incoming stream.  Only meaningful in case of and must precede
the option.  If not specified, the program will try to guess.  In
case of error, try to force the mode.  Set default fwmem  target.
Hostname  will  be converted to EUI64.  Send a link‐on PHY packet
to the node.  Dump PHY registers.  Receive DV or MPEG  TS  stream
and  dump  it  to a file.  Use Ctrl‐C to stop receiving.  Some DV
cameras seem not to send the stream if a bus manager  exits.   If
you  cannot  get  the  stream, try the following commands: sysctl
hw.ieee1394if.try_bmr=0 fwctl ‐r The resulting file contains  raw
DV  data  excluding isochronous header and CIP header.  It can be
handled by the package.  The resulting  MPEG  TS  stream  can  be
played and sent over a network using the VideoLAN tool The stream
can  be piped directly to see Initiate bus reset.  Send a DV file
as isochronous stream.  Write to the register on the node.   Show
the topology map.  Specify the IEEE1394 bus number to be operated
on.  The default is bus 0.  Each DV frame has a fixed size and it
is  easy  to  edit  the frame order.  Receive a DV stream with DV
camera attached.  Get first 30  frames  (NTSC).   Get  second  30
frames  (NTSC).  Swap first and second 30 frames and send them to
DV recorder.  For PAL, replace with Receive  an  MPEG  TS  stream
from  a  camera  producing  MPEG transport stream.  This has been
tested with SONY HDR‐FX1E camera that produces HD  MPEG‐2  stream
at  25  Mbps  bandwidth.  To send the stream from the camera over
the network using TCP (which surprisingly works better with vlc),
you can use with and to receive the stream, use  To  netcast  via
UDP,  you  need  to  use since vlc is not fast enough to read UDP
packets from buffers and thus it experiences  dropouts  when  run
directly.   The  sending  side can use and to receive the stream,
use For more information on how to work with see  its  documenta‐
tion.  The command first appeared in as It was added to under its
present  name.   ‐ MPEG TS mode.  This utility is still under de‐
velopment and provided for debugging purposes.   Especially  MPEG
TS  reception  support is very rudimental and supports only high‐
bandwidth MPEG‐2 streams (fn field in CIP header equals 3).
