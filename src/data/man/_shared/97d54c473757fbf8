SYSPATCH(8)               BSD System Manager's Manual              SYSPATCH(8)

NNAAMMEE
     ssyyssppaattcchh — manage base system binary patches

SSYYNNOOPPSSIISS
     ssyyssppaattcchh [--cc | --ll | --RR | --rr]

DDEESSCCRRIIPPTTIIOONN
     ssyyssppaattcchh is a utility to fetch, verify, install and revert OpenBSD binary
     patches.

     When run without any options, ssyyssppaattcchh will apply _a_l_l missing patches,
     creating a rollback tarball containing the files it is about to replace,
     then extracting and installing all files contained in the syspatch tar‐
     ball.  If any sets are missing, patches are skipped accordingly.  Patches
     are cumulative and as such it is not possible to install only a subset of
     them.

     The options are as follows:

     --cc      List available patches; suitable for cron(8).

     --ll      List installed patches.

     --RR      Revert all patches.

     --rr      Revert the most recently installed patch.

FFIILLEESS
     /etc/installurl  OpenBSD mirror top-level URL for fetching patches.
     /var/syspatch/*  Directories containing the rollback tarball and original
                      signed diff(1) of installed patches.

EEXXIITT SSTTAATTUUSS
     The ssyyssppaattcchh utility exits 0 on success, and >0 if an error occurs.  In
     particular, 2 indicates that applying patches was requested but no addi‐
     tional patch was installed.

SSEEEE AALLSSOO
     signify(1), installurl(5), release(8)

HHIISSTTOORRYY
     ssyyssppaattcchh first appeared in OpenBSD 6.1.

AAUUTTHHOORRSS
     ssyyssppaattcchh was written by Antoine Jacoutot <_a_j_a_c_o_u_t_o_t_@_o_p_e_n_b_s_d_._o_r_g>.

CCAAVVEEAATTSS
     ssyyssppaattcchh is designed to work solely on official OpenBSD releases.

BSD                            December 7, 2020                            BSD
