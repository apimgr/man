The function initializes the object referenced by to an empty set
of file actions for subsequent use in a call to or A object is an
opaque type that is used to collect set of file actions specified
by  calls  to and such that once the spawn operation is complete,
the child process will have a different set of  file  descriptors
than  the  parent, in the order that the operations were added to
the Additionally, any descriptor in the parent not modified by  a
file  action,  and  which  has  the  FD_CLOEXEC flag set, will be
closed in the child.  The function destroys the object referenced
by which was previously intialized by returning any resources ob‐
tained at the time of initialization to the system for subsequent
reuse.  A may be reinitialized after having been  destroyed,  but
be  reused  after  destruction, unless it has been reinitialized.
On success, these functions return 0; on failure they  return  an
error  number  from These functions will fail and return an error
if: Insufficient resources to initialize the object.   The  being
destroyed is invalid.  The and function calls appeared in
