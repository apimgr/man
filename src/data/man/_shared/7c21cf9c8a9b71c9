copies  the  named  files  side by side onto the standard output.
The input lines from the input are considered  fragments  of  the
single  long output line into which they are assembled.  The name
means the standard input, and may be  repeated.   Normally,  each
option  affects only the after it.  If the option letter is capi‐
talized it affects all subsequent files until  it  appears  again
uncapitalized.   The  options  are  described  below.  Print line
fragments according to the format string  where  is  the  minimum
field  width and the maximum field width.  If begins with a zero,
zeros will be added to make up the field width, and if it  begins
with a the fragment will be left‐adjusted within the field.  Like
but  pad  this file’s field when end‐of‐file is reached and other
files are still active.  Print  before  printing  line  fragments
from  the next file.  This option may appear after the last file.
(A capitalized version appearing before the last file is not car‐
ried over past the last file.)  The input line terminator is  in‐
stead of a newline.  The newline normally appended to each output
line  is omitted.  To print files simultaneously for easy viewing
use The command lam file1 file2 file3 file4  joins  4  files  to‐
gether  along  each line.  To merge the lines from four different
files use lam file1 -S $’\n’ file2 file3 file4 (The means for in‐
serting a newline may depend on your shell.)  Every 2 lines of  a
file  may  be  joined  on one line with lam - - < file and a form
letter with substitutions keyed by can be done with lam -t @ let‐
ter changes The utility first appeared in
