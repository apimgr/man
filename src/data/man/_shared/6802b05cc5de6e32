_t_c_g_e_t_p_g_r_p(3)               Library Functions Manual               _t_c_g_e_t_p_g_r_p(3)

NNAAMMEE
       tcgetpgrp, tcsetpgrp - get and set terminal foreground process group

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<uunniissttdd..hh>>

       ppiidd__tt ttccggeettppggrrpp((iinntt _f_d));;
       iinntt ttccsseettppggrrpp((iinntt _f_d,, ppiidd__tt _p_g_r_p));;

DDEESSCCRRIIPPTTIIOONN
       The function ttccggeettppggrrpp() returns the process group ID of the foreground
       process  group on the terminal associated to _f_d, which must be the con‐
       trolling terminal of the calling process.

       The function ttccsseettppggrrpp() makes the process group with process group  ID
       _p_g_r_p  the  foreground  process  group on the terminal associated to _f_d,
       which must be the controlling terminal  of  the  calling  process,  and
       still  be  associated with its session.  Moreover, _p_g_r_p must be a (non‐
       empty) process group belonging to  the  same  session  as  the  calling
       process.

       If  ttccsseettppggrrpp()  is called by a member of a background process group in
       its session, and the calling process is not blocking or ignoring  SSIIGGTT‐‐
       TTOOUU, a SSIIGGTTTTOOUU signal is sent to all members of this background process
       group.

RREETTUURRNN VVAALLUUEE
       When  _f_d refers to the controlling terminal of the calling process, the
       function ttccggeettppggrrpp() will return the foreground  process  group  ID  of
       that terminal if there is one, and some value larger than 1 that is not
       presently  a process group ID otherwise.  When _f_d does not refer to the
       controlling terminal of the calling process, -1 is returned, and  _e_r_r_n_o
       is set to indicate the error.

       When  successful, ttccsseettppggrrpp() returns 0.  Otherwise, it returns -1, and
       _e_r_r_n_o is set to indicate the error.

EERRRROORRSS
       EEBBAADDFF  _f_d is not a valid file descriptor.

       EEIINNVVAALL _p_g_r_p has an unsupported value.

       EENNOOTTTTYY The calling process does not have a controlling terminal, or  it
              has one but it is not described by _f_d, or, for ttccsseettppggrrpp(), this
              controlling terminal is no longer associated with the session of
              the calling process.

       EEPPEERRMM  _p_g_r_p has a supported value, but is not the process group ID of a
              process in the same session as the calling process.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       ttccggeettppggrrpp(), ttccsseettppggrrpp() T}   Thread safety  MT‐Safe

VVEERRSSIIOONNSS
       These functions are implemented via the TTIIOOCCGGPPGGRRPP and TTIIOOCCSSPPGGRRPP ioctls.

SSTTAANNDDAARRDDSS
       POSIX.1‐2008.

HHIISSTTOORRYY
       POSIX.1‐2001.

       The ioctls appeared in 4.2BSD.  The functions are POSIX inventions.

SSEEEE AALLSSOO
       sseettppggiidd(2), sseettssiidd(2), ccrreeddeennttiiaallss(7)

Linux man‐pages (unreleased)      2025‐05‐17                      _t_c_g_e_t_p_g_r_p(3)
