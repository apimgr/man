SCTP_GETADDRLEN(3)       BSD Library Functions Manual       SCTP_GETADDRLEN(3)

NNAAMMEE
     ssccttpp__ggeettaaddddrrlleenn — return the address length of an address family

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ttyyppeess..hh>>
     ##iinncclluuddee <<ssyyss//ssoocckkeett..hh>>
     ##iinncclluuddee <<nneettiinneett//ssccttpp..hh>>

     _i_n_t
     ssccttpp__ggeettaaddddrrlleenn(_s_a___f_a_m_i_l_y___t _f_a_m_i_l_y);

DDEESSCCRRIIPPTTIIOONN
     The ssccttpp__ggeettaaddddrrlleenn() function returns the size of a specific address
     family.  This function is provided for application binary compatibility
     since it provides the application with the size the operating system
     thinks the specific address family is.  Note that the function will actu‐
     ally create an SCTP socket and then gather the information via a
     ggeettssoocckkoopptt() system calls.  If for some reason a SCTP socket cannot be
     created or the ggeettssoocckkoopptt() call fails, an error will be returned with
     _e_r_r_n_o set as specified in the ssoocckkeett() or ggeettssoocckkoopptt() system call.

RREETTUURRNN VVAALLUUEESS
     The call returns the number of bytes that the operating system expects
     for the specific address family or -1.

EERRRROORRSS
     The ssccttpp__ggeettaaddddrrlleenn() function can return the following errors:

     [EINVAL]           The address family specified does NOT exist.

SSEEEE AALLSSOO
     getsockopt(2), socket(2), sctp(4)

BSD                            December 15, 2006                           BSD
