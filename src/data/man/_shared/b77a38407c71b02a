These  functions  represent an Apple extension to and and as such
should not be  used  by  programs  intending  their  code  to  be
portable to other platforms.  The function sets a new value for a
special  port into the value referenced by to the port referenced
by for a port value of as if the new process had called The func‐
tion sets the port to for a number of exception ports represented
by the bitfield and sets the new default and default  as  if  the
new  process  had called On success, these functions return 0; on
failure they return an error number from These functions may fail
if: The value specified by is invalid.  The value of is  invalid.
Insufficient  resources  exist to set the ports.  Nonstandard The
and function calls appeared in Mac OS X.
