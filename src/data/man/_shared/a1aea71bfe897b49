is  the command script which controls the startup of various ser‐
vices, and is invoked by as part of the process of  entering  the
automatic  reboot to multi‐user startup, or after the single user
mode shell has exited.   If  is  starting  the  automatic  reboot
process,  is  invoked with the argument of is a command script to
which local boot‐time actions can be added.  It is  (nearly)  the
last  thing  invoked  by  during  a  normal boot.  is the command
script which shuts down various services, and is  invoked  by  as
part  of  the process of shutting down the system.  is the direc‐
tory which contains various scripts, one for each service,  which
are  called  by  at startup, at shutdown, and as necessary during
system operation to stop, start, restart,  reload,  or  otherwise
control  the  service.  Source to load various shell functions to
use.  If autobooting, set and enable a flag  which  prevents  the
scripts  from  performing the check for already running processes
(thus speeding up the boot process).  This  speedup  won’t  occur
when  is  started up after exiting the single‐user shell.  Invoke
to order the files in that do not have a keyword (refer to flag),
and assigns the result to a variable.  Calls each script in  turn
using  (from which sets to and sources the script in a sub‐shell.
If the script has a suffix then it is sourced directly  into  the
current  shell.   The  output  from  the above steps is sent to a
post‐processor.  If is false, then  the  post‐processor  displays
the output.  If is true, then the post‐processor invokes the com‐
mand  specified in once for each line, without otherwise display‐
ing the output.  Useful values for include to display nothing  at
all, and to display a spinning symbol on the console.  Regardless
of  the value of the post‐processor saves the output in Source to
load various shell functions to use.  Invoke to order  the  files
in  that have a keyword (refer to flag), reverses that order, and
assigns the result to a variable.  Calls each script in turn  us‐
ing  (from  which  sets to and sources the script in a sub‐shell.
If the script has a suffix then it is sourced directly  into  the
current  shell.   is located in The following file naming conven‐
tions are currently used in Scripts that are to ensure that  cer‐
tain  operations  are  performed  before  others.   In  order  of
startup, these are: Ensure basic network  services  are  running,
including general network configuration and Ensure basic services
(such as and exist for services that start early (such as because
they’re  required  by  below.  Before all general purpose daemons
such as and Before user login services and as well as before ser‐
vices which might run commands as  users  and  Scripts  that  are
sourced  into  the  current  shell rather than a sub‐shell have a
suffix.  Extreme care must be taken in using this, as the startup
sequence will terminate if the script causes  the  current  shell
process  to  terminate.  uses this behaviour to allow the user to
select a different configuration (including early  in  the  boot.
Scripts  that are sourced in a sub‐shell.  The boot does not stop
if such a script terminates with a non‐zero status, but a  script
can  stop  the  boot  if necessary by invoking the function (from
Each script should contain keywords,  especially  an  appropriate
entry.   The scripts are expected to support at least the follow‐
ing arguments: Start the service.  This  should  check  that  the
service  is to be started as specified by Also checks if the ser‐
vice is already running and refuses to start if it is.  This lat‐
ter check is not performed by standard scripts if the  system  is
starting  directly  to  multi‐user  mode,  to  speed  up the boot
process.  If the service is to be started as  specified  by  stop
the  service.   This should check that the service is running and
complain if it’s not.  Perform a then a If the  script  starts  a
process  (rather  than  performing a one‐off operation), show the
status of the process.  Otherwise it’s not necessary  to  support
this argument.  Defaults to displaying the process ID of the pro‐
gram  (if  running).  If the script starts a process (rather than
performing a one‐off operation), wait for the  command  to  exit.
Otherwise  it’s  not necessary to support this argument.  Display
which variables are used to control the startup  of  the  service
(if  any).   Other arguments (such as etc) can be added if neces‐
sary.  The argument may have one of the following prefixes to al‐
ter its  operation:  Skip  the  check  for  an  existing  running
process.  Sets Skips the check, ignores a failure result from any
of  the prerequisite checks, executes the command, and always re‐
turns a zero exit status.  Sets Skips the check, but performs all
other prerequisite tests.  In  order  to  simplify  scripts,  the
function  from  may  be used.  Startup script called by Directory
containing control  scripts  for  each  service.   Local  startup
script.   Shutdown  script  called  by Contains functions used by
various scripts.  System startup configuration  file.   Log  file
created by The command appeared in The support was implemented in
by  The  post‐processor, support for and saving output to a file,
was implemented in by
