The utility creates an UDF file system on device suitable for the
media currently inserted.  The options are as follow: When creat‐
ing  image  files, specify the blocking size or packetsize of the
media to Perform a crude surface check first  to  weed  out  disc
faults  on  rewritable  media.  Force file system construction on
non‐empty recordable media or create an image file.  Set the disc
logical label to the specified Disable  metadata  partition  cre‐
ation  when  selected  UDF  version  or media dictates this.  For
strict conformance and interchange, don’t disable this unless its
causing problems.  Set the physical disc label to  the  specified
Prepend  with  volsetname  separated  with  a ’:’ if wanted.  For
strict conformance and interchange, don’t set this  manually  un‐
less  it  has  a unique hex number in the first 8 character posi‐
tions.  Percentage of partition  to  be  initially  reserved  for
metadata  on  the  Metadata partition.  It defaults to 20 %.  Set
the sectorsize for image files.  For strict conformance  and  in‐
terchange,  don’t  set  this  manually.  For image files, set the
file size to the humanized size Use the  specified  as  gmt  time
offset  for  recording  times on the disc.  Select as the maximum
UDF version to be supported.  For UDF version 2.50, use or Select
as the minimum UDF version to  be  supported.   For  UDF  version
2.01,  use or The UDF file system is defined for the entire opti‐
cal medium.  It can only function on the entire CD/DVD/BD so  the
raw  partition  has  to be specified for read/write actions.  For
this means specifying the raw device with the raw partition, i.e.
or Some rewritable optical media needs to be formatted first  be‐
fore  it  can be used by UDF.  This can be done using The default
UDF version is version 2.01.  Create a  file  system,  using  the
specified  names  on  the  device  with the default UDF version :
newfs_udf ‐P "Encyclopedia:copy‐nr‐1" ‐L  "volume  2"  /dev/rcd0d
Create  a 4.8 GiB sparse file and configure it using to be a 2048
sector size disc and create  a  new  UDF  file  system  on  :  dd
if=/dev/zero of=bigdisk.2048.udf seek=9999999 count=1 vnconfig ‐c
vnd0 bigdisk.2048.udf 2048/1/1/1 newfs_udf ‐L bigdisk /dev/rvnd0d
Create  a 2 GiB file and create a new UDF file system on it using
the default 512 byte sector size : newfs_udf ‐L bigdisk2 ‐F ‐s 2G
bigdisk2.iso Create a 200 MiB file and create a new UDF file sys‐
tem on it using a sector size of 2048 : newfs_udf ‐L bigdisk2  ‐F
‐s  200M ‐S 2048 bigdisk3.iso Create a new UDF file system on the
inserted USB stick using its native sectorsize of 512 : newfs_udf
‐L "My USB stick" /dev/rsd0d The command first  appeared  in  The
and  the arguments have changed meaning.  The meaning of has been
merged into since
