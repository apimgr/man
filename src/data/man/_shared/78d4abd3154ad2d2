The  command  attaches the MS‐DOS file system residing on the de‐
vice to the global file system namespace at  the  location  indi‐
cated  by  Both  and  are converted to absolute paths before use.
This command is normally executed by at boot  time,  but  can  be
used  by any user to mount an MS‐DOS file system on any directory
that they own (provided, of course, that  they  have  appropriate
access to the device that contains the file system).  Support for
FAT16  and  VFAT32  as well as long file names is available.  The
options are as follows: Ignore the special Win’95  directory  en‐
tries  even if deleting or renaming a file.  This forces This op‐
tion causes the file system to be interpreted as an  Atari‐Gemdos
file system.  The differences to the MS‐DOS file system are mini‐
mal  and  limited  to  the  boot  block.  This option also allows
mounting X680x0’s Human68k floppies.  This  option  enforces  Set
the group of the files in the file system to The default group is
the  group  of  the  directory  on which the file system is being
mounted.  Force listing and generation of Win’95  long  filenames
and  separate creation/modification/access dates.  If neither nor
are given, searches the root directory of the file system  to  be
mounted for any existing Win’95 long filenames.  If the file sys‐
tem  is  not empty and no such entries are found, is the default.
Otherwise is assumed.  The MS‐DOS file system stores filenames in
a short version using 8‐bit characters according to some  charac‐
ter  set  and a long version with 16‐bit unicode characters.  The
default method to store encoding‐agnostic UNIX  filenames  is  to
copy  them  byte‐wise  into both fields.  This is transparent but
generates wrong unicode  characters  for  anything  that  is  not
ASCII.   Setting  the flag interprets UNIX filenames as UTF‐8 and
generates correctly encoded long filenames.  This forces  Specify
the  maximum file permissions for directories in the file system.
The value of is used if it is supplied and is  omitted.   Specify
the  maximum file permissions for files in the file system.  (For
example, a mask of specifies that, by default, the  owner  should
have  read,  write, and execute permissions for files, but others
should only have read and execute permissions.  See for more  in‐
formation  about octal file modes.)  Only the nine low‐order bits
of are used.  The value of is used if it is supplied and is omit‐
ted.  The default mask is taken from the directory on  which  the
file  system  is  being  mounted.  Use the specified mount as de‐
scribed in Force behaviour to ignore and not generate Win’95 long
filenames.  See also Set the time zone offset (in  seconds)  from
UTC  to  with positive values indicating east of the Prime Merid‐
ian.  If not set, the user’s current time zone will be used.  Set
the owner of the files in the file system to The default owner is
the owner of the directory on which  the  file  system  is  being
mounted.   To  remove the ’execute’ permission bit for all files,
but still keep directories searchable, use: mount_msdos  ‐m  0644
‐M  0755  /dev/wd0e  /msdos  The predecessor to utility named ap‐
peared in It was abandoned in favour of the more  aptly‐named  in
and  rewritten  entirely by Initial implementation as was written
by It was rewritten by Compressed partitions are  not  supported.
The  use of the flag could result in damaged file systems, albeit
the damage is in part taken care of by procedures similar to  the
ones used in Win’95.
