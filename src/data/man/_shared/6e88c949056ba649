_s_e_t_l_o_c_a_l_e(3)               Library Functions Manual               _s_e_t_l_o_c_a_l_e(3)

NNAAMMEE
       setlocale - set the current locale

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<llooccaallee..hh>>

       cchhaarr **sseettllooccaallee((iinntt _c_a_t_e_g_o_r_y,, ccoonnsstt cchhaarr **__NNuullllaabbllee _l_o_c_a_l_e));;

DDEESSCCRRIIPPTTIIOONN
       The  sseettllooccaallee() function is used to set or query the program’s current
       locale.

       If _l_o_c_a_l_e is not NULL, the program’s current locale is modified accord‐
       ing to the arguments.  The argument _c_a_t_e_g_o_r_y determines which parts  of
       the  program’s  current locale should be modified.  lB lB lB lx.  Cate‐
       gory  Governs LC_ALL    All of the locale LC_ADDRESS     T{  Formatting
       of  addresses  and geography‐related items (*) T} LC_COLLATE     String
       collation  LC_CTYPE  Character  classification   LC_IDENTIFICATION   T{
       Metadata  describing  the  locale (*) T} LC_MEASUREMENT T{ Settings re‐
       lated to measurements (metric  versus  US  customary)  (*)  T}  LC_MES‐
       SAGES    T{  Localizable natural‐language messages T} LC_MONETARY    T{
       Formatting of monetary values T} LC_NAME   T{ Formatting of salutations
       for persons (*) T} LC_NUMERIC     T{ Formatting of nonmonetary  numeric
       values  T} LC_PAPER  T{ Settings related to the standard paper size (*)
       T} LC_TELEPHONE   T{ Formats to be used with telephone services (*)  T}
       LC_TIME   T{ Formatting of date and time values T}

       The  categories  marked with an asterisk in the above table are GNU ex‐
       tensions.  For further information on these locale categories, see  lloo‐‐
       ccaallee(7).

       The  argument  _l_o_c_a_l_e is a pointer to a character string containing the
       required setting of _c_a_t_e_g_o_r_y.  Such a string  is  either  a  well‐known
       constant  like "C" or "da_DK" (see below), or an opaque string that was
       returned by another call of sseettllooccaallee().

       If _l_o_c_a_l_e is an empty string, """", each part of the locale  that  should
       be modified is set according to the environment variables.  The details
       are  implementation‐dependent.   For  glibc, first (regardless of _c_a_t_e_‐
       _g_o_r_y), the environment variable LLCC__AALLLL is inspected, next the  environ‐
       ment variable with the same name as the category (see the table above),
       and finally the environment variable LLAANNGG.  The first existing environ‐
       ment  variable  is used.  If its value is not a valid locale specifica‐
       tion, the locale is unchanged, and sseettllooccaallee() returns NULL.

       The locale ""CC"" or ""PPOOSSIIXX"" is a portable locale; it exists on  all  con‐
       forming systems.

       A  locale  name  is  typically  of the form _l_a_n_g_u_a_g_e[__t_e_r_r_i_t_o_r_y][._c_o_d_e_‐
       _s_e_t][@_m_o_d_i_f_i_e_r], where _l_a_n_g_u_a_g_e is an ISO 639 language code,  _t_e_r_r_i_t_o_r_y
       is an ISO 3166 country code, and _c_o_d_e_s_e_t is a character set or encoding
       identifier  like  IISSOO--88885599--11 or UUTTFF--88.  For a list of all supported lo‐
       cales, try "locale -a" (see llooccaallee(1)).

       If _l_o_c_a_l_e is NULL, the current locale is only queried, not modified.

       On startup of the main program, the portable ""CC"" locale is selected  as
       default.  A program may be made portable to all locales by calling:

           setlocale(LC_ALL, "");

       after program initialization, and then:

       •  using  the  values returned from a llooccaalleeccoonnvv(3) call for locale‐de‐
          pendent information;

       •  using the multibyte and wide character functions for text processing
          if MMBB__CCUURR__MMAAXX >> 11;

       •  using ssttrrccoollll(3) and ssttrrxxffrrmm(3) to compare strings; and

       •  using wwccssccoollll(3) and wwccssxxffrrmm(3) to compare wide‐character strings.

RREETTUURRNN VVAALLUUEE
       A successful call to sseettllooccaallee() returns an opaque string  that  corre‐
       sponds to the locale set.  This string may be allocated in static stor‐
       age.   The  string  returned  is  such that a subsequent call with that
       string and its associated  category  will  restore  that  part  of  the
       process’s  locale.   The  return value is NULL if the request cannot be
       honored.

AATTTTRRIIBBUUTTEESS
       For an explanation of the terms  used  in  this  section,  see  aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       sseettllooccaallee() T}   Thread safety  MT‐Unsafe const:locale env

SSTTAANNDDAARRDDSS
       C11, POSIX.1‐2008.

   CCaatteeggoorriieess
       LLCC__AALLLL
       LLCC__CCOOLLLLAATTEE
       LLCC__CCTTYYPPEE
       LLCC__MMOONNEETTAARRYY
       LLCC__NNUUMMEERRIICC
       LLCC__TTIIMMEE
              C11, POSIX.1‐2008.

       LLCC__MMEESSSSAAGGEESS
              POSIX.1‐2008.

       Others:
              GNU.

HHIISSTTOORRYY
       POSIX.1‐2001, C89.

   CCaatteeggoorriieess
       LLCC__AALLLL
       LLCC__CCOOLLLLAATTEE
       LLCC__CCTTYYPPEE
       LLCC__MMOONNEETTAARRYY
       LLCC__NNUUMMEERRIICC
       LLCC__TTIIMMEE
              C89, POSIX.1‐2001.

       LLCC__MMEESSSSAAGGEESS
              POSIX.1‐2001.

       Others:
              GNU.

SSEEEE AALLSSOO
       llooccaallee(1),  llooccaalleeddeeff(1),  iissaallpphhaa(3),  llooccaalleeccoonnvv(3),  nnll__llaannggiinnffoo(3),
       rrppmmaattcchh(3), ssttrrccoollll(3), ssttrrffttiimmee(3), cchhaarrsseettss(7), llooccaallee(7)

Linux man‐pages (unreleased)      2025‐09‐21                      _s_e_t_l_o_c_a_l_e(3)
