displays all shared objects that are needed to run the given pro‐
gram.   Contrary  to  the list includes dependencies that are the
result of needed shared objects which themselves  depend  on  yet
other  shared  objects.   Zero,  one or two options may be given.
The argument is a format string passed to and  allows  customiza‐
tion  of  output.   The first format argument is used for library
objects and defaults to The second format argument  is  used  for
non‐library  objects  and  defaults to These arguments are inter‐
preted as format strings a la to customize the trace  output  and
allow  to be operated as a filter more conveniently.  The follow‐
ing conversions can be used: The main program’s name The value of
the environment variable in a.out and the program name  from  the
argument vector from elf.  The library name.  The library’s major
version  number.  The library’s minor version number (a.out only,
ignored in elf).  The full  pathname  as  determined  by  library
search  rules.   The library’s load address Additionally, and are
recognized and have their usual meaning.  The option is an  alias
for which makes behave analogously to The option turns on verbose
mode.   A utility first appeared in SunOS 4.0, it appeared in its
current form in
