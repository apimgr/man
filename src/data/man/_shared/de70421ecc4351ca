_g_e_t_p_w_e_n_t(3)                Library Functions Manual                _g_e_t_p_w_e_n_t(3)

NNAAMMEE
       getpwent, setpwent, endpwent - get password file entry

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssyyss//ttyyppeess..hh>>
       ##iinncclluuddee <<ppwwdd..hh>>

       ssttrruucctt ppaasssswwdd **ggeettppwweenntt((vvooiidd));;
       vvooiidd sseettppwweenntt((vvooiidd));;
       vvooiidd eennddppwweenntt((vvooiidd));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       ggeettppwweenntt(), sseettppwweenntt(), eennddppwweenntt():
           _XOPEN_SOURCE >= 500
               || /* glibc >= 2.19: */ _DEFAULT_SOURCE
               || /* glibc <= 2.19: */ _BSD_SOURCE || _SVID_SOURCE

DDEESSCCRRIIPPTTIIOONN
       The ggeettppwweenntt() function returns a pointer to a structure containing the
       broken‐out fields of a record from the password database (e.g., the lo‐
       cal  password  file  _/_e_t_c_/_p_a_s_s_w_d, NIS, and LDAP).  The first time ggeettpp‐‐
       wweenntt() is called, it returns the first entry;  thereafter,  it  returns
       successive entries.

       The  sseettppwweenntt() function rewinds to the beginning of the password data‐
       base.

       The eennddppwweenntt() function is used to close the  password  database  after
       all processing has been performed.

       The _p_a_s_s_w_d structure is defined in _<_p_w_d_._h_> as follows:

           struct passwd {
               char   *pw_name;       /* username */
               char   *pw_passwd;     /* user password */
               uid_t   pw_uid;        /* user ID */
               gid_t   pw_gid;        /* group ID */
               char   *pw_gecos;      /* user information */
               char   *pw_dir;        /* home directory */
               char   *pw_shell;      /* shell program */
           };

       For more information about the fields of this structure, see ppaasssswwdd(5).

RREETTUURRNN VVAALLUUEE
       The  ggeettppwweenntt()  function  returns  a pointer to a _p_a_s_s_w_d structure, or
       NULL if there are no more entries or an error occurred.   If  an  error
       occurs,  _e_r_r_n_o is set to indicate the error.  If one wants to check _e_r_‐
       _r_n_o after the call, it should be set to zero before the call.

       The return value may point to a static area, and may be overwritten  by
       subsequent  calls  to ggeettppwweenntt(), ggeettppwwnnaamm(3), or ggeettppwwuuiidd(3).  (Do not
       pass the returned pointer to ffrreeee(3).)

EERRRROORRSS
       EEIINNTTRR  A signal was caught; see ssiiggnnaall(7).

       EEIIOO    I/O error.

       EEMMFFIILLEE The per‐process limit on the number of open file descriptors has
              been reached.

       EENNFFIILLEE The system‐wide limit on the total number of open files has been
              reached.

       EENNOOMMEEMM Insufficient memory to allocate _p_a_s_s_w_d structure.

       EERRAANNGGEE Insufficient buffer space supplied.

FFIILLEESS
       _/_e_t_c_/_p_a_s_s_w_d
              local password database file

AATTTTRRIIBBUUTTEESS
       For an explanation of the terms  used  in  this  section,  see  aattttrriibb‐‐
       uutteess(7).  allbox; lb lb lbx l l l.  Interface Attribute Value T{
       ggeettppwweenntt() T}   Thread safety  T{ MT‐Unsafe race:pwent race:pwentbuf
       locale T} T{ sseettppwweenntt(), eennddppwweenntt() T}   Thread safety  T{ MT‐Unsafe
       race:pwent locale T}

       In the above table, _p_w_e_n_t in _r_a_c_e_:_p_w_e_n_t signifies that if any of the
       functions sseettppwweenntt(), ggeettppwweenntt(), or eennddppwweenntt() are used in parallel in
       different threads of a program, then data races could occur.

VVEERRSSIIOONNSS
       The _p_w___g_e_c_o_s field is not specified in POSIX, but is present on most
       implementations.

SSTTAANNDDAARRDDSS
       POSIX.1‐2008.

HHIISSTTOORRYY
       POSIX.1‐2001, SVr4, 4.3BSD.

SSEEEE AALLSSOO
       ffggeettppwweenntt(3), ggeettppww(3), ggeettppwweenntt__rr(3), ggeettppwwnnaamm(3), ggeettppwwuuiidd(3),
       ppuuttppwweenntt(3), ppaasssswwdd(5)

Linux man‐pages (unreleased)      2025‐09‐21                       _g_e_t_p_w_e_n_t(3)
