NSS_TACPLUS(8)            BSD System Manager's Manual           NSS_TACPLUS(8)

NNAAMMEE
     nnssss__ttaaccpplluuss — TACACS+ nsswitch module

SSYYNNOOPPSSIISS
     ppaasssswwdd: ffiilleess ttaaccpplluuss

DDEESSCCRRIIPPTTIIOONN
     The nnssss__ttaaccpplluuss module is a loadable NSS module which provides a minimal
     identity service using a TACACS+ backend.

     Due to the limitations of the TACACS+ protocol, the functionality pro‐
     vided by the nnssss__ttaaccpplluuss module is very limited: it can look up a user by
     name, but not by uid, and it cannot enumerate users.

     To look up a user, the nnssss__ttaaccpplluuss module submits an authorization re‐
     quest with authentication method TAC_PLUS_AUTHEN_METH_NOT_SET, authenti‐
     cation type TAC_PLUS_AUTHEN_TYPE_NOT_SET, and authentication service
     TAC_PLUS_AUTHEN_SVC_LOGIN, for the “shell” service.  If the response sta‐
     tus is either TAC_PLUS_AUTHOR_STATUS_PASS_ADD or
     TAC_PLUS_AUTHOR_STATUS_PASS_REPL, the user is considered to exist and the
     nnssss__ttaaccpplluuss module fills out a _s_t_r_u_c_t _p_a_s_s_w_d for it.

     The following attributes, if included in the response from the TACACS+
     server, are used to construct the response:

     _U_I_D    Numeric user ID.  Must be between 0 and UID_MAX.  Defaults to
            65534.

     _G_I_D    Numeric primary group ID.  Must be between 0 and GID_MAX.  De‐
            faults to 65534.

     _C_L_A_S_S  Login class.  Left blank if not provided.

     _G_E_C_O_S  Display name.  If not provided, the user name is used instead.

     _H_O_M_E   Home directory.  Defaults to _/.

     _S_H_E_L_L  Shell.  Defaults to _/_b_i_n_/_s_h.

     Case is ignored when matching attribute names.  If an attribute is in‐
     cluded multiple times, the last value takes effect.

SSEEEE AALLSSOO
     libtacplus(3), tacplus.conf(5), pam_tacplus(8)

HHIISSTTOORRYY
     The nnssss__ttaaccpplluuss module and this manual page were written by Dag-Erling
     SmÃ¸rgrav <_d_e_s_@_F_r_e_e_B_S_D_._o_r_g> for Klara Systems.

BSD                              June 13, 2023                             BSD
