When  the  kernel  encounters a fatal error, the routine arranges
for a snapshot of the contents of physical memory to  be  written
into a dump area, typically in the swap partition.  Upon a subse‐
quent reboot, is typically run out of before swapping is enabled,
to  copy  the kernel and the saved memory image into and enters a
reboot message and information about the core dump into the  sys‐
tem  log.   If  a  directory is not specified, then is used.  The
kernel and core file can then be analyzed  using  various  tools,
including  and  to  attempt  to  deduce  the  cause of the crash.
Crashes are usually the result of hardware faults or kernel bugs.
If a kernel bug is suspected, a full bug report should  be  filed
at  or using containing as much information as possible about the
circumstances of the crash.  Since crash dumps are typically very
large and may contain whatever (potentially confidential)  infor‐
mation  was in memory at the time of the crash, do include a copy
of the crash dump file in the bug report; instead, save it  some‐
where in the event that a developer wants to examine it.  The op‐
tions  are as follows: Only clears the dump without saving it, so
that future invocations of will ignore it.  Forces a dump  to  be
taken  even if the dump doesn’t appear correct or there is insuf‐
ficient disk space.  Check whether a dump is present without tak‐
ing further action.  The command exits with zero status if a dump
is present, or with non‐zero status otherwise.  Use as the kernel
instead of the default (returned by Note that uses to check  that
kernel  file  is  and will default to if the check fails.  Prints
out some additional debugging information.  Compresses  the  core
dump and kernel (see Set the compression level for to Defaults to
1  (the fastest compression mode).  Refer to for more information
regarding the compression level.  checks the core dump in various
ways to make sure that it is current and that it  corresponds  to
the  currently  running  system.   If  it passes these checks, it
saves the core image in and the system  in  (or  in  and  respec‐
tively, if the option is used).  The is the number from the first
line  of  the file and it is incremented and stored back into the
file each time successfully runs.  also checks the available disk
space before attempting to make the copies.  If there is insuffi‐
cient disk space in the file system containing or if the file ex‐
ists and the number of free kilobytes (for non‐superusers) in the
file system after the copies were made would  be  less  than  the
number  in  the  first  line of this file, the copies are not at‐
tempted.  If successfully copies the kernel and  the  core  dump,
the  core  dump is cleared so that future invocations of will ig‐
nore it.  The command appeared in The minfree code does not  con‐
sider the effect of compression.
