MOUNT_NTFS(8)             BSD System Manager's Manual            MOUNT_NTFS(8)

NNAAMMEE
     mmoouunntt__nnttffss — mount an NTFS file system

SSYYNNOOPPSSIISS
     mmoouunntt__nnttffss [--aaii] [--gg _g_r_o_u_p] [--mm _m_a_s_k] [--oo _o_p_t_i_o_n_s] [--uu _u_s_e_r] _s_p_e_c_i_a_l _n_o_d_e

DDEESSCCRRIIPPTTIIOONN
     The mmoouunntt__nnttffss command attaches the NTFS filesystem residing on the de‐
     vice _s_p_e_c_i_a_l to the global filesystem namespace at the location indicated
     by _n_o_d_e.  This command is invoked by mount(8) when using the syntax

       mmoouunntt [options] -t ntfs _s_p_e_c_i_a_l _n_o_d_e

     The _s_p_e_c_i_a_l device is either a disklabel(8) UID (DUID) or an entry in
     _/_d_e_v.  If it is a DUID, it will be automatically mapped to the appropri‐
     ate entry in _/_d_e_v.  In either case the partition must be present in the
     disklabel loaded from the device.  The partition name is the last letter
     in the entry name.  For example, /dev/sd0a and 3eb7f9da875cb9ee.a both
     refer to the ‘a’ partition.

     The supported NTFS versions include both NTFS4, as used by Microsoft Win‐
     dows NT 4.0, and NTFS5, as used by Microsoft Windows 2000 and XP.  Only
     read-only operation is permitted, which is automatically enforced.

     The options are as follows:

     --aa      Force behaviour to return MS-DOS 8.3 names also on rreeaaddddiirr().

     --gg _g_r_o_u_p
             Specifies the group name or GID of the root inode of the file
             system.  Defaults to the mount point's GID.

     --ii      Make name lookup case insensitive for all names except POSIX
             names.

     --mm _m_a_s_k
             Specify the maximum file permissions for files in the file sys‐
             tem.

     --oo _o_p_t_i_o_n_s
             Options are specified with a --oo flag followed by a comma sepa‐
             rated string of options.  See the mount(8) man page for possible
             options and their meanings.

     --uu _u_s_e_r
             Specifies the user name or UID of the root inode of the file sys‐
             tem.  Defaults to the mount point's UID.

FFEEAATTUURREESS
   NNTTFFSS ffiillee aattttrriibbuutteess
     NTFS file attributes can be accessed in the following way:

           foo[[:ATTRTYPE]:ATTRNAME]

     ‘ATTRTYPE’ is one of identifier listed in $AttrDef file of volume.  De‐
     fault is $DATA.  ‘ATTRNAME’ is an attribute name.  Default is none.

     EExxaammpplleess:

     To get volume name (in Unicode):

           # cat /mnt/\$Volume:\$VOLUME_NAME

     To read directory raw data:

           # cat /mnt/foodir:\$INDEX_ROOT:\$I30

SSEEEE AALLSSOO
     mount(2), unmount(2), disklabel(5), fstab(5), disklabel(8), mount(8)

HHIISSTTOORRYY
     Support for NTFS first appeared in FreeBSD 3.0.  It was later ported to
     OpenBSD and first appeared in OpenBSD 3.4.

AAUUTTHHOORRSS
     NTFS kernel implementation, mmoouunntt__nnttffss, and this manual were originally
     written by Semen Ustimenko <_s_e_m_e_n_u_@_F_r_e_e_B_S_D_._o_r_g>.

     The OpenBSD port was done by Julien Bordet <_z_e_j_a_m_e_s_@_g_r_e_y_h_a_t_s_._o_r_g>.

BBUUGGSS
     Only read support is enabled.

     If the NTFS partition is marked as ‘dynamic’ under Microsoft Windows XP,
     it won't be possible to access it under OpenBSD anymore.

BSD                            November 14, 2022                           BSD
