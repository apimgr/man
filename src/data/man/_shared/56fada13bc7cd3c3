THR_SUSPEND(2)              BSD System Calls Manual             THR_SUSPEND(2)

NNAAMMEE
     tthhrr__ssuussppeenndd — suspend the calling thread

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//tthhrr..hh>>

     _i_n_t
     tthhrr__ssuussppeenndd(_s_t_r_u_c_t _t_i_m_e_s_p_e_c _*_t_i_m_e_o_u_t);

DDEESSCCRRIIPPTTIIOONN
     TThhiiss ffuunnccttiioonn iiss iinntteennddeedd ffoorr iimmpplleemmeennttiinngg tthhrreeaaddiinngg..  NNoorrmmaall aapppplliiccaa‐‐
     ttiioonnss sshhoouulldd uussee pthread_cond_timedwait(33) ttooggeetthheerr wwiitthh
     pthread_cond_broadcast(33) ffoorr ttyyppiiccaall ssaaffee ssuussppeennssiioonn wwiitthh ccooooppeerraattiioonn ooff
     tthhee tthhrreeaadd bbeeiinngg ssuussppeennddeedd,, oorr pthread_suspend_np(33) aanndd
     pthread_resume_np(33) iinn ssoommee ssppeecciiffiicc ssiittuuaattiioonnss,, iinnsstteeaadd..

     The tthhrr__ssuussppeenndd() system call puts the calling thread in a suspended
     state, where it is not eligible for CPU time.  This state is exited by
     another thread calling thr_wake(2), when the time interval specified by
     _t_i_m_e_o_u_t has elapsed, or by the delivery of a signal to the suspended
     thread.

     If the _t_i_m_e_o_u_t argument is NULL, the suspended state can be only termi‐
     nated by explicit tthhrr__wwaakkee() or signal.

     If a wake from thr_wake(2) was delivered before the tthhrr__ssuussppeenndd call, the
     thread is not put into a suspended state.  Instead, the call returns im‐
     mediately without an error.

     If a thread previously called thr_wake(2) with its own thread identifier,
     which resulted in setting the internal kernel flag to immediately abort
     interruptible sleeps with an EINTR error (see thr_wake(2)), the flag is
     cleared.  As with thr_wake(2) called from another thread, the next
     tthhrr__ssuussppeenndd call does not result in suspension.

RREETTUURRNN VVAALLUUEESS
     The tthhrr__ssuussppeenndd() function returns the value 0 if successful; otherwise
     the value -1 is returned and the global variable _e_r_r_n_o is set to indicate
     the error.

EERRRROORRSS
     The tthhrr__ssuussppeenndd() operation returns the following errors:

     [EFAULT]           The memory pointed to by the _t_i_m_e_o_u_t argument is not
                        valid.

     [ETIMEDOUT]        The specified timeout expired.

     [ETIMEDOUT]        The _t_i_m_e_o_u_t argument specified a zero time interval.

     [EINTR]            The sleep was interrupted by a signal.

SSEEEE AALLSSOO
     ps(1), thr_wake(2), pthread_resume_np(3), pthread_suspend_np(3)

SSTTAANNDDAARRDDSS
     The tthhrr__ssuussppeenndd() system call is non-standard.

HHIISSTTOORRYY
     The tthhrr__ssuussppeenndd() system call first appeared in FreeBSD 5.2.

BSD                               May 5, 2020                              BSD
