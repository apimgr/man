_m_b_l_e_n(3)                   Library Functions Manual                   _m_b_l_e_n(3)

NNAAMMEE
       mblen - determine number of bytes in next multibyte character

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssttddlliibb..hh>>

       iinntt mmbblleenn((size_t n;
                 ccoonnsstt cchhaarr _s[[_n]],, ssiizzee__tt _n));;

DDEESSCCRRIIPPTTIIOONN
       If  _s is not NULL, the mmbblleenn() function inspects at most _n bytes of the
       multibyte string starting at _s and extracts the next complete multibyte
       character.  It uses a static anonymous shift state known  only  to  the
       mmbblleenn()  function.   If  the  multibyte  character is not the null wide
       character, it returns the number of bytes that were  consumed  from  _s.
       If the multibyte character is the null wide character, it returns 0.

       If  the _n bytes starting at _s do not contain a complete multibyte char‐
       acter, mmbblleenn() returns -1.  This can happen even if _n is  greater  than
       or  equal  to  _M_B___C_U_R___M_A_X,  if  the multibyte string contains redundant
       shift sequences.

       If the multibyte string starting at _s contains an invalid multibyte se‐
       quence before the next complete character, mmbblleenn() also returns -1.

       If _s is NULL, the mmbblleenn() function resets the  shift  state,  known  to
       only  this  function,  to the initial state, and returns nonzero if the
       encoding has nontrivial shift state, or zero if the encoding is  state‐
       less.

RREETTUURRNN VVAALLUUEE
       The mmbblleenn() function returns the number of bytes parsed from the multi‐
       byte  sequence  starting  at _s, if a non‐null wide character was recog‐
       nized.  It returns 0, if a null wide character was recognized.  It  re‐
       turns  -1,  if  an  invalid multibyte sequence was encountered or if it
       couldn’t parse a complete multibyte character.

AATTTTRRIIBBUUTTEESS
       For an explanation of the terms  used  in  this  section,  see  aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       mmbblleenn() T}   Thread safety  MT‐Unsafe race

VVEERRSSIIOONNSS
       The function mmbbrrlleenn(3) provides a better interface to the same
       functionality.

SSTTAANNDDAARRDDSS
       C11, POSIX.1‐2008.

HHIISSTTOORRYY
       POSIX.1‐2001, C99.

NNOOTTEESS
       The behavior of mmbblleenn() depends on the LLCC__CCTTYYPPEE category of the current
       locale.

SSEEEE AALLSSOO
       mmbbrrlleenn(3)

Linux man‐pages (unreleased)      2025‐06‐28                          _m_b_l_e_n(3)
