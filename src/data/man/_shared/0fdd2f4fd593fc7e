is  used  to create and verify detached X509 signatures of files.
Private keys and certificates are expected  to  be  PEM  encoded,
signatures  are  in  PEM/SMIME  format.  Supported commands: Sign
placing the signature in The options and are  required  for  this
command.   Verify  signature  for  If  is not specified, is used.
This is a short cut for verify with the option  code.   Supported
options:  A  file containing one or more (concatenated) keys that
are considered trusted.  A file  containing  additional  certifi‐
cates  that  will  be  added  to the signature when creating one.
They will be used to fill missing links in the trust  chain  when
verifying  the  signature.   A file containing the certificate to
use for signing.  The certificate must match the key given  by  A
file  containing the private key to use for signing.  Verify that
the extended  key‐usage  attribute  in  the  signing  certificate
matches  Otherwise, the signature is rejected.  can be one of: or
Print verbose information about the signing certificate.   Create
signature  file  for  file  The  private key is found in file the
matching certificate is in additional certificates from  are  in‐
cluded  in  the  created signature.  Verify that the signature is
valid for file and that the signing certificate allows code sign‐
ing.  Certificates in are considered trusted, and there must be a
certificate chain from one of those certificates to  the  signing
certificate.   As there is currently no default trust anchor, you
must explicilty specify one with otherwise  no  verification  can
succeed.
