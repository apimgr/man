MAC_IS_PRESENT(3)        BSD Library Functions Manual        MAC_IS_PRESENT(3)

NNAAMMEE
     mmaacc__iiss__pprreesseenntt — report whether the running system has MAC support

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//mmaacc..hh>>

     _i_n_t
     mmaacc__iiss__pprreesseenntt(_c_o_n_s_t _c_h_a_r _*_p_o_l_i_c_y_n_a_m_e);

DDEESSCCRRIIPPTTIIOONN
     The mmaacc__iiss__pprreesseenntt() function determines whether the currently-running
     kernel supports MAC for a given policy or not.  If _p_o_l_i_c_y_n_a_m_e is
     non-NULL, the presence of the named policy (e.g. “biba”, “mls”, “te”) is
     checked, otherwise the presence of any MAC policies at all is checked.

RREETTUURRNN VVAALLUUEESS
     If the system supports the given MAC policy, the value 1 is returned.  If
     the specified MAC policy is not supported, the value 0 is returned.  If
     an error occurs, the value -1 is returned.

EERRRROORRSS
     [EINVAL]           The value of _p_o_l_i_c_y_n_a_m_e is not valid.

     [ENOMEM]           Insufficient memory was available to allocate internal
                        storage.

SSEEEE AALLSSOO
     mac(3), mac_free(3), mac_get(3), mac_prepare(3), mac_set(3), mac_text(3),
     mac(4), mac(9)

HHIISSTTOORRYY
     Support for Mandatory Access Control was introduced in FreeBSD 5.0 as
     part of the TrustedBSD Project.

BSD                              July 7, 2006                              BSD
