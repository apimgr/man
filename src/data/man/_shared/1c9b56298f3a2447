The interface uses a memory mapped regular file as a virtual Eth‐
ernet  bus.   All  interfaces  connected to the same bus see each
others’ traffic.  Using a memory mapped regular file as a bus has
two implications: The bus identifier is not in flat global  name‐
space.   Configuring  and using the interface is possible without
superuser privileges on the host (normal host file access permis‐
sions for the bus hold).  It is not possible to  directly  access
the  host  networking  facilities from a rump kernel using purely
However, traffic can be routed to another  rump  kernel  instance
which  provides both and networking.  An interface can be created
in two ways: Programmatically by  calling  The  bus  pathname  is
passed  in The number of the newly created interface is available
after a successful call by dereferencing Dynamically  at  runtime
with  or equivalent using the command.  In this case the bus path
must be configured with before the interface address can be  con‐
figured.   Destroying an interface is possible only via An inter‐
face emulates TX/RX offload options in software.  They are speci‐
fied by Alternatively, its flag is directly specified by environ‐
ment variable for example: for all TX/RX offload for all  TX  of‐
fload for all RX offload See for more details.
