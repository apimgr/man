QUICK_EXIT(3)            BSD Library Functions Manual            QUICK_EXIT(3)

NNAAMMEE
     qquuiicckk__eexxiitt — exits a program quickly, running minimal cleanup

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssttddlliibb..hh>>

     ___N_o_r_e_t_u_r_n _v_o_i_d
     qquuiicckk__eexxiitt(_i_n_t _s_t_a_t_u_s);

DDEESSCCRRIIPPTTIIOONN
     The qquuiicckk__eexxiitt() function exits the program quickly calling any cleanup
     functions registered with at_quick_exit(3) but not any C++ destructors or
     cleanup code registered with atexit(3).  The stdio(3) file buffers are
     not flushed.

     The function qquuiicckk__eexxiitt() is _a_s_y_n_c_-_s_i_g_n_a_l _s_a_f_e when the functions regis‐
     tered with at_quick_exit(3) are.

RREETTUURRNN VVAALLUUEESS
     The qquuiicckk__eexxiitt() function does not return.

SSEEEE AALLSSOO
     at_quick_exit(3), exit(3)

SSTTAANNDDAARRDDSS
     The qquuiicckk__eexxiitt() function conforms to ISO/IEC 9899:2011 (“ISO C11”).

BSD                            November 4, 2017                            BSD
