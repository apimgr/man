FPGETROUND(3)            BSD Library Functions Manual            FPGETROUND(3)

NNAAMMEE
     ffppggeettrroouunndd, ffppsseettrroouunndd, ffppsseettpprreecc, ffppggeettpprreecc, ffppggeettmmaasskk, ffppsseettmmaasskk,
     ffppggeettssttiicckkyy, ffpprreesseettssttiicckkyy — IEEE floating point interface

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<iieeeeeeffpp..hh>>

     typedef enum {
             FP_RN,          /* round to nearest */
             FP_RM,          /* round down to minus infinity */
             FP_RP,          /* round up to plus infinity */
             FP_RZ           /* truncate */
     } fp_rnd_t;

     _f_p___r_n_d___t
     ffppggeettrroouunndd(_v_o_i_d);

     _f_p___r_n_d___t
     ffppsseettrroouunndd(_f_p___r_n_d___t _d_i_r_e_c_t_i_o_n);

     typedef enum {
             FP_PS,          /* 24 bit (single-precision) */
             FP_PRS,         /* reserved */
             FP_PD,          /* 53 bit (double-precision) */
             FP_PE           /* 64 bit (extended-precision) */
     } fp_prec_t;

     _f_p___p_r_e_c___t
     ffppggeettpprreecc(_v_o_i_d);

     _f_p___p_r_e_c___t
     ffppsseettpprreecc(_f_p___p_r_e_c___t _p_r_e_c_i_s_i_o_n);

     #define fp_except_t     int
     #define FP_X_INV        0x01    /* invalid operation */
     #define FP_X_DNML       0x02    /* denormal */
     #define FP_X_DZ         0x04    /* zero divide */
     #define FP_X_OFL        0x08    /* overflow */
     #define FP_X_UFL        0x10    /* underflow */
     #define FP_X_IMP        0x20    /* (im)precision */
     #define FP_X_STK        0x40    /* stack fault */

     _f_p___e_x_c_e_p_t___t
     ffppggeettmmaasskk(_v_o_i_d);

     _f_p___e_x_c_e_p_t___t
     ffppsseettmmaasskk(_f_p___e_x_c_e_p_t___t _m_a_s_k);

     _f_p___e_x_c_e_p_t___t
     ffppggeettssttiicckkyy(_v_o_i_d);

     _f_p___e_x_c_e_p_t___t
     ffpprreesseettssttiicckkyy(_f_p___e_x_c_e_p_t___t _s_t_i_c_k_y);

DDEESSCCRRIIPPTTIIOONN
     The routines described herein are deprecated.  New code should use the
     functionality provided by fenv(3).

     When a floating point exception is detected, the exception sticky flag is
     set and the exception mask is tested.  If the mask is set, then a trap
     occurs.  These routines allow both setting the floating point exception
     masks, and resetting the exception sticky flags after an exception is de‐
     tected.  In addition, they allow setting the floating point rounding mode
     and precision.

     The ffppggeettrroouunndd() function returns the current floating point rounding
     mode.

     The ffppsseettrroouunndd() function sets the floating point rounding mode and re‐
     turns the previous mode.

     The ffppggeettpprreecc() function returns the current floating point precision.

     The ffppsseettpprreecc() function sets the floating point precision and returns
     the previous precision.

     The ffppggeettmmaasskk() function returns the current floating point exception
     masks.

     The ffppsseettmmaasskk() function sets the floating point exception masks and re‐
     turns the previous masks.

     The ffppggeettssttiicckkyy() function returns the current floating point sticky
     flags.

     The ffpprreesseettssttiicckkyy() function clears the floating point sticky flags and
     returns the previous flags.

     Sample code which prevents a trap on divide-by-zero:

           fpsetmask(~FP_X_DZ);
           a = 1.0;
           b = 0;
           c = a / b;
           fpresetsticky(FP_X_DZ);
           fpsetmask(FP_X_DZ);

IIMMPPLLEEMMEENNTTAATTIIOONN NNOOTTEESS
     The ffppggeettpprreecc() and ffppsseettpprreecc() functions provide functionality unavail‐
     able on many platforms.  At present, they are implemented only on the
     i386 and amd64 platforms.  Changing precision is not a supported feature:
     it may be ineffective when code is compiled to take advantage of SSE, and
     many library functions and compiler optimizations depend upon the default
     precision for correct behavior.

SSEEEE AALLSSOO
     fenv(3), isnan(3)

HHIISSTTOORRYY
     These routines are based on SysV/386 routines of the same name.

CCAAVVEEAATTSS
     After a floating point exception and before a mask is set, the sticky
     flags must be reset.  If another exception occurs before the sticky flags
     are reset, then a wrong exception type may be signaled.

BSD                            December 3, 2010                            BSD
