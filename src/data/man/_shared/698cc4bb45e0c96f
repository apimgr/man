The  parameter is a socket.  In general, may be used as a substi‐
tute for cases when and are issued in succession, as  well  as  a
mechanism to transmit data at connection establishment time.  The
system  call  uses a structure to minimize the number of directly
supplied arguments. This structure has the following form, as de‐
fined in  typedef  struct  sa_endpoints  {          unsigned  int
sae_srcif;       /* optional source interface   */         struct
sockaddr  *sae_srcaddr;     /*  optional  source  address      */
        socklen_t         sae_srcaddrlen;  /*  size of source ad‐
dress      */         struct sockaddr *sae_dstaddr;    /*  desti‐
nation  address          */          socklen_t         sae_dstad‐
drlen; /* size of destination address  */  }sa_endpoints_t;  When
the  optional  source  address  parameter is specified, binds the
connection to the address, as if is used.  The length  of  buffer
is specified by The source address can be obtained by calling The
optional  parameter  may also be specified, in order to force the
connection to use the interface whose interface index  equals  to
The  value  for  may  be obtained by issuing a call to If only is
specified, the communication domain will choose a source  address
on that interface for communicating to the peer socket.  Both and
parameters may also be specified in order to add more constraints
to  the connection, and will fail unless the address is currently
assigned to that interface.  A destination address must be speci‐
fied in the parameter.  The specifies the length of that  buffer.
Data  to be transmitted may optionally be defined via the buffers
specified by members of the array, along with a non‐NULL  parame‐
ter,  which  upon success, indicates the number of bytes enqueued
for transmission.  When the and parameters are non‐NULL, the com‐
munication domain will copy the data to the socket  send  buffer.
The communication domain may impose a limit on the amount of data
allowed to be buffered before connection establishment.  When the
flags  parameter is set to CONNECT_RESUME_ON_READ_WRITE and an is
not passed in, the communication domain will trigger  the  actual
connection  establishment  upon the first read or write following
the system call. This flag is ignored if the iov is specified  in
the  call  itself.   The  flags parameter may also be set to CON‐
NECT_DATA_IDEMPOTENT to indicate to the communication domain that
the data is idempotent. For example, this will trigger  TCP  Fast
Open (RFC 7413) with SOCK_STREAM type. The data must be passed in
the parameter in , or passed in with the first write call such as
with   the   or   similar   system   call   if   the  CONNECT_RE‐
SUME_ON_READ_WRITE is also set.  In  general,  the  communication
domain  makes  the  final decision on the amount of data that may
get transmitted at connection establishment time.  If the  socket
requires the data be sent atomically and the data size makes this
impossible, EMSGSIZE will be returned and the state of the socket
is  left  unchanged  as  if was not called.  The parameter is re‐
served for future use, and must always be set to The parameter is
also reserved for future use and should be set to NULL.  is  cur‐
rently  supported  only  on  AF_INET and AF_INET6 sockets of type
SOCK_DGRAM and SOCK_STREAM.  Generally, connection‐oriented sock‐
ets may successfully only once.  Connectionless sockets  may  use
to  create  an association to the peer socket, and it may call to
dissolve any existing  association.   Unlike  connection‐oriented
sockets,  connectionless sockets may call again afterwards to as‐
sociate to another peer socket.  If  CONNECT_RESUME_ON_READ_WRITE
is  set  without  data supplied, will immediately return success,
assuming the rest of the parameters  are  valid.   will  indicate
that  the  socket is ready for writing, and the actual connection
establishment is attempted once the initial data  is  written  to
the  socket  via  or  similar.  Subsequent attempts to write more
data will fail until the existing  connection  establishment  at‐
tempt  is  successful.  The error status of the socket may be re‐
trieved via the SO_ERROR option using Upon successful completion,
a value of 0 is returned.  The number of bytes from  array  which
were  enqueued  for  transmission is returned via Upon failure, a
value of ‐1 is returned and the global integer variable is set to
indicate the error.  The system call will fail  if:  The  address
specified in parameter is already in use.  The specified in para‐
meter is not available on this machine, or is not assigned to the
interface  specified by The cannot find any usable addresses of a
specific address family as required by the communication  domain.
A previous connection attempt has not yet been completed.  is not
a  valid descriptor.  The attempt to connect was ignored (because
the target is not listening for connections)  or  explicitly  re‐
jected.   Part  of  or  data  to be written to points outside the
process’s allocated address space.  The  target  host  cannot  be
reached  (e.g.,  down,  disconnected).   The connection cannot be
completed immediately.  It is possible to for completion  by  se‐
lecting the for writing.  Its execution was interrupted by a sig‐
nal.   The  size of the message exceeds the available send buffer
space in the An invalid  argument  was  detected  (e.g.,  is  not
valid, the contents of or buffer is invalid, etc.)  The socket is
already  connected.  The local network interface is not function‐
ing.  The network isn’t reachable from  this  host.   The  system
call  was  unable  to  allocate a needed memory buffer.  is not a
file descriptor for a socket.  Because is listening,  no  connec‐
tion  is allowed.  Connection establishment timed out without es‐
tablishing a connection.  The function call  appeared  in  Darwin
15.0.0
