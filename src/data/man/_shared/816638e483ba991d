PPP(4)                   BSD Kernel Interfaces Manual                   PPP(4)

NNAAMMEE
     pppppp — Point-to-Point Protocol network interface

SSYYNNOOPPSSIISS
     ppsseeuuddoo--ddeevviiccee pppppp

DDEESSCCRRIIPPTTIIOONN
     The pppppp interface allows serial lines to be used as network interfaces
     using the _P_o_i_n_t_-_t_o_-_P_o_i_n_t _P_r_o_t_o_c_o_l (PPP).

     A pppppp interface can be created at runtime using the iiffccoonnffiigg pppppp_N ccrreeaattee
     command or by setting up a hostname.if(5) configuration file for
     netstart(8).

DDIIAAGGNNOOSSTTIICCSS
     pppppp00:: aaffXX nnoott ssuuppppoorrtteedd  The interface was handed a message with ad‐
     dresses formatted in an unsuitable address family; the packet was
     dropped.

SSEEEE AALLSSOO
     inet(4), pppoe(4), pppx(4), hostname.if(5), ifconfig(8), netstart(8),
     pppd(8), pppstats(8)

SSTTAANNDDAARRDDSS
     W. Simpson, _T_h_e _P_o_i_n_t_-_t_o_-_P_o_i_n_t _P_r_o_t_o_c_o_l _(_P_P_P_), RFC 1661, July 1994.

HHIISSTTOORRYY
     The pppppp device appeared in OpenBSD 1.2.

BSD                            February 28, 2024                           BSD
