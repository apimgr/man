The system call is used to manipulate various audit control oper‐
ations.   The argument should point to a structure whose type de‐
pends on the command.  The argument  specifies  the  size  of  in
bytes.   The argument may be any of the following: Set audit pol‐
icy flags.  The argument must point to a value set to one or more
the following audit policy control values bitwise OR’ed together:
and If on space and discontinue  logging  events  until  the  low
space  condition  is  remedied.  If it is not set, audited events
will block until the low space condition is remedied.   Unaudited
events,  however, are unaffected.  If if it cannot write an event
to the global audit log file.  If is set, then the argument  list
passed  to  the system call will be audited.  If is set, then the
environment variables passed to the system call will be  audited.
The default policy is none of the audit policy control flags set.
Set the host information.  The argument must point to a structure
containing the host IP address information.  After setting, audit
records  that  are created as a result of kernel events will con‐
tain this information.  Set the kernel preselection  masks  (suc‐
cess  and  failure).  The argument must point to a structure con‐
taining the mask values as defined in These masks  are  used  for
non‐attributable  audit  event preselection.  The field specifies
which classes of successful audit events are to be logged to  the
audit  trail.   The field specifies which classes of failed audit
events are to be logged.  The value of both fields is the bitwise
OR’ing of the audit event classes specified in The various  audit
classes  are described more fully in Set kernel audit queue para‐
meters.  The argument must point to a structure (defined in  con‐
taining  the  kernel  audit queue control settings: and The field
defines the maximum number of audit record entries in  the  queue
used  to store the audit records ready for delivery to disk.  New
records are inserted at the tail of the queue  and  removed  from
the  head.   For  new  records  which would exceed the high water
mark, the calling thread is inserted into the wait queue, waiting
for the audit queue to have enough  space  available  as  defined
with  the field The field defines the maximum length of the audit
record that can be supplied with The field is unused.  The  field
specifies  the  minimum  amount of free blocks on the disk device
used to store audit records.  If the value of free  blocks  falls
below the configured minimum amount, the kernel informs the audit
daemon  about  low  disk  space.  The value is to be specified in
percent of free file system blocks.  A value of 0  results  in  a
disabling  of  the  check.   The  default and maximum values (de‐
fault/maximum) for the audit queue control parameters are: Return
(Not implemented.)  Return (Not implemented.)  Return (Not imple‐
mented.)  Set the current auditing condition.  The argument  must
point to a value containing the new audit condition, one of or If
is  set,  then auditing is temporarily suspended.  If is set, au‐
diting is resumed.  If is set, the auditing system will shutdown,
draining all audit records and closing out the audit trail  file.
Set  the  event  class preselection mask for an audit event.  The
argument must point to a structure containing the audit event and
mask.  The field is the audit event and is the audit class  mask.
See  for  more  information on audit event to class mapping.  Set
the preselection masks for a process.  The argument must point to
a structure that contains the given process’s audit  preselection
masks  for both success and failure.  The field is the process id
of the target process.  The field must point to a structure which
holds the preselection masks as described in the  section  above.
Set  the  maximum  size of the audit log file.  The argument must
point to a structure with the field set to the maximum audit  log
file size.  A value of 0 indicates no limit to the size.  Set the
audit  sessions flags for the current session.  The argument must
point to an value containing the new audit session flags.   Audit
session  flags may be updated only according to local access con‐
trol policy.  Return the event to class mapping  for  the  desig‐
nated  audit event.  The argument must point to a structure.  See
the section above for more information.  Get the current host in‐
formation.  The argument must point to a structure.   Return  the
audit  settings  for  a  process.   The  argument must point to a
structure which will be set to contain (the audit ID), (the pres‐
election mask), (the terminal ID), and (the audit session ID)  of
the  given  target process.  The process ID of the target process
is passed into the kernel using the field.  See the section above
and for more information.  Return the extended audit settings for
a process.  The argument must point to a structure which is simi‐
lar to the structure described above.  The exception is the  (the
terminal  ID)  field  which points to a structure can hold much a
larger terminal address and an address type.  The process  ID  of
the  target  process  is  passed into the kernel using the field.
See the section above and for more information.  Return  the  ex‐
tended  audit settings for a session.  The argument must point to
a structure.  The audit session  ID  of  the  target  session  is
passed into the kernel using the field.  See for more information
about  the  structure.   Return  the  current kernel preselection
masks.  The argument must point to a structure which will be  set
to  the  current  kernel  preselection masks for non‐attributable
events.  Return the current audit policy setting.   The  argument
must point to a value which will be set to one of the current au‐
dit  policy  flags.   The audit policy flags are described in the
section above.  Return the current kernel audit queue control pa‐
rameters.  The argument must point to a structure which  will  be
set  to  the  current kernel audit queue control parameters.  See
the section above for more information.  Returns the maximum size
of the audit log file.  The argument must point to  a  structure.
The  field  will  be  set  to the maximum audit log file size.  A
value of 0 indicates no limit to the size.  The field will be set
to the current audit log file size.  Returns  the  audit  session
flags  for  the  current  session.  The argument must point to an
value which will be set with the current session  flags.   Return
(Not implemented.)  Return (Not implemented.)  Return (Not imple‐
mented.)   Return  the  current auditing condition.  The argument
must point to a value which will be set to the current audit con‐
dition, one of or See the section  above  for  more  information.
Send a trigger to the audit daemon.  The argument must point to a
value  set  to  one  of  the acceptable trigger values: (low disk
space where the audit log resides), (open a new audit log  file),
(read  the file), (close the current log file and exit), (no disk
space left for audit log file).  (request audit  log  file  rota‐
tion).   (initialize audit subsystem for Mac OS X only).  or (re‐
quest audit log file expiration).  The function will fail if: Re‐
turned by options not yet implemented.  A failure occurred  while
data  transferred to or from the kernel failed.  Illegal argument
was passed by a system call.  The process does  not  have  suffi‐
cient  permission to complete the operation.  The command is spe‐
cific to the and Mac OS X implementations, and is not present  in
Solaris.   The  OpenBSM  implementation was created by McAfee Re‐
search, the security division of McAfee Inc., under  contract  to
Apple  Computer Inc. in 2004.  It was subsequently adopted by the
TrustedBSD Project as the foundation for  the  OpenBSM  distribu‐
tion.  This software was created by McAfee Research, the security
research  division  of McAfee, Inc., under contract to Apple Com‐
puter Inc.  Additional authors include and SPARTA Inc.  The Basic
Security Module (BSM) interface to audit records and audit  event
stream format were defined by Sun Microsystems.  This manual page
was written by and
