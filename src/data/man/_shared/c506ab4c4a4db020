The  file  contains the system‐wide private phone numbers for the
program.  This file is normally unreadable, and  so  may  contain
privileged  information.   The  format of the file is a series of
lines containing whitespace separate fields, of the form: The  is
one  of  those  defined in the file.  The is constructed from any
sequence of characters terminated only by a comma or the  end  of
the  line.   The equals and asterisk characters are indicators to
the auto call units to pause and wait  for  a  second  dial  tone
(when going through an exchange).  The is required by the and the
is  required by the 1030.  Only one phone number per line is per‐
mitted.  However, if more than one line in the file contains  the
same  system name will attempt to dial each one in turn, until it
establishes a connection.  The file appeared in
