RPC(3)                   BSD Library Functions Manual                   RPC(3)

NNAAMMEE
     rrppcc__sseeccuurree — library routines for secure remote procedure calls

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<rrppcc//rrppcc..hh>>

     _A_U_T_H _*
     aauutthhddeess__ccrreeaattee(_c_h_a_r _*_n_a_m_e, _u_n_s_i_g_n_e_d _w_i_n_d_o_w, _s_t_r_u_c_t _s_o_c_k_a_d_d_r _*_a_d_d_r,
         _d_e_s___b_l_o_c_k _*_c_k_e_y);

     _i_n_t
     aauutthhddeess__ggeettuuccrreedd(_s_t_r_u_c_t _a_u_t_h_d_e_s___c_r_e_d _*_a_d_c, _u_i_d___t _*_u_i_d, _g_i_d___t _*_g_i_d,
         _i_n_t _*_g_r_o_u_p_l_e_n, _g_i_d___t _*_g_r_o_u_p_s);

     _i_n_t
     ggeettnneettnnaammee(_c_h_a_r _*_n_a_m_e);

     _i_n_t
     hhoosstt22nneettnnaammee(_c_h_a_r _*_n_a_m_e, _c_o_n_s_t _c_h_a_r _*_h_o_s_t, _c_o_n_s_t _c_h_a_r _*_d_o_m_a_i_n);

     _i_n_t
     kkeeyy__ddeeccrryyppttsseessssiioonn(_c_o_n_s_t _c_h_a_r _*_r_e_m_o_t_e_n_a_m_e, _d_e_s___b_l_o_c_k _*_d_e_s_k_e_y);

     _i_n_t
     kkeeyy__eennccrryyppttsseessssiioonn(_c_o_n_s_t _c_h_a_r _*_r_e_m_o_t_e_n_a_m_e, _d_e_s___b_l_o_c_k _*_d_e_s_k_e_y);

     _i_n_t
     kkeeyy__ggeennddeess(_d_e_s___b_l_o_c_k _*_d_e_s_k_e_y);

     _i_n_t
     kkeeyy__sseettsseeccrreett(_c_o_n_s_t _c_h_a_r _*_k_e_y);

     _i_n_t
     nneettnnaammee22hhoosstt(_c_h_a_r _*_n_a_m_e, _c_h_a_r _*_h_o_s_t, _i_n_t _h_o_s_t_l_e_n);

     _i_n_t
     nneettnnaammee22uusseerr(_c_h_a_r _*_n_a_m_e, _u_i_d___t _*_u_i_d_p, _g_i_d___t _*_g_i_d_p, _i_n_t _*_g_i_d_l_e_n_p,
         _g_i_d___t _*_g_i_d_l_i_s_t);

     _i_n_t
     uusseerr22nneettnnaammee(_c_h_a_r _*_n_a_m_e, _c_o_n_s_t _u_i_d___t _u_i_d, _c_o_n_s_t _c_h_a_r _*_d_o_m_a_i_n);

DDEESSCCRRIIPPTTIIOONN
     These routines are part of the RPC library.  They implement DES Authenti‐
     cation.  See rpc(3) for further details about RPC.

     The aauutthhddeess__ccrreeaattee() is the first of two routines which interface to the
     RPC secure authentication system, known as DES authentication.  The sec‐
     ond is aauutthhddeess__ggeettuuccrreedd(), below.

     Note: the keyserver daemon keyserv(8) must be running for the DES authen‐
     tication system to work.

     The aauutthhddeess__ccrreeaattee() function, used on the client side, returns an au‐
     thentication handle that will enable the use of the secure authentication
     system.  The first argument _n_a_m_e is the network name, or _n_e_t_n_a_m_e, of the
     owner of the server process.  This field usually represents a _h_o_s_t_n_a_m_e
     derived from the utility routine hhoosstt22nneettnnaammee(), but could also represent
     a user name using uusseerr22nneettnnaammee().  The second field is window on the va‐
     lidity of the client credential, given in seconds.  A small window is
     more secure than a large one, but choosing too small of a window will in‐
     crease the frequency of resynchronizations because of clock drift.  The
     third argument _a_d_d_r is optional.  If it is NULL, then the authentication
     system will assume that the local clock is always in sync with the
     server's clock, and will not attempt resynchronizations.  If an address
     is supplied, however, then the system will use the address for consulting
     the remote time service whenever resynchronization is required.  This ar‐
     gument is usually the address of the RPC server itself.  The final argu‐
     ment _c_k_e_y is also optional.  If it is NULL, then the authentication sys‐
     tem will generate a random DES key to be used for the encryption of cre‐
     dentials.  If it is supplied, however, then it will be used instead.

     The aauutthhddeess__ggeettuuccrreedd() function, the second of the two DES authentication
     routines, is used on the server side for converting a DES credential,
     which is operating system independent, into a UNIX credential.  This rou‐
     tine differs from utility routine nneettnnaammee22uusseerr() in that
     aauutthhddeess__ggeettuuccrreedd() pulls its information from a cache, and does not have
     to do a Yellow Pages lookup every time it is called to get its informa‐
     tion.

     The ggeettnneettnnaammee() function installs the unique, operating-system indepen‐
     dent netname of the caller in the fixed-length array _n_a_m_e.  Returns TRUE
     if it succeeds and FALSE if it fails.

     The hhoosstt22nneettnnaammee() function converts from a domain-specific hostname to
     an operating-system independent netname.  Returns TRUE if it succeeds and
     FALSE if it fails.  Inverse of nneettnnaammee22hhoosstt().

     The kkeeyy__ddeeccrryyppttsseessssiioonn() function is an interface to the keyserver dae‐
     mon, which is associated with RPC's secure authentication system (DES au‐
     thentication).  User programs rarely need to call it, or its associated
     routines kkeeyy__eennccrryyppttsseessssiioonn(), kkeeyy__ggeennddeess() and kkeeyy__sseettsseeccrreett().  System
     commands such as login(1) and the RPC library are the main clients of
     these four routines.

     The kkeeyy__ddeeccrryyppttsseessssiioonn() function takes a server netname and a DES key,
     and decrypts the key by using the public key of the server and the secret
     key associated with the effective uid of the calling process.  It is the
     inverse of kkeeyy__eennccrryyppttsseessssiioonn().

     The kkeeyy__eennccrryyppttsseessssiioonn() function is a keyserver interface routine.  It
     takes a server netname and a des key, and encrypts it using the public
     key of the server and the secret key associated with the effective uid of
     the calling process.  It is the inverse of kkeeyy__ddeeccrryyppttsseessssiioonn().

     The kkeeyy__ggeennddeess() function is a keyserver interface routine.  It is used
     to ask the keyserver for a secure conversation key.  Choosing one
     "random" is usually not good enough, because the common ways of choosing
     random numbers, such as using the current time, are very easy to guess.

     The kkeeyy__sseettsseeccrreett() function is a keyserver interface routine.  It is
     used to set the key for the effective _u_i_d of the calling process.

     The nneettnnaammee22hhoosstt() function converts from an operating-system independent
     netname to a domain-specific hostname.  Returns TRUE if it succeeds and
     FALSE if it fails.  Inverse of hhoosstt22nneettnnaammee().

     The nneettnnaammee22uusseerr() function converts from an operating-system independent
     netname to a domain-specific user ID.  Returns TRUE if it succeeds and
     FALSE if it fails.  Inverse of uusseerr22nneettnnaammee().

     The uusseerr22nneettnnaammee() function converts from a domain-specific username to
     an operating-system independent netname.  Returns TRUE if it succeeds and
     FALSE if it fails.  Inverse of nneettnnaammee22uusseerr().

SSEEEE AALLSSOO
     rpc(3), xdr(3), keyserv(8)

     The following manuals:

     _R_e_m_o_t_e _P_r_o_c_e_d_u_r_e _C_a_l_l_s_: _P_r_o_t_o_c_o_l _S_p_e_c_i_f_i_c_a_t_i_o_n.

     _R_e_m_o_t_e _P_r_o_c_e_d_u_r_e _C_a_l_l _P_r_o_g_r_a_m_m_i_n_g _G_u_i_d_e.

     _R_p_c_g_e_n _P_r_o_g_r_a_m_m_i_n_g _G_u_i_d_e.

     _R_P_C_: _R_e_m_o_t_e _P_r_o_c_e_d_u_r_e _C_a_l_l _P_r_o_t_o_c_o_l _S_p_e_c_i_f_i_c_a_t_i_o_n, RFC1050, Sun
     Microsystems Inc., USC-ISI.

BSD                            February 16, 1988                           BSD
