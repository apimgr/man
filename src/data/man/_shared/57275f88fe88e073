INITGROUPS(3)            BSD Library Functions Manual            INITGROUPS(3)

NNAAMMEE
     iinniittggrroouuppss — initialize group access list

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<uunniissttdd..hh>>

     _i_n_t
     iinniittggrroouuppss(_c_o_n_s_t _c_h_a_r _*_n_a_m_e, _g_i_d___t _b_a_s_e_g_i_d);

DDEESSCCRRIIPPTTIIOONN
     The iinniittggrroouuppss() function uses the getgrouplist(3) function to calculate
     the group access list for the user specified in _n_a_m_e.  This group list is
     then setup for the current process using setgroups(2).  The _b_a_s_e_g_i_d is
     automatically included in the groups list.  Typically this value is given
     as the group number from the password file.

RREETTUURRNN VVAALLUUEESS
     The iinniittggrroouuppss() function returns the value 0 if successful; otherwise
     the value -1 is returned and the global variable _e_r_r_n_o is set to indicate
     the error.

EERRRROORRSS
     The iinniittggrroouuppss() function may fail and set _e_r_r_n_o for any of the errors
     specified for the library function setgroups(2).  It may also return:

     [ENOMEM]           The iinniittggrroouuppss() function was unable to allocate tem‐
                        porary storage.

SSEEEE AALLSSOO
     setgroups(2), getgrouplist(3)

HHIISSTTOORRYY
     The iinniittggrroouuppss() function appeared in 4.2BSD.

BSD                            October 26, 2014                            BSD
