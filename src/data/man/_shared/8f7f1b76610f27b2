is  a  fundamental  part of the network information system called
This server provides information from maps to the clients on  the
network.   A map is stored on the server as a database.  A number
of maps is grouped together in a domain.  determines the  domains
it  serves  by looking for a directory with the domain name in In
an effort to improve the security of  (which  has,  historically,
not  been  very  good), this has support for libwrap‐based access
control.  See for more information.  The used for access  control
is  the name which was invoked as (typically If a host is not al‐
lowed to query this server, will return the result code YP_NODOM.
To avoid problems with DNS lookups causing to hang, disables  DNS
lookups  for  its  client lists.  The result is that can only use
address based patterns.  This also means that  wildcard  patterns
such  as  LOCAL  or  KNOWN will not work.  The process pid of the
process can be found in the file The options are as follows:  Use
internet  Domain Name System.  If a query to map or fails, make a
DNS query and return the result if successful.  Run in the  fore‐
ground.   Enable  logging of all requests.  Bind to the specified
instead of dynamically allocating one.  All messages are sent  to
the system log with the facility Error messages have the priority
Refused  requests are logged with the priority All other messages
are logged with the priority This implementation  of  was  origi‐
nally  written  by  The  access control code was later re‐written
from scratch by
