The interface provides support for 802.1Q Virtual Local Area Net‐
works  This  supports  the trunking of more than one network on a
single network interface by  using  802.1Q  tagged  and  untagged
frames.   To use a interface, the administrator must first create
the interface and then specify the VID identifier, the  first  12
bits from a 16‐bit integer which distinguishes each from any oth‐
ers  and  physical interface associated with the This can be done
by using the and subcommands from a shell command line or script.
From within a C program, use the system call with the  and  argu‐
ments.   Packets sent through a interface are tagged with the VID
and passed to the  parent  interface  for  transmission.   Tagged
packets received on the parent interface are passed to the inter‐
face with the corresponding VID associated with the parent inter‐
face.   Packets  sent  directly  through the parent interface are
transmitted as untagged frames.  Untagged frames received on  the
parent  interface  are  handled  by the parent interface.  Tagged
frames received on the parent interface with a VID of  0  and  an
EtherType  of  IP  or IPv6 are processed on the parent interface.
Tagged frames received on the parent interface for which  no  in‐
terface  with  a  matching  VID exists are dropped and counted as
These are displayed by the option.  If the pseudo‐device  is  not
configured  in the kernel only packets tagged with a VID of 0 are
processed.  To be compatible with other 802.1Q devices,  the  in‐
terface  supports  a 1500 byte which means that the parent inter‐
face will have to handle packets that are 4 bytes larger than the
original standard.  can be used with devices not  supporting  the
802.1Q  but  then  the of the interface will be 4 bytes too small
and will not interoperate properly with other 802.1Q devices, un‐
less the of the other hosts on the are  also  lowered  to  match.
The  following  will create interface with VID six, on the inter‐
face ifconfig vlan0 create ifconfig vlan0 vlan 6 vlanif tlp0  Af‐
ter  this  set  up, addresses (and/or other protocols) can be as‐
signed to the interface.  All other hosts  on  the  connected  to
which  configure a and use VID six will see all traffic transmit‐
ted through The same can be created at  system  startup  time  by
placing  the  following  in  create vlan 6 vlanif tlp0 The device
first appeared in and was derived from a implementation that  ap‐
peared  in  and  The  interfaces do not currently inherit changes
made to the physical interfaces’
