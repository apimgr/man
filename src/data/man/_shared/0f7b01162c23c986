SEM_INIT(3)              BSD Library Functions Manual              SEM_INIT(3)

NNAAMMEE
     sseemm__iinniitt — initialize an unnamed semaphore

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<sseemmaapphhoorree..hh>>

     _i_n_t
     sseemm__iinniitt(_s_e_m___t _*_s_e_m, _i_n_t _p_s_h_a_r_e_d, _u_n_s_i_g_n_e_d _i_n_t _v_a_l_u_e);

DDEESSCCRRIIPPTTIIOONN
     The sseemm__iinniitt() function initializes the unnamed semaphore pointed to by
     _s_e_m to have the value _v_a_l_u_e.

     A non-zero value for _p_s_h_a_r_e_d specifies a shared semaphore that can be
     used by multiple processes, the semaphore should be located in shared
     memory region (see mmap(2), shm_open(2), and shmget(2)), any process hav‐
     ing read and write access to address _s_e_m can perform semaphore operations
     on _s_e_m.

     Following a successful call to sseemm__iinniitt(), _s_e_m can be used as an argument
     in subsequent calls to sem_wait(3), sem_trywait(3), sem_post(3), and
     sem_destroy(3).  The _s_e_m argument is no longer valid after a successful
     call to sem_destroy(3).

RREETTUURRNN VVAALLUUEESS
     The sseemm__iinniitt() function returns the value 0 if successful; otherwise the
     value -1 is returned and the global variable _e_r_r_n_o is set to indicate the
     error.

EERRRROORRSS
     The sseemm__iinniitt() function will fail if:

     [EINVAL]           The _v_a_l_u_e argument exceeds SEM_VALUE_MAX.

     [ENOSPC]           Memory allocation error.

SSEEEE AALLSSOO
     sem_destroy(3), sem_getvalue(3), sem_post(3), sem_trywait(3), sem_wait(3)

SSTTAANNDDAARRDDSS
     The sseemm__iinniitt() function conforms to ISO/IEC 9945-1:1996 (“POSIX.1”).

BSD                             January 9, 2010                            BSD
