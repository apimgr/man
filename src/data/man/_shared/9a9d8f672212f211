GSHADOW(5)              File Formats and Configuration              GSHADOW(5)



NNAAMMEE
       gshadow - shadowed group file

DDEESSCCRRIIPPTTIIOONN
       /etc/gshadow contains the shadowed information for group accounts.

       This file must not be readable by regular users if password security is
       to be maintained.

       Each line of this file contains the following colon-separated fields:

       ggrroouupp nnaammee
           It must be a valid group name, which exist on the system.

       eennccrryypptteedd ppaasssswwoorrdd
           Refer to ccrryypptt(3) for details on how this string is interpreted.

           If the password field contains some string that is not a valid
           result of ccrryypptt(3), for instance ! or *, users will not be able to
           use a unix password to access the group (but group members do not
           need the password).

           The password is used when a user who is not a member of the group
           wants to gain the permissions of this group (see nneewwggrrpp(1)).

           This field may be empty, in which case only the group members can
           gain the group permissions.

           A password field which starts with an exclamation mark means that
           the password is locked. The remaining characters on the line
           represent the password field before the password was locked.

           This password supersedes any password specified in /etc/group.

       aaddmmiinniissttrraattoorrss
           It must be a comma-separated list of user names.

           Administrators can change the password or the members of the group.

           Administrators also have the same permissions as the members (see
           below).

       mmeemmbbeerrss
           It must be a comma-separated list of user names.

           Members can access the group without being prompted for a password.

           You should use the same list of users as in /etc/group.

FFIILLEESS
       /etc/group
           Group account information.

       /etc/gshadow
           Secure group account information.

SSEEEE AALLSSOO
       ggppaasssswwdd(5), ggrroouupp(5), ggrrppcckk(8), ggrrppccoonnvv(8), nneewwggrrpp(1).



shadow-utils 4.13                 12/14/2025                        GSHADOW(5)
