processes the named files or the standard input writing the stan‐
dard  output with tabs changed into blanks.  Backspace characters
are preserved into the output and decrement the column count  for
tab  calculations.   is useful for pre‐processing character files
(before sorting, looking at specific columns, etc.) that  contain
tabs.   If  a  single argument is given, then tabs are set spaces
apart instead of the default 8.  If multiple tabstops  are  given
then  the tabs are set at those specific columns.  puts tabs back
into the data from the standard input  or  the  named  files  and
writes the result on the standard output.  Option (with only): By
default,  only leading blanks and tabs are reconverted to maximal
strings of tabs.  If the option is given, then tabs are  inserted
whenever  they would compress the resultant file by replacing two
or more characters.  The command appeared in
