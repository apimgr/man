alters  the scheduling priority of one or more running processes.
The first argument is the new priority to apply, or if is  given,
the  change  to  make (applied additively) to the priority.  This
argument may be negative.  (The interpretation of  priorities  is
discussed  below.)   The  following  parameters  name  the target
processes, as either process IDs,  process  group  IDs,  or  user
names.  The options control the interpretation as follows: Inter‐
pret  parameters  as process group ID’s.  Interpret parameters as
process IDs.  This is the default.  Interpret parameters as  user
names.   Each  parameter  is processed separately and updates the
priority of the processes it names as follows: All  processes  in
the process group are updated to the selected priority.  If an is
used,  the increment is added to the highest priority found among
the members of the process group prior to the change.  The  named
process  is updated to the selected priority.  If an is used, the
increment is added  to  the  process’s  previous  priority.   All
processes  belonging to the specified user are updated to the se‐
lected priority.  If an is used, the increment is  added  to  the
highest  priority found among the processes belonging to the user
prior to the change.  In conventional terminology a  process  re‐
ceives  a  lot of CPU time and a process receives relatively lit‐
tle.  is the inverse concept: a  process  with  a  high  niceness
level  receives  relatively  little  CPU  time.   It is about the
process being nice to the rest of the  system,  rather  than  the
system  being nice to the process.  The numerical priority values
accepted by are priorities but  are  actually  nicenesses.   They
range  from (-20) to (20).  is the highest priority, lowest nice‐
ness, and receives the most CPU time.  is  the  lowest  priority,
highest  niceness, and receives the least CPU time.  This is con‐
fusing but enshrined in historical practice and standards.  If in
doubt, check with processes running with elevated priority  (get‐
ting  more  CPU time) include in the FLAGS column; processes run‐
ning with reduced priority (getting less CPU time)  show  for  in
FLAGS.   The  default  priority  is 0.  At priority 20, processes
will specifically run only when nothing  else  wants  to.   Users
other  than  the  super‐user  may  only  alter  the  priority  of
processes they own, and only by increasing the  niceness.   (This
prevents  overriding  administrative  fiats.)  The super‐user may
alter the priority of any process to any  legal  value.   to  map
user  names  to  user  ID’s  renice  +1  987 ‐u daemon root ‐p 32
changes the  priority  of  process  ID’s  987  and  32,  and  all
processes  owned  by users daemon and root.  The command appeared
in Non‐super‐users cannot increase scheduling priorities of their
own processes, even if they were the ones that decreased the pri‐
orities in the first place.
