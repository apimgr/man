gets  or  verifies a FIDO2 assertion.  The input of is defined by
the parameters of the assertion to be obtained/verified.  See the
section for details.  The output of is defined by the  result  of
the  selected operation.  See the section for details.  If an as‐
sertion is successfully obtained or verified,  exits  0.   Other‐
wise, exits 1.  The options are as follows: Tells to obtain a new
assertion from Tells to verify an assertion using the PEM‐encoded
public  key  in  of  type  where may be (denoting ECDSA over NIST
P‐256 with SHA‐256), (denoting 2048‐bit RSA with PKCS#1.5 padding
and SHA‐256), or (denoting EDDSA over Curve25519  with  SHA‐512).
If  is  not  specified,  is  assumed.  Request the credential’s a
32‐byte symmetric key associated with  the  asserted  credential.
If  obtaining  an  assertion, enable the FIDO2 hmac‐secret exten‐
sion.  If verifying an assertion,  check  whether  the  extension
data  bit was signed by the authenticator.  Causes to emit debug‐
ging output on Tells to read the parameters of the assertion from
instead of Tells to write output on instead of  If  obtaining  an
assertion,  request  user  presence.   If verifying an assertion,
check whether the user presence bit was signed by the authentica‐
tor.  Obtain an assertion using a  resident  credential.   If  is
specified,  will not expect a credential id in its input, and may
output multiple assertions.  Resident credentials are  called  in
CTAP  2.1.  Toggles a key/value where is a string of the form The
options supported at present are: Asks the authenticator for user
presence to be enabled or disabled.  Asks the  authenticator  for
user  verification  to  be enabled or disabled.  Tells whether to
prompt for a PIN and request user verification.  The  option  may
be  specified multiple times.  Obtain an assertion using U2F.  By
default, will use FIDO2 if supported by  the  authenticator,  and
fallback to U2F otherwise.  If obtaining an assertion, prompt the
user for a PIN and request user verification from the authentica‐
tor.  If verifying an assertion, check whether the user verifica‐
tion  bit  was  signed  by the authenticator.  If a is available,
will use it to obtain the PIN.  Otherwise, is used.  The input of
consists of base64 blobs and UTF‐8 strings separated  by  newline
characters  (’\n’).  When obtaining an assertion, expects its in‐
put to consist of: client data hash (base64 blob); relying  party
id  (UTF‐8  string);  credential  id,  if credential not resident
(base64 blob); hmac salt, if the FIDO2 hmac‐secret  extension  is
enabled  (base64  blob); When verifying an assertion, expects its
input to consist of: client  data  hash  (base64  blob);  relying
party id (UTF‐8 string); authenticator data (base64 blob); asser‐
tion  signature  (base64  blob); UTF‐8 strings passed to must not
contain embedded newline or NUL characters.  The output  of  con‐
sists  of  base64  blobs  and  UTF‐8 strings separated by newline
characters (’\n’).  For each generated assertion, outputs: client
data hash (base64 blob); relying party id (UTF‐8 string); authen‐
ticator data (base64 blob); assertion  signature  (base64  blob);
user  id,  if  credential resident (base64 blob); hmac secret, if
the FIDO2 hmac‐secret extension is  enabled  (base64  blob);  the
credential’s   associated  32‐byte  symmetric  key  if  requested
(base64 blob).  When verifying an assertion, produces no  output.
Assuming  contains  a  credential  created according to the steps
outlined in obtain an assertion from an authenticator at and ver‐
ify it:
