WHICH(1)                  BSD General Commands Manual                 WHICH(1)

NNAAMMEE
     wwhhiicchh — locate a program file in the user's path

SSYYNNOOPPSSIISS
     wwhhiicchh [--aass] _p_r_o_g_r_a_m _._._.

DDEESSCCRRIIPPTTIIOONN
     The wwhhiicchh utility takes a list of command names and searches the path for
     each executable file that would be run had these commands actually been
     invoked.

     The following options are available:

     --aa      List all instances of executables found (instead of just the
             first one of each).

     --ss      No output, just return 0 if all of the executables are found, or
             1 if some were not found.

     Some shells may provide a builtin wwhhiicchh command which is similar or iden‐
     tical to this utility.  Consult the builtin(1) manual page.

EEXXAAMMPPLLEESS
     Locate the ls(1) and cp(1) commands:

           $ /usr/bin/which ls cp
           /bin/ls
           /bin/cp

     Same as above with a specific PATH and showing all occurrences:

           $ PATH=/bin:/rescue /usr/bin/which -a ls cp
           /bin/ls
           /rescue/ls
           /bin/cp
           /rescue/cp

     wwhhiicchh will show duplicates if the same executable is found more than
     once:

           $ PATH=/bin:/bin /usr/bin/which -a ls
           /bin/ls
           /bin/ls

     Do not show output.  Just exit with an appropriate return code:

           $ /usr/bin/which -s ls cp
           $ echo $?
           0

           $ /usr/bin/which -s fakecommand
           $ echo $?
           1

SSEEEE AALLSSOO
     builtin(1), csh(1), find(1), locate(1), whereis(1)

HHIISSTTOORRYY
     The wwhhiicchh command first appeared in FreeBSD 2.1.

AAUUTTHHOORRSS
     The wwhhiicchh utility was originally written in Perl and was contributed by
     Wolfram Schneider <_w_o_s_c_h_@_F_r_e_e_B_S_D_._o_r_g>.  The current version of wwhhiicchh was
     rewritten in C by Daniel Papasian <_d_p_a_p_a_s_i_a_@_a_n_d_r_e_w_._c_m_u_._e_d_u>.

BSD                           September 24, 2020                           BSD
