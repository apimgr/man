MTW(4)                   BSD Kernel Interfaces Manual                   MTW(4)

NNAAMMEE
     mmttww — MediaTek USB IEEE 802.11b/g/n wireless network device

SSYYNNOOPPSSIISS
     mmttww** aatt uuhhuubb?? ppoorrtt ??

DDEESSCCRRIIPPTTIIOONN
     The mmttww driver supports USB 2.0 wireless adapters based on the MediaTek
     MT7601U chipset.

     These are the modes the mmttww driver can operate in:

     BSS mode       Also known as _i_n_f_r_a_s_t_r_u_c_t_u_r_e mode, this is used when asso‐
                    ciating with an access point, through which all traffic
                    passes.  This mode is the default.

     monitor mode   In this mode the driver is able to receive packets without
                    associating with an access point.  This disables the in‐
                    ternal receive filter and enables the card to capture
                    packets from networks which it wouldn't normally have ac‐
                    cess to, or to scan for access points.

     The mmttww driver can be configured to use Wired Equivalent Privacy (WEP) or
     Wi-Fi Protected Access (WPA1 and WPA2).  WPA2 is currently the most se‐
     cure encryption standard for wireless networks supported by OpenBSD.  It
     is strongly recommended that neither WEP nor WPA1 are used as the sole
     mechanism to secure wireless communication, due to serious weaknesses.
     WPA1 is disabled by default and may be enabled using the option
     "wwppaapprroottooss _w_p_a_1_,_w_p_a_2".  For standard WPA networks which use pre-shared
     keys (PSK), keys are configured using the "wwppaakkeeyy" option.  WPA-Enter‐
     prise networks require use of the wpa_supplicant package.  The mmttww driver
     offloads both encryption and decryption of data frames to the hardware
     for the WEP40, WEP104, TKIP(+MIC) and CCMP ciphers.

     The mmttww driver can be configured at runtime with ifconfig(8) or on boot
     with hostname.if(5).

FFIILLEESS
     The driver needs the following firmware files, which are loaded when an
     interface is brought up:

           /etc/firmware/mtw-mt7601u

HHAARRDDWWAARREE
     The following adapters should work:

           ASUS USB-N10 v2
           D-Link DWA-127 rev B1
           Edimax EW-7711UAn v2
           Edimax EW-7711ULn v2
           Foxconn WFU03
           Tenda U2
           Tenda W311MI v2
           TP-LINK TL-WN727N v4
           Yealink WF40

EEXXAAMMPPLLEESS
     The following example scans for available networks:

           # ifconfig mtw0 scan

     The following hostname.if(5) example configures mtw0 to join network
     “mynwid”, using WPA key “mywpakey”, obtaining an IP address using DHCP:

           join mynwid wpakey mywpakey
           inet autoconf

DDIIAAGGNNOOSSTTIICCSS
     mmttww00:: eerrrroorr NN,, ccoouulldd nnoott rreeaadd ffiirrmmwwaarree ......  For some reason, the driver
     was unable to read the microcode file from the filesystem.  The file
     might be missing or corrupted.

     mmttww00:: ccoouulldd nnoott llooaadd ffiirrmmwwaarree  An error occurred while attempting to up‐
     load the microcode to the onboard 8051 microcontroller unit.

     mmttww00:: ddeevviiccee ttiimmeeoouutt  A frame dispatched to the hardware for transmission
     did not complete in time.  The driver will reset the hardware.  This
     should not happen.

SSEEEE AALLSSOO
     arp(4), ifmedia(4), intro(4), netintro(4), usb(4), hostname.if(5),
     ifconfig(8)

HHIISSTTOORRYY
     The mmttww driver first appeared in OpenBSD 7.1.

AAUUTTHHOORRSS
     The mmttww driver was written by James Hastings <_h_a_s_t_i_n_g_s_@_o_p_e_n_b_s_d_._o_r_g> based
     on the run(4) driver by Damien Bergamini <_d_a_m_i_e_n_._b_e_r_g_a_m_i_n_i_@_f_r_e_e_._f_r>.

CCAAVVEEAATTSS
     The mmttww driver does not support any of the 802.11n capabilities offered
     by the MT7601U chipset.  Additional work is required in ieee80211(9) be‐
     fore those features can be supported.

BSD                             March 27, 2025                             BSD
