TFTP-PROXY(8)             BSD System Manager's Manual            TFTP-PROXY(8)

NNAAMMEE
     ttffttpp--pprrooxxyy — Internet Trivial File Transfer Protocol proxy

SSYYNNOOPPSSIISS
     ttffttpp--pprrooxxyy [--4466ddvv] [--aa _a_d_d_r_e_s_s] [--ll _a_d_d_r_e_s_s] [--pp _p_o_r_t] [--ww _t_r_a_n_s_w_a_i_t]

DDEESSCCRRIIPPTTIIOONN
     ttffttpp--pprrooxxyy is a proxy for the Internet Trivial File Transfer Protocol.
     TFTP connections should be redirected to the proxy using a pf(4) rule us‐
     ing the _d_i_v_e_r_t_-_t_o option, after which the proxy connects to the server on
     behalf of the client.  The connection from the proxy to the server needs
     to be passed by a rule with divert-reply set.

     The proxy inserts pf(4) pass and/or rdr rules using the _a_n_c_h_o_r facility
     to allow payload packets between the client and the server.  Once the
     rules are inserted, ttffttpp--pprrooxxyy forwards the initial request from the
     client to the server to begin the transfer.  After _t_r_a_n_s_w_a_i_t seconds, the
     states are assumed to have been established and the pf(4) rules are
     deleted and the program exits.  Once the transfer between the client and
     the server is completed, the states will naturally expire.

     The options are as follows:

     --44      Forces ttffttpp--pprrooxxyy to use IPv4 addresses only.

     --66      Forces ttffttpp--pprrooxxyy to use IPv6 addresses only.

     --aa _a_d_d_r_e_s_s
             The proxy will use _a_d_d_r_e_s_s as the source address for the initial
             request from the client to the server for NAT traversal.  Instead
             of a “pass in” rule an “rdr” rule will be generated.  It is pos‐
             sible to have two --aa options to specify both an IPv4 and an IPv6
             address.

     --dd      Do not daemonize.  If this option is specified, ttffttpp--pprrooxxyy will
             run in the foreground and log the client IP, type of request, and
             filename to stderr.

     --ll _a_d_d_r_e_s_s
             Listen on the specified address.  By default ttffttpp--pprrooxxyy listens
             on localhost addresses.

     --pp _p_o_r_t
             Listen on the specified port.  By default ttffttpp--pprrooxxyy listens on
             port 6969.

     --vv      Log the connection and request information to syslogd(8).

     --ww _t_r_a_n_s_w_a_i_t
             Number of seconds to wait for the data transmission to begin be‐
             fore removing the pf(4) rule.  The default is 2 seconds.

CCOONNFFIIGGUURRAATTIIOONN
     To make use of the proxy, pf.conf(5) needs the following rules.  The an‐
     chor is mandatory.  Adjust the rule as needed for your configuration.

           anchor "tftp-proxy/*"
           pass in quick on $int_if inet proto udp from $lan to port tftp \
               divert-to 127.0.0.1 port 6969
           pass out quick on $ext_if inet proto udp from $lan to port tftp \
               group _tftp_proxy divert-reply

SSEEEE AALLSSOO
     tftp(1), pf(4), pf.conf(5), ftp-proxy(8), syslogd(8), tftpd(8)

HHIISSTTOORRYY
     The current stand-alone implementation first appeared in OpenBSD 5.3.

AAUUTTHHOORRSS
     David Gwynne <_d_l_g_@_o_p_e_n_b_s_d_._o_r_g>

BSD                             March 31, 2022                             BSD
