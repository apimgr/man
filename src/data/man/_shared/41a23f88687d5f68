_l_a_n_d_l_o_c_k___r_e_s_t_r_i_c_t___s_e_l_f(2)     System Calls Manual    _l_a_n_d_l_o_c_k___r_e_s_t_r_i_c_t___s_e_l_f(2)

NNAAMMEE
       landlock_restrict_self - enforce a Landlock ruleset

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<lliinnuuxx//llaannddlloocckk..hh>>  /* Definition of LLAANNDDLLOOCCKK__** constants */
       ##iinncclluuddee <<ssyyss//ssyyssccaallll..hh>>     /* Definition of SSYYSS__** constants */

       iinntt ssyyssccaallll((SSYYSS__llaannddlloocckk__rreessttrriicctt__sseellff,, iinntt _r_u_l_e_s_e_t___f_d,,
                   uuiinntt3322__tt _f_l_a_g_s));;

DDEESSCCRRIIPPTTIIOONN
       Once  a Landlock ruleset is populated with the desired rules, the llaanndd‐‐
       lloocckk__rreessttrriicctt__sseellff() system call enforces this ruleset on  the  calling
       thread.  See llaannddlloocckk(7) for a global overview.

       A  thread  can  be restricted with multiple rulesets that are then com‐
       posed together to form the thread’s Landlock domain.  This can be  seen
       as  a  stack of rulesets but it is implemented in a more efficient way.
       A domain can only be updated in such a way that the constraints of each
       past and future composed rulesets will restrict the thread and its  fu‐
       ture  children for their entire life.  It is then possible to gradually
       enforce tailored access  control  policies  with  multiple  independent
       rulesets  coming  from  different sources (e.g., init system configura‐
       tion, user session policy, built‐in application policy).  However, most
       applications should only need one call to llaannddlloocckk__rreessttrriicctt__sseellff()  and
       they  should  avoid arbitrary numbers of such calls because of the com‐
       posed rulesets limit.  Instead, developers are encouraged  to  build  a
       single tailored ruleset with multiple calls to llaannddlloocckk__aadddd__rruullee(2).

       In  order  to  enforce  a  ruleset,  either  the  caller  must have the
       CCAAPP__SSYYSS__AADDMMIINN capability in its user namespace, or the thread must  al‐
       ready  have  the  _n_o___n_e_w___p_r_i_v_s bit set.  As for sseeccccoommpp(2), this avoids
       scenarios where unprivileged processes can affect the behavior of priv‐
       ileged children (e.g., because of set‐user‐ID binaries).  If  that  bit
       was not already set by an ancestor of this thread, the thread must make
       the following call:

              prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0);

       _r_u_l_e_s_e_t___f_d  is  a  Landlock ruleset file descriptor obtained with llaanndd‐‐
       lloocckk__ccrreeaattee__rruulleesseett(2) and fully populated with a set of calls to llaanndd‐‐
       lloocckk__aadddd__rruullee(2).

       _f_l_a_g_s must be 0.

RREETTUURRNN VVAALLUUEE
       On success, llaannddlloocckk__rreessttrriicctt__sseellff() returns 0.  On error,  -1  is  re‐
       turned and _e_r_r_n_o is set to indicate the error.

EERRRROORRSS
       llaannddlloocckk__rreessttrriicctt__sseellff() can fail for the following reasons:

       EEOOPPNNOOTTSSUUPPPP
              Landlock is supported by the kernel but disabled at boot time.

       EEIINNVVAALL _f_l_a_g_s is not 0.

       EEBBAADDFF  _r_u_l_e_s_e_t___f_d is not a file descriptor for the current thread.

       EEBBAADDFFDD _r_u_l_e_s_e_t___f_d is not a ruleset file descriptor.

       EEPPEERRMM  _r_u_l_e_s_e_t___f_d  has no read access to the underlying ruleset, or the
              calling thread is not running with _n_o___n_e_w___p_r_i_v_s, or  it  doesn’t
              have the CCAAPP__SSYYSS__AADDMMIINN in its user namespace.

       EE22BBIIGG  The maximum number of composed rulesets is reached for the call‐
              ing thread.  This limit is currently 64.

SSTTAANNDDAARRDDSS
       Linux.

HHIISSTTOORRYY
       Linux 5.13.

EEXXAAMMPPLLEESS
       See llaannddlloocckk(7).

SSEEEE AALLSSOO
       llaannddlloocckk__ccrreeaattee__rruulleesseett(2), llaannddlloocckk__aadddd__rruullee(2), llaannddlloocckk(7)

Linux man‐pages (unreleased)      2025‐05‐17         _l_a_n_d_l_o_c_k___r_e_s_t_r_i_c_t___s_e_l_f(2)
