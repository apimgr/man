A is a system‐level notification delivered to a process.  Signals
may  be  generated  as the result of process activity, by certain
user inputs, by kernel facilities or subsystems, or sent program‐
matically by other processes or by users.  There is a small fixed
set of signals, each with a symbolic name and a number.  For his‐
torical reasons many of the numbers  are  ‘‘well‐known  values’’,
which are in practice the same on all implementations and realis‐
tically can never be changed.  (Nonetheless, compiled code should
always use only the symbolic names.)  Many/most signals also have
specific  semantics,  both  in  how  they can be generated and in
their effects.  Some are special cases in ways  that  have  quite
far‐reaching  consequences.   When  a  signal is to a process, in
general any of several things can happen.   If  the  process  has
elected  to  the  signal,  it  is  discarded and nothing happens.
(Some signals may not be ignored, however.)  If the  process  has
elected  to  the  signal temporarily, delivery is postponed until
the process later unblocks that signal.  Otherwise, the signal is
meaning that whatever the process is doing is interrupted in  or‐
der  to react to the signal.  (Note that processes that are wait‐
ing in the kernel must unwind what they are doing for signals  to
be  delivered.  This can sometimes be expensive.  See for further
information.)  If the process has elected to  the  signal,  which
means  that  the  process has installed a handler to react to the
signal in some process‐specific way, the kernel arranges for  the
process’s  handler logic to be invoked.  This is always done in a
way that allows the process to resume if  desired.   (Note,  how‐
ever,  that  some signals may not be caught.)  Otherwise, the de‐
fault action for the signal is taken.  For most signals  the  de‐
fault  action  is  to  terminate  the process and generate a core
dump.  See the table below.  Note that the term is also used  for
the  specific process of arranging for a signal handler to be in‐
voked.  In general, signals are delivered as  soon  as  they  are
posted.   (Some delays may occur due to scheduling.)  However, in
some cases a process that has been sleeping  in  the  kernel  may
need  to  do slow things as part of unwinding its state; this can
sometimes lead to human‐perceptible  delays.   Also,  some  sleep
states  within  the  kernel  are meaning that signals posted will
have no effect until the state clears.  These states are supposed
to be short‐term only, but sometimes kernel bugs  make  this  not
the  case  and  one  can  end up with unkillable processes.  Such
processes appear in state "D" in In general the only way  to  get
rid of them is to reboot.  (However, when the "wchan" reported is
"tstile",  it means the process is waiting for some other process
to release resources; sometimes if one can  find  and  kill  that
process the situation is recoverable.)  The following signals are
defined  in These are numbered 1 to 32.  (There is no signal 0; 0
is a reserved value that can be used as a no‐op with some  signal
operations.)   Detailed  descriptions  of  these  signals follow.
This signal is generated by the driver to indicate a hangup  con‐
dition  on a process’s controlling terminal: the user has discon‐
nected.  Accordingly, the default  action  is  to  terminate  the
process.   This signal is also used by many daemons, such as as a
cue to reload configuration.  The number for is 1, which is quite
well known.  This signal is generated by the driver when the user
presses the interrupt character, normally control‐C.  The default
action is to terminate the process.  The number for is  2.   This
signal  is generated by the driver when the user presses the quit
character, normally control‐backspace.  The default action is  to
terminate  the process and dump core.  The number for is 3.  This
signal is generated synchronously by the kernel when the  process
executes an invalid instruction.  The default action is to termi‐
nate  the  process and dump core.  Note: the results of executing
an illegal instruction when is blocked or  ignored  are  formally
unspecified.   The  number  for is 4.  This signal is used when a
process is being traced (see to indicate  that  the  process  has
stopped at a breakpoint or after single‐stepping.  It is normally
intercepted by the debugger and not exposed to the debuggee.  The
default  action  is  to terminate the process and dump core.  The
number for is 5.  This signal is generated when the standard  li‐
brary function is called.  The default action is to terminate the
process  and  dump  core.   The number for is 6.  This number was
also formerly used for which is no longer defined, as it was spe‐
cific to the PDP‐11 instruction In theory this signal  is  gener‐
ated  when  an instruction needs to be emulated.  The default ac‐
tion is to terminate the process and dump core.  The  number  for
is  7.   This  signal is generated when an invalid floating point
operation is detected by hardware or  by  a  soft‐float  library.
The  default  action  is  to terminate the process and dump core.
The number for is 8.  This signal cannot be  caught  or  ignored.
The  (unconditional)  action  is to terminate the process.  It is
most often sent by system administrators, but is  also  generated
by the kernel in response to running completely out of memory and
swap  space.   Note  that  because many processes need to perform
cleanup before exiting, it is usually best (as a user or adminis‐
trator) to not deploy until a process has failed  to  respond  to
other  signals.   The  number  for  is 9, which is extremely well
known.  This signal is generated synchronously by the kernel when
the process performs certain kinds of  invalid  memory  accesses.
The  most common cause of is an unaligned memory access; however,
on some architectures it may cover other memory conditions,  such
as  attempts  to  access memory belonging to the kernel.  The de‐
fault action is to terminate the process and  dump  core.   Note:
the  results  of performing such invalid accesses when is blocked
or ignored are formally unspecified.  The number for is 10.  This
signal is generated synchronously by the kernel when the  process
attempts  to access unmapped memory, or access memory in a manner
that the protection settings for that memory region do  not  per‐
mit.   On some architectures other assorted permission or protec‐
tion errors also yield On  passing  invalid  pointers  to  system
calls  will yield failure with but not also The default action is
to terminate the process and dump core.  Note: the results of  an
invalid memory access when is blocked or ignored are formally un‐
specified.  The number for is 11, which is very well known.  This
signal  is  generated  by the kernel, in addition to failing with
when a system call is made using an invalid system  call  number.
The  default  action  is  to terminate the process and dump core.
The number for is 12.  This signal is generated by the kernel, in
addition to failing with when a call or similar is made on a pipe
or socket that has been closed and has no readers.   The  default
action  is to terminate the process.  The number for is 13.  This
signal is generated by the kernel when a real‐time timer expires.
See and The default action is to terminate the process.  The num‐
ber for is 14.  This signal is the default  signal  sent  by  and
represents  a  user  or administrator request that a program shut
down.  It is sent to all processes as part of the procedure.  The
default action is to  terminate  the  process.   The  number  for
is 15.  This signal is generated when an ‘‘urgent condition’’ ex‐
ists  on  a socket.  In practice this means when out‐of‐band data
has arrived.  The default action is to do  nothing.   The  number
for is 16.  This signal cannot be caught or ignored.  The (uncon‐
ditional)  action  is  to  stop the process.  Note that like with
(and for similar reasons) it is best to not send this signal  un‐
til  a  process  has  failed to respond to It can also be used by
processes to stop themselves after catching A process that is ex‐
plicitly stopped will not run again until told to with The number
for is 17.  This signal is generated by the driver when the  user
presses  the stop character, normally control‐Z.  The default ac‐
tion is to stop the process.  The number for is 18.  This  signal
is  generated  by  the  job‐control  feature  of shells to manage
processes.  It causes the target process to start executing again
after previously being stopped.  This happens as  a  magic  extra
effect the signal is actually delivered.  The default action when
the  signal is delivered is to do nothing (else).  The number for
is 19.  This signal is generated by the  kernel  when  one  of  a
process’s  immediate  children  exits and can be waited for using
one of the family of functions.  The  default  action  is  to  do
nothing.   As  a  special  case, if a child exits when its parent
process has ignored by having its signal handler set to or if the
signal action has the flag set then the child is detached so that
in the parent will wait for children to exit and then  fail  with
without  returning  any  information  about  any  specific  child
processes.  The number for is 20.  This signal was spelled in old
System V versions and today many systems provide both  spellings.
This signal is generated by the driver when a process that is not
in  the  foreground  of its controlling terminal attempts to read
from this terminal.  The default action is to stop  the  process.
The  number  for  is  21.  This signal is generated by the driver
when a process that is not in the foreground of  its  controlling
terminal  attempts  to write to this terminal, if the terminal is
configured accordingly, which is not the default.  (See  The  de‐
fault action is to stop the process.  The number for is 22.  This
signal  is sent by the kernel when I/O becomes possible on a file
handle opened for asynchronous access with See  and  The  default
action  is  to do nothing.  The number for is 23.  This signal is
sent by the kernel when the amount of CPU time  consumed  exceeds
the  configured  limit.   See and the and builtins of and respec‐
tively.  The default action is to  terminate  the  process.   The
number for is 24.  This signal is sent by the kernel when a write
causes  the  size  of a file to exceed the configured limit.  See
and the and builtins of and respectively.  The default action  is
to  terminate the process.  The number for is 25.  This signal is
generated by the kernel when a  virtual‐time  (process  execution
time)  timer expires.  See and The default action is to terminate
the process.  The number for is 26.  This signal is generated  by
the  kernel  when a profiling timer expires.  See and The default
action is to terminate the process.  The number for is 27.   This
signal  is generated by the driver when the stored window size of
the process’s controlling terminal has changed.  The default  ac‐
tion  is  to  do  nothing.  The number for is 28.  This signal is
generated by the driver when the user presses the status  request
character, normally control‐T.  The default action is to do noth‐
ing.   The number for is 29.  This signal is not generated by the
system and is made available for applications to  use  for  their
own purposes.  Many daemons use it for restart or reload requests
of  various  types.   The  default  action  is  to  terminate the
process.  The number for is 30.  This signal is not generated  by
the  system  and  is  made  available for applications to use for
their own purposes.  The  default  action  is  to  terminate  the
process.   The  number for is 31.  This signal is notionally sent
by the kernel or by a privileged monitor process when an external
power failure is detected, and again  when  power  has  been  re‐
stored.   Currently does not in fact send although it is possible
to prepare a custom configuration for that does so.  The  default
action  is  to do nothing.  The number for is 32.  Signals may be
sent with the utility, either by  number  or  the  symbolic  name
without the ‘‘SIG’’ part.  This utility is built into many shells
to  allow  addressing job control jobs.  In C code signals may be
sent using and some other  related  functions.   Signals  may  be
caught  or ignored using or the simpler and blocked using The and
signals are long‐existing Berkeley extensions, available on  most
systems.   The signal comes from System V.  The remaining signals
conform to was introduced in
