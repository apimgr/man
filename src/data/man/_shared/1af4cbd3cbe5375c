J0(3)                    BSD Library Functions Manual                    J0(3)

NNAAMMEE
     jj00, jj00ff, jj11, jj11ff, jjnn, jjnnff, yy00, yy00ff, yy11, yy11ff, yynn, yynnff — Bessel functions
     of first and second kind

LLIIBBRRAARRYY
     Math Library (libm, -lm)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<mmaatthh..hh>>

     _d_o_u_b_l_e
     jj00(_d_o_u_b_l_e _x);

     _f_l_o_a_t
     jj00ff(_f_l_o_a_t _x);

     _d_o_u_b_l_e
     jj11(_d_o_u_b_l_e _x);

     _f_l_o_a_t
     jj11ff(_f_l_o_a_t _x);

     _d_o_u_b_l_e
     jjnn(_i_n_t _n, _d_o_u_b_l_e _x);

     _f_l_o_a_t
     jjnnff(_i_n_t _n, _f_l_o_a_t _x);

     _d_o_u_b_l_e
     yy00(_d_o_u_b_l_e _x);

     _f_l_o_a_t
     yy00ff(_f_l_o_a_t _x);

     _d_o_u_b_l_e
     yy11(_d_o_u_b_l_e _x);

     _f_l_o_a_t
     yy11ff(_f_l_o_a_t _x);

     _d_o_u_b_l_e
     yynn(_i_n_t _n, _d_o_u_b_l_e _x);

     _f_l_o_a_t
     yynnff(_i_n_t _n, _f_l_o_a_t _x);

DDEESSCCRRIIPPTTIIOONN
     The functions jj00(), jj00ff(), jj11(), and jj11ff() compute the Bessel function of
     the first kind of orders 0 and 1 for the real value _x; the functions jjnn()
     and jjnnff() compute the Bessel function of the first kind of the integer
     order _n for the real value _x.

     The functions yy00(), yy00ff(), yy11(), and yy11ff() compute the linearly indepen‐
     dent Bessel function of the second kind of orders 0 and 1 for the posi‐
     tive _r_e_a_l value _x; the functions yynn() and yynnff() compute the Bessel func‐
     tion of the second kind for the integer order _n for the positive _r_e_a_l
     value _x.

RREETTUURRNN VVAALLUUEESS
     These routines return values of their respective Bessel functions.  For
     large positive inputs, they may underflow and return ±0.

     The following applies to yy00(), yy00ff(), yy11(), yy11ff(), yynn(), and yynnff().  If _x
     is negative, including -∞, these routines will generate an invalid excep‐
     tion and return _N_a_N.  If _x is ±0, these routines will generate a divide-
     by-zero exception and return -∞.  If _x is a sufficiently small positive
     number, then yy11(), yy11ff(), yynn(), and yynnff() will generate an overflow ex‐
     ception and return -∞.

SSEEEE AALLSSOO
     math(3)

SSTTAANNDDAARRDDSS
     The jj00(), jj11(), jjnn(), yy00(), yy11(), and yynn() functions conform to IEEE Std
     1003.1-2001 (“POSIX.1”).  The _f_l_o_a_t versions are extensions.

HHIISSTTOORRYY
     This set of functions appeared in Version 7 AT&T UNIX.

BSD                             March 10, 2015                             BSD
