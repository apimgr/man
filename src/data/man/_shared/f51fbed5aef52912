The  utility provides access to the command line or scripts.  The
first two forms lock the specified or while the provided  command
is  executed.   If  the  path does not exist, then a file of that
name is created with mode 0600.  The third form can use an  arbi‐
trary  file  descriptor  that is provided from a shell script for
example: (         flock ‐s 100         # commands to be executed
under the lock ) 100> /path/to/lockfile The following options are
available: Pass a command to a the shell.  Provide debugging out‐
put.  Don’t block and fail immediately if the lock could  not  be
obtained.   Close the file before executing the command.  This is
useful if the child forks and should not  be  holding  the  lock.
Obtain  a  shared lock.  Unlock an existing lock.  This is avail‐
able only for a file descriptor.  On error print  an  explanation
of the failure.  Fail if the lock could not be obtained after Ob‐
tain  an  exclusive  lock.   This is the default.  An utility ap‐
peared in
