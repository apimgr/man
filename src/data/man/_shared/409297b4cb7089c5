The  file  is  read at system startup time if is set to in is run
with the option to load in IPsec manual keys and policies from at
boot time, before any interfaces are configured.  Please see  the
manpage  for all the commands available.  The file resides in The
file appeared in
