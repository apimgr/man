The  driver  provides kernel services necessary for Specifically,
it makes its backend driver collect profiling samples and provide
them to the userland consumer.  The API/ABI is currently  undocu‐
mented  and will likely change in future without keeping compati‐
bility.  On x86, the tprof_x86 module must be loaded in  addition
to the driver.  The driver was written by There is no way to con‐
figure multiple backend drivers statically.
