The  and  utilities flush the file system cache to disk, send all
running processes a wait for up to 30 seconds for  them  to  die,
send  a  to  the survivors and, respectively, power down, halt or
restart the system.  The action is logged, including  entering  a
shutdown record into the login accounting file and sending a mes‐
sage via The options are as follows: Create a dump before halting
or  restarting.   This option is useful for debugging system dump
procedures or capturing the state of a corrupted  or  misbehaving
system.   Suppress  sending  a  message  via  before  halting  or
restarting.  Do not flush the file  system  cache.   This  option
should be used with extreme caution.  It can be used if a disk or
a processor is on fire.  Attempt to powerdown the system.  If the
powerdown  fails,  or the system does not support software power‐
down, the system will halt.  This option is only valid for To en‐
able verbose messages on the console, pass the flag to To  enable
debugging  messages  on  the console, pass the flag to To silence
some shutdown messages on the console, pass the flag  to  Do  not
give  processes  a chance to shut down before halting or restart‐
ing.  This option should not normally be used.  If there are  any
arguments  passed to they are concatenated with spaces and passed
as to the system call.  The string is passed to the  firmware  on
platforms  that  support  it.  Normally, the utility is used when
the system needs to be halted or restarted, giving users  advance
warning  of their impending doom.  A command appeared in The com‐
mand first appeared in Once the command has begun its work, stop‐
ping it before it completes will probably result in a  system  so
crippled  it must be physically reset.  To prevent premature ter‐
mination, the command blocks many signals early in its execution.
However, nothing can defend against deliberate attempts to  evade
this.   This  command  will  stop  the system without running any
scripts.  Amongst other things, this means that swapping will not
be disabled so that can shutdown cleanly.   You  should  normally
use  unless you are running in single user mode.  The single user
shell will ignore the signal.  To avoid waiting for  the  timeout
when rebooting or halting from the single user shell, you have to
or
