LOADER.4TH(8)             BSD System Manager's Manual            LOADER.4TH(8)

NNAAMMEE
     llooaaddeerr..44tthh — loader.conf processing tools

DDEESSCCRRIIPPTTIIOONN
     The file that goes by the name of llooaaddeerr..44tthh is a set of commands de‐
     signed to manipulate loader.conf(5) files.  The default _/_b_o_o_t_/_l_o_a_d_e_r_._r_c
     includes llooaaddeerr..44tthh and uses one of its commands to automatically read
     and process the standard loader.conf(5) files.  Other commands exists to
     help the user specify alternate configurations.

     The commands of llooaaddeerr..44tthh by themselves are not enough for most uses.
     Please refer to the examples below for the most common situations, and to
     loader(8) for additional commands.

     Before using any of the commands provided in llooaaddeerr..44tthh, it must be in‐
     cluded through the command:

           include loader.4th

     This line is present in the default _/_b_o_o_t_/_l_o_a_d_e_r_._r_c file, so it is not
     needed (and should not be re-issued) in a normal setup.

     The commands provided by it are:
           bboooott
           bboooott _k_e_r_n_e_l_n_a_m_e [......]
           bboooott _d_i_r_e_c_t_o_r_y [......]
           bboooott --ffllaagg ......         Boot as specified by the loader.conf(5)
                                  files read.

                                  Depending on the arguments passed, it can
                                  override boot flags and either the kernel
                                  name or the search path for kernel and mod‐
                                  ules.

           bboooott--ccoonnff
           bboooott--ccoonnff _k_e_r_n_e_l_n_a_m_e [......]
           bboooott--ccoonnff _d_i_r_e_c_t_o_r_y [......]
           bboooott--ccoonnff --ffllaagg ......    Works like bboooott described above, but instead
                                  of booting immediately, uses aauuttoobboooott, so it
                                  can be stopped.

           ssttaarrtt                  Reads _/_b_o_o_t_/_d_e_f_a_u_l_t_s_/_l_o_a_d_e_r_._c_o_n_f, all other
                                  loader.conf(5) files specified in it, then
                                  loads the desired kernel and modules (if not
                                  already loaded).  After which you can use
                                  the bboooott or aauuttoobboooott commands or simply exit
                                  (provided _a_u_t_o_b_o_o_t___d_e_l_a_y is not set to NO)
                                  to boot the system.  ssttaarrtt is the command
                                  used in the default _/_b_o_o_t_/_l_o_a_d_e_r_._r_c file
                                  (see loader(8)).

           iinniittiiaalliizzee             Initialize the support library so commands
                                  can be used without executing ssttaarrtt first.
                                  Like ssttaarrtt, it reads
                                  _/_b_o_o_t_/_d_e_f_a_u_l_t_s_/_l_o_a_d_e_r_._c_o_n_f and all other
                                  loader.conf(5) files specified in it (but
                                  does not load kernel or modules).  Returns a
                                  flag on the stack to indicate if any config‐
                                  uration files were successfully loaded.

           rreeaadd--ccoonnff _f_i_l_e_n_a_m_e     Reads and processes a loader.conf(5) file.
                                  Does not proceed to boot.

           eennaabbllee--mmoodduullee _m_o_d_u_l_e   Enables the loading of _m_o_d_u_l_e.

           ddiissaabbllee--mmoodduullee _m_o_d_u_l_e  Disables the loading of _m_o_d_u_l_e.

           ttooggggllee--mmoodduullee _m_o_d_u_l_e   Toggles the loading of _m_o_d_u_l_e on and off.

           sshhooww--mmoodduullee _m_o_d_u_l_e     Shows the information gathered in the
                                  loader.conf(5) files about the module
                                  _m_o_d_u_l_e.

           rreettrryy                  Used inside loader.conf(5) files to specify
                                  the action after a module loading fails.

           iiggnnoorree                 Used inside loader.conf(5) files to specify
                                  the action after a module loading fails.
           ttrryy--iinncclluuddee _f_i_l_e [_f_i_l_e _._._.]
                                  Process script files if they exist.  Each
                                  file, in turn, is completely read into mem‐
                                  ory, and then each of its lines is passed to
                                  the command line interpreter.  If any error
                                  is returned by the interpreter, the try-in‐
                                  clude command aborts immediately, without
                                  reading any other files, and silently re‐
                                  turns without error.

FFIILLEESS
     /boot/loader      The loader(8).
     /boot/loader.4th  llooaaddeerr..44tthh itself.
     /boot/loader.rc   loader(8) bootstrapping script.
     /boot/defaults/loader.conf
                       File loaded by the ssttaarrtt command.

EEXXAAMMPPLLEESS
     Standard _/_b_o_o_t_/_l_o_a_d_e_r_._r_c:

           include /boot/loader.4th
           start

     Load a different kernel with the standard configuration:

           set kernel="kernel.old"
           unload
           boot-conf

     Read an additional configuration file and then proceed to boot:

           unload
           read-conf /boot/special.conf
           boot-conf

     Disable the loading of the splash screen module and bitmap and then pro‐
     ceed to boot:

           unload
           disable-module splash_bmp
           disable-module bitmap
           boot-conf

SSEEEE AALLSSOO
     loader.conf(5), loader(8)

HHIISSTTOORRYY
     The llooaaddeerr..44tthh set of commands first appeared in FreeBSD 3.2.

AAUUTTHHOORRSS
     The llooaaddeerr..44tthh set of commands was written by Daniel C. Sobral
     <dcs@FreeBSD.org>.

BBUUGGSS
     A British espionage series.

BSD                            November 13, 2013                           BSD
