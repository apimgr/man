is used to assign a line to a Bluetooth Host Controller Interface
using  the or line disciplines, and can optionally initialize the
line for a given device before activating  the  line  discipline.
Supported  types  are: Broadcom BCM2035 Broadcom BCM43xx Broadcom
BCM43xx (3‐wire) Generic BCSP (BlueCore Serial Protocol)  Philips
BGB2xx  module Generic UART (this is the default) Cambridge Sili‐
con Radio Casira serial  adapter,  or  Brainboxes  serial  dongle
(BL642)  Ericsson  based modules Digianswer based cards ST Micro‐
electronics minikits based on STLC2410/STLC2415 ST Microelectron‐
ics minikits based on STLC2500 Silicon Wave  kits  Texas  Instru‐
ments  modules Infineon UniStone (PBA31308) modules When the line
discipline is activated, detaches and sleeps until it receives  a
The  command line options are as follows: debug mode.  print ini‐
tialization IO and do not detach.  Disable flow control.   Enable
flow  control.   Specify an alternate for the Bluetooth module to
use during the initialization phase.  Enable odd parity.  Disable
parity.  Enable parity (even parity).  Test mode.  Only  the  su‐
per‐user  may  attach a Bluetooth HCI interface.  Test mode tries
to guess the speed using the received link‐establish packet  from
HCI,  or  if  there is no response.  The program was written with
reference to as provided with the BlueZ tools for Linux and first
appeared in Not all initializations have been tested.
