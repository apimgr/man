RARPD(8)                  BSD System Manager's Manual                 RARPD(8)

NNAAMMEE
     rraarrppdd — reverse ARP daemon

SSYYNNOOPPSSIISS
     rraarrppdd [--aaddfflltt] _i_f_0 [_._._. _i_f_N]

DDEESSCCRRIIPPTTIIOONN
     rraarrppdd services Reverse ARP requests on the Ethernet connected to the
     specified interfaces.  Upon receiving a request, rraarrppdd maps the target
     hardware address to an IP address via its name, which must be present in
     both the ethers(5) and hosts(5) databases.  If a host does not exist in
     both databases, the translation cannot proceed and a reply will not be
     sent.

     In normal operation, rraarrppdd forks a copy of itself and runs in the back‐
     ground.  Anomalies and errors are reported via syslog(3).

     The options are as follows:

     --aa      Listen on all the Ethernets attached to the system.  If --aa is
             omitted, a list of interfaces must be specified.

     --dd      Run in debug mode, with all the output to stderr.  This option
             implies the --ff option.

     --ff      Run in the foreground.

     --ll      Log all requests to syslog(3).

     --tt      Only honour a request if the server (the host that rraarrppdd is run‐
             ning on) can "boot" the target; that is, if a file or directory
             called _/_t_f_t_p_b_o_o_t_/_i_p_a_d_d_r exists, where _i_p_a_d_d_r is the target IP ad‐
             dress expressed in uppercase hexadecimal (only the first 8 char‐
             acters of filenames are checked).

FFIILLEESS
     /etc/ethers  Ethernet host name database.
     /etc/hosts   Host name database.

SSEEEE AALLSSOO
     bpf(4), diskless(8)

SSTTAANNDDAARRDDSS
     R. Finlayson, T. Mann, J. Mogul, and M. Theimer, _A _R_e_v_e_r_s_e _A_d_d_r_e_s_s
     _R_e_s_o_l_u_t_i_o_n _P_r_o_t_o_c_o_l, RFC 903, June 1984.

AAUUTTHHOORRSS
     Craig Leres <_l_e_r_e_s_@_e_e_._l_b_l_._g_o_v> and Steven McCanne <_m_c_c_a_n_n_e_@_e_e_._l_b_l_._g_o_v>,
     Lawrence Berkeley Laboratory, University of California, Berkeley, CA.

BSD                            October 28, 2015                            BSD
