The  function  removes  the  link named by from its directory and
decrements the link count of the file which was referenced by the
link.  If that decrement reduces the link count of  the  file  to
zero,  and no process has the file open, then all resources asso‐
ciated with the file are reclaimed.  If one or more process  have
the file open when the last link is removed, the link is removed,
but the removal of the file is delayed until all references to it
have  been closed.  The system call is equivalent to or except in
the case where specifies a relative path.  In this case  the  di‐
rectory  entry to be removed is determined relative to the direc‐
tory associated with the file descriptor instead of  the  current
working  directory.  The values for are constructed by a bitwise‐
inclusive OR of flags from the following list, defined in  Remove
the directory entry specified by and as a directory, not a normal
file.   If contains a symbolic link in any intermediate directory
of the path, it is not followed and an error is returned instead.
If does not reside in the hierarchy beneath the  starting  direc‐
tory,  an  error  is returned.  If refers to a file with any open
file descriptors, an error is returned.  If is passed the special
value in the parameter, the current working directory is used and
the behavior is identical to a call to or respectively, depending
on whether or not the bit is set in flag.  Upon  successful  com‐
pletion,  a  value of 0 is returned.  Otherwise, a value of ‐1 is
returned and is set to indicate the error.  The system call  will
fail  if: Search permission is denied for a component of the path
prefix.  Write permission is denied on the  directory  containing
the  link  to  be removed.  The entry to be unlinked is the mount
point for a mounted file system.  The file named by the  argument
cannot  be  unlinked because it is being used by the system or by
another process.  The flag parameter has the AT_NODELETEBUSY  bit
set  and  the  file  referred to by has one or more open file de‐
scriptors.  points outside the process’s allocated address space.
An I/O error occurs while deleting the directory entry or deallo‐
cating the inode.  Too many symbolic  links  are  encountered  in
translating  the  pathname.   This is taken to be indicative of a
looping symbolic link.  A component of a pathname exceeds charac‐
ters, or an entire path name exceeds characters  (possibly  as  a
result  of  expanding a symlink).  The named file does not exist.
A component of the path prefix is not  a  directory.   The  named
file  is  a directory and the effective user ID of the process is
not the super‐user.  The directory containing the file is  marked
sticky,  and  neither the containing directory nor the file to be
removed are owned by the effective user ID.  The named  file  re‐
sides  on a read‐only file system.  In addition to the errors re‐
turned by the the may fail if: The argument does not  specify  an
absolute  path  and  the argument is neither nor a valid file de‐
scriptor open for searching.  The parameter has the bit  set  and
the argument names a directory that is not an empty directory, or
there  are hard links to the directory other than dot or a single
entry in dot‐dot.  The parameter has the bit  set  and  does  not
name  a  directory.  The value of the argument is not valid.  The
argument is not an absolute path and is neither nor  a  file  de‐
scriptor  associated with a directory.  The parameter has the bit
set and one of the intermediate directories in the argument is  a
symbolic  link.   The flag parameter has the bit set and does not
reside in the directory hierarchy beneath the starting directory.
The system call is expected to  conform  to  POSIX.1‐2008  .   An
function call appeared in The system call appeared in OS X 10.10
