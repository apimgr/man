The file contains a series of lines of the form The first word of
each line is the of a program invoking (For example, on a typical
system would be a symbolic link to as would and Thus, might be or
etc.)   The  second word of each line is the name of the to actu‐
ally execute when the first name is invoked.  The further if any,
are passed to the followed by the arguments was called with.  The
file may also contain comment lines, denoted by  a  mark  in  the
first  column of any line.  The default mailer is which will also
start by default (unless specifically disabled via an setting) so
that locally generated mail can be delivered, if the  setting  in
is  set  to  /etc/mailer.conf This example shows how to set up to
invoke the program: sendmail        /usr/libexec/postfix/sendmail
mailq               /usr/libexec/postfix/sendmail      newaliases
/usr/libexec/postfix/sendmail  This  example shows the use of the
mini‐sendmail package from in place of # Send outgoing mail to  a
smart        relay        using        mini‐sendmail        send‐
mail        /usr/pkg/sbin/mini‐sendmail     ‐srelayhost     send‐
mail       /usr/pkg/sbin/mini‐sendmail  ‐srelayhost  Note the use
of additional arguments.  appeared in The entire reason this pro‐
gram exists is a crock.  Instead, a command  for  how  to  submit
mail  should be standardized, and all the behavior of things like
should go away.
