_c_r_e_d_e_n_t_i_a_l_s(7)         Miscellaneous Information Manual         _c_r_e_d_e_n_t_i_a_l_s(7)

NNAAMMEE
       credentials - process identifiers

DDEESSCCRRIIPPTTIIOONN
   PPrroocceessss IIDD ((PPIIDD))
       Each  process  has  a unique nonnegative integer identifier that is as‐
       signed when the process is created using ffoorrkk(2).  A process can obtain
       its PID using ggeettppiidd(2).  A PID is represented  using  the  type  _p_i_d___t
       (defined in _<_s_y_s_/_t_y_p_e_s_._h_>).

       PIDs  are  used  in a range of system calls to identify the process af‐
       fected by the call, for example:  kkiillll(2),  ppttrraaccee(2),  sseettpprriioorriittyy(2),
       sseettppggiidd(2), sseettssiidd(2), ssiiggqquueeuuee(3), and wwaaiittppiidd(2).

       A process’s PID is preserved across an eexxeeccvvee(2).

   PPaarreenntt pprroocceessss IIDD ((PPPPIIDD))
       A  process’s parent process ID identifies the process that created this
       process using ffoorrkk(2).  A process can obtain its PPID using ggeettppppiidd(2).
       A PPID is represented using the type _p_i_d___t.

       A process’s PPID is preserved across an eexxeeccvvee(2).

   PPrroocceessss ggrroouupp IIDD aanndd sseessssiioonn IIDD
       Each process has a session ID and a process group ID, both  represented
       using  the  type _p_i_d___t.  A process can obtain its session ID using ggeett‐‐
       ssiidd(2), and its process group ID using ggeettppggrrpp(2).

       A child created by ffoorrkk(2) inherits its parent’s session ID and process
       group ID.  A process’s session ID and process group  ID  are  preserved
       across an eexxeeccvvee(2).

       Sessions  and  process groups are abstractions devised to support shell
       job control.  A process group (sometimes called a "job") is  a  collec‐
       tion  of processes that share the same process group ID; the shell cre‐
       ates a new process group for the process(es)  used  to  execute  single
       command  or  pipeline  (e.g.,  the two processes created to execute the
       command "ls | wc" are placed in the same process group).   A  process’s
       group  membership  can  be  set  using  sseettppggiidd(2).   The process whose
       process ID is the same as its process group ID  is  the  _p_r_o_c_e_s_s  _g_r_o_u_p
       _l_e_a_d_e_r for that group.

       A  session is a collection of processes that share the same session ID.
       All of the members of a process group also have  the  same  session  ID
       (i.e.,  all of the members of a process group always belong to the same
       session, so that sessions and process groups form  a  strict  two‐level
       hierarchy of processes.)  A new session is created when a process calls
       sseettssiidd(2),  which creates a new session whose session ID is the same as
       the PID of the process that called sseettssiidd(2).  The creator of the  ses‐
       sion is called the _s_e_s_s_i_o_n _l_e_a_d_e_r.

       All  of  the  processes in a session share a _c_o_n_t_r_o_l_l_i_n_g _t_e_r_m_i_n_a_l.  The
       controlling terminal is established when the session leader first opens
       a  terminal  (unless  the  OO__NNOOCCTTTTYY  flag  is  specified  when  calling
       ooppeenn(2)).   A  terminal  may be the controlling terminal of at most one
       session.

       At most one of the jobs in a session may be the _f_o_r_e_g_r_o_u_n_d  _j_o_b;  other
       jobs  in  the session are _b_a_c_k_g_r_o_u_n_d _j_o_b_s.  Only the foreground job may
       read from the terminal; when a process in the  background  attempts  to
       read  from  the  terminal,  its process group is sent a SSIIGGTTTTIINN signal,
       which suspends the job.  If the TTOOSSTTOOPP flag has been set for the termi‐
       nal (see tteerrmmiiooss(3)), then only the foreground job  may  write  to  the
       terminal; writes from background jobs cause a SSIIGGTTTTOOUU signal to be gen‐
       erated,  which  suspends  the  job.  When terminal keys that generate a
       signal (such as the _i_n_t_e_r_r_u_p_t key, normally control‐C) are pressed, the
       signal is sent to the processes in the foreground job.

       Various system calls and library functions may operate on  all  members
       of  a process group, including kkiillll(2), kkiillllppgg(3), ggeettpprriioorriittyy(2), sseett‐‐
       pprriioorriittyy(2), iioopprriioo__ggeett(2), iioopprriioo__sseett(2), wwaaiittiidd(2),  and  wwaaiittppiidd(2).
       See  also  the  discussion  of the FF__GGEETTOOWWNN, FF__GGEETTOOWWNN__EEXX, FF__SSEETTOOWWNN, and
       FF__SSEETTOOWWNN__EEXX operations in ffccnnttll(2).

   UUsseerr aanndd ggrroouupp iiddeennttiiffiieerrss
       Each process has various associated user and group IDs.  These IDs  are
       integers, respectively represented using the types _u_i_d___t and _g_i_d___t (de‐
       fined in _<_s_y_s_/_t_y_p_e_s_._h_>).

       On Linux, each process has the following user and group identifiers:

       •  Real  user  ID  and real group ID.  These IDs determine who owns the
          process.  A process can obtain its real user (group)  ID  using  ggee‐‐
          ttuuiidd(2) (ggeettggiidd(2)).

       •  Effective user ID and effective group ID.  These IDs are used by the
          kernel  to determine the permissions that the process will have when
          accessing shared resources such as message  queues,  shared  memory,
          and  semaphores.  On most UNIX systems, these IDs also determine the
          permissions when accessing files.  However, Linux uses the  filesys‐
          tem IDs described below for this task.  A process can obtain its ef‐
          fective user (group) ID using ggeetteeuuiidd(2) (ggeetteeggiidd(2)).

       •  Saved  set‐user‐ID  and  saved  set‐group‐ID.  These IDs are used in
          set‐user‐ID and set‐group‐ID programs to save a copy of  the  corre‐
          sponding  effective  IDs that were set when the program was executed
          (see eexxeeccvvee(2)).  A set‐user‐ID program can assume and  drop  privi‐
          leges  by switching its effective user ID back and forth between the
          values in its real user ID and saved set‐user‐ID.  This switching is
          done via calls to sseetteeuuiidd(2), sseettrreeuuiidd(2), or sseettrreessuuiidd(2).  A  set‐
          group‐ID  program performs the analogous tasks using sseetteeggiidd(2), ssee‐‐
          ttrreeggiidd(2), or sseettrreessggiidd(2).  A process can  obtain  its  saved  set‐
          user‐ID (set‐group‐ID) using ggeettrreessuuiidd(2) (ggeettrreessggiidd(2)).

       •  Filesystem  user ID and filesystem group ID (Linux‐specific).  These
          IDs, in conjunction with the supplementary group IDs  described  be‐
          low,  are  used  to  determine  permissions for accessing files; see
          ppaatthh__rreessoolluuttiioonn(7) for details.  Whenever a process’s effective user
          (group) ID is changed, the kernel  also  automatically  changes  the
          filesystem  user  (group)  ID  to the same value.  Consequently, the
          filesystem IDs normally have the same values  as  the  corresponding
          effective  ID, and the semantics for file‐permission checks are thus
          the same on Linux as on other UNIX systems.  The filesystem IDs  can
          be  made to differ from the effective IDs by calling sseettffssuuiidd(2) and
          sseettffssggiidd(2).

       •  Supplementary group IDs.  This is a set of additional group IDs that
          are used for permission checks when accessing files and other shared
          resources.  Before Linux 2.6.4, a process can be a member of  up  to
          32  supplementary groups; since Linux 2.6.4, a process can be a mem‐
          ber   of   up   to   65536   supplementary   groups.     The    call
          _s_y_s_c_o_n_f_(___S_C___N_G_R_O_U_P_S___M_A_X_) can be used to determine the number of sup‐
          plementary groups of which a process may be a member.  A process can
          obtain its set of supplementary group IDs using ggeettggrroouuppss(2).

       A child process created by ffoorrkk(2) inherits copies of its parent’s user
       and  groups  IDs.  During an eexxeeccvvee(2), a process’s real user and group
       ID and supplementary group IDs are preserved; the effective  and  saved
       set IDs may be changed, as described in eexxeeccvvee(2).

       Aside  from the purposes noted above, a process’s user IDs are also em‐
       ployed in a number of other contexts:

       •  when determining the permissions for sending signals (see kkiillll(2));

       •  when determining the permissions for setting process‐scheduling  pa‐
          rameters  (nice value, real time scheduling policy and priority, CPU
          affinity, I/O priority) using sseettpprriioorriittyy(2),  sscchheedd__sseettaaffffiinniittyy(2),
          sscchheedd__sseettsscchheedduulleerr(2),  sscchheedd__sseettppaarraamm(2), sscchheedd__sseettaattttrr(2), and iioo‐‐
          pprriioo__sseett(2);

       •  when checking resource limits (see ggeettrrlliimmiitt(2));

       •  when checking the limit on the number of inotify instances that  the
          process may create (see iinnoottiiffyy(7)).

   MMooddiiffyyiinngg pprroocceessss uusseerr aanndd ggrroouupp IIDDss
       Subject  to rules described in the relevant manual pages, a process can
       use the following APIs to modify its user and group IDs:

       sseettuuiidd(2) (sseettggiidd(2))
              Modify the process’s real (and possibly effective and saved‐set)
              user (group) IDs.

       sseetteeuuiidd(2) (sseetteeggiidd(2))
              Modify the process’s effective user (group) ID.

       sseettffssuuiidd(2) (sseettffssggiidd(2))
              Modify the process’s filesystem user (group) ID.

       sseettrreeuuiidd(2) (sseettrreeggiidd(2))
              Modify the process’s real and effective (and possibly saved‐set)
              user (group) IDs.

       sseettrreessuuiidd(2) (sseettrreessggiidd(2))
              Modify the process’s real, effective, and saved‐set user (group)
              IDs.

       sseettggrroouuppss(2)
              Modify the process’s supplementary group list.

       Any changes to a process’s effective user (group) ID are  automatically
       carried over to the process’s filesystem user (group) ID.  Changes to a
       process’s  effective  user  or  group  ID  can  also affect the process
       "dumpable" attribute, as described in pprrccttll(2).

       Changes to process user and group IDs can affect  the  capabilities  of
       the process, as described in ccaappaabbiilliittiieess(7).

SSTTAANNDDAARRDDSS
       Process IDs, parent process IDs, process group IDs, and session IDs are
       specified  in  POSIX.1.   The  real,  effective, and saved set user and
       groups IDs, and the supplementary group IDs, are specified in POSIX.1.

       The filesystem user and group IDs are a Linux extension.

NNOOTTEESS
       Various fields in the _/_p_r_o_c_/pid_/_s_t_a_t_u_s file show  the  process  creden‐
       tials described above.  See pprroocc(5) for further information.

       The POSIX threads specification requires that credentials are shared by
       all  of  the threads in a process.  However, at the kernel level, Linux
       maintains separate user and group credentials  for  each  thread.   The
       NPTL  threading implementation does some work to ensure that any change
       to user or group credentials (e.g., calls to  sseettuuiidd(2),  sseettrreessuuiidd(2))
       is  carried  through  to  all  of  the POSIX threads in a process.  See
       nnppttll(7) for further details.

SSEEEE AALLSSOO
       bbaasshh(1), ccsshh(1), ggrroouuppss(1), iidd(1), nneewwggrrpp(1), ppss(1),  rruunnuusseerr(1),  sseett‐‐
       pprriivv(1),  ssgg(1),  ssuu(1),  aacccceessss(2),  eexxeeccvvee(2), ffaacccceessssaatt(2), ffoorrkk(2),
       ggeettggrroouuppss(2), ggeettppggrrpp(2), ggeettppiidd(2),  ggeettppppiidd(2),  ggeettssiidd(2),  kkiillll(2),
       sseetteeggiidd(2),   sseetteeuuiidd(2),  sseettffssggiidd(2),  sseettffssuuiidd(2),  sseettggiidd(2),  sseett‐‐
       ggrroouuppss(2),  sseettppggiidd(2),  sseettrreessggiidd(2),  sseettrreessuuiidd(2),  sseettssiidd(2),   ssee‐‐
       ttuuiidd(2),  wwaaiittppiidd(2),  eeuuiiddaacccceessss(3), iinniittggrroouuppss(3), kkiillllppgg(3), ttccggeettpp‐‐
       ggrrpp(3), ttccggeettssiidd(3), ttccsseettppggrrpp(3), ggrroouupp(5), ppaasssswwdd(5), sshhaaddooww(5),  ccaa‐‐
       ppaabbiilliittiieess(7),  nnaammeessppaacceess(7),  ppaatthh__rreessoolluuttiioonn(7),  ppiidd__nnaammeessppaacceess(7),
       pptthhrreeaaddss(7),  ssiiggnnaall(7),  ssyysstteemm__ddaattaa__ttyyppeess(7),   uunniixx(7),   uusseerr__nnaammee‐‐
       ssppaacceess(7), ssuuddoo(8)

Linux man‐pages (unreleased)      2025‐09‐21                    _c_r_e_d_e_n_t_i_a_l_s(7)
