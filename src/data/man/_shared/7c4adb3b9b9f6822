ZMORE(1)                  BSD General Commands Manual                 ZMORE(1)

NNAAMMEE
     zzmmoorree, zzlleessss — view compressed files

SSYYNNOOPPSSIISS
     zzmmoorree [_f_l_a_g_s] [_f_i_l_e _._._.]
     zzlleessss [_f_l_a_g_s] [_f_i_l_e _._._.]

DDEESSCCRRIIPPTTIIOONN
     zzmmoorree is a filter that allows the viewing of files compressed with Lem‐
     pel-Ziv encoding.  Such files generally have a “Z” or “gz” extension
     (both the compress(1) and gzip(1) formats are supported).  Any _f_l_a_g_s that
     are specified are passed to more(1) or less(1), respectively.

     zzlleessss is equivalent to zzmmoorree but uses less(1) as a pager instead of
     more(1).

     When multiple files are specified, zzmmoorree will pause at the end of each
     file and present the following prompt to the user:

           prev_file (END) - Next: next_file

     Where pprreevv__ffiillee is the file that was just displayed and nneexxtt__ffiillee is the
     next file to be displayed.  The following keys are recognized at the
     prompt:

           ee or qq  quit zzmmoorree.

           ss       skip the next file (or exit if the next file is the last).

     If no files are specified, zzmmoorree will read from the standard input.  In
     this mode zzmmoorree will assume gzip(1) style compression since there is no
     suffix on which to make a decision.

SSEEEE AALLSSOO
     compress(1), gzip(1), less(1), more(1)

BSD                            January 25, 2019                            BSD
