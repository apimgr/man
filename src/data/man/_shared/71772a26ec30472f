verifies  the integrity of specified executables and files before
they are run or read.  This makes it much more difficult  to  in‐
sert a trojan horse into the system and also makes it more diffi‐
cult to run binaries that are not supposed to be running, for ex‐
ample,  packet  sniffers, DDoS clients and so on.  The pseudo‐de‐
vice is used to load and delete entries to and from the in‐kernel
databases, as well as query information about them.  It can  also
be  used to dump the entire database.  uses for communication be‐
tween the kernel and userland.  Load an entry for a  file  to  be
monitored  by  The  dictionary passed contains the following ele‐
ments: can be one or more (binary‐OR’d) of the following: Removes
either an entry for a single file or entries for an entire  mount
from  The dictionary passed contains the following elements: Dump
the monitored files database from the  kernel.   Only  files  for
which  the  filename was kept will be dumped.  The returned array
contains dictionaries with  the  following  elements:  Flush  the
database,  removing all entries.  This command has no parameters.
Queries about a file, returning information that  may  be  useful
about it.  The dictionary passed contains the following elements:
The  dictionary  returned contains the following elements: can be
one of the following: Note that the requests and are not  permit‐
ted once the strict level has been raised past 0.  is part of the
default  configuration  on  the  following  architectures: amd64,
i386, macppc, prep, sparc64.
