(standard  PC keyboard) (USB keyboard) (DEC LK200/400 serial key‐
board) (Apple ADB keyboard) (NeXT keyboard)  (NEC  VR4000  series
HPC  keyboard) (keyboard of misc hpcmips handheld devices) (Blue‐
tooth keyboard) The driver handles  common  tasks  for  keyboards
within  the  framework.   It is attached to the hardware specific
keyboard drivers and provides their connection to devices  and  a
character device interface.  The common keyboard support consists
of:  Mapping from keycodes (defined by the specific keyboard dri‐
ver) to keysyms (hardware independent, defined in Handling of se‐
quences.  Characters commonly not present as separate key on key‐
boards can be generated after either a special key is pressed  or
a character is used.  Certain translations, like turning an modi‐
fier into an prefix.  Automatic key repetition Parameter handling
for  Generation  of  for use by X servers.  The driver provides a
number of ioctl functions to control key maps and  other  parame‐
ters.   These  functions are accessible though the associated de‐
vice as well.  A complete list is in The utility allows to access
key maps and other variables.  The locator in  the  configuration
line  refers  to  the device’s use as input part of the operating
system console.  A device  specification  containing  a  positive
value here will only match if the device is in use as system con‐
sole.   (The  console device selection in early system startup is
not influenced.)  This way, the console device can  be  connected
to  a  known  wskbd device instance.  The following encodings are
supported.  Device drivers for  legacy  keyboard  interfaces  may
only  support  a subset of these.  However, generally, all encod‐
ings are supported by and keyboards with  PC  keyboard  interface
(e.g.,  layout  layout The suffix flag) can be applied to layouts
with to switch them off.  The and mappings  can  be  modified  to
swap the left and the keys by the variant bit or the suffix.  The
suffix  flag)  option can be applied to any layout.  If set, keys
pressed together with the modifier are prefixed by an  character.
(Standard  behaviour  is to add 128 to the value.)  The following
calls are provided by the driver or  by  devices  which  use  it.
Their  definitions  are  found in Get the keyboard type.  Get the
keyboard mode, 0 means translated through keyboard map,  1  means
raw.  Set the keyboard mode.  Get and set keyboard bell settings.
Get  and  set keyboard autorepeat settings.  Get and set keyboard
LED settings.  Get and set keyboard keymapping settings.  Get and
set keyboard encoding settings.  Get and  set  keyboard  keyclick
settings.  Set the protocol version.  The default is 0 for binary
compatibility.   The latest version is always available as and is
currently 1.  All new code should use a call similar to the below
to ensure the correct version  is  returned.   int  ver  =  WSKB‐
DIO_EVENT_VERSION; if (ioctl(fd, WSKBDIO_SETVERSION, &ver) == ‐1)
        err(EXIT_FAILURE,  "cannot  set  version");  The  list of
builtin mappings doesn’t follow any logic.   It  grew  as  people
submitted what they needed.
