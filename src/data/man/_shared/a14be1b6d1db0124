The  file specifies which of the standard services are performed.
The script is run, by default, every night from on a  system,  if
configured do to so from The variables described below can be set
to  "NO"  to disable the test: This checks whether the system has
enough entropy This checks the file  for  inconsistencies.   This
checks  the file for inconsistencies.  This checks the root users
startup files for sane settings of $PATH and umask.  This test is
not fail safe and any  warning  generated  from  this  should  be
checked  for correctness.  This checks that the correct users are
in the file.  This checks for security problems in the file.  For
backward compatibility, will be checked as well if exists.   This
checks  for  system and user rhosts files with "+" in them.  This
checks that home directories are owned by the correct  user,  and
have  appropriate permissions.  This checks that the correct user
owns mail in and that the mail box  has  the  right  permissions.
This  checks  that  the  file  does not export filesystems to the
world.  This checks for changes  to  devices  and  setuid  files.
This  runs to ensure that the system is installed correctly.  The
following configuration  files  are  checked:  Default  files  to
check.   Local  site  additions and overrides.  Specification for
the directory Backup text copies of the disklabels  of  available
disk  drives  into  and  display any differences in those and the
previous copies as per below.  If is  available  on  the  current
platform,  the  output of for each available disk drive is stored
in and any differences displayed as  per  the  disklabels.   This
stores  a  list  of all installed pkgs into and checks it for any
changes.  This determines a list of files from  the  contents  of
and  the  output of for and For each file in the list it compares
the files with their backups in and and displays any  differences
found.   The  following  modify how files are determined from and
The entry is ignored; no backups are made and the differences are
not displayed.  This includes dynamic or binary files such as The
entry is backed up but the differences are not displayed  because
the contents of the file are sensitive.  This includes files such
as  Checks the currently installed packages against a database of
known vulnerabilities and  reports  those  that  are  vulnerable.
Check the setting in to keep the database up to date.  Checks the
digital  signature of all files installed by packages against the
expected values stored in the packages database.   The  variables
described below can be set to modify the tests: During the phase,
allow the checked files to be group‐writable if the group name is
the  same  as the username.  During the phase, allow the home di‐
rectory and files of the listed users to be owned by a  different
user.   Lists  filesystem types to ignore during the phase.  Pre‐
fixing the type with a inverts the match.  For example, will  ig‐
nore  type  filesystems  and filesystems that are not Lists path‐
names to ignore during the phase.  Prefixing the path with a  in‐
verts the match.  For example, will ignore paths under while will
ignore  paths that are not under During the phase, instruct mtree
to follow symbolic links.  Please note, this may cause the  phase
to  report  errors  for entries for these symbolic links (i.e. of
type=link in the mtree specification) as they will always  appear
to  be plain files for the purposes of the check.  may be used to
override the checks for the affected links.  If is enabled,  most
warnings  will  be suppressed for entries whose shells are listed
in this space‐separated list.  This is of particular  value  when
those  shells  are  not  in  If is enabled, suppress warnings for
these users.  If is enabled, do not warn about duplicate uids for
the listed login names.  If is enabled, do not warn  about  login
names  which  use non‐alphanumeric characters.  If is enabled, do
not warn about password fields set to Note that the use of  pass‐
word  fields such as is encouraged, instead.  If is enabled, this
determines the maximum permitted length of group  names.   If  is
enabled,  this  determines  the maximum permitted length of login
names.  Change the backup  directory  from  Specify  the  options
passed  to  when  it  is  invoked  to show changes made to system
files.  Defaults to for unified‐format context‐diffs.  Please set
in instead.  If defined, points to the location of  the  packages
database.  Defaults to Use for maintaining backup copies of files
noted  in and instead of just keeping a current copy and a backup
copy.  Name of the entropy seed file used at boot.  Default is as
used by Set to empty to disable saving a seed  every  time  runs.
defaults for /etc/security.conf daily security check script daily
security check configuration local site additions to The file ap‐
peared  in  The  functionality was added in The and features were
added in appeared in prior to that,  traditional‐format  (context
free) diffs were generated.
