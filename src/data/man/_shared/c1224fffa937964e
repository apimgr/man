The  command  displays or sets various wscons system driver vari‐
ables.  If a list of variables is present on  the  command  line,
then  prints  the current value of those variables for the speci‐
fied device.  Specify all variables for the device.   Select  the
display  portion  of  the device.  Specify an alternative control
device.  Select the keyboard portion of the device (this  is  the
default).   Select the mouse portion of the device.  Suppress the
printing of the variable name in the output ‐ only the value will
appear.  Set or modify the specified variables to the given  val‐
ues.   The value can be specified as either an absolute value, by
using the symbol or as a relative value,  by  using  the  symbol.
See  the  section  for  more details.  The utility can be used to
view and modify aspects of the keyboard, display, and mouse,  us‐
ing  the standard, machine‐independent workstation console device
driver The keyboard type can be  modified,  the  keyboard  bell’s
pitch,  period,  and  duration  can be modified, the value can be
changed, and the keyboard encoding  can  be  modified  to  switch
keys,  should the user find a keyboard’s default layout difficult
to use.  The keyboard types and other  relevant  definitions  can
all  be  found  in  the file.  The mouse types are defined in the
file.  The display types, height, width, depth (bits per  pixel),
color map size, and color map are defined in the file.  There are
also  definitions  relating  to video control and cursor control,
which are not applicable to all display types, and to text emula‐
tion and graphics (mapped) modes.  keyboard control device  mouse
control  device  display  control device The following are just a
few examples of and its functionality.  Set a UK keyboard  encod‐
ing.  For a full list of supported keyboard encodings, see Modify
the  current  keyboard  encoding so that when the key is pressed,
the same encoding sequence as  is  sent.   For  a  full  list  of
keysyms  and  keycodes,  please refer to the file.  Set a US key‐
board encoding, with the and keys swapped.  The encoding does not
work for all national keyboard encodings.  For most purposes, the
ability to set the value returned by the key is enough ‐ see  the
previous  example  for  details.   Set the bell pitch to be 1200.
Add 200 to the current pitch of the bell.  Set the color of  ker‐
nel  messages to brown on red with the highlighting flag set (be‐
coming yellow on red).  Set the initial delay for  keyboard  auto
repeat  to  200ms,  and subsequent delays to 50ms.  Turn off auto
repeat.  If scroll support is enabled in the kernel, set the num‐
ber of lines used in the fast scroll function to 50.   If  scroll
support is enabled in the kernel, set the number of lines used in
the  slow  scroll  function to 2.  In order to use this function,
you have to have and defined in your keyboard map.   The  command
first appeared in
