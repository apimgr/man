BTXLD(8)                  BSD System Manager's Manual                 BTXLD(8)

NNAAMMEE
     bbttxxlldd — link editor for BTX clients

SSYYNNOOPPSSIISS
     bbttxxlldd [--qqvv] [--bb _f_i_l_e] [--EE _a_d_d_r_e_s_s] [--ee _a_d_d_r_e_s_s] [--ff _f_o_r_m_a_t] [--ll _f_i_l_e]
           [--oo _f_i_l_e_n_a_m_e] [--PP _p_a_g_e] [--WW _p_a_g_e] _f_i_l_e

DDEESSCCRRIIPPTTIIOONN
     The bbttxxlldd utility binds the specified client executable together with a
     BTX loader program and the BTX kernel, and creates a composite object
     file suitable for loading during the boot process.

     The options are:

     --qq      Quiet: inhibit warnings.

     --vv      Verbose: display information about the files processed.

     --bb _f_i_l_e
             Specify the BTX kernel to be bound with the client.

     --EE _a_d_d_r_e_s_s
             Set the client entry point.

     --ee _a_d_d_r_e_s_s
             Set the BTX loader entry point.

     --ff _f_o_r_m_a_t
             Specify the output format, where _f_o_r_m_a_t is one of ‘bin’, ‘aout’,
             or ‘elf’.

     --ll _f_i_l_e
             Specify the BTX loader to be bound with the client.

     --oo _f_i_l_e_n_a_m_e
             Name the output file.  The default is “a.out”.

     --PP _p_a_g_e
             Specify the first page of the client's segment to be marked
             ‘present’, where _p_a_g_e may be 0 or 1.

     --WW _p_a_g_e
             Specify the first page of the client's segment to be marked
             ‘writable’, where _p_a_g_e may be 0, and should not exceed the number
             of pages occupied by the combined .text and .data segments of the
             client image.

EEXXIITT SSTTAATTUUSS
     The bbttxxlldd utility exits 0 on success, and >0 if an error occurs.

SSEEEE AALLSSOO
     ld(1), boot(8)

AAUUTTHHOORRSS
     Robert Nordier <_r_n_o_r_d_i_e_r_@_F_r_e_e_B_S_D_._o_r_g>

BSD                           September 12, 1998                           BSD
