USBHID(4)                BSD Kernel Interfaces Manual                USBHID(4)

NNAAMMEE
     uussbbhhiidd — USB HID transport driver

SSYYNNOOPPSSIISS
     To compile this driver into the kernel, place the following lines in your
     kernel configuration file:

           ddeevviiccee uussbbhhiidd

     Alternatively, to load the driver as a module at boot time, place the
     following line in loader.conf(5):

           usbhid_load="YES"

DDEESSCCRRIIPPTTIIOONN
     The uussbbhhiidd driver provides a interface to USB Human Interface Devices
     (HIDs).

SSYYSSCCTTLL VVAARRIIAABBLLEESS
     The following variables are available as both sysctl(8) variables and
     loader(8) tunables:

     _h_w_._u_s_b_._u_s_b_h_i_d_._e_n_a_b_l_e
             Enable uussbbhhiidd and make its priority greater than other USB HID
             drivers have.  Default is 0.

     _h_w_._u_s_b_._u_s_b_h_i_d_._d_e_b_u_g
             Debug output level, where 0 is debugging disabled and larger val‐
             ues increase debug message verbosity.  Default is 0.

SSEEEE AALLSSOO
     ehci(4), ohci(4), uhci(4), usb(4), xhci(4), usbconfig(8)

HHIISSTTOORRYY
     The uussbbhhiidd driver first appeared in FreeBSD 13.0.

AAUUTTHHOORRSS
     The uussbbhhiidd driver was written by Vladimir Kondratyev <_w_u_l_f_@_F_r_e_e_B_S_D_._o_r_g>.

BSD                            January 12, 2021                            BSD
