NETGRAPH(3)              BSD Library Functions Manual              NETGRAPH(3)

NNAAMMEE
     NNggMMkkSSoocckkNNooddee, NNggNNaammeeNNooddee, NNggSSeennddMMssgg, NNggSSeennddAAsscciiiiMMssgg, NNggSSeennddRReeppllyyMMssgg,
     NNggRReeccvvMMssgg, NNggAAllllooccRReeccvvMMssgg, NNggRReeccvvAAsscciiiiMMssgg, NNggAAllllooccRReeccvvAAsscciiiiMMssgg,
     NNggSSeennddDDaattaa, NNggRReeccvvDDaattaa, NNggAAllllooccRReeccvvDDaattaa, NNggSSeettDDeebbuugg, NNggSSeettEErrrrLLoogg — net‐
     graph user library

LLIIBBRRAARRYY
     Netgraph User Library (libnetgraph, -lnetgraph)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<nneettggrraapphh..hh>>

     _i_n_t
     NNggMMkkSSoocckkNNooddee(_c_o_n_s_t _c_h_a_r _*_n_a_m_e, _i_n_t _*_c_s_p, _i_n_t _*_d_s_p);

     _i_n_t
     NNggNNaammeeNNooddee(_i_n_t _c_s, _c_o_n_s_t _c_h_a_r _*_p_a_t_h, _c_o_n_s_t _c_h_a_r _*_f_m_t, _._._.);

     _i_n_t
     NNggSSeennddMMssgg(_i_n_t _c_s, _c_o_n_s_t _c_h_a_r _*_p_a_t_h, _i_n_t _c_o_o_k_i_e, _i_n_t _c_m_d, _c_o_n_s_t _v_o_i_d _*_a_r_g,
         _s_i_z_e___t _a_r_g_l_e_n);

     _i_n_t
     NNggSSeennddAAsscciiiiMMssgg(_i_n_t _c_s, _c_o_n_s_t _c_h_a_r _*_p_a_t_h, _c_o_n_s_t _c_h_a_r _*_f_m_t, _._._.);

     _i_n_t
     NNggSSeennddRReeppllyyMMssgg(_i_n_t _c_s, _c_o_n_s_t _c_h_a_r _*_p_a_t_h, _s_t_r_u_c_t _n_g___m_e_s_g _*_m_s_g,
         _c_o_n_s_t _v_o_i_d _*_a_r_g, _s_i_z_e___t _a_r_g_l_e_n);

     _i_n_t
     NNggRReeccvvMMssgg(_i_n_t _c_s, _s_t_r_u_c_t _n_g___m_e_s_g _*_r_e_p, _s_i_z_e___t _r_e_p_l_e_n, _c_h_a_r _*_p_a_t_h);

     _i_n_t
     NNggAAllllooccRReeccvvMMssgg(_i_n_t _c_s, _s_t_r_u_c_t _n_g___m_e_s_g _*_*_r_e_p, _c_h_a_r _*_p_a_t_h);

     _i_n_t
     NNggRReeccvvAAsscciiiiMMssgg(_i_n_t _c_s, _s_t_r_u_c_t _n_g___m_e_s_g _*_r_e_p, _s_i_z_e___t _r_e_p_l_e_n, _c_h_a_r _*_p_a_t_h);

     _i_n_t
     NNggAAllllooccRReeccvvAAsscciiiiMMssgg(_i_n_t _c_s, _s_t_r_u_c_t _n_g___m_e_s_g _*_*_r_e_p, _c_h_a_r _*_p_a_t_h);

     _i_n_t
     NNggSSeennddDDaattaa(_i_n_t _d_s, _c_o_n_s_t _c_h_a_r _*_h_o_o_k, _c_o_n_s_t _u___c_h_a_r _*_b_u_f, _s_i_z_e___t _l_e_n);

     _i_n_t
     NNggRReeccvvDDaattaa(_i_n_t _d_s, _u___c_h_a_r _*_b_u_f, _s_i_z_e___t _l_e_n, _c_h_a_r _*_h_o_o_k);

     _i_n_t
     NNggAAllllooccRReeccvvDDaattaa(_i_n_t _d_s, _u___c_h_a_r _*_*_b_u_f, _c_h_a_r _*_h_o_o_k);

     _i_n_t
     NNggSSeettDDeebbuugg(_i_n_t _l_e_v_e_l);

     _v_o_i_d
     NNggSSeettEErrrrLLoogg(_v_o_i_d (_*_l_o_g)(_c_o_n_s_t _c_h_a_r _*_f_m_t_, _._._.),
         _v_o_i_d (_*_l_o_g_x)(_c_o_n_s_t _c_h_a_r _*_f_m_t_, _._._.));

DDEESSCCRRIIPPTTIIOONN
     These functions facilitate user-mode program participation in the kernel
     netgraph(4) graph-based networking system, by utilizing the netgraph
     _s_o_c_k_e_t node type (see ng_socket(4)).

     The NNggMMkkSSoocckkNNooddee() function should be called first, to create a new
     _s_o_c_k_e_t type netgraph node with associated control and data sockets.  If
     _n_a_m_e is non-NULL, the node will have that global name assigned to it.
     The _c_s_p and _d_s_p arguments will be set to the newly opened control and
     data sockets associated with the node; either _c_s_p or _d_s_p may be NULL if
     only one socket is desired.  The NNggMMkkSSoocckkNNooddee() function loads the _s_o_c_k_e_t
     node type KLD if it is not already loaded.

     The NNggNNaammeeNNooddee() function assigns a global name to the node addressed by
     _p_a_t_h.

     The NNggSSeennddMMssgg() function sends a binary control message from the _s_o_c_k_e_t
     node associated with control socket _c_s to the node addressed by _p_a_t_h.
     The _c_o_o_k_i_e indicates how to interpret _c_m_d, which indicates a specific
     command.  Extra argument data (if any) is specified by _a_r_g and _a_r_g_l_e_n.
     The _c_o_o_k_i_e, _c_m_d, and argument data are defined by the header file corre‐
     sponding to the type of the node being addressed.  The unique, non-nega‐
     tive token value chosen for use in the message header is returned.  This
     value is typically used to associate replies.

     Use NNggSSeennddRReeppllyyMMssgg() to send reply to a previously received control mes‐
     sage.  The original message header should be pointed to by _m_s_g.

     The NNggSSeennddAAsscciiiiMMssgg() function performs the same function as NNggSSeennddMMssgg(),
     but adds support for ASCII encoding of control messages.  The
     NNggSSeennddAAsscciiiiMMssgg() function formats its input a la printf(3) and then sends
     the resulting ASCII string to the node in a NGM_ASCII2BINARY control mes‐
     sage.  The node returns a binary version of the message, which is then
     sent back to the node just as with NNggSSeennddMMssgg().  As with NNggSSeennddMMssgg(), the
     message token value is returned.  Note that ASCII conversion may not be
     supported by all node types.

     The NNggRReeccvvMMssgg() function reads the next control message received by the
     node associated with control socket _c_s.  The message and any extra argu‐
     ment data must fit in _r_e_p_l_e_n bytes.  If _p_a_t_h is non-NULL, it must point
     to a buffer of at least NG_PATHSIZ bytes, which will be filled in (and
     NUL terminated) with the path to the node from which the message was re‐
     ceived.

     The length of the control message is returned.  A return value of zero
     indicates that the socket was closed.

     The NNggAAllllooccRReeccvvMMssgg() function works exactly like NNggRReeccvvMMssgg(), except that
     the buffer for a message is dynamically allocated to guarantee that a
     message is not truncated.  The size of the buffer is equal to the
     socket's receive buffer size.  The caller is responsible for freeing the
     buffer when it is no longer required.

     The NNggRReeccvvAAsscciiiiMMssgg() function works exactly like NNggRReeccvvMMssgg(), except that
     after the message is received, any binary arguments are converted to
     ASCII by sending a NGM_BINARY2ASCII request back to the originating node.
     The result is the same as NNggRReeccvvMMssgg(), with the exception that the reply
     arguments field will contain a NUL-terminated ASCII version of the argu‐
     ments (and the reply header argument length field will be adjusted).

     The NNggAAllllooccRReeccvvAAsscciiiiMMssgg() function works exactly like NNggRReeccvvAAsscciiiiMMssgg(),
     except that the buffer for a message is dynamically allocated to guaran‐
     tee that a message is not truncated.  The size of the buffer is equal to
     the socket's receive buffer size.  The caller is responsible for freeing
     the buffer when it is no longer required.

     The NNggSSeennddDDaattaa() function writes a data packet out on the specified hook
     of the node corresponding to data socket _d_s.  The node must already be
     connected to some other node via that hook.

     The NNggRReeccvvDDaattaa() function reads the next data packet (of up to _l_e_n bytes)
     received by the node corresponding to data socket _d_s and stores it in
     _b_u_f, which must be large enough to hold the entire packet.  If _h_o_o_k is
     non-NULL, it must point to a buffer of at least NG_HOOKSIZ bytes, which
     will be filled in (and NUL terminated) with the name of the hook on which
     the data was received.

     The length of the packet is returned.  A return value of zero indicates
     that the socket was closed.

     The NNggAAllllooccRReeccvvDDaattaa() function works exactly like NNggRReeccvvDDaattaa(), except
     that the buffer for a data packet is dynamically allocated to guarantee
     that a data packet is not truncated.  The size of the buffer is equal to
     the socket's receive buffer size.  The caller is responsible for freeing
     the buffer when it is no longer required.

     The NNggSSeettDDeebbuugg() and NNggSSeettEErrrrLLoogg() functions are used for debugging.  The
     NNggSSeettDDeebbuugg() function sets the debug level (if non-negative), and returns
     the old setting.  Higher debug levels result in more verbosity.  The de‐
     fault is zero.  All debug and error messages are logged via the functions
     specified in the most recent call to NNggSSeettEErrrrLLoogg().  The default logging
     functions are vwarn(3) and vwarnx(3).

     At debug level 3, the library attempts to display control message argu‐
     ments in ASCII format; however, this results in additional messages being
     sent which may interfere with debugging.  At even higher levels, even
     these additional messages will be displayed, etc.

     Note that select(2) can be used on the data and the control sockets to
     detect the presence of incoming data and control messages, respectively.
     Data and control packets are always written and read atomically, i.e., in
     one whole piece.

     User mode programs must be linked with the --llnetgraph flag to link in
     this library.

IINNIITTIIAALLIIZZAATTIIOONN
     To enable netgraph in your kernel, either your kernel must be compiled
     with ooppttiioonnss NNEETTGGRRAAPPHH in the kernel configuration file, or else the
     netgraph(4) and ng_socket(4) KLD modules must have been loaded via
     kldload(8).

RREETTUURRNN VVAALLUUEESS
     The NNggSSeettDDeebbuugg() function returns the previous debug setting.

     The NNggSSeettEErrrrLLoogg() function has no return value.

     All other functions return -1 if there was an error and set _e_r_r_n_o accord‐
     ingly.

     A return value of zero from NNggRReeccvvMMssgg() or NNggRReeccvvDDaattaa() indicates that
     the netgraph socket has been closed.

     For NNggSSeennddAAsscciiiiMMssgg() and NNggRReeccvvAAsscciiiiMMssgg(), the following additional er‐
     rors are possible:

     [ENOSYS]           The node type does not know how to encode or decode
                        the control message.

     [ERANGE]           The encoded or decoded arguments were too long for the
                        supplied buffer.

     [ENOENT]           An unknown structure field was seen in an ASCII con‐
                        trol message.

     [EALREADY]         The same structure field was specified twice in an
                        ASCII control message.

     [EINVAL]           ASCII control message parse error or illegal value.

     [E2BIG]            ASCII control message array or fixed width string buf‐
                        fer overflow.

SSEEEE AALLSSOO
     select(2), socket(2), warnx(3), kld(4), netgraph(4), ng_socket(4)

HHIISSTTOORRYY
     The nneettggrraapphh system was designed and first implemented at Whistle Commu‐
     nications, Inc. in a version of FreeBSD 2.2 customized for the Whistle
     InterJet.

AAUUTTHHOORRSS
     Archie Cobbs <_a_r_c_h_i_e_@_F_r_e_e_B_S_D_._o_r_g>

BSD                              May 15, 2020                              BSD
