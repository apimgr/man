executes a collection of test cases, test programs, or a complete
test  suite.  The results of each test are collected by the tool,
and are then multiplexed into a single machine‐parseable  report;
see  for more details.  This report can later be transformed into
many different and saner formats using the  tool.   The  list  of
test  programs  to execute is read from an present in the current
directory.  This file describes the test suite stored in the  di‐
rectory  it  lives in, which aside from the list of test programs
also includes meta‐data and configuration variables.  is also  in
charge  of reading the configuration files that tune the behavior
of each test program and passing down the necessary variables  to
them.  More details on how this is done are given in the section.
In  the  first synopsis form, parses the in the current directory
and runs all the test programs specified in it.  If any arguments
are given as part of the command line, those tests  are  executed
instead  of  the  complete list.  Each argument can be either the
name of a test program, or a string of the form to execute a sin‐
gle test case.  In the second synopsis form, will print  informa‐
tion  about all supported options and their purpose.  The follow‐
ing options are available: Shows a short summary of all available
options and their purpose.  Sets the  configuration  variable  to
the  given  value  reads configuration data from multiple places.
After all of these places have been analyzed, a list of variable‐
value pairs are passed to the test programs to be run.  The  fol‐
lowing  locations  are  scanned for configuration data, in order.
Items down the list override values defined above them:  Configu‐
ration  variables  defined in the Configuration variables defined
in the system‐wide  configuration  file  shared  among  all  test
suites.   This  lives  in  Configuration variables defined in the
system‐wide test‐suite‐specific configuration file.   This  lives
in  Configuration variables defined in the user‐specific configu‐
ration file shared among all test suites.  This lives in Configu‐
ration variables defined in the user‐specific test‐suite‐specific
configuration file.  This lives in Configuration  variables  pro‐
vided  as part of the command line through the option.  The value
of in the above list determined as detailed in The following con‐
figuration variables are globally recognized:  The  name  of  the
system  user that atf‐run will drop root privileges into for test
cases defining Note that this is this feature  is  only  for  the
convenience  of the user.  internal behavior can be customized by
the system administrator and the user by means of  hooks.   These
hooks are written in the shell script language for simplicity and
are  stored  in  the following files, which are read in the order
provided  below:  ${ATF_CONFDIR}/atf‐run.hooks  ${HOME}/.atf/atf‐
run.hooks  The  following hooks are supported: Called before exe‐
cutes any test program.  The purpose of this hook is to write ad‐
ditional stanzas to the top of the output report; these  are  de‐
fined by the described in Always use the function to print these.
This takes no parameters.  Similar to but executed after all test
programs have been run so that additional stanzas can be added to
the  bottom of the output report.  This takes no parameters.  All
hooks are accompanied by a function named that can be executed by
them to invoke the default behavior built into  For  example,  in
order  to  extend  the default hook, we could write the following
function: info_start_hook() {
    default_info_start_hook "${@}"

    atf_tps_writer_info "uptime" "$(uptime)" }
