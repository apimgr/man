time‐of‐day  clock  driver  The driver provides access to several
models of time‐of‐day clock chips.  Access  methods  to  retrieve
and  set date and time are provided through the interface defined
in To tie an instance of this device to the system, use the func‐
tion and the mk48txx_softc structure defined as follows:  typedef
uint8_t (*mk48txx_nvrd_t)(struct mk48txx_softc *, int off); type‐
def void (*mk48txx_nvwr_t)(struct mk48txx_softc *, int off,
    uint8_t  datum); struct mk48txx_softc {         struct device
sc_dev;          bus_space_tag_t  sc_bst;          bus_space_han‐
dle_t    sc_bsh;           struct   todr_chip_handle   sc_handle;
        const                                char      *sc_model;
        bus_size_t      sc_nvramsz;
        bus_size_t      sc_clkoffset;
        u_int           sc_year0;
        u_int           sc_flag;         mk48txx_nvrd_t  sc_nvrd;
        mk48txx_nvwr_t  sc_nvwr;  };  Specify bus space access to
the chip’s non‐volatile memory TODR handle passed to the function
to register interface.  The chip model which this instance should
serve.  Must be one of or Size of non‐volatile RAM in  the  chip.
This  value  is  set  by Offset into the control registers of the
chip.  This value is set by The actual year  represented  by  the
clock’s  counter.  This is generally dependent on the system con‐
figuration in which the clock device is mounted.   For  instance,
on machines the convention is to have clock’s two‐digit year rep‐
resent  the  year 1968.  This flag is used to specify machine‐de‐
pendent features.  Specify alternate access methods  for  reading
resp.  writing  clock  device  registers.   The  default, when is
passed as an access method, is to access the chip  memory  as  if
they were direct‐mapped with using the specified bus space.  Oth‐
erwise,  the  driver will call the respective function to perform
the access, passing it the specified bus space and the offset  of
the  chip  memory location to be read from or written to, respec‐
tively.  Note that if  the  resulting  date  retrieved  with  the
todr_gettime() method is earlier that January 1, 1970, the driver
will  assume  that  the chip’s year counter actually represents a
year in the 21st century.  This behaviour can  be  overridden  by
setting  the flag in The following models are supported: The dri‐
ver first appeared in The driver was written by
