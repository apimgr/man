The  daemon  provides  monitored  and unmonitored file and record
locking services in an NFS environment.  To monitor the status of
hosts requesting locks, the locking daemon typically operates  in
conjunction  with  Options  and operands available for Listen for
requests using IPv4.  Do not listen using IPv6 unless the  option
is  also  specified.  By default, listens for requests using both
IPv4 and IPv6 if available.  Listen for requests using IPv6.   It
is  an  error if IPv6 is not available.  Cause debugging informa‐
tion to be written to syslog, recording all RPC  transactions  to
the  daemon.   These  messages are logged with level and facility
Specifying a of 1 results in the generation of one log  line  per
protocol  operation.  Higher debug levels can be specified, caus‐
ing display of operation arguments and internal operations of the
daemon.  Set the grace period (in seconds) to  During  the  grace
period  only accepts requests from hosts which are reinitialising
locks which existed before the server restarted.  Default  is  30
seconds.   Error conditions are logged to syslog, irrespective of
the debug level, using log level and facility The daemon must NOT
be invoked by because the protocol assumes that the  daemon  will
run  from system start time.  Instead, it should be configured in
to run at system startup.  RPC  protocol  specification  for  the
network  lock  manager  protocol.  The implementation is based on
the specification in X/Open CAE  Specification  C218,  "Protocols
for  X/Open  PC Interworking: XNFS, Issue 4", ISBN 1 872630 66 9.
A version of appeared in The current implementation provides only
the server side of the protocol (i.e., clients running  other  OS
types can establish locks on a fileserver, but there is currently
no means for a client to establish locks).  The current implemen‐
tation serialises lock requests that could be shared.
