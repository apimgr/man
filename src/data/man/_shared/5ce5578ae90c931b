reads  each  file  in sequence and removes all and command lines,
backslash constructions, macro definitions,  constructs  (between
and  lines  or  between delimiters), pictures, and table descrip‐
tions and writes the remainder to the standard  output.   follows
chains  of included files and commands if a file has already been
included, a is ignored and a terminates execution.  If  no  input
file is given, reads from the standard input.  The options are as
follows:  Ignore and commands.  Keep blocks of text intact.  This
is the default behavior unless the option is given.  Enable  sup‐
port  for  common macro packages.  The option takes the following
arguments: recognize macros.  recognize macros.  remove list con‐
structs.  recognize macros.  recognize  macros.   Preserve  para‐
graph  macros.   This  option only has an effect if the option is
also specified.  Output a word list, one (string of letters, dig‐
its, and apostrophes, beginning with a  letter;  apostrophes  are
removed)  per  line, and all other characters ignored.  Normally,
the output follows the original,  with  the  deletions  mentioned
above.   appeared  in is not a complete interpreter, so it can be
confused by subtle constructs.  Most errors result  in  too  much
rather  than  too  little  output.  The option does not correctly
handle nested lists.
