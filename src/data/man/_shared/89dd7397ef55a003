grows or shrinks a mounted log‐structured filesystem to the spec‐
ified  size.  is the name of the filesystem to be resized, and is
the desired new filesystem size, in sectors.  If  is  not  speci‐
fied,  will default to the current size of the partition contain‐
ing the filesystem in question.  When growing, the partition must
be large enough to contain a filesystem of  the  specified  size;
when shrinking, must first the segments that will be invalid when
the  filesystem  is  shrunk.  If this cleaning process results in
these segments becoming redirtied, this indicates that the  given
new  size  is not large enough to contain the existing filesystem
data, and will return  an  error.   To  resize  the  file  system
mounted at to 32576 sectors: The command first appeared in should
be able to resize an unmounted filesystem as well.
