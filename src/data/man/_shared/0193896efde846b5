CPUCONTROL(8)             BSD System Manager's Manual            CPUCONTROL(8)

NNAAMMEE
     ccppuuccoonnttrrooll — control utility for the cpuctl(4) device

SSYYNNOOPPSSIISS
     ccppuuccoonnttrrooll [--vv] --mm _m_s_r _d_e_v_i_c_e
     ccppuuccoonnttrrooll [--vv] --mm _m_s_r=_v_a_l_u_e _d_e_v_i_c_e
     ccppuuccoonnttrrooll [--vv] --mm _m_s_r&=_m_a_s_k _d_e_v_i_c_e
     ccppuuccoonnttrrooll [--vv] --mm _m_s_r|=_m_a_s_k _d_e_v_i_c_e
     ccppuuccoonnttrrooll [--vv] --ii _l_e_v_e_l _d_e_v_i_c_e
     ccppuuccoonnttrrooll [--vv] --ii _l_e_v_e_l_,_l_e_v_e_l___t_y_p_e _d_e_v_i_c_e
     ccppuuccoonnttrrooll [--vvnn] [--dd _d_a_t_a_d_i_r] --uu _d_e_v_i_c_e
     ccppuuccoonnttrrooll --ee _d_e_v_i_c_e

DDEESSCCRRIIPPTTIIOONN
     The ccppuuccoonnttrrooll utility can be used to read and write arbitrary machine-
     specific CPU registers via the cpuctl(4) special device.  It can also be
     used to apply CPU firmware updates.

     The following options are available:

     --dd _d_a_t_a_d_i_r
             Directory paths where to look for microcode images.  The option
             can be specified multiple times.  The paths are added in order of
             the options appearance on the command line, default directories
             are appended after the user-supplied paths.

     --nn      Do not look for the microcode images in the standard directories.
             Currently standard directory to look for the microcode update
             files is _/_u_s_r_/_l_o_c_a_l_/_s_h_a_r_e_/_c_p_u_c_o_n_t_r_o_l.

     --mm _m_s_r  Show value of the specified MSR.  MSR register number should be
             given as a hexadecimal number.  The high word is printed first,
             then the low word is printed second.

     --mm _m_s_r=_v_a_l_u_e
             Store the _v_a_l_u_e in the specified MSR register.  The _v_a_l_u_e argu‐
             ment can be prefixed with ~ operator.  In this case the inverted
             value of argument will be stored in the register.

     --mm _m_s_r&=_m_a_s_k
             Store the result of bitwise AND operation between _m_a_s_k and the
             current MSR value in the MSR register.  The _m_a_s_k argument can be
             prefixed with ~ operator.  In this case the inverted value of
             mask will be used.

     --mm _m_s_r|=_m_a_s_k
             Store the result of bitwise OR operation between _m_a_s_k and the
             current MSR value in the MSR register.  The _m_a_s_k argument can be
             prefixed with ~ operator.  In this case the inverted value of
             mask will be used.

     --ii _l_e_v_e_l
             Retrieve CPUID info.  Level should be given as a hex number.

     --ii _l_e_v_e_l_,_l_e_v_e_l___t_y_p_e
             Retrieve CPUID info.  Level and level_type should be given as hex
             numbers.

     --uu      Apply CPU firmware updates.  The ccppuuccoonnttrrooll utility will walk
             through the configured data directories and apply all firmware
             updates available for this CPU.

     --ee      Re-evaluate the kernel flags indicating the present CPU features.
             This command is typically executed after a firmware update was
             applied which changes information reported by the CPUID instruc‐
             tion.

             OOnnllyy eexxeeccuuttee tthhee --ee ccoommmmaanndd aafftteerr tthhee mmiiccrrooccooddee uuppddaattee wwaass aapp‐‐
             pplliieedd ttoo aallll CCPPUUss iinn tthhee ssyysstteemm..  TThhee kkeerrnneell ddooeess nnoott ooppeerraattee
             ccoorrrreeccttllyy iiff tthhee ffeeaattuurreess ooff pprroocceessssoorrss aarree nnoott iiddeennttiiccaall..

     --vv      Increase the verbosity level.

     --hh      Show help message.

EEXXIITT SSTTAATTUUSS
     The ccppuuccoonnttrrooll utility exits 0 on success, and >0 if an error occurs.

EEXXAAMMPPLLEESS
     The command

     “cpucontrol -m 0x10 /dev/cpuctl0”

     will read the contents of TSC MSR from CPU 0.

     To set the CPU 0 TSC MSR register value to 0x1 issue

     “cpucontrol -m 0x10=0x1 /dev/cpuctl0”.

     The following command will clear the second bit of TSC register:

     “cpucontrol -m 0x10&=~0x02 /dev/cpuctl0”.

     The following command will set the forth and second bit of TSC register:

     “cpucontrol -m 0x10|=0x0a /dev/cpuctl0”.

     The command

     “cpucontrol -i 0x1 /dev/cpuctl1”

     will retrieve the CPUID level 0x1 from CPU 1.

     To perform firmware updates on CPU 0 from images located at
     _/_u_s_r_/_l_o_c_a_l_/_s_h_a_r_e_/_c_p_u_c_t_l use the following command:

     “cpucontrol -nd /usr/local/share/cpuctl -u /dev/cpuctl0”

SSEEEE AALLSSOO
     cpuctl(4)

HHIISSTTOORRYY
     The ccppuuccoonnttrrooll utility first appeared in FreeBSD 7.2.

AAUUTTHHOORRSS
     The ccppuuccoonnttrrooll utility and this manual page was written by Stanislav
     Sedov <_s_t_a_s_@_F_r_e_e_B_S_D_._o_r_g>.

BSD                             January 5, 2018                            BSD
