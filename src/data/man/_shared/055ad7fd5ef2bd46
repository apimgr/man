GETPID(2)                   BSD System Calls Manual                  GETPID(2)

NNAAMMEE
     ggeettppiidd, ggeettppppiidd — get parent or calling process identification

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<uunniissttdd..hh>>

     _p_i_d___t
     ggeettppiidd(_v_o_i_d);

     _p_i_d___t
     ggeettppppiidd(_v_o_i_d);

DDEESSCCRRIIPPTTIIOONN
     The ggeettppiidd() system call returns the process ID of the calling process.
     Though the ID is guaranteed to be unique, it should _N_O_T be used for con‐
     structing temporary file names, for security reasons; see mkstemp(3) in‐
     stead.

     The ggeettppppiidd() system call returns the process ID of the parent of the
     calling process.

EERRRROORRSS
     The ggeettppiidd() and ggeettppppiidd() system calls are always successful, and no re‐
     turn value is reserved to indicate an error.

SSEEEE AALLSSOO
     fork(2), getpgrp(2), kill(2), setpgid(2), setsid(2), exec(3)

SSTTAANNDDAARRDDSS
     The ggeettppiidd() and ggeettppppiidd() system calls are expected to conform to
     ISO/IEC 9945-1:1990 (“POSIX.1”).

HHIISSTTOORRYY
     The ggeettppiidd() function appeared in Version 7 AT&T UNIX.

BSD                            December 15, 2015                           BSD
