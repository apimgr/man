UNGETWC(3)               BSD Library Functions Manual               UNGETWC(3)

NNAAMMEE
     uunnggeettwwcc — un-get wide character from input stream

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssttddiioo..hh>>
     ##iinncclluuddee <<wwcchhaarr..hh>>

     _w_i_n_t___t
     uunnggeettwwcc(_w_i_n_t___t _w_c, _F_I_L_E _*_s_t_r_e_a_m);

DDEESSCCRRIIPPTTIIOONN
     The uunnggeettwwcc() function pushes the wide character _w_c (converted to an
     _w_c_h_a_r___t) back onto the input stream pointed to by _s_t_r_e_a_m.  The pushed-
     backed wide characters will be returned by subsequent reads on the stream
     (in reverse order).  A successful intervening call, using the same
     stream, to one of the file positioning functions fseek(3), fsetpos(3), or
     rewind(3) will discard the pushed back wide characters.

     One wide character of push-back is guaranteed, but as long as there is
     sufficient memory, an effectively infinite amount of pushback is allowed.

     If a character is successfully pushed-back, the end-of-file indicator for
     the stream is cleared.

RREETTUURRNN VVAALLUUEESS
     The uunnggeettwwcc() function returns the wide character pushed-back after the
     conversion, or WEOF if the operation fails.  If the value of the argument
     _c character equals WEOF, the operation will fail and the stream will re‐
     main unchanged.

SSEEEE AALLSSOO
     fseek(3), getwc(3)

SSTTAANNDDAARRDDSS
     The uunnggeettwwcc() function conforms to ISO/IEC 9899:1999 (“ISO C99”).

BSD                              March 3, 2004                             BSD
