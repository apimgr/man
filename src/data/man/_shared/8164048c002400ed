PTHREAD_ATFORK(3)        BSD Library Functions Manual        PTHREAD_ATFORK(3)

NNAAMMEE
     pptthhrreeaadd__aattffoorrkk — register fork handlers

LLIIBBRRAARRYY
     POSIX Threads Library (libpthread, -lpthread)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<pptthhrreeaadd..hh>>

     _i_n_t
     pptthhrreeaadd__aattffoorrkk(_v_o_i_d (_*_p_r_e_p_a_r_e)(_v_o_i_d), _v_o_i_d (_*_p_a_r_e_n_t)(_v_o_i_d),
         _v_o_i_d (_*_c_h_i_l_d)(_v_o_i_d));

DDEESSCCRRIIPPTTIIOONN
     The pptthhrreeaadd__aattffoorrkk() function declares fork handlers to be called before
     and after fork(2), in the context of the thread that called fork(2).

     The handlers registered with pptthhrreeaadd__aattffoorrkk() are called at the moments
     in time described below:

     _p_r_e_p_a_r_e  Before fork(2) processing commences in the parent process.  If
              more than one _p_r_e_p_a_r_e handler is registered they will be called
              in the opposite order they were registered.

     _p_a_r_e_n_t   After fork(2) completes in the parent process.  If more than one
              _p_a_r_e_n_t handler is registered they will be called in the same or‐
              der they were registered.

     _c_h_i_l_d    After fork(2) processing completes in the child process.  If
              more than one _c_h_i_l_d handler is registered they will be called in
              the same order they were registered.

     If no handling is desired at one or more of these three points, a null
     pointer may be passed as the corresponding fork handler.

RREETTUURRNN VVAALLUUEESS
     If successful, the pptthhrreeaadd__aattffoorrkk() function will return zero.  Otherwise
     an error number will be returned to indicate the error.

EERRRROORRSS
     The pptthhrreeaadd__aattffoorrkk() function will fail if:

     [ENOMEM]           Insufficient table space exists to record the fork
                        handler addresses.

SSEEEE AALLSSOO
     fork(2), pthread(3)

SSTTAANNDDAARRDDSS
     The pptthhrreeaadd__aattffoorrkk() function is expected to conform to IEEE Std 1003.1
     (“POSIX.1”).

AAUUTTHHOORRSS
     This manpage was written by Alex Vasylenko <_l_x_v_@_o_m_u_t_._o_r_g>.

BSD                              June 21, 2004                             BSD
