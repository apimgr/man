_I_P_P_O_O_L(8)                   System Manager’s Manual                  _I_P_P_O_O_L(8)

NNAAMMEE
       ippool - user interface to the IPFilter pools

SSYYNNOOPPSSIISS
       iippppooooll  ‐a  [‐dnv]  [‐m  <name>]  [‐o  <role>]  [‐t <type>] [‐T ttl] ‐i
       <ipaddr>[/<netmask>]
       iippppooooll ‐A [‐dnv] [‐m <name>] [‐o <role>] [‐S <seed>] [‐t <type>]
       iippppooooll ‐f <file> [‐dnuv]
       iippppooooll ‐F [‐dv] [‐o <role>] [‐t <type>]
       iippppooooll ‐l [‐dv] [‐m <name>] [‐t <type>]
       iippppooooll ‐r [‐dnv] [‐m <name>] [‐o <role>] [‐t <type>] ‐i <ipaddr>[/<net‐
       mask>]
       iippppooooll ‐R [‐dnv] [‐m <name>] [‐o <role>] [‐t <type>]
       iippppooooll ‐s [‐dtv] [‐M <core>] [‐N <namelist>]

DDEESSCCRRIIPPTTIIOONN
       IIppppooooll is used to manage information stored in the IP  pools  subsystem
       of  IPFilter.   Configuration file information may be parsed and loaded
       into the kernel, currently configured pools removed or changed as  well
       as inspected.

       The  command line options used are broken into two sections: the global
       options and the instance specific options.

GGLLOOBBAALL OOPPTTIIOONNSS
       --dd     Toggle debugging of processing the configuration file.

       --nn     This flag (no‐change) prevents iippppooooll from actually  making  any
              ioctl  calls  or  doing anything which would alter the currently
              running kernel.

       --vv     Turn verbose mode on.

CCOOMMMMAANNDD OOPPTTIIOONNSS
       ‐‐aa     Add a new data node to an existing pool in the kernel.

       ‐‐AA     Add a new (empty) pool to the kernel.

       ‐‐ff <<ffiillee>>
              Read in IP pool configuration information from the file and load
              it into the kernel.

       ‐‐FF     Flush loaded pools from the kernel.

       ‐‐ll     Display a list of pools currently loaded into the kernel.

       ‐‐rr     Remove an existing data node from a pool in the kernel.

       ‐‐RR     Remove an existing pool from within the kernel.

       ‐‐ss     Display IP pool statistical information.

OOPPTTIIOONNSS
       ‐‐ii <<iippaaddddrr>>[[//<<nneettmmaasskk>>]]
              Sets the IP address for the operation being undertaken  with  an
              all‐one’s  mask  or, optionally, a specific netmask given in ei‐
              ther the dotted‐quad notation or a single integer.

       ‐‐mm <<nnaammee>>
              Sets the pool name for the current operation.

       ‐‐MM <<ccoorree>>
              Specify an alternative path to /dev/kmem to retrieve statistical
              information from.

       ‐‐NN <<nnaammeelliisstt>>
              Specify an alternative path to lookup  symbol  name  information
              from when retrieving statistical information.

       ‐‐oo <<rroollee>>
              Sets  the  role  with  which this pool is to be used.  Currently
              only iippff,, aauutthh and ccoouunntt are accepted as arguments to  this  op‐
              tion.

       ‐‐SS <<sseeeedd>>
              Sets  the  hashing  seed  to the number specified.  Only for use
              with hhaasshh type pools.

       ‐‐tt <<ttyyppee>>
              Sets the type of pool being defined.  Myst be one of ttrreeee,, hhaasshh,,
              ggrroouupp‐‐mmaapp..

       ‐‐TT <<ttttll>>
              Sets the expiration of the node being added. The timeout is  ex‐
              pressed as a number of seconds.  ttrreeee,, hhaasshh,, ggrroouupp‐‐mmaapp..

       ‐‐uu     When  parsing  a  configuration  file, rather than load new pool
              data into the kernel, unload it.

FFIILLEESS
       /dev/iplookup
       /etc/ippool.conf

SSEEEE AALLSSOO
       ippool(5), ipf(8), ipfstat(8)

                                                                     _I_P_P_O_O_L(8)
