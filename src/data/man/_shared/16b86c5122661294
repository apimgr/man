The  driver  supports Intel Ethernet 700 series controllers based
on the Intel X710, XXV710, XL710,  and  X722  Ethernet  chipsets.
The driver comes from It first appeared in The driver was written
by  and  It  was  imported  from into by The VLAN hardware filter
doesn’t work while the interface is in promiscuous mode.  If reg‐
istering a VLAN tag fails due to lack of resources, the interface
doesn’t send and receive packets containing the tag.
