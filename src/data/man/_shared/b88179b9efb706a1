runs  on  or  stdin, if no argument is given, after decompressing
them with The and arguments are passed on to If an flag is  found
in  the  will not look for a argument.  calls while calls In case
of missing arguments or missing pattern, 1 will be returned, oth‐
erwise 0.
