is  a  server  which returns performance statistics obtained from
the kernel.  These statistics are read using  the  command.   The
daemon  is  normally invoked by The argument specifies the number
of seconds that should stay active, updating its internal statis‐
tics every second.  If no value is specified, 20 seconds will  be
used.   After  seconds  with  no new RPC requests, if was invoked
from exits.  Otherwise, loops, becoming dormant until it receives
a new RPC request, and staying active until seconds pass with  no
new requests.  uses an RPC protocol defined in
