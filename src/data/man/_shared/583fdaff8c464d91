The  interface  provides  access  to a network via the Am7990 and
Am79C90 (CMOS, pin‐compatible) (Local Area Network  Controller  ‐
Ethernet) chip set.  In previous releases of the driver also sup‐
ported  PCnet‐PCI  cards based on the chipset, which is a single‐
chip implementation of an Ethernet interface that has a  compati‐
bility  mode combined with a bus interface.  PCnet‐PCI interfaces
have been supported by the driver since Each of the  host’s  net‐
work  addresses  is  specified at boot time with an The interface
employs the Address Resolution Protocol (ARP) described in to dy‐
namically map between Internet and addresses on  the  local  net‐
work.   Selective  reception of multicast Ethernet frames is pro‐
vided by a  64‐bit  mask;  multicast  destination  addresses  are
hashed  to  a bit entry using the Ethernet CRC function.  The use
of encapsulation to minimize copying data on input and output  is
supported  by  the  interface  but offers no advantage on systems
with large page sizes.  The use of trailers is automatically  ne‐
gotiated  with  This negotiation may be disabled, on a per‐inter‐
face basis, with The interface supports the  following  Zorro  II
expansion  cards:  Commodore’s  Ethernet  card, manufacturer 514,
product 112 Ameristar’s Ethernet card,  manufacturer 1053,  prod‐
uct 1  Village  Tronic’s  Ethernet card, manufacturer 2167, prod‐
uct 201 The A2065 and Ameristar Ethernet cards support only  man‐
ual  media selection.  The Ariadne card supports a software media
selection for its two different connectors: also known  as  thin‐
wire‐Ethernet also known as twisted pair The Ariadne card uses an
autoselect between UTP and BNC, so it uses UTP when an active UTP
line  is connected or otherwise BNC.  See for media selection op‐
tions for The ISA‐bus Ethernet cards supported by  the  interface
are:  The EISA‐bus Ethernet cards supported by the interface are:
The MCA‐bus Ethernet cards supported by the  interface  are:  All
interfaces on are supported, as are interfaces on machines with a
bus.   No  support is provided for switching between media ports.
The 3100 provides both AUI and BNC (thinwire or 10BASE2)  connec‐
tors.   Port selection is via a manual switch and is not software
configurable.  The model 5000/200 baseboard device provides  only
a  BNC connector.  The baseboard devices and the option card pro‐
vide only an AUI port.  The cards supported by the interface  in‐
clude:  and  Buffered  (sun part 501‐1860) Fast and Buffered (sun
part 501‐2015) (antares part 20‐050‐1007) Interfaces attached  to
an on SPARC systems typically have two types of connectors: Stan‐
dard  15 pin connector UTP, also known as twisted pair The appro‐
priate connector can be selected by supplying a parameter to  The
supported  arguments  for are: to select the AUI connector, or to
select the UTP connector.  If a parameter is not specified, a de‐
fault connector is selected for use by examining all media  types
for  carrier.  The first connector on which a carrier is detected
will be selected.  Additionally, if carrier is dropped on a port,
the driver will switch between the possible ports until one  with
carrier  is  found.   More packets came in from the Ethernet than
there was space in the receive  buffers.   Packets  were  missed.
Ran  out  of  buffer space, packet dropped.  The Ethernet carrier
disappeared during an attempt to transmit.  It will finish trans‐
mitting the current packet,  but  will  not  automatically  retry
transmission if there is a collision.  Ethernet extremely busy or
jammed, outbound packets dropped after 16 attempts to retransmit.
is  The  TDR value is an internal counter of the interval between
the start of a transmission and the occurrence  of  a  collision.
This  value can be used to determine the distance from the Ether‐
net tap to the point on the Ethernet cable  that  is  shorted  or
open  (unterminated).   Packet  didn’t  fit into a single receive
buffer, packet dropped.  Since the driver allocates buffers large
enough to receive the maximum size packet, this means some  other
station  on  the  LAN transmitted a packet larger than allowed by
the standard.  ran out of buffer before finishing  the  transmis‐
sion  of a packet.  If this error occurs, the driver software has
a bug.  ran out of buffer before finishing the transmission of  a
packet.   If  this  error  occurs, the driver software has a bug.
Driver failed to start the AM7990 This is potentially a  hardware
failure.  failed to respond within the timeout when the wanted to
read  or  write it.  This is potentially a hardware failure.  The
receiver was turned off due to an  error.   The  transmitter  was
turned  off  due  to an error.  The pmax driver is derived from a
driver that first appeared in Support for  multiple  bus  attach‐
ments  first appeared in The interface first appeared in The Ari‐
adne Ethernet card first appeared with the ae  interface  in  and
was  converted  to  the  interface in The Am7990 Revision C chips
have a bug which causes garbage to be inserted in  front  of  the
received packet occasionally.  The work‐around is to ignore pack‐
ets with an invalid destination address (garbage will usually not
match),  by  double‐checking  the  destination  address  of every
packet in the driver.  This work‐around is enabled with the  ker‐
nel  option.   When  is  enabled,  the driver executes one or two
calls to an inline Ethernet address comparison function for every
received packet.  On the it is exactly eight instructions  of  16
bits  each.  There is one comparison for each unicast packet, and
two comparisons for each broadcast packet.  In summary, the  cost
of  the  LANCE_REVC_BUG option is: loss of multicast support, and
eight extra instructions per received packet, sometimes  sixteen,
depending  on  both  the  processor, and the type of packet.  All
sun3 systems are presumed  to  have  this  bad  revision  of  the
Am7990, until proven otherwise.  Alas, the only way to prove what
revision  of  the chip is in a particular system is inspection of
the date code on the chip package, to compare against a  list  of
what  chip  revisions were fabricated between which dates.  Alas,
the Am7990 chip is so old that  has  the  production  information
about  it; pending a search elsewhere, we don’t know how to iden‐
tify the revision C chip from the date codes.  On all pmax front‐
ends, performance is impaired by hardware which forces a software
copy of packets to and from DMA buffers.  The  machines  and  the
3100  must  copy  packets to and from non‐contiguous DMA buffers.
The 5000/200 and the must copy to and from an  onboard  SRAM  DMA
buffer.   The  CPU  overhead  is noticeable, but all machines can
sustain full 10 Mb/s media speed.
