prints  a  summary of the disk usage and quotas for the specified
file systems.  Available options: Print the  quotas  of  all  the
mounted file systems.  Print only group quotas (the default is to
print  both  group  and  user quotas if they exist).  Numbers are
displayed in a human readable format.   Print  only  user  quotas
(the  default  is to print both group and user quotas if they ex‐
ist).  Print a header line before printing each file system  quo‐
tas.  Print all exiting quotas, including those whose current us‐
age is 0.  Export file system quota data in a tabular dump format
suitable  for  A  single file system should be specified.  If in‐
voked as the behavior is the same as For each user or group,  the
current  number  files  and amount of space (in kilobytes, unless
the flag is used) is printed, along with any quotas created  with
Only  the  super‐user may use this command.  The command appeared
in
