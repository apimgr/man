The  program  retrieves and displays entries from the administra‐
tive database specified by using the lookup  order  specified  in
The display format for a given is as per the file format for that
database.   may be one of: If one or more arguments are provided,
they will be looked up in using the  appropriate  function.   For
example,  supports  a  numeric UID or user name; supports an IPv4
address, IPv6 address, or host name; and supports a service name,
service name/protocol name, numeric port, or numeric  port/proto‐
col  name.   If  no is provided and supports enumeration, all en‐
tries for will be retrieved  using  the  appropriate  enumeration
function  and  printed.   For  style  databases specifying a key,
lists the entry for that key, and specifying more arguments after
the key are used as fields in that key, and only  the  values  of
the  keys  are returned.  For boolean keys is returned if the key
is found.  If a key is not found, then is always returned.  exits
0 on success, 1 if there was an error in the command syntax, 2 if
one of the specified key names was not found in or 3 if there  is
no  support for enumeration on A command appeared in It was based
on the command of the same name in Solaris and Linux.
