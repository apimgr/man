_k_i_l_l_p_g(3)                  Library Functions Manual                  _k_i_l_l_p_g(3)

NNAAMMEE
       killpg - send signal to a process group

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssiiggnnaall..hh>>

       iinntt kkiillllppgg((iinntt _p_g_r_p,, iinntt _s_i_g));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       kkiillllppgg():
           _XOPEN_SOURCE >= 500
               || /* Since glibc 2.19: */ _DEFAULT_SOURCE
               || /* glibc <= 2.19: */ _BSD_SOURCE

DDEESSCCRRIIPPTTIIOONN
       kkiillllppgg() sends the signal _s_i_g to the process group _p_g_r_p.  See ssiiggnnaall(7)
       for a list of signals.

       If  _p_g_r_p  is  0,  kkiillllppgg()  sends  the  signal to the calling process’s
       process group.  (POSIX says: if _p_g_r_p is less than or equal  to  1,  the
       behavior is undefined.)

       For  the  permissions required to send a signal to another process, see
       kkiillll(2).

RREETTUURRNN VVAALLUUEE
       On success, zero is returned.  On error, -1 is returned, and  _e_r_r_n_o  is
       set to indicate the error.

EERRRROORRSS
       EEIINNVVAALL _s_i_g is not a valid signal number.

       EEPPEERRMM  The  process  does not have permission to send the signal to any
              of the target processes.   For  the  required  permissions,  see
              kkiillll(2).

       EESSRRCCHH  No process can be found in the process group specified by _p_g_r_p.

       EESSRRCCHH  The  process  group  was given as 0 but the sending process does
              not have a process group.

VVEERRSSIIOONNSS
       There are various differences between the permission checking  in  BSD‐
       type  systems  and  System V‐type systems.  See the POSIX rationale for
       kkiillll(3p).  A difference not mentioned  by  POSIX  concerns  the  return
       value  EEPPEERRMM:  BSD  documents that no signal is sent and EEPPEERRMM returned
       when the permission check failed for at least one target process, while
       POSIX documents EEPPEERRMM only when the permission  check  failed  for  all
       target processes.

   CC lliibbrraarryy//kkeerrnneell ddiiffffeerreenncceess
       On  Linux, kkiillllppgg() is implemented as a library function that makes the
       call _k_i_l_l_(_-_p_g_r_p_, _s_i_g_).

SSTTAANNDDAARRDDSS
       POSIX.1‐2008.

HHIISSTTOORRYY
       POSIX.1‐2001, SVr4, 4.4BSD (first appeared in 4BSD).

SSEEEE AALLSSOO
       ggeettppggrrpp(2), kkiillll(2), ssiiggnnaall(2), ccaappaabbiilliittiieess(7), ccrreeddeennttiiaallss(7)

Linux man‐pages (unreleased)      2025‐05‐17                         _k_i_l_l_p_g(3)
