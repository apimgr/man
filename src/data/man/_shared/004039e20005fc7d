_I_P_F(8)                      System Manager’s Manual                     _I_P_F(8)

NNAAMMEE
       ipf - alters packet filtering lists for IP packet input and output

SSYYNNOOPPSSIISS
       iippff  [  --66AAccddDDEEIInnooPPrrssvvVVyyzzZZ  ] [ --ll <block|pass|nomatch> ] [ --TT <option‐
       list> ] [ --FF <i|o|a|s|S> ] --ff <_f_i_l_e_n_a_m_e> [ --ff <_f_i_l_e_n_a_m_e> [...]]

DDEESSCCRRIIPPTTIIOONN
       iippff opens the filenames listed (treating "-" as stdin) and  parses  the
       file  for  a  set  of  rules  which are to be added or removed from the
       packet filter rule set.

       Each rule processed by iippff is added to the kernel’s internal  lists  if
       there  are  no parsing problems.  Rules are added to the end of the in‐
       ternal lists, matching the order in which they  appear  when  given  to
       iippff.

OOPPTTIIOONNSS
       --66     This  option  is  required  to parse IPv6 rules and to have them
              loaded.

       --AA     Set the list to make changes to the active list (default).

       --cc <<llaanngguuaaggee>>
              This option causes iippff to generate output files for  a  compiler
              that  supports  llaanngguuaaggee_.   _A_t _p_r_e_s_e_n_t_, _t_h_e _o_n_l_y _t_a_r_g_e_t _l_a_n_g_u_a_g_e
              _s_u_p_p_o_r_t_e_d _i_s CC ((‐‐cccc))  ffoorr  wwhhiicchh  ttwwoo  ffiilleess  ‐‐  iipp__rruulleess..cc  aanndd
              iipp__rruulleess..hh  aarree  ggeenneerraatteedd  iinn tthhee CCUURRRREENNTT DDIIRREECCTTOORRYY wwhheenn iippff iiss
              bbeeiinngg rruunn..  TThheessee ffiilleess ccaann bbee uusseedd wwiitthh  tthhee  IIPPFFIILLTTEERR__CCOOMMPPIILLEEDD
              kkeerrnneell ooppttiioonn ttoo bbuuiilldd ffiilltteerr rruulleess ssttaattiiccllyy iinnttoo tthhee kkeerrnneell..

       --dd     Turn debug mode on.  Causes a hexdump of filter rules to be gen‐
              erated as it processes each one.

       --DD     Disable  the  filter  (if  enabled).  Not effective for loadable
              kernel versions.

       --EE     Enable the filter (if disabled).   Not  effective  for  loadable
              kernel versions.

       --FF <i|o|a>
              This option specifies which filter list to flush.  The parameter
              should  either  be  "i" (input), "o" (output) or "a" (remove all
              filter rules).  Either a single letter or an entire word  start‐
              ing  with  the appropriate letter maybe used.  This option maybe
              before, or after, any other with the order on the  command  line
              being that used to execute options.

       --FF <s|S>
              To  flush entries from the state table, the ‐‐FF option is used in
              conjunction with either "s" (removes state information about any
              non‐fully established connections) or "S"  (deletes  the  entire
              state  table).   Only  one  of  the two options may be given.  A
              fully established connection will show up in iippffssttaatt  ‐‐ss  output
              as  5/5,  with  deviations either way indicating it is not fully
              established any more.

       --FF<5|6|7|8|9|10|11>
              For the TCP states that represent the closing  of  a  connection
              has begun, be it only one side or the complete connection, it is
              possible  to flush those states directly using the number corre‐
              sponding to that state.  The numbers relate  to  the  states  as
              follows:  5 = close‐wait, 6 = fin‐wait‐1, 7 = closing, 8 = last‐
              ack, 9 = fin‐wait‐2, 10 = time‐wait, 11 = closed.

       --FF<number>
              If the argument supplied to ‐‐FF is greater than  30,  then  state
              table  entries  that have been idle for more than this many sec‐
              onds will be flushed.

       --ff <filename>
              This option specifies which files iippff should use  to  get  input
              from for modifying the packet filter rule lists.

       --II     Set the list to make changes to the inactive list.

       --ll  <<bblloocckk||ppaassss||nnoommaattcchh||nnoonnee||ssttaattee||nnaatt>>
              Use  of  the  ‐‐ll flag toggles default logging of packets.  Valid
              arguments to this option are ppaassss, bblloocckk, nnoommaattcchh, nnoonnee,  ssttaattee,
              and  nnaatt.  When an option is set, any packet which exits filter‐
              ing and matches the set category is logged.  This is most useful
              for causing all packets which don’t  match  any  of  the  loaded
              rules to be logged.

       --nn     This  flag  (no‐change)  prevents  iippff  from actually making any
              ioctl calls or doing anything which would  alter  the  currently
              running kernel.

       --oo     Force  rules  by  default to be added/deleted to/from the output
              list, rather than the (default) input list.

       --PP     Add rules as temporary entries in the authentication rule table.

       --rr     Remove matching filter rules rather than add them to the  inter‐
              nal lists

       --ss     Swap the active filter list in use to be the "other" one.

       --TT <<ooppttiioonnlliisstt>>
              This  option  allows  run‐time changing of IPFilter kernel vari‐
              ables.  Some variables require IPFilter  to  be  in  a  disabled
              state  (‐‐DD) for changing, others do not.  The optionlist parame‐
              ter is a comma separated list of tuning commands.  A tuning com‐
              mand is either "list" (retrieve a list of all variables  in  the
              kernel,  their  maximum,  minimum  and  current value), a single
              variable name (retrieve its current value) and a  variable  name
              with  a  following assignment to set a new value.  Some examples
              follow.
              # Print out all IPFilter kernel tunable parameters
              ipf ‐T list
              # Display the current TCP idle timeout and then set it to 3600
              ipf ‐D ‐T fr_tcpidletimeout,fr_tcpidletimeout=3600 ‐E
              # Display current values for fr_pass and fr_chksrc, then set fr_chksrc to 1.
              ipf ‐T fr_pass,fr_chksrc,fr_chksrc=1

       --vv     Turn verbose mode on.  Displays  information  relating  to  rule
              processing.

       --VV     Show  version information.  This will display the version infor‐
              mation compiled into the ipf binary and  retrieve  it  from  the
              kernel  code (if running/present).  If it is present in the ker‐
              nel, information about  its  current  state  will  be  displayed
              (whether logging is active, default filtering, etc).

       --yy     Manually  resync  the  in‐kernel interface list maintained by IP
              Filter with the current interface status list.

       --zz     For each rule in the input file, reset the statistics for it  to
              zero and display the statistics prior to them being zeroed.

       --ZZ     Zero  global  statistics  held  in the kernel for filtering only
              (this doesn’t affect fragment or state statistics).

FFIILLEESS
       /dev/ipauth
       /dev/ipl
       /dev/ipstate

SSEEEE AALLSSOO
       ipftest(1), mkfilters(1), ipf(4), ipl(4), ipf(5), ipfstat(8), ipmon(8),
       ipnat(8)

DDIIAAGGNNOOSSTTIICCSS
       Needs to be run as root for the packet filtering lists to  actually  be
       affected inside the kernel.

BBUUGGSS
       If you find any, please send email to me at darrenr@pobox.com

                                                                        _I_P_F(8)
