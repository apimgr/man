_l_o_c_k_f(3)                   Library Functions Manual                   _l_o_c_k_f(3)

NNAAMMEE
       lockf - apply, test or remove a POSIX lock on an open file

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<uunniissttdd..hh>>

       iinntt lloocckkff((iinntt _f_d,, iinntt _o_p,, ooffff__tt _s_i_z_e));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       lloocckkff():
           _XOPEN_SOURCE >= 500
               || /* glibc >= 2.19: */ _DEFAULT_SOURCE
               || /* glibc <= 2.19: */ _BSD_SOURCE || _SVID_SOURCE

DDEESSCCRRIIPPTTIIOONN
       Apply,  test, or remove a POSIX lock on a section of an open file.  The
       file is specified by _f_d, a file descriptor open for writing, the action
       by _o_p, and the section consists of byte  positions  _p_o_s.._p_o_s+_s_i_z_e-1  if
       _s_i_z_e is positive, and _p_o_s-_s_i_z_e.._p_o_s-1 if _s_i_z_e is negative, where _p_o_s is
       the  current  file  position,  and if _s_i_z_e is zero, the section extends
       from the current file position to infinity,  encompassing  the  present
       and future end‐of‐file positions.  In all cases, the section may extend
       past current end‐of‐file.

       On  Linux,  lloocckkff()  is  just  an interface on top of ffccnnttll(2) locking.
       Many other systems implement lloocckkff() in this way, but note that POSIX.1
       leaves the relationship between lloocckkff() and ffccnnttll(2) locks unspecified.
       A portable application should probably avoid mixing calls to these  in‐
       terfaces.

       Valid operations are given below:

       FF__LLOOCCKK Set  an exclusive lock on the specified section of the file.  If
              (part of) this section is already locked, the call blocks  until
              the previous lock is released.  If this section overlaps an ear‐
              lier  locked  section, both are merged.  File locks are released
              as soon as the process holding the locks closes  some  file  de‐
              scriptor  for  the file.  A child process does not inherit these
              locks.

       FF__TTLLOOCCKK
              Same as FF__LLOOCCKK but the call never blocks and  returns  an  error
              instead if the file is already locked.

       FF__UULLOOCCKK
              Unlock  the  indicated  section  of  the file.  This may cause a
              locked section to be split into two locked sections.

       FF__TTEESSTT Test the lock: return 0 if the specified section is unlocked  or
              locked  by  this process; return -1, set _e_r_r_n_o to EEAAGGAAIINN (EEAACCCCEESS
              on some other systems), if another process holds a lock.

RREETTUURRNN VVAALLUUEE
       On success, zero is returned.  On error, -1 is returned, and  _e_r_r_n_o  is
       set to indicate the error.

EERRRROORRSS
       EEAACCCCEESS or EEAAGGAAIINN
              The  file  is locked and FF__TTLLOOCCKK or FF__TTEESSTT was specified, or the
              operation is prohibited because the file has been  memory‐mapped
              by another process.

       EEBBAADDFF  _f_d  is  not  an open file descriptor; or _o_p is FF__LLOOCCKK or FF__TTLLOOCCKK
              and _f_d is not a writable file descriptor.

       EEDDEEAADDLLKK
              _o_p was FF__LLOOCCKK and this lock operation would cause a deadlock.

       EEIINNTTRR  While waiting to acquire a lock, the call was interrupted by de‐
              livery of a signal caught by a handler; see ssiiggnnaall(7).

       EEIINNVVAALL An invalid operation was specified in _o_p.

       EENNOOLLCCKK Too many segment locks open, lock table is full.

AATTTTRRIIBBUUTTEESS
       For an explanation of the terms  used  in  this  section,  see  aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       lloocckkff() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       POSIX.1‐2008.

HHIISSTTOORRYY
       POSIX.1‐2001, SVr4.

SSEEEE AALLSSOO
       ffccnnttll(2), fflloocckk(2)

       _l_o_c_k_s_._t_x_t and _m_a_n_d_a_t_o_r_y_-_l_o_c_k_i_n_g_._t_x_t in the Linux kernel source
       directory _D_o_c_u_m_e_n_t_a_t_i_o_n_/_f_i_l_e_s_y_s_t_e_m_s (on older kernels, these files are
       directly under the _D_o_c_u_m_e_n_t_a_t_i_o_n directory, and _m_a_n_d_a_t_o_r_y_-_l_o_c_k_i_n_g_._t_x_t
       is called _m_a_n_d_a_t_o_r_y_._t_x_t)

Linux man‐pages (unreleased)      2025‐09‐21                          _l_o_c_k_f(3)
