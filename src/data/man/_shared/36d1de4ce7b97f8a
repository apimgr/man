_g_e_t_p_a_s_s(3)                 Library Functions Manual                 _g_e_t_p_a_s_s(3)

NNAAMMEE
       getpass - get a password

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<uunniissttdd..hh>>

       [[[[ddeepprreeccaatteedd]]]] cchhaarr **ggeettppaassss((ccoonnsstt cchhaarr **_p_r_o_m_p_t));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       ggeettppaassss():
           Since glibc 2.2.2:
               _XOPEN_SOURCE && ! (_POSIX_C_SOURCE >= 200112L)
                   || /* glibc >= 2.19: */ _DEFAULT_SOURCE
                   || /* glibc <= 2.19: */ _BSD_SOURCE
           Before glibc 2.2.2:
               none

DDEESSCCRRIIPPTTIIOONN
       This  function is obsolete.  Do not use it.  See NOTES.  If you want to
       read input without terminal echoing enabled, see the description of the
       _E_C_H_O flag in tteerrmmiiooss(3).

       The ggeettppaassss() function opens _/_d_e_v_/_t_t_y (the controlling terminal of  the
       process),  outputs the string _p_r_o_m_p_t, turns off echoing, reads one line
       (the "password"), restores  the  terminal  state  and  closes  _/_d_e_v_/_t_t_y
       again.

RREETTUURRNN VVAALLUUEE
       The  function ggeettppaassss() returns a pointer to a static buffer containing
       (the first PPAASSSS__MMAAXX bytes of) the password without  the  trailing  new‐
       line, terminated by a null byte ('\0').  This buffer may be overwritten
       by  a  following call.  On error, the terminal state is restored, _e_r_r_n_o
       is set to indicate the error, and NULL is returned.

EERRRROORRSS
       EENNXXIIOO  The process does not have a controlling terminal.

FFIILLEESS
       _/_d_e_v_/_t_t_y

AATTTTRRIIBBUUTTEESS
       For an explanation of the terms  used  in  this  section,  see  aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       ggeettppaassss() T}   Thread safety  MT‐Unsafe term

SSTTAANNDDAARRDDSS
       None.

HHIISSTTOORRYY
       Version 7 AT&T UNIX.  Present in SUSv2, but marked LEGACY.  Removed in
       POSIX.1‐2001.

NNOOTTEESS
       You should use instead rreeaaddppaasssspphhrraassee(3bsd), provided by _l_i_b_b_s_d.

       In the GNU C library implementation, if _/_d_e_v_/_t_t_y cannot be opened, the
       prompt is written to _s_t_d_e_r_r and the password is read from _s_t_d_i_n.  There
       is no limit on the length of the password.  Line editing is not
       disabled.

       According to SUSv2, the value of PPAASSSS__MMAAXX must be defined in _<_l_i_m_i_t_s_._h_>
       in case it is smaller than 8, and can in any case be obtained using
       _s_y_s_c_o_n_f_(___S_C___P_A_S_S___M_A_X_).  However, POSIX.2 withdraws the constants
       PPAASSSS__MMAAXX and __SSCC__PPAASSSS__MMAAXX, and the function ggeettppaassss().  The glibc
       version accepts __SSCC__PPAASSSS__MMAAXX and returns BBUUFFSSIIZZ (e.g., 8192).

BBUUGGSS
       The calling process should zero the password as soon as possible to
       avoid leaving the cleartext password visible in the process’s address
       space.

SSEEEE AALLSSOO
       ccrryypptt(3)

Linux man‐pages (unreleased)      2025‐05‐17                        _g_e_t_p_a_s_s(3)
