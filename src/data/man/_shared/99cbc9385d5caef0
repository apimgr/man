This  document  describes  the  layout of releases and snapshots.
This layout should be consistent between FTP servers and CD‐ROMs,
except possibly the path that leads to the release hierarchy.  In
this document, the following special  words  have  these  defini‐
tions:  The platform for which the release was built, correspond‐
ing to the sysctl variable, e.g., or The architecture for which a
particular installation  set  was  built,  corresponding  to  the
sysctl  variable, e.g., or The release version number.  All files
are descriptions of the various files in  directories  that  have
contents.   There may also be a file at the top‐level, describing
who built the snapshot and under what circumstances snapshot,  or
not  All  files are MD5 digests for the various files in that di‐
rectory, in the format produced by the  command:  All  files  are
SHA512  digests  for  the various files in that directory, in the
format produced by the command: The SHA512 digest is  safer,  but
MD5  checksums  are  provided  so that a wider range of operating
systems can check the integrity of the release files.  Files that
end in are gzipped tar archives.  This is used in lieu of because
the software used to download the sets may  incorrectly  auto‐un‐
pack  files  ending in and to accommodate systems which only sup‐
port 3 character extensions to file names.  All tar archives  are
relative  to  the target’s directory, and include the leading The
root of the release hierarchy may be the root directory of a  CD‐
ROM,  but in all other cases it should be The root of the release
hierarchy should contain the following files and  subdirectories:
Image  files  intended  for use in installing There are different
types of images for different platforms, and  sometimes  multiple
different  image types for a single platform.  Images in this di‐
rectory, unlike images in the directories, contain  file  systems
that  have  an internal layout that corresponds to a complete re‐
lease for a given machine type.  If built with then it will  also
contain  a directory.  These images are usually bootable.  CD‐ROM
images in ISO 9660 format, usually created with  after  a  in  or
created  with after a in Bootable installation images intended to
be written to any drive which appears as an  type,  such  as  USB
flash  disks.   These  images are created with in Files shared by
two or more machine types.  Files which may be shared by all sys‐
tems of the same will be located in with symbolic links  pointing
to these files from the subdirectory.  Files which are completely
machine‐independent will be located in with symbolic links point‐
ing to these files from the subdirectory.  Source code of the op‐
erating  system  should  be  put into using the following layout:
Sources for the various system sets.  Contains  sources  for  all
GPLed  and  possibly other programs that contains restrictions in
their licensing that prevent others from using these programs  in
closed‐source  environments.   Contains  machine‐independent data
files that can be shared across architectures/systems.  The oper‐
ating system’s userland  source  code,  including  all  programs,
tools, libraries, etc.  Kernel sources for all architectures plus
sources of the tools needed to build kernels (like Source code of
the  X  Window  System  used  on  all  architectures.  Includes X
clients and servers.  The binary releases in  fit  the  following
layout:  Installation  notes,  including complete descriptions of
files contained within  the  release  hierarchy  pretty  version,
suited  for  viewing  with HTML version PostScript version system
binaries compressed system images The  primary  system  image  on
those  platforms  that  provide  them.   These  images may not be
bootable.  A bootable system image for  a  particular  board,  on
platforms  that  provide them.  installation sets The base binary
distribution.  This set contains the base utilities that are nec‐
essary for the system to run and be minimally  functional.   This
set  excludes all things listed in the sets described below.  The
compiler tools distribution.  This set contains  the  C  and  C++
compilers,  assembler,  linker,  other  toolchain components, and
their manual pages.  It also includes the  system  include  files
and  the  static  system libraries.  This set contains the system
configuration files that reside in and in  several  other  places
throughout  the  file  system  hierarchy.   This set includes the
games and their manual pages.  This set includes a  kernel  built
from  the kernel configuration file.  This is meant as an example
only; different platforms may  have  differently  named  kernels.
This  set  includes  all of the manual pages for the binaries and
other software contained in the set which are not included in the
other sets.  This set includes miscellaneous non‐essential files,
including dictionaries, the typesettable document set, and  vari‐
ous  other  documentation  and example configuration files.  This
set includes all the kernel modules.  This set contains the util‐
ities.  This set includes the for the  automated  test  framework
used by NetBSD.  This set includes the text processing tools, in‐
cluding  all  related programs, and their manual pages.  This set
includes the base X11 distribution, including  manual  pages  and
excluding  everything  contained in the other X11 sets.  This set
includes X11 configuration files.  This set includes the X11  in‐
clude  files and static X11 libraries.  This set includes the X11
fonts.  This set includes the X servers and manual  pages  for  a
given  machine.   suitably  named, gzipped kernels A kernel built
from the kernel configuration file.  This is meant as an  example
only;  different  platforms  may  have differently named kernels.
installation helper items CD‐ROM images in ISO 9660 format,  cre‐
ated  as  part  of  in Images in this directory are bootable, and
contain one a kernel, installation tools, and rescue tools.  They
do not contain installation sets, source sets,  or  other  compo‐
nents of a complete release.  VGA console Serial console disk im‐
ages,  on  those  platforms  that  provide them floppy images, on
those platforms that provide them installation kernels for  plat‐
forms that can boot them directly miniroot images, on those plat‐
forms  that provide them miscellaneous installation helper utili‐
ties, including boot selectors, floppy  writing  software,  other
software that runs under foreign operating systems, etc.  network
boot  programs  tape images, on those platforms that provide them
The manual page first appeared in
