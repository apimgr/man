The  function sets the on the attributes object referenced by The
function retrieves the on the attributes object referenced by The
argument is either 0 or a bitwise OR of one or more of  the  fol‐
lowing  flags: If this bit is set, the child process will inherit
the real (rather than the effective) user and  group  ID  of  the
parent  process. NOTE: This flag has no effect when the set‐group
bit is set on the process image being spawned.  If  this  bit  is
not  set,  then  the  child  process  inherits the parent process
group; if it is set, then the child process will behave as if the
function had been called with a parameter of 0  and  a  parameter
equal  to  the  value  of the spawn‐pgroup value of the as set by
Signals set to either be caught or to the default action  in  the
parent  process  will  also  be  set to the default action in the
child process.  Signals set to be ignored in the  parent  process
will  be ignored in the child.  However, if this bit is set, then
signals in the value of the as set by which are caught or ignored
in the parent will instead be reset to their default  actions  in
the  child.   If this bit is set, then the initial signal mask of
the child process will be set to the value of the as  set  by  If
this  bit  is  set, rather than returning to the caller, and will
behave as a more featureful If this bit is set,  then  the  child
process  will  be created as if it immediately received a signal,
permitting debuggers, profilers, and other programs to manipulate
the process before it begins execution in user space.  This  per‐
mits,  for example, obtaining exact instruction counts, or debug‐
ging very early in To resume the child process, it must be sent a
signal.  If this bit is set, then only file  descriptors  explic‐
itly  created  by  the  argument  are  available  in  the spawned
process; all of the  other  file  descriptors  are  automatically
closed  in  the spawned process.  On success, these functions re‐
turn 0; on failure they return an error number from The  function
additionally, upon successful completion, makes the value pointed
to  by the argument equal to the attribute of the These functions
may fail if: The value specified by is invalid.  The value of  is
invalid.  The and function calls appeared in
