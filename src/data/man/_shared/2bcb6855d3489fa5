_p_t_h_r_e_a_d___g_e_t_c_p_u_c_l_o_c_k_i_d(3)   Library Functions Manual   _p_t_h_r_e_a_d___g_e_t_c_p_u_c_l_o_c_k_i_d(3)

NNAAMMEE
       pthread_getcpuclockid - retrieve ID of a thread’s CPU time clock

LLIIBBRRAARRYY
       POSIX threads library (_l_i_b_p_t_h_r_e_a_d, _-_l_p_t_h_r_e_a_d)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<pptthhrreeaadd..hh>>
       ##iinncclluuddee <<ttiimmee..hh>>

       iinntt pptthhrreeaadd__ggeettccppuucclloocckkiidd((pptthhrreeaadd__tt _t_h_r_e_a_d,, cclloocckkiidd__tt **_c_l_o_c_k_i_d));;

DDEESSCCRRIIPPTTIIOONN
       The  pptthhrreeaadd__ggeettccppuucclloocckkiidd()  function  obtains  the ID of the CPU‐time
       clock of the thread whose ID is given in _t_h_r_e_a_d, and returns it in  the
       location pointed to by _c_l_o_c_k_i_d.

RREETTUURRNN VVAALLUUEE
       On success, this function returns 0; on error, it returns a nonzero er‐
       ror number.

EERRRROORRSS
       EENNOOEENNTT Per‐thread CPU time clocks are not supported by the system.

       EESSRRCCHH  No thread with the ID _t_h_r_e_a_d could be found.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       pptthhrreeaadd__ggeettccppuucclloocckkiidd() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       POSIX.1‐2008.

HHIISSTTOORRYY
       glibc 2.2.  POSIX.1‐2001.

NNOOTTEESS
       When _t_h_r_e_a_d refers to the calling thread, this function returns an
       identifier that refers to the same clock manipulated by
       cclloocckk__ggeettttiimmee(2) and cclloocckk__sseettttiimmee(2) when given the clock ID
       CCLLOOCCKK__TTHHRREEAADD__CCPPUUTTIIMMEE__IIDD.

EEXXAAMMPPLLEESS
       The program below creates a thread and then uses cclloocckk__ggeettttiimmee(2) to
       retrieve the total process CPU time, and the per‐thread CPU time
       consumed by the two threads.  The following shell session shows an
       example run:

           $ ..//aa..oouutt;
           Main thread sleeping
           Subthread starting infinite loop
           Main thread consuming some CPU time...
           Process total CPU time:    1.368
           Main thread CPU time:      0.376
           Subthread CPU time:        0.992

   PPrrooggrraamm ssoouurrccee

       /* Link with "-lrt" */

       #include <err.h>
       #include <errno.h>
       #include <pthread.h>
       #include <stdint.h>
       #include <stdio.h>
       #include <stdlib.h>
       #include <string.h>
       #include <sys/time.h>
       #include <time.h>
       #include <unistd.h>

       static void *
       thread_start(void *arg)
       {
           printf("Subthread starting infinite loop\n");
           for (;;)
               continue;
       }

       static void
       pclock(char *msg, clockid_t cid)
       {
           struct timespec ts;

           printf("%s", msg);
           if (clock_gettime(cid, &ts) == -1)
               err(EXIT_FAILURE, "clock_gettime");
           printf("%4jd.%03ld\n", (intmax_t) ts.tv_sec, ts.tv_nsec / 1000000);
       }

       int
       main(void)
       {
           pthread_t thread;
           clockid_t cid;
           int s;

           s = pthread_create(&thread, NULL, thread_start, NULL);
           if (s != 0)
               errc(EXIT_FAILURE, s, "pthread_create");

           printf("Main thread sleeping\n");
           sleep(1);

           printf("Main thread consuming some CPU time...\n");
           for (unsigned int j = 0; j < 2000000; j++)
               getppid();

           pclock("Process total CPU time: ", CLOCK_PROCESS_CPUTIME_ID);

           s = pthread_getcpuclockid(pthread_self(), &cid);
           if (s != 0)
               errc(EXIT_FAILURE, s, "pthread_getcpuclockid");
           pclock("Main thread CPU time:   ", cid);

           /* The preceding 4 lines of code could have been replaced by:
              pclock("Main thread CPU time:   ", CLOCK_THREAD_CPUTIME_ID); */

           s = pthread_getcpuclockid(thread, &cid);
           if (s != 0)
               errc(EXIT_FAILURE, s, "pthread_getcpuclockid");
           pclock("Subthread CPU time: 1    ", cid);

           exit(EXIT_SUCCESS);         /* Terminates both threads */
       }

SSEEEE AALLSSOO
       cclloocckk__ggeettttiimmee(2), cclloocckk__sseettttiimmee(2), ttiimmeerr__ccrreeaattee(2),
       cclloocckk__ggeettccppuucclloocckkiidd(3), pptthhrreeaadd__sseellff(3), pptthhrreeaaddss(7), ttiimmee(7)

Linux man‐pages (unreleased)      2025‐08‐20          _p_t_h_r_e_a_d___g_e_t_c_p_u_c_l_o_c_k_i_d(3)
