configures a token bucket regulator for the output network inter‐
face  queue.   A  token  bucket regulator limits both the average
amount and instantaneous amount of packets  that  the  underlying
driver  can dequeue from the network interface within the kernel.
Conceptually, tokens accumulate in a bucket at the average up  to
the  The driver can dequeue packets as long as there are positive
amount of tokens, and the length of the dequeued packet  is  sub‐
tracted  from  the remaining tokens.  Tokens can be negative as a
deficit, and packets are not dequeued from  the  interface  queue
until  the  tokens become positive again.  The limits the average
rate, and the limits the maximum burst size.  Limiting the  burst
size  is  essential  to  packet  scheduling,  since the scheduler
schedules packets backlogged at the network interface.   Limiting
the  burst  size  is  also needed for drivers which dequeues more
packets than they can send and  end  up  with  discarding  excess
packets.   When the is set to higher than the actual transmission
rate, the transmission complete interrupt will trigger  the  next
dequeue.   On  the  other hand, when the is set to lower than the
actual transmission rate,  the  transmission  complete  interrupt
would occur before the tokens become positive.  In this case, the
next  dequeue  will  be  triggered by a timer event.  Because the
kernel timer has a limited granularity, a larger is required  for
a  higher  The parameter is a string of the form for example, The
parameter specifies the average rate in bits per second,  and  or
can  be  appended to as a short hand of or respectively.  When is
omitted, displays the current parameter  values.   The  parameter
specifies  the  bucket size in bytes, and can be appended to as a
short hand of When is omitted, assumes the regulator is driven by
transmission complete interrupts and, using heuristics, assigns a
small bucket size according to the When the keyword is  given  as
assumes the regulator is driven by the kernel timer, and computes
the bucket size from and the kernel clock frequency.  If the flag
is  passed before an interface name, will remove the token bucket
regulator for the specified interface.  Optionally, the flag  may
be  used  instead  of  an interface name.  This flag instructs to
display information about all interfaces in the system.  To  con‐
figure a token bucket regulator for the interface en0 with 10Mbps
token  rate  and 8KB bucket size, # tbrconfig en0 10M 8K To rate‐
limit the interface en0 up to 3Mbps, # tbrconfig en0 3M auto  The
command  first  appeared  in WIDE/KAME IPv6 protocol stack kit as
part of ALTQ tools.
