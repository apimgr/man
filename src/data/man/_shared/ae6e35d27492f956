The  daemon monitors the Advanced Power Management pseudo‐device,
acting on signaled events and upon user requests as sent  by  the
utility.   The  daemon  is only installed on supported platforms.
Modern systems supporting should rely on and  the  framework  in‐
stead.   For  suspend and standby request events delivered by the
BIOS, or via runs the appropriate configuration program  (if  one
exists),  syncs  the  buffer  cache to disk and initiates the re‐
quested mode.  When resuming after suspend or standby,  runs  the
appropriate  configuration  utility  (if  one exists).  For power
status change events, fetches the current status and  reports  it
via  with  logging  facility  announces the transition to standby
mode with a single high tone on the speaker (using  the  device).
Suspends  are  announced with two high tones.  periodically polls
the driver for the current power state.  If  the  battery  charge
level changes substantially or the external power status changes,
the  new status is logged.  The polling rate defaults to once per
10 minutes, but this may be altered  by  using  the  command‐line
flag.  supports the following options: Any BIOS‐initiated suspend
or  standby  requests  are  ignored if the system is connected to
line current and not running from batteries  (user  requests  are
still  honored).   Enter  debug mode, log to facility and stay in
the foreground on the controlling terminal.  Specify an alternate
device file name.  A low‐battery event causes a  suspend  request
to  occur.  Use instead of for the mode of Use instead of for the
owner/group of Do not announce suspend and  standby  requests  on
the speaker.  Specify an alternate socket name (used by to commu‐
nicate  with  The current battery statistics are reported via and
exit without monitoring the APM status.  Change the polling  rate
from  600  seconds to seconds.  Periodically log the power status
via When a client requests a suspend or stand‐by mode,  does  not
wait  for  positive confirmation that the requested mode has been
entered before replying to the client; to do so  would  mean  the
client  does  not  get  a reply until the system resumes from its
sleep state.  Rather, replies with  the  intended  state  to  the
client  and  then  places  the system in the requested mode after
running the configuration script and flushing the  buffer  cache.
Actions  can  be configured for the five transitions: or The sus‐
pend and standby actions are run prior to  performing  any  other
actions  (such as disk syncs) and entering the new mode.  The re‐
sume program is run after resuming from a stand‐by  or  suspended
state.   The  line  and  battery  actions are run after switching
power sources to AC (line) or battery, respectively.  The  appro‐
priate  line  or  battery  action is also run upon the startup of
apmd based on the current power source.  Contain the host’s  cus‐
tomized actions.  Each file must be an executable binary or shell
script  suitable  for  execution by the function.  If you wish to
have the same program or script control all transitions,  it  may
determine  which transition is in progress by examining its which
is set to one of or See for such an example script.  The  default
UNIX‐domain socket used for communication with The socket is pro‐
tected by default to mode 0660, UID 0, GID 0.  The default device
used to control the APM kernel driver.  The daemon appeared in
