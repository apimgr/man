PMC_CAPABILITIES(3)      BSD Library Functions Manual      PMC_CAPABILITIES(3)

NNAAMMEE
     ppmmcc__ccaappaabbiilliittiieess, ppmmcc__ccppuuiinnffoo, ppmmcc__nnccppuu, ppmmcc__nnppmmcc, ppmmcc__ppmmcciinnffoo, ppmmcc__wwiiddtthh
     — retrieve information about performance monitoring counters

LLIIBBRRAARRYY
     Performance Counters Library (libpmc, -lpmc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ppmmcc..hh>>

     _i_n_t
     ppmmcc__ccaappaabbiilliittiieess(_p_m_c___i_d___t _p_m_c, _u_i_n_t_3_2___t _*_c_a_p_s);

     _i_n_t
     ppmmcc__ccppuuiinnffoo(_c_o_n_s_t _s_t_r_u_c_t _p_m_c___c_p_u_i_n_f_o _*_*_c_p_u___i_n_f_o);

     _i_n_t
     ppmmcc__nnccppuu(_v_o_i_d);

     _i_n_t
     ppmmcc__nnppmmcc(_i_n_t _c_p_u);

     _i_n_t
     ppmmcc__ppmmcciinnffoo(_i_n_t _c_p_u, _s_t_r_u_c_t _p_m_c___p_m_c_i_n_f_o _*_*_p_m_c___i_n_f_o);

     _i_n_t
     ppmmcc__wwiiddtthh(_p_m_c___i_d___t _p_m_c, _u_i_n_t_3_2___t _*_w_i_d_t_h);

DDEESSCCRRIIPPTTIIOONN
     These functions retrieve information about performance monitoring hard‐
     ware.

     Function ppmmcc__ccaappaabbiilliittiieess() retrieves the hardware capabilities of a PMC.
     Argument _p_m_c is a PMC handle obtained by a prior call to ppmmcc__aallllooccaattee().
     The function sets argument _c_a_p_s to a bit mask of capabilities supported
     by the PMC denoted by argument _p_m_c.  PMC capabilities are described in
     pmc(3).

     Function ppmmcc__ccppuuiinnffoo() retrieves information about the CPUs in the sys‐
     tem.  Argument _c_p_u___i_n_f_o will be set to point to an internal structure
     with information about the system's CPUs.  The caller should not free
     this pointer value.  This structure has the following fields:
           pm_cputype  Specifies the CPU type.
           pm_ncpu     Specifies the number of CPUs in the system.
           pm_npmc     Specifies the number of PMC rows per CPU.
           pm_nclass   Specifies the number of distinct classes of PMCs in the
                       system.
           pm_classes  Contains an array of _s_t_r_u_c_t _p_m_c___c_l_a_s_s_i_n_f_o descriptors
                       describing the properties of each class of PMCs in the
                       system.

     Function ppmmcc__nnccppuu() is a convenience function that returns the maximum
     CPU number in the system.  On systems that support sparsely numbered
     CPUs, not all CPUs may be physically present.  Applications need to be
     prepared to deal with nonexistent CPUs.

     Function ppmmcc__nnppmmcc() is a convenience function that returns the number of
     PMCs available in the CPU specified by argument _c_p_u.

     Function ppmmcc__ppmmcciinnffoo() returns information about the current state of the
     PMC hardware in the CPU specified by argument _c_p_u.  The location speci‐
     fied by argument _p_m_c___i_n_f_o is set to point an array of _s_t_r_u_c_t _p_m_c___i_n_f_o
     structures each describing the state of one PMC in the CPU.  These struc‐
     ture contain the following fields:
           pm_name      A human readable name for the PMC.
           pm_class     The PMC class for the PMC.
           pm_enabled   Non-zero if the PMC is enabled.
           pm_rowdisp   The disposition of the PMC row for this PMC.  Row dis‐
                        positions are documented in hwpmc(4).
           pm_ownerpid  If the hardware is in use, the process id of the owner
                        of the PMC.
           pm_mode      The PMC mode as described in pmc(3).
           pm_event     If the hardware is in use, the PMC event being mea‐
                        sured.
           pm_flags     If the hardware is in use, the flags associated with
                        the PMC.
           pm_reloadcount
                        For sampling PMCs, the reload count associated with
                        the PMC.

     Function ppmmcc__wwiiddtthh() is used to retrieve the width in bits of the hard‐
     ware counters associated with a PMC.  Argument _p_m_c is a PMC handle ob‐
     tained by a prior call to ppmmcc__aallllooccaattee().  The function sets the location
     pointed to by argument _w_i_d_t_h to the width of the physical counters asso‐
     ciated with PMC _p_m_c.

RREETTUURRNN VVAALLUUEESS
     Functions ppmmcc__nnccppuu() and ppmmcc__nnppmmcc() returns a positive integer if suc‐
     cessful; otherwise the value -1 is returned and the global variable _e_r_r_n_o
     is set to indicate the error.

     Functions ppmmcc__ccaappaabbiilliittiieess(), ppmmcc__ccppuuiinnffoo(), ppmmcc__ppmmcciinnffoo() and
     ppmmcc__wwiiddtthh() return 0 if successful; otherwise the value -1 is returned
     and the global variable _e_r_r_n_o is set to indicate the error.

EERRRROORRSS
     A call to function ppmmcc__ccaappaabbiilliittiieess() may fail with the following errors:

     [EINVAL]           The argument to the function was invalid.

     Calls to functions ppmmcc__ccppuuiinnffoo(), ppmmcc__nnccppuu() and ppmmcc__nnppmmcc() may fail with
     the following errors:

     [ENXIO]            A prior call to ppmmcc__iinniitt() to initialize the PMC li‐
                        brary had failed.

     A call to function ppmmcc__ppmmcciinnffoo() may fail with the following errors:

     [EINVAL]           The argument _c_p_u was invalid.

     [ENXIO]            The argument _c_p_u specified a disabled or absent CPU.

     A call to function ppmmcc__wwiiddtthh() may fail with the following errors:

     [EINVAL]           The argument to the function was invalid.

SSEEEE AALLSSOO
     pmc(3), pmc_allocate(3), pmc_get_driver_stats(3),
     pmc_name_of_capability(3), pmc_name_of_class(3), pmc_name_of_cputype(3),
     pmc_name_of_event(3), pmc_name_of_mode(3), hwpmc(4)

BSD                           September 22, 2008                           BSD
