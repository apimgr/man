The  command  calls  the system call (or an external unmount pro‐
gram) to remove a or the remote node (rhost:path) from  the  file
system  tree  at the point If either or are not provided, the ap‐
propriate information is taken from the file.   By  default,  the
file  system type is extracted from the kernel and used to choose
an external unmount program, whose name is formed by appending an
underscore and the type string to This matches the form  used  by
the  external  mount programs used by If this program is found on
the path, it is used in preference  to  calling  directly.   Note
that  in the base system no such external unmount programs exist;
the facility is provided in the hopes that it will be useful  for
third party file systems and/or for research.  The options are as
follows:  All  the currently mounted file systems except the root
are unmounted.  The file system is  forcibly  unmounted.   Active
special  devices continue to work, but all other files return er‐
rors if further accesses are attempted.   The  root  file  system
cannot  be  forcibly  unmounted.   Fake  the unmount; perform all
other processing but do not actually attempt the unmount.   (This
is  most  useful in conjunction with to see what would attempt to
do).  Take the argument as a path to be passed  directly  to  by‐
passing  all  attempts to be smart about mechanically determining
the correct path from the argument.  This option is  incompatible
with any option that potentially unmounts more than one file sys‐
tem,  such as but it can be used with and/or This is the only way
to unmount something that does not appear as a directory (such as
a nullfs mount of a plain file); there are probably  other  cases
where  it is necessary.  This option also disables the use of any
external unmount program.  Only file  systems  mounted  from  the
specified host will be unmounted.  This option implies the option
and,  unless  otherwise  specified with the option, will only un‐
mount NFS file systems.  Is used to indicate the  actions  should
only  be  taken on file systems of the specified type.  More than
one type may be specified in a comma separated list.  The list of
file system types can be prefixed with to specify the file system
types for which action should be taken.  For  example,  the  com‐
mand:  umount ‐a ‐t nfs,mfs unmounts all file systems of the type
and whereas the command: umount ‐a ‐t nonfs,mfs unmounts all file
systems except those of type and Verbose, additional  information
is printed out as each file system is unmounted.  file system ta‐
ble A command appeared in
