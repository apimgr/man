RTIME(3)                 BSD Library Functions Manual                 RTIME(3)

NNAAMMEE
     rrttiimmee — get remote time

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ttyyppeess..hh>>
     ##iinncclluuddee <<ssyyss//ttiimmee..hh>>
     ##iinncclluuddee <<nneettiinneett//iinn..hh>>

     _i_n_t
     rrttiimmee(_s_t_r_u_c_t _s_o_c_k_a_d_d_r___i_n _*_a_d_d_r_p, _s_t_r_u_c_t _t_i_m_e_v_a_l _*_t_i_m_e_p,
         _s_t_r_u_c_t _t_i_m_e_v_a_l _*_t_i_m_e_o_u_t);

DDEESSCCRRIIPPTTIIOONN
     The rrttiimmee() function consults the Internet Time Server at the address
     pointed to by _a_d_d_r_p and returns the remote time in the _t_i_m_e_v_a_l struct
     pointed to by _t_i_m_e_p.  Normally, the UDP protocol is used when consulting
     the Time Server.  The _t_i_m_e_o_u_t argument specifies how long the routine
     should wait before giving up when waiting for a reply.  If _t_i_m_e_o_u_t is
     specified as NULL, however, the routine will instead use TCP and block
     until a reply is received from the time server.

RREETTUURRNN VVAALLUUEESS
     The rrttiimmee() function returns the value 0 if successful; otherwise the
     value -1 is returned and the global variable _e_r_r_n_o is set to indicate the
     error.

BSD                              May 13, 2021                              BSD
