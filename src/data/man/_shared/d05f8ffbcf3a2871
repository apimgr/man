The  POSIX  semaphore code is included by default in all kernels.
The facility provides system calls used by the standard C library
to implement semaphores.  The facility appeared as a  kernel  op‐
tion in and became non‐optional in
