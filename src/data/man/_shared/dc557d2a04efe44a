The  utility is used to print out increasing, decreasing, random,
or redundant data (usually numbers) one per line.  The default is
to produce sequential data.  The following options are available:
Just print repetitively.  This is  an  abbreviation  for  Do  not
print  the  final newline normally appended to the output.  Print
only as many digits or characters of the data as indicated by the
integer In the absence of the precision is  the  greater  of  the
precisions of and The option is overridden by whatever appears in
a  conversion  following  Generate random data.  Print data sepa‐
rated by Normally, newlines separate data.  Print with the gener‐
ated data  appended  to  it.   Octal,  hexadecimal,  exponential,
ASCII, zero padded, and right‐adjusted representations are possi‐
ble  by  using the appropriate conversion specification inside in
which case the data are inserted rather than appended.  The  last
four  arguments  indicate,  respectively, the number of data, the
lower bound, the upper bound, and the step size  or,  for  random
data, the seed.  Any argument may be omitted, and will be consid‐
ered  as such if given as Any three of these arguments determines
the fourth.  If four are specified and  the  given  and  computed
values of conflict, the lower value is used.  If fewer than three
are  specified,  defaults  are assigned left to right, except for
which assumes its default unless both and are  given.   When  se‐
quential  data  are requested the defaults for the four arguments
are 100 data, a lower bound of 1, an upper bound  of  100  and  a
step  size  of  1.   When random data are requested defaults to a
seed depending upon the time of day.  is expected to  be  an  un‐
signed  integer,  and  if  given as zero is taken to be infinite.
and may be given as real numbers or  as  characters  representing
the  corresponding  value  in ASCII.  The last argument must be a
real number.  Random numbers are obtained through  The  name  de‐
rives  in  part  from a function in APL.  The command: prints the
integers from 42 to 87, inclusive.  The command: prints 21 evenly
spaced numbers increasing from -1 to 1.  The command: prints  the
ASCII character set.  The command: prints the strings through The
command:  prints  20  random  8‐letter  strings.  The command: is
equivalent to The command: prints  thirty  substitution  commands
applying  to  lines 2, 7, 12, etc.  The command: prints the stut‐
tering sequence 9, 8, 8, 7, etc.  The  command:  creates  a  file
containing  exactly  1024  bytes.   The  command:  sets tabs four
spaces apart starting from column 10 and ending  in  column  132.
The command: prints all lines 80 characters or longer.  The util‐
ity first appeared in
