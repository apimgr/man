USERMGMT.CONF(5)            BSD File Formats Manual           USERMGMT.CONF(5)

NNAAMMEE
     uusseerrmmggmmtt..ccoonnff — user management tools configuration file

DDEESSCCRRIIPPTTIIOONN
     The uusseerrmmggmmtt..ccoonnff file defines the default values used by the user man‐
     agement tools, user(8).

     Options in this file can be set by manually editing _/_e_t_c_/_u_s_e_r_m_g_m_t_._c_o_n_f or
     using the --DD option to useradd(8).

     bbaassee__ddiirr   Sets the base directory name, in which new users' home direc‐
                tories are created when using the --mm option to useradd(8).

     ccllaassss      Sets the default login class for new users.  See login.conf(5)
                for more information on user login classes.

     eexxppiirree     Sets the default time at which the new accounts expire.  Both
                the _e_x_p_i_r_e and _i_n_a_c_t_i_v_e fields should be entered in the form
                “month day year”, where month is the month name (the first
                three characters are sufficient), day is the day of the month,
                and year is the year.  Time in seconds since the Epoch (UTC)
                is also valid.  A value of 0 can be used to disable this fea‐
                ture.

     ggrroouupp      Sets the default primary group for new users.  If this is the
                special string ==uuiidd, a group is created with the same numeric
                ID as the UID; if such a group already exists a warning is
                given and no group is created.  It has the format:

                      ggrroouupp _g_i_d | _n_a_m_e | ==uuiidd

     iinnaaccttiivvee   Sets the default time at which the passwords of new accounts
                expire.  A value of 0 can be used to disable this feature.
                Also see the _e_x_p_i_r_e field.

     ppaasssswwoorrdd   Specifies a default password encrypted with encrypt(1).

     pprreesseerrvvee   If this value is one of ‘true’, ‘yes’, or a non-zero number,
                then the user login information will be preserved when remov‐
                ing a user with userdel(8).

     rraannggee      Specifies the UID boundaries for new users.  If unspecified,
                the default is “1000..60000”.  It has the format:

                      rraannggee _s_t_a_r_t_i_n_g_-_u_i_d.._e_n_d_i_n_g_-_u_i_d

     sshheellll      Sets the default login shell for new users.

     sskkeell__ddiirr   Sets the default skeleton directory in which to find files
                with which to populate the new user's home directory.

FFIILLEESS
     /etc/usermgmt.conf
     /etc/skel/*
     /etc/login.conf

SSEEEE AALLSSOO
     login.conf(5), passwd(5), user(8), useradd(8), userdel(8), usermod(8)

SSTTAANNDDAARRDDSS
     Other implementations of the user(8) utilities use the _i_n_a_c_t_i_v_e_-_t_i_m_e pa‐
     rameter to refer to the maximum number of days allowed between logins
     (this is used to lock "stale" accounts that have not been used for a pe‐
     riod of time).  However, on OpenBSD systems this parameter refers instead
     to the password change time.  This is due to differences in the passwd(5)
     database compared to other operating systems.

HHIISSTTOORRYY
     The uusseerrmmggmmtt..ccoonnff configuration file first appeared in OpenBSD 2.7.

BSD                            November 30, 2016                           BSD
