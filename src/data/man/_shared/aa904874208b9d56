The and drivers provide support for the 5380 Bus Controller (SBC)
chip  found  on various Sun Microsystems motherboards (obio), and
on the board used in systems with bus.  The and version  of  this
driver  can be configured with a directive in the file.  The val‐
ues are bits in a bitfield, and are interpreted as  follows:  Set
bit  (1<<target) to disable disconnect/reselect Set bit (1<<(tar‐
get+8)) to disable parity checking Set this bit to disable inter‐
rupts (poll) Set this bit to disable entirely (use PIO) For exam‐
ple: would disable interrupts,  and  disable  disconnect/reselect
for  targets 0‐3.  The is the ID number of a particular device on
a particular bus.  The version of this driver can also be config‐
ured with a directive in the file.  The values are bits in a bit‐
field, and are interpreted as follows: Use Use completion  inter‐
rupts  Allow disconnect/reselect For example: would enable inter‐
rupts, and reselect.  By  default,  is  enabled  in  the  driver.
David  Jones,  The variant has a bit to enable or disable the en‐
gine, but that bit also gates the interrupt line from the  There‐
fore,  in order to get any interrupt from the (i.e. for reselect)
one must clear the engine transfer count and then enable This has
the further complication that you CAN NOT touch the while the en‐
able bit is set, so we have to turn back off before we even  look
at the Support for the Sun 4/100 is not complete.  works, but in‐
terrupts  (and,  thus,  reselection)  don’t  for reasons unknown.
Further progress has halted pending the availability of a machine
for testing.  completion interrupts, and reselection work fine on
a Sun 4/260 with modern disks attached.  There have been  reports
of reselection failing on Shoebox‐type configurations where there
are  multiple  non‐SCSI devices behind or bridges.  These devices
pre‐date the spec, and might not behave the way the NCR5380  code
expects.  For this reason, only is enabled by default in the dri‐
ver.
