_t_s_e_a_r_c_h(3)                 Library Functions Manual                 _t_s_e_a_r_c_h(3)

NNAAMMEE
       tsearch,  tfind,  tdelete,  twalk,  twalk_r, tdestroy - manage a binary
       search tree

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<sseeaarrcchh..hh>>

       ttyyppeeddeeff eennuumm {{ pprreeoorrddeerr,, ppoossttoorrddeerr,, eennddoorrddeerr,, lleeaaff }} VVIISSIITT;;

       vvooiidd **ttffiinndd((ccoonnsstt vvooiidd **_k_e_y,, vvooiidd **ccoonnsstt **_r_o_o_t_p,,
                   ttyyppeeooff((iinntt ((ccoonnsstt vvooiidd **,, ccoonnsstt vvooiidd **)))) **_c_o_m_p_a_r));;
       vvooiidd **ttsseeaarrcchh((ccoonnsstt vvooiidd **_k_e_y,, vvooiidd ****_r_o_o_t_p,,
                   ttyyppeeooff((iinntt ((ccoonnsstt vvooiidd **,, ccoonnsstt vvooiidd **)))) **_c_o_m_p_a_r));;
       vvooiidd **ttddeelleettee((ccoonnsstt vvooiidd **rreessttrriicctt _k_e_y,, vvooiidd ****rreessttrriicctt _r_o_o_t_p,,
                   ttyyppeeooff((iinntt ((ccoonnsstt vvooiidd **,, ccoonnsstt vvooiidd **)))) **_c_o_m_p_a_r));;
       vvooiidd ttwwaallkk((ccoonnsstt vvooiidd **_r_o_o_t,,
                   ttyyppeeooff((vvooiidd ((ccoonnsstt vvooiidd **_n_o_d_e_p,, VVIISSIITT _w_h_i_c_h,, iinntt _d_e_p_t_h))))
                       **_a_c_t_i_o_n));;

       ##ddeeffiinnee __GGNNUU__SSOOUURRCCEE         /* See feature_test_macros(7) */
       ##iinncclluuddee <<sseeaarrcchh..hh>>

       vvooiidd ttwwaallkk__rr((ccoonnsstt vvooiidd **_r_o_o_t,,
                   ttyyppeeooff((vvooiidd ((ccoonnsstt vvooiidd **_n_o_d_e_p,, VVIISSIITT _w_h_i_c_h,, vvooiidd **_c_l_o_s_u_r_e))))
                       **_a_c_t_i_o_n,,
                   vvooiidd **_c_l_o_s_u_r_e));;
       vvooiidd ttddeessttrrooyy((vvooiidd **_r_o_o_t,,
                   ttyyppeeooff((vvooiidd ((vvooiidd **_n_o_d_e_p)))) **_f_r_e_e___n_o_d_e));;

DDEESSCCRRIIPPTTIIOONN
       ttsseeaarrcchh(), ttffiinndd(), ttwwaallkk(), and ttddeelleettee() manage a binary search tree.
       They are generalized from Knuth (6.2.2) Algorithm T.  The  first  field
       in  each  node of the tree is a pointer to the corresponding data item.
       (The calling program must store the actual data.)  _c_o_m_p_a_r points  to  a
       comparison  routine,  which takes pointers to two items.  It should re‐
       turn an integer which is negative,  zero,  or  positive,  depending  on
       whether the first item is less than, equal to, or greater than the sec‐
       ond.

       ttsseeaarrcchh()  searches the tree for an item.  _k_e_y points to the item to be
       searched for.  _r_o_o_t_p points to a variable which points to the  root  of
       the tree.  If the tree is empty, then the variable that _r_o_o_t_p points to
       should  be  set  to  NULL.   If  the  item  is  found in the tree, then
       ttsseeaarrcchh() returns a pointer to the corresponding tree node.  (In  other
       words,  ttsseeaarrcchh() returns a pointer to a pointer to the data item.)  If
       the item is not found, then ttsseeaarrcchh() adds it, and returns a pointer to
       the corresponding tree node.

       ttffiinndd() is like ttsseeaarrcchh(), except that if the item is not  found,  then
       ttffiinndd() returns NULL.

       ttddeelleettee() deletes an item from the tree.  Its arguments are the same as
       for ttsseeaarrcchh().

       ttwwaallkk() performs depth‐first, left‐to‐right traversal of a binary tree.
       _r_o_o_t  points  to  the starting node for the traversal.  If that node is
       not the root, then only part of the  tree  will  be  visited.   ttwwaallkk()
       calls  the  user  function _a_c_t_i_o_n each time a node is visited (that is,
       three times for an internal node, and once for  a  leaf).   _a_c_t_i_o_n,  in
       turn,  takes  three  arguments.  The first argument is a pointer to the
       node being visited.  The structure of the node is unspecified,  but  it
       is  possible  to cast the pointer to a pointer‐to‐pointer‐to‐element in
       order to access the element stored within the  node.   The  application
       must  not modify the structure pointed to by this argument.  The second
       argument is an integer which takes one of  the  values  pprreeoorrddeerr,  ppooss‐‐
       ttoorrddeerr,  or eennddoorrddeerr depending on whether this is the first, second, or
       third visit to the internal node, or the value lleeaaff if this is the sin‐
       gle visit to a leaf node.  (These symbols are defined  in  _<_s_e_a_r_c_h_._h_>.)
       The  third  argument  is the depth of the node; the root node has depth
       zero.

       (More commonly, pprreeoorrddeerr, ppoossttoorrddeerr, and eennddoorrddeerr  are  known  as  pprree‐‐
       oorrddeerr,  iinnoorrddeerr, and ppoossttoorrddeerr: before visiting the children, after the
       first and before the second, and after visiting  the  children.   Thus,
       the choice of name ppoossttoorrddeerr is rather confusing.)

       ttwwaallkk__rr() is similar to ttwwaallkk(), but instead of the _d_e_p_t_h argument, the
       _c_l_o_s_u_r_e  argument  pointer  is  passed to each invocation of the action
       callback, unchanged.  This pointer can be used to pass  information  to
       and  from  the  callback function in a thread‐safe fashion, without re‐
       sorting to global variables.

       ttddeessttrrooyy() removes the whole tree pointed to by _r_o_o_t, freeing  all  re‐
       sources allocated by the ttsseeaarrcchh() function.  For the data in each tree
       node  the  function  _f_r_e_e___n_o_d_e  is  called.  The pointer to the data is
       passed as the argument to the function.  If no such work is  necessary,
       _f_r_e_e___n_o_d_e must point to a function doing nothing.

RREETTUURRNN VVAALLUUEE
       ttsseeaarrcchh()  returns  a pointer to a matching node in the tree, or to the
       newly added node, or NULL if there was insufficient memory to  add  the
       item.   ttffiinndd()  returns  a pointer to the node, or NULL if no match is
       found.  If there are multiple items that match the key, the item  whose
       node is returned is unspecified.

       ttddeelleettee()  returns a pointer to the parent of the node deleted, or NULL
       if the item was not found.  If the deleted  node  was  the  root  node,
       ttddeelleettee() returns a dangling pointer that must not be accessed.

       ttsseeaarrcchh(), ttffiinndd(), and ttddeelleettee() also return NULL if _r_o_o_t_p was NULL on
       entry.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       ttsseeaarrcchh(), ttffiinndd(), ttddeelleettee() T}   Thread safety  MT‐Safe race:rootp T{
       ttwwaallkk() T}   Thread safety  MT‐Safe race:root T{ ttwwaallkk__rr() T}   Thread
       safety  MT‐Safe race:root T{ ttddeessttrrooyy() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       ttsseeaarrcchh()
       ttffiinndd()
       ttddeelleettee()
       ttwwaallkk()
              POSIX.1‐2008.

       ttddeessttrrooyy()
       ttwwaallkk__rr()
              GNU.

HHIISSTTOORRYY
       ttsseeaarrcchh()
       ttffiinndd()
       ttddeelleettee()
       ttwwaallkk()
              POSIX.1‐2001, POSIX.1‐2008, SVr4.

       ttwwaallkk__rr()
              glibc 2.30.

NNOOTTEESS
       ttwwaallkk() takes a pointer to the root, while the other functions  take  a
       pointer to a variable which points to the root.

       ttddeelleettee() frees the memory required for the node in the tree.  The user
       is responsible for freeing the memory for the corresponding data.

       The  example  program depends on the fact that ttwwaallkk() makes no further
       reference to a node after  calling  the  user  function  with  argument
       "endorder"  or "leaf".  This works with the GNU library implementation,
       but is not in the System V documentation.

EEXXAAMMPPLLEESS
       The following program inserts twelve random numbers into a binary tree,
       where duplicate numbers are  collapsed,  then  prints  the  numbers  in
       order.

       #define _GNU_SOURCE     /* Expose declaration of tdestroy() */
       #include <search.h>
       #include <stddef.h>
       #include <stdio.h>
       #include <stdlib.h>
       #include <time.h>

       static void *root = NULL;

       static void *
       xmalloc(size_t n)
       {
           void *p;

           p = malloc(n);
           if (p)
               return p;
           fprintf(stderr, "insufficient memory\n");
           exit(EXIT_FAILURE);
       }

       static int
       compare(const void *pa, const void *pb)
       {
           if (*(int *) pa < *(int *) pb)
               return -1;
           if (*(int *) pa > *(int *) pb)
               return 1;
           return 0;
       }

       static void
       action(const void *nodep, VISIT which, int depth)
       {
           int *datap;

           switch (which) {
           case preorder:
               break;
           case postorder:
               datap = *(int **) nodep;
               printf("%6d\n", *datap);
               break;
           case endorder:
               break;
           case leaf:
               datap = *(int **) nodep;
               printf("%6d\n", *datap);
               break;
           }
       }

       int
       main(void)
       {
           int  *ptr;
           int  **val;

           srand(time(NULL));
           for (unsigned int i = 0; i < 12; i++) {
               ptr = xmalloc(sizeof(*ptr));
               *ptr = rand() & 0xff;
               val = tsearch(ptr, &root, compare);
               if (val == NULL)
                   exit(EXIT_FAILURE);
               if (*val != ptr)
                   free(ptr);
           }
           twalk(root, action);
           tdestroy(root, free);
           exit(EXIT_SUCCESS);
       }

SSEEEE AALLSSOO
       bbsseeaarrcchh(3), hhsseeaarrcchh(3), llsseeaarrcchh(3), qqssoorrtt(3)

Linux man‐pages (unreleased)      2025‐05‐17                        _t_s_e_a_r_c_h(3)
