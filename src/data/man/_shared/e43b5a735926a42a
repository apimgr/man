curs_scroll(3X)                  Library calls                 curs_scroll(3X)



NNAAMMEE
       ssccrroollll, ssccrrll, wwssccrrll - scroll a _c_u_r_s_e_s window

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ccuurrsseess..hh>>

       iinntt ssccrroollll((WWIINNDDOOWW **_w_i_n));;

       iinntt ssccrrll((iinntt _n));;
       iinntt wwssccrrll((WWIINNDDOOWW **_w_i_n,, iinntt _n));;

DDEESSCCRRIIPPTTIIOONN
       ssccrroollll  scrolls  the  given window up one line.  That is, every visible
       line we might number _i becomes line _i-1.  The text of the top  line  in
       the window disappears and the bottom line is populated with blank char‐
       acters; see  bbkkggdd(3X)  or  bbkkggrrnndd(3X).   As  an  optimization,  if  the
       scrolling  region  of  the  window  is  the entire screen, the physical
       screen may be scrolled at the same time; see ccuurrssccrr(3X).

       ssccrrll and wwssccrrll scroll ssttddssccrr or the specified window up or down depend‐
       ing on the sign of _n.

       •   For positive _n, line _i+_n becomes _i (scrolling up);

       •   for negative _n, line _i-_n becomes _i (scrolling down).

       The  cursor does not move.  These functions perform no operation unless
       scrolling is enabled for the window via ssccrroollllookk(3X).

RREETTUURRNN VVAALLUUEE
       These functions return EERRRR upon failure and OOKK upon success.

       _n_c_u_r_s_e_s returns EERRRR if scrolling is not enabled in the window, for  ex‐
       ample with ssccrroollllookk(3X), or if the _W_I_N_D_O_W pointer is null.

NNOOTTEESS
       Unusually,  there  is  no wwssccrroollll function; ssccrroollll behaves as one would
       expect wwssccrroollll to, accepting a _W_I_N_D_O_W pointer argument.

       ssccrrll and ssccrroollll may be implemented as macros.

PPOORRTTAABBIILLIITTYY
       X/Open Curses, Issue 4 describes these functions.  It defines no  error
       conditions.

       SVr4  specifies  only “an integer value other than EERRRR” as a successful
       return value.

       SVr4 indicates that the optimization of  physically  scrolling  immedi‐
       ately  if  the  scroll  region is the entire screen “is” performed, not
       “may be” performed.  _n_c_u_r_s_e_s deliberately does not guarantee that  this
       will  occur,  to  leave open the possibility of smarter optimization of
       multiple scroll actions on the next update.

       Neither SVr4 _c_u_r_s_e_s nor X/Open Curses specify whether the  current  at‐
       tribute  or  current color pair of blanks generated by the scroll func‐
       tion are zeroed.  _n_c_u_r_s_e_s does so.

SSEEEE AALLSSOO
       ccuurrsseess(3X), ccuurrss__oouuttooppttss(3X)



ncurses 6.5                       2024-04-20                   curs_scroll(3X)
