The device attaches to the GPIO controller and provides a uniform
programming  interface to its pins.  Each GPIO controller with an
attached device has an associated device file  under  the  direc‐
tory,  e.g.  Access  from  userland is performed through calls on
these devices.  Whether the layout of the GPIO device can be con‐
figured is subject to authorization by the framework.  If for ex‐
ample is active, the layout of the GPIO device is  defined  at  a
securelevel  less  than 1, i.e. typically during system boot, and
cannot be changed later.  GPIO pins can be configured and given a
symbolic name and device drivers that use GPIO pins  can  be  at‐
tached  to  the  device  at a securelevel less than 1.  All other
pins will not be accessible once the runlevel  has  been  raised.
The  following structures and constants are defined in the header
file: Returns information about the GPIO controller in the struc‐
ture: struct gpio_info {         int gpio_npins;         /* total
number of pins available */ }; Returns the input pin value in the
structure: #define GPIOMAXNAME             64

struct gpio_req {         char gp_name[GPIOMAXNAME];      /*  pin
name  */         int gp_pin;                     /* pin number */
        int gp_value;                   /* value  */  };  The  or
field must be set before calling.  If both are set, gp_name takes
precedence.   On  return,  the field contains the current name of
the pin, if set by the controller driver or an  earlier  call  to
Writes  the  output value to the pin.  The value set in the field
must be either (logical 0) or (logical 1).  On return, the  field
contains  the  old pin state.  Toggles the pin output value, i.e.
changes it to the opposite.  field is ignored and on return  con‐
tains  the  old  pin state.  Changes pin configuration flags with
the new ones  provided  in  the  structure:  #define  GPIOMAXNAME
64

struct gpio_set {
        char gp_name[GPIOMAXNAME];   /* pin name */
        int gp_pin;                     /* pin number */
        int  gp_caps;                    /* pin capabilities (ro)
*/
        int  gp_flags;                    /*  pin   configuration
flags */
        char  gp_name2[GPIOMAXNAME];  /* new name */ }; The field
is a combination of the following flags: input  direction  output
direction  bi‐directional open‐drain output push‐pull output out‐
put disabled internal pull‐up enabled internal pull‐down  enabled
invert  input  invert  output pulsate output select alternate pin
function 0 to 7 Note that the GPIO controller may not support all
of these flags.  On return the field contains flags that are sup‐
ported.  If no flags are specified, the pin  configuration  stays
unchanged.   Only  GPIO pins that have been set using will be ac‐
cessible at securelevels greater than  0.   Unset  the  specified
pin, i.e. clear its name and make it inaccessible at securelevels
greater  than 0.  Attach the device described in the structure on
this gpio device.  struct gpio_attach {
        char ga_dvname[16];     /* device name */
        int ga_offset;          /* pin number */
        uint32_t ga_mask;       /* binary mask */
        uint32_t ga_flags;      /* driver  dependent  */  };  The
command can be used to detach a device from a gpio pin.  GPIO de‐
vice  unit file.  The device first appeared in and The driver was
written by was ported to by Runtime device attachment  was  added
by Event capabilities are not supported.
