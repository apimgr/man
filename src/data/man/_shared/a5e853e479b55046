BUS_DESCRIBE_INTR(9)     BSD Kernel Developer's Manual    BUS_DESCRIBE_INTR(9)

NNAAMMEE
     BBUUSS__DDEESSCCRRIIBBEE__IINNTTRR, bbuuss__ddeessccrriibbee__iinnttrr — associate a description with an
     active interrupt handler

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ppaarraamm..hh>>
     ##iinncclluuddee <<ssyyss//bbuuss..hh>>

     _i_n_t
     BBUUSS__DDEESSCCRRIIBBEE__IINNTTRR(_d_e_v_i_c_e___t _d_e_v, _d_e_v_i_c_e___t _c_h_i_l_d, _s_t_r_u_c_t _r_e_s_o_u_r_c_e _*_i_r_q,
         _v_o_i_d _*_c_o_o_k_i_e, _c_o_n_s_t _c_h_a_r _*_d_e_s_c_r);

     _i_n_t
     bbuuss__ddeessccrriibbee__iinnttrr(_d_e_v_i_c_e___t _d_e_v, _s_t_r_u_c_t _r_e_s_o_u_r_c_e _*_i_r_q, _v_o_i_d _*_c_o_o_k_i_e,
         _c_o_n_s_t _c_h_a_r _*_f_m_t, _._._.);

DDEESSCCRRIIPPTTIIOONN
     The BBUUSS__DDEESSCCRRIIBBEE__IINNTTRR() method associates a description with an active
     interrupt handler.  The _c_o_o_k_i_e parameter must be the value returned by a
     successful call to BUS_SETUP_INTR(9) for the interrupt _i_r_q.

     The bbuuss__ddeessccrriibbee__iinnttrr() function is a simple wrapper around
     BBUUSS__DDEESSCCRRIIBBEE__IINNTTRR().  As a convenience, bbuuss__ddeessccrriibbee__iinnttrr() allows the
     caller to use printf(9) style formatting to build the description string
     using _f_m_t.

     When an interrupt handler is established by BUS_SETUP_INTR(9), the han‐
     dler is named after the device the handler is established for.  This name
     is then used in various places such as interrupt statistics displayed by
     systat(1) and vmstat(8).  For devices that use a single interrupt, the
     device name is sufficiently unique to identify the interrupt handler.
     However, for devices that use multiple interrupts it can be useful to
     distinguish the interrupt handlers.  When a description is set for an ac‐
     tive interrupt handler, a colon followed by the description is appended
     to the device name to form the interrupt handler name.

RREETTUURRNN VVAALLUUEESS
     Zero is returned on success, otherwise an appropriate error is returned.

SSEEEE AALLSSOO
     systat(1), vmstat(8), BUS_SETUP_INTR(9), device(9), printf(9)

HHIISSTTOORRYY
     The BBUUSS__DDEESSCCRRIIBBEE__IINNTTRR() method and bbuuss__ddeessccrriibbee__iinnttrr() functions first
     appeared in FreeBSD 8.1.

BBUUGGSS
     It is not currently possible to remove a description from an active in‐
     terrupt handler.

BSD                            December 9, 2015                            BSD
