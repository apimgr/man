ELFTC_SET_TIMESTAMPS(3)  BSD Library Functions Manual  ELFTC_SET_TIMESTAMPS(3)

NNAAMMEE
     eellffttcc__sseett__ttiimmeessttaammppss — set file timestamps

LLIIBBRRAARRYY
     library “libelftc”

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<lliibbeellffttcc..hh>>

     _i_n_t
     eellffttcc__sseett__ttiimmeessttaammppss(_c_o_n_s_t _c_h_a_r _*_f_i_l_e_n_a_m_e, _s_t_r_u_c_t _s_t_a_t _*_s_b);

DDEESSCCRRIIPPTTIIOONN
     The eellffttcc__sseett__ttiimmeessttaammppss() function is used to set the access and modi‐
     fied time stamps on a file based on the contents of a _s_t_r_u_c_t _s_t_a_t de‐
     scriptor.

     Argument _f_i_l_e_n_a_m_e names an existing file in the file system.

     Argument _s_b points to structure of type _s_t_r_u_c_t _s_t_a_t populated by a prior
     call to fstat(2) or stat(2).

IIMMPPLLEEMMEENNTTAATTIIOONN NNOOTTEESS
     This function will invoke the high-resolution utimes(2) system call if
     the underlying operating system supports it.  On operating systems lack‐
     ing support for utimes(2), the function will use lower resolution
     utime(2) system call.

EEXXAAMMPPLLEESS
     To set the access and modified times for a new file to those of an exist‐
     ing file, use:

           struct stat sb;
           const char *existing_filename, *new_filename;

           if (stat(existing_filename, &sb) < 0)
                   err(EXIT_FAILURE, "stat failed");

           if (elftc_set_timestamps(new_filename, &sb) < 0)
                   err(EXIT_FAILURE, "timestamps could not be set");

SSEEEE AALLSSOO
     fstat(2), stat(2), utime(2), utimes(2)

BSD                            December 15, 2011                           BSD
