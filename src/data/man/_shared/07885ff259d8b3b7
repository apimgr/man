The interface provides access to 10Mb/s Ethernet networks via the
Am79C940  (MACE)  Ethernet  controller.   The is found on the Sun
QuadEthernet boards (Sun part number SUNW,501‐2062).  Each of the
host’s network addresses is specified at boot time  with  an  The
interface employs the address resolution protocol described in to
dynamically  map  between  Internet and Ethernet addresses on the
local network.  Support for the first appeared in
