The  interface  brings clock control to non‐root users.  Any user
with write access to will be able to perform operations  such  as
or  which  are  normally restricted to the super‐user.  Using the
pseudo‐device, it is possible to run daemons such as as non‐priv‐
ileged users, thus reducing the security exposure if a compromise
is found in such a daemon.  The pseudo‐device driver provides  an
call  for  each privileged clock‐related system call.  The system
call stubs in C library will use the on if the  special  file  is
present  and  accessible, or will revert to the plain super‐user‐
restricted system call if the special  file  is  not  accessible.
The following calls are defined in This will run the system call.
Argument  should be a pointer to a struct clockctl_settimeofday {
        const struct timeval *tv;          const  void  *tzp;  };
This will run the system call.  Argument should be a pointer to a
struct   clockctl_clock_settime   {          clockid_t  clock_id;
        struct timespec *tp; }; This will run  the  system  call.
Argument  should  be  a  pointer  to  a struct clockctl_adjtime {
        const struct timeval *delta;         struct timeval *old‐
delta; }; This will run the system call.  Argument  should  be  a
pointer  to  a struct clockctl_ntp_adjtime {         struct timex
*tp; }; appeared in
