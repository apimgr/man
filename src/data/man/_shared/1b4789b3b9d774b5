starts  the  with its If is still running after it is killed.  By
default, is sent.  Always exits with the same status as  even  if
it times out.  Do not propagate timeout to the children.  Specify
the signal to send on timeout.  By default, is sent.  Send a sec‐
ond  kill signal if is still running after after the first signal
was sent.  and can be integer or decimal numbers.  Values without
unit symbols are interpreted as seconds.  Supported unit  symbols
are:  seconds  minutes hours days If the timeout was not reached,
the exit status of is returned.  If the timeout was  reached  and
is  set,  the exit status of is returned.  If is not set, an exit
status of 124 is returned.  If exits after  receiving  a  signal,
the  exit status returned is the signal number plus 128.  A util‐
ity appeared in a development branch of and was imported into The
work is compatible with GNU by  from  GNU  Coreutils  8.21.   The
utility first appeared in GNU Coreutils 7.0.
