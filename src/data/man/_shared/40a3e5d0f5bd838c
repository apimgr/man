_g_e_t_s_p_n_a_m(3)                Library Functions Manual                _g_e_t_s_p_n_a_m(3)

NNAAMMEE
       getspnam,  getspnam_r,  getspent, getspent_r, setspent, endspent, fget‐
       spent, fgetspent_r, sgetspent, sgetspent_r, putspent, lckpwdf, ulckpwdf
       - get shadow password file entry

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       /* General shadow password file API */
       ##iinncclluuddee <<sshhaaddooww..hh>>

       ssttrruucctt ssppwwdd **ggeettssppnnaamm((ccoonnsstt cchhaarr **_n_a_m_e));;
       ssttrruucctt ssppwwdd **ggeettssppeenntt((vvooiidd));;

       vvooiidd sseettssppeenntt((vvooiidd));;
       vvooiidd eennddssppeenntt((vvooiidd));;

       ssttrruucctt ssppwwdd **ffggeettssppeenntt((FFIILLEE **_s_t_r_e_a_m));;
       ssttrruucctt ssppwwdd **ssggeettssppeenntt((ccoonnsstt cchhaarr **_s));;

       iinntt ppuuttssppeenntt((ccoonnsstt ssttrruucctt ssppwwdd **_p,, FFIILLEE **_s_t_r_e_a_m));;

       iinntt llcckkppwwddff((vvooiidd));;
       iinntt uullcckkppwwddff((vvooiidd));;

       /* GNU extension */
       ##iinncclluuddee <<sshhaaddooww..hh>>

       iinntt ggeettssppeenntt__rr((size_t size;
                      ssttrruucctt ssppwwdd **_s_p_b_u_f,,
                      cchhaarr _b_u_f[[_s_i_z_e]],, ssiizzee__tt _s_i_z_e,,
                      ssttrruucctt ssppwwdd ****_s_p_b_u_f_p));;
       iinntt ggeettssppnnaamm__rr((size_t size;
                      ccoonnsstt cchhaarr **_n_a_m_e,, ssttrruucctt ssppwwdd **_s_p_b_u_f,,
                      cchhaarr _b_u_f[[_s_i_z_e]],, ssiizzee__tt _s_i_z_e,,
                      ssttrruucctt ssppwwdd ****_s_p_b_u_f_p));;

       iinntt ffggeettssppeenntt__rr((size_t size;
                      FFIILLEE **_s_t_r_e_a_m,, ssttrruucctt ssppwwdd **_s_p_b_u_f,,
                      cchhaarr _b_u_f[[_s_i_z_e]],, ssiizzee__tt _s_i_z_e,,
                      ssttrruucctt ssppwwdd ****_s_p_b_u_f_p));;
       iinntt ssggeettssppeenntt__rr((size_t size;
                      ccoonnsstt cchhaarr **_s,, ssttrruucctt ssppwwdd **_s_p_b_u_f,,
                      cchhaarr _b_u_f[[_s_i_z_e]],, ssiizzee__tt _s_i_z_e,,
                      ssttrruucctt ssppwwdd ****_s_p_b_u_f_p));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       ggeettssppeenntt__rr(), ggeettssppnnaamm__rr(), ffggeettssppeenntt__rr(), ssggeettssppeenntt__rr():
           Since glibc 2.19:
               _DEFAULT_SOURCE
           glibc 2.19 and earlier:
               _BSD_SOURCE || _SVID_SOURCE

DDEESSCCRRIIPPTTIIOONN
       Long ago it was considered safe to have encrypted passwords openly vis‐
       ible in the password file.  When computers got faster  and  people  got
       more  security‐conscious,  this  was  no  longer  acceptable.  Julianne
       Frances Haugh implemented the shadow password suite that keeps the  en‐
       crypted  passwords  in  the  shadow  password database (e.g., the local
       shadow password file _/_e_t_c_/_s_h_a_d_o_w, NIS,  and  LDAP),  readable  only  by
       root.

       The  functions described below resemble those for the traditional pass‐
       word database (e.g., see ggeettppwwnnaamm(3) and ggeettppwweenntt(3)).

       The ggeettssppnnaamm() function returns a pointer to a structure containing the
       broken‐out fields of the record in the shadow  password  database  that
       matches the username _n_a_m_e.

       The  ggeettssppeenntt()  function  returns  a  pointer to the next entry in the
       shadow password database.  The position in the input stream is initial‐
       ized by sseettssppeenntt().  When done reading, the program may call eennddssppeenntt()
       so that resources can be deallocated.

       The ffggeettssppeenntt() function is similar to ggeettssppeenntt() but uses the supplied
       stream instead of the one implicitly opened by sseettssppeenntt().

       The ssggeettssppeenntt() function parses the supplied string  _s  into  a  struct
       _s_p_w_d.

       The ppuuttssppeenntt() function writes the contents of the supplied struct _s_p_w_d
       _*_p as a text line in the shadow password file format to _s_t_r_e_a_m.  String
       entries with value NULL and numerical entries with value -1 are written
       as an empty string.

       The llcckkppwwddff() function is intended to protect against multiple simulta‐
       neous  accesses of the shadow password database.  It tries to acquire a
       lock, and returns 0 on success, or -1 on  failure  (lock  not  obtained
       within  15  seconds).  The uullcckkppwwddff() function releases the lock again.
       Note that there is no protection against direct access  of  the  shadow
       password file.  Only programs that use llcckkppwwddff() will notice the lock.

       These were the functions that formed the original shadow API.  They are
       widely available.

   RReeeennttrraanntt vveerrssiioonnss
       Analogous  to  the reentrant functions for the password database, glibc
       also has reentrant functions for the  shadow  password  database.   The
       ggeettssppnnaamm__rr()  function  is  like  ggeettssppnnaamm()  but  stores the retrieved
       shadow password structure in the  space  pointed  to  by  _s_p_b_u_f.   This
       shadow  password  structure  contains  pointers  to  strings, and these
       strings are stored in the buffer _b_u_f of size _s_i_z_e.  A  pointer  to  the
       result  (in  case of success) or NULL (in case no entry was found or an
       error occurred) is stored in _*_s_p_b_u_f_p.

       The functions ggeettssppeenntt__rr(), ffggeettssppeenntt__rr(), and ssggeettssppeenntt__rr() are  simi‐
       larly analogous to their nonreentrant counterparts.

       Some non‐glibc systems also have functions with these names, often with
       different prototypes.

   SSttrruuccttuurree
       The shadow password structure is defined in _<_s_h_a_d_o_w_._h_> as follows:

           struct spwd {
               char *sp_namp;     /* Login name */
               char *sp_pwdp;     /* Encrypted password */
               long  sp_lstchg;   /* Date of last change
                                     (measured in days since
                                     1970-01-01 00:00:00 +0000 (UTC)) */
               long  sp_min;      /* Min # of days between changes */
               long  sp_max;      /* Max # of days between changes */
               long  sp_warn;     /* # of days before password expires
                                     to warn user to change it */
               long  sp_inact;    /* # of days after password expires
                                     until account is disabled */
               long  sp_expire;   /* Date when account expires
                                     (measured in days since
                                     1970-01-01 00:00:00 +0000 (UTC)) */
               unsigned long sp_flag;  /* Reserved */
           };

RREETTUURRNN VVAALLUUEE
       The  functions that return a pointer return NULL if no more entries are
       available or if an error occurs during processing.  The functions which
       have _i_n_t as the return value return 0 for success and -1  for  failure,
       with _e_r_r_n_o set to indicate the error.

       For  the  nonreentrant  functions, the return value may point to static
       area, and may be overwritten by subsequent calls to these functions.

       The reentrant functions return zero on success.  In case of  error,  an
       error number is returned.

EERRRROORRSS
       EEAACCCCEESS The  caller  does not have permission to access the shadow pass‐
              word file.

       EERRAANNGGEE Supplied buffer is too small.

FFIILLEESS
       _/_e_t_c_/_s_h_a_d_o_w
              local shadow password database file

       _/_e_t_c_/_._p_w_d_._l_o_c_k
              lock file

       The include file _<_p_a_t_h_s_._h_> defines the  constant  __PPAATTHH__SSHHAADDOOWW  to  the
       pathname of the shadow password file.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lb lb lbx l l l.  Interface Attribute Value T{
       ggeettssppnnaamm() T}   Thread safety  T{ MT‐Unsafe race:getspnam locale T} T{
       ggeettssppeenntt() T}   Thread safety  T{ MT‐Unsafe race:getspent race:spentbuf
       locale T} T{ sseettssppeenntt(), eennddssppeenntt(), ggeettssppeenntt__rr() T}   Thread
       safety  T{ MT‐Unsafe race:getspent locale T} T{ ffggeettssppeenntt() T}   Thread
       safety  T{ MT‐Unsafe race:fgetspent T} T{ ssggeettssppeenntt() T}   Thread
       safety  T{ MT‐Unsafe race:sgetspent T} T{ ppuuttssppeenntt(), ggeettssppnnaamm__rr(),
       ssggeettssppeenntt__rr() T}   Thread safety  T{ MT‐Safe locale T} T{ llcckkppwwddff(),
       uullcckkppwwddff(), ffggeettssppeenntt__rr() T}   Thread safety  T{ MT‐Safe T}

       In the above table, _g_e_t_s_p_e_n_t in _r_a_c_e_:_g_e_t_s_p_e_n_t signifies that if any of
       the functions sseettssppeenntt(), ggeettssppeenntt(), ggeettssppeenntt__rr(), or eennddssppeenntt() are
       used in parallel in different threads of a program, then data races
       could occur.

VVEERRSSIIOONNSS
       Many other systems provide a similar API.

SSTTAANNDDAARRDDSS
       None.

SSEEEE AALLSSOO
       ggeettggrrnnaamm(3), ggeettppwwnnaamm(3), ggeettppwwnnaamm__rr(3), sshhaaddooww(5)

Linux man‐pages (unreleased)      2025‐09‐07                       _g_e_t_s_p_n_a_m(3)
