RANDOM(3)                BSD Library Functions Manual                RANDOM(3)

NNAAMMEE
     rraannddoomm, ssrraannddoomm, ssrraannddoommddeevv, iinniittssttaattee, sseettssttaattee — non-cryptographic
     pseudorandom number generator; routines for changing generators

LLIIBBRRAARRYY
     Standard C Library (libc, -lc)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssttddlliibb..hh>>

     _l_o_n_g
     rraannddoomm(_v_o_i_d);

     _v_o_i_d
     ssrraannddoomm(_u_n_s_i_g_n_e_d _i_n_t _s_e_e_d);

     _v_o_i_d
     ssrraannddoommddeevv(_v_o_i_d);

     _c_h_a_r _*
     iinniittssttaattee(_u_n_s_i_g_n_e_d _i_n_t _s_e_e_d, _c_h_a_r _*_s_t_a_t_e, _s_i_z_e___t _n);

     _c_h_a_r _*
     sseettssttaattee(_c_h_a_r _*_s_t_a_t_e);

DDEESSCCRRIIPPTTIIOONN
     TThhee ffuunnccttiioonnss ddeessccrriibbeedd iinn tthhiiss mmaannuuaall ppaaggee aarree nnoott sseeccuurree..  AApppplliiccaattiioonnss
     wwhhiicchh rreeqquuiirree uunnpprreeddiiccttaabbllee rraannddoomm nnuummbbeerrss sshhoouulldd uussee arc4random(33) iinn‐‐
     sstteeaadd..

     Unless initialized with less than 32 bytes of state, the rraannddoomm() func‐
     tion uses a non-linear additive feedback random number generator employ‐
     ing a default table of size 31 long integers to return successive pseudo-
     random numbers in the range from 0 to (2**31)−1.  The period of this ran‐
     dom number generator is very large, approximately 16*((2**31)−1).

     If initialized with less than 32 bytes of state, rraannddoomm() uses the poor-
     quality 32-bit Park-Miller LCG.

     The rraannddoomm() and ssrraannddoomm() functions are analagous to rand(3) and
     srand(3).

     Like rand(3), rraannddoomm() is implicitly initialized as if ssrraannddoomm(_1) had
     been invoked explicitly.

     The ssrraannddoommddeevv() routine initializes the state array using random numbers
     obtained from the kernel.  This can generate states which are impossible
     to reproduce by calling ssrraannddoomm(), because the succeeding terms in the
     state buffer are no longer derived from the Park-Miller LCG algorithm ap‐
     plied to a fixed seed.

     The iinniittssttaattee() routine initializes the provided state array of _u_i_n_t_3_2___t
     values and uses it in future rraannddoomm() invocations.  (Despite the _c_h_a_r _*
     type of _s_t_a_t_e, the underlying object must be a naturally aligned array of
     32-bit values.)  The size of the state array (in bytes) is used by
     iinniittssttaattee() to decide how sophisticated a random number generator it
     should use — the more state, the better the random numbers will be.
     (Current "optimal" values for the amount of state information are 8, 32,
     64, 128, and 256 bytes; other amounts will be rounded down to the nearest
     known amount.  Using less than 8 bytes will cause an error.)  The _s_e_e_d is
     used as in ssrraannddoomm().  The iinniittssttaattee() function returns a pointer to the
     previous state information array.

     The sseettssttaattee() routine switches rraannddoomm() to using the provided state.  It
     returns a pointer to the previous state.

     Once a state array has been initialized, it may be restarted at a differ‐
     ent point either by calling iinniittssttaattee() (with the desired seed, the state
     array, and its size) or by calling both sseettssttaattee() (with the state array)
     and ssrraannddoomm() (with the desired seed).  The advantage of calling both
     sseettssttaattee() and ssrraannddoomm() is that the size of the state array does not
     have to be remembered after it is initialized.

     With 256 bytes of state information, the period of the random number gen‐
     erator is greater than 2**69 which should be sufficient for most pur‐
     poses.

DDIIAAGGNNOOSSTTIICCSS
     If iinniittssttaattee() is called with less than 8 bytes of state information, or
     if sseettssttaattee() detects that the state information has been garbled, NULL
     is returned.

SSEEEE AALLSSOO
     arc4random(3), lrand48(3), rand(3), random(4)

HHIISSTTOORRYY
     These functions appeared in 4.2BSD.

AAUUTTHHOORRSS
     Earl T. Cohen

BSD                            February 1, 2020                            BSD
