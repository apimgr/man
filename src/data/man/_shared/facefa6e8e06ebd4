An  outline of the source code hierarchy.  Critical utilities for
the system and users.  Critical utilities for the system and  the
superuser.   Not‐so  critical utilities for the system and users.
Not‐so critical utilities  for  the  system  and  the  superuser.
Sources  shared  between  kernel and userland.  Utilities.  Every
utility has its own subdirectory, where its source  and  Makefile
are  located.   Include headers.  Every group of header files has
its own subdirectory, where it and its Makefile are located.  Li‐
braries.  Every library has its own subdirectory,  where  it  and
its Makefile are located.  A framework to (re)build the libraries
shipped  with  for  different ABI than the default for that plat‐
form.  Every directory contains a Makefile and a  makefile  frag‐
ment  for  building an compat libraries for For example, is where
the 32‐bit compat libraries for the amd64 port are  being  built.
The list of subdirectories (the libraries and ld.elf_so) to build
with  this  ABI.   The list of subdirectories for each port.  The
basic  framework  to  force  the  right  paths  for  library  and
ld.elf_so  linkage.  A hack to get objdirs created timely.  Cryp‐
tographic source, which may have import or  export  restrictions.
Original  sources.   This  is deprecated; should be used instead.
Original sources, grouped by license, and then  package  per  li‐
cense.   contain original sources for given package; other direc‐
tories contain Makefiles and given package’s config  files.   Un‐
modified  sources from third parties.  This is deprecated; should
be used instead.  Tools and data‐files for making  distributions.
Architecture‐specific files, grouped by image type.  For example,
contains  Makefiles  for making images for various types of flop‐
pies; contains makefiles for making ramdisks etc.   Was  used  to
create  bootable CD images.  This is deprecated; ´s target should
be used instead.  Common files for images generation.  Files  for
miniroot.  Architecture‐specific parts of release notes.  Scripts
for  making  file sets.  Utilities for installation ramdisk.  De‐
velopment documentation files:  changelogs,  build  readmes  etc.
contains  roadmaps.   Default  configuration files to be put into
Architecture‐specific  config  files.   Unmodified  sources  from
third parties, grouped by license.  Every may contain: Unmodified
third  party  source for a given package Such subdirectories con‐
tain reachover Makefiles,  README’s  and  various  import  helper
scripts.  For example, contains sources licensed under Public Do‐
main  license; contains original sources; and itself contain rea‐
chover Makefiles.  Sources for utilities/files  in  each  utility
has its own subdirectory, where its sources and Makefiles are lo‐
cated.   Files  to  be  put into Source for libraries in and some
scripts for them.  Every directory contains source for given  li‐
brary  and  Makefiles.   Source  for utilities in Every directory
contains source for given utility and Makefiles.  Various regres‐
sion tests in This is deprecated; most tests are  being  migrated
into once they are migrated to the test framework.  Makefiles for
copying  utilities to Source for utilities/files in Every utility
has its own subdirectory, where its source and Makefile  are  lo‐
cated.  Kernel source.  Network packet alternate queueing.  Files
to  specific  hardware platforms.  Coda file system driver.  Sup‐
port for older version binaries and  binaries.   Misc  files  for
building  kernel.   Crypt  algorithms used by IPsec.  Client code
for local kernel debugger.  Device drivers.  Parent directory for
the packet filter Sources from third parties, grouped by license.
File systems storing data on physical  drives.   macros.   Kernel
code,  such  as resource management, signal delivering, etc.  Li‐
braries used by the kernel.  Drivers for  file  systems  used  to
store  layered  data for kernel features.  Kernel components, in‐
cluding  hardware  specific  drivers  and  upper‐level   drivers.
Lowlevel network: protocol drivers, packet filters and access in‐
terfaces  for  NICs.   Drivers  for 802.11 wireless network.  Ap‐
pletalk protocol stack Bluetooth stack  Controller  Area  Network
stack  IPv4  protocol  stack IPv6 protocol stack.  IPsec protocol
stack MPLS protocol stack Network file  system  driver.   Crypto‐
graphic  hardware  framework Rump kernel Security model framework
Source for several standalone programs that aren’t used  by  cur‐
rently.   Header  files  that  get installed into UFS file system
driver.  Virtual memory manager.  Source  for  test  programs  in
These  tests  use  the test framework.  For library routines, in‐
cluding system calls, the directory structure of the tests should
follow the directory structure of the real source tree.  For  in‐
stance,  interfaces  available  via  the C library should follow:
etc.  Equivalently, all tests for userland utilities  should  try
to  follow their location in the source tree.  If this can not be
satisfied, the tests for a utility should be  located  under  the
directory  to  which  the utility is installed.  Thus, a test for
should go to Likewise, a test for should be in  even  though  the
source  code  for  the  program  is located under Reachover build
structure for the host build tools.  Every utility  has  its  own
directory, where its Makefile is located.  Reachover build struc‐
ture  for  X11R7; the source is in The directory structure copies
the system´s; every directory contains a Makefile.  This file was
created as a part of Google Code‐in 2012/2013.  This manpage  was
written by the part by
