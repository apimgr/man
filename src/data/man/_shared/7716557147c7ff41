_n_f_s_s_e_r_v_c_t_l(2)                 System Calls Manual                _n_f_s_s_e_r_v_c_t_l(2)

NNAAMMEE
       nfsservctl - syscall interface to kernel nfs daemon

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<lliinnuuxx//nnffssdd//ssyyssccaallll..hh>>

       lloonngg nnffsssseerrvvccttll((iinntt _c_m_d,, ssttrruucctt nnffssccttll__aarrgg **_a_r_g_p,,
                       uunniioonn nnffssccttll__rreess **_r_e_s_p));;

DDEESSCCRRIIPPTTIIOONN
       _N_o_t_e:  Since Linux 3.1, this system call no longer exists.  It has been
       replaced by a set of files in the _n_f_s_d filesystem; see nnffssdd(7).

           /*
            * These are the commands understood by nfsctl().
            */
           #define NFSCTL_SVC        0  /* This is a server process.  */
           #define NFSCTL_ADDCLIENT  1  /* Add an NFS client.  */
           #define NFSCTL_DELCLIENT  2  /* Remove an NFS client.  */
           #define NFSCTL_EXPORT     3  /* Export a filesystem.  */
           #define NFSCTL_UNEXPORT   4  /* Unexport a filesystem.  */
           #define NFSCTL_UGIDUPDATE 5  /* Update a client's UID/GID map
                                           (only in Linux 2.4.x and earlier).  */
           #define NFSCTL_GETFH      6  /* Get a file handle (used by mountd(8))
                                           (only in Linux 2.4.x and earlier).  */

           struct nfsctl_arg {
               int                       ca_version;     /* safeguard */
               union {
                   struct nfsctl_svc     u_svc;
                   struct nfsctl_client  u_client;
                   struct nfsctl_export  u_export;
                   struct nfsctl_uidmap  u_umap;
                   struct nfsctl_fhparm  u_getfh;
                   unsigned int          u_debug;
               } u;
           }

           union nfsctl_res {
                   struct knfs_fh          cr_getfh;
                   unsigned int            cr_debug;
           };

RREETTUURRNN VVAALLUUEE
       On success, zero is returned.  On error, -1 is returned, and  _e_r_r_n_o  is
       set to indicate the error.

SSTTAANNDDAARRDDSS
       Linux.

HHIISSTTOORRYY
       Removed in Linux 3.1.  Removed in glibc 2.28.

SSEEEE AALLSSOO
       nnffssdd(7)

Linux man‐pages (unreleased)      2025‐09‐21                     _n_f_s_s_e_r_v_c_t_l(2)
