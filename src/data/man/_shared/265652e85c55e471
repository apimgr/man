framework  The  file  configures all the features provided by the
framework.  It consists of a series of device and sensor  blocks.
Each  sensor  block  defines  a group of The file format is free‐
form: new line markers and  indentation  are  ignored.   Comments
start  with a sign and extend until the end of line.  A is like a
variable assignment.  It has a name, which goes to  the  left  of
the  equal  sign,  and a value, which goes to the right.  The as‐
signment ends with a semicolon.  It looks like: There is no  dif‐
ference between string or integer values when defining them.  The
value  must  be surrounded by double quotes if it contains white‐
space.  There can be multiple  groups  of  devices  and  multiple
groups of sensors in the configuration file.  A device block con‐
sists of one or more sensor blocks and one or more global proper‐
ties.    It  has  the  following  syntax:          device_name  {
                prop = value;                 ...
                sensor0 {                         prop = value;
                        ...                                     }
                ...                            sensorN          {
                        prop = value;                         ...
                }         }         ...  Device names  are  those
shown  by the command; sensor blocks are named by the index posi‐
tion in which they are shown.  For example, if we have  the  fol‐
lowing output from the command:
  CPU Temperature:     32.000 degC
   MB Temperature:     37.000 degC
    Vcore Voltage:      1.232 V
     +3.3 Voltage:      3.248 V
       +5 Voltage:      4.992 V
      +12 Voltage:     11.985 V
    CPU  FAN  Speed:       1250 RPM corresponds to the sensor and
corresponds to the sensor.  There is another way that  will  give
you  the correct index sensor; the command will print the raw XML
property list.  You only have to find the object in the appropri‐
ate dictionary.  The object will be  shown  as:          <key>in‐
dex</key>          <string>sensor2</string>  Invalid  sensors and
devices will be detected by the parser and will  be  reported  as
errors.   The following properties are provided for sensor blocks
(please note that not all properties apply to all  type  of  sen‐
sors): Sets a critical capacity limit property of 10 percent in a
battery sensor.  Battery sensors are those that report a percent‐
age  from  the  output.  It is possible to find out if the sensor
accepts this property by running and looking if the object is de‐
fined as on its dictionary.  For example:          <key>want‐per‐
centage</key>          <true/>  Only a value between 0 and 100 is
allowed.  When the limit is reached in the sensor, a  event  will
be sent to the daemon (if running) and will execute the block for
this event in If this property is set, its value will be shown in
the  display  output  with a column named Sets a warning capacity
limit property of 20 percent in a battery sensor.   Battery  sen‐
sors  are  those that report a percentage from the output.  It is
possible to find out if the sensor accepts this property by  run‐
ning  and  looking if the object is defined as on its dictionary.
For example:          <key>want‐percentage</key>          <true/>
Only  a  value  between  0 and 100 is allowed.  When the limit is
reached in the sensor, a event will be sent  to  the  daemon  (if
running)  and  will  execute  the block for this event in If this
property is set, its value will be shown in  the  display  output
with  a  column  named  Sets a high capacity limit property of 90
percent in a battery sensor.  Battery sensors are those that  re‐
port a percentage from the output.  It is possible to find out if
the  sensor  accepts  this property by running and looking if the
object  is  defined  as  on   its   dictionary.    For   example:
        <key>want‐percentage</key>          <true/>  Only a value
between 0 and 100 is allowed.  When the limit is reached  in  the
sensor,  a event will be sent to the daemon (if running) and will
execute the block for this event in If this property is set,  its
value  will  be  shown  in the display output with a column named
Sets a warning capacity limit property of 99 percent in a battery
sensor.  Battery sensors are those that report a percentage  from
the  output.   It  is  possible to find out if the sensor accepts
this property by running and looking if the object is defined  as
on   its  dictionary.   For  example:          <key>want‐percent‐
age</key>         <true/> Only a value between 0 and 100  is  al‐
lowed.   When the limit is reached in the sensor, a event will be
sent to the daemon (if running) and will execute  the  block  for
this event in If this property is set, its value will be shown in
the  display  output  with a column named Sets a critical maximum
limit property in a sensor.  Note that in this  example,  we  are
specifying the keyword at the end; that means that this will only
be  valid  for sensors and that the value is specified as degrees
If degrees Fahrenheit are wanted, just  use  the  letter  as  in:
critical‐max = 140F; To know sensor type, you have to look at the
object in the XML property list.  Remember: the XML property list
has  all  the  information that the application uses to print the
values!  Other sensors that are not of type must not include  the
final  character for the unit.  A dot is allowed in the value, if
it corresponds to the range that the sensor is  reporting.   When
the limit has been reached in the sensor, a event will be sent to
the daemon (if running) and will execute the block for this event
in  the  appropriate  script  (depending  on  the sensor’s type).
Please note that this property cannot be set in battery  capacity
sensors  (those  that have the object in their dictionary).  This
rule applies for the and properties too.   If  this  property  is
set,  its value will be shown in the display output with a column
named Sets a critical minimum limit property in  a  sensor.   The
rules  for  and are the same.  When the limit has been reached in
the sensor, a event will be sent to the daemon (if  running)  and
will  execute  the block for this event in the appropriate script
(depending on the sensor’s type).  If this property is  set,  its
value  will  be  shown  in the display output with a column named
Sets a warning maximum limit property in a sensor.  The rules for
and are the same.  When the limit has been reached in the sensor,
a event will be sent to the daemon (if running) and will  execute
the  block for this event in the appropriate script (depending on
the sensor’s type).  Please note that this property cannot be set
in battery capacity sensors (those that have the object in  their
dictionary).   This  rule  applies for the property too.  If this
property is set, its value will be shown in  the  display  output
with  a  column named Sets a critical minimum limit property in a
sensor.  The rules for and are the same.  When the limit has been
reached in the sensor, a event will be sent  to  the  daemon  (if
running)  and will execute the block for this event in the appro‐
priate script (depending on the sensor’s type).  If this property
is set, its value will be shown in the display output with a col‐
umn named Sets a new description in a sensor.  You can  set  this
property  in  all sensors, except that you won’t be able to set a
description that is currently  used  for  the  specified  device.
Sets  a  new resistor factor property in a sensor.  This property
is only allowed in sensors and if the device has enabled the  ap‐
propriate flag for the mentioned sensor.  The resistor factor may
be  used  to change the behavior of the value returned by the de‐
vice.  If a sensor supports  this,  the  object  appears  enabled
(true) in the dictionary.  The following properties are available
for  device  blocks: This property sets the refresh timeout value
in a device, and will be used to refresh data and check for crit‐
ical conditions any time the timeout is met.  The  value  may  be
specified  in seconds, minutes or hours.  To specify the value in
seconds, the character must be appended last, if minutes  is  de‐
sired,  a and a for hours.  For example for 10 seconds or for one
hour.  Default configuration file.  The configuration file  first
appeared in
