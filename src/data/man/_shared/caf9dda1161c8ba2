CHCPU(8)                     System Administration                    CHCPU(8)



NNAAMMEE
       chcpu - configure CPUs

SSYYNNOOPPSSIISS
       cchhccppuu --cc|--dd|--ee|--gg _c_p_u_-_l_i_s_t

       cchhccppuu --pp _m_o_d_e

       cchhccppuu --rr|--hh|--VV

DDEESSCCRRIIPPTTIIOONN
       cchhccppuu can modify the state of CPUs. It can enable or disable CPUs, scan
       for new CPUs, change the CPU dispatching _m_o_d_e of the underlying
       hypervisor, and request CPUs from the hypervisor (configure) or return
       CPUs to the hypervisor (deconfigure).

       Some options have a _c_p_u_-_l_i_s_t argument. Use this argument to specify a
       comma-separated list of CPUs. The list can contain individual CPU
       addresses or ranges of addresses. For example, 00,,55,,77,,99--1111 makes the
       command applicable to the CPUs with the addresses 0, 5, 7, 9, 10, and
       11.

OOPPTTIIOONNSS
       --cc, ----ccoonnffiigguurree _c_p_u_-_l_i_s_t
           Configure the specified CPUs. Configuring a CPU means that the
           hypervisor takes a CPU from the CPU pool and assigns it to the
           virtual hardware on which your kernel runs.

       --dd, ----ddiissaabbllee _c_p_u_-_l_i_s_t
           Disable the specified CPUs. Disabling a CPU means that the kernel
           sets it offline.

       --ee, ----eennaabbllee _c_p_u_-_l_i_s_t
           Enable the specified CPUs. Enabling a CPU means that the kernel
           sets it online. A CPU must be configured, see --cc, before it can be
           enabled.

       --gg, ----ddeeccoonnffiigguurree _c_p_u_-_l_i_s_t
           Deconfigure the specified CPUs. Deconfiguring a CPU means that the
           hypervisor removes the CPU from the virtual hardware on which the
           Linux instance runs and returns it to the CPU pool. A CPU must be
           offline, see --dd, before it can be deconfigured.

       --pp, ----ddiissppaattcchh _m_o_d_e
           Set the CPU dispatching _m_o_d_e (polarization). This option has an
           effect only if your hardware architecture and hypervisor support
           CPU polarization. Available _m_o_d_e_s are:

           hhoorriizzoonnttaall
               The workload is spread across all available CPUs.

           vveerrttiiccaall
               The workload is concentrated on few CPUs.

       --rr, ----rreessccaann
           Trigger a rescan of CPUs. After a rescan, the Linux kernel
           recognizes the new CPUs. Use this option on systems that do not
           automatically detect newly attached CPUs.

       --hh, ----hheellpp
           Display help text and exit.

       --VV, ----vveerrssiioonn
           Print version and exit.

EEXXIITT SSTTAATTUUSS
       cchhccppuu has the following exit status values:

       00
           success

       11
           failure

       6644
           partial success

AAUUTTHHOORRSS
       Heiko Carstens <heiko.carstens@de.ibm.com>

CCOOPPYYRRIIGGHHTT
       Copyright IBM Corp. 2011

SSEEEE AALLSSOO
       llssccppuu(1)

RREEPPOORRTTIINNGG BBUUGGSS
       For bug reports, use the issue tracker at
       https://github.com/util-linux/util-linux/issues.

AAVVAAIILLAABBIILLIITTYY
       The cchhccppuu command is part of the util-linux package which can be
       downloaded from Linux Kernel Archive
       <https://www.kernel.org/pub/linux/utils/util-linux/>.



util-linux 2.38.1                 2022-05-11                          CHCPU(8)
