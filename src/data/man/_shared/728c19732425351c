The  file contains descriptive information about the various file
systems.  is only read by programs, and not written;  it  is  the
duty  of the system administrator to properly create and maintain
this file.  Each file system is described  on  a  separate  line;
fields  on  each line are separated by tabs or spaces.  Lines be‐
ginning with are comments.  The order of records in is  important
because  and  sequentially iterate through doing their respective
tasks.  Each configuration line/record in  has  the  format:  The
first  field,  describes  the block special device or remote file
system to be mounted.  For file systems of type the special  file
name  is  the block special file name, and not the character spe‐
cial file name.  If a program needs the  character  special  file
name, the program must create it by appending a after the last in
the  special  file name.  (Note that for some file systems, e.g.,
and this field has no applicable use, and any string may be  sup‐
plied  as a placeholder.  It is present simply for consistency of
argument number and order.)  If the first field is  of  the  form
then  all  the  wedge  partitions are searched for one that has a
wedge name equal to and the device corresponding  to  it  is  se‐
lected.   If the first field starts with the prefix the prefix is
replaced with where is the  value  of  the  sysctl.   The  second
field,  describes  the mount point for the file system.  For swap
and dump partitions, this field should be specified as The  third
field,  describes  the  type of the file system.  The system cur‐
rently supports these file systems: an file system.  an 9660  CD‐
ROM  file  system.  an implementation of the Linux an implementa‐
tion of a local file system.   a  file  system  for  various  and
sundry kernel statistics.  a log‐structured file‐system.  a local
memory‐based  file  system.   an  a Sun Microsystems compatible a
file system used by Still experimental.  a loop‐back file system,
allowing parts of the system to be viewed  elsewhere.   a  demon‐
stration  of  layered file systems.  a general file system inter‐
face, currently supports TCP and FS mounts.  a local file  system
of process information.  a pseudo‐terminal device file system.  a
disk  partition to be used for swapping and paging.  an efficient
memory file system.  a user and group re‐mapping file system.   a
translucent  file  system.  a ZFS file system.  The fourth field,
describes the mount options associated with the file system.   It
is  formatted  as a comma separated list of options.  It contains
at least the type of mount (see below) plus  any  additional  op‐
tions  appropriate  to  the  file system type.  The option can be
used in the form to cause a file system not to be  mounted  auto‐
matically  (with  ,  or system boot time).  If the options and/or
are specified, the file system is automatically processed by  the
command,  and  legacy  user  and/or group disk quotas are enabled
with By default, file system quotas are maintained in files named
and which are located at the root of the associated file  system.
These  defaults may be overridden by putting an equal sign and an
alternative absolute pathname following the quota option.   Thus,
if  the  user  quota  file  for is stored in this location can be
specified as: userquota=/var/quotas/tmp.user It is recommended to
turn on the new, in‐file system quota with or at time, and to not
use the or options.  Migration of limits to the new in‐file  sys‐
tem  quota can be handled via and The option is used to mount the
file system using  a  userspace  server  instead  of  the  kernel
server.   The  type  of the mount is extracted from the field and
stored separately in the  field  (it  is  not  deleted  from  the
field).  If is or then the file system whose name is given in the
field  is  normally mounted read‐write or read‐only on the speci‐
fied special file.  If is or then the special file is made avail‐
able as a piece of swap or dump space by the command towards  the
beginning  of the system reboot procedure.  See for more informa‐
tion on configuring swap and dump devices.  The fields other than
and are unused.  If is specified as the entry is  ignored.   This
is  useful  to  show  disk partitions which are currently unused.
The fifth field, is used for these file systems by the command to
determine which file systems need to be  dumped.   If  the  fifth
field is not present, a value of zero is returned and will assume
that  the  file  system  does  not  need to be dumped.  The sixth
field, is used by the program to determine  the  order  in  which
file system checks are done at reboot time.  The root file system
should  be  specified  with a of 1, and other file systems should
have a of 2.  Filesystems within a drive will be checked  sequen‐
tially,  but  file systems on different drives will be checked at
the same time to use parallelism available in the  hardware.   If
the  sixth  field  is not present or zero, a value of zero is re‐
turned and will assume that the file system does not need  to  be
checked.  #define FSTAB_RW        "rw"    /* read‐write device */
#define FSTAB_RQ        "rq"    /* read/write with quotas */ #de‐
fine FSTAB_RO        "ro"    /*    read‐only   device   */   #de‐
fine FSTAB_SW        "sw"    /*    swap    device     */     #de‐
fine FSTAB_DP        "dp"    /*     dump     device    */    #de‐
fine FSTAB_XX        "xx"    /* ignore totally */

struct fstab {         char    *fs_spec;       /*  block  special
device  name  */          char    *fs_file;       /*  file system
path prefix */         char    *fs_vfstype;    /*  type  of  file
system   */          char    *fs_mntops;     /*  comma  separated
mount options */         char    *fs_type;       /* rw,  ro,  sw,
or  xx  */          int     fs_freq;        /* dump frequency, in
days */         int     fs_passno;      /* pass number on  paral‐
lel  fsck */ }; The proper way to read records from is to use the
routines and The location of  configuration  file.   Some  useful
configuration   examples.    To  use  on  a  non‐GPT  disk,  use:
NAME=sb2k5Root/a         /        ffs      rw,log            1  1
NAME=sb2k5Root/b         none    swap    sw,dp            0 0 For
a  disk,  use:  NAME=firstpartition      /        ffs      rw,log
1  1 NAME=secondpartition    none    swap    sw,dp            0 0
The file format appeared in
