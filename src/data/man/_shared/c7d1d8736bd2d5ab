The  utility examines the bus of an Ethernet interface.  The most
useful feature is converting the bus to the file format for later
examination.  itself is limited to displaying only very basic in‐
formation about each frame.  accepts the following  flags:  Print
bus  header  only and skip contents.  Convert bus contents to the
format and write the result to The file signifies  stdout.   Feed
the busfile contents to pcap: $ shmif_dumpbus ‐p ‐ busfile | tcp‐
dump  ‐r  ‐  does not lock the busfile and is best used for post‐
mortem analysis of the bus traffic.  The timestamp for each frame
contains the sender’s timestamp and may not be monotonically  in‐
creasing with respect to the frame order in the dump.
