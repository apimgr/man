_t_t_y_n_a_m_e(3)                 Library Functions Manual                 _t_t_y_n_a_m_e(3)

NNAAMMEE
       ttyname, ttyname_r - return name of a terminal

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<uunniissttdd..hh>>

       cchhaarr **ttttyynnaammee((iinntt _f_d));;
       iinntt ttttyynnaammee__rr((size_t size;
                     iinntt _f_d,, cchhaarr _b_u_f[[_s_i_z_e]],, ssiizzee__tt _s_i_z_e));;

DDEESSCCRRIIPPTTIIOONN
       The  function  ttttyynnaammee() returns a pointer to the null‐terminated path‐
       name of the terminal device that is open on the file descriptor _f_d,  or
       NULL on error (for example, if _f_d is not connected to a terminal).  The
       return value may point to static data, possibly overwritten by the next
       call.   The function ttttyynnaammee__rr() stores this pathname in the buffer _b_u_f
       of size _s_i_z_e.

RREETTUURRNN VVAALLUUEE
       The function ttttyynnaammee() returns a pointer to a pathname on success.   On
       error,  NULL  is returned, and _e_r_r_n_o is set to indicate the error.  The
       function ttttyynnaammee__rr() returns 0 on success, and an error number upon er‐
       ror.

EERRRROORRSS
       EEBBAADDFF  Bad file descriptor.

       EENNOODDEEVV _f_d refers to a slave pseudoterminal device but the corresponding
              pathname could not be found (see NOTES).

       EENNOOTTTTYY _f_d does not refer to a terminal device.

       EERRAANNGGEE (ttttyynnaammee__rr()) _s_i_z_e was too small to allow storing the pathname.

AATTTTRRIIBBUUTTEESS
       For an explanation of the terms  used  in  this  section,  see  aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       ttttyynnaammee() T}   Thread safety  MT‐Unsafe race:ttyname T{ ttttyynnaammee__rr()
       T}   Thread safety   MT‐Safe

SSTTAANNDDAARRDDSS
       POSIX.1‐2008.

HHIISSTTOORRYY
       POSIX.1‐2001, 4.2BSD.

NNOOTTEESS
       A process that keeps a file descriptor that refers to a ppttss(4) device
       open when switching to another mount namespace that uses a different
       _/_d_e_v_/_p_t_m_x instance may still accidentally find that a device path of
       the same name for that file descriptor exists.  However, this device
       path refers to a different device and thus can’t be used to access the
       device that the file descriptor refers to.  Calling ttttyynnaammee() or
       ttttyynnaammee__rr() on the file descriptor in the new mount namespace will
       cause these functions to return NULL and set _e_r_r_n_o to EENNOODDEEVV.

SSEEEE AALLSSOO
       ttttyy(1), ffssttaatt(2), cctteerrmmiidd(3), iissaattttyy(3), ppttss(4)

Linux man‐pages (unreleased)      2025‐06‐28                        _t_t_y_n_a_m_e(3)
