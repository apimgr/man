ACPIDUMP(8)               BSD System Manager's Manual              ACPIDUMP(8)

NNAAMMEE
     aaccppiidduummpp — dump ACPI tables

SSYYNNOOPPSSIISS
     aaccppiidduummpp --oo _p_r_e_f_i_x

DDEESSCCRRIIPPTTIIOONN
     The aaccppiidduummpp command stores ACPI tables from physical memory into files
     specified by _p_r_e_f_i_x.  If _p_r_e_f_i_x specifies a directory, the generated
     files will be of the form <prefix>/<sig>.<id>.  Otherwise, they will be
     named <prefix>.<sig>.<id>.  “sig” is the signature of the ACPI Table;
     “id” is unique for each table.

     Additionally a file called <prefix>/headers or <prefix>.headers will be
     created that contains additional human readable information pertaining to
     this specific dump.

     The ACPICA disassembler is available through the OpenBSD ports tree or
     package system:

           # pkg_add acpica
           $ iasl -d <prefix>.<sig>.<id>

     aaccppiidduummpp requires the ability to open _/_d_e_v_/_k_m_e_m, which may be restricted
     based upon the value of the _k_e_r_n_._a_l_l_o_w_k_m_e_m sysctl(8).

     aaccppiidduummpp is run at startup and stores the results in _/_v_a_r_/_d_b_/_a_c_p_i.

FFIILLEESS
     /dev/mem

     /var/db/acpi

SSEEEE AALLSSOO
     pkg_add(1), mem(4), packages(7), ports(7)

HHIISSTTOORRYY
     The aaccppiidduummpp command first appeared in OpenBSD 3.8.

BSD                            November 22, 2020                           BSD
