AZALIA(4)                BSD Kernel Interfaces Manual                AZALIA(4)

NNAAMMEE
     aazzaalliiaa — generic High Definition Audio device

SSYYNNOOPPSSIISS
     aazzaalliiaa** aatt ppccii??
     aauuddiioo** aatt aazzaalliiaa??

DDEESSCCRRIIPPTTIIOONN
     The aazzaalliiaa device driver is expected to support any PCI device which is
     compliant with the High Definition Audio Specification 1.0.

     The High Definition Audio (HDA) specification describes a flexible and
     extensible infrastructure to support high quality audio.  HDA supports
     sample rates up to 192 kHz, bit rates of up to 32 bits per sample, and up
     to 16 channels in audio streams.  It also supports S/PDIF input/output as
     well as jack retasking and sensing.  The HDA specification defines a con‐
     troller register set and a codec architecture.

MMIIXXEERR IINNTTEERRFFAACCEE
     The codec is the main audio processor.  Configuration of the device, such
     as selecting playback sample rate or decreasing the volume, is handled by
     the codec.  Codecs are comprised of interconnected widgets.  Common exam‐
     ples of widgets include DACs (digital to analog converter), ADCs (analog
     to digital converter), selectors, mixers, and pin complexes (input/output
     jacks).  Widgets may have fixed or controllable properties.  Some of
     these properties include source selection, amplifier gain, mute, and op‐
     erational direction.

     The mixer interface for aazzaalliiaa is designed to allow the user to control
     most properties of the widgets.  When aazzaalliiaa first attaches and config‐
     ures the device, it will set all widgets to unmuted, moderate volume, and
     configure connections such that stereo playback and recording will work
     with the expected input/output pins.

     There are a wide range of HDA codecs.  Some have only a few widgets that
     don't have many controllable properties, and some may have many, very
     controllable widgets.  The mixer interface on one aazzaalliiaa may be very dif‐
     ferent than the mixer interface on another.

     Mixer item names describe the widget and property being controlled.
     Names take the following form:

     <mixer class>.<widget type>[<widget type enumerator>][_<property>]

     The mixer class gives a hint as to the purpose of the control.  The wid‐
     get type enumerator is used to distinguish different widgets of the same
     type.  The enumeration starts at 2: the first widget of each type is not
     enumerated.  The enumeration order is meaningless.  The property is op‐
     tional.  Generally, if there is no property, the mixer item is an ampli‐
     fier gain control.

     The following are the widget type names used in mixer control names:

     ddaacc       Digital to analog converter, usually used for playback.  The
               audio stream channels these widgets will convert are encoded
               into their name in the form of <start channel>:<end channel>.
               For example, ddaacc--00::11 converts channels 0 and 1 (stereo).  How‐
               ever, a dac that is connected to built-in speakers or front
               panel headphone jacks by default will convert audio stream
               channels starting at 0 if the dac would otherwise not be con‐
               verting any channels.  For example, if ddaacc--22::33 is the default
               dac for the built-in speakers in a laptop, ddaacc--22::33 will convert
               channels 0 and 1 when a stereo audio stream is being played.
               This is to allow simultaneous stereo playback on both the
               built-in speakers and a line or headphone jack.

     ddiigg--ddaacc   Digital output converter, usually an S/PDIF transmitter.  The
               audio stream channels these widgets will convert are encoded
               into their name in the form of <start channel>:<end channel>.
               For example, ddiigg--ddaacc--00::11 converts channels 0 and 1 (stereo).

     aaddcc       Analog to digital converter, usually used for recording.  The
               audio stream channels these widgets will convert are encoded
               into their name in the form of <start channel>:<end channel>.
               For example, aaddcc--00::11 converts channels 0 and 1 (stereo).

     ddiigg--aaddcc   Digital input converter, usually an S/PDIF receiver.  The audio
               stream channels these widgets will convert are encoded into
               their name in the form of <start channel>:<end channel>.  For
               example, ddiigg--aaddcc--00::11 converts channels 0 and 1 (stereo).

     mmiixx       Sums multiple audio sources into a single stream, but sometimes
               only used for amplifier or mute properties.  The property part
               of a mix widget mixer control name may be the name of another
               widget.  Such a mixer item controls the input level from the
               widget in the property part of the name.

     sseell       Selects a single audio source, but sometimes used only for am‐
               plifier or mute properties.

     ppooww       Controls power state for a group of widgets.

     vvoolluummee    Interprets data from external controls, such as a volume knob.

     bbeeeepp      Creates a tone at a certain frequency.

     lliinnee      Pin complex for a line-level jack that is normally used for
               analog audio output.  The name may also be appended with a
               color abbreviation such as -grn (green), -blk (black), -org
               (orange), or -gry (grey), to help differentiate jacks.

     ssppkkrr      Pin complex for a speaker, often built into the machine.

     hhpp        Pin complex for a jack that is normally used for output to a
               headphone.

     ccdd        Pin complex for analog audio input from a CD device.

     SSPPDDIIFF     Pin complex for an S/PDIF output connector.

     lliinnee--iinn   Pin complex for a line-level jack that is normally used for
               analog audio input.

     aauuxx       Pin complex for an auxiliary analog audio input.

     mmiicc       Pin complex for analog audio input from a microphone, either a
               jack or built into the machine.

     SSPPDDIIFF--iinn  Pin complex for an S/PDIF input connector.

     bbeeeepp      Pin complex for analog audio input from a beep generator.

     The following are the property names used in mixer control names:

     mmuuttee    Mutes input or output audio stream.

     ssoouurrccee  Selects input source.  Can either select a single source or mul‐
             tiple sources.  Sometimes these controls may only have a single
             choice, but they are provided to show how audio streams move
             through the codec.

     ddiirr     Selects pin direction and input VRef signal.  VRef is represented
             in percent of the analog voltage rail.  For example, 'input-vr80'
             means input direction with 80% VRef.

     bboooosstt   Enables/disables low impedance amplifier, usually for use with
             headphones.

     eeaappdd    Powers on/off external amplifier circuitry.

     sseennssee   Reports current jack sense state.

     There are also some special mixer controls that don't directly correspond
     to properties of the widgets, but control aspects of the driver or other
     mixer controls.  The following describes these special controls:

     oouuttppuuttss..ssppkkrr__mmuutteerrss    List of pins that can mute a built-in speaker.  A
                            pin mutes the speaker by sending a signal when it
                            senses that something has been plugged into its
                            jack.  If a pin supports both input and output op‐
                            eration, the pin will only mute the speaker when
                            plugged into if that pin is configured for output.

     oouuttppuuttss..mmaasstteerr..ssllaavveess  List of widgets with amplifier gain or mute con‐
                            trols that are configured with the parameters used
                            for oouuttppuuttss..mmaasstteerr and oouuttppuuttss..mmaasstteerr..mmuuttee.  For
                            example, if ddaacc is in the list and has an input
                            amplifier gain control, setting oouuttppuuttss..mmaasstteerr to
                            200 will set iinnppuuttss..ddaacc to 200.

     oouuttppuuttss..mmaasstteerr         Amplifier gain control for widgets listed in
                            oouuttppuuttss..mmaasstteerr..ssllaavveess.

     oouuttppuuttss..mmaasstteerr..mmuuttee    Mute control for widgets listed in
                            oouuttppuuttss..mmaasstteerr..ssllaavveess.

     rreeccoorrdd..vvoolluummee..ssllaavveess   List of widgets with amplifier gain or mute con‐
                            trols that are configured with the parameters used
                            for rreeccoorrdd..vvoolluummee and rreeccoorrdd..vvoolluummee..mmuuttee.  For ex‐
                            ample, if aaddcc is in the list and has an input am‐
                            plifier gain control, setting rreeccoorrdd..vvoolluummee to 200
                            will set rreeccoorrdd..aaddcc to 200.

     rreeccoorrdd..vvoolluummee          Amplifier gain control for widgets listed in
                            rreeccoorrdd..vvoolluummee..ssllaavveess.

     rreeccoorrdd..vvoolluummee..mmuuttee     Mute control for widgets listed in
                            rreeccoorrdd..vvoolluummee..ssllaavveess.

     oouuttppuuttss..mmooddee           Controls whether the digital or analog converters
                            and pins will be used for playback.

     rreeccoorrdd..mmooddee            Controls whether the digital or analog converters
                            and pins will be used for recording.

SSEEEE AALLSSOO
     audio(4), pci(4)

HHIISSTTOORRYY
     The aazzaalliiaa device driver first appeared in NetBSD 3.0.  OpenBSD support
     first appeared in OpenBSD 4.0.

BBUUGGSS
     This driver does not support codecs that are intended for HDMI or Dis‐
     playPort connectivity.

BSD                               May 4, 2025                              BSD
