_s_t_r_c_p_y(3)                  Library Functions Manual                  _s_t_r_c_p_y(3)

NNAAMMEE
       stpcpy, strcpy, strcat - copy or catenate a string

LLIIBBRRAARRYY
       Standard C library (_l_i_b_c, _-_l_c)

SSYYNNOOPPSSIISS
       ##iinncclluuddee <<ssttrriinngg..hh>>

       cchhaarr **ssttppccppyy((cchhaarr **rreessttrriicctt _d_s_t,, ccoonnsstt cchhaarr **rreessttrriicctt _s_r_c));;
       cchhaarr **ssttrrccppyy((cchhaarr **rreessttrriicctt _d_s_t,, ccoonnsstt cchhaarr **rreessttrriicctt _s_r_c));;
       cchhaarr **ssttrrccaatt((cchhaarr **rreessttrriicctt _d_s_t,, ccoonnsstt cchhaarr **rreessttrriicctt _s_r_c));;

   Feature Test Macro Requirements for glibc (see ffeeaattuurree__tteesstt__mmaaccrrooss(7)):

       ssttppccppyy():
           Since glibc 2.10:
               _POSIX_C_SOURCE >= 200809L
           Before glibc 2.10:
               _GNU_SOURCE

DDEESSCCRRIIPPTTIIOONN
       ssttppccppyy()
       ssttrrccppyy()
              These functions copy the string pointed to by _s_r_c, into a string
              at  the buffer pointed to by _d_s_t.  The programmer is responsible
              for allocating a  destination  buffer  large  enough,  that  is,
              _s_t_r_l_e_n_(_s_r_c_)  _+ _1.  For the difference between the two functions,
              see RETURN VALUE.

       ssttrrccaatt()
              This function catenates the string pointed to by _s_r_c, after  the
              string  pointed  to  by  _d_s_t  (overwriting  its terminating null
              byte).  The programmer is responsible for allocating a  destina‐
              tion  buffer  large enough, that is, _s_t_r_l_e_n_(_d_s_t_) _+ _s_t_r_l_e_n_(_s_r_c_) _+
              _1.

       An implementation of these functions might be:

           char *
           stpcpy(char *restrict dst, const char *restrict src)
           {
               char  *p;

               p = mempcpy(dst, src, strlen(src));
               *p = '\0';

               return p;
           }

           char *
           strcpy(char *restrict dst, const char *restrict src)
           {
               stpcpy(dst, src);
               return dst;
           }

           char *
           strcat(char *restrict dst, const char *restrict src)
           {
               stpcpy(dst + strlen(dst), src);
               return dst;
           }

RREETTUURRNN VVAALLUUEE
       ssttppccppyy()
              This function returns a pointer to the terminating null byte  of
              the copied string.

       ssttrrccppyy()
       ssttrrccaatt()
              These functions return _d_s_t.

AATTTTRRIIBBUUTTEESS
       For  an  explanation  of  the  terms  used in this section, see aattttrriibb‐‐
       uutteess(7).  allbox; lbx lb lb l l l.  Interface Attribute Value T{
       ssttppccppyy(), ssttrrccppyy(), ssttrrccaatt() T}   Thread safety  MT‐Safe

SSTTAANNDDAARRDDSS
       ssttppccppyy()
              POSIX.1‐2008.

       ssttrrccppyy()
       ssttrrccaatt()
              C11, POSIX.1‐2008.

SSTTAANNDDAARRDDSS
       ssttppccppyy()
              POSIX.1‐2008.

       ssttrrccppyy()
       ssttrrccaatt()
              POSIX.1‐2001, C89, SVr4, 4.3BSD.

CCAAVVEEAATTSS
       The strings _s_r_c and _d_s_t may not overlap.

       If the  destination  buffer  is  not  large  enough,  the  behavior  is
       undefined.  See __FFOORRTTIIFFYY__SSOOUURRCCEE in ffeeaattuurree__tteesstt__mmaaccrrooss(7).

       ssttrrccaatt()  can  be  very  inefficient.   Read about Shlemiel the painter
       ⟨https://www.joelonsoftware.com/2001/12/11/back-to-basics/⟩.

EEXXAAMMPPLLEESS
       #include <err.h>
       #include <stdio.h>
       #include <stdlib.h>
       #include <string.h>

       int
       main(void)
       {
           char    *p;
           char    *buf1;
           char    *buf2;
           size_t  len, size;

           size = strlen("Hello ") + strlen("world") + strlen("!") + 1;
           buf1 = malloc(sizeof(*buf1) * size);
           if (buf1 == NULL)
               err(EXIT_FAILURE, "malloc()");
           buf2 = malloc(sizeof(*buf2) * size);
           if (buf2 == NULL)
               err(EXIT_FAILURE, "malloc()");

           p = buf1;
           p = stpcpy(p, "Hello ");
           p = stpcpy(p, "world");
           p = stpcpy(p, "!");
           len = p - buf1;

           printf("[len = %zu]: ", len);
           puts(buf1);  // "Hello world!"
           free(buf1);

           strcpy(buf2, "Hello ");
           strcat(buf2, "world");
           strcat(buf2, "!");
           len = strlen(buf2);

           printf("[len = %zu]: ", len);
           puts(buf2);  // "Hello world!"
           free(buf2);

           exit(EXIT_SUCCESS);
       }

SSEEEE AALLSSOO
       ssttrrdduupp(3), ssttrriinngg(3), wwccssccppyy(3), ssttrriinngg__ccooppyyiinngg(7)

Linux man‐pages (unreleased)      2025‐05‐17                         _s_t_r_c_p_y(3)
