places  the contents of the symbolic link in the buffer which has
size does not append a character to The system call is equivalent
to except in the case where specifies a relative path.   In  this
case  the symbolic link whose content is read relative to the di‐
rectory associated with the file descriptor instead of  the  cur‐
rent  working  directory.   If is passed the special value in the
parameter, the current working directory is used and the behavior
is identical to a call to The system call returns the same infor‐
mation about an open symbolic link file referenced by  descriptor
The  call returns the count of characters placed in the buffer if
it succeeds, or a ‐1 if an error occurs, placing the  error  code
in  the global variable will fail if: Search permission is denied
for  a  component  of  the  path  prefix.   extends  outside  the
process’s  allocated address space.  The named file is not a sym‐
bolic link.  An I/O error occurred while reading  from  the  file
system.   Too  many symbolic links are encountered in translating
the pathname.  This is taken to be indicative of a  looping  sym‐
bolic link.  A component of a pathname exceeded characters, or an
entire  path name (possibly expanded by a symbolic link) exceeded
characters.  The named file does not exist.  A component  of  the
path  prefix  is  not a directory.  In addition to the errors re‐
turned by the the may fail if: The argument does not  specify  an
absolute  path  and  the argument is neither nor a valid file de‐
scriptor open for searching.  The argument  is  not  an  absolute
path  and  is neither nor a file descriptor associated with a di‐
rectory.
The function type and the type of have changed.  The system  call
is  expected  to conform to POSIX.1‐2008 .  The function call ap‐
peared in The system call appeared in OS  X  10.10.   The  system
call appeared in macOS 13.0.
