The  utility uses the function to resolve all symbolic links, ex‐
tra characters and references to and in each and writes  the  re‐
sult of each to standard output followed by a newline.  If is ab‐
sent,  the current working directory is assumed.  With the option
(the default) it is not an error for the final component  of  the
resolved  pathname to reference a file which does not exist.  The
option reverses the effect of requiring the to resolve to an  ex‐
isting  pathname.   If is specified, warnings will not be printed
when fails.  Messages about other errors, such as bad usage,  are
still  printed.   On error, nothing is written to standard output
for that If was not given a diagnostic is written to standard er‐
ror.  Any failure to resolve a is an error for this purpose,  the
option is irrelevant.  The utility is expected to comply with the
forthcoming  edition  of  the  POSIX standard.  To be fully POSIX
compliant, applications must use either the or option,  as  which
of  those  (if  in fact either, and not some other behaviour) ap‐
plies in their absence is  unspecified.   The  standard  requires
support  for  only  a  single,  mandatory, argument.  The utility
first appeared in and  was  imported  into  but  modified  to  be
slightly more compatible with the coreutils version, and the pro‐
posed POSIX standard requirements.
