signatures  file  loads  entries to the in‐kernel database from a
file describing files to be monitored and the type of monitoring.
This file is often referred to as the or The signatures file  can
be  easily created using The signatures database has a line based
structure, where each line has several fields separated by white‐
space (space, tabs, etc.) taking the following form: The descrip‐
tion for each field is as follows: The full  path  to  the  file.
White‐space  characters can be escaped if prefixed with a Type of
fingerprinting algorithm used for the file.  Requires kernel sup‐
port for the specified algorithm.  List of  fingerprinting  algo‐
rithms  supported by the kernel can be obtained by using the fol‐
lowing command: # sysctl kern.veriexec.algorithms The fingerprint
for the file.  Can (usually) be  generated  using  the  following
command:  % cksum ‐a <algorithm> <file> Optional listing of entry
flags, separated by a comma.  These may include: Allow direct ex‐
ecution only.  Execution of a program is said to be when the pro‐
gram is invoked by the user (either in a script, manually  typing
it,  etc.) via the syscall.  Allow indirect execution only.  Exe‐
cution of a program is said to be if it is invoked by the  kernel
to  interpret  a  script  Allow  opening  the  file only, via the
syscall (no execution is allowed).  Indicate that the file is lo‐
cated on untrusted storage and its fingerprint evaluation  status
should  not  be  cached, but rather re‐calculated each time it is
accessed.  Fingerprints for untrusted files will always be evalu‐
ated on load.  To improve readability of the signatures file, the
following aliases are provided: An alias  for  An  alias  for  An
alias  for  both and An alias for both and If no flags are speci‐
fied, is assumed.  Comments begin with a character  and  span  to
the end of the line.  first appeared in
