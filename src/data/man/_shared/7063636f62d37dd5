_D_N_S_S_E_C_‐_K_E_Y_G_E_N(1)                    BIND 9                    _D_N_S_S_E_C_‐_K_E_Y_G_E_N(1)

NNAAMMEE
       dnssec‐keygen - DNSSEC key generation tool

SSYYNNOOPPSSIISS
       ddnnsssseecc--kkeeyyggeenn  [--33]  [--AA  date/offset] [--aa algorithm] [--bb keysize] [--CC]
       [--cc class] [--DD date/offset] [--dd bits] [--DD sync date/offset] [--EE engine]
       [--ff flag] [--GG] [--gg generator] [--hh] [--II date/offset] [--ii  interval]  [--KK
       directory]  [--kk  policy] [--LL ttl] [--ll file] [--nn nametype] [--PP date/off‐
       set] [--PP sync date/offset] [--pp protocol] [--qq] [--RR date/offset] [--SS key]
       [--ss strength] [--TT rrtype] [--tt type] [--VV] [--vv level] {name}

DDEESSCCRRIIPPTTIIOONN
       ddnnsssseecc--kkeeyyggeenn generates keys for DNSSEC (Secure DNS), as defined in _R_F_C
       _2_5_3_5 and _R_F_C _4_0_3_4. It can also generate keys for use with TSIG  (Trans‐
       action Signatures) as defined in _R_F_C _2_8_4_5, or TKEY (Transaction Key) as
       defined in _R_F_C _2_9_3_0.

       The  nnaammee of the key is specified on the command line. For DNSSEC keys,
       this must match the name of the zone for which the key is being  gener‐
       ated.

OOPPTTIIOONNSS
       --33     This option uses an NSEC3-capable algorithm to generate a DNSSEC
              key. If this option is used with an algorithm that has both NSEC
              and  NSEC3 versions, then the NSEC3 version is selected; for ex‐
              ample, ddnnsssseecc--kkeeyyggeenn --33 --aa RRSSAASSHHAA11  specifies  the  NSEC3RSASHA1
              algorithm.

       --aa aallggoorriitthhmm
              This  option  selects  the  cryptographic  algorithm. For DNSSEC
              keys,  the  value  of  aallggoorriitthhmm  must  be   one   of   RSASHA1,
              NSEC3RSASHA1,   RSASHA256,   RSASHA512,   ECDSAP256SHA256,  ECD‐
              SAP384SHA384, ED25519, or ED448. For TKEY, the value must be  DH
              (Diffie-Hellman);  specifying  this value automatically sets the
              _-_T _K_E_Y option as well.

              These values are case-insensitive. In some cases,  abbreviations
              are supported, such as ECDSA256 for ECDSAP256SHA256 and ECDSA384
              for  ECDSAP384SHA384.  If RSASHA1 is specified along with the _-_3
              option, NSEC3RSASHA1 is used instead.

              This parameter _m_u_s_t be specified except when using  the  _-_S  op‐
              tion, which copies the algorithm from the predecessor key.

              In prior releases, HMAC algorithms could be generated for use as
              TSIG  keys,  but  that  feature  was removed in BIND 9.13.0. Use
              _t_s_i_g_-_k_e_y_g_e_n to generate TSIG keys.

       --bb kkeeyyssiizzee
              This option specifies the number of bits in the key. The  choice
              of  key size depends on the algorithm used: RSA keys must be be‐
              tween 1024 and 4096 bits; Diffie-Hellman keys  must  be  between
              128  and  4096  bits. Elliptic curve algorithms do not need this
              parameter.

              If the key size is not specified, some algorithms  have  pre-de‐
              fined  defaults.  For  example,  RSA  keys  for  use  as  DNSSEC
              zone-signing keys have a default size of 1024 bits; RSA keys for
              use as key-signing keys (KSKs, generated with _-_f _K_S_K) default to
              2048 bits.

       --CC     This option  enables  compatibility  mode,  which  generates  an
              old-style   key,   without  any  timing  metadata.  By  default,
              ddnnsssseecc--kkeeyyggeenn includes the key's creation date in  the  metadata
              stored  with  the  private  key; other dates may be set there as
              well, including publication date,  activation  date,  etc.  Keys
              that  include  this data may be incompatible with older versions
              of BIND; the _-_C option suppresses them.

       --cc ccllaassss
              This option indicates that the DNS  record  containing  the  key
              should  have  the specified class. If not specified, class IN is
              used.

       --dd bbiittss
              This option specifies the key size in bits. For  the  algorithms
              RSASHA1, NSEC3RSASA1, RSASHA256, and RSASHA512 the key size must
              be  between  1024 and 4096 bits; DH size is between 128 and 4096
              bits. This option is  ignored  for  algorithms  ECDSAP256SHA256,
              ECDSAP384SHA384, ED25519, and ED448.

       --EE eennggiinnee
              This  option  specifies  the cryptographic hardware to use, when
              applicable.

              When BIND 9 is built with OpenSSL, this needs to be set  to  the
              OpenSSL engine identifier that drives the cryptographic acceler‐
              ator or hardware service module (usually ppkkccss1111).

       --ff ffllaagg
              This  option  sets  the  specified flag in the flag field of the
              KEY/DNSKEY record.  The only recognized flags are KSK (Key-Sign‐
              ing Key) and REVOKE.

       --GG     This option generates a key, but does not  publish  it  or  sign
              with it. This option is incompatible with _-_P and _-_A.

       --gg ggeenneerraattoorr
              This  option  indicates  the  generator  to  use if generating a
              Diffie-Hellman key. Allowed values are 2 and 5. If no  generator
              is  specified,  a known prime from _R_F_C _2_5_3_9 is used if possible;
              otherwise the default is 2.

       --hh     This option prints a short summary of the options and  arguments
              to ddnnsssseecc--kkeeyyggeenn.

       --KK ddiirreeccttoorryy
              This  option sets the directory in which the key files are to be
              written.

       --kk ppoolliiccyy
              This option creates keys for a specific ddnnsssseecc--ppoolliiccyy. If a pol‐
              icy uses multiple keys, ddnnsssseecc--kkeeyyggeenn generates  multiple  keys.
              This  also  creates  a  ".state"  file  to keep track of the key
              state.

              This option creates keys according to the ddnnsssseecc--ppoolliiccyy configu‐
              ration, hence it cannot be used at the same time as many of  the
              other options that ddnnsssseecc--kkeeyyggeenn provides.

       --LL ttttll This  option sets the default TTL to use for this key when it is
              converted into a DNSKEY RR. This is the TTL used when the key is
              imported into a zone, unless there was already a DNSKEY RRset in
              place, in which case the existing TTL takes precedence. If  this
              value  is not set and there is no existing DNSKEY RRset, the TTL
              defaults to the SOA TTL. Setting the default TTL to 00 or nnoonnee is
              the same as leaving it unset.

       --ll ffiillee
              This option  provides  a  configuration  file  that  contains  a
              ddnnsssseecc--ppoolliiccyy statement (matching the policy set with _-_k).

       --nn nnaammeettyyppee
              This  option  specifies  the owner type of the key. The value of
              nnaammeettyyppee  must  either  be  ZONE  (for   a   DNSSEC   zone   key
              (KEY/DNSKEY)),  HOST or ENTITY (for a key associated with a host
              (KEY)), USER (for a key associated with a user (KEY)), or  OTHER
              (DNSKEY). These values are case-insensitive. The default is ZONE
              for DNSKEY generation.

       --pp pprroottooccooll
              This  option  sets the protocol value for the generated key, for
              use with _-_T _K_E_Y. The protocol is a number between 0 and 255. The
              default is 3 (DNSSEC). Other possible values for  this  argument
              are listed in _R_F_C _2_5_3_5 and its successors.

       --qq     This  option  sets quiet mode, which suppresses unnecessary out‐
              put, including progress indication. Without  this  option,  when
              ddnnsssseecc--kkeeyyggeenn is run interactively to generate an RSA or DSA key
              pair,  it  prints  a  string of symbols to ssttddeerrrr indicating the
              progress of the key generation. A .. indicates that a random num‐
              ber has been found which passed an initial sieve test; ++ means a
              number has passed a single round of the  Miller-Rabin  primality
              test;  and  a space ( ) means that the number has passed all the
              tests and is a satisfactory key.

       --SS kkeeyy This option creates a new key which is an explicit successor  to
              an existing key.  The name, algorithm, size, and type of the key
              are  set  to  match the existing key. The activation date of the
              new key is set to the inactivation date of the existing one. The
              publication date is set to the activation date minus the prepub‐
              lication interval, which defaults to 30 days.

       --ss ssttrreennggtthh
              This option  specifies  the  strength  value  of  the  key.  The
              strength  is a number between 0 and 15, and currently has no de‐
              fined purpose in DNSSEC.

       --TT rrrrttyyppee
              This option specifies the resource record type to  use  for  the
              key.  rrrrttyyppee must be either DNSKEY or KEY. The default is DNSKEY
              when using a DNSSEC algorithm, but it can be overridden  to  KEY
              for use with SIG(0).

       --tt ttyyppee
              This  option  indicates the type of the key for use with _-_T _K_E_Y.
              ttyyppee must be one of AUTHCONF, NOAUTHCONF, NOAUTH, or NOCONF. The
              default is AUTHCONF. AUTH refers to the ability to  authenticate
              data, and CONF to the ability to encrypt data.

       --VV     This option prints version information.

       --vv lleevveell
              This option sets the debugging level.

TTIIMMIINNGG OOPPTTIIOONNSS
       Dates  can be expressed in the format YYYYMMDD or YYYYMMDDHHMMSS (which
       is the format used inside key files), or 'Day Mon DD HH:MM:SS YYYY' (as
       printed by ddnnsssseecc--sseettttiimmee --pp),  or  UNIX  epoch  time  (as  printed  by
       ddnnsssseecc--sseettttiimmee --uupp), or the literal nnooww.

       The  argument  can  be  followed by ++ or -- and an offset from the given
       time. The literal nnooww can be omitted before an offset. The  offset  can
       be followed by one of the suffixes yy, mmoo, ww, dd, hh, or mmii, so that it is
       computed  in  years (defined as 365 24-hour days, ignoring leap years),
       months (defined as 30 24-hour days), weeks, days,  hours,  or  minutes,
       respectively. Without a suffix, the offset is computed in seconds.

       To unset a date, use nnoonnee, nneevveerr, or uunnsseett.

       --PP ddaattee//ooffffsseett
              This  option  sets the date on which a key is to be published to
              the zone. After that date, the key is included in the  zone  but
              is not used to sign it. If not set, and if the _-_G option has not
              been used, the default is the current date.

              ssyynncc ddaattee//ooffffsseett
                     This  option  sets  the  date  on  which  CDS and CDNSKEY
                     records that match this key are to be  published  to  the
                     zone.

       --AA ddaattee//ooffffsseett
              This  option  sets the date on which the key is to be activated.
              After that date, the key is included in the  zone  and  used  to
              sign it. If not set, and if the _-_G option has not been used, the
              default is the current date. If set, and _-_P is not set, the pub‐
              lication  date is set to the activation date minus the prepubli‐
              cation interval.

       --RR ddaattee//ooffffsseett
              This option sets the date on which the key is to be revoked. Af‐
              ter that date, the key is flagged as revoked. It is included  in
              the zone and is used to sign it.

       --II ddaattee//ooffffsseett
              This option sets the date on which the key is to be retired. Af‐
              ter  that date, the key is still included in the zone, but it is
              not used to sign it.

       --DD ddaattee//ooffffsseett
              This option sets the date on which the key is to be deleted. Af‐
              ter that date, the key is no longer included in the zone.  (How‐
              ever, it may remain in the key repository.)

              ssyynncc ddaattee//ooffffsseett
                     This  option  sets  the date on which the CDS and CDNSKEY
                     records that match this key are to be deleted.

       --ii iinntteerrvvaall
              This option sets the prepublication interval for a key. If  set,
              then  the  publication and activation dates must be separated by
              at least this much time. If the activation date is specified but
              the publication date is not, the publication  date  defaults  to
              this  much  time  before the activation date; conversely, if the
              publication date is specified but not the activation date, acti‐
              vation is set to this much time after publication.

              If the key is being created as an explicit successor to  another
              key, then the default prepublication interval is 30 days; other‐
              wise it is zero.

              As  with date offsets, if the argument is followed by one of the
              suffixes yy, mmoo, ww, dd, hh, or mmii,  the  interval  is  measured  in
              years,  months,  weeks,  days,  hours, or minutes, respectively.
              Without a suffix, the interval is measured in seconds.

GGEENNEERRAATTEEDD KKEEYYSS
       When ddnnsssseecc--kkeeyyggeenn completes successfully, it prints a  string  of  the
       form KKnnnnnnnn..++aaaaaa++iiiiiiiiii to the standard output. This is an identification
       string for the key it has generated.

       • nnnnnnnn is the key name.

       • aaaaaa is the numeric representation of the algorithm.

       • iiiiiiiiii is the key identifier (or footprint).

       ddnnsssseecc--kkeeyyggeenn  creates  two  files,  with  names  based  on the printed
       string.   KKnnnnnnnn..++aaaaaa++iiiiiiiiii..kkeeyy   contains   the   public    key,    and
       KKnnnnnnnn..++aaaaaa++iiiiiiiiii..pprriivvaattee contains the private key.

       The  ..kkeeyy  file  contains  a DNSKEY or KEY record. When a zone is being
       signed by _n_a_m_e_d or _d_n_s_s_e_c_-_s_i_g_n_z_o_n_e _-_S, DNSKEY records are included  au‐
       tomatically.  In other cases, the ..kkeeyy file can be inserted into a zone
       file manually or with an $$IINNCCLLUUDDEE statement.

       The ..pprriivvaattee file contains algorithm-specific fields. For obvious secu‐
       rity reasons, this file does not have general read permission.

EEXXAAMMPPLLEE
       To generate an ECDSAP256SHA256 zone-signing  key  for  the  zone  eexxaamm‐‐
       ppllee..ccoomm, issue the command:

       ddnnsssseecc--kkeeyyggeenn --aa EECCDDSSAAPP225566SSHHAA225566 eexxaammppllee..ccoomm

       The command prints a string of the form:

       KKeexxaammppllee..ccoomm..++001133++2266116600

       In    this    example,   ddnnsssseecc--kkeeyyggeenn   creates   the   files   KKeexxaamm‐‐
       ppllee..ccoomm..++001133++2266116600..kkeeyy and KKeexxaammppllee..ccoomm..++001133++2266116600..pprriivvaattee.

       To generate a matching key-signing key, issue the command:

       ddnnsssseecc--kkeeyyggeenn --aa EECCDDSSAAPP225566SSHHAA225566 --ff KKSSKK eexxaammppllee..ccoomm

SSEEEE AALLSSOO
       _d_n_s_s_e_c_-_s_i_g_n_z_o_n_e_(_8_), BIND 9 Administrator Reference  Manual,  _R_F_C  _2_5_3_9,
       _R_F_C _2_8_4_5, _R_F_C _4_0_3_4.

AAUUTTHHOORR
       Internet Systems Consortium

CCOOPPYYRRIIGGHHTT
       2024, Internet Systems Consortium

@PACKAGE_VERSION@                                             _D_N_S_S_E_C_‐_K_E_Y_G_E_N(1)
