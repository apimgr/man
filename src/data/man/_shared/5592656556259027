_L_a_n_d_l_o_c_k(7)            Miscellaneous Information Manual            _L_a_n_d_l_o_c_k(7)

NNAAMMEE
       Landlock - unprivileged access‐control

DDEESSCCRRIIPPTTIIOONN
       Landlock  is an access‐control system that enables any processes to se‐
       curely restrict themselves and their future children.  Because Landlock
       is a stackable Linux Security Module (LSM), it  makes  it  possible  to
       create  safe  security  sandboxes as new security layers in addition to
       the existing system‐wide access‐controls.  This kind of sandbox is  ex‐
       pected  to help mitigate the security impact of bugs, and unexpected or
       malicious behaviors in applications.

       A Landlock security policy is a set of access rights (e.g., open a file
       in read‐only, make a directory, etc.)  tied to a file hierarchy.   Such
       policy can be configured and enforced by processes for themselves using
       three system calls:

       •  llaannddlloocckk__ccrreeaattee__rruulleesseett(2) creates a new ruleset;

       •  llaannddlloocckk__aadddd__rruullee(2) adds a new rule to a ruleset;

       •  llaannddlloocckk__rreessttrriicctt__sseellff(2) enforces a ruleset on the calling thread.

       To  be  able to use these system calls, the running kernel must support
       Landlock and it must be enabled at boot time.

   LLaannddlloocckk rruulleess
       A Landlock rule describes an action on an object which the process  in‐
       tends to perform.  A set of rules is aggregated in a ruleset, which can
       then restrict the thread enforcing it, and its future children.

       The two existing types of rules are:

       FFiilleessyysstteemm rruulleess
              For these rules, the object is a file hierarchy, and the related
              filesystem actions are defined with _f_i_l_e_s_y_s_t_e_m _a_c_c_e_s_s _r_i_g_h_t_s.

       NNeettwwoorrkk rruulleess (since ABI v4)
              For  these  rules, the object is a TCP port, and the related ac‐
              tions are defined with _n_e_t_w_o_r_k _a_c_c_e_s_s _r_i_g_h_t_s.

   FFiilleessyysstteemm aaccttiioonnss
       These flags enable to restrict a sandboxed process to a set of  actions
       on files and directories.  Files or directories opened before the sand‐
       boxing are not subject to these restrictions.  See llaannddlloocckk__aadddd__rruullee(2)
       and llaannddlloocckk__ccrreeaattee__rruulleesseett(2) for more context.

       The following access rights apply only to files:

       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__EEXXEECCUUTTEE
              Execute a file.

       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__WWRRIITTEE__FFIILLEE
              Open a file with write access.

              When opening files for writing, you will often additionally need
              the  LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__TTRRUUNNCCAATTEE  right.   In  many cases, these
              system calls  truncate  existing  files  when  overwriting  them
              (e.g., ccrreeaatt(2)).

       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__RREEAADD__FFIILLEE
              Open a file with read access.

       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__TTRRUUNNCCAATTEE
              Truncate  a  file  with  ttrruunnccaattee(2), ffttrruunnccaattee(2), ccrreeaatt(2), or
              ooppeenn(2) with OO__TTRRUUNNCC.

              This access right is available since the third  version  of  the
              Landlock ABI.

       Whether  an opened file can be truncated with ffttrruunnccaattee(2) or used with
       iiooccttll(2) is determined during ooppeenn(2), in the  same  way  as  read  and
       write   permissions  are  checked  during  ooppeenn(2)  using  LLAANNDDLLOOCCKK__AACC‐‐
       CCEESSSS__FFSS__RREEAADD__FFIILLEE and LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__WWRRIITTEE__FFIILLEE.

       A directory can receive access rights related to files or  directories.
       The  following access right is applied to the directory itself, and the
       directories beneath it:

       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__RREEAADD__DDIIRR
              Open a directory or list its content.

       However, the following access rights only apply to the content of a di‐
       rectory, not the directory itself:

       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__RREEMMOOVVEE__DDIIRR
              Remove an empty directory or rename one.

       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__RREEMMOOVVEE__FFIILLEE
              Unlink (or rename) a file.

       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__MMAAKKEE__CCHHAARR
              Create (or rename or link) a character device.

       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__MMAAKKEE__DDIIRR
              Create (or rename) a directory.

       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__MMAAKKEE__RREEGG
              Create (or rename or link) a regular file.

       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__MMAAKKEE__SSOOCCKK
              Create (or rename or link) a UNIX domain socket.

       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__MMAAKKEE__FFIIFFOO
              Create (or rename or link) a named pipe.

       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__MMAAKKEE__BBLLOOCCKK
              Create (or rename or link) a block device.

       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__MMAAKKEE__SSYYMM
              Create (or rename or link) a symbolic link.

       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__RREEFFEERR
              Link or rename a file from or to a  different  directory  (i.e.,
              reparent a file hierarchy).

              This  access  right is available since the second version of the
              Landlock ABI.

              This is the only access right which is denied by default by  any
              ruleset,  even if the right is not specified as handled at rule‐
              set creation time.  The only way to make a  ruleset  grant  this
              right  is  to  explicitly  allow  it for a specific directory by
              adding a matching rule to the ruleset.

              In particular, when using the first Landlock ABI version,  Land‐
              lock will always deny attempts to reparent files between differ‐
              ent directories.

              In addition to the source and destination directories having the
              LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__RREEFFEERR access right, the attempted link or re‐
              name operation must meet the following constraints:

              •  The  reparented  file  may not gain more access rights in the
                 destination directory than it previously had  in  the  source
                 directory.  If this is attempted, the operation results in an
                 EEXXDDEEVV error.

              •  When linking or renaming, the LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__MMAAKKEE___* right
                 for the respective file type must be granted for the destina‐
                 tion  directory.  Otherwise, the operation results in an EEAACC‐‐
                 CCEESS error.

              •  When renaming, the LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__RREEMMOOVVEE___* right for  the
                 respective  file  type  must be granted for the source direc‐
                 tory.  Otherwise, the operation results in an EEAACCCCEESS error.

              If multiple requirements are not  met,  the  EEAACCCCEESS  error  code
              takes precedence over EEXXDDEEVV.

       The following access right applies to both files and directories:

       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__IIOOCCTTLL__DDEEVV
              Invoke iiooccttll(2) commands on an opened character or block device.

              This  access  right applies to all iiooccttll(2) commands implemented
              by device drivers.  However, the following common IOCTL commands
              continue  to  be  invokable  independent  of  the   LLAANNDDLLOOCCKK__AACC‐‐
              CCEESSSS__FFSS__IIOOCCTTLL__DDEEVV right:

              •  IOCTL  commands  targeting  file  descriptors (FFIIOOCCLLEEXX, FFIIOONN‐‐
                 CCLLEEXX),

              •  IOCTL commands targeting file  descriptions  (FFIIOONNBBIIOO,  FFIIOOAA‐‐
                 SSYYNNCC),

              •  IOCTL  commands  targeting  file  systems  (FFIIFFRREEEEZZEE, FFIITTHHAAWW,
                 FFIIGGEETTBBSSZZ, FFSS__IIOOCC__GGEETTFFSSUUUUIIDD, FFSS__IIOOCC__GGEETTFFSSSSYYSSFFSSPPAATTHH)

              •  Some IOCTL commands which do not make sense  when  used  with
                 devices,  but  whose  implementations are safe and return the
                 right error codes (FFSS__IIOOCC__FFIIEEMMAAPP, FFIICCLLOONNEE, FFIICCLLOONNEERRAANNGGEE, FFIIDD‐‐
                 EEDDUUPPEERRAANNGGEE)

              This access right is available since the fifth  version  of  the
              Landlock ABI.

   NNeettwwoorrkk ffllaaggss
       These  flags enable to restrict a sandboxed process to a set of network
       actions.

       This is supported since Landlock ABI version 4.

       The following access rights apply to TCP port numbers:

       LLAANNDDLLOOCCKK__AACCCCEESSSS__NNEETT__BBIINNDD__TTCCPP
              Bind a TCP socket to a local port.

       LLAANNDDLLOOCCKK__AACCCCEESSSS__NNEETT__CCOONNNNEECCTT__TTCCPP
              Connect an active TCP socket to a remote port.

   SSccooppee ffllaaggss
       These flags enable isolating a sandboxed process from a set of IPC  ac‐
       tions.   Setting  a flag for a ruleset will isolate the Landlock domain
       to forbid connections to resources outside the domain.

       This is supported since Landlock ABI version 6.

       The following scopes exist:

       LLAANNDDLLOOCCKK__SSCCOOPPEE__AABBSSTTRRAACCTT__UUNNIIXX__SSOOCCKKEETT
              Restrict a sandboxed process from connecting to an abstract UNIX
              socket created by a process outside the related Landlock  domain
              (e.g., a parent domain or a non‐sandboxed process).

       LLAANNDDLLOOCCKK__SSCCOOPPEE__SSIIGGNNAALL
              Restrict  a  sandboxed  process from sending a signal to another
              process outside the domain.

   LLaayyeerrss ooff ffiillee ppaatthh aacccceessss rriigghhttss
       Each time a thread enforces a ruleset on itself, it updates  its  Land‐
       lock  domain  with  a  new layer of policy.  Indeed, this complementary
       policy is composed with the  potentially  other  rulesets  already  re‐
       stricting  this  thread.   A  sandboxed thread can then safely add more
       constraints to itself with a new enforced ruleset.

       One policy layer grants access to a file path if at least  one  of  its
       rules  encountered  on  the path grants the access.  A sandboxed thread
       can only access a file path if all its enforced policy layers grant the
       access as well as all the other system access controls (e.g.,  filesys‐
       tem DAC, other LSM policies, etc.).

   BBiinndd mmoouunnttss aanndd OOvveerrllaayyFFSS
       Landlock  enables  restricting  access to file hierarchies, which means
       that these access rights  can  be  propagated  with  bind  mounts  (cf.
       mmoouunntt__nnaammeessppaacceess(7)) but not with OverlayFS.

       A  bind  mount  mirrors  a source file hierarchy to a destination.  The
       destination hierarchy is then composed of  the  exact  same  files,  on
       which Landlock rules can be tied, either via the source or the destina‐
       tion  path.  These rules restrict access when they are encountered on a
       path, which means that they can restrict access to multiple file  hier‐
       archies  at  the same time, whether these hierarchies are the result of
       bind mounts or not.

       An OverlayFS mount point consists of upper  and  lower  layers.   These
       layers  are  combined  in a merge directory, result of the mount point.
       This merge hierarchy may include files from the upper and lower layers,
       but modifications performed on the merge hierarchy only reflect on  the
       upper  layer.   From a Landlock policy point of view, each of the Over‐
       layFS layers and merge hierarchies is standalone and contains  its  own
       set  of files and directories, which is different from a bind mount.  A
       policy restricting an OverlayFS layer will not  restrict  the  resulted
       merged  hierarchy,  and  vice  versa.   Landlock users should then only
       think about file hierarchies they want to allow access  to,  regardless
       of the underlying filesystem.

   IInnhheerriittaannccee
       Every new thread resulting from a cclloonnee(2) inherits Landlock domain re‐
       strictions  from its parent.  This is similar to the sseeccccoommpp(2) inheri‐
       tance or any other LSM dealing with  tasks’  ccrreeddeennttiiaallss(7).   For  in‐
       stance,  one  process’s  thread may apply Landlock rules to itself, but
       they will not be automatically applied to other sibling threads (unlike
       POSIX thread credential changes, cf.  nnppttll(7)).

       When a thread sandboxes itself, we have the guarantee that the  related
       security  policy  will  stay enforced on all this thread’s descendants.
       This allows creating standalone and modular security policies  per  ap‐
       plication,  which will automatically be composed between themselves ac‐
       cording to their run‐time parent policies.

   PPttrraaccee rreessttrriiccttiioonnss
       A sandboxed process has less privileges than  a  non‐sandboxed  process
       and  must  then be subject to additional restrictions when manipulating
       another process.  To be allowed to use ppttrraaccee(2) and  related  syscalls
       on  a  target  process, a sandboxed process should have a subset of the
       target process rules, which means the tracee must be in a sub‐domain of
       the tracer.

   IIPPCC ssccooppiinngg
       Similar to the implicit PPttrraaccee rreessttrriiccttiioonnss, we may want to further re‐
       strict interactions between sandboxes.  Therefore, at ruleset  creation
       time,  each  Landlock  domain can restrict the scope for certain opera‐
       tions, so that these operations can only reach out to processes  within
       the same Landlock domain or in a nested Landlock domain (the "scope").

       The operations which can be scoped are:

       LLAANNDDLLOOCCKK__SSCCOOPPEE__SSIIGGNNAALL
              This limits the sending of signals to target processes which run
              within the same or a nested Landlock domain.

       LLAANNDDLLOOCCKK__SSCCOOPPEE__AABBSSTTRRAACCTT__UUNNIIXX__SSOOCCKKEETT
              This  limits the set of abstract uunniixx(7) sockets to which we can
              ccoonnnneecctt(2) to socket addresses which were created by  a  process
              in the same or a nested Landlock domain.

              A  sseennddttoo(2) on a non‐connected datagram socket is treated as if
              it were doing an implicit ccoonnnneecctt(2) and will be blocked if  the
              remote  end does not stem from the same or a nested Landlock do‐
              main.

              A sseennddttoo(2) on a socket which was previously connected will  not
              be restricted.  This works for both datagram and stream sockets.

       IPC  scoping  does not support exceptions via llaannddlloocckk__aadddd__rruullee(2).  If
       an operation is scoped within a domain, no rules can be added to  allow
       access to resources or processes outside of the scope.

   TTrruunnccaattiinngg ffiilleess
       The  operations  covered  by  LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__WWRRIITTEE__FFIILLEE  and  LLAANNDD‐‐
       LLOOCCKK__AACCCCEESSSS__FFSS__TTRRUUNNCCAATTEE both change the contents of a  file  and  some‐
       times overlap in non‐intuitive ways.  It is recommended to always spec‐
       ify both of these together.

       A  particularly surprising example is ccrreeaatt(2).  The name suggests that
       this system call requires the rights to create and write  files.   How‐
       ever, it also requires the truncate right if an existing file under the
       same name is already present.

       It  should  also  be  noted  that truncating files does not require the
       LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__WWRRIITTEE__FFIILLEE right.  Apart from the ttrruunnccaattee(2) system
       call,  this  can  also  be  done  through  ooppeenn(2)   with   the   flags
       _O___R_D_O_N_L_Y _| _O___T_R_U_N_C.

       When  opening  a file, the availability of the LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__TTRRUUNN‐‐
       CCAATTEE right is associated with the newly  created  file  descriptor  and
       will  be  used  for  subsequent truncation attempts using ffttrruunnccaattee(2).
       The behavior is similar to opening a file for reading or writing, where
       permissions are checked during ooppeenn(2), but not during  the  subsequent
       rreeaadd(2) and wwrriittee(2) calls.

       As a consequence, it is possible to have multiple open file descriptors
       for  the same file, where one grants the right to truncate the file and
       the other does not.  It is also possible to pass such file  descriptors
       between  processes,  keeping their Landlock properties, even when these
       processes do not have an enforced Landlock ruleset.

VVEERRSSIIOONNSS
       Landlock was introduced in Linux 5.13.

       To determine which Landlock features are available, users should  query
       the  Landlock  ABI  version: box; ntb| ntb| lbx nt| nt| lbx.  ABI  Ker‐
       nel    Newly  introduced  access  rights  _    _    _   1    5.13 LAND‐
       LOCK_ACCESS_FS_EXECUTE                    LANDLOCK_ACCESS_FS_WRITE_FILE
                 LANDLOCK_ACCESS_FS_READ_FILE                     LANDLOCK_AC‐
       CESS_FS_READ_DIR                          LANDLOCK_ACCESS_FS_REMOVE_DIR
                 LANDLOCK_ACCESS_FS_REMOVE_FILE                   LANDLOCK_AC‐
       CESS_FS_MAKE_CHAR           LANDLOCK_ACCESS_FS_MAKE_DIR           LAND‐
       LOCK_ACCESS_FS_MAKE_REG                    LANDLOCK_ACCESS_FS_MAKE_SOCK
                 LANDLOCK_ACCESS_FS_MAKE_FIFO                     LANDLOCK_AC‐
       CESS_FS_MAKE_BLOCK             LANDLOCK_ACCESS_FS_MAKE_SYM  _    _    _
       2    5.19 LANDLOCK_ACCESS_FS_REFER  _    _    _  3    6.2  LANDLOCK_AC‐
       CESS_FS_TRUNCATE   _    _    _   4    6.7  LANDLOCK_ACCESS_NET_BIND_TCP
              LANDLOCK_ACCESS_NET_CONNECT_TCP   _    _    _    5    6.10 LAND‐
       LOCK_ACCESS_FS_IOCTL_DEV    _    _    _    6    6.12 LANDLOCK_SCOPE_AB‐
       STRACT_UNIX_SOCKET           LANDLOCK_SCOPE_SIGNAL

       Users should use the Landlock ABI version rather than the  kernel  ver‐
       sion  to  determine  which features are available.  The mainline kernel
       versions listed here are only included for orientation.   Kernels  from
       other  sources  may contain backported features, and their version num‐
       bers may not match.

       To query the running kernel’s Landlock ABI version, programs  may  pass
       the LLAANNDDLLOOCCKK__CCRREEAATTEE__RRUULLEESSEETT__VVEERRSSIIOONN flag to llaannddlloocckk__ccrreeaattee__rruulleesseett(2).

       When building fallback mechanisms for compatibility with older kernels,
       users are advised to consider the special semantics of the LLAANNDDLLOOCCKK__AACC‐‐
       CCEESSSS__FFSS__RREEFFEERR  access right: In ABI v1, linking and moving of files be‐
       tween different directories is always forbidden, so programs relying on
       such operations are only compatible with Landlock ABI v2 and higher.

NNOOTTEESS
       Landlock is enabled by CCOONNFFIIGG__SSEECCUURRIITTYY__LLAANNDDLLOOCCKK.  The _l_s_m_=_l_s_m_1_,_._._._,_l_s_m_N
       command line parameter controls the sequence of the  initialization  of
       Linux  Security Modules.  It must contain the string _l_a_n_d_l_o_c_k to enable
       Landlock.  If the command line parameter is not specified, the initial‐
       ization falls back to the value of  the  deprecated  _s_e_c_u_r_i_t_y_=  command
       line  parameter  and  further to the value of CCOONNFFIIGG__LLSSMM.  We can check
       that Landlock is enabled by looking for _l_a_n_d_l_o_c_k_: _U_p _a_n_d  _r_u_n_n_i_n_g_.   in
       kernel logs.

CCAAVVEEAATTSS
       It  is currently not possible to restrict some file‐related actions ac‐
       cessible  through  these  system  call  families:  cchhddiirr(2),   ssttaatt(2),
       fflloocckk(2),  cchhmmoodd(2),  cchhoowwnn(2),  sseettxxaattttrr(2),  uuttiimmee(2),  ffccnnttll(2), aacc‐‐
       cceessss(2).  Future Landlock evolutions will enable to restrict them.

EEXXAAMMPPLLEESS
       We first need to create the ruleset that will contain our rules.

       For this example, the ruleset will contain rules that only  allow  read
       actions,  but  write actions will be denied.  The ruleset then needs to
       handle both of these kinds of actions.  See the DDEESSCCRRIIPPTTIIOONN section for
       the description of filesystem actions.

           struct landlock_ruleset_attr attr = {0};
           int ruleset_fd;

           attr.handled_access_fs =
                   LANDLOCK_ACCESS_FS_EXECUTE |
                   LANDLOCK_ACCESS_FS_WRITE_FILE |
                   LANDLOCK_ACCESS_FS_READ_FILE |
                   LANDLOCK_ACCESS_FS_READ_DIR |
                   LANDLOCK_ACCESS_FS_REMOVE_DIR |
                   LANDLOCK_ACCESS_FS_REMOVE_FILE |
                   LANDLOCK_ACCESS_FS_MAKE_CHAR |
                   LANDLOCK_ACCESS_FS_MAKE_DIR |
                   LANDLOCK_ACCESS_FS_MAKE_REG |
                   LANDLOCK_ACCESS_FS_MAKE_SOCK |
                   LANDLOCK_ACCESS_FS_MAKE_FIFO |
                   LANDLOCK_ACCESS_FS_MAKE_BLOCK |
                   LANDLOCK_ACCESS_FS_MAKE_SYM |
                   LANDLOCK_ACCESS_FS_REFER |
                   LANDLOCK_ACCESS_FS_TRUNCATE |
                   LANDLOCK_ACCESS_FS_IOCTL_DEV;

       To be compatible with older Linux versions,  we  detect  the  available
       Landlock  ABI  version,  and  only  use  the available subset of access
       rights:

           /*
            * Table of available file system access rights by ABI version,
            * numbers hardcoded to keep the example short.
            */
           __u64 landlock_fs_access_rights[] = {
               (LANDLOCK_ACCESS_FS_MAKE_SYM  << 1) - 1,  /* v1                  */
               (LANDLOCK_ACCESS_FS_REFER     << 1) - 1,  /* v2: add "refer"     */
               (LANDLOCK_ACCESS_FS_TRUNCATE  << 1) - 1,  /* v3: add "truncate"  */
               (LANDLOCK_ACCESS_FS_TRUNCATE  << 1) - 1,  /* v4: TCP support     */
               (LANDLOCK_ACCESS_FS_IOCTL_DEV << 1) - 1,  /* v5: add "ioctl_dev" */
           };

           int abi = landlock_create_ruleset(NULL, 0,
                                             LANDLOCK_CREATE_RULESET_VERSION);
           if (abi == -1) {
               /*
                * Kernel too old, not compiled with Landlock,
                * or Landlock was not enabled at boot time.
                */
               perror("Unable to use Landlock");
               return;  /* Graceful fallback: Do nothing.  */
           }
           abi = MIN(abi, 3);

           /* Only use the available rights in the ruleset.  */
           attr.handled_access_fs &= landlock_fs_access_rights[abi - 1];

       The available access rights for each ABI version are listed in the VVEERR‐‐
       SSIIOONNSS section.

       If our program needed to create hard links or rename files between dif‐
       ferent directories (LLAANNDDLLOOCCKK__AACCCCEESSSS__FFSS__RREEFFEERR),  we  would  require  the
       following change to the backwards compatibility logic: Directory repar‐
       enting  is  not possible in a process restricted with Landlock ABI ver‐
       sion 1.  Therefore, if the program needed to do file  reparenting,  and
       if only Landlock ABI version 1 was available, we could not restrict the
       process.

       Now  that the ruleset attributes are determined, we create the Landlock
       ruleset and acquire a file descriptor as a handle to  it,  using  llaanndd‐‐
       lloocckk__ccrreeaattee__rruulleesseett(2):

           ruleset_fd = landlock_create_ruleset(&attr, sizeof(attr), 0);
           if (ruleset_fd == -1) {
               perror("Failed to create a ruleset");
               exit(EXIT_FAILURE);
           }

       We can now add a new rule to the ruleset through the ruleset’s file de‐
       scriptor.   The  requested access rights must be a subset of the access
       rights which were specified in _a_t_t_r_._h_a_n_d_l_e_d___a_c_c_e_s_s___f_s at  ruleset  cre‐
       ation time.

       In  this  example,  the rule will only allow reading the file hierarchy
       _/_u_s_r.  Without another rule, write actions would then be denied by  the
       ruleset.   To  add _/_u_s_r to the ruleset, we open it with the _O___P_A_T_H flag
       and fill the _s_t_r_u_c_t _l_a_n_d_l_o_c_k___p_a_t_h___b_e_n_e_a_t_h___a_t_t_r with this file  descrip‐
       tor.

           struct landlock_path_beneath_attr path_beneath = {0};
           int err;

           path_beneath.allowed_access =
                   LANDLOCK_ACCESS_FS_EXECUTE |
                   LANDLOCK_ACCESS_FS_READ_FILE |
                   LANDLOCK_ACCESS_FS_READ_DIR;

           path_beneath.parent_fd = open("/usr", O_PATH | O_CLOEXEC);
           if (path_beneath.parent_fd == -1) {
               perror("Failed to open file");
               close(ruleset_fd);
               exit(EXIT_FAILURE);
           }
           err = landlock_add_rule(ruleset_fd, LANDLOCK_RULE_PATH_BENEATH,
                                   &path_beneath, 0);
           close(path_beneath.parent_fd);
           if (err) {
               perror("Failed to update ruleset");
               close(ruleset_fd);
               exit(EXIT_FAILURE);
           }

       We  now have a ruleset with one rule allowing read access to _/_u_s_r while
       denying all other handled accesses for the filesystem.  The  next  step
       is  to  restrict the current thread from gaining more privileges (e.g.,
       thanks to a set‐user‐ID binary).

           if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0)) {
               perror("Failed to restrict privileges");
               close(ruleset_fd);
               exit(EXIT_FAILURE);
           }

       The current thread is now ready to sandbox itself with the ruleset.

           if (landlock_restrict_self(ruleset_fd, 0)) {
               perror("Failed to enforce ruleset");
               close(ruleset_fd);
               exit(EXIT_FAILURE);
           }
           close(ruleset_fd);

       If the llaannddlloocckk__rreessttrriicctt__sseellff(2)  system  call  succeeds,  the  current
       thread  is  now  restricted and this policy will be enforced on all its
       subsequently created children as well.  Once a  thread  is  landlocked,
       there  is  no  way  to remove its security policy; only adding more re‐
       strictions is allowed.  These threads are now in a new Landlock domain,
       merge of their parent one (if any) with the new ruleset.

       Full working code  can  be  found  in  ⟨https://git.kernel.org/pub/scm/
       linux/kernel/git/stable/linux.git/tree/samples/landlock/sandboxer.c⟩

SSEEEE AALLSSOO
       llaannddlloocckk__ccrreeaattee__rruulleesseett(2),      llaannddlloocckk__aadddd__rruullee(2),     llaannddlloocckk__rree‐‐
       ssttrriicctt__sseellff(2)

       ⟨https://landlock.io/⟩

Linux man‐pages (unreleased)      2025‐09‐21                       _L_a_n_d_l_o_c_k(7)
