The deny authentication service module for PAM provides function‐
ality for all the PAM categories: authentication, account manage‐
ment,  session  management  and password management.  In terms of
the parameter, these are the and features.  The Deny module  will
universally  deny  all  requests.   It is primarily of use during
testing, and to unwanted functionality.   The  following  options
may  be  passed  to  the  module: debugging information at level.
suppress warning messages to the user.   These  messages  include
reasons  why the user’s authentication attempt was declined.  for
password management feature), return in  the  preliminary  phase.
This allows the module to be used (with the flag) at the end of a
chain  of modules with this service (where the entire chain is in
fact run twice).
