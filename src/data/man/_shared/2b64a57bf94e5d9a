Extended  attributes  extend the basic attributes associated with
files and directories in the file system.   They  are  stored  as
name:data  pairs  associated with file system objects (files, di‐
rectories, symlinks, etc).  deletes the extended attribute  asso‐
ciated  with  An extended attribute’s is a simple NULL‐terminated
UTF‐8 string.  is a bit mask specifying various options:  do  not
follow  symbolic links.  Normally, acts on the target of if it is
a symbolic link.  With this option, will act on the link  itself.
do not follow any symbolic links encountered during pathname res‐
olution.  An error is returned if a symlink is encountered before
the last component of path.  If does not reside in the  hierarchy
beneath  the  starting directory, an error is returned.  and will
remove HFS Plus Compression extended attribute (if  present)  for
the file referred to by or is identical to except that it removes
an  extended  attribute  from an open file referenced by file de‐
scriptor On success, 0 is returned.  On failure, ‐1  is  returned
and  the  global  variable  is set as follows.  The specified ex‐
tended attribute does not exist.  The file system does  not  sup‐
port  extended  attributes or has the feature disabled.  The file
system is mounted read‐only.  This type of object does  not  sup‐
port  extended  attributes.   or is invalid.  must be valid UTF‐8
must make sense.  XATTR_NOFOLLOW, XATTR_NOFOLLOW_ANY or XATTR_RE‐
SOLVE_BENEATH option was passed in A component of the  prefix  is
not  a  directory.   exceeded  UTF‐8 bytes, or a component of ex‐
ceeded characters, or the  entire  exceeded  characters.   Search
permission  is denied for a component or permission to remove the
attribute is denied.  Too many symbolic links were encountered in
If XATTR_NOFOLLOW_ANY option was passed and a symbolic  link  was
encountered  in  translating  the pathname.  If XATTR_RESOLVE_BE‐
NEATH was passed and does not reside in the  directory  hierarchy
beneath the starting directory.  or points to an invalid address.
An  I/O  error occurred while reading from or writing to the file
system.  and first appeared in Mac OS X 10.4.
