writes  random inode generation numbers for all the inodes on de‐
vice These random numbers make  the  NFS  filehandles  less  pre‐
dictable, increasing security of exported filesystems.  should be
run on a clean and unmounted filesystem.  The options are as fol‐
lows: Indicates that is a file system image, rather than a device
name.   will be accessed without requiring that it is a raw char‐
acter device and without attempting to read a  disklabel.   Print
the current inode generation numbers; the filesystem is not modi‐
fied.   Exclusive‐or  the  given  constant with the random number
used in the generation process.  exits zero on success,  non‐zero
on  failure.   If  receives a signal, statistics on the amount of
work completed and estimated completion time (in minutes:seconds)
will be written to the standard error output.  A utility appeared
in
