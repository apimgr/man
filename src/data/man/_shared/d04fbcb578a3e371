VRELE(9)                 BSD Kernel Developer's Manual                VRELE(9)

NNAAMMEE
     vvppuutt, vvrreellee, vvuunnrreeff — decrement the use count for a vnode

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<ssyyss//ppaarraamm..hh>>
     ##iinncclluuddee <<ssyyss//vvnnooddee..hh>>

     _v_o_i_d
     vvppuutt(_s_t_r_u_c_t _v_n_o_d_e _*_v_p);

     _v_o_i_d
     vvrreellee(_s_t_r_u_c_t _v_n_o_d_e _*_v_p);

     _v_o_i_d
     vvuunnrreeff(_s_t_r_u_c_t _v_n_o_d_e _*_v_p);

DDEESSCCRRIIPPTTIIOONN
     Decrement the _v___u_s_e_c_o_u_n_t field of a vnode.

     _v_p  the vnode to decrement

     The vvrreellee() function takes an unlocked vnode and returns with the vnode
     unlocked.

     The vvppuutt() function should be given a locked vnode as argument, the vnode
     is unlocked after the function returned.  The vvppuutt() is operationally
     equivalent to calling VOP_UNLOCK(9) followed by vvrreellee(), with less over‐
     head.

     The vvuunnrreeff() function takes a locked vnode as argument, and returns with
     the vnode locked.

     Any code in the system which signified its use of a vnode by usecount
     should call one of the listed function to decrement use counter.  If the
     _v___u_s_e_c_o_u_n_t field of the non-doomed vnode reaches zero, then it will be
     inactivated and placed on the free list.

     The vvrreellee() function may lock the vnode.  All three functions may sleep.

     The hold count for the vnode is always greater or equal to the usecount.
     Non-forced unmount fails when mount point owns a vnode that has non-zero
     usecount, see vflush(9).

SSEEEE AALLSSOO
     vget(9), vnode(9), vref(9), vrefcnt(9)

AAUUTTHHOORRSS
     This manual page was written by Doug Rabson and
     Konstantin Belousov.

BSD                            February 24, 2016                           BSD
