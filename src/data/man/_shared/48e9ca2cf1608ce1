COLUMN(1)                 BSD General Commands Manual                COLUMN(1)

NNAAMMEE
     ccoolluummnn — columnate lists

SSYYNNOOPPSSIISS
     ccoolluummnn [--ttxx] [--cc _c_o_l_u_m_n_s] [--ss _s_e_p] [_f_i_l_e _._._.]

DDEESSCCRRIIPPTTIIOONN
     The ccoolluummnn utility formats its input into multiple columns.  Each input
     line provides the text for one output cell.  Columns are filled before
     rows.

     The column width is determined by the longest input line rounded up to
     the nearest tabstop; tabstops are assumed to be at multiples of eight.
     Output uses tab characters to advance to the next column.  The number of
     columns is chosen to fill the terminal width.

     Input is taken from _f_i_l_e operands or, by default, from the standard in‐
     put.  Empty lines and lines containing only whitespace are ignored.

     The options are as follows:

     --cc _c_o_l_u_m_n_s
             Output is formatted for a display _c_o_l_u_m_n_s wide.

     --ss _s_e_p  Specify a set of characters to delimit columns for the --tt option.
             It defaults to space and tab.

     --tt      Table mode.  Each input line provides the text for one output
             row.  It is split into cells using the --ss option.  Leading,
             trailing, and multiple adjacent delimiters are ignored.  Each
             column is as wide as the widest cell in it.  Columns are sepa‐
             rated by two spaces.

     --xx      Fill rows before filling columns.

EENNVVIIRROONNMMEENNTT
     COLUMNS   If set to a positive integer, output is formatted to the given
               width in display columns.  Otherwise, ccoolluummnn defaults to the
               terminal width, or 80 columns if the output is not a terminal.

     LC_CTYPE  The character encoding locale(1).  It decides which byte se‐
               quences form characters, what their display width is, and which
               characters are whitespace.  If unset or set to "C", "POSIX", or
               an unsupported value, each byte except the tab is treated as a
               character of display width 1.

EEXXIITT SSTTAATTUUSS
     The ccoolluummnn utility exits 0 on success, and >0 if an error occurs.

SSEEEE AALLSSOO
     colrm(1), fmt(1), paste(1), rs(1)

HHIISSTTOORRYY
     The ccoolluummnn command appeared in 4.3BSD-Reno.

BSD                            October 24, 2016                            BSD
