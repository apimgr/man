is  designed  to  copy  magnetic tapes.  The only assumption made
about the tape is that there are two tape marks at the end.  with
only a source tape by default) specified will  print  information
about  the  sizes of records and tape files.  If a destination is
specified a copy will be made of the source tape.   The  blocking
on  the  destination  tape  will be identical to that used on the
source tape.  Copying a tape will yield the  same  output  as  if
just  printing  the sizes.  Options: Copy to and then verify that
the two tapes are identical.  Specify a maximum block size, Given
the two tapes, and verify that they are  identical.   Output  all
informational  messages  to  the  standard error.  This option is
useful when is The command appeared in
