CHKPRINTCAP(8)            BSD System Manager's Manual           CHKPRINTCAP(8)

NNAAMMEE
     cchhkkpprriinnttccaapp — check validity of entries in the print spooler database

SSYYNNOOPPSSIISS
     cchhkkpprriinnttccaapp [--dd] [--ff _p_r_i_n_t_c_a_p]

DDEESSCCRRIIPPTTIIOONN
     The cchhkkpprriinnttccaapp utility scans a printcap(5) database (named by the
     _p_r_i_n_t_c_a_p argument, or by default _/_e_t_c_/_p_r_i_n_t_c_a_p), looking for entries
     which are invalid in one way or another.  The following checks are cur‐
     rently implemented:

           1.   ‘tc=’ references were properly expanded

           2.   ‘tc=’ references did not form a loop

           3.   No two printers share the same spool directory (‘sd=’ capabil‐
                ity).

     The cchhkkpprriinnttccaapp utility exits with a status equal to the number of errors
     encountered before processing stopped.  (In some cases, processing can
     stop before the entire file is scanned.)

     If the --dd flag is given, cchhkkpprriinnttccaapp will attempt to create any missing
     spool directories, giving them ‘u=rwx,go=rx’ (0755) mode, group ‘daemon’,
     and the owner specified by the ‘du=’ capability in the database (default
     1, which corresponds to user ‘daemon’).

SSEEEE AALLSSOO
     lpr(1), printcap(5), lpd(8)

AAUUTTHHOORRSS
     The cchhkkpprriinnttccaapp utility was written by Garrett A. Wollman
     <_w_o_l_l_m_a_n_@_l_c_s_._m_i_t_._e_d_u>.

BBUUGGSS
     Not enough sanity-checking is done.  At a minimum, the ownership and mode
     of the spool directories should also be checked.  Other parameters whose
     value could cause lpd(8) to fail should be diagnosed.

BSD                            November 30, 1997                           BSD
