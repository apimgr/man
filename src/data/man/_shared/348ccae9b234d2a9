MIXER(3)                 BSD Library Functions Manual                 MIXER(3)

NNAAMMEE
     mmiixxeerr__ooppeenn, mmiixxeerr__cclloossee, mmiixxeerr__ggeett__ddeevv, mmiixxeerr__ggeett__ddeevv__bbyynnaammee,
     mmiixxeerr__aadddd__ccttll, mmiixxeerr__aadddd__ccttll__ss, mmiixxeerr__rreemmoovvee__ccttll, mmiixxeerr__ggeett__ccttll,
     mmiixxeerr__ggeett__ccttll__bbyynnaammee, mmiixxeerr__sseett__vvooll, mmiixxeerr__sseett__mmuuttee, mmiixxeerr__mmoodd__rreeccssrrcc,
     mmiixxeerr__ggeett__dduunniitt, mmiixxeerr__sseett__dduunniitt, mmiixxeerr__ggeett__mmooddee, mmiixxeerr__ggeett__nnmmiixxeerrss,
     mmiixxeerr__ggeett__ppaatthh, MMIIXX__IISSDDEEVV, MMIIXX__IISSMMUUTTEE, MMIIXX__IISSRREECC, MMIIXX__IISSRREECCSSRRCC,
     MMIIXX__VVOOLLNNOORRMM, MMIIXX__VVOOLLDDEENNOORRMM — interface to OSS mixers

LLIIBBRRAARRYY
     Mixer library (libmixer, -lmixer)

SSYYNNOOPPSSIISS
     ##iinncclluuddee <<mmiixxeerr..hh>>

     _s_t_r_u_c_t _m_i_x_e_r _*
     mmiixxeerr__ooppeenn(_c_o_n_s_t _c_h_a_r _*_n_a_m_e);

     _i_n_t
     mmiixxeerr__cclloossee(_s_t_r_u_c_t _m_i_x_e_r _*_m);

     _s_t_r_u_c_t _m_i_x___d_e_v _*
     mmiixxeerr__ggeett__ddeevv(_s_t_r_u_c_t _m_i_x_e_r _*_m, _i_n_t _d_e_v_n_o);

     _s_t_r_u_c_t _m_i_x___d_e_v _*
     mmiixxeerr__ggeett__ddeevv__bbyynnaammee(_s_t_r_u_c_t _m_i_x_e_r _*_m, _n_a_m_e);

     _i_n_t
     mmiixxeerr__aadddd__ccttll(_s_t_r_u_c_t _m_i_x___d_e_v _*_p_a_r_e_n_t, _i_n_t _i_d, _c_o_n_s_t _c_h_a_r _*_n_a_m_e,
         _i_n_t _(_*_m_o_d_)_(_s_t_r_u_c_t _m_i_x___d_e_v _*_d_, _v_o_i_d _*_p_),
         _i_n_t _(_*_p_r_i_n_t_)_(_s_t_r_u_c_t _m_i_x___d_e_v _*_d_, _v_o_i_d _*_p_));

     _i_n_t
     mmiixxeerr__aadddd__ccttll__ss(_m_i_x___c_t_l___t _*_c_t_l);

     _i_n_t
     mmiixxeerr__rreemmoovvee__ccttll(_m_i_x___c_t_l___t _*_c_t_l);

     _m_i_x___c_t_l___t _*
     mmiixxeerr__ggeett__ccttll(_s_t_r_u_c_t _m_i_x___d_e_v _*_d, _i_n_t _i_d);

     _m_i_x___c_t_l___t _*
     mmiixxeerr__ggeett__ccttll__bbyynnaammee(_s_t_r_u_c_t _m_i_x___d_e_v _*_d, _c_o_n_s_t _c_h_a_r _*_n_a_m_e);

     _i_n_t
     mmiixxeerr__sseett__vvooll(_s_t_r_u_c_t _m_i_x_e_r _*_m, _m_i_x___v_o_l_u_m_e___t _v_o_l);

     _i_n_t
     mmiixxeerr__sseett__mmuuttee(_s_t_r_u_c_t _m_i_x_e_r _*_m, _i_n_t _o_p_t);

     _i_n_t
     mmiixxeerr__mmoodd__rreeccssrrcc(_s_t_r_u_c_t _m_i_x_e_r _*_m, _i_n_t _o_p_t);

     _i_n_t
     mmiixxeerr__ggeett__dduunniitt(_v_o_i_d);

     _i_n_t
     mmiixxeerr__sseett__dduunniitt(_s_t_r_u_c_t _m_i_x_e_r _*_m, _i_n_t _u_n_i_t);

     _i_n_t
     mmiixxeerr__ggeett__mmooddee(_i_n_t _u_n_i_t);

     _i_n_t
     mmiixxeerr__ggeett__nnmmiixxeerrss(_v_o_i_d);

     _i_n_t
     mmiixxeerr__ggeett__ppaatthh(_c_h_a_r _* _b_u_f, _s_i_z_e___t _s_i_z_e, _i_n_t _u_n_i_t);

     _i_n_t
     MMIIXX__IISSDDEEVV(_s_t_r_u_c_t _m_i_x_e_r _*_m, _i_n_t _d_e_v_n_o);

     _i_n_t
     MMIIXX__IISSMMUUTTEE(_s_t_r_u_c_t _m_i_x_e_r _*_m, _i_n_t _d_e_v_n_o);

     _i_n_t
     MMIIXX__IISSRREECC(_s_t_r_u_c_t _m_i_x_e_r _*_m, _i_n_t _d_e_v_n_o);

     _i_n_t
     MMIIXX__IISSRREECCSSRRCC(_s_t_r_u_c_t _m_i_x_e_r _*_m, _i_n_t _d_e_v_n_o);

     _f_l_o_a_t
     MMIIXX__VVOOLLNNOORRMM(_i_n_t _v);

     _i_n_t
     MMIIXX__VVOOLLDDEENNOORRMM(_f_l_o_a_t _v);

DDEESSCCRRIIPPTTIIOONN
     The mmiixxeerr library allows userspace programs to access and manipulate OSS
     sound mixers in a simple way.

   MMiixxeerr
     A mixer is described by the following structure:

     struct mixer {
             TAILQ_HEAD(mix_devhead, mix_dev) devs;  /* device list */
             struct mix_dev *dev;                    /* selected device */
             oss_mixerinfo mi;                       /* mixer info */
             oss_card_info ci;                       /* audio card info */
             char name[NAME_MAX];                    /* mixer name (e.g /dev/mixer0) */
             int fd;                                 /* file descriptor */
             int unit;                               /* audio card unit */
             int ndev;                               /* number of devices */
             int devmask;                            /* supported devices */
     #define MIX_MUTE                0x01
     #define MIX_UNMUTE              0x02
     #define MIX_TOGGLEMUTE          0x04
             int mutemask;                           /* muted devices */
             int recmask;                            /* recording devices */
     #define MIX_ADDRECSRC           0x01
     #define MIX_REMOVERECSRC        0x02
     #define MIX_SETRECSRC           0x04
     #define MIX_TOGGLERECSRC        0x08
             int recsrc;                             /* recording sources */
     #define MIX_MODE_MIXER          0x01
     #define MIX_MODE_PLAY           0x02
     #define MIX_MODE_REC            0x04
             int mode;                               /* dev.pcm.X.mode sysctl */
             int f_default;                          /* default mixer flag */
     };

     The fields are follows:

     _d_e_v_s       A tail queue structure containing all supported mixer devices.

     _d_e_v        A pointer to the currently selected device.  The device is one
                of the elements in _d_e_v_s.

     _m_i         OSS information about the mixer.  Look at the definition of
                the _o_s_s___m_i_x_e_r_i_n_f_o structure in <_s_y_s_/_s_o_u_n_d_c_a_r_d_._h> to see its
                fields.

     _c_i         OSS audio card information.  This structure is also defined in
                <_s_y_s_/_s_o_u_n_d_c_a_r_d_._h>.

     _n_a_m_e       Path to the mixer (e.g /dev/mixer0).

     _f_d         File descriptor returned when the mixer is opened in
                mmiixxeerr__ooppeenn().

     _u_n_i_t       Audio card unit.  Since each mixer device maps to a pcmX de‐
                vice, _u_n_i_t is always equal to the number of that pcmX device.
                For example, if the audio device's number is 0 (i.e pcm0),
                then _u_n_i_t is 0 as well.  This number is useful when checking
                if the mixer's audio card is the default one.

     _n_d_e_v       Number of devices in _d_e_v_s.

     _d_e_v_m_a_s_k    Bit mask containing all supported devices for the mixer.  For
                example, if device 10 is supported, then the 10th bit in the
                mask will be set.  By default, mmiixxeerr__ooppeenn() stores only the
                supported devices in devs, so it is very unlikely this mask
                will be needed.

     _m_u_t_e_m_a_s_k   Bit mask containing all muted devices.  The logic is the same
                as with _d_e_v_m_a_s_k.

     _r_e_c_m_a_s_k    Bit mask containing all recording devices.  Again, same logic
                as with the other masks.

     _r_e_c_s_r_c     Bit mask containing all recording sources.  Yes, same logic
                again.

     _m_o_d_e       Bit mask containing the supported modes for this audio device.
                It holds the value of the _d_e_v_._p_c_m_._X_._m_o_d_e sysctl.

     _f___d_e_f_a_u_l_t  Flag which tells whether the mixer's audio card is the default
                one.

   MMiixxeerr ddeevviiccee
     Each mixer device stored in a mixer is described as follows:

     struct mix_dev {
             struct mixer *parent_mixer;             /* parent mixer */
             char name[NAME_MAX];                    /* device name (e.g "vol") */
             int devno;                              /* device number */
             struct mix_volume {
     #define MIX_VOLMIN              0.0f
     #define MIX_VOLMAX              1.0f
     #define MIX_VOLNORM(v)          ((v) / 100.0f)
     #define MIX_VOLDENORM(v)        ((int)((v) * 100.0f + 0.5f))
                     float left;                     /* left volume */
                     float right;                    /* right volume */
             } vol;
             int nctl;                               /* number of controls */
             TAILQ_HEAD(mix_ctlhead, mix_ctl) ctls;  /* control list */
             TAILQ_ENTRY(mix_dev) devs;
     };

     The fields are follows:

     _p_a_r_e_n_t___m_i_x_e_r  Pointer to the mixer the device is attached to.

     _n_a_m_e          Device name given by the OSS API.  Devices can have one of
                   the following names:

                   vol, bass, treble, synth, pcm, speaker, line, mic, cd, mix,
                   pcm2, rec, igain, ogain, line1, line2, line3, dig1, dig2,
                   dig3, phin, phout, video, radio, and monitor.

     _d_e_v_n_o         Device's index in the SOUND_MIXER_NRDEVICES macro defined
                   in <_s_y_s_/_s_o_u_n_d_c_a_r_d_._h>.  This number is used to check against
                   the masks defined in the _m_i_x_e_r structure.

     _l_e_f_t _r_i_g_h_t    Left and right-ear volumes.  Although the OSS API stores
                   volumes in integers from 0-100, we normalize them to 32-bit
                   floating point numbers.  However, the volumes can be denor‐
                   malized using the _M_I_X___V_O_L_D_E_N_O_R_M macro if needed.

     _n_c_t_l          Number of user-defined mixer controls associated with the
                   device.

     _c_t_l_s          A tail queue containing user-defined mixer controls.

   UUsseerr--ddeeffiinneedd mmiixxeerr ccoonnttrroollss
     Each mixer device can have user-defined controls.  The control structure
     is defined as follows:

     struct mix_ctl {
             struct mix_dev *parent_dev;             /* parent device */
             int id;                                 /* control id */
             char name[NAME_MAX];                    /* control name */
             int (*mod)(struct mix_dev *, void *);   /* modify control values */
             int (*print)(struct mix_dev *, void *); /* print control */
             TAILQ_ENTRY(mix_ctl) ctls;
     };

     The fields are follows:

     _p_a_r_e_n_t___d_e_v  Pointer to the device the control is attached to.

     _i_d          Control ID assigned by the caller.  Even though the library
                 will report it, care has to be taken to not give a control
                 the same ID in case the caller has to choose controls using
                 their ID.

     _n_a_m_e        Control name.  As with _i_d, the caller has to make sure the
                 same name is not used more than once.

     _m_o_d         Function pointer to a control modification function.  As in
                 mixer(8), each mixer control's values can be modified.  For
                 example, if we have a volume control, the _m_o_d function will
                 be responsible for handling volume changes.

     _p_r_i_n_t       Function pointer to a control print function.

   OOppeenniinngg aanndd cclloossiinngg tthhee mmiixxeerr
     The application must first call the mmiixxeerr__ooppeenn() function to obtain a
     handle to the device, which is used as an argument in most other func‐
     tions and macros.  The parameter _n_a_m_e specifies the path to the mixer.
     OSS mixers are stored under _/_d_e_v_/_m_i_x_e_r_N where _N is the number of the
     mixer device.  Each device maps to an actual _p_c_m audio card, so
     _/_d_e_v_/_m_i_x_e_r_0 is the mixer for _p_c_m_0, and so on.  If _n_a_m_e is _N_U_L_L or
     _/_d_e_v_/_m_i_x_e_r, mmiixxeerr__ooppeenn() opens the default mixer (hw.snd.default_unit).

     The mmiixxeerr__cclloossee() function frees resources and closes the mixer device.
     It is a good practice to always call it when the application is done us‐
     ing the mixer.

   MMaanniippuullaattiinngg tthhee mmiixxeerr
     The mmiixxeerr__ggeett__ddeevv() and mmiixxeerr__ggeett__ddeevv__bbyynnaammee() functions select a mixer
     device, either by its number or by its name respectively.  The mixer
     structure keeps a list of all the devices, but only one can be manipu‐
     lated at a time.  Each time a new device is to be manipulated, one of the
     two functions has to be called.

     The mmiixxeerr__sseett__vvooll() function changes the volume of the selected mixer de‐
     vice.  The _v_o_l parameter is a structure that stores the left and right
     volumes of a given device.  The allowed volume values are between
     MIX_VOLMIN (0.0) and MIX_VOLMAX (1.0).

     The mmiixxeerr__sseett__mmuuttee() function modifies the mute of a selected device.
     The _o_p_t parameter has to be one of the following options:

           MIX_MUTE        Mute the device.

           MIX_UNMUTE      Unmute the device.

           MIX_TOGGLEMUTE  Toggle the device's mute (e.g mute if unmuted and
                           unmute if muted).

     The mmiixxeerr__mmoodd__rreeccssrrcc() function modifies a recording device.  The se‐
     lected device has to be a recording device, otherwise the function will
     fail.  The _o_p_t parameter has to be one of the following options:

           MIX_ADDRECSRC     Add device to the recording sources.

           MIX_REMOVERECSRC  Remove device from the recording sources.

           MIX_SETRECSRC     Set device as the only recording source.

           MIX_TOGGLERECSRC  Toggle device from the recording sources.

     The mmiixxeerr__ggeett__dduunniitt() and mmiixxeerr__sseett__dduunniitt() functions get and set the de‐
     fault audio card in the system.  Although this is not really a mixer fea‐
     ture, it is useful to have instead of having to use the sysctl(3) con‐
     trols.

     The mmiixxeerr__ggeett__mmooddee() function returns the operating mode of the audio de‐
     vice the mixer belongs to.  The following values can be OR'ed in case
     more than one mode is supported:

           MIX_MODE_MIXER  The audio device has a mixer.

           MIX_MODE_PLAY   The audio device supports playback.

           MIX_MODE_REC    The audio device supports recording.

     The mmiixxeerr__ggeett__nnmmiixxeerrss() function returns the maximum mixer unit number.
     Although this might sound as incorrect behavior, given that one would ex‐
     pect "nmixers" to refer to the total number of active mixers, it is more
     intuitive for applications that want to loop through all mixer devices
     (see the _E_X_A_M_P_L_E_S section).

     The mmiixxeerr__ggeett__ppaatthh() function writes the path of the mixer device speci‐
     fied in the _u_n_i_t argument to the buffer specified in _b_u_f.  _u_n_i_t can be
     either -1, in which case mmiixxeerr__ggeett__ppaatthh() will fetch the path of the de‐
     fault mixer, or between 0 and the maximum mixer unit.

     The MMIIXX__IISSDDEEVV() macro checks if a device is actually a valid device for a
     given mixer.  It is very unlikely that this macro will ever be needed
     since the library stores only valid devices by default.

     The MMIIXX__IISSMMUUTTEE() macro checks if a device is muted.

     The MMIIXX__IISSRREECC() macro checks if a device is a recording device.

     The MMIIXX__IISSRREECCSSRRCC() macro checks if a device is a recording source.

     The MMIIXX__VVOOLLNNOORRMM() macro normalizes a value to 32-bit floating point num‐
     ber.  It is used to normalize the volumes read from the OSS API.

     The MMIIXX__VVOOLLDDEENNOORRMM() macro denormalizes the left and right volumes stores
     in the _m_i_x___d_e_v structure.

   DDeeffiinniinngg aanndd uussiinngg mmiixxeerr ccoonnttrroollss
     The mmiixx__aadddd__ccttll() function creates a control and attaches it to the de‐
     vice specified in the _p_a_r_e_n_t argument.

     The mmiixx__aadddd__ccttll__ss() function does the same thing as with mmiixx__aadddd__ccttll()
     but the caller passes a _m_i_x___c_t_l___t _* structure instead of each field as a
     separate argument.

     The mmiixxeerr__rreemmoovvee__ccttll() functions removes a control from the device its
     attached to.

     The mmiixxeerr__ggeett__ccttll() function searches for a control in the device speci‐
     fied in the _d argument and returns a pointer to it.  The search is done
     using the control's ID.

     The mmiixxeerr__ggeett__ccttll__bbyynnaammee() function is the same as with mmiixxeerr__ggeett__ccttll()
     but the search is done using the control's name.

RREETTUURRNN VVAALLUUEESS
     The mmiixxeerr__ooppeenn() function returns the newly created handle on success and
     NULL on failure.

     The mmiixxeerr__cclloossee(), mmiixxeerr__sseett__vvooll(), mmiixxeerr__sseett__mmuuttee(), mmiixxeerr__mmoodd__rreeccssrrcc(),
     mmiixxeerr__ggeett__dduunnuutt(), mmiixxeerr__sseett__dduunniitt(), mmiixxeerr__ggeett__nnmmiixxeerrss(), and
     mmiixxeerr__ggeett__ppaatthh() functions return 0 or positive values on success and -1
     on failure.

     The mmiixxeerr__ggeett__ddeevv() and mmiixxeerr__ggeett__ddeevv__bbyynnaammee() functions return the se‐
     lected device on success and NULL on failure.

     All functions set the value of _e_r_r_n_o on failure.

EEXXAAMMPPLLEESS
   CChhaannggee tthhee vvoolluummee ooff aa ddeevviiccee
     struct mixer *m;
     mix_volume_t vol;
     char *mix_name, *dev_name;

     mix_name = ...;
     if ((m = mixer_open(mix_name)) == NULL)
             err(1, "mixer_open: %s", mix_name);

     dev_name = ...;
     if ((m->dev = mixer_get_dev_byname(m, dev_name)) < 0)
             err(1, "unknown device: %s", dev_name);

     vol.left = ...;
     vol.right = ....;
     if (mixer_set_vol(m, vol) < 0)
             warn("cannot change volume");

     (void)mixer_close(m);

   MMuuttee aallll uunnmmuutteedd ddeevviicceess
     struct mixer *m;
     struct mix_dev *dp;

     if ((m = mixer_open(NULL)) == NULL)     /* Open the default mixer. */
             err(1, "mixer_open");
     TAILQ_FOREACH(dp, &m->devs, devs) {
             m->dev = dp;                    /* Select device. */
             if (M_ISMUTE(m, dp->devno))
                     continue;
             if (mixer_set_mute(m, MIX_MUTE) < 0)
                     warn("cannot mute device: %s", dp->name);
     }

     (void)mixer_close(m);

   PPrriinntt aallll rreeccoorrddiinngg ssoouurrcceess'' nnaammeess aanndd vvoolluummeess
     struct mixer *m;
     struct mix_dev *dp;

     char *mix_name, *dev_name;

     mix_name = ...;
     if ((m = mixer_open(mix_name)) == NULL)
             err(1, "mixer_open: %s", mix_name);

     TAILQ_FOREACH(dp, &m->devs, devs) {
             if (M_ISRECSRC(m, dp->devno))
                     printf("%s\t%.2f:%.2f\n",
                         dp->name, dp->vol.left, dp->vol.right);
     }

     (void)mixer_close(m);

   LLoooopp tthhrroouugghh aallll mmiixxeerr ddeevviicceess iinn tthhee ssyysstteemm
     struct mixer *m;
     char buf[NAME_MAX];
     int n;

     if ((n = mixer_get_nmixers()) < 0)
             errx(1, "no mixers present in the system");
     for (i = 0; i < n; i++) {
             (void)mixer_get_path(buf, sizeof(buf), i);
             if ((m = mixer_open(buf)) == NULL)
                     continue;
             ...
             (void)mixer_close(m);
     }

SSEEEE AALLSSOO
     queue(3), sysctl(3), sound(4), mixer(8) and errno(2)

AAUUTTHHOORRSS
     Christos Margiolis <_c_h_r_i_s_t_o_s_@_F_r_e_e_B_S_D_._o_r_g>

BSD                             August 4, 2024                             BSD
