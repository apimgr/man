TRIM(8)                   BSD System Manager's Manual                  TRIM(8)

NNAAMMEE
     ttrriimm — erase device blocks that have no needed contents

SSYYNNOOPPSSIISS
     ttrriimm [--NNffqqvv] [--[[lloo]] _o_f_f_s_e_t[KK|kk|MM|mm|GG|gg|TT|tt]] [--rr _r_f_i_l_e] _d_e_v_i_c_e _._._.

DDEESSCCRRIIPPTTIIOONN
     The ttrriimm utility erases specified region of the device.  It is mostly
     relevant for storage that implement trim (like flash based, or thinly
     provisioned storage).  AAllll eerraasseedd ddaattaa iiss lloosstt..

     The following options are available:

     --NN      Do not actually erase anything but show what it would do (dry
             run).  Implies --vv.  This is the default.  Overrides --ff.

     --ff      Perform the operation.  Overrides --NN.

     --ll _o_f_f_s_e_t[KK|kk|MM|mm|GG|gg|TT|tt]

     --oo _o_f_f_s_e_t[KK|kk|MM|mm|GG|gg|TT|tt]
             Specify the length --ll of the region to trim or its offset --oo from
             the beginning of the device.  TThhee wwhhoollee ddeevviiccee iiss eerraasseedd bbyy
             ddeeffaauulltt unless one or both of these options are presented.

             The argument may be suffixed with one of KK, MM, GG or TT (either up‐
             per or lower case) to indicate a multiple of Kilobytes,
             Megabytes, Gigabytes or Terabytes respectively.

     --qq      Do not output anything except of possible error messages (quiet
             mode).  Overrides --vv.

     --rr _r_f_i_l_e
             Uses the length of given _r_f_i_l_e as length of the region to erase.
             TThhee wwhhoollee ddeevviiccee iiss eerraasseedd bbyy ddeeffaauulltt..

     --vv      Show offset and length of actual region being erased, in bytes.

     Later options override previous ones.

     Note that actual success of the operation depends of underlying device
     driver such as ada(4), da(4) and others.  Refer to corresponding manual
     pages for detail on possible caveats in low level support for ATA TRIM or
     SCSI UNMAP commands.

EEXXIITT SSTTAATTUUSS
     The ttrriimm utility exits 0 on success, and >0 if an error occurs.  If the
     final erase operation fails for an argument, the ttrriimm utility returns
     exit code 1.  It can also return one of the exit codes defined in
     sysexits(3), as follows:

     EX_USAGE        The specified offset or length of the region is incor‐
                     rect.

     EX_OSERR        There is no enough memory to proceed.

     EX_NOINPUT      The specified _r_f_i_l_e cannot be opened (perhaps, it does
                     not exist).

     EX_IOERR        The specified _r_f_i_l_e cannot be examined for its size due
                     to some system input/output error.

     EX_DATAERR      The specified _r_f_i_l_e is not regular file, directory nor
                     special device, so its size cannot be examined.

     EX_UNAVAILABLE  The specified _r_f_i_l_e is special device file not supporting
                     DIOCGMEDIASIZE ioctl(2) (probably not a disk), so its
                     size cannot be examined.

SSEEEE AALLSSOO
     ioctl(2), sysexits(3), ada(4), da(4), nda(4)

HHIISSTTOORRYY
     The ttrriimm utility first appeared in FreeBSD 12.1.

AAUUTTHHOORRSS
     The ttrriimm utility was written by Eugene Grosbein <_e_u_g_e_n_@_F_r_e_e_B_S_D_._o_r_g>.

BSD                            January 18, 2019                            BSD
